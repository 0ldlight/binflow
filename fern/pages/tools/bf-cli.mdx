# bf CLI

`bf` 是 BinFlow 的命令行客户端：建仓、传制品、建用户、发 Token——CI 脚本与自动化场景的趁手面。配置走 `~/.bf/config.yaml` 多 profile，**密钥永不落盘**（配置里只存环境变量名）。

## 安装

随发布产物分发，或源码构建：

```bash
go build -o bf ./cmd/bf
bf --version    # bf <version> (<revision>)
```

## 配置：profile 与凭据

`~/.bf/config.yaml`（路径可用 `BF_CONFIG` 覆盖；文件缺失不是错误——零配置直接用默认地址）：

```yaml
profiles:
  default:
    base_url: http://localhost:8080
    username: admin
    password_env: BINFLOW_PASSWORD     # 只存变量名，口令在环境里
  prod:
    base_url: https://binflow.example.com
    token_env: PROD_BF_TOKEN           # Bearer 优先于 Basic
default_profile: prod                  # 可选：--profile 缺省时的选择
```

覆盖优先级（高 → 低）：

- **地址**：`--server` > `BF_BASE_URL` / `BINFLOW_SERVER_URL` > profile `base_url` > 内置默认。
- **凭据**：`BF_TOKEN` > profile `token_env` 指到的值；`BF_USERNAME`+`BF_PASSWORD` > profile `username`+`password_env` 指到的值。
- 配了口令但没配用户名直接报错（不给静默匿名）。

## 四个子命令

全局旗标须放在命令词**之前**（`bf --profile prod repo create ...`）。

### repo create — 建仓

```bash
bf repo create libs-generic --type local --package-type generic
# Repository 'libs-generic' created.
```

| 旗标 | 缺省 | 说明 |
|---|---|---|
| `--type` | `local` | `local` / `remote` / `virtual` |
| `--package-type` | `generic` | `generic` / `docker` / `maven` / `npm` / `pypi` |
| `--description` | 空 | 描述 |

remote/virtual 的成员、上游地址等字段本命令不带——用 REST（`PUT /api/repositories/{key}`）配全。

### artifact upload — 上传制品

```bash
bf artifact upload f.txt --repo libs-generic --path doc/hello.txt
# sha256: e5ae3757…（本地算，脚本可断言）
# uri: http://localhost:8080/binflow/libs-generic/doc/hello.txt
```

旗标：`--repo`（必填）、`--path`（必填）、`--content-type`（缺省服务端按扩展名推断）。

### user create — 建用户

```bash
bf user create ci-user --password-env CI_PW --email ci@example.com
# User 'ci-user' created.
```

| 旗标 | 说明 |
|---|---|
| `--password` / `--password-env` | 直接给口令（会进 shell 历史，优先 `--password-env`）/ 环境变量 |
| `--email` | 必填（服务端拒空） |
| `--admin` | 授管理员 |

用户名必须小写（服务端拒混合大小写）。

### token create — 发 Token

```bash
bf token create --username deployer --expires-in 3600
# <token 值——首行，仅此一次显示>
# token_id: 1 / expires_in: 3600
```

旗标：`--username`（缺省取 profile 用户名）、`--expires-in`（秒；0 = 服务端默认 TTL）、`--scope`、`--description`。**Token 值只在创建时返回一次**，服务端无法找回。

## 完整 roundtrip

```bash
export BF_BASE_URL=http://localhost:8080 BF_USERNAME=admin BF_PASSWORD=<pw>
bf repo create t-repo --type local --package-type generic
bf artifact upload app.bin --repo t-repo --path v1/app.bin
curl -s $BF_BASE_URL/binflow/t-repo/v1/app.bin | shasum -a 256    # 与上传打印一致
```

## 约定与边界

- 成功结果打 stdout（机器可读优先）；失败打 stderr、退出码 1（服务端 `errors[].message` 带出来）。
- 请求超时 30s/请求；5xx 与网络错误自动重试（默认 5 次指数退避）。
- **只有 create 类动作**：删仓/删制品/改密请用 REST 或控制台；`bf config set` 未实现——profile 手工编辑 YAML。

## 下一步

- [API 参考](/api-reference) · [从 Artifactory 迁移](/tools/migrate-artifactory) · [接入指南](/integrations/generic)
