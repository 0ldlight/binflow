// Package httpapi hosts the /binflow router, the fixed middleware chain, the
// unified error envelope, health endpoints and graceful shutdown
// (architecture section 7; implemented by T-14 with compatible handlers in T-15).
package httpapi
