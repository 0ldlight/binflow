// Package config loads binflow.yaml, applies BINFLOW_-prefixed environment
// overrides, validates fail-fast, and exposes an immutable Config tree
// (architecture section 8; implemented by T-8).
package config
