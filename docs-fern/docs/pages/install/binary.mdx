# 单二进制安装

BinFlow 是零 CGo 的静态链接 Go 二进制——不依赖任何运行时与系统包管理器，下载、解压、运行即可。

## 平台支持

| 操作系统 | 架构 | 产物格式 |
|---|---|---|
| Linux | amd64, arm64 | `.tar.gz` |
| macOS (darwin) | amd64, arm64 | `.tar.gz` |
| Windows | amd64, arm64 | `.zip` |

> Windows 腿当前为「文档验证」级别——路径分隔符与服务模式可能与 Linux/macOS 行为不同；以下命令以 macOS/Linux 为基准。

## 安装步骤

### 1. 下载

从 GitHub Releases 下载对应平台的归档与校验文件：

```bash
export VER=v1.0.0
export OS=$(uname -s | tr '[:upper:]' '[:lower:]')
export ARCH=$(uname -m)
case "$ARCH" in
  x86_64|amd64) ARCH="amd64" ;;
  aarch64|arm64) ARCH="arm64" ;;
esac

curl -fsSLO "https://github.com/example/binflow/releases/download/${VER}/binflow_${VER}_${OS}_${ARCH}.tar.gz"
curl -fsSLO "https://github.com/example/binflow/releases/download/${VER}/binflow_${VER}_checksums.txt"
```

### 2. 校验 sha256

```bash
grep "binflow_${VER}_${OS}_${ARCH}.tar.gz" binflow_${VER}_checksums.txt | shasum -a 256 -c
# binflow_v1.0.0_darwin_arm64.tar.gz: OK
```

校验失败则立即停止，不要解压。

### 3. 解压

```bash
tar xzf "binflow_${VER}_${OS}_${ARCH}.tar.gz"
# 生成目录 binflow_<VER>_<OS>_<ARCH>/binflow-server（约 40MB）
```

### 4. 启动服务

```bash
cd "binflow_${VER}_${OS}_${ARCH}"
./binflow-server serve
```

默认行为：监听 `:8080`（所有接口）；数据目录 `./data/`（自动创建）；管理员密码默认 `password`（启动时打 WARN 提醒修改）；日志输出到 stderr。

自定义配置：

```bash
./binflow-server serve -c /path/to/binflow.yaml

# 环境变量覆盖（全大写 + 点改下划线 + 双下划线分隔层级）
BINFLOW_ADMIN_PASSWORD=my-secret \
BINFLOW_STORAGE__DATA_DIR=/data/binflow \
./binflow-server serve
```

### 5. 验证

```bash
curl -s http://127.0.0.1:8080/readyz
# ok（HTTP 200；免认证，会检查元数据存储与存储目录可写）

./binflow-server --version
# binflow-server v1.0.0 (abc1234)
```

## 后台运行

```bash
nohup ./binflow-server serve > binflow.log 2>&1 &
```

Linux 裸机生产推荐 systemd（见 [systemd 服务](/install/systemd)）；Windows 从 PowerShell 启动 `.\binflow-server.exe serve`。

## 目录约定

| 路径 | 说明 |
|---|---|
| `./binflow.yaml` | 默认配置文件（`-c` 未指定时查找：当前目录 → `$BINFLOW_HOME/`） |
| `./data/` | 默认数据目录（blobs + SQLite + sessions） |
| `$BINFLOW_HOME/` | 配置与数据的备用根目录（可选环境变量） |

## 卸载

```bash
pkill binflow-server
rm -rf binflow_${VER}_${OS}_${ARCH}   # 二进制
rm -rf ./data                          # 数据（谨慎）
```

## 下一步

- [Docker 运行](/install/docker) · [systemd 服务](/install/systemd) · [Generic 接入](/integrations/generic)
