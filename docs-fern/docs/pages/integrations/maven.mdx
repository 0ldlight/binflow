# Maven 接入

把 BinFlow 当作 Maven 2 仓库：`mvn deploy` 发内部构件、`settings.xml`/pom 指过来解析依赖——layout、`maven-metadata.xml`、checksum 语义与 Nexus/Artifactory 一致，迁移时 URL 前缀从 `/artifactory` 改成 `/binflow`。

## URL 形态

Maven 客户端直接走内容路径（无 API 前缀），layout 即 Maven 2 标准：

```
$BASE/binflow/<repoKey>/<groupId 点转斜杠>/<artifactId>/<version>/<artifactId>-<version>[-<classifier>].<ext>
```

```bash
export BASE=http://localhost:8080
export ADMIN_PW=<你的管理员口令>
export MVN_REPO=$BASE/binflow/maven-local
```

匿名读默认开启（resolve 免凭据）；deploy（PUT）必须认证。

## 接入步骤

### 1. 创建仓库

```bash
curl -su admin:$ADMIN_PW -X PUT $BASE/binflow/api/repositories/maven-local \
  -H 'Content-Type: application/json' \
  -d '{"rclass":"local","packageType":"maven"}' \
  -o /dev/null -w '%{http_code}\n'        # 200
```

### 2. 发布：settings.xml + altDeploymentRepository

`~/.m2/settings.xml`（或 `-s` 指定独立文件，CI 推荐）：

```xml
<settings>
  <servers>
    <server>
      <id>binflow</id>                            <!-- 必须与 altDeploymentRepository 首段一致 -->
      <username>admin</username>
      <password>$ADMIN_PW</password>
    </server>
  </servers>
</settings>
```

```bash
mvn -B -DskipTests deploy -DaltDeploymentRepository=binflow::default::$MVN_REPO
# BUILD SUCCESS；pom/jar/旁车 checksum/metadata 全部 201

# 落盘验证：旁车 .sha1 返回 40 位裸 hex，应等于 sha1sum 本地文件
curl -su admin:$ADMIN_PW $MVN_REPO/com/acme/demo-app/1.0.0/demo-app-1.0.0.jar.sha1
```

### 3. 解析：pom `<repositories>`

```xml
<repositories>
  <repository>
    <id>bf</id>
    <url>http://localhost:8080/binflow/maven-local</url>
  </repository>
</repositories>
```

### 4. 全量收口：settings.xml `<mirror>`

把一切 Maven 流量（含插件）收进一个 virtual 仓：

```xml
<settings>
  <mirrors>
    <mirror>
      <id>binflow</id>
      <mirrorOf>*</mirrorOf>
      <url>http://localhost:8080/binflow/maven-virtual</url>
    </mirror>
  </mirrors>
  <servers>
    <server><id>binflow</id><username>admin</username><password>$ADMIN_PW</password></server>
  </servers>
</settings>
```

两个高频坑：

- **`mirrorOf: *` 会连插件解析一起接管**——virtual 必须含一个代理 Maven Central 的 remote 成员，否则 `mvn compile` 在默认插件处直接失败：

  ```bash
  curl -su admin:$ADMIN_PW -X PUT $BASE/binflow/api/repositories/maven-remote-central \
    -H 'Content-Type: application/json' \
    -d '{"rclass":"remote","packageType":"maven","url":"https://repo.maven.apache.org/maven2"}' \
    -o /dev/null -w '%{http_code}\n'        # 200
  ```

- **解析失败会缓存在本地仓的 `.lastUpdated` 标记里**——修好配置后仍报 `Could not find artifact` 时，清 `-Dmaven.repo.local` 目录或加 `-U` 重试。

## SNAPSHOT 语义

- mvn 默认 unique deploy：落盘文件名带 timestamp + buildNumber（`demo-app-1.2.0-20260819.212603-2.jar`）；两次 deploy 后 version 级 `maven-metadata.xml` 的 `buildNumber` 递增。
- 拿最新 SNAPSHOT：消费侧声明 SNAPSHOT 依赖 + `mvn -B -U compile`——`-U` 强刷 metadata 解析到最新 buildNumber。
- 服务端按存储事实计算 `maven-metadata.xml`；客户端 PUT metadata 被接受并触发重算。
- 强刷已缓存 metadata：对缓存路径执行 `DELETE`（删本地缓存）后下次 GET 回源——remote 仓没有 `?refresh` 参数（见 [仓库管理](/admin/repositories)）。

仓级配置（建仓 JSON）：

| 字段 | 取值 | 行为 |
|---|---|---|
| `snapshotVersionBehavior` | `deployer`（默认）/ `non-unique` / `unique` | 均按上传名落盘（`unique` 的服务端改写未实现，行为等同 `deployer`） |
| `handleSnapshots` | `true`（默认）/ `false` | `false` 时 SNAPSHOT deploy → **409**（GET 不受影响） |
| `handleReleases` | `true`（默认）/ `false` | `false` 时 release deploy → **409** |

## checksum 策略

| `checksumPolicyType` | 客户端摘要与实测不一致时 |
|---|---|
| `client-checksums`（默认） | **409** `Checksum error for '<repo>/<path>': received '<x>' but actual is '<y>'` |
| `server-generated-checksums` | 接受，落盘与回显以服务端实测为准（历史构件迁移场景） |

客户端一个摘要都没给时服务端计算并接受。注意：**remote 仓的 checksum 旁车请求（`.jar.sha1` 等）不回源，404** `Checksums are not downloadable.`——Maven 3.9 resolver 对缺失 checksum 只是告警不失败。

## Gradle

Gradle 8.x 走 BinFlow maven 仓可用：

```kotlin
repositories {
    maven {
        url = uri("http://localhost:8080/binflow/maven-local")
        allowInsecureProtocol = true          // 明文 HTTP 必配
    }
}
```

`.module` 元数据 404 后 Gradle 自动回落 POM，属正常行为。

## 常见报错对照

| 症状 | 原因 | 处置 |
|---|---|---|
| deploy 409 `handling of snapshots is disabled` | 仓配置关了 SNAPSHOT/Release | 改仓配置或换仓 |
| deploy 409 `Checksum error ...` | 摘要与内容不符（client-checksums 仓） | 检查构件；或迁移仓配 `server-generated-checksums` |
| PUT 400 `maven layout: ... at least 3 directories` | 路径不是标准 GAV layout | 用 mvn deploy；通用文件放 generic 仓 |
| PUT 400 `file name "x.jar" must start with "<artifactId>-<version>"` | 文件名与目录 GAV 不符 | 同上 |
| 401 `authentication required` | 匿名 deploy，或 `<server>` id 与 `altDeploymentRepository` 首段不一致 | 对齐 id；检查口令 |
| `Could not find artifact`（修好配置后仍报） | 本地 `.lastUpdated` 负缓存 | 清 `maven.repo.local` 或 `mvn -U` |
| mirror 形态下默认插件解析失败 | virtual 无 Central 成员 | 加 remote 成员（上文第 4 步） |

## 下一步

- 代理 Central / 聚合 local+remote：[仓库管理](/admin/repositories) · [npm](/integrations/npm) · [PyPI](/integrations/pypi)
