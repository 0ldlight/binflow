# Sprint 700 迭代报告 — 第 16 次熔断（终验票）；T-297 发现真缺陷，conductor 窗口内修复（`7b84a71`）

**日期**: 2026-08-26 18:01
**上轮**: Sprint 699

## 事件

- **17:56** T-297 死于配额上限（第 16 次；重置 18:30:32）。死前报出疑似差一缺陷：全新节点单次 64-key PUT 被拒「more than 64」。
- 3 个堆积触发合并本轮处理。

## 缺陷确认与修复（conductor 配额窗口内手术）

- **根因**：`internal/httpapi/properties.go` PUT 合并视图计数——`merged := len(props)` 后又遍历 props 对每个不在 existing 的键 `merged++`，**写集数了两遍**。全新节点上 merged=2×N，实际效果：单次 PUT ≤32 键才能过，>32 全部误拒（64 键 → 128 → 「more than 64」）
- **为何漏网**：T-286 测试用 2-3 键（双倍后仍 <64）；FE L21 腿同小基数——基数敏感性测试缺失
- **修复**：循环反转——写集数一次 + **存活**的既有键（写集未命名的）各数一次（正确的事后合并基数）
- **回归测试**：`TestStoragePropertiesNodeKeyCapBoundary` 钉死边界（全新 64 键 PUT 204 / 第 65 键 400 / 同键替换 204）
- **验证**：TestStorageProperties 族绿 → **httpapi 全包 86.6s 绿** → lint 0 issues → 提交 `7b84a71` → origin 推送
- 该修复归 T-286 票面（缺陷引入票），T-297 复验时重跑其属性腿

## 在途

T-297 待 18:30 配额恢复后复活（剩余：L 序列尾段 + DoD 核账 + 总裁定；属性差一腿按修复后的 HEAD 重跑）。VM 仍失联（vm 推送积压 6 个提交待恢复后补推）。

## 状态

M10：19/21。HEAD=`7b84a71`（origin）。
