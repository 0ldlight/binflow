# 评审报告 PR #148（形态: reviewer-b — architecture）

分支 `claude/wonderful-sanderson-ee626f` → `develop`，9 commits（2b7ec6dc…c5bcc70f，含前置 27761063 凭据清剿、93e6080e 治理基线切换）。
评审人：code-reviewer / Reviewer B。评审日 2026-09-24，工作树 `/Users/lzw/dev-center/.claude/worktrees/clever-grothendieck-a3fa4f`。

> **最终结论: APPROVE**（初判 REQUEST_CHANGES；唯一 blocking 项 B1 已经 commit 3ee5b0b2 修复并复核通过，见文末「复核结论」段。non-blocking 7 条已由 conductor 登记到对应后继票。）

## 15 字段模板

- **Ticket**: PR #148 载 T-515/T-516/T-517/T-518/T-519（Phase A 首批五票，P1 层）
- **Role**: code-reviewer (reviewer-b)
- **Area**: internal/metadata（T-519）、tools/difftest/v2（T-518）、docs/reverse/（T-515/516/517）、BOARD.md（收编记录）
- **Input**: conductor 派发单（五维评审范围）+ 通读改动 diff、docs/reverse/README 体例、docs/compatibility/fixtures/normalize.yaml、五份 reports/agents/T-51*.md、cmd/binflow-server/main.go serve 闸
- **Changes**: 逐 commit diff（2b7ec6dc/ca0339e3/156a3276/bfe09819/e68a3db7/d720cc74/c5bcc70f）+ 上下游追读：metadata.Open 调用链（cmd serve 闸 → openStack）、Vacuum/VacuumInto 消费点（httpapi/maintenance_carriers.go:195、cmd/binflow-server/backup.go:146）、internal/config DSN 校验口径（validate.go:297-300）、normalize.yaml 全文结构与 v2 scanner 的对拍
- **Files**: 见下「分域结论」
- **Tests**: 见「Commands」；关键结果——metadata 全量 sqlite 套件 PASS（16.6s）、dialect parity/rebind 定向 PASS、`CGO_ENABLED=0` build PASS、`go vet`（metadata+cmd）零告警、golangci-lint 0 issues、下游 internal/auth PASS；difftest v2 四态行为本机复演（dry-run→NOT_RUN、缺 env→BLOCKED、score.sh 单行 JSON）
- **Commands**（实际执行原文）:
  - `go test ./internal/metadata/ -count=1` → `ok ... 16.600s`
  - `go test ./internal/metadata/ -run 'TestMigrationVersionParity|TestMigrationsDir|TestRebind|TestOpenPostgres' -count=1` → ok
  - `go vet ./internal/metadata/` / `go vet ./cmd/...` → exit 0
  - `CGO_ENABLED=0 go build ./internal/metadata/` → exit 0；`go build ./...` → exit 0
  - `golangci-lint run ./internal/metadata/...` → `0 issues.`
  - `go test ./internal/auth/ -count=1` → `ok ... 27.855s`
  - `python3 tools/difftest/v2/runner.py --dry-run` → `PASS=0 FAIL=0 BLOCKED=0 NOT_RUN=1`；`--list` → `demo-ping ...`；`bash tools/difftest/v2/score.sh` → `{"pass":0,...,"not_run":1,...,"coverage":null}`
  - `env -u A_BASE ... python3 tools/difftest/v2/runner.py --out /tmp/...` → `BLOCKED | missing required env: A_BASE, B_BASE, A_USER, A_PASSWORD, B_USER, B_PASSWORD`
  - 取证 grep：postgres 迁移全目录 `BOOLEAN` 零命中；`git show --shortstat` 逐 commit 对账 BOARD 数字
- **Outputs**: 本文件（reports/review/PR-148-B.md）
- **Compatibility**: PR 未触 docs/compatibility/contracts 与 known-divergence；normalize.yaml 零 `status:` 行（v2 注册制今天判全域 raw——与 T-518 实证一致，保守正确）。规格票（T-515/516/517）为契约化素材上游，版本双写口径齐全（见分域结论 3）
- **Security**: 走查通过——store.go:132-143/154 DSN 不回显、DBPath 脱敏标签（store_postgres_test.go:129-135 断言）；rebind 仅作用于树内 3 条常量语句（无外部输入注入面）；difftest v2 凭据零字面量（本机 grep 复核 runner/score/cases/README）；evidence Authorization 恒 `<redacted>`。范围外注记：27761063 清剿抽查（l0203/conan-auth401.sh）已改 env 注入，残留 `172.16.58.130` 为内网 IP 非凭据
- **Performance**: sqlite 路径零行为变更（全量套件绿）；PG 连接池暂套 NumCPU 预算（ADR-0007 口径，未按 PG 调优——T-519 risks 已记）；difftest runner 逐请求超时永不自动放宽
- **Risks**: ① 11 个新 PG 迁移 SQL 从未在真 PG 执行（NOT_RUN）——首跑可能暴露方言笔误（回滚安全：每迁移一事务）；② v2 内置归一词表尚不覆盖 normalize.yaml docker-remote 语义（keep_semantic/time_headers/superset_policy），L000 系批次迁 v2 前需扩词表；③ BOARD 记 done 的五票中 T-519 依赖本 PR 合并后才算真正闭环
- **Blockers**: 无（取证命令全部可跑，零障碍）
- **Next**: 已闭环——conductor 确认 27761063/93e6080e 属 Phase A 治理批（先前已独立过评审口径）；Reviewer A 实例并行在途（migration 关键域 A/B 双实例满足）；N1-N7 登记去向见复核段

## 初判结论（2026-09-24，被 3ee5b0b2 修复后由复核段取代）

**结论: REQUEST_CHANGES**

### 必须修改（blocking）

- **B1** `internal/metadata/migrations/postgres/README.md:78-79, 87-88, 110-111` — 三处版本注记写「booleans are the INTEGER 0/1 convention on the sqlite side, **BOOLEAN here**」（018_webhook / 019_replication_globals / 022_backups），与实码相矛盾：三份 postgres 迁移文件全部用 `INTEGER NOT NULL DEFAULT 0/1`（018_webhook.sql:18/25/31、019:14-15、022:13；全目录 grep `BOOLEAN` 零命中），也与同文件第 11 行方言规则节「booleans stay INTEGER 0/1 in **both** dialects」自相矛盾。T-519 报告 compat_evidence 声称「README 已改写为与实码一致」、commit message 声称 "README rewritten to match shipped reality"——该声称在这三处不成立（证据与声称不符类缺陷）。此 README 是后继 sub-store 方言化票的直接约定来源，错误指令会把 BOOLEAN 引入新迁移、破坏 parity 约定。→ 最小改法：三处括注统一改为与第 11 行一致的说法。

### 建议改进（non-blocking）

- **N1** `internal/metadata/store.go:305-308` — `sqliteStore = sqlStore` 别名只为 34 处测试断言（`(*sqliteStore).db`）编译零改动而保留；sub-store 方言化落地后该名字对 PG 实例是误导。建议后继票把测试断言切到 `sqlStore` 后删别名。
- **N2** `internal/metadata/snapshot.go:32/70` — `VacuumInto/Vacuum`（sqlite 专属语句）挂在双方言共享的 `sqlStore` 上，PG 实例调用会响亮报 PG 语法错（不静默；今天被 cmd serve 闸挡住不可达）。`sqlStore` 未携带 dialect 字段，后继方言化票需在接线或拆分上处置——建议明确记入该票范围（T-519 deferred 已列 PRAGMA/VACUUM 面，方向正确）。
- **N3** `cmd/binflow-server/main.go:1003-1007` — serve 闸保留本身合理（票面范围外、fail-fast 语义正确：sub-store 未方言化前放行会产生运行期碎片失败），但日志文案 "M1 ships SQLite only" 已过时（metadata 层本 PR 已启用 PG）。闸拆除票落地时一并刷新文案即可。
- **N4** `docs/reverse/README.md` 规格清单未加 `remote-cache-projection.md` / `virtual-resolution.md` 两行——与 de facto 状态一致（cargo/conan/webhook 等十余份存量也未入索引），不构成违规；若该清单定位是权威索引则建议补行。
- **N5** `tools/difftest/v2/runner.py:52-58` — 内置归一词表（drop-volatile-headers / base-url-placeholder）尚不能表达 normalize.yaml docker-remote 域的既有语义（keep_semantic / time_headers / superset_policy / body 规则）；docker 域未来迁 v2 需先扩词表再登记。T-518 open_questions #3 已自报，保持在 compatibility-engineer 台面上。
- **N6** `tools/difftest/v2/runner.py:69-89` — normalize.yaml 行导向 scanner 依赖 2 空格缩进风格（`ponytail:` 注释已标 ceiling）；yaml 手工改缩进会漏判注册。可接受，换真 YAML 解析的触发条件已写明。
- **N7** `internal/metadata/store_postgres_test.go:1-135` — PG 集成腿 NOT_RUN 是本 PR 最大遗留风险（11 个迁移文件仅经人工方言审校 + 版本 parity 测试），BOARD/T-519 均已如实登记并留复跑命令；建议 CI PG service container 后继票（D5 关联）尽早落地把该腿翻 PASS。

## 分域结论（对应派发五维）

1. **T-519 internal/metadata 架构面**——整体合格。pgx v5 stdlib 直连、无全局 wrapper、rebind 收敛在 Open 路径必经 3 条语句（store.go:277/289、migrate.go:147），给后继 sub-store 方言化留了干净路径：rebind 已按方言参数化且表驱动测试覆盖（migrations_dialect_test.go:63-101，字面量/注释/美元引号边界齐全），迁移体走方言原生文件不经 rebind（applyMigration 只 rebind 记账 INSERT）——职责切分正确。`sqliteStore→sqlStore` 改名 + 别名不破坏既有 API（别名仅为测试保留，非导出符号，包外零影响；全树 build/vet/lint 绿）。28 版本 lockstep 有 `TestMigrationVersionParity` 护住（版本号+文件名双侧断言，本机复跑 PASS）。README 口径与实码一致性初判不达标（B1，已修复）。cmd serve 闸保留合理（见 N3）。配置侧 DSN 校验口径与 internal/config/validate.go:297-300 一致。
2. **T-518 tools/difftest/v2**——合格。注册制扩展性干净（用例 .py 双轨：声明式 CASE + 编程式 run(ctx)，发现/校验/重复 id 防护齐备）；四态纪律落实到位（skip≠PASS：dry-run/过滤→NOT_RUN，缺 env/transport→BLOCKED，HTTP 4xx/5xx 参与比对——runner.py:39/444-484 与 README §四态一致，本机三态复演通过）；与 reports/compatibility 产物约定一致（results.json 机读 + score.sh 单行 JSON + evidence 相对路径 + run/ gitignore）。与旧 l0xxx 完全隔离（零触碰）。凭据纪律零字面量（grep 复核）。
3. **T-515/516/517 文档票**——体例合格。三文档族（config-formats 重写、remote-cache-projection + virtual-resolution 新建、enterprise/ha-cluster + federation 新建）均带：置信度三档定义（且族内自洽）、证据锚（模块名+相对路径[+行号]，与 repo-semantics/license-behavior 既有锚式同构）、待验证清单（T-515 §16 九项、T-516 两文件各五项、T-517 两文件 UNKNOWN 表 7+8 条）、与既有规格交叉引用不重复（T-516 双文件明确定位为 repo-semantics §7/§8 深化增量；T-517 与 replication.md §7 互链）。版本双写（获取口径 7.161.24 + 实测 pom 7.161.14）在全部五份规格头部齐全（config-formats.md:3、remote-cache-projection.md:2、virtual-resolution.md:3、ha-cluster.md:3、federation.md:3）。clean-room 抽查：行为语句为「当客户端…服务端…」形态，锚引用为既有全仓体例，未见逐行翻译或私有结构誊写，无 clean-room 嫌疑。T-516 勘误链闭环：virtual-resolution §7.5 ↔ repo-semantics §8.2 勘误行 + 待验证第 5 条划收敛（156a3276），裁定记录在 T-516 日志 risks #1。
4. **BOARD.md 收编记录（c5bcc70f）**——合格。五票逐票 done + commit 哈希 + 文件数/行数与 `git show --shortstat` 逐项对账一致（+342/−107、+355、+521、+783、+1113/−97 全对）；收编三铁律口径显式（agent 停止→清单对账→build+tsc 哨兵）；M17 预留号复用注记切分了旧指针行语义；测试四态纪律落实（PG 腿 NOT_RUN 不掩饰）。done 时点与八态流的关系沿用本仓既有实践（收编即 done、PR 独立评审在合并口），无违规。
5. **遗留风险登记完备性**——合格。T-519 三条移交（sub-store 方言化 / serve 闸拆除 main.go:1001 / CI PG container）、T-518 两开放问题转 compatibility-engineer、T-516 待验证 10 条转差分选题（§7.5 勘误与 §3-9 优先）、D1–D5 悬决状态——均在 BOARD 2162 段闭环登记，与各 agent 日志 handoff_to 一致。

## 范围外发现（交 conductor；已闭环）

- PR 内含 27761063（凭据清剿）与 93e6080e（治理基线切 7.161.26 + UAT 审批门 + dba/ha 角色）——conductor 确认属 Phase A 治理批，先前已独立过评审口径。闭环。
- tools/difftest/l0203/conan-auth401.sh 仍 `source /Users/lzw/dev-center/deploy/compose/.env.uat`（绝对本机路径，存量模式）——跨机复跑会断，属旧目录存量，非本 PR 引入。留档。

## 复核结论（2026-09-24，commit 3ee5b0b2）

**结论: APPROVE**

- **B1 修复复核通过**。`git show 3ee5b0b2`：单文件（internal/metadata/migrations/postgres/README.md）3 个 hunk，精确命中初判点名的三处（018_webhook 注记 78-79、019_replication_globals 注记 87-88、022_backups 注记 110-111），无夹带改动。三处现均写「booleans stay/as INTEGER 0/1 in both dialects」，与同文件方言规则节（第 11 行）及实码（28 份 postgres 迁移全目录 grep `BOOLEAN` 零命中，本复核时点复跑确认）三方一致。全仓 `internal/` 下 `grep "BOOLEAN here"` 零命中。README 口径与实码一致性的缺口关闭，T-519「README rewritten to match shipped reality」声称恢复成立。
- 复核取证命令：`git show 3ee5b0b2`；`grep -n "BOOLEAN here" internal/metadata/migrations/postgres/README.md`（exit 1 = 零命中）；`grep -rn "BOOLEAN here" internal/ | wc -l` → 0。
- non-blocking N1–N7 不重评，按 conductor 登记去向跟踪：N1/N2 → sub-store 方言化票；N3 → serve 闸拆除票；N4 顺带；N5/N6 → difftest 首批差分票；N7 → CI PG service container 票。
- 范围外两条 + 双实例关切已由 conductor 确认闭环（治理批先前已评审；Reviewer A 并行在途）。
- 本 PR 可合入 develop（Reviewer B 视角；A 实例结论由其独立报告给出）。

## 断点快照

已完成：全部五维评审 + B1 修复复核 + 报告落盘更新。
未完成：无。
断点位置：无（评审终结，最终结论 APPROVE）。
