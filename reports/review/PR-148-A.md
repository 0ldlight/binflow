# PR #148 评审报告（形态: reviewer-a / correctness）

评审范围：develop←claude/wonderful-sanderson-ee626f（merge-base 5be67e36..HEAD），重点 T-519（internal/metadata，e68a3db7）、T-518（tools/difftest/v2，d720cc74）、T-515/T-516/T-517 文档票抽查、全 diff 凭据扫描复核。

## 15 字段模板

- **Ticket**: PR #148（含 T-515..T-519 五票 + 治理/凭据清扫两提交）；P1
- **Role**: code-reviewer (reviewer-a, correctness)
- **Area**: internal/metadata（postgres 方言启用）、tools/difftest/v2、docs/reverse、reports
- **Input**: conductor 派发（PR 范围+四项重点）；通读 store.go/migrate.go 全文、11 个新 postgres 迁移与 sqlite 对照、双方言测试、pgx v5.11.0 模块缓存源码（stdlib/sql.go:501-517、conn.go:499-587、pgconn/errors.go:137-147、config.go:363）、cmd/binflow-server openStack/gc/backup 三条 Open 调用链、internal/storage/engine.go OpenEngine→sweepSessions、internal/config/validate.go postgres 段、runner.py/score.sh 全文、docs/reverse 六文件全文
- **Changes**: metadata 全 diff + 上游 4 个 Open 调用点追读到失败端（sweepSessions ListExpired / VacuumInto）；difftest v2 框架逐行 + 实际执行取证；文档票按行为句式逐节过
- **Files**: 见下方逐文件结论
- **Tests**: 亲自执行（本 worktree）：
  - `go test ./internal/metadata/ -count=1` → `ok 18.018s`（与 T-519 报告声称一致，复验通过）
  - `go test ./internal/metadata/ -run 'TestMigrationVersionParity|TestRebind|TestOpenPostgres|TestOpenIdempotentMigration' -count=1` → ok
  - `go test -race ./internal/metadata/ -run 'TestMigrationVersionParity|TestRebind|TestOpenPostgres|TestOpenIdempotentMigration'` → ok 2.702s
  - `go vet ./internal/metadata/` → 空；`gofmt -l internal/metadata/` → 空；`CGO_ENABLED=0 go build ./...` → exit 0（零 CGo 门成立）
  - difftest：`python3 runner.py --dry-run` → `NOT_RUN=1`；`./score.sh`（缺文件/正常/SKIP 未知态/坏 JSON 四路）→ 前三路正确，坏 JSON 路违背自述契约（见 N2）
  - PG 实库腿：本机无 postgres（psql/pg_ctl 无、5432 无监听）→ BLOCKED，与 T-519 报告的 NOT_RUN 声明一致，非虚假证据
- **Commands**: 上列原文；另 `git diff 5be67e36..HEAD`、pgx 源码 grep、postgresql.org/docs/16 关键字附录核对（GROUPS=PostgreSQL 列 non-reserved，疑点排除）、python 脚本对 138 个新迁移标识符做 reserved 黑名单扫描（0 命中）
- **Outputs**: reports/review/PR-148-A.md（本文件）
- **Compatibility**: 本视角部分适用——postgres 迁移与 sqlite 同版本号语义对齐逐文件核对：002/003/006/018/019/022/025 代码行零差异；001/004 仅 SERIAL 差、005/007 为 strftime→to_char、instr→strpos、OR IGNORE→ON CONFLICT DO NOTHING 的等价移植；无 sqlite-only 构造（AUTOINCREMENT/PRAGMA/WITHOUT ROWID/COLLATTE NOCASE/strftime 等全树 grep 0 命中）；7.161 行为基准标注齐备
- **Security**: 全 diff 新增行凭据扫描（password/secret/token/JFrog@/AKIA/user:pass@URL 模式）仅命中文档级一次性示例（POSTGRES_PASSWORD=test、postgres:test@127.0.0.1）；27761063 清扫提交抽样核实为真（JFrog@2026 → ${ARTIFACTORY_REF_PASSWORD:?}）；openPostgres 错误路径不回显 DSN（ping/open 两路核实），但见 N4 的 pgx ParseConfigError 边角；DBPath 恒 "postgres"（有测试钉住）
- **Performance**: Open 路径无热 path 变化；PG 池沿用 NumCPU 预算（ADR-0007 口径，报告已注记）；rebind O(n) 单遍、仅 3 条语句；007 递归 CTE 在两方言同构
- **Risks**: 11 个 postgres 迁移从未在真 PG 执行（集成腿 NOT_RUN）——静态审查（语法构造、保留字、多语句协议路径、事务语义）未发现具体缺陷，但「首跑暴露方言笔误」风险仍在，唯一收敛途径 = CI 起 PG service container 跑 BINFLOW_TEST_POSTGRES_DSN 腿（建议随本 PR 或紧随其后落地）
- **Blockers**: PG 实库不可得（集成腿无法亲自复跑）；golangci-lint 本机未验证（go vet/gofmt 已覆盖）
- **Next**: ① B1 修复后重审（小 diff，可只审 cmd 三处闸门）；② 建 CI 票：PG service container + BINFLOW_TEST_POSTGRES_DSN 集成腿；③ sub-store 方言化票（已由 T-519 handoff 提出）应显式收编 N1/N3；④ 范围外发现：无

## 逐文件结论

- internal/metadata/store.go — openPostgres DSN 前缀校验、错误不回显 DSN、DBPath 脱敏、Ping 先行：正确。**但上游调用面见 B1**。IsReferenced 未 rebind 见 N1。sqliteStore→sqlStore 别名保 30 处测试不动：干净。
- internal/metadata/migrate.go — 每迁移一事务 + 账行同事务落账 + defer Rollback：PG 侧失败即整体不落账（PG DDL 事务性成立），链上已成功迁移保留（断点续迁语义，正确）。多语句 body 经 pgx：stdlib/sql.go:501 零参 → conn.go:524-527 强制 simple protocol → 多语句 Exec 合法（关键疑点已从驱动源码排除）。rebind 的引号/双引号/两种注释/双 dollar-quote 处理经测试表与源码双确认正确。
- migrations/postgres/（11 新文件）— 与 sqlite 同版本号逐文件语义对齐（见 Compatibility）；标识符 reserved-word 扫描 0 命中（含 groups——PG 官方附录核对为 non-reserved，可用）；005/007 方言替换等价（strpos/instr 同为 1-based 返回 0-when-missing；to_char 'YYYY-MM-DD"T"HH24:MI:SS"Z"' 与 strftime 同为秒截断）。N5 为 007 MAX(text) 排序的注记。
- migrations_dialect_test.go — parity/dir/panic/rebind 用例覆盖实到位；rebind 用例含全部边界形态。
- store_postgres_test.go — 门控 + NOT_RUN 纪律正确；表存在性抽查覆盖链头尾；admin seed/reopen 幂等/DSN 不外泄三腿设计合理。
- internal_test.go 改写 — postgres 拒绝测试从「恒拒」改为「DSN 校验 + 不可达 + secret 不泄」三腿，与新行为匹配。
- snapshot.go / doc.go — receiver 改名 + 文档同步，无行为变化。
- tools/difftest/v2/runner.py — import 全 stdlib（「零第三方依赖」声明属实）；四态判定（env 缺失→BLOCKED、transport→BLOCKED、case 崩→BLOCKED 不杀批、未选中/dry-run→NOT_RUN）实现正确；凭据纪律（env-only、evidence redact、env 段只记 set/MISSING）落实；重复 id 处理循环语义推演正确（每 id 恰一条 BLOCKED）。
- tools/difftest/v2/score.sh — 单行 JSON 与 coverage 公式正确（实测）；N2 契约违例。
- docs/reverse/config-formats.md（T-515）— 当…则… 行为句式 + 〔码·模块〕锚规范；负证条目（db.properties/storage.properties/${} 插值）标注与置信度纪律严格；§16 待验证收敛完整。
- docs/reverse/{remote-cache-projection,virtual-resolution}.md + repo-semantics §8.2 勘误（T-516）— **§8.2 勘误行与 virtual-resolution.md §7.5 逐点一致**（仅删 virtual 自身 `<key>-cache` 投影存储、不动成员实体、无条目 404 ITEM_NOT_FOUND），待验证清单 #5 同步收敛；诚实标注「参照实例凭据不可用、全部反编译单源」。
- docs/reverse/enterprise/{ha-cluster,federation}.md（T-517）— 行为句式为主、锚带路径；N6 风格注记；UNKNOWN/待验证清单纪律好（「无脑裂仲裁」为显式否定性结论而非猜测补齐）。

## 评审报告 PR-148（形态: reviewer-a）

**结论: APPROVE（2026-09-24 复审后；初审 REQUEST_CHANGES 因 B1，已由 964e8aca 修复并复审通过，见文末复审记录）**

### 必须修改（blocking）

- **B1 · cmd/binflow-server/main.go:2058（gc）、cmd/binflow-server/backup.go:127（export）、backup.go:477（import）** — T-519 使 `metadata.Open(Driver: "postgres", …)` 从恒拒（errPostgresDisabled）变为真开库，但 postgres 启动闸只在 serve 路径（main.go:1001-1008，注释自称 "this is the one gate"）存在；config 层 validate.go:294-300 对 driver=postgres 是 pass-through。后果（均已追读代码链证实）：`metadata.driver: postgres` + DSN 下，`binflow gc` 会先对外部 PG 库**执行全部 28 个迁移并以默认密码 seed admin 用户**（成功落账），随后才在 storage.OpenEngine→sweepSessions→`Sessions.ListExpired`（engine.go:432，`?` 占位符）处以裸 pgx 语法错误失败；`binflow backup export` 同样先迁移+seed 再在 `VACUUM INTO ?`（snapshot.go:38）失败；backup import 传文件路径作 DSN 走前缀校验失败（尚响亮）。改动前这三条路径都以清晰的 errPostgresDisabled 快速失败——这是本 PR 引入的错误路径行为回归 + 对外部库的非预期持久写入（含默认口令 admin 落库，安全面不可忽视）。→ 修法（最小 diff）：把 main.go:1001 的闸提取成共享 helper，在上述三处 metadata.Open 前同判 `cfg.Metadata.Driver == config.DriverPostgres` 即拒（拆除闸的后继票再统一回收）；不许以「cmd 不在本票 area」放行——回归由本票的被调方语义变化直接造成。

### 建议改进（non-blocking）

- **N1 · internal/metadata/store.go:383-391** — `IsReferenced` 是含 `?` 的方言共通语句却未过 rebind（migrate.go:147、store.go:277/289 均已 rebind）。今日仅 PG store 无人调用（GC 面），失败也响亮；但与「Open 路径语句方言共通」的口径不一致，sub-store 方言化票应显式收编（一行 `rebind` 或明确排除理由）。
- **N2 · tools/difftest/v2/score.sh:4-5** — 头注断言 "Exit code is always 0: state travels in the JSON"，实测坏 JSON（`echo 'not json' > f; ./score.sh f`）→ traceback + exit 1；缺 python3 → 127。消费方若按契约以「exit 0 必有 JSON 行」解析会被打断。→ 二选一：python 侧 try/except 兜底输出全零 JSON，或改注释为「results.json 损坏 = 响亮失败」并写入 README。
- **N3 · internal/metadata/migrate.go:212-216** — rebind 会把 PG jsonb 运算符 `?`/`?|`/`?&` 改写成 `$n`。当前 3 个调用点无此形态，无实际误伤（含 `?` 字面量已被引号/注释/dollar-quote 分支正确保护）；sub-store 方言化扩大 rebind 使用面时须防此坑（建议在 rebind doc 注释记一句，或用例钉住）。
- **N4 · internal/metadata/store.go:132-136** — "DSN 本身绝不回显"对 ping/open 路成立，但通过前缀校验而 URL 非法的 DSN（如坏百分号转义）会让 pgx `ParseConfigError` 回显经 redactPW **尽力**脱敏的连接串（pgconn/errors.go:137-147 自述 best effort）。config 路径有 validate.go:294 的 url.Parse 前置（安全），仅直连 Open 的调用者可触。建议 openPostgres 同样先 `url.Parse` 一次以完全闭合口径。
- **N5 · internal/metadata/migrations/postgres/007_folder_rows_backfill.sql:57** — `MAX(stamp)` 对 TEXT 列在非 C collation 库上的排序与 sqlite BINARY collation 可能不同（仅当历史时间戳格式不 uniform 时）；当前 Now() 全 uniform，风险理论化。在 README porting notes 记一句即可。
- **N6 · docs/reverse/enterprise/ha-cluster.md:53/58/69、federation.md:60/152/170 等** — 行为句内嵌内部方法/异常类名（`verifyAllArtifactoryServers(true)`、`getAffinityTasksForNode()`、`shouldActOnEvent`、`FederatedRemoteMembersUpdateException` 等），超出两文自declared 的 `模块名 路径` 锚形式（federation.md:5、virtual-resolution.md:6）。未发现私有结构逐行翻译或整段代码转写——不构成 clean-room 违例，属风格收紧项：后续 docs pass 把这些标识符挪进锚位。
- **N7 · docs/reverse/virtual-resolution.md:117、:145** — 正文段落残留表格管道符（`。| 高 |`），渲染残留，随手修。

### 范围外发现

无（T-519 报告自身已如实记录 PG 腿 NOT_RUN 与 cmd 闸归属，未发现证据与声称不符项；实现日志声称的全部可在本机复验的命令均复验通过）。

## 复审记录（A-B1 修复 · commit 964e8aca）

复审范围：仅 `git diff 3ee5b0b2..964e8aca`（cmd/binflow-server 三文件，+86/−10），按初审建议收窄为 cmd 三处闸门。

### B1 复核（resolved）

- `main.go:112-124` refusePostgresDriver helper：语义与旧内联闸逐字等价（同判 DriverPostgres、同 logger.Error、同 errPostgresDisabled 包装）；openStack 替换为 helper 调用，serve 行为不变。
- `main.go:2023-2031` runGC：闸位于 writeStartupWarnings 之后、graceOverride 解析 / `storage.AcquireDataLock` / `metadata.Open`（main.go:2082）之前——被拒的 gc 不取维护锁、不触外部库。logger 提前创建且仅创建一次（旧位置删除，无重复）。
- `backup.go:102-107` exportSnapshot：闸位于函数顶、`prepareBackupDir` 与 `AcquireDataLock` 之前——被拒的 export 不留 bk 目录；因 exportSnapshot 是 CLI 与 cron 备份域共享内核，闸同时覆盖两条入口（与 serve 闸叠加为无害双闸）。
- 全部 4 个 `metadata.Open` 调用点复核：backup.go:132（闸前）、main.go:1026（闸前）、main.go:2082（闸前）、backup.go:482（import，见下）。

### 对初审报告的更正（接受并记录）

- 初审 B1 列了 backup.go:477（import）为缺闸——**过列**。实核：runImport 在 backup.go:374-378 已有更严的非-sqlite 全拒闸（先于该 Open 与一切文件操作执行），且 TestImportRefusesNonSQLiteDriver（backup_test.go，复审实跑 PASS）覆盖。实际缺闸只有 gc/export 两处，修复与之吻合。

### 测试与工具链复核（亲自执行）

- `go test ./cmd/binflow-server/ -run 'TestGcExportRefusePostgresDriver|TestServePostgresRefusedToBoot|TestImportRefusesNonSQLiteDriver' -count=1 -v` → 三腿全 PASS（含 gc/export 子腿，断言 errors.Is 链 + stderr 文案 + 无 binflow.db + export 无 bk 残留）
- `go test ./cmd/binflow-server/ -count=1` → `ok 16.602s`（与提交者声称的 16.540s 一致）
- `gofmt -l`（cmd/binflow-server）→ 空；`go vet ./cmd/binflow-server/` → exit 0

### 复审新发现

无。新测试为表驱动两腿、负证充分（DSN 指向真端口形态，闸失效即测试失败）；闸门文案改为 "the sub-store query layer is still sqlite-only"，与 T-519 后实态一致（B-N3 一并关闭）。

**复审结论: APPROVE** —— B1（blocking）已解决；N1–N7（non-blocking）维持原判，随各自后继票收口（N1/N3 → sub-store 方言化票；N2 → difftest v2 小修；N4/N5 → metadata 后继票顺手项；N6/N7 → docs pass）。
