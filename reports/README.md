# reports/ — 团队状态存档

- `iteration-NNN.md`：主会话每轮迭代后写的迭代报告（NNN 三位递增）。
- `agents/T-xxx.md`：各 agent 完成单个 ticket 时写的工作日志（变更、自测证据、遗留问题）。
  `-qa.md` / `-review.md` 后缀为 QA 报告与评审报告。
- `security-audit-<日期>.md`：安全审计报告。

规则：
- 只追加不删除；重命名/清理需用户明确同意。
- 迭代报告是下轮迭代的第一输入，写清楚"进行中/阻塞/下轮计划"。
