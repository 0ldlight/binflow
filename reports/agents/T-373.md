# T-373 — M13 中期回归（QA）工作日志

- 票：T-373 [P1] QA 中期回归（L02~L13 首跑 + M1~M12 P0 抽样 + 断言反转预核实 + 归属审计）
- 角色：qa-engineer ｜ 日期：2026-08-31 ｜ 被测：HEAD `56b0185`（develop；自 HEAD 重编译 `make build`，94.13MB 维持）
- 纪律：共享工作树**零 git 操作**（审计只读 log/diff/status）；零产品/测试代码改动；测试凭据全驻 /tmp/t373（会话 scratch，已清）；license 用仓内开发签发钥铸 30 天内 pro 文档（`bin/bf license issue`，注入式 REST 安装，非本报告生成）
- 结论：**PASS**（L02~L13 全绿；三处断言反转预核实全绿；P0 抽样双形态全绿；归属审计 81 文件 100% 票号归属 + **1 个无票号 style 提交登记〔P2 记账，零语义〕**）

## 0. 编排（scratch 六实例 + 双接收器 + dind + kind）

| 实例 | 端口 | 形态 | 用途 |
|---|---|---|---|
| community | 18501 | 无 license | L05 门控负臂 / P0 抽样负形态 |
| pro（B） | 18502 | license pro | L02~L09 webhook 主面 + HelmOCI 下游 + P0 抽样正形态 |
| A | 18503 | license pro | HelmOCI 上游 / L12 index 宿主 |
| A2 | 18506 | license pro + `security.anonymous_access: false` | L10 认证上游腿 |
| knob | 18507 | license pro + `webhook.allow_private_target: true` + `folder_download` | L09 双臂 / L14 开关化 |
| surv | 18508 | license pro（独立数据目录） | L05 降级臂 / L06 kill -9 |

接收器：自写编排接收器 `/tmp/t373/recv.mjs`（node 标准库；`/ok` `/flaky`(500×2→200) `/fail500` `/fourxx`(404) `/mode/<m>` 全局故障面 + `/__log`(latin1 原字节+头) `/__count` `/__reset`）——GUA 绑定 `[240e:341:d808:bb00:14bd:4a1f:3366:eef1]:18490`（默认 SSRF 姿态放行类）与 `127.0.0.1:18491`（旋钮放行面）各一。dind 29.7.2（`--insecure-registry host.docker.internal:1850{2,3}`，镜像经 `docker save|load` 注入——宿主 docker.io TLS 超时，环境限制非产品面）；kind bf-ga v1.36.1（真集群腿）。

dind 注意（as-built，复现者提示）：docker 客户端在 dind 内走 token realm，实例 `server.base_url` 须拼 `host.docker.internal:<port>`（T-367 手法），否则 401 挑战的 realm 指向 dind 自身 127.0.0.1 拒连。

## 1. AC1 — L02~L13 逐条判定

### Webhook 域（L02~L09）

| L | 判定 | 命令与关键输出（实测） |
|---|---|---|
| **L02** 订阅 CRUD + test | **PASS** | curl 全链（`/binflow/event/api/v1/subscriptions`）：POST 201（回显全字段）→ GET 200 → list 200 → PUT 204 无体 → GET 反映（event_types=[deleted,deployed]；secret 哨兵 `********`）→ **test 200** `{"message":"Test successful","ok":true,"attempt":{"status_code":200,"elapsed_millis":2}}` → DELETE 204 → GET **404 "Subscription not found"**（逐字）。负面：未知 event_type 400 / 未知 criteria 键（strict）400 / ftp url 400 / 双 handler 400 / 坏 key 400。空 criteria → 201（设计语义：空选择不命中，L04 旁证） |
| **L03** 事件触发 | **PASS** | generic PUT 201(0.08s) → **七字段信封逐字段**：domain/event_type/data{repo_key,path,name,sha256,size=25,sha 与本地 `shasum -a 256` 一致}/subscription_key/jpd_origin/source=`binflow/binflow@<26位ULID>`/userContext{id:admin,isToken:false,realm:internal}（反向：无发明字段）。DELETE→deleted；copy→copied（含 source_repo_path+target_repo_path）；move→moved；props PUT→artifact_property/added **按键逐发**（team、license 两事件）；props DELETE→deleted（values:[]）。docker pushed/deleted 经 dind 真 push+manifest DELETE(202)：image_name/tag/image_type=oci/platforms=[]/path=digest 键。cached 经 generic remote 首拉（CacheMiss）：`{"name":"leg.bin","repo_key":"genrem",...}`。**9 织入型全数命中** |
| **L04** 过滤器 | **PASS** | 三订阅双臂：repoKeys=[t373src] 收 2（hit/f1.bin+hit/skip-me.bin）；repoKeys=[t373dst] 收 1（other/f2.bin）；include `**/hit/*`+exclude `**/skip-*` 恰收 hit/f1.bin——命中发/未命中不发逐臂核对 |
| **L05** 权限与门控 | **PASS** | plain user GET/POST 403；readonly_admin GET 200 / POST 403；匿名 401。**三缝**：community 写 → **403 + `X-Binflow-License-Required: webhook`**（头逐字）+ 读 200（D1）；pro 写 201；**降级不入箱**：DELETE license(200) 后 PUT 201 且接收器零新事件 + `binflow_webhook_queue_depth 0`（门控是路由决策不是损失） |
| **L06** outbox 幸存 | **PASS** | 接收器 mode=500 → PUT 201(0.065s，主路径零阻塞) → 首试 500 落 pending → **kill -9**（10s 重试窗内）→ ping DOWN → 同数据目录重启 → 启动水位行 `webhook: delivery dispatcher started {"outbox_pending":2,"retry_count":5,"retry_wait":"10s","attempt_timeout":"30s"}` → mode=ok → **补投零丢**（2/2 行 delivered；ring 成功记录 retries_attempted=2）→ `queue_depth 0`、`deliveries_total 2`、`retries_total 2` |
| **L07** 重试/死信 | **PASS** | **flaky**（500,500→200）：3 次到达，间隔 **10065ms/10105ms（固定 10s，无退避曲线）**，ring retries 0,1,2 → 第 3 次成功。**fail500**：恰 **5 次尝试（首试计入）**，间隔 10094/10106/10059/10055ms → dead；ring 5 条（retries 0..4）。**fourxx(404)**：**恰 1 次到达、零重试**、终态 dead（4xx 不重试负面臂）。WARN 告警行：`webhook: delivery dead-lettered {"subscription":"t373L07x","attempts":1,"status_code":404,...}` ×2（L07x/L07d）。metrics：dead_letter 2 / deliveries 19 / retries 6。死信可查（troubleshooting 环）；**Replay REST 面缺席**（T-364 §5-3 已登记遗留，非新缺口） |
| **L08** 签名与真实消费者 | **PASS** | 签名态（secret+use_secret_for_signing=true）：接收器收 `X-JFrog-Event-Auth: 9b9a1d9d…`，`openssl dgst -sha256 -hmac t373-hmac-secret` 对**线上字节**（latin1 往返还原）逐字 **MATCH**；篡改一字节 → 摘要 4ca3bfc3…≠签名（**红臂**）。明文态（false+secret）：头=secret 本体（官方双态默认臂）。真实消费者=脚本接收器腿（BOARD AC2 指定替代形态；Jenkins dogfood 条件腿未探活——T-366 §5 同状） |
| **L09** SSRF 负面 | **PASS** | 默认姿态（18502）：loopback → `target rejected: upstream target 127.0.0.1:18491 rejected: loopback address 127.0.0.1`（status_code=0）；RFC1918 10.9.9.9 → `private_rfc1918`（192.168.1.1 同拒）。**旋钮臂（18507，allow_private_target=true——T-366 死键修复后）**：loopback test **ok=true**（接收器 /ok n=1）+ **dispatcher 投递**也达（PUT→ /deliv n=1）。双臂闭环，修复接线 live 证实 |

### HelmOCI 域（L10/L11）

| L | 判定 | 命令与关键输出 |
|---|---|---|
| **L10** remote 全链 | **PASS** | helm 4.2.4：push 0.1.0/0.2.0 → A/up（Digest sha256:a78ec021…/0e65e6f2…）→ **经 B/r10 pull**：Digest 与 push 一致、tgz sha256 与本地打包**逐字节一致**（64576662…）；二拉+by-digest：上游 A 内容 GET 计数 27→30（=首拉 +3）**后冻结**，manifest 探头 `X-Binflow-Cache: HIT`。**认证腿**：A2 匿名关（直连 401）+ B/r10b 配 username/password → pull Digest 一致（64a3627e…），A2 日志上游 GET `"user":"admin"` 三条（凭据链绿；Bearer 全舞步=单测 TestSessionBearerDance 在案）。**kind 真集群腿**：`helm install oci://127.0.0.1:18502/v11/t373chart --version 0.1.0`（virtual remote 域）→ pod **1/1 Running**（nginx 镜像节点已有），uninstall+ns 清理干净 |
| **L11** virtual 与边界 | **PASS** | v11=[v11loc,r10]：remote 域 pull Digest a78ec021… + `X-Binflow-Resolved-From: r10` + `X-Binflow-Cache: HIT`；local 域 pull Digest f46544bd… + `Resolved-From: v11loc`（无 Cache 头）；by-digest 达。tags/list=`["0.1.0","0.2.0"]`（**未缓存 0.3.0 不可见——D-2 口径维持**）；_catalog 200 含 v11/ 前缀并集（T-365 修复面维持）。**push→virtual 405**（C5 原文 "No local repository was configured…"）。混仓边界：**helmoci virtual+docker 成员 400（T-367 rider 逐字）**；+helm 成员 400（T-309 双向）；docker virtual 建仓 400（M3 矩阵）。**停上游**（kill A）：已缓存 manifest+blob 经 v11 **200 HIT**（blob sha 64a3627e… 一致）；未缓存 0.3.0 → **404 MANIFEST_UNKNOWN**（"upstream unreachable" 摘要）且 B 日志零 5xx |

### 配置缝域（L12/L13）

| L | 判定 | 命令与关键输出 |
|---|---|---|
| **L12** chartsBaseUrl | **PASS** | 拓扑：A/idxup（generic 宿主 index，相对 urls）+ P1=18504（tgz 实体，charts base）；B/xbase2 remote 配 `chartsBaseUrl`。helm repo add/update（index 走 A）+ `helm pull xbase2/hetchart` → tgz sha256 **80f43873… 与本地打包一致**；**A 的 idxup tgz GET=0**（实体全走 charts base）、P1 计数恰 +1；二拉+探头 `X-Binflow-Cache: HIT`、P1 **冻结**。REST 回显 `configuration.chartsBaseUrl:"http://127.0.0.1:18504"`。**per-protocol 门**：maven remote 携带 → 400 点名字段（"is not accepted (the divergent charts fetch base is a helm remote-repository behavior)"）；`ftp://` → 400 scheme。**缺省回退臂**：nobase（无 chartsBaseUrl，url=P1）pull 成功 sha 一致（S10 回退链） |
| **L13** `_external`/`_transitive` | **PASS** | P2=18505 宿主手制 index（**绝对外部 URL**）→ B/vdep remote + vdepv virtual：虚仓 index 折叠为 **`_external/http/127.0.0.1:18504/deps/depchart-0.1.0.tgz`**（S8）。**`helm dependency update`**（appchart 依赖 repository=虚仓）→ `Downloading depchart from repo …/vdepv` 成功，charts/ 落地 sha **aed866be… 与源一致**。折叠路径 GET：**200 HIT** + `X-Binflow-Upstream: http://…18504/deps/…`，P1 deps 计数冻结（二次零 egress）。**storage REST 节点可寻址**：`GET /api/storage/vdep/_external/http/…` → checksums.sha256=aed866be…、size 4931。**_transitive 双跳**：A 侧 remote+virtual(upv) → B/tchain+tv：`_transitive/…` 路径 GET **200 MISS→HIT**、sha 一致、P1 冻结（B 虚仓 index 本拓扑折叠为 `_external` 拼写，两种拼写均 200 HIT——见 §4 观察②） |

## 2. AC2 — P0 抽样 / 断言反转预核实 / 归属审计

### 2.1 M1~M12 P0 双形态抽样（community 负形态 + pro 正形态）

| 面 | 结果 | 关键证据 |
|---|---|---|
| docker（dind 29.7.2） | ✅ | push A digest sha256:c2ee4c88… → **经 B/r10 pull RepoDigest 同值** → run 执行（镜像内 binflow-server 报 unknown command "echo"=真运行）；local 域 push→pushed 事件 |
| maven（3.9 容器 temurin 21） | ✅ | `mvn deploy` → BUILD SUCCESS（Uploading pom/jar/metadata 到 BinFlow）；resolve（fresh .m2）+ `java -cp` 执行 → **"hello from binflow t373 maven"**（central 不可达 → settings mirror 指向本地 file:// 仓库离线解析插件，部署目标仍是 BinFlow——环境适配非放松） |
| npm（10.9.8 本机） | ✅ | `npm publish` → `+ @t373/qa-pkg@1.0.0`；install `added 1 package in 386ms` + `require() → t373`；**清缓存重装** 390ms 同样绿（`.npmrc` registry 行须带尾斜杠——docs/user/integrations/npm.md:51 既有形态，照抄即绿） |
| pip（twine 7.0.0） | ✅ | twine upload → pip install --index-url BinFlow → `installed from binflow: 1.0.0` |
| curl generic | ✅ | 全程 roundtrip + sha256 对账（L03/L06 等）；**blob 去重**：同字节经 deploy/copy×2/move 落 4 节点 → blobs/19/1916977b… **恰一份** |
| helm 经典 + helmoci | ✅ | repo add/update/pull（L12）；OCI 全链（L10/L11） |
| conan / nuget | ✅ | 见 §2.2 反转预核实（真客户端/四臂 curl） |
| 操作族 / Trash | ✅ | copy 200 messages[]、move 200、props 随行；trash：delete 204→GET 404→**restore 200**（"moving auto-trashcan/… completed successfully"）→ sha256 逐位一致 |
| community 负形态 | ✅ | webhook 写 403+License-Required 头；helmoci 建仓 400（"package type 'helmoci' is not available…"）；folderDownload 403 license 门（repo-operations）；archive 下载关态 403 逐字（见 §2.2） |
| 自动化面 | ✅ | `go test ./internal/webhook/` ok 3.9s；`./internal/adapter/helmoci/` ok 10.9s；`./internal/adapter/helm/` ok 11.7s（与 T-361 重载轮错峰的定向面；全树 race 归 T-361/T-377） |

### 2.2 断言反转预核实（三处，全部独立复证）

| 反转 | 判定 | 证据 |
|---|---|---|
| **conan D8 整树删（T-369）** | **PASS** | conan 1.66.0 真客户端（`BINFLOW_T308_CLIENT1_E2E=1`，本机独立执行）：访问日志序列 `DELETE /v1/conans/hello/1.0/myuser/stable → 200` → v1 GET **404** → v2 revisions **404** → **revisions/0/files/conanfile.py 404（客户端自己的修订 0 随树灭）**；conan 2.31.2 全矩阵 PASS（12.76s）。多修订夹具 = 旧姿势可分辨（latest 链删会留旧修订 → revisions 200） |
| **folderDownload 开关化（T-368）** | **PASS** | 默认（pro 无旋钮）：`GET /api/archive/download/…?archiveType=zip` → **403 "Download Folder functionality is disabled." 逐字**。开启（knob 实例）：**200** zip → 解包 fd/alpha.txt+fd/beta.txt，**sha256 与种子逐位一致**（eff06793…/11275b7a…）。匿名（enabled_for_anonymous=false）→ **401 "You must be logged in to download a folder or repository."** 逐字。**max_files=1 live 臂**（重启实例收紧旋钮）：2 文件目录 → **400 "Number of files under the path 't373knob/fd' (2) exceeds the max allowed file count for folder download (1)."** 逐字。`GET /api/v1/system/settings` 回显两族（默认 1024/5000/10/false + retention 14；knob 实例 enabled=true） |
| **nuget D-10 对齐 409（T-378）** | **PASS** | 自建夹具（自铸 nupkg + t373w 用户 + w-only 权限靶）：**arm4** 新包 admin → 201；**arm2b（本票翻转）同字节 + w-only 重推 → 409 "Package already exist: live.a/1.0.0/live.a.1.0.0.nupkg" 逐字**；arm2a 异字节 w-only → 409；arm3 admin（有 d）异字节 → 201 覆盖。谓词 byte-blind 由构造证实（同字节/异字节同判） |

### 2.3 契约变更面归属审计（`git diff m12-done..HEAD -- internal/ cmd/`）

- 范围内触及 internal/cmd 的提交 **13 条**，81 文件；文件→票号映射 **100%**：

| 提交 | 票 | 面 |
|---|---|---|
| 8b30177 | **T-362** | internal/webhook 新包 11 文件 + httpapi/{webhooks,server,properties,router} + repo 织入 + addons/slots 第 19 槽 + migration 018 + cmd 接线/槽计数 |
| 25e8afd | **T-364** | webhook/dispatcher + store/bus/sender/service/model 缝 + main.go 引擎组装 |
| e717e1b | **T-362**（测试补落） | httpapi 两测试文件 |
| 7afa947 | **T-366**（正文点名 "the dead key T-366's consumer leg caught"） | config/load.go 接线 + webhook_test |
| d274634 | **T-363** | adapter/docker remote 链 + repo/dockerremote + helmoci 读面 |
| af2f6c0 | **T-365** | docker/virtual.go + repo/dockervirtual + 四读 virtual 臂 |
| f3ab785 | **T-367** | remote/fetcher FetchAbsolute + repo/{config,remoteexternal} + adapter/helm 消费点 + httpapi/repositories 运输字段（票内已登记 area 外最小增量） |
| 019af12 | **T-367,T-366**（测试） | remote/t367_absolute_test |
| 12c3f48 | **T-369** | conan/v1.go 翻转 + 测试 |
| 6b209da | **T-368** | config 四文件 + httpapi/system_settings + 三测试件 + main.go 旋钮接线 |
| 7bb3039 | **T-371** | conan sweep/rewrite + docker 测试面（D-F2 迁移——**中期票刚落，其行为面归 T-377 终验复跑，本票未测**） |
| 6ac1695 | **T-378** | nuget v2.go 零值 BlobRef + nuspec 死代码清除 + 四臂测试 |
| **45e73c4** | **（无票号）** | **style: De Morgan 形式改写 internal/repo/operations.go 一处布尔式**（staticcheck QF1001，CI build 39 单发现，零语义变化；落点=M12 T-354 restore-leg seam） |

- **审计发现（唯一）**：`45e73c4` 主题行无 M13 票号——按「每处改动归属 M13 票号」的字面为**孤儿 hunk**（该文件同区间亦被 T-362 触碰，文件级不孤儿）。commit 正文完整自释（CI lint 门修复、零语义、套件绿），实质可归属（M12 seam 的 lint 追补）。**处置建议：conductor 记账即可**（P2；或随下笔 chore 补注票号归属）。除此之外**零孤儿**。
- 工作树未提交改动（不在 m12-done..HEAD diff 内）：`internal/adapter/deb/{handler_test,t327g_retention_test}.go` + `internal/repo/operations_test.go` 修改与四个 `race_{disabled,enabled}_test.go` 新文件 + `.circleci/config.yml` —— **全部属并行票 T-361（de-flake）在制品**（reports/agents/T-361.md 在档），非审计缺口。

## 3. 遗留 / 登记

1. **归属审计 1 无票号 style 提交（45e73c4，P2 记账）** — 见 §2.3。不阻塞（零语义、自释完整）。
2. **`_transitive` vs `_external` 折叠拼写（观察，非缺陷）**：我的双跳拓扑里 B 虚仓 index 折叠为 `_external/http/…` 而 T-367 #11 观察到 `_transitive/…`；两种拼写实测均 200 HIT、落盘/冻结行为一致。差异疑与成员 index 的折叠深度有关（remote 成员已自折叠 vs virtual 成员直挂）。建议 T-377 终验时以同一拓扑复核拼写规则是否照 helm.md §2/§3 逐字（行为面无缺口）。
3. **/v2 remote 面（helmoci/docker remote）不织入 artifact/cached**：v2 拉取（RemoteV2Plane 缓存填充）不触发 cached 事件（经典引擎 getRemote CacheMiss 臂才有——generic/helm 经典 remote 实测有）。与 T-362 §1-4 的织入位点一致（seam 边界），登记供 conductor/architect 确认是否属预期覆盖界。
4. **Replay REST/控制台面缺席**（死信可查不可 REST 重放）——T-364 §5-3 已登记，维持。
5. **环境限制记录**：宿主无 docker.io/central 出网（dind 经 save|load、maven 经 file-mirror 离线插件、无 nuget.exe/dotnet——D-10 以 curl 四臂 + 权限夹具等价覆盖，T-378 同姿势）；Jenkins dogfood 未探活（BOARD 指定替代形态已交付）。T-361 全树重载轮与本票并行（load 11~12.5），本票重负载腿（dind/kind）串行执行零 flake。
6. **T-371（D-F2 迁移，7bb3039）行为面未测**：票刚落（L17 域）——本票 AC 面为 L02~L13，T-371 归 T-377 终验（BOARD 关键路径一致）；其测试面已随本票自动化抽样中的 conan 包测试间接全绿（`go test ./internal/adapter/conan/` 两条真客户端腿 PASS）。

## 4. 清理

六实例 + 双接收器 + 双 python 文件服务全停（端口复核全 DOWN）；t373-dind 容器 rm；kind t373ns/release 清；helm 三个 repo/registry 凭据 logout；/tmp/t373 全删（含 venv/-license/测试凭据）；共享工作树零 git 操作、零代码改动（现存工作树改动均为 T-361 在制品与 docs-site 生成物，非本票引入）。
