# T-356 工作日志 — M12 终验（L01~L35 终态复验 + DoD 八条终核 + 断言反转核实 + 契约审计 + 收官检查）

- 角色：qa-engineer ｜ 状态：**done——总裁定 FAIL（单一 P1 实现缺口：协议仓 restore 索引联动未接线〔T-354 登记遗留，无票承载〕+ 一枚 P2 测试断言过期〔L27a 17→18〕；其余全部绿——含中期 5 红全数翻绿、18 槽矩阵、真实客户端全族、四闸门）**
- 被测：HEAD `d41f6d0`（工作树 = bfdc8c6 + sprint 938 报告；M12 全部实现票在档）
- 工具链：go 1.26 / nuget.exe latest（mono 容器）+ dotnet 8.0（容器）/ docker 29 dind（insecure-registry）/ helm 4.2.4 + kind bf-ga（v1.36.1 真集群）/ maven 3.9（容器，temurin 21）/ npm 10.9.8 / pip + twine / conan 2.31.2（venv 新装）/ cargo（curl 面）/ MinIO + mc（容器）/ playwright（chromium+m9+m10，workers=2 CI 姿势）
- 纪律遵守：共享工作树**零 git 写操作**（审计只读 log/diff/status）；零产品代码与测试代码改动（唯一工作树新增 = make docs 生成物）；license 用 `./binflow-license-private.pem` 真签名（kid=bf-lic-2026，pro.lic 30 天，f8de6703）；测试凭据全驻 /tmp/t356（会话 scratch，已清）；误入仓根的测试文件 t.bin 已删（git status 复核零非生成物改动）

## 0. 实例编排（scratch 双形态 + 专用腿）

| 实例 | 端口 | 形态 | 用途 |
|---|---|---|---|
| community | 18380 | 无 license | 门控负臂/硬删/P0 抽样 |
| pro | 18381 | 真签名 pro.lic | NuGet/操作族/Trash/conan/cargo/HelmOCI/npm/maven/pip/helm 全量 |
| dual-write | 18390 | [filestore,s3]+dual-write（MinIO 19000） | L18/L19 fail-open 全链 |
| e2e 复刻 | 8080 | 裸起 + seed-m8/m9/m10 | 全量 playwright（CI 同姿势 workers=2） |

## 1. 中期 5 红逐一复验（全部翻绿）

| # | 项 | 判定 | 关键证据（HEAD 实测） |
|---|---|---|---|
| D-1/D-2 | nuget.exe 双腿（bb69d76） | ✅ | **nuget.exe（mono）list 经 v2 源出包**（`t356.sample 1.1.0`——裸 `$filter=IsLatestVersion` 200+2 entries）；**install 全链落盘**（content/x.txt 抽出 + .nupkg sha256=服务器字节 a98f9f9a 逐位一致——standalone entry `xmlns:d/m` 声明在场，`'m' is an undeclared prefix` 形态消灭） |
| D-3 | dual-write boot panic（bb69d76） | ✅ | 双写实例**正常起服**（`storage chain resolved providers=filestore,s3 mode=dual-write`）；五 gauge 全部裸名（`binflow_replay_{queue_depth,window_open,drained,failed_retry,failed_permanent,source_gone,read_fallback}`——`_total` 后缀消灭）；**停机窗全链**：MinIO stop → PUT×3 全 201 + GET 新旧全 200 字节一致 + depth=3/window_open=1 → 窗口期重启队列幸存（盘上 per-sha 文件）→ MinIO start → **水位行** `watermark depth=3 drain=scheduled` → 1s 级排空 `drained:3 source_gone:0 permanent_failed:0` → **mc 侧 6 对象逐 sha256 对账零缺**（blobs/<2hex>/<sha> 布局，3 个窗前 + 3 个排空时间戳分野清晰） |
| D-4 | 协议仓索引联动（65b1832） | ✅❌ | **copy 腿绿**：npm publish → 仅搬 tarball 目录（packument 留源仓，目标 404）→ 观察者异步重建 → 目标 packument 200（versions=[1.0.0] latest=1.0.0）→ **真 `npm install` `added 1 package in 325ms`**（notarget 翻转）；**restore 腿仍断**：`npm unpublish` → versions [] → restore 200 + tgz 回 200 字节一致 → **packument 仍 []** → npm install 仍不可用，且 **npm 无 reindex REST 逃生口**（四包型 reindex 族只覆盖 conan/helm/deb/yum）——`internal/repo/operations.go:1165` 缝条件 `OpCopy && !pl.system` 钉死 copy-only，系统身份 move（restore）连带不触发。**= 本票唯一实现缺口（见 §6 缺陷 D-356-1）** |
| D-5 | forceConanAuthentication + cargo 409 记账（4cca52d） | ✅ | **forceConanAuthentication 三层落**：REST PUT/POST true/false 双臂显式回显（configuration.forceConanAuthentication）；**真 conan 2.31.2**：匿名 `conan list` → "Remote needs authentication, obtaining credentials"（引导登录形态）→ `remote login` 成功 → upload/list 全绿；**cargo 死上游双姿态**：默认（hardFail 关）→ **404 unfound**（"assumed offline (no cached copy; retry later)"）/ hardFail=true → **409 + errors 信封**（"hardFail enabled"）——与 cargo.md §8.1 R-3 登记逐字一致；`.cargo/**` PUT 405 收敛面在场。**记账修正未落**：PRD FR-110.4/AC3 与 BOARD T-340 AC2 仍写「死上游 search → 409」单臂（T-355A §2.3 修正案未执行，见 §6 D-356-3） |
| D-6 | T-342 HelmOCI 全链（0b45877） | ✅ | community 建仓 **400 点名 helmoci/pro**；pro 200；**helm 4.2.4 全链**：registry login（--plain-http）→ push（Digest sha256:d7bc0ddd…）→ **manifest 三 media type 齐**（config `application/vnd.cncf.helm.config.v1+json` / layer `…chart.content.v1.tar+gzip` / **layer `…chart.provenance.v1.prov`〔gpg 签名后自动附加〕**）→ pull 字节一致（sha256 bde18323 双侧同）→ digest 寻址 GET 200 → tags/list → `/v2/_catalog` 含 helmoci 镜像 → helm show 全字段回显 → **helm install 上真 kind 集群（pod 1/1 Running）→ uninstall 干净** → **`.prov` 经 OCI 面 `helm pull --verify` → `Chart Hash Verified: sha256:bde18323…`**；混仓 virtual（helm+helmoci）**400 逐字**；docker dind 同实例 push/pull/run 零回归 |

## 2. ⚠️ 7 项复核

| # | 项 | 终态 | 证据 |
|---|---|---|---|
| L02 | $filter bare（D-1） | ✅ 翻绿 | `Search()?%24filter=IsLatestVersion&semVerLevel=2.0.0` → 200 + 2 entries；eq true 形态维持 200 |
| L03 | 同字节幂等（D-10） | ⚠️ 维持登记 | **同字节 + 仅 w 权限主体重传 → 201**（nuget.md §5.1 臂②字面应 409「包已存在且无 d 权限」；不同字节同主体 → 409 `Package already exist: <path>` 逐字 ✓；admin（有 d）→ 201 覆盖 + sha 新值 ✓）——规格-实现分歧未上 BOARD，维持 P2 登记 |
| L04 | 真客户端+门控 | ✅ | nuget.exe 双腿绿（§1）；门控三缝 = m10-tier-matrix 0 deviations + 18 槽断言 + 活体（community 400/pro 200） |
| L12 | flat 措辞 | ⚠️ 维持登记 | `/api/flat/*` → 404（repo-operations.md §1.6 规格裁定：flat 折叠进 copy 主参数族）；dryRun **零副作用实证**（dry 200 → 目标 404 / 源 200 对照）；PRD「flat 扁平化」措辞待 PM 回写（文面债，非缺陷） |
| L14 | folderDownload 旋钮（D-7） | ⚠️ 维持登记 | 默认关 403 `Download Folder functionality is disabled.` 逐字 ✓；开启旋钮未落（api-reference 自注「配置旋钮未落」）；P2 在案 |
| L17 | retention 旋钮（D-8） | ⚠️ 维持登记 | trash-can.md 已知边界表如实登记（恒 enabled/14d，配置域小票承载）；保留期 cron 单测在中期绿（13/15 天臂），本轮未重跑（无代码变更面） |
| L31 | env 拒启次序（D-9） | ⚠️ 裁定翻转 | **T-349 已把该姿态设计化**（commit 0a729d0 明文：「incomplete S3 env group **yields to binstore.yaml's own semantics when the file is present**」）；本轮复现确认：binstore.yaml 在场且 schema 错 → binstore 解析错先出；无 binstore/可解析 → env 拒启点名缺键先出（`storage.s3.secret_access_key is required when backend=s3`）。**实现与 PRD FR-113 AC5「先于 binstore 报错」字面冲突 → PM 回写 AC5 措辞**（D-356-4 记账） |

## 3. ✅ 24 项承证核定 + 抽样复验

- **影响面核定**（m11-done..HEAD 全 15 条代码提交逐条）：bb69d76（nuget v2 面）后无提交再触 `internal/adapter/nuget` 的 v3 search/proxy 路径 → T-351 L05~L09（v3 回归/动态解析/remote 代理/virtual 合并/降级）承证成立；本轮补 v3 面 HEAD 抽样（index.json 16 资源族 / flatcontainer versions / registration page / query totalHits=1 / **dotnet 8 add package + restore 真客户端绿**）。归档族/Trash/操作族/conan/cargo 由本轮全量重测覆盖（见 §1/§2）。
- **L01/L10 规格走查承证**（nuget.md 258 行 / repo-operations.md 221 行，T-351 已核；无修订）。
- **L11 copy/move 全链重测绿**：树级 200 messages[]；**属性随行**（`PUT ?properties=team=qa` → copy 后目标 `{"team":["qa"]}`）；copy 源留 / move 源失；community 403 + `X-Binflow-License-Required: repo-operations`；dryRun 零副作用。
- **L14 归档族重测绿**：`archive!/` 成员字节一致 + 缺员 404；**explode 反转重证**：`X-Explode-Archive` zip → 201 + `X-Binflow-Exploded-Files: 2` + 原件不落库；非白名单 .rar → 400 维持。
- **L15~L17 Trash 重测绿**：五元组打标（time/deletedBy/originalRepository/originalRepositoryType/originalPath 全五键）；restore → sha256 逐位一致 + trash.* 剥净（properties 空）；empty 权限门（plain user 403）+ community 403 + `X-Binflow-License-Required: trashcan`。
- **L20 资源门重跑**：`make footprint EXPECT=1` **GREEN**（vmmap 10.1MB ≤100MB，冷启动 81ms <2s）；`make build` check-size **94.13MB ≤100MB 维持**。
- **L21/L22 HelmOCI**：见 §1 D-6（本轮全量）。
- **L23 conan**：D-F 三臂（`_/_` 坐标混批 → **200**；未知 ref → 404 `Path not found`；非法 pid → 400）+ forceConanAuthentication（§1）+ conan 2 真客户端全链；1.x 回归 = T-340/T-355A gated 真机承证（无代码变更面）。
- **L24 cargo**：双姿态 + `.cargo` 收敛（§1）。
- **L25/L26 MUI 四闸门重跑**：锚册 ledger（全量 playwright 内含）/assert-tokens（tsc+eslint 于 T-351 在案，无 web/ 变更）；**全量 playwright 209 passed/2 failed/23 skipped（6.8m，CI 姿势）**——1 failed = N01 已知 straddle flake（**串行复跑绿 ✓**，协议通过）、1 failed = **L27a 槽位数断言过期（17 vs 18，确定性红）= D-356-2**；gzip 预算：SPA 无变更（T-351 +20.01% 承证，web/ 零 FE 提交后无漂移）；**m12 QA spec 双形态重跑**：community 锁定腿（在套件内）+ pro 双腿（BASE=18381 定向：capture→浏览→五元组详情→restore roundtrip ✓ / 审计 picker ≥54 词含 cleanup.run/trash.restore ✓）。
- **L27 回写批承证**：architecture §15.4/§23、cargo.md §8、conan.md D-F 行（T-347/T-348/T-340 已落）；D-G/D-H grep 零残留维持；`make docs` **SUCCESS 4.20MB** 零断链（本轮终跑）。
- **L28/L29 重测绿**：socketTimeoutMillis 新拼写回显 2500 / 旧拼写接受回显新拼写 3500（别名链）；byHash 非法值 → **400 逐字**（`must be one of ALL, SHA256, NONE (debian.md section 5)`）；策略键表单 = repo-policy-keys.spec 3 测试在套件内绿。
- **L30 token 窄域承证**：uploads/token 路径自 732ff7c 后零产品 diff（T-355A 触 repositories.go、T-342 触 license_gate.go，均不在 MPU token 面）；T-351 真机证据（原路径 201/他路径 403 逐字）承证。
- **L32 CI runner**：`.github/workflows/ci.yml` e2e 专用 job 在盘（真二进制+三 seed+三项目 workers=2，AUTHORITATIVE 声明）；本机同姿势复刻 = 本轮全量跑（N01 串行绿协议验证了 de-flake 处置路径）。
- **L33/L34**：见 §4/§5；100 并发混合负载（NuGet download+query+操作族 dry-run+archive!/）**200 请求全 200 零 5xx**（6.6s + 2.76s 两轮）。
- **L35 文档**：api-reference M12 速览命令本轮逐条实跑对得上；**收官文档漂移清单见 §7（D-356-3 半 + D-356-5）**。

## 4. 断言反转核实（DoD#3）

| 反转 | PRD §5.4 | 实现 | 判定 |
|---|---|---|---|
| nuget v2 404 → 路由全集 | 行在 | 本轮 17 端点 sweep 全绿（service doc/$metadata/Search±count 命中与 404 空臂/FindPackagesById±count（未知 id 空 feed 200/缺参 404）/Packages()±单条±/Id/GetUpdates±count（同版 404 空臂+旧基 200）/`$batch` 202+multipart 回段+DataServiceVersion 2.0+Unsupported entry 400 整批失败/Download sha256 对账/裸 .nupkg/DELETE 200→404/PUT 双形态（multipart base 根 + 直传路径）） | ✅ 三处一致 |
| virtual search 缓存 → 上游代理 | 行在 | T-351 L07/L08 实证承证（nuget v3 路径零后续 diff）+ 本轮 v3 面抽样绿 | ✅ |
| X-Explode 400 → 接受 | 行在 | 本轮重证（201 + 计数头 + 非白名单 400 维持） | ✅ |
| footprint 红 → 绿 | 行在 | `make footprint EXPECT=1` GREEN | ✅ |
| **cargo 409 记账修正（T-355A §2.3）** | **未执行** | 代码/规格（cargo.md §8.1）/用户文档（cargo.md L181 双姿态）三处一致；**PRD FR-110.4/AC3 与 BOARD T-340 AC2 仍单臂 409 旧文** | ❌ 记账缺口（D-356-3） |

## 5. DoD 八条终核

| # | DoD | 判定 | 证据 |
|---|---|---|---|
| 1 | P0/P1 AC 全绿；条件票按余量 | ❌ | P0 全绿（FR-103/104/105 copy-move/108）；P1：FR-105 归档族 ✓/107 ✓（本轮真 MinIO 全链）/109 ✓/110 ✓（D-5 双件+D-F）/111 ✓/112 ✓/113 ✓；**FR-106 AC2 协议仓臂 ✗（restore 索引不重算，D-356-1）**；条件票：symbol server Q2 未触发（BOARD 留痕在案）/conan 活体互证 Q5 不可得留痕 ✓/D-3 Q6 评估结论在 T-342 报告 ✓（建议 M13）/chartsBaseUrl P2 段余量后置（T-342 §5，conductor 授权在案） |
| 2 | §8 剧本 + 量化门槛 | ❌（一行） | v2 覆盖 ✓/search 时效 ✓（承证）/动态解析 ✓/操作族 ✓（copy 侧）/回收站 ✗（协议仓 restore 臂）/fail-open ✓/资源门 ✓/HelmOCI ✓/包型收尾 ✓/MUI ✓/回写 ✓/遗留 ✓；**18 槽矩阵活体**：`/api/v1/addons` = 18 槽（13 package-type：5 community + 8 pro 含 helmoci；5 feature：properties community、repo-operations/trashcan pro、ha/xray enterprise 占位）+ 两条 18 计数 wiring 断言（make test 内绿） |
| 3 | 回归硬门槛 + 反转回写 | ✅ | M1~M11 P0 抽样双形态（docker dind 同 digest 链 push/pull/run、maven deploy→resolve→exec `hello from binflow t356 maven`、npm publish+install、pip twine+install+import、curl generic、helm 经典 repo add/search/pull sha 同、conan/cargo/nuget 全链）；契约归属 §6 审计 100%；四处反转核实（§4，cargo 记账除外——归 D-356-3 文面） |
| 4 | 前置产物 + Q3/Q4 终裁 | ✅ | nuget.md/repo-operations.md/ADR-0040 Accepted（K40~K43 校准在案）；Q3 trashcan=pro、Q4 repo-operations=pro 均活体三缝 + 文档在档 |
| 5 | tech-writer 五类增量 | ⚠️ | 四类交付且命令可复跑（本轮逐条实跑）；**HelmOCI 接入文档缺席 + 三处 post-hoc 漂移**（§7 收官检查——T-347A 先于 T-342/T-355A 落稿） |
| 6 | NFR + 闸门 | ✅（两笔登记） | P53 ✓（100 并发零 5xx）/P54 ⚠️→✓（万节点 copy 单测隔离绿×3；满载下 dry-run 预算臂超时 = 满载 flake 族新成员，登记观察③）/P55 ✓/P56 ✓（承证，web/ 零漂移）/P57 ✓；**`make test`（race 全树）：32 ok + 2 FAIL**（deb 满载 flake 族〔T-329 以来在案〕+ repo TestBigTreeCopyNo5xx 预算臂）——**两包隔离复跑绿×3 + 全包静载复跑绿**，按既有协议环境性归因；`make lint` 0 issues / gofmt 空 / 四矩阵门 0 deviations ×2 / make docs SUCCESS；**默认并发 e2e：1 确定性红（L27a 槽数 17→18 过期断言，D-356-2）+ N01 串行绿** |
| 7 | Non-goals 对账 + 出处义务 | ⚠️ | §2.2 滚程去向全登记（PRD 表内）；**ROADMAP「M12 未纳入项」段未建**（M11 期 D-R1 同款收口动作，归 conductor/PM——与 D-356-5 同批）；19:05 出处义务抽查（nuget.md/conan.md/cargo.md/helm.md 出处列）在 |
| 8 | tag m12-done | —（conductor） | 前置 = 本报告 + D-356-1 修复复验 + 收官笔头清单（§8） |

## 6. 契约归属审计（m11-done..HEAD）

- 非报告提交 35 条（152 条中 117 条为 sprint chore）；**触及 `internal/` `cmd/` 的代码提交 15 条，100% 主题行带票号**（T-333~T-355A 全谱：333/334/335/336/337/338/339/340fix/341/343/344B~E/345/346/349+350/352+353〔FE，零 internal 触碰——L26 契约 diff=0 维持〕/354/355A/342 + bb69d76 T-351 修复票）。docs/chore 提交（PRD/ADR/规格/团队配置）均有人工归属或流程标记。
- REST 新面核对：copy/move、archive/download、trash 三动词、explode 双头全部在 api-reference M12 速览；helmoci 复用 /v2 族（无新 REST 路由）；ReindexDirs 为进程内缝（无路由）；forceConanAuthentication 为配置字段（api-reference 字段表漂移归 §7 docs 批）。
- **票号簿记备注**：BOARD 拆票表 T-354=tech-writer、T-355=release；实际派发复用 T-354（观察者接线）/T-355A（conan+cargo 缺口），tech-writer 落在 T-347A、**T-355（release 部署烟测 + UAT 链）未执行且无报告**（M11 期 L11 以 CD 链承证处置；M12 建议随里程碑 PR 首跑 UAT 补实证，或 conductor 显式留痕滚动）。

## 7. 收官检查（用户规程：README 双语 / 文档站 / 漂移清扫）

- **README.md / README.zh-CN.md（T-347A 刷新）**：M12 叙述段六面（操作族/Trash/NuGet/fail-open/MPU/MUI）与 HEAD 行为一致——本轮把段内命令族逐类实跑（community 403 门/pro copy messages[]/trash 五元组/restore/empty）**全部对得上**；「in progress/进行中」措辞 = tag 时例行翻转（T-329 先例）。**漂移**：① pro 档能力表与 M12 段**均无 helmoci**（T-342 落于 T-347A 之后）——双语同缺。
- **文档站**：`make docs` SUCCESS 4.20MB、新篇路由全生成、断链 0。**漂移三处**：② `docs/user/integrations/conan.md:148`「forceConanAuthentication 仓配置｜未实现」——**STALE**（T-355A 已落地，本轮真客户端证绿）；③ `docs/user/integrations/helm-charts.md:161`「helmoci 包型｜…待其落地」——**STALE**（T-342 已落地）；④ `docs/user/admin/license.md` 与 `docs/user/README.md:45` 槽位矩阵 **17** → 应为 **18**（+helmoci 行）。**缺口一处**：⑤ PRD §8-10 五类文档的「HelmOCI 接入（与经典仓辨析）」篇**未写**（T-347A 排期时 T-342 未裁定派发）。trash-can.md「FE 未落」注记 = **已清**（无残留；该篇对 T-352 回收站管理页面的提及可随 docs 批补一句，非漂移）。
- **trash-can.md / artifact-operations.md 新页与 HEAD 对齐**：逐命令实跑（浏览/五元组/restore/empty/clean/门控/报错对照 + copy/move/dryRun/archive!/explode/目录 zip 默认关）**行为、状态码、文案逐字对得上**；已知边界表（配置旋钮/档位/docker manifest/覆盖入站）与实现态一致（如实登记维持）。

## 8. 缺陷与收口前置清单

**产品/测试缺陷（建议 m12-done 前清）**：

| # | 级 | 摘要 | 复现与修法 |
|---|---|---|---|
| **D-356-1** | **P1（DoD#1/2 阻塞位）** | **协议仓 restore 索引联动未接线**：npm（及全部协议仓）trash restore 后 tgz 回位但派生索引不重算——npm packument 维持空 → 真 `npm install` notarget；npm 无 reindex REST 逃生口（conan/helm/deb/yum 有）。FR-106 AC2「（协议仓）索引重算断言」未兑；T-354 §5.1 已登记修法但**无票承载**（sprint 934 记「一行裁定路由」后未派） | 复现：npm publish → unpublish（versions []）→ `POST /api/trash/restore/...` 200 → tgz GET 200 → packument 仍 []。修法（T-354 拟）：`internal/repo/operations.go:1165` 缝条件扩为「copy 任意非系统目标 ∪ **系统身份 move 且目标 ≠ auto-trashcan**」+ 契约注释 + trash_test 补臂（move 腿 = 规格钉死 copy-only，维持登记分歧，勿同票翻转） |
| **D-356-2** | P2（DoD#6 确定性红） | `web/e2e/m10/console-license-addons.spec.ts:68` `toHaveCount(17)` **过期**——T-342 槽位 17→18 后该 UI 断言未随改（cmd 侧两条已改，UI spec 漏）；UI 实际渲染 18（与 API/wiring 一致，**产品面正确，测试面过期**） | 修：17→18 + 注释补「T-342 helmoci 后 = 18」；修后全量 e2e 即恢复确定性全绿 |
| **D-356-3** | P2（记账） | cargo 409 修正三处文面未落：PRD FR-110.4「对齐 Artifactory 409」句 + AC3「死上游 search → 409」/ BOARD T-340 AC2 同款（T-355A §2.3 修正案：双姿态——默认 404 unfound / hardFail 409+信封，照 cargo.md §8.1 R-3） | PM 改 PRD 两处 + conductor 改 BOARD 一处；行为已三处一致（代码/规格/用户文档），纯文面 |
| **D-356-4** | P2（记账） | env 拒启次序：T-349 设计化「binstore.yaml 在场时其语义优先」与 PRD FR-113 AC5「先于 binstore 报错」字面冲突（实现自洽且 commit 明文） | PM 回写 AC5 措辞（或裁回实现——两动作二选一，勿悬置） |
| **D-356-5** | P2（文档批，tech-writer） | §7 漂移五处：README 双语 pro 表 + M12 段补 helmoci；conan.md:148 翻转「已实现（M12 T-355A）」；helm-charts.md:161 翻转已落地；license.md/docs README 槽位 17→18；**新写 HelmOCI 接入篇**（push/pull/--verify/install + 与经典仓辨析 + 混仓 400）——落笔后重跑 make docs | 一个 tech-writer 小票收口（含 ROADMAP「M12 未纳入项」段，见下） |

**收口笔头（conductor/PM，随里程碑 PR）**：⑥ ROADMAP 建「M12 未纳入项」段 + 滚程登记（DoD#7 字面）；⑦ T-355（release 烟测/UAT）未执行的处置留痕（随里程碑 PR 首跑 UAT 或显式滚动）；⑧ tag 时 README「in progress」例行翻转；⑨ 观察登记（不阻塞）：满载 flake 族新成员 TestBigTreeCopyNo5xx 预算臂（隔离绿×3，CI 专用 runner 权威重申）；dual-write **整进程在 S3 全停窗内重启 = bucket 探针拒启**（fail-closed 既有姿势，T-338 按「重启排在恢复后」验证且 ADR-0040 未修订 boot 探针——队列在盘幸存 + 恢复后水位/排空本轮实证；PRD AC2「停机窗内重启」措辞与 as-built 有解释空间，PM 可加注）；D-10 同字节幂等分歧维持待裁。

## 9. 总裁定

# **FAIL（单一 P1 实现缺口 D-356-1 + 一行测试断言 D-356-2；修复面极小且修法在案）**

- 中期 5 红**全数翻绿**（nuget.exe 双腿/fail-open 全链含 mc 对账/索引联动 copy 侧/forceConan+cargo 双姿势/HelmOCI 真集群全链）；⚠️ 7 项中 3 翻绿 4 维持登记（均 P2 文面/旋钮级）；✅ 24 项承证核定 + 抽样全绿。
- 闸门：race 全树 32 ok + 2 环境性 FAIL（隔离×3 绿）；lint 0/gofmt 空/四矩阵 0 deviations/footprint GREEN/check-size 94.13MB/docs SUCCESS；playwright 209/2/23（1 flake 串行绿 + 1 确定性过期断言）。
- 真实客户端全族绿（nuget.exe/dotnet/docker/maven/npm/pip/conan/helm/kind 装机/cargo 面）。
- **建议收口序**：① D-356-1 小票（repo 缝一行 + 测试臂，dev-go-core，小时级）→ QA 复验该臂（npm restore→install + deb/conan 抽样）即可翻 PASS；② D-356-2 一行测试改（可与①同票或 conductor 直改）；③ D-356-3/4/5 笔头批随里程碑 PR；④ 然后 conductor 走 m12-done + 里程碑 PR（对外发布制品先经用户确认——红线维持）。

## 10. 清理

- 实例 18380/18381/18390/8080 全停；t356-minio/t356-mc/t356-dind 容器 rm；kind 集群零 t356 残留（release uninstalled、pod 清）；/tmp/t356 数据目录已清（工具 venv/license/gpg 测试钥会话 scratch，不入仓）。
- 共享工作树：零 git 操作；误入仓根的 t.bin 已删并复核；现存改动仅 `make docs` 生成物（docs-site/ + internal/docs/dist——收官文档批落笔后 conductor 统一重跑提交）。
