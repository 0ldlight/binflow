# T-144 [P0] QA: debt consolidation full chain + four-milestone regression baseline

**Agent**: qa-engineer
**Status**: qa
**Date**: 2026-08-21

## 测试环境

- 二进制：`/tmp/binflow-server`（`go build -o /tmp/binflow-server ./cmd/binflow-server/`）
- 配置：`/tmp/t144-binflow.yaml`（listen :18080, metadata dsn /tmp/t144-qa-data/bf.db, storage data_dir /tmp/t144-qa-data, anonymous_access=true）
- 管理密码：`BINFLOW_ADMIN_PASSWORD=TestAdmin123!`
- Playwright：`BASE=http://127.0.0.1:18080 ADMIN_PW=TestAdmin123!`

## G30a~G30d: FR-44 目录实体化

### G30a: 深度上传隐式目录 200

| 步骤 | 验证 | 结果 |
|---|---|---|
| 创建 generic-local 仓库 | PUT /api/repositories/generic-local | PASS |
| 深度上传 a/b/c/d.bin | PUT /generic-local/a/b/c/d.bin | 201 |
| 祖先目录 GET | /api/storage/generic-local/a/ → 200 FolderInfo | PASS |
| 祖先目录 GET | /api/storage/generic-local/a/b/ → 200 FolderInfo | PASS |
| 祖先目录 GET | /api/storage/generic-local/a/b/c/ → 200 FolderInfo | PASS |
| 子目录列表 | children 字段包含 d.bin | PASS |

### G30b: 删除后空链 prune 三态

| 步骤 | 验证 | 结果 |
|---|---|---|
| 删除 d.bin | DELETE /generic-local/a/b/c/d.bin | 200 |
| 空链自动 prune | 所有祖先目录 404 | PASS |
| 同目录兄弟文件保留 | 上传 a/b/c/d.bin 和 a/b/c/e.bin，删除 e.bin，c/ 保留 | PASS |
| 显式 mkdir 空目录 | mkdir 后上传子文件，删除子文件，空目录 prune | PASS |

### G30c: 007 迁移幂等

| 步骤 | 验证 | 结果 |
|---|---|---|
| 全新 data dir 启动 | 深度上传后所有祖先目录 200 | PASS |
| 007 迁移回填 | 幂等、确定性 | PASS |

### G30d: 导出/导入往返

| 步骤 | 验证 | 结果 |
|---|---|---|
| 导出 | `binflow-server export -output /tmp/t144-export-dir` | 成功 |
| 导入 | 新鲜 data dir 导入，7 folder + 2 file 行完整保留 | PASS |
| FolderMarkerSHA 排除 | 64x'0' 不在 manifest 中 | PASS |

**G30a~G30d: ALL PASS**

## G30e: Playwright FE 回归

| 测试 | 描述 | 结果 |
|---|---|---|
| G30e-1 | 零 /api/search 请求（上传 + mkdir + 导航） | PASS |
| G30e-2 | typo 深链 404 EmptyState 呈现 | PASS |
| G30e-3 | mkdir 单段回归（一条 PUT） | PASS |
| G30e-4 | 大目录分页回归（30 files） | PASS |

**修复**：测试文件 `t131-g30e.spec.ts` 中 `[data-testid="mkdir-name"]` → `[data-testid="tree-mkdir-input"]`，`[data-testid="breadcrumb-root"]` → `[data-testid="tree-breadcrumb"] button.crumb:first-child`，对齐实际 FE 实现。

**G30e: ALL PASS (4/4)**

## G31a~G31c: Token 审计

### G31a: token.issue 审计事件

| 步骤 | 验证 | 结果 |
|---|---|---|
| 创建 token（form） | 审计事件 `token.issue`，fingerprint=sha256[:8]，ttl_seconds | PASS |
| 创建 token（JSON） | TTL=3600 正确 | PASS |
| expiresIn=0 | ttl_seconds=0（永不过期） | PASS |
| 明文零出现 | detail 中无原始 token 明文 | PASS |

### G31b: token.revoke 审计事件

| 步骤 | 验证 | 结果 |
|---|---|---|
| 按值吊销 | 审计事件 `token.revoke`，fingerprint | PASS |
| 按 ID 吊销 | 审计事件 `token.revoke`，token_id | PASS |
| 吊销后重放 | 401 | PASS |
| 二次吊销 | 无审计事件（幂等） | PASS |

### G31c: docker /v2/token 隔离

| 步骤 | 验证 | 结果 |
|---|---|---|
| GET /v2/token | 无 token.issue 审计事件 | PASS |
| POST /v2/token | 无 token.issue 审计事件 | PASS |

**G31a~G31c: ALL PASS**

## G32: Docker 树视图（Playwright e2e）

| 测试 | 描述 | 结果 |
|---|---|---|
| G32a-1 | 零 /v2/* 请求（树浏览全走 storage API） | PASS |
| G32a-2 | tag badge 渲染（tag-badge-latest / tag-badge-v1） | PASS |
| G32a-3 | 数据一致性（树 tag 集与 tags/list 一致） | PASS |
| G32b-1 | 列头「摘要」/「标签」；manifest digest 截断 | PASS |
| G32b-2 | root 级 image 目录列表 + breadcrumb + 零 /api/search | PASS |
| DC-02 | 帮助链接 → /binflow/docs/ | PASS |

**G32: ALL PASS (6/6)**

## G33: Storage URI 基址族修正

| 端点 | 验证 | 结果 |
|---|---|---|
| item info | uri = `/binflow/api/storage/{repo}/{path}` | PASS |
| ?list | uri 族一致 | PASS |
| ?permissions | uri 族一致 | PASS |
| 自洽性 | curl URI 返回 200 | PASS |

**G33: ALL PASS**

## G34: 四里程碑回归基线

### M1 C 序列（storage, auth, metadata, repo）

```
ok  github.com/lzwzzy/binflow/internal/storage   19.012s
ok  github.com/lzwzzy/binflow/internal/auth       6.238s
ok  github.com/lzwzzy/binflow/internal/metadata    4.137s
ok  github.com/lzwzzy/binflow/internal/repo       28.001s
```

### M2 D 序列（docker adapter）

```
ok  github.com/lzwzzy/binflow/internal/adapter/docker   23.712s
```

### M3 M 序列（maven, npm, pypi adapters）

```
ok  github.com/lzwzzy/binflow/internal/adapter/maven   29.516s
ok  github.com/lzwzzy/binflow/internal/adapter/npm     28.251s
ok  github.com/lzwzzy/binflow/internal/adapter/pypi    11.071s
```

### M4 W 序列（httpapi, docs, cmd）

```
ok  github.com/lzwzzy/binflow/internal/httpapi          49.683s
ok  github.com/lzwzzy/binflow/internal/docs              0.798s
ok  github.com/lzwzzy/binflow/cmd/binflow-server        10.813s
```

### Race 竞态检测

```
ok  github.com/lzwzzy/binflow/internal/repo      83.764s  (race)
ok  github.com/lzwzzy/binflow/internal/httpapi  110.584s  (race)
```

**G34: ALL PASS（四序列 P0 全绿）**

## §5.6 反转表断言验证

| 既有断言 | M5 起期望 | 验证结果 |
|---|---|---|
| 隐式目录 GET → 404 | **200 FolderInfo** | PASS（`/api/storage/generic-local/implicit/dir/` → 200） |
| docker 树 image 目录不可浏览 | **可浏览** | PASS（G32 Playwright 6/6） |
| token.issue/token.revoke 词表缺位 | **可查 >= 1** | PASS（audit API 返回 token.issue + token.revoke） |
| --version = dev | `<VER>`（裸构建回退 dev） | PASS（裸构建预期 dev，goreleaser 注入版本） |
| /binflow/docs/** → 404 | **200** | PASS（root 页 200） |
| M1 C / M2 D / M3 M / M4 W 序列 | **全绿** | PASS（全部 OK） |

## FR-44-AC6 专项

| 验证项 | 方式 | 结果 |
|---|---|---|
| docker 套件全绿 | `go test ./internal/adapter/docker/...` | PASS |
| governance 拒绝上传零祖先残留 | 代码审查：governance.permitPut 在 putNode 之前 | PASS |
| 并发同目录上传无 unique violation | 代码审查：`ON CONFLICT DO UPDATE` UPSERT | PASS |

## 综合结果

| 验收区 | 测试数 | 通过 | 失败 |
|---|---|---|---|
| G30a~G30d（目录实体化） | 4 | 4 | 0 |
| G30e（Playwright FE） | 4 | 4 | 0 |
| G31a~G31c（Token 审计） | 3 | 3 | 0 |
| G32（Docker 树视图） | 6 | 6 | 0 |
| G33（URI 基址族） | 1 | 1 | 0 |
| G34（四里程碑回归） | 1 | 1 | 0 |
| §5.6 反转表 | 6 | 6 | 0 |
| FR-44-AC6 | 3 | 3 | 0 |
| **总计** | **28** | **28** | **0** |

**全量验收通过。票状态：qa → done。**

## 遗留项

- e2e Playwright 测试尚未在 CI 中执行（同 T-134 遗留项）
- `--version` 裸构建回退 `dev` 为预期行为（goreleaser `-ldflags` 注入正式版本号）
- `t131-g30e.spec.ts` 中修复了两处 data-testid 不匹配（`mkdir-name` → `tree-mkdir-input`，`breadcrumb-root` → `tree-breadcrumb button.crumb:first-child`）