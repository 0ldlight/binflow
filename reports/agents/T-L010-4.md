Ticket:        L010-4 — R-6 搭车取证窗（单窗口封顶一节，architect 排程裁定版）；优先级随 LOOP 010 轨道
Role:          devops-engineer（本票为参照实例取证窗执行，非 CI 面改动）
Area:          参照实例运行时取证（docker/binarystore.xml/system.yaml 操纵 + 恢复），零仓库 CI 面写入
Input:         conductor 派发（两发协议+双败定谳路径+产出路径）；自读 L003-s3-chain-evidence.md（恢复配方三验）、s3-storage-layout.md（阈值语义）、容器启动链（artifactory.sh:45 extraJavaOpts→JAVA_OPTIONS 合流 artifactoryCommon.sh:125）
Changes:       1) 双侧备份 binarystore.xml+system.yaml（sha 三份核对）；2) 基线标记制品 l0104（PUT 201/GET 200 sha1 一致）；3) 一次性 MinIO 起立+建桶+双侧探针 200；4) 第一发：s3-storage-v3 换链（path-style/httpsOnly 子元素双保险+enableSignedUrlRedirect=true）+ system.yaml shared.extraJavaOpts 注入 -Dbinary.provider.s3.https.only=false → init TLS 握手失败（属性已验证入 catalina cmdline，有效阴性）；5) 第二发：叠加 property 属性形态 httpsOnly=false → 同点失败（11:37:32.531Z 时间戳确认非残留）；6) 双败定谳 ③；7) 完整恢复+清理；8) 证据报告落盘
Files:         reports/compatibility/L010-r6-window-evidence.md（新增）；参照实例容器内配置（已还原）；/tmp 中转件（已清理，仅留无凭据 binarystore 宿主备份）
Tests:         恢复三验全过：binarystore sha=2d1409c6…（=L003 基线）、system.yaml sha=29951b7c…（extraJavaOpts 计 0）、ping 200（~192s 锁恢复）、标记制品 GET 200 sha1 逐字节一致；终态 ping 200；l0104 容器/凭据残留 grep 零
Commands:      docker exec artifactory cp …（备份）；curl -u admin:… PUT/GET example-repo-local/restore-marker/l0104-marker.txt；docker run -d --name l0104-minio -p 127.0.0.1:19000:9000 …；docker run --rm -e MC_HOST_l0104=… minio/mc:latest mb l0104/artifactory-filestore；docker exec artifactory sh -c "tr '\0' '\n' < /proc/10550/cmdline | grep binary.provider.s3.https.only"；docker restart artifactory；curl ping 轮询；docker exec … sed -n '12506,12560p' artifactory-service.log
Outputs:       reports/compatibility/L010-r6-window-evidence.md（窗口终态=双败定谳 ③；四问零新增；§5 静态面备选立票建议；§4 残余假说含最低成本 endpoint-带-scheme 变体）
Compatibility: R-6 四问维持 evidence-blocked（不盲裁）；TLS 常量层定谳把 L003 §6 备选路径升为推荐主路径（默认直读+redirect 显式开启+signedUrlExpirySeconds 可配，产品裁定权）
Security:      AK/SK openssl 随机生成、0600 中转、窗口末全部删除；凭据零入仓库/报告；system.yaml 含加密 DB 口令的副本已删；本票零 CI 供应链面改动
Performance:   无 CI 面影响（本票不动 workflow/Makefile）；参照实例停机窗口 ~10 分钟（两发+恢复三次重启）
Risks:         残余未测假说两条（endpoint 带 scheme / camel JVM 键）——若后续窗口测得其一有效，"常量层"结论收窄为"已测三面不可覆盖"（报告 §4 已注明）
Blockers:      无（双败为裁定内终态，非阻塞）
Next:          ① 立静态面备选票（报告 §5，产品裁定权）；② 若 conductor 批第三窗，首选 endpoint-带-scheme 单发（成本最低）
