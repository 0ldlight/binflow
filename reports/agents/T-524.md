# T-524 · virtual DELETE 语义裁定（DISCOVERY）

- **状态**: DISCOVERY（2026-09-25 由 conductor 依 T-520 票面外发现登记；BOARD 冻结期，Linear 回填 pending）
- **来源**: T-520（ADR-0051 四桶设计过程）报告「偏差」字段登记。
- **问题**: docs/reverse/virtual-resolution.md §7.5 的 DELETE 勘误（参照语义：virtual 仓库 DELETE 仅作用于
  自身聚合缓存，**不透传**给成员仓库）与 BinFlow 现状 `refuseVirtualDelete`（对 virtual DELETE 一律 405
  不透传）语义不同。两者都不把删除落到成员仓，但参照是否有「删自身聚合缓存」的窄面行为，BinFlow 未实现。
- **待裁定**: INTENTIONAL（405 即最终语义）vs 对齐（实现聚合缓存删除窄面）。裁定需参照实例活体验证
  （当前 401 无凭据，BLOCKED——版本三角：目标基准 7.161.26 E+）。
- **初步建议**: 归入 BIN-M1（S1 virtual 四桶业务闭环）差分用例面，随 S6 参照复验一并裁定；不单开实现票。
