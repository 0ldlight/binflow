# T-284 — 规格批次一：Conan/Cargo/Debian 三份行为规格（工作日志）

- 票据：T-284 [P1]（M10 B3 批次漏跑补跑；FR-91.1 覆盖集 5 份中的前 3 份）
- 状态：done（2026-08-26）
- 产出：`docs/reverse/conan.md`、`docs/reverse/cargo.md`、`docs/reverse/debian.md`
- 约束遵守：只写上述三文件 + 本日志；reverse-src/ 只读未改；未 git 提交；未触碰 internal/、scripts/ 等在途文件。

## 方法与材料

1. 样板：`docs/reverse/goproxy.md`（唯一样板——docs/reverse/ 无 nuget.md）+ `repo-semantics.md` 三态框架 + `inv-3-protocols.md` §2.3/§2.4 三型条目 + `artifactory-full-feature-matrix.md` 对应行。
2. 官方规范先行（WebFetch）：
   - Cargo：The Cargo Book「Registry Index」+「Registry Web API」两页全文（config.json/索引路径规则/索引行 schema/publish wire/yank/search/errors 信封）。
   - Conan：官方无独立 wire 规范页 → 采用 GitLab Conan v2 API 文档（de-facto 公开实现规范）+ Conan 官方 Revisions 文档（RREV/PREV 语义）。
   - Debian：Debian Wiki「DebianRepository/Format」全文（Release 字段/by-hash/Packages/Sources/压缩/flat 布局）。
3. 反编译只补规范空白（reverse-src/artifactory/src，7.161.11）：
   - Conan：`addon/conan/rest/ConanResource(.Constants)`、`repomd/conan/**`（v1/v2 资源、ConanPaths 布局、ConanUtils、LocalUpDownConanHandler、ConanVersionHeadersResponseDecorator、LocalRepoHandlerCommon）。
   - Cargo：`addon/cargo/rest/CargoResource`、`repomd/cargo/rest/CargoSubResource + handler + utils`、`repomd/cargo/CargoMetadataIndexer`、`addon/cargo/{repo,merger}`。
   - Debian：`addon/debian/rest/DebianResource`、`repomd/debian/**`（release/packages/by-hash/util）、`addon/debian/{resolver,interceptor,DebianUtils}`。

## 每份规格摘要

### conan.md（216 行）

- 规范锚点：GitLab Conan v2 API（端点族/响应体）+ Conan 官方 revisions 文档。
- 端点：v2 16 条 + v1 17 条 + 管理 2 条；能力协商头 3 个。
- 反编译补充：12 条（S1–S12：存储布局/index.json/.timestamp、能力头、v1 files 直传通道、属性集、checksum deploy、virtual 聚合等）。
- 置信度：高 25 / 中 10 / 低 0。
- 低置信：无；待验证 3 项（`_` 占位段、virtual 归并实测、latest 最终一致延迟）。
- 拆票就绪度：**可拆**；裁决点 2（v1 子集边界、绝对 URL base 来源）。

### cargo.md（205 行）

- 规范锚点：Cargo Book 官方两页（全字段级对齐，LC-10 口径）。
- 端点：9 条（config/index/download/publish/yank/unyank/search/仓根/reindex）+ owners 4 条显式不做。
- 反编译补充：8 条（S1–S8：存储布局、yank 属性化、remote config.original.json 翻译链、virtual 路由、git 索引弃用闸门）。
- 置信度：高 21 / 中 8 / 低 0（virtual 索引去重单列为待验证）。
- 低置信：无；待验证 3 项（virtual 重复行、http sparse 边界、features2 兼容）。
- 拆票就绪度：**可拆**（与 T-294 条件票直接衔接）；裁决点 3（虚仓去重、publish 错误形态、重复版本拒绝码）。

### debian.md（239 行）

- 规范锚点：Debian Wiki DebianRepository/Format（字段级）。
- 端点：内容面 8 行语义表 + 管理面 4 条。
- 反编译补充：12 条（S1–S12：debPUT、坐标属性体系、By-Hash 三档、压缩默认集、Release 生成序、签名链、trivial、管理端点、remote 归一化、virtual 聚合）。
- 置信度：高 18 / 中 6 / 低 0。
- 低置信：无；待验证 4 项（virtual apt 全链、归一化覆盖面、by-hash 代数、apt 容忍边界）。
- 拆票就绪度：**可拆**；依赖点 1（实例密钥对→无则 unsigned 先行）+ 裁决点 3。

## 真实客户端本机验证情况

本机（macOS）无 conan / cargo / rustc / apt / dpkg 工具链（`which` 全空）——三份规格的客户端命令均为 qa 环境就绪清单（conan 2.x via pipx；cargo 1.8x via rustup；apt 2.x 需 Debian/Ubuntu 容器），未在本机跑通。已标注于各规格 §8/§9。

## 汇总

| 规格 | 行数 | 端点 | 高 | 中 | 低 | 待验证 |
|---|---|---|---|---|---|---|
| conan.md | 216 | 35 | 25 | 10 | 0 | 3 |
| cargo.md | 205 | 13 | 21 | 8 | 0 | 3 |
| debian.md | 239 | 12 | 18 | 6 | 0 | 4 |

无低置信条目 → 三份均满足「低置信不作验收依赖」的强形式。M11 拆票（Conan/Cargo/Debian 适配票 + T-294）规格依赖已就位。
