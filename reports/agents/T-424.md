# T-424 工作日志 — FR-140.1/140.2 L2 行内快捷 + e2e 纪律成文

- 角色：dev-frontend ｜ 状态：done（四闸门绿 + 新 spec 5/5 首轮绿 + **净实例全量 301 passed /
  0 failed / 24 skipped（7.9m，exit 0）** + axe 双主题 serious=0（spec 内承证）+ ledger PASS +
  **src 零改动（纪律票 diff 审计：web/ 工作树仅 e2e 两文件）** + SPA 增量 **+0B**）
- area：`web/e2e/m15/t424-l2-inline.spec.ts`（新）+ `web/e2e/README.md`（§3~§6 增量）。
  **web/src 零改动**——L2 快捷（复制 key / Set Me Up 行内入口）自 M8 起既有（CopyButton 挂
  key 列〔T-344 批 B 起 MUI IconButton〕/ `repos-setmeup-*` 挂操作列〔T-242〕），本票按 PRD
  AC1 把既有能力**钉成断言**（L31 主腿）+ 纪律成文（AC2），零行为改动（AC3）。

## 1. AC1（L31 主腿）——新 spec `web/e2e/m15/t424-l2-inline.spec.ts`（5 test）

| test | 断言面 |
|---|---|
| 1 复制 key | aria-label `复制 仓库 key <key>`（§8 命名纪律）+ **Space 键盘激活**（focus→Space）+ 剪贴板拿**完整 key**（`navigator.clipboard.readText()` 逐字等值——展示可截断/拷贝不截断 §7.3）+ **回显断言**（`span.mono` 字形 ⧉→✓ = CopyButton done 态）+ 隔离层回归（URL 钉在列表 Tab，行导航不触发） |
| 2 Set Me Up 直开 | 行内入口 Space 激活开出 **T-382 抽屉本体**——以抽屉规范锚族为证（`smu-dialog` + `smu-repo` 预选值 = key + `smu-tab-{configure,deploy,resolve}` 三 Tab 全在场），**零重复实现**（不存在 repos-smu-* 平行锚）+ URL 不离列表 + Esc 关闭回焦启动钮（L02 抽屉族规格） |
| 3 E1 不倒退（admin） | 缺席断言：行内 `button[aria-haspopup]` 计数 = 0（**无 ⋮ 菜单形态**——T-385 撤旗后 T-400 precision 注记的注册口径，MoreVert 全树 grep=0）；行内删除唯一形态 = admin **文本钮**（可发现性优于 icon-only 直删）+ aria-label `删除仓库 <key>` + 行内 `[aria-label*="删除"]` 计数恰 1；**点击只开强确认**（confirm-dialog「没有撤销」+ 空输入 confirm-accept 禁用 + 行仍在）+ 取消腿安全退出（dialog 关 + 行在 + API GET 200） |
| 4 E1 不倒退（readonly） | L2 快捷是读面动作：copy-btn + setmeup 在场可用；**行内零删除动作**（`repos-delete-*` 计数 0）+ 无 ⋮（CapRepoWrite 门 L4 预收敛，服务端 403 兜底在 m8/repositories-admin 腿） |
| 5 axe 双主题 | light/dark 各一轮全页 `expectA11yClean`（serious+critical = 0；主题经 `binflow-console-theme` localStorage + `html[data-theme]` 断言承证） |

夹具纪律：全部 uniq key + API 直备 + `?deleteContent=true` 收尾（README §3 口径的本票自证）。

**在册观察（非阻塞，本票不修——共享件超 area）**：行 `onKeyDown`（`web/src/lib/keys.ts`
`onTableRowKeys`）不判 `e.target`——焦点在行内按钮上按 **Enter** 会被劫持为行导航
（`preventDefault` 同时吞掉按钮原生 click）；**Space 不受影响**（行处理器只拦 Enter/↑↓，
WAI-ARIA button 标准双激活键之一，WCAG 2.1.1 不破）。本 spec 以 Space 为键盘激活键并在
文件头注记；若要收口 Enter 语义应改 keys.ts 共享件（Permissions/Users/Repositories 等六页
消费），建议单独提票。

## 2. AC2（三节纪律成文 + 两条 flake 注记）——`web/e2e/README.md` §3~§6

CONTRIBUTING 不在树（派单「若在」口径）→ 全量落 README，§1~§2 原文零改动：

- **§3 破坏性动作纪律**（INC-1 教训，T-381 §INC-1 成文）：默认禁点 + **差集法**（形态探测
  用前后 DOM 差集，INC-1 事后还原正是差集法证明 trash 点击即弹 V8 确认框）+ **永不点确认**
  （零例外）+ spec 破坏性腿只打自备夹具（uniq + deleteContent 收尾）+ 共享 fixture 快照前置
  （数据目录副本/VM 快照——与用户实例报障「沙箱复现」纪律同源）。
- **§4 pkill 按端口精确杀**：唯一合法形态 `lsof -ti :PORT | xargs kill` + 杀完 lsof 复核；
  禁宽模式串（`pkill -f binflow-server` 族在共享机命中所有人的实例）。事故锚全量在文：
  T-307R（licensed 18280/18290）/ T-384 §4（:8143）/ T-389 §5（:8143/:8144）/ 2026-09-02
  04:08:34 conductor 会话自杀（iteration-1193 + T-426 §环境观察）；另收录 T-111/T-239 的
  宽模式**静默漏杀**反向坑（argv[0] 形态不命中）——端口法两方向都诚实。
- **§5 assert-tokens 属性选择器豁免**（T-390 §遗留-1）：hex 在**声明值位**必须 token 化、在
  **选择器匹配串内**（pkg-icon.css `[stroke*='d70a53' i]` 官方色匹配键形态）= 定位键豁免；
  闸门显式规则仍待票（治理工具放宽单独走票）；相邻 `color-mix(... var(--bf-*))` 豁免注记。
- **§6 两条 flake 注记**：m9 N01 spec 级竞态（仪表盘惰性取数污染页面级请求预算——T-384 §4.1
  A/B 归因；串行 6/6 甄别口径 + 修复方向登记）+ m9 seed 并行互撞（create-if-absent PUT 竞态，
  CI `--workers=2` 理论可复现——T-391 L-b；converge() 常态吸收 + 修法登记）。

## 3. 自测证据（命令 + 关键输出）

```sh
# 四闸门（web/）
npm run typecheck        # tsc --noEmit：零输出（clean）
npm run lint             # 0 errors, 37 warnings——全部为既有 src 文件 react-hooks
                         #   set-state-in-effect 家族（AuthContext/AppShell/...），
                         #   本票两文件零告警（npx eslint e2e/m15/t424-l2-inline.spec.ts：clean）
npm run build            # assert-tokens: OK — css 12 个 + tsx 51 个；✓ built in 3.43s
node scripts/anchor-audit.mjs --ledger
                         # PASS（registered 与册一致：unregistered/broken 双零，retired 全部在表）

# 被测实例（scratch :8151；make console 同义拷贝 web/dist→internal/console/dist + make build）
#   起服前先核对 config 生效行：listen=127.0.0.1:8151 data_dir=/tmp/bf-t424/data（见 §5 事故）
node scripts/seed-m8.mjs --base http://127.0.0.1:8151
                         # seed-m8: tree verified 10291 nodes (planned 10291) in 74.1s

# 新 spec（首轮）
BASE=http://127.0.0.1:8151 npx playwright test e2e/m15/t424-l2-inline.spec.ts --project=chromium
                         # 5 passed (8.9s)

# 全量（三 project：chromium + m9 + m10）
BASE=http://127.0.0.1:8151 npx playwright test
                         # 301 passed / 0 failed / 24 skipped (7.9m)，EXIT=0
                         # （24 skip = armed 实例腿〔oidc-stepup 族〕等既有口径；
                         #   在册 m9 N01 flake 本轮未现——0 failed）

# SPA 预算（dist js+css gzip 合计）
                         # 385,606 B（当前树；本票增量 +0B——见 §4 diff 审计）

# 实例收尾（README §4 口径自证）：lsof -ti :8151 | xargs kill → 端口复核 0 监听
#   + serve.log「binflow stopped」优雅退出在案
```

## 4. AC3 判定（四闸门 + axe 双主题 + 全量绿〔零行为改动 diff 审计〕+ SPA）

| AC | 判定 | 证据 |
|---|---|---|
| AC1 L31 主腿 | **PASS** | §1 五 test（5/5 首轮绿）；复制 key（剪贴板/回显双断言）+ Set Me Up 直开（抽屉锚族复用零平行实现）+ E1 不倒退缺席断言（admin/readonly 双视角）+ a11y（aria-label 逐钮 + Space 键盘激活 + Esc 回焦） |
| AC2 纪律成文 | **PASS** | §2：三节（INC-1/pkill/assert-tokens）+ 两条 flake 注记落 README §3~§6；既有 e2e 全量绿（301/0/24）——**纪律票零行为改动**（下行） |
| AC3 四闸门 + axe + 全量 + SPA | **PASS** | §3 全绿；diff 审计：`git status --porcelain -- web/` 仅 `M web/e2e/README.md` + `?? web/e2e/m15/t424-l2-inline.spec.ts`——**web/src / web/public / index.html / scripts / vite.config 零改动**（`git diff --stat` 空），构建输入未触 → SPA 增量 +0B（≤10KB；385,606B 为含 T-422 增量的当前树值）。axe 双主题 = spec test 5 承证（本票零 DOM 改动，a11y-sweep 全路由面结论按 diff 推理不变，未重跑——零 src 变更下 sweep 无新信息面） |

锚册：**零新锚零改名**（本票只消费既有注册锚：repos-* / smu-* / confirm-* + `.copy-btn`
类钩子）→ 无 v-bump；`anchor-audit --ledger` PASS（A1~A4 全过）为证。

## 5. 过程事故披露（已按纪律处置，零数据损失）

08:26 起服首轮：shell heredoc 未生效 → `-c` 指到**空配置文件** → 全默认参数起服
（`:8080` + `data_dir=./data`）——**误开了仓库根 ./data（用户本地实例目录）14 秒**。
日志（/tmp/bf-t424/serve.log 首段）逐行核对影响面：startup session sweep（24h TTL 过期
会话清扫——用户实例自启时同款例行动作）+ replication block state seed（false/false 无翻转）
+ WAL checkpoint（binflow.db mtime 翻转）；**blobs/uploads mtime 未动、无 GC 日志行、
零写请求**（无任何客户端连接）。处置：`lsof -ti :8080 | xargs kill`（端口法）+ 端口释放
复核；随后以 printf 重写配置、核对该轮 serve.log 的 `listen`/`data_dir` 生效行后才开测。
教训已并入 README §4 末条（起服后核对 listening 行 + data_dir + lsof 再跑腿）。

## 6. 契约漂移

无（零 API 面、零 src 改动）。

## 7. 遗留与移交

1. **Enter-on-inline-button 劫持**（§1 在册观察）：修法 = `onTableRowKeys` 判
   `e.target.closest('button, a, input, select, textarea, summary')` 让行内交互子元素的
   Enter 归还子元素——web/src/lib/keys.ts 共享件（六页消费），超本票 area，建议 conductor
   单独提票（可与 keys 共享件其它键盘债合票）。
2. **assert-tokens 属性选择器豁免显式规则**（T-390 §遗留-1，README §5 已成文口径）：闸门
   脚本侧开显式 allowlist 仍待票（治理工具放宽单独走）。
3. /tmp/bf-t424/（实例数据 + serve.log + e2e-full.log 全量输出）留供复验，可整删。
