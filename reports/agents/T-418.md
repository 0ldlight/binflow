# T-418 — replication.md 增量段（包 B 前置规格——双源材料）

- role: reverse-engineer；area: docs/reverse/replication.md（增量段）；日期:2026-09-01
- 状态: done（待 conductor review/qa）
- 写入物: `docs/reverse/replication.md` §9 增量段 + §1.1/§2.4/§5/§6/待验证清单 五处留痕升级；本报告
- 纪律: BOARD 零写、零 git 操作、reverse-src 零改动；只动上述两文件

## 1. AC 判定

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| AC1 | 三面 wire 规格（executereplicationnow 对位 / blockPush·blockPull / Test）——官方 REST 文档锚点主源 + T-402a 补白；路径/方法/参数/envelope/错误码/幂等逐条出处+置信度 | ✅ | replication.md §9.1（11 行端点表，官方 REST 面 + UI-API 面双列）、§9.2（A/B/C 三面语义流程，幂等语义逐条 + UI 面错误文案逐字 12 条）、§9.8 锚点全录 |
| AC2 | Test 端点形态定案 + 审计词表补词清单 | ✅ | §9.3 定案（官方参考无 test 对位——索引核对 → `/api/v1` 自有 C 层登记 + 语义钉死表）；§9.4 补词 4 词（P0 遗留清偿 1 + P1×2 + P2 裁决项 1） |
| AC3 | tech-lead 就绪度确认（解锁 T-420/T-422）；clean-room 合规 | ✅ | §9.6 三面全「就绪」、阻断项无、两处交裁点明示（封锁端点形态二选一 / 审计词 #4 单双词）；clean-room:官方 OpenAPI 逐字为主源、反编译只产行为事实（文案/错误码/流程），零代码翻译，UI-API 面逐条标「不在官方参考内」 |

## 2. 方法与证据链

- **官方主源**:docs.jfrog.com REST 参考（OpenAPI 3.1）。发现官方为 AI 提供 `.md` 后缀形态（页面 URL 加 `.md` 得 markdown+OpenAPI）+ `llms.txt` 索引 + sitemap 全量 slug——用 sitemap 锁定复制族全部 12 参考页,实取 6 页（execute/block×2/global/status/enable-disable）,镜像 /tmp/t418/（随 /tmp 回收）。索引核对结论:**官方参考无 test/executeall/executereplicationnow 面**——这三者只存在于 UI-API。
- **反编译补白源**:reverse-src 双 batch 齐——batch1-core（UI rest 树:仓库资源五路由 + GlobalReplicationsUIResource + GlobalReplicationResource + Test/Execute 系服务实现 + RestResponse/FeedbackMsg envelope）+ batch3-addons（Pro 版 ReplicationAddonImpl:blockPushPull/unblockPushPull 实现、排程封锁门、PushStrategy 枚举、立即全树排程链）。
- **第三源**:T-402a R2/R5/R6/R8（license 门 400 文案、bundle 调用、UI-API global config 活体 200）。

## 3. 关键定案摘要

1. **手动触发**:官方对位 = `POST /api/replication/execute/{repoPath}`（body 可缺省=触发该仓全部既有配置）;策略参数闭集 AUTO/TREE/EVENT、未知回落 AUTO（M6 §5 猜测 push/push-pull **勘误**）;封锁期触发立即失败非排队;触发不去重。
2. **全局封锁**:`push`/`pull` query 是「选方向」非「设布尔」（default true,false/非 true 串=该方向不动,null 同 true）;响应 text/plain 单串四变体;幂等（同值重写+同文案）;对 cron 轨与事件轨双生效。
3. **Test**:官方无 REST 对位 → BinFlow `/api/v1/replications/{id}/test` 自有登记;HTTP HEAD 探测、200/302 通过、`-cache` 目标拒绝、无副作用幂等、不看封锁态。
4. **顺手清偿（同场清,票面预授权）**:M6 待验证 #1（ReplicationRequest schema——官方 OpenAPI 全字段）与 #3（strategy 枚举）双解;§2.4 全局配置字段集升高置信。
5. **审计词表**（M14 T-405 遗留同场清）:`replication.config.update`（P0,emit 已在、只欠入册）+ `replication.config.test` + `replication.run`（沿 gc.run run 族,与引擎层 replication.push 分层）+ `replication.block.update`（单/双词交裁）。

## 4. 置信度分布（§9 增量段）

高 ×9（官方 REST 三面 wire/封锁生效面/strategy 闭集/ReplicationRequest schema/文档瑕疵登记/Test 无对位/GET global 三证）｜中高 ×4（UI-API trigger 面/封锁消息变体/UI-API 全量补白）｜中 ×3（PUT global UI 面/test 两面/pull test 形态、触发不去重）｜低 ×0（新增低置信 0;待验证 V1~V4 为"键集/错误体展开"级,不阻断实现）

## 5. 遗留 / 交上游

| # | 项 | 建议 |
|---|---|---|
| L01 | §9.6 两交裁点 | 封锁端点形态（兼容三端点 vs 自有 GET/PUT global-config）+ 审计词 #4 单/双词——tech-lead 在 T-422 派单前裁 |
| L02 | 审计词 4 词登记 | 归 T-420/T-422 emit 票同场入 internal/audit 常量块（沿 T-346 体例）,P0 词可先单独清偿 |
| L03 | T-405 遗留 #1（architecture.md §7.1 路由表缺 PUT 行）仍在案 | 本票未扩,conductor 文档环合并处理 |
| L04 | V1~V3 升格路径 = Pro license 实例 | 成本敏感,维持「以核验为准」档,零静默升格 |
| L05 | /tmp/t418/ 官方页镜像 | 随 /tmp 生命周期回收,锚点 URL 已全录 §9.8 |
