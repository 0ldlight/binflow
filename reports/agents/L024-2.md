# L024-2 — CircleCI 流水线修复（uat 链自 09-12 起恒红）

Ticket:        L024-2 · CircleCI 流水线修复（用户指令「一直跑不通，修复一下」）· P0
Role:          devops-engineer
Area:          CI workflow（.circleci/config.yml、.github/workflows/ci.yml）+ 一处测试竞态小修（internal/httpapi/system_storage_admin_test.go，ticket 明示允许的小修面）
Input:         conductor 派发 + 自读：`.circleci/config.yml`、circleci CLI 1.0.49536（keychain 登录）、`ci/protocol-matrix.sh`、`deploy/ci/uat-deploy.sh`、`internal/httpapi/system_storage_admin.go`、`internal/storage/prune.go`。用户新指令两条：docs-site 退役（撤 build job 的 make docs 腿与 npm-docs-v1 缓存，internal/docs/dist 已提交嵌入面保留）；禁止本地 docker。
Changes:
               1. 诊断（见 Tests 字段的根因清单）：10 个 main run（#84-#93）全部 build job 红 → deploy_uat/e2e/protocol_leg 全部 not_run → UAT 停在 uat.72ae611（=run #82 于 09-10 部署，已 curl 证实）。
               2. 修复当前拦路虎：`TestStoragePruneStoppedRunLandsStoppedTerminal` 测试竞态——空目录 walk 在 CI 慢 CPU 上跑赢带 argon2 鉴权的 stop POST，run 自然 finished、5s eventually 超时（#90/#92/#93 三连红、每次 5.2-5.4s 撞满 deadline；本地 30+ 次全绿因 argon2 快）。修法：gatingPruner 增加 parkAfterFirstDir 停车 seam——stop POST 返回 202 之前 walk 停在目录 00 的 Observe 内，释放后下一个目录边界必消费 stop 标记，测试从时序赌局变确定性断言；另加 t.Cleanup 失败时 dump 捕获日志。
               3. 按用户指令撤 docs 腿：build job `make docs` 移除、npm-docs-v1 restore/save_cache 移除、相关注释同步；`internal/docs/dist`（5.4MB 已提交真内容）继续作为嵌入面，uat-deploy.sh 的 /binflow/docs/ 200 smoke 基质不变，internal/docs 包测试绿。
               4. 同源不漂移：.github/workflows/ci.yml 三处 `make docs` 腿同批撤除（ci/e2e/release-snapshot job），actionlint 零告警。
               5. e2e 姿态（ticket 授权的「显式 skip」）：e2e job 移出 uat workflow，入独立 `e2e` workflow，参数门控 `pipeline.parameters.run_e2e`（默认 false）。非删除、config 内注释注明票号与恢复路径；主干九段闸门链（build→deploy_uat→protocol_leg×10）不受影响。动因：e2e 自 run #81（09-10）起每次 ~50 处 spec 红（pro-license 租约毒化 + t434/t445/t449/t492 族 + ADR-0050 后 PUT-as-replace），52 分钟零信号，且把 deploy 链的红埋在底下。
Files:         .circleci/config.yml；.github/workflows/ci.yml；internal/httpapi/system_storage_admin_test.go；reports/agents/L024-2.md
Tests:         失败根因清单（run×job×根因×证据）：
               - #84-#88（09-12→09-15 05:28）build 双 shard exit 2：`make lint` gosec G115 `storage_info_unix.go:19 int64→uint64`（6ca8f72e 09-11 引入；89ddcd5d8 已修，之后 lint 过门）。证据：`circleci job output get <build-job> --step-num 112` 五个 run 同一报错尾。
               - #90/#92/#93（09-15）build shard0 exit 1：单测 `TestStoragePruneStoppedRunLandsStoppedTerminal` 超时（上文 Changes 2）。证据：三 run 的 step 112 输出尾 + `circleci testresult list`（5.36s/5.24s/5.44s，single failure）。
               - #82（09-10，最后一个 build+deploy 绿的 run，UAT 现行版本来源）附加红：e2e（52m45s、~50 失败，testresult 可查）+ protocol_leg-helm（`GET https://uat.binflow.org/v2/ → 401`）+ npm（remote 段 ETARGET 新 stamp 不可见）+ conan（remote 段 hello/1.0 两轮 retry 不解析）+ pypi（remote 段 curl 409）——四腿全是 remote/virtual 段、local 段全过，且都是对 09-10 旧二进制的观测；当前代码（LOOP 015-023 兼容批）是否已愈须等新部署后由腿级归因验证，不预判、不跳腿。
               - #71（09-05）为最后一个 13/13 全绿 run。
               修复后自测：
               - `go test ./internal/httpapi/ -run 'TestStoragePrune' -count=5 -short` ok；`-count=20`（GOMAXPROCS=2 + 6 路 yes 负载）0 失败；全包 `go test ./internal/httpapi/ -short -count=1` ok 187.7s。
               - `make lint` 0 issues；`make vet` 全树过；`make build` 过（117.16MB/120MB 预算内）；`gofmt -l internal/httpapi/` 空；`go test ./internal/docs/` ok。
               - `circleci config validate` valid；`circleci config process`（默认 false：e2e workflow 被编译剔除）与 `--pipeline-parameters 'run_e2e: true'`（e2e+自带 build 编入）双态 OK；`actionlint .github/workflows/ci.yml` 零告警。
Commands:      自测命令原文（可重放）：
               - `go test ./internal/httpapi/ -run 'TestStoragePruneStoppedRunLandsStoppedTerminal' -count=20 -short -v`（GOMAXPROCS=2 与默认各一轮）
               - `GOMAXPROCS=2 go test ./internal/httpapi/ -short -count=1`
               - `make lint && make vet && make build`
               - `circleci config validate && circleci config process .circleci/config.yml`；`circleci config process .circleci/config.yml --pipeline-parameters 'run_e2e: true'`
               - `actionlint .github/workflows/ci.yml`
               诊断命令（API 500 绕行口径，项目 UUID eaa9da69-2e51-4bbd-a02d-91704d5f7ae5）：
               - `TOKEN=$(security find-generic-password -s "com.circleci.cli:https://circleci.com" -a lzw -w | sed 's/^go-keyring-base64://' | base64 -d)`
               - `curl -s -H "Circle-Token: $TOKEN" "https://circleci.com/api/v3/runs?filter[project_id]=eaa9da69-2e51-4bbd-a02d-91704d5f7ae5"`（分页用 `&page[cursor]=<next>`；`page_cursor` 参数无效）
               - `circleci job get <job-uuid>` / `circleci job output get <job-uuid> --step-num 112 --execution 0` / `circleci testresult list <job-uuid>`
               远程终验触发（devops 无 commit 权，conductor 执行）：develop 合 main → push origin main（chunk 镜像发 webhook）→ `circleci run list --project circleci/binflow/binflow`（偶发 API 500 重试，或直接用上面 v3 runs 端点）→ 若单腿红：`circleci workflow rerun <workflow-id> --from-failed`。注意：v2 触发面（`circleci pipeline run`/`run trigger`）对本独立 org 项目返回 "Project not found"（已实测），CLI 触发不可用。
Outputs:       `.circleci/config.yml`（docs 腿/缓存撤除 + run_e2e 参数门控 workflow + 注释三处）；`.github/workflows/ci.yml`（三处 make docs 撤除）；`internal/httpapi/system_storage_admin_test.go`（确定性停车 seam + 失败日志 dump）
Compatibility: uat/deploy/protocol 九段闸门链顺序与内容不动，只增不拆；docs 嵌入面不变（committed dist 继续服务 /binflow/docs/，smoke 基质保留）；e2e 从 uat workflow 移至独立门控 workflow = 显式降触发频（票内授权 + 恢复路径注释在 config），非删除；known-divergence/契约面零触碰。
Security:      无新增 secret 引用；未动 secret 值；GHA workflow 只删步骤（hook 安全提示已阅，无不可信输入进 run:）；protocol 腿的 https/insecure-registries 条件逻辑未动。
Performance:   build job 撤 make docs（原 ~1m55s 腿的一部分）+ 撤 npm-docs-v1 缓存往返，预计 build 缩短约 1-2 分钟；go-mod/go-build/npm-web/pw-browsers 四缓存面未退化（key 未动）；e2e 默认不跑 → main run 少 ~55 分钟与一台 runner 占用；首轮真实 run 后应以 Insights 时长复核（留 Next）。
Risks:         1) 远程终验未做（无 commit 权）：build 修好后 deploy_uat/protocol_leg 五天来首次重跑，UAT secrets（UAT_SSH_KEY_B64/UAT_MATRIX_PASSWORD）与 caddy 层假设仍按 09-10 原样——若红属新事实，按腿归因另立票；2) npm/pypi/conan/helm 的 remote 段红源可能仍在（对旧二进制的观测，无法本地等价复现——需真实部署）；3) e2e 门控是可见降级，e2e 债务票落地前无自动 e2e 覆盖（GHA 面仍被 billing 挡）；4) `web/e2e/m8/README.md` 仍写「make docs 不能省」，与本批姿态矛盾（web/ 域，未动）。
Blockers:      无
Next:          1) conductor：commit 三文件 → 合 main → push → 观察首轮全绿（重点：deploy_uat、十腿、以及 build 时长对比 10m43s 基线）；2) e2e 债务转票（pro-license 租约 afterAll 毒化 ~50 失败族 + t434/t445/t449/t492 + ADR-0050 PUT-as-replace spec 面），落票后把 e2e job 移回 uat workflow；3) docs-site/ 目录整体退役转票（删目录 + Makefile docs 目标退役 + web/e2e/m8/README.md 更正 + fern 侧承接）；4) nightly 观察：`nightly` workflow（race_full）从未实际执行——schedule 事件跑的是 uat workflow（run #91 证据），schedule→workflow 映射需校准，建议单独票；5) 首轮 run 后用 Insights 复核 build 时长与分片均衡（Performance 字段的远端证据）。
