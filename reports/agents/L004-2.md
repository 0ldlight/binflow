Ticket:       L004-2 · C14/ADR-0048 双臂探针 + 消失机理定谳 · P1
Role:         differential-qa-engineer
Area:         tools/difftest（未新增脚本——本票为一次性取证，复现骨架入报告 §5） + reports/compatibility/L004-negative-cache-probe.md
Input:        conductor 派发（UAT :8083 重建到 HEAD c81119b7 用 L003-3 指南；:8082 参照；manifest-404/blob-404 双臂 ×3 + 60s TTL 复核 + 三选一素材）；自读 internal/adapter/docker/remote.go、internal/repo/{dockerremote,config}.go、internal/remote/{fetcher,cachestate}.go、DECISIONS.md ADR-0047/0048、known-divergence.yaml C14、L000/L002 差分报告、reports/agents/L003-3.md
Changes:      无产品码/契约/金样改动。UAT 基线重建（:8083 → uat-l0042-c81119b7，.env.uat 版本键随改，gitignored）；两实例各建/删 difftest 测试仓（l004-docker-remote[TTL=60]、参照另建 t1800 复刻仓）；本地 registry:3 上游 + dind 客户端（用后全删）。重建注记：mirror 断网致 build-release.sh phase 3 失败 ×2，以本地 5588 index + PATH shim（/tmp，已清）零改动绕行，alpine 内容同源 3.24、distroless 合成 index 未消费
Files:        reports/compatibility/L004-negative-cache-probe.md（裁决素材）、reports/agents/L004-2.md（本文件）；镜像 binflow:uat-l0042-c81119b7-alpine{-amd64}（本地留存，与历轮一致）
Tests:        代码定谳 1 项（全历史 5 commit 链核验）；活体 case 8：M-a curl×3（双端）/M-a CLI（双端）/M-b×3（双端）/B1×3（双端）/B2×3（双端）/过期复核×2（双端）/B2 真实 CLI pull（BinFlow）——一致 2（B1、B2）差异 2（M-a、M-b）skipped 0；断点续跑一次（API 限额中断，仓零残留核实后续跑）
Commands:     git log -S CacheRemoteMiss（internal/ 全域 + adapter/docker 单文件）；git show d2746341/af2f6c02:internal/adapter/docker/remote.go（引入时 vs LOOP000 时两臂守卫）；make console/docs/release + build-release.sh（shim 绕网）+ compose up -d --force-recreate；curl 双端 /v2 manifest/blob ×3 连发（Accept 全集）；docker exec dind docker pull（缺失 tag/seed）；docker logs difftest-upstream access 行计数（按路径+UA）；docker cp sqlite 快照 + sqlite3 remote_cache 查询；DELETE 仓（BinFlow ?deleteContent=true / Artifactory ×2）
Outputs:      报告 reports/compatibility/L004-negative-cache-probe.md；无提金候选（本轮全部为行为/时序观察，无形态金样价值——B2 的 404 错误体形态已有契约覆盖）
Compatibility: 差异分类建议：BUG 1（M-a：BinFlow manifest 冷 miss 面未兑现 missedRetrievalCachePeriodSecs 承诺，参照兑现——ADR-0048 Errata 条款已触发，建议 C14 翻 BUG+实现票）+ UNKNOWN 1（M-b 未知 image 名：参照本地确定性 vs BinFlow 每次回源，单列票）；INTENTIONAL 0（无权限裁）。回归对照：L000 C14「BinFlow 更优」证伪（fixed=账实不符闭账）、L000 E6-3「参照未生效」证伪、L002-2 #16「双端趋同」半推翻（BinFlow 3/3 复现、Artifactory 1/3 不复现，Risks 存疑）；B1/B2 面 SAME 维持
Security:     凭据仅命令行内联（admin 双端），不落盘不落报告；dind login 凭据随容器删除；实例操作限 difftest 仓（双端建/删配平，BinFlow 仓 deleteContent 清 2 节点）；参照实例除建/删本票测试仓外零接触（binarystore 等 L003-3 遗产未碰）
Performance:  全部臂本地上游 RTT 16-111ms；:8083 重建 24 分钟（mirror 失败 ×2 重试 + shim 绕行后一次过）；无超时 case
Risks:        ① L002-2「Artifactory 3/3 回源」不可复现且不可回放（实例当日重启、历史仓状态不可考）——本轮以贴齐条件（同上游型/TTL=1800/真实 CLI）第一手双证据为准，建议 conductor 若需终裁可再跑一轮复验 ② 本地上游 RTT 与本地负缓存应答时延同量级——凡时延判负缓存必须配上游计数/行证据（本轮已按此纪律，写入报告判据节） ③ sqlite 快照为 WAL 拷贝（提交态足够、非严格一致性快照）④ registry:3 删 blob 需 rm 文件+restart（内存描述符缓存）——构造法已写入报告 §5
Blockers:     无（mirror 断网已绕行非阻断；限额中断已续跑）
Next:         conductor 三选一终裁（报告 §3 建议 C 变体：BUG 非回归 + ADR-0048 按 Errata 作废 + C14 翻 BUG）；M-b 新分歧单列票；实现票落定后契约 #16 断言翻回「窗口内本地负缓存」语义并重放
