# T-112 工作日志 — architecture §7.6/ADR-0015 备份面勘误

- ticket: T-112 [P1] architecture §7.6/ADR-0015 备份面勘误 `role:architect` `area:docs/design/architecture.md、DECISIONS.md`
- 状态: done（文档改动完成，grep 自查零活体旧表述）
- 日期: 2026-08-20
- 输入: reports/agents/T-96-review-architecture.md（第三节勘误草案，逐条采用）、reports/agents/T-96.md（实现事实）、docs/prd/milestone-4.md（FR-32/GE-09/§7 Q2/Q4）、docs/design/architecture.md §3/§4.6/§7.6/§11.19、DECISIONS.md ADR-0015（既有勘误 T-108 行）、BOARD.md（只读）
- 基线口径: T-88 R1 先例「PRD 命名面 + ADR 机制内核」——形态以 M4 PRD（后出、W 序列验收锚）与 T-96 实现（commit 75c6d95）为准，机制内核（顺序硬规则/空实例/mtime/互斥锁）不动

## 做了什么

### 1. 草案采纳情况（第三节约逐条采用，无否决项）

草案五项（§7.6 四处 + ADR-0015 勘误行 + §11.19 标题 + §3 公共面）全部成立、全部落地。**在草案基础上按 T-96 事实小幅扩写**（非偏离，均为草案条目的补强）：

- export 条目补：产物权限位（目录 0700 / metadata.db+manifest.json 0600，NFR-S22）、`export.run` 审计写源实例活库的归属语义（review §五已裁决正确，入契约防后人「修正」回快照）、blob 拷贝保权限位；
- import 条目补：不取维护锁（目标空 + 停机，无争用对象）、备份内名 `metadata.db` 固定 vs 落位尊重目标机 config 命名（T-96 遗留⑤要求文档面写明，此处为架构面锚点）；
- §3.1 公共面行补：fail-fast 不排队的理由（排队的 export 无限期扣押输出目录）、锁文件常驻不删、op 值域 {gc, export}（吸收 review N2 的词表钉住建议——以文档面钉死，常量化留给 T-94 二选一时定）。

### 2. 改动清单

| 文件 | 位置 | 变更 |
|---|---|---|
| docs/design/architecture.md | §7.6（原 842-845 行） | 标题加勘误注；新增勘误引言块（四条修订 ①②③④）；export/import 两 bullet 按草案全文重写（目录形态 / `--output` / 无 REST 面 / CLI 退出码 / dry-run 降格） |
| docs/design/architecture.md | §11.19（原 948 行） | 标题「备份不含 remote_cache/web_sessions」→「备份不含 web_sessions（remote_cache 随 db 走）」；正文补「export 时从快照 purge」；加标题勘误注（N7） |
| docs/design/architecture.md | §3.1（sentinel 段后） | 新增「备份/锁公共面」一行：`AcquireDataLock`/`DataLock`/`ErrDataLockHeld` + manifest 族 + `CopyBlobsTree`/`HashFile`/`BlobPath`（blob 路径形态唯一定义点） |
| docs/design/architecture.md | §3.2（事务边界注后） | 代码块内补「快照检查面」注释：`SnapshotChecksums` 等为包级函数非 Store 方法（制品不是活库）、`VacuumInto` 经消费侧接口断言 |
| docs/design/architecture.md | §4.6 | （顺手收口，见 §3）键名 `quota_bytes`→`quotaBytes` + remote 计量口径改「不计量」；标题加勘误注 |
| DECISIONS.md | ADR-0015 文末 | 追加「勘误二」一条（① 产物形态 ② 入口形态 ③ 幂等释义与 import 语态 ④ 配额键名与 remote 口径）——决策 3/4/5 原文存目未改（对齐 T-108 勘误先例：只追加不重写） |

### 3. 顺手收口两条（票面指令：本要单独立票，在场收口，特此注明）

1. **T-95 review NB4——§4.6 键名 `quota_bytes` 应为 `quotaBytes`**：核实成立（代码 `internal/repo/config.go`/`governance.go`、`internal/httpapi/usage.go`/`repositories.go`、PRD §7 Q2、REST 面全为 camelCase `quotaBytes`；ADR-0015 决策 2 原文的 snake_case 已由 T-108 勘误行更正过，architecture §4.6 是最后一处活体残留）。勘误句已落 §4.6（含溯源注记）。
2. **已登记的 §4.6 remote usage 口径相抵**（T-95.md 规格依据段登记：「architecture §4.6 说 remote 缓存 node 计入，PRD Q2 写死 pull-through 落盘不计量，建议 architect 勘误收口」）：核实成立。裁定按 PRD Q2 + T-95 实现（engine cache node 写不走计量；005 回填一次性含既有 remote nodes，快照语义不回滚；原 `quota_include_cache` 豁免位方向作废——M5+ 若需计量按「计入开关」重开）。勘误句已落 §4.6，ADR-0015 勘误二④同步收口（决策 2 同款表述在该 ADR 内也有活体）。

### 4. 勘误前后对照（§7.6 四处 + 联动）

| # | 勘误前（过时） | 勘误后 |
|---|---|---|
| ① | 「② tar `blobs/`…产物 = db + blobs.tar + manifest」 | 目录树拷贝（保 mtime+权限位）；产物 = `<out>/metadata.db` + `<out>/blobs/` + `<out>/manifest.json`；`--tar` 单文件产物 P2 债务（flag 已定义、显式报未实现） |
| ② | 「CLI `binflow-server export --out <dir>`」 | `binflow-server export -c <cfg> --output <dir> [--tar]` |
| ③ | 「+ admin REST 异步触发（产物落 data_dir/exports/）」 | 删除——无 REST 面（`/api/export/**` 404 + E-01，GE-09；ADR-0015 决策 5 该句 M4 不做） |
| ④ | import「非空 409」「启动 GC dry-run 报差异（预期仅多余 blob）」 | 「非空 → CLI 退出码非 0（import 无 REST 面）」；「启动 GC dry-run 报差异」降格为运维建议（文档面），非 CLI 内置——多余 blob 由常规 GC 收敛；补先验证后写盘与无半恢复语义 |
| 联动 | ADR-0015 决策 3「blobs.tar」/决策 4「db 覆盖 + tar 解压覆盖」「409」/决策 5「--out + admin REST 可选触发」 | 勘误二①②③（原文存目）；幂等释义 = 清空目标后重复导入等价 |
| 联动 | §11.19 标题「备份不含 remote_cache/web_sessions」 | 「备份不含 web_sessions（remote_cache 随 db 走）」 |
| 联动 | §3 无备份/锁公共面 | §3.1 一行（锁 + manifest 族）、§3.2 注释一行（快照检查面） |

### 5. 文档 diff 自查（grep 验证记录）

命令（2026-08-20 执行，工作树 vs 勘误后）：

```
grep -n 'blobs\.tar\|--out \|data_dir/exports\|非空 409\|admin REST 异步\|quota_bytes\|报差异\|不含 remote_cache' docs/design/architecture.md
```

结果：6 处命中（390/850/852/853/956 行），**全部位于勘误注记的「引用后作废」内**（「原误写 `quota_bytes`」「修订为目录形态」「『admin REST 异步触发』M4 不做」等 quote-then-revoke 形态，与 ADR-0014 勘误①-④引用旧名 `bf_session` 的先例同构）——**零活体旧表述**。DECISIONS.md 侧 `git diff` 确认纯追加 5 行（勘误二），决策 1-5 与 T-108 勘误原文零改动。

其他备份相关表述核对（无需勘误）：§4.1「备份面 = blobs/ + SQLite 文件」与目录形态一致；§4.4/ADR-0006 勘误②的「`tar`/`rsync -a` 默认保留」是 mtime 约束的运维工具示例非产物形态表述；§7.1 路由表 GC↔export 互斥 409 行在 T-108 勘误时已对齐。ADR-0006 后果行「备份 = `blobs/` 目录 + SQLite 文件的一致性快照」本就是目录形态，无需动。

## 遗留 / 需上游确认

1. review 第四节技术债三条（引用集双实现 N5、Windows 锁运行时未验 N3/M5、export 空 data dir 制造空备份 N4）属技术债台账增补——本票 area 限定 §7.6/ADR-0015 勘误，未动 §11 台账主表（只在 19 号条目内做标题勘误）；建议 conductor 随 T-94 派单一并归置（N5 的等价性钉子测试可挂 T-94 QA）。
2. T-96 遗留⑤（备份内名 `metadata.db` vs 落位 `binflow.db`）已在 §7.6 import 条目落架构面锚点，用户文档侧展开归 T-107（tech-writer）。
3. N1/N2（锁持有者编程面入口、op 词表常量化）：本票已在 §3.1 以文档面钉死 `pid=<n> op=<op>` 契约与 op 值域 {gc, export}；代码侧二选一（409 detail 引错误串 / `storage.LockHolder` 助手）仍归 T-94 派单注记。
