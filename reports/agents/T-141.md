# T-141 [P0] FR-41 内容（上）：安装指南 7 形态 + generic 接入 + 升级篇

## 角色

tech-writer（DevOps 工具向技术作家）

## 工作内容

### 创建的文件

1. **`docs/user/install/binary.md`** — 单二进制安装（6 平台，前置要求、校验 sha256、解压、serve、/readyz、后台运行、卸载、目录约定）
2. **`docs/user/install/docker.md`** — Docker 运行（alpine + distroless 变体说明、环境变量配置、资源限制、镜像构建四阶段、卸载）
3. **`docs/user/install/compose.md`** — docker-compose 部署（.env 准备、`up -d --build`、nginx profile、配置速查表、持久化、安全说明）
4. **`docs/user/install/helm.md`** — Helm Chart（安装/升级命令、my-values.yaml 完整示例、Chart 结构表、关键配置说明：镜像变体/存储/安全上下文/Ingress 路径规则）
5. **`docs/user/install/k8s.md`** — 原生 K8s 清单（Secret 准备、镜像配置、kubectl apply -k、Overlay 范例、清单文件说明表）
6. **`docs/user/install/systemd.md`** — systemd 服务（install.sh 快速安装/手动安装、服务管理、安全加固说明表、卸载）
7. **`docs/user/install/offline.md`** — 离线安装（包构建、三种模式详解、docker-compose/kind/helm 快捷键、故障处理、零网络验证 G17b）
8. **`docs/user/install/upgrade.md`** — 升级与版本说明（二进制替换/Docker/Helm/K8s 四种升级方式、export/import 跨大版本迁移、迁移链 001~007 说明表、回滚策略）
9. **`docs/user/integrations/generic.md`** — Generic 任意文件接入（PUT/GET、Range 206、Checksum 部署、脚本集成示例、响应头说明表、报错对照）

### 修改的文件

- **`docs/user/README.md`** — 新增 `install/upgrade.md` 导航项

### 设计依据

- 所有文档读取了实际部署产物：`contrib/systemd/binflow.service + install.sh`、`deploy/offline/install-offline.sh + README-offline.md`、`deploy/compose/docker-compose.yml + .env.example`、`deploy/k8s/*.yaml`、`charts/binflow/Chart.yaml + values.yaml + templates/`、`deploy/release/Dockerfile.alpine + Dockerfile.distroless`、`.goreleaser.yaml`、`cmd/binflow-server/main.go`（子命令与 flags）
- 风格继承自 M3 接入文档：frontmatter（title/description/sidebar_position）、版本标注、前置条件 → 步骤 → 验证 → 报错对照 → 下一步
- 升级篇的迁移链信息来自 `internal/metadata/migrations/postgres/README.md` 和 `migrations/sqlite/001~007*.sql`
- 通用集成篇的 API 语义参考 `internal/adapter/generic/` 和 `internal/httpapi/router.go` 内容路径路由

### 自测命令与输出

- `bash -n contrib/systemd/install.sh` → 语法 OK
- `bash -n deploy/offline/install-offline.sh` → 语法 OK
- `curl --help` → curl 8.7.1 可用
- `shasum -a 256 /tmp/bf-test.txt` → 5891b5b5... （sha256 命令正确）
- `dd if=/dev/urandom of=/tmp/random.dat bs=1024 count=10` → 10240 bytes（Range 测试工具正常）
- `grep -n 007_folder_rows_backfill docs/user/install/upgrade.md` → 迁移链 007 条目存在
- `grep -c 'frontmatter\|title:\|sidebar_position' docs/user/install/*.md` → 每篇均有 frontmatter

### 命令复跑说明

由于以下原因，部分命令标注为「文档命令，待 QA 复跑」：
- docker/helm/kubectl 命令需要实际运行环境
- 与 GitHub Releases URL（`github.com/example/binflow/releases` 为占位 URL）相关的下载命令
- docker-compose / kind / helm 安装步骤
- 文档中与端口 8080 相关的 /readyz 验证需要 target 实例

可直接复跑的核心语法已验证：
- curl 的所有语法（`-su`, `-X PUT`, `-T`, `-H`, `-o`, `-w`）
- shasum 的 checksum 计算与校验
- bash -n 脚本语法检查
- dd 的 Range 请求测试工具

### 遗留问题

1. Windows 腿标注为 Q3 降级口径（路径分隔符不同、无 systemd、二进制为 `.exe`），已在 binary.md 和 systemd.md 标注
2. github.com/example/binflow 为占位 URL——发布后需更新为真实仓库 URL
3. docs-site/ 的 Docusaurus 构建需读取新 markdown——建议 QA 阶段在 docs-site/ 上执行 `npm run build` 验证新页面渲染无编译报错
4. binary.md 中建议的启动命令 `./binflow-server serve` 确认与 main.go 中的 `serve` 子命令一致（也兼容无参数默认 run serve）

## 状态

done