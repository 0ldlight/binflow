# T-77 — M3 用户文档（DoD 第 4 条）

- ticket: T-77 [P1] M3 用户文档（AC 全文见 `reports/agents/T-61.md` T-77 节）
- 角色: tech-writer；area: `docs/user/`
- 日期: 2026-08-20
- 输入（技术事实来源，均已完成验收）: `reports/agents/T-74-qa.md`（三协议功能矩阵 49/49）、`reports/agents/T-75-qa.md`（remote/virtual + SSRF 25/25）、`reports/agents/T-76-qa.md`（客户端矩阵 + 回归 + 性能 + M3 QA 总报告）、`reports/agents/T-46.md`（docker 指南范式与 Docusaurus frontmatter 子集，ADR-0011）、`docs/prd/milestone-3.md` **v1.2**（§2.2 有意不兼容全表、§5.5 C1~C8 定案、报错码勘误口径）、DECISIONS.md ADR-0012（remote 基线 + 勘误一/二）/ ADR-0013（virtual 两桶序 + 联动记录）、`docs/design/architecture.md` M3 网络增补（出站 443/80）、`internal/repo/config.go`·`internal/remote/*`（报错文案逐字核对）、BOARD.md（只读）
- 结论: **done**。AC 三条全满足；五条核心链 + 管理面事实组在 T-76 验收基线（commit `0f86229`）复跑，退出码全 0（预期失败码逐字对齐）。

## 1. 产出

| 文件 | 内容 |
|---|---|
| `docs/user/integrations/maven.md` | Maven 接入（URL 形态/settings.xml/altDeploymentRepository/pom repositories/mirror 收口/snapshot 与 -U/checksum 三态/匿名凭据/Gradle P2 注记；报错表 7 行；不兼容 5 行） |
| `docs/user/integrations/npm.md` | npm 接入（registry URL 形态/.npmrc registry 限定 `_auth`/publish plain+scoped/install/dist-tag/unpublish/login/remote+virtual 用法与 npmjs 上游边界/匿名凭据；报错表 7 行；不兼容 6 行） |
| `docs/user/integrations/pypi.md` | PyPI 接入（pip.conf/twine .pypirc 即拷即用/hash 对账/wheel+sdist/依赖链/PEP 691/remote+virtual/匿名凭据；报错表 6 行；不兼容 6 行） |
| `docs/user/admin/remote-virtual.md` | remote/virtual 管理指南（建仓字段表 v1.2 默认值 7200/1800/15/300/false、六步请求流与缓存/负缓存/assumed-offline、DELETE=强刷、写路由**【暂行】**标注、SSRF 放行操作指引（allowPrivateUpstream 审计与风险边界）、出站 443/80、`BINFLOW_REMOTE_CREDENTIALS_KEY` 部署与无钥 fail-fast、上游兼容性速查、M3 有意不兼容**全表**、跨域报错码汇总 14 行） |
| `docs/user/README.md` | 导航补 M3 条目（三协议 + admin/remote-virtual） |

frontmatter 仅 `title` / `sidebar_position`（ADR-0011 兼容子集，与 docker-registry.md 先例一致）；中文叙述、命令/标识符英文；每篇头部标注适用版本（M3 / PRD v1.2）与复跑基线（`0f86229`）。

## 2. AC 对照

| # | AC | 结果 |
|---|---|---|
| ① | 三协议接入指南（配置即拷即用 + scoped/snapshot/checksum/匿名凭据）+ remote/virtual 管理指南（字段表定案默认值、缓存语义、强刷、写路由【暂行】、SSRF 放行指引、出站 443/80、凭据密钥与 fail-fast） | ✅ 四篇齐；写路由节显式标【暂行】（PRD Q2 未定案）；SSRF 节含操作四步 + 三条风险底线 + 审计查询 |
| ② | 有意不兼容清单（§2.2 全表）+ 常见报错码对照（v1.2 定案码） | ✅ remote-virtual.md §「M3 有意不兼容清单（汇总）」13 行覆盖 §2.2 全部条目；跨域报错码表 14 行（409 snapshot/403 重复 publish/405 写拒绝/404 Checksums are not downloadable./400 SSRF/404 offline/502 hardFail 等，message 逐字取自代码 `internal/repo`、`internal/remote`、`internal/adapter/{maven,npm,pypi}`）+ 三篇指南各带本域表 |
| ③ | 全部命令在 T-76 验收产物上抽样复跑（退出码 0） | ✅ 见 §3：五条核心链（maven deploy+resolve、npm publish+install、pypi twine+pip、remote 缓存循环、virtual 收口+写路由）+ 管理面事实组；正向链退出码全 0，预期失败码（404/405/401/exit 2）逐字对齐文档 |

## 3. 抽样复跑记录（AC③）

环境：T-76 验收基线 **commit `0f86229`** 的干净 git worktree（`/tmp/t77/src`），`go build` → `/tmp/t77/binflow-server`（19.6MB）；实例 `127.0.0.1:8280`（`BINFLOW_ADMIN_PASSWORD` 一次性随机 + `BINFLOW_REMOTE_CREDENTIALS_KEY` 随机 32B + `BINFLOW_SERVER__LISTEN`）；mock 上游 `python3 -m http.server 9299`。客户端：mvn 3.9.9 + Temurin JDK 21.0.12（/tmp/t67-tools）、npm 10.9.8（node 22.23.2）、pip 26.2.1 + twine 7.0.0（独立 venv，自公网 PyPI 装入）、curl/jq、python3 3.14.6。全部工作资产隔离于 /tmp/t77（不触用户 ~/.m2、~/.npm、~/.cache/pip）。

| # | 链 | 命令要点 | 结果 |
|---|---|---|---|
| R1a | mvn deploy | `-s settings.xml -DaltDeploymentRepository=binflow::default::$BASE/binflow/maven-local deploy` | **DEPLOY_OK exit 0**（pom/jar/旁车/metadata 201） |
| R1b | mvn resolve | 消费 pom `<repositories>` + `-Dmaven.repo.local` 全新仓 compile | **RESOLVE_OK exit 0** |
| R1c | mvn mirror 收口 | settings `<mirror><mirrorOf>*</mirrorOf>` → maven-virtual | 首跑**失败**（virtual 无 Central 成员，默认插件 404）→ 建 `maven-remote-central`（https://repo.maven.apache.org/maven2）+ 清 `.lastUpdated` 后 **MIRROR_RESOLVE_OK exit 0**；`maven-resources-plugin` 经 virtual GET 200（Central 真上游代理同时实证）。两坑写入文档 |
| R2a | npm publish | 项目 `.npmrc`（registry + `//host/path/:_auth` + always-auth）→ publish/whoami/view | `+ demo-pkg@1.0.0`、`admin`、`1.0.0` 全 exit 0；**裸 `_auth` 首跑被 npm 10 拒**（`ERR_INVALID_AUTH: '_auth' must be renamed to ...`）——坑写入文档 |
| R2b | npm install | 项目 .npmrc + `npm cache clean --force` 后重装 + `require` | `added 1 package` ×2 exit 0，`require("demo-pkg")()` → 42；**consumer 无 .npmrc 首跑装到公网同名包**（demo-pkg@1.0.1+colors）——坑写入文档 |
| R3a | twine upload | `.pypirc`（`[binflow] repository/username/password`）+ `twine upload --repository binflow dist/*` | exit 0（服务端 200） |
| R3b | pip install | `PIP_CONFIG_FILE=pip.conf`（`[global] index-url=.../simple`）+ `--target` 安装 + import | exit 0，`demo_pkg.hello()` → `binflow-pypi-ok` |
| R3c | hash 对账 | simple 页 `#sha256=` fragment vs `shasum -a 256` 下载文件 | **HASH_MATCH**（c063cb42…） |
| R4a | remote 建仓+回显 | PUT generic-remote（allowPrivateUpstream + username/password）→ GET 配置 | 200；`configuration` 内默认值 **7200/1800/15/300/false** 逐字段对；**password 不回显** |
| R4b | 缓存循环 | GET（MISS）→ GET（HIT）→ 上游计数 | `X-Binflow-Cache: MISS`→`HIT`，内容 cmp 一致，上游 GET 计数 **恒 1**（命中期冻结） |
| R4c | 定案码 | `.sha1` 后缀 / PUT / DELETE 强刷 | `.sha1` → **404** `{"status":404,"message":"Checksums are not downloadable."}` 逐字；PUT → **405 + Allow: GET**；DELETE → **204** → 上游换内容后 GET `MISS` 且**取到新内容**（强刷手法实证） |
| R4d | 凭据落盘 | `strings` 元数据库+WAL | `enc:v1:` ×1、明文口令 ×0（NFR-S14① 同构复证） |
| R5a | virtual 收口 | mvn-virtual [maven-local, maven-remote-x(mock)] 解析 demo-app（本地）+ up-lib（上游） | **VIRTUAL_RESOLVE_OK exit 0**；`X-Binflow-Resolved-From: maven-local` / `maven-remote-x`（+`Cache: HIT`）双头齐（M50 门槛） |
| R5b | 写路由 | 未配 → PUT；配 `defaultDeploymentRepo` → PUT/双路径 GET | 未配：**405 + Allow: GET** + C5 文案**逐字**（`No local repository was configured as local deployment repository for the (maven-virtual) virtual repository.`）；配置后：virtual PUT **201** → virtual GET `Resolved-From: maven-local` + 成员路径 `/binflow/maven-local/...` **200**（node 实落成员） |
| X1 | 探针/匿名 | `/-/ping`；匿名 npm PUT | `{}` 200；匿名 PUT **401** |
| X2 | 无钥 fail-fast | 停服 → 去 `BINFLOW_REMOTE_CREDENTIALS_KEY` 重启 | 进程 **exit 2**，日志含逐字 `stores encrypted credentials but no master key is configured: set BINFLOW_REMOTE_CREDENTIALS_KEY (base64 of exactly 32 bytes) and restart`；带钥重启 health 200 且已缓存路径直接 **HIT**（缓存跨重启保留实证） |
| X3 | 放行审计 | sqlite 查 `audit_events` | `repo.create|generic-remote|{"allowPrivateUpstream":true,...}` 等 2 行（审计查询手法即文档示例） |

正向链（R1a/R1b/R1c/R2/R3/R5a/R5b-配置后）退出码全 0；预期失败码（404/405/401/exit 2/R2a 首跑）均为设计行为且与文档口径逐字一致。未在本轮复跑的命令（snapshot `-U`、dist-tag/unpublish、checksum 409 两态、PEP 691、依赖链、SSRF 拒绝矩阵、1GB 流式等）全部引用 T-74/T-75/T-76 验收记录（同一基线 `0f86229` 谱系），各篇头部已如实标注复跑项与引用项的边界。

## 4. 文档事实核对来源（抽样）

- 报错 message 逐字取自代码：`internal/adapter/maven/put.go`（409 snapshot/checksum）、`internal/adapter/maven/layout.go`（400 layout 两式）、`internal/adapter/npm/errors.go`（403/404 npm）、`internal/adapter/pypi/upload.go`（400 重复 filename/unknown action）、`internal/repo/service.go`（405 remote 只读）、`internal/repo/virtual.go`（C5 与 routed-DELETE 文案）、`internal/remote/fetcher.go`（SSRF/offline/hardFail/401 摘要）。
- 字段名/默认值：`internal/repo/config.go`（remoteConfig 全字段、默认常量、unknown-field 容忍——迁移脚本兼容姿态）；`internal/adapter/maven/config.go`（checksumPolicyType/snapshotVersionBehavior/handle* 三组取值与 M3 unique=P2 行为）。
- `priorityResolution` 为 **per-repository 字段**（标记在成员仓自身配置，非 virtual 成员数组）——`internal/repo/virtual.go` 两桶序注释核对后写入文档（易错点）。
- 无钥运行时建带密码仓 → password 丢弃 + WARN、回源走匿名：`internal/repo/service.go sealPassword` 行为核对（区别于「库中有密文而无钥」的启动 fail-fast，两者都写入文档）。
- 出站 443/80：`docs/design/architecture.md` M3 网络增补（ADR-0012）——compose/Helm 不内置 egress 代理、网络策略归用户。
- npmjs 真上游不可代理（packument 布局边界）/ pypi.org 真上游可用（302 跟随）：T-75 §2-M46/M47 与观察项 O3。
- PEP 691 JSON simple 已支持：T-74 M35b（P2 提前收口），文档如实写「已支持」。

## 5. 观察项（转 conductor，均不阻塞）

- **O-1（npm 客户端坑 ×3 已收编进文档）**：npm 10 项目级裸 `_auth` 被拒（须 registry 限定形态）；consumer 目录缺 `.npmrc` 时静默回落公网 registry（可能装到同名公网包——供应链风险，建议 CI 里显式配 registry 或 `--registry`）；mirror `*` 形态要求 virtual 含 Central 成员且 mvn `.lastUpdated` 负缓存会掩盖修复。三条均为本轮复跑首跑失败实证。
- **O-2（T-76 O3 base_url 建议归属）**：反代/多视角场景「不设 base_url（或设客户端可达名）」的建议属 docker/部署篇，本轮未动 `docker-registry.md`（非本票票面内容），建议随部署文档票收编。
- **O-3（faq.md 占位链接）**：四篇与 docker-registry.md 先例一致地链接 `faq.md`（尚不存在，README 已注明随里程碑补齐）；Artifactory 迁移对照表归 FAQ 票。
- **O-4（`npm login --auth-type=legacy`）**：交互式流程未在本轮直跑（NE-06 端点已由 T-69/T-74 覆盖，QA 按 .npmrc 等效凭据验证）；文档按 PRD 口径描述并提示日常用 `_auth` 等效。

## 6. 清理

- 实例（8280）、mock 上游（9299）已停止，端口无监听残留；/tmp/t77（worktree、二进制、数据目录、venv、npm/mvn/pip 工作区、一次性口令与密钥的 env.sh）已删除；git worktree 已注销。
- 主仓工作树新增/修改仅 docs/user 五个文件（integrations/ 三篇 + admin/remote-virtual.md + README.md）；/tmp/t67-tools 为既有工具目录未动。

## 7. AC 判定

| AC | 判定 |
|---|---|
| ① 三协议接入指南 + remote/virtual 管理指南 | ✅ |
| ② 有意不兼容全表 + 报错码对照（v1.2 定案码） | ✅ |
| ③ 命令在 T-76 验收产物上抽样复跑退出码 0 | ✅ 五核心链 + 三事实组（详 §3） |

**状态: done（待主会话审核提交）。**
