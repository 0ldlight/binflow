# L023-2B — append + promote 对位（§11.10 票 B 边界）

Ticket:       L023-2B（LOOP 023 批次 1 票 B：append 列表拼接 + promote 九步执行序 + 403 文案族）P0
Role:         dev-go-core（area = internal/build + internal/httpapi + internal/metadata 迁移增量）
Area:         与 L023-2A 同域（既有 internal/build，无新包——票 A 已裁决并上报的落位）
Input:        docs/reverse/build-info.md §2.4（promotion 状态机/body）/§7（403 文案族六条逐字）/§11.1（E3/E5/E12）/§11.4（append 深规格）/§11.5（promote 九步）；L023-1 报告（活体探针：append 同 id ×2 → GET 两行 mod-a / promote status-only INFO / targetRepo 404 逐字 / failFast E12 400）；既有 internal/build/promote.go（T-509）+ upload.go Append/mergeModules（T-508）
Changes:      按序：
              1) **append 列表拼接（E5 翻案）**：mergeModules 按 id 合并（被 E5 证伪的语义）删除，改为纯列表拼接（existing 在前 + incoming 尾接，整段重存）；toStoreModules 的「同文档 duplicate module id 拒绝」移除（block.duplicate.entries 缺省 false）；同文档重复 id 也并存。
              2) **026 迁移（schema 前置）**：024 的 build_modules PK 含 module_id——重复 id 无法落库。新增 026_build_module_ord.sql（sqlite+postgres 双方言）：三表重建为 ord 键（modules PK=(coords,ord)；artifacts/dependencies PK=(coords,module_ord,seq)，FK 随迁）；ROW_NUMBER 按 module_id 定序赋 ord，旧行 1:1 JOIN 回填；索引随 DROP 重建。PutModules/ListModules 改 ord 读写与拼接（ListModules 按 ord 返回，attach 按 ord stitch）。
              3) **promote 九步对位（§11.5）**：③ 虚拟仓解析——targetRepo 为 virtual 时经 Config blob 三别名（defaultDeploymentRepo/…Ref/DeploymentRepository，routeVirtualWrite 同集）解析 default deployment local 仓，req.TargetRepo 替换为解析后键（statuses[].repository=解析后仓）；④ target 校验——缺失/非 local 族/不可解析 virtual 统一 404 逐字 `Cannot find target repository by the key '<caller key>'`（原 400 作废）；无 target INFO 逐字 `Skipping build item relocation: no target repository selected.`；⑤ 收集——不可解析制品（record-only/节点缺失/源仓消失）E12 双文：failFast（缺省）error 行逐字 `Unable to find artifacts of build '<name>' #<number> from <buildRepo> repo: aborting promotion.` + 中止；lenient 跳过 + 走完一遍后单条 warning 逐字 `Unable to find the following artifacts of build '<name>' #<number>: <names>`（", " 连接）；⑥ 搬迁失败 error 行 `Error occurred while copying/moving: <cause>`（carrier 4xx refusal 文案即 cause）；⑦ 属性——upfront a() 403 门删除，改逐项 annotate 检查（warning 逐字 `User doesn't have permissions to annotate '<path>'` + failFast 中止标注）；⑧ 状态记录——failFast+有 error/warning → 跳过 + info 逐字 `Skipping promotion status update: item promotion was completed with errors and warnings.`；status 空 → 跳过 + info 逐字 `Skipping promotion status update: no status received.`（原「空 status 照落历史行」作废）；timestamp 非法 → **更新时**跳过（原 upfront 400 作废）+ error 逐字 `Skipping promotion status update: invalid\unparsable timestamp <ts>.`（字面单反斜杠）；promotionStamp 统一 SSSZ 形态（服务器时钟也 .SSS+0000，原 RFC3339 作废）；⑨ 响应——level 全大写（E3，msg() 统一 ToUpper）；failFast + 任一 ERROR/WARNING → **400 + 同形 messages body**（PromotionResult.HTTPStatus；原 StatusError errors[] 信封作废）。run 404 逐字 `Cannot find a build by the name: <n>, number: <n>, repo: <r>`（handler 拦截）。
              4) **403 文案族（§7 逐字）**：新增 ForbiddenError（Unwrap→ErrForbidden 保 sentinel；wire 渲染干净句无 wrap 前缀）——read `The user: '<u>' is not authorized to access build info. Read permission is needed.` / upload `…upload build info. Upload permission is needed.` / delete `…delete build info. Delete permission is needed.` 落位：GET/read 面、PUT w 门、覆盖 d 门、append（d 先 w 后，§11.4-2 断言序）、DELETE/retention d 门、promote build 侧门。writeBuildError 增 ForbiddenError 臂（403 逐字）。
              5) timestampDate 解析兼容 SSSZ+RFC3339（票 A 遗留 RFC3339 行）。
Files:        新增：internal/metadata/migrations/{sqlite,postgres}/026_build_module_ord.sql；internal/httpapi/build_append_promote_semantics_test.go（六用例：append 重复条目/status-only 逐字+statuses wire/E12 双臂/target 404/403 族）；internal/metadata/build_module_ord_migration_test.go（026 数据搬运升级测试）。
              修改：internal/build/upload.go（拼接法+重复 id 接受+append 门序/文案）；internal/build/promote.go（九步全套+ForbiddenError 接线+errAbortPromotion 解卷）；internal/build/service.go（ForbiddenError 型+forbiddenf）；internal/build/delete.go（§7 delete 文案）；internal/metadata/substores_builds.go（ord 读写）；internal/httpapi/build.go（promote 404/messages 状态/ForbiddenError 臂/timestampDate 双形态）。
              测试对位：internal/build/upload_test.go（拼接法重写+单文档重复 id 臂）；internal/build/promote_test.go（E12 双臂/target 404+路由 virtual 臂/annotate 逐项 warning 臂/wenda 权限面扩源 r）；internal/httpapi/build_promote_test.go（INFO 大写/run 404 逐字/timestamp 跳行/target 404 独立臂）；internal/httpapi/build_rest_test.go（append 拼接断言）；internal/metadata/builds_store_test.go（ord 序契约）。
              删除：无。
Tests:        新增 9 用例：append 重复条目（服务级 3 行 + REST 级 mod-a×3 各持己物——diff 断言钉死「不合并」）；promote status-only 逐字 INFO 行 + statuses wire（SSSZ/timestampDate/user/comment/repository-按需省略/ciUser 省略）；E12 failFast 400（aborting 句 + 跳行通知 + 无历史行）与 lenient 200（单条 names warning + 历史行落地）；target 404 逐字（REST 两处 + 服务级 missing/unrouted/routed-virtual 三臂——路由 virtual 解析后 200 落历史）；403 族四臂逐字（read/upload/delete/append-d-先）；026 迁移数据搬运（024 形状两 run 真行回绕 → 升级 → 行全存活 + 升级后重复 id 可写）。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；make lint（0 issues）；go test ./internal/build/ ./internal/metadata/ -count=1；go test ./internal/httpapi/ -count=1（全量 186.6s）；go test -race ./internal/build/ -count=1（45.9s）；go test -race ./internal/metadata/ -count=1（188.1s）；go test -race ./internal/httpapi/ -count=1 -run 'TestAppendDuplicates|TestPromote|TestBuildRESTPromote|TestBuild403|TestBuildRESTAppend|TestCurlCompatBuild'（15.3s）；冒烟：隔离实例 127.0.0.1:18098（独立 data-dir，零 docker 零 172.16.58.130，用后清除）curl 八臂 + jf CLI 2.122.0 真腿两条。
Outputs:      四门全绿（build/vet 0、gofmt 空、golangci-lint 0 issues、三包 -count=1 + 三包 -race 全 ok）。冒烟（§11.4/§11.5 逐断言）：B1 seed PUT 204；B2 同 id append ×2 均 204；B3 GET 回显 `[('mod-a',['a.jar']),('mod-a',['a.pom']),('mod-a',['a.pom'])]`——三行 mod-a 各持己物（E5 钉死）；B4 status-only promote 200 `[{level:"INFO",message:"Skipping build item relocation: no target repository selected."}]`；B5 坏 targetRepo 404 `Cannot find target repository by the key 'no-such-repo'`；B6 failFast 400 messages body 双行（E12 aborting 句逐字 + `Skipping promotion status update: item promotion was completed with errors and warnings.`）；B7 lenient 200 `Unable to find the following artifacts of build 'probe-b' #1: a.jar, a.pom, a.pom` + 状态落地；B8 statuses wire 两行（released/smoked）：timestamp `.SSS+0000` SSSZ、timestampDate epoch-ms、user=admin、repository=rel-smoke（有 target 时）、ciUser 省略。**jf 真腿**：`jf rt build-publish jf-app 7`（2.122.0，url=http://127.0.0.1:18098/binflow）→ PUT 落库（GET 号单 200；CLI 尾部 version-parse 报错 = T-508 登记的 /api/system/version='dev' 既有债，非本票面）；`jf rt build-promote jf-app 7 rel-smoke --status=smoked` → CLI 成功 + statuses 落地全 wire 形态。探针 build 用后 deleteAll 清零。
Compatibility: 契约对照（D07 契约族仍待 compatibility-engineer 落盘）：E5（拼接不合并——服务+REST+迁移三层钉死，diff 断言断言重复条目）；E3（level 大写）；E12（双文案逐字——aborting 句无名单、lenient 单条 warning 带名单）；§11.5-3/4（virtual 解析 + 统一 404 逐字）；§11.5-8（三条 Skipping 行逐字含字面反斜杠 invalid\unparsable）；§11.4-2（append d 先 w 后断言序）；§7 403 三文案逐字（read/upload/delete）。**登记漂移/留痕**：① promote build 侧门 = ADR-0045 点 5 的 r(buildRepo)（§11.5-1 是 upload 权断言）——语义门未动（架构契约归 architect 裁），文案按实际跑的门（read 句）渲染，ADR Accepted 期复核输入；② §11.5-7 逐项 annotate warning 取代 ADR-0045 Errata ④㋔的 upfront a(targetRepo) 403 门——效力序（规格>ADR-0045）落位，ADR 复核输入；③ E12 lenient 名单连接符 ", " 为合理推断（活体样本单名，未钉多名字面）——低风险待契约探针；④ promote 空间坐标 blank name/number 400（`Build name cannot be blank`）路由语法上不可达（path 段非空）——POST /build/delete body 面（票 C）可达，届时落位；⑤ 413 配额文案（`Promotion status update … storage quota exceeded`）BinFlow build JSON 走 DB 无配额面——未实现登记；⑥ manage/basic-read/permissions-view 三条 §7 文案无对应面（BinFlow 无独立 build 权限可视面）——不承接；⑦ comment 空值回显 ""（§3.1 只点名 repository/ciUser 整体省略）——按明载两字段执行。
Security:     §7 403 文案含用户名内插（内插值经 writeError JSON 转义 + nosniff 家族）；append 门序 d-先（§11.4-2）保证无 oracle 姿势不回退；properties 逐项 annotate 检查防越权标注（原 upfront 门更严但被规格证伪为 warning 姿势——权限判定本身仍在 allow() 单点）；迁移 026 双方言语句参数化之外零拼接（静态 DDL）；jf 冒烟口令仅 shell 传递，实例与临时文件用后清除。
Performance:  无新测量面。026 迁移一次性三表重建（携带行 1:1 JOIN，测试覆盖）；ListModules 三读 stitch 改 ord 键（同量级）；promote failFast 中止走 errAbortPromotion 解卷（无额外查询）；名清单 warning 单行收集（O(n) 内存，制品数量级 KB 级 CI 元数据）。其余无影响。
Risks:        ① 026 迁移对在途部署是破坏性 schema 变更（DROP+RENAME 三表）——数据搬运有专测覆盖，但生产升级路径（serve 启动自动迁移）建议 dev 实例先演练一轮（冒烟实例是 fresh DB，未覆盖「真实体量携带升级」）；② promote 响应形变（failFast 400 从 errors[] 信封 → messages body）对现有消费方是 breaking——jf CLI 真腿验证通过（E12 400 未触发于 jf 路径），契约站快照需更新；③ E12 lenient 名单连接符与 promote 各 INFO 汇总句（非 §11.5 逐字面）为 BinFlow 自有文案——契约化时以「实现真值」定 golden；④ postgres 025_release_bundles.sql 缺失（sqlite-only，既有漂移非本票造成）——026 postgres 已按 lockstep 补齐，025 postgres 建议补票；⑤ append 并发读改写仍全局锁（CI 频率下免费——存量注释口径维持）。
Blockers:     无。
Next:         ① conductor/architect：Changes-3①② 两条 ADR-0045 复核输入（promote build 侧门 r vs upload；annotate 403→warning）；② 票 C（批删 POST /build/delete 6 字段 body + retention §11.6；blank-name/number 400 文案在 POST /delete 面落位）；③ compatibility-engineer：D07 契约族 golden 候选=本票冒烟输出（append 重复条目/E12 双臂/Skipping 三行/403 三句）；④ postgres 025 补齐小票；⑤ version='dev' 债（jf 尾错）候票。
