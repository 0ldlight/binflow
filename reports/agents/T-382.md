# T-382 工作日志 — FR-124.1 D1 Set Me Up 抽屉化（M14 B1 FE 主轴票）

- 角色：dev-frontend ｜ 状态：**done**（四闸门 + 全量 Playwright + armed 双腿全绿；4 例全量红全数按 flake 协议串行复跑甄别为绿）
- area：`web/src/components`（SetMeUpDialog/dialogs.css/AppShell 注记）+ `web/src/pages/repositories/commands.ts`（Set Me Up 数据源——本票独占消费面）+ `web/e2e/m8/setmeup-deploy.spec.ts` + 锚册 console-ux.md（v1.18 批块）。**并行票 T-392（internal/adapter/docker + internal/repo）零触碰；DeployDialog 零改动（决策项 C 暂行）**
- 票面参数源：**T-381 活体核验 v1.1**（reports/agents/t381-evidence/t381-measurements.json + 20~22-setmeup*.png）——右抽屉 50vw 档（覆盖 BOARD 原票面 480px 旧值）、Tab 三枚 Configure/Deploy/Resolve、步 0 药丸、片段平铺、底栏「返回链接 + Done」

## 1. 实现

### 1.1 壳：居中 Dialog → 右侧 Drawer（SetMeUpDialog.tsx）

- `Drawer anchor="right" variant="temporary"` + `slotProps.paper`：`width: 'min(clamp(480px, 50vw, 800px), 100vw)'`（**票内定案留痕**：两侧钳制取 v1.1 实测区间端点〔1600 视口 800px / 1280 视口 640px〕，`min(…, 100vw)` 兜窄屏防横向溢出）+ `display:flex; flexDirection:column`（SubscriptionDrawer T-366 同款姿势）。
- 头部：标题（`DialogTitle id="smu-dialog-title"` 承 `aria-labelledby` 可及名）+ 右上 `IconButton` X（`aria-label="关闭"`，**`smu-close` 复役**——抽屉族通用规格 §4）。
- 底栏：`DialogActions sx={{justifyContent:'space-between'}}` = 左「← 选择不同的包类型」文本链接（`smu-back` 锚不变、自内容区顶部迁底栏左）+ 右「完成」contained 主按钮（**`smu-done` 复役**）。Artifactory v1.1 实测底栏形态。
- **锚族冻结**：`smu-*` 全族零改名——`smu-dialog` 落 Drawer paper（e2e 选择器/axe include 域原样）、`smu-grid`/`smu-grid-item-<pt>` 与 radiogroup 键盘链路（←↑→↓ 循环 + Enter/Space 选择）原样。
- Esc/backdrop/回焦：temporary Drawer 走 MUI Modal 底座（TrapFocus + restoreFocus + backdropClick + Esc）——AC1 的 L02 关闭回焦由 MUI 归焦承载；既有文档级 Esc 兜底监听保留（药丸/主面板切换会卸载焦点元素、焦点掉 body 的场景，注释随票更新）。
- AppShell `resumeOpen` 分支（OIDC 回跳重开）零逻辑改动（注记随壳形态更新）。

### 1.2 步 0：网格 → 药丸横排（dialogs.css）

`.smu-grid-items` grid → flex wrap；`.smu-grid-item` 竖卡 → 胶囊（inline-flex + `border-radius:999px`，形状值非色值——token 纪律不涉）；desc 收进 `title` 提示（药丸紧凑、信息不丢）。五项闭集（generic/docker/maven/npm/pypi）与「只列实例内已有仓库的包类型并集」语义不变。

### 1.3 Tab 三枚化：内容三分映射（commands.ts + 组件，票内留痕）

| 包型 | Configure（配置+凭据） | Deploy（推，原样） | Resolve（拉，T-382 新面） |
|---|---|---|---|
| generic | **无配置步** → 组件给导航提示（curl 开箱即用） | curl PUT | 「下载与校验（curl）」**自 Configure 迁入** |
| docker | 「Docker 登录」**自「登录与拉取」拆出** | build+push | 「拉取镜像」**拆出**（note：镜像名首段=key；登录见 Configure） |
| maven | settings.xml（servers） | mvn deploy | 「解析：pom \<repositories\>」**自 Configure 迁入** |
| npm | .npmrc（registry+_auth） | publish+whoami | 「安装与验证」**新增**（npm install/cache clean/require 验证——npm.md §4 同源，P3 不发明命令） |
| pypi | **无配置步** → 导航提示 | .pypirc+twine | 「安装侧：pip.conf」**自 Configure 迁入** |
| 门控包型 | gatedPkgBlock 兜底 | 兜底（原样） | 兜底（同款） |

- 新导出 `smuResolveCommands(pkg, repo)`（Resolve 侧凭据在 Configure 一次配置、命令不内嵌）；`smuConfigureCommands` 收敛为配置/凭据侧；`clientCommands`（详情页/树页）**原样未动**。
- Tab 键盘：方向键三枚循环（`TABS` 常量 + 取模循环，role=tablist 语义不变）。
- 命令块平铺 + 右缘 Copy 维持（BinFlow 无手风琴——parity §D1「按工具折叠是超集增强，保留无妨」反向成立：本来就平铺）；`pre` 的 `overflow-x:auto` 不折行维持（pages.css 既有规则，spec 补 computed 断言）。

### 1.4 文案随壳：「关闭对话框后不可再查看」→「关闭抽屉后不可再查看」、「本对话框的上下文会被记住」→「本抽屉的上下文会被记住」（OIDC 引导面）。

## 2. e2e 迁移（e2e/m8/setmeup-deploy.spec.ts）

- **几何断言（AC1 的「50vw 档——非 480」）**：`expectDrawerGeometry` 先等 Slide 收敛（`transform:none`——入场中途取 box 会读到滑入途中的 x），再断言宽 = `min(max(480, vw/2), 800)`、`x+width = vw`（anchor right）、高 = vh。默认 1280 视口 = 640px（若回退 480 固定档即红）。
- **三 Tab 断言**：`smu-tab-{configure,deploy,resolve}` 三锚在场 + `role=tab` 计数 3；Resolve 面断言 `smu-cmd-res-generic-0` 含 `/binflow/<key>/acme/app.tar.gz` + pre `overflow-x:auto`。
- **焦点**：连打 Tab ×10 焦点仍在抽屉内（AC3 Drawer 形态复测——键盘腿在 Deploy Dialog 有同款，此处对 Drawer 补齐）。
- **关闭三通道分摊**：Done（test 1，断言回焦 `tree-setmeup`——L02）/ 右上 X（grid 腿，`smu-close`）/ 遮罩点击（entry-points 腿，断言回焦 `repos-setmeup-<key>`）；Esc 由 keyboard.spec（quick 菜单链路→回焦 session-toggle）与 oidc-stepup 承载。
- **axe 双主题**：药丸态 + 主面板（Resolve 面）× light/dark 两轮（a11y-sweep 同款 localStorage 姿势——路由面扫不到抽屉态，抽屉 serious=0 门在本 spec 承载）；Deploy Dialog 单主题腿维持（形态未动）。
- **行为语义断言全量保留**：铸币/token 回填/剪贴板全值/Bearer 对账/step-up mock 三段（required→invalid→放行）/armed 实例腿/Deploy 拖拽上传 T-231/三入口——锚与断言语义零删减。
- oidc-stepup.spec.ts / keyboard.spec.ts **零改动回归**。

## 3. 锚册（console-ux.md v1.18）

- T-382 批块入册 §10.2：`smu-tab-resolve` / `smu-pane-resolve` / `smu-cmd-res-<pt>-<i>` + 复役 `smu-close`（头部 X）/`smu-done`（底栏主按钮）——二者自 §10.6 退役表摘除（v1.9 曾以零 spec 消费退役，抽屉化后形态成立且有 spec 消费）；退役行注记同步。
- anchor-audit.mjs STOP 表 +1：`console-artifactory-parity`（v1.18 批块行文引用的上游规范文件名段，册侧解析假阳性——v1.17 `t372-trash-node` 同款处置）。

## 4. 自测证据（实际执行）

```sh
cd web && npm run typecheck        # 通过（零输出）
         npm run lint              # 通过（零输出）
         npm run build             # assert-tokens: OK — css 11 + tsx 46；✓ built in 3.79s
         node scripts/anchor-audit.mjs --ledger   # PASS（unregistered/broken 双零，retired 全部在表）
# 嵌入体积：SPA 353449 B gz（5MB 预算内；T-391 基线 352872 + 577B）

# 实例：/tmp/bf-t382（净实例 :8142）+ /tmp/bf-t382-armed（armed :18094 = token_step_up + oidc→mock-idp:18095；
#       armed 起法随 T-260 §2.2 配方 + BINFLOW_REMOTE_CREDENTIALS_KEY（T-260 后新增的必配项——见契约漂移）
BASE=:8142  npx playwright test e2e/m8/setmeup-deploy.spec.ts --project=chromium   # 6 passed + 1 skipped(armed 腿按探针 skip)
BASE=:8142  npx playwright test e2e/m8/keyboard.spec.ts --project=chromium         # 6 passed
BASE=:18094 npx playwright test e2e/m9/oidc-stepup.spec.ts --project=m9            # 4 passed（含真 IdP 全链）
BASE=:18094 npx playwright test e2e/m8/setmeup-deploy.spec.ts --project=chromium   # 7 passed（含 armed step-up 真门腿 + 双主题 axe）
BASE=:18094 npx playwright test e2e/m8/setmeup-deploy.spec.ts e2e/m9/oidc-stepup.spec.ts --project=chromium --project=m9
                                                                                   # 11 passed（17.5s，最终 spec 态复跑）
BASE=:8142  npx playwright test（全量三 project）                                   # 231 passed / 4 failed / 23 skipped / 3 did-not-run（7.3m）

# 4 例全量红 → flake 协议串行复跑甄别（全绿）：
BASE=:8142  npx playwright test e2e/m13/t366-consumer.spec.ts --project=chromium              # 8 passed（整文件 1.6m）
BASE=:8142  npx playwright test e2e/m8/governance-monitoring.spec.ts --project=chromium       # 3 passed
BASE=:8142  npx playwright test e2e/m9/permissions-mholder.spec.ts e2e/m9/usage-fanout.spec.ts --project=m9 --workers=1
                                                                                              # 8 passed（59.2s）

# 几何实证（一次性探针，用后即删未入库）：1600×900 视口 → drawer {x:800, w:800, h:900}
#   （= T-381 活体实测逐位一致）；1280×720 → w:640（=50vw）；pill {display:flex, radius:999px}；
#   底栏 back.x=648 / done 右缘 1272 / drawer 右缘 1280（左链接右主按钮）。
```

**4 例全量红与本票 diff 的耦合分析**（皆非本票面）：
- `m13/t366-consumer:446`（DRAWER 腿）：**spec 内跨腿依赖 × fullyParallel 调度**——`deadleg` 订阅由同文件 404 行的先前腿播种，两腿分落不同 worker 时各腿独立跑 beforeAll（各起一套 ephemeral server/tmpdir），DRAWER 腿单独成 worker 即无该行；整文件跑 8/8 绿。与本 diff（Set Me Up 抽屉）零关联；本票新增的双主题 axe 腿使全量时序略有推移，是本轮显形的触发面而非根因。**登记为套件观察（遗留 L-1），不追**。
- `m8/governance-monitoring:32`：axe 共居负载假阳性家族（T-352/T-374 在册同族），串行 3/3 绿。
- `m9/permissions-mholder:83` + `m9/usage-fanout:139`：**T-391 已登记的 m9 用户 seed 并行互撞**（`PUT users/u1 → 500 UNIQUE`），串行 8/8 绿——与本票零耦合。

## 5. AC 判定

| AC | 判定 | 证据 |
|---|---|---|
| AC1 抽屉化 + 50vw + 锚族 + Esc/backdrop + 回焦 | **PASS** | `expectDrawerGeometry`（1280→640、右缘贴齐、全高；1600→800 实证）；anchor-audit unregistered/broken 双零 + `smu-*` 零改名（§1.1）；Esc（keyboard/oidc 腿）+ backdrop（entry-points 腿）+ X（grid 腿）+ Done 四通道；回焦断言两处（`tree-setmeup` / `repos-setmeup-<key>`）+ quick 菜单链路（keyboard.spec `session-toggle`） |
| AC2 OIDC 续铸 + step-up 内联 + pre 不折行 + 三 Tab + 底栏 | **PASS** | armed :18094 真实例全链 4/4（fragment 回跳→抽屉自动重开→真 grant 单次消费→Bearer 200→审计 oidc_reauth→重放逐字 401）；step-up 口令腿抽屉内内联（mock 三段 + armed 真门两腿，`smu-stepup` 计数 1 不出二级框）；pre `overflow-x:auto` computed 断言 + 平铺无手风琴；三 Tab 锚 + `role=tab`×3；底栏左链接/右主按钮实测坐标 |
| AC3 四闸门 + axe 双主题 + 键盘复测 + spec 迁移 + 服务端 diff=0 | **PASS** | §4 自测块；axe 药丸+主面板 × light/dark serious=0（governance 腿的红为共居负载假阳性家族、串行绿）；Tab×10 陷阱复测；setmeup-deploy 迁移后铸币/step-up/OIDC 断言全量保留；`git status` 仅 web/ + console-ux.md（internal/console/dist/assets gitignored），Go 源码零改动 |

## 6. 契约漂移

- **BINFLOW_REMOTE_CREDENTIALS_KEY 成为 armed OIDC 实例必配**（非 API 契约，e2e 基建面）：T-260 报告 §2.2 的 armed 起法（仅 `BINFLOW_ADMIN_PASSWORD` + `BINFLOW_AUTH_OIDC_CLIENT_SECRET`）在当前主干已不足——`auth.oidc` 落库需凭据主密钥，缺则启动即拒（`seeding section oidc: field "client_secret": no master key`）。本票按 internal/remote/credentials.go 的口径（base64×32B）补齐后 4/4。**建议 conductor 派 docs/user 或 README 一行修订**（或归 T-260 配方的后续维护票）。
- 服务端 API 契约：无漂移（本票零 API 消费变化）。

## 7. 遗留

1. **L-1（套件观察，非本票）**：`m13/t366-consumer.spec.ts` 的 DRAWER 腿依赖同文件先前腿播种的 `deadleg` 订阅，fullyParallel 下两腿分 worker 时各自独立 beforeAll（独立 ephemeral server）→ 该腿独跑必红、全量偶发红。处置方向（归 QA/conductor）：该文件 `test.describe.configure({ mode: 'serial' })` 或播种挪进 beforeAll。本票未扩面。
2. **parity 册状态列未翻**：console-artifactory-parity.md D1 行仍标「△ 需改造」、§7 矩阵 D1 列仍 △——落地事实的回写归 ux/conductor（本票按锚册纪律只动 console-ux.md；parity 册非本票 area）。
3. npm Resolve 的「安装与验证」命令取自 npm.md §4（cache clean + require 冐锁验证三行）——若 PM 希望 Resolve 面更精简（仅 install 一行），是文案级另裁，锚与结构不受影响。
4. /tmp/bf-t382*（两实例数据 + serve 日志）与 /tmp/t382-*.png 留供复验，可整删。

## 8. 变更清单

```
web/src/components/SetMeUpDialog.tsx    壳 Dialog→Drawer + 三 Tab + 底栏 + 药丸标记 + 文案（关闭抽屉）
web/src/components/dialogs.css          步 0 网格→药丸横排（.smu-grid-* 语义不变）
web/src/components/AppShell.tsx         注记随壳形态（resumeOpen 分支逻辑零改动）
web/src/pages/repositories/commands.ts  smuConfigureCommands 收敛 + smuResolveCommands 新增（三分映射 §1.3）
web/e2e/m8/setmeup-deploy.spec.ts       迁移：几何/三 Tab/陷阱/关闭四通道/双主题 axe + 语义断言保留
web/scripts/anchor-audit.mjs            STOP +1（console-artifactory-parity 行文假阳性，v1.17 同款）
docs/design/console-ux.md               v1.18：T-382 批块入册 + smu-close/smu-done 复役摘表 + 版本行
```
