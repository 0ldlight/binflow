# T-503 — 全量 REST 兼容跨切程：差距清单矩阵（rest-compat-matrix.md）

- **票**：M17 W1 P0 跨切程锚票（M17-SPLIT 全量 REST 跨切程首程 4 票之首）
- **角色**：reverse-engineer
- **日期**：2026-09-06
- **产出**：`docs/reverse/rest-compat-matrix.md`（新建，活体 registry，443 行）+ `docs/reverse/README.md` 索引行
- **git**：零操作（conductor 统一提交）

## 0. 取证基线（Q10 结论执行）

- 7.161 pro 参照容器（router 不拉起）与 t226 OSS 7.84.10（access→PG 拒连）双损坏——**本轮零活体**（T-488 §0 同源结论）。
- 纯书面三源：① **官方 REST reference 三索引实取**（2026-09-06）：`docs.jfrog.com/artifactory/reference/llms.txt`（230 条）+ `docs.jfrog.com/administration/reference/llms.txt`（385 条）+ `docs.jfrog.com/integrations/reference/llms.txt`（38 条）= 653 条目——administration 索引是本轮新发现（legacy help center 301 重定向后经典 REST 面的现行落点：users/permissions/license/system/import-export 等）；关键端点路径以逐页 OpenAPI 块实取核验（existence `/api/repositories/existence`、batch `/api/v2/repositories/batch`、GC `/api/system/storage/gc`、system info `/api/system`、permissions `/api/security/permissions`、license/ping/version 族）；② docs/reverse/ 既有 37 份规格（rest-api.md 权威面 + inv-2 表面测绘 ≈1,866 方法级操作）；③ fern/openapi/binflow.json 158 ops（tools/openapi-spec 生成器维护）。
- 置信度逐行标注：高（双源一致）/ 中（单源）/ 低（推测）。零静默升格：路径字面单源行全部进矩阵 §16 待验证清单（9 条）。

## 1. AC 对账

### AC1 矩阵本体 ✅

- **规模**：195 登记行，14 域分组（D01 制品存储/D02 仓库/D03 搜索/D04 安全/D05 复制/D06 系统/D07 Build-info 指针/D08 RB·Distribution 指针/D09 Webhook/D10 插件/D11 生命周期/D12 协议面/D13 UI 内部面/D14 外部产品面），每域表内按 P0 差异修复→P1→P2→P3 排序。
- **四态分布**：✅ 57 / ◐ 27（差异行级注记在列）/ ❌ 83 / ⛔ 17（pro-only 已裁不做 + xray_tied〔Q9 license 族：`/api/search/license` + Curation + licences.xml 识别族〕+ 物理不可行〔signed URL 云专属、Bintray sunset、SHA2 迁移、HA 集群锁〕+ 产品界外〔Distribution 服务侧、Mission Control/Bridge/JFConnect/Workers/Access 联邦、Xray〕）；另有超集（C 层自有）11 行。
- **158 ops 全部落位**：直接对位 119 / 路径别名变体 19（gc+backups+maintenance+replication 族）/ 纯超集 20（audit/health/session/settings/schedules/cleanup/stats/usage/permissions/test 族等——全部登记为 C 层行，Artifactory 面无对位端点）。
- **档位口径**（沿 aql.md §8.2 先例）：官方「Requires Pro」≠ ⛔——BinFlow 无许可门按官方全集对齐；⛔ 仅四类（见上）。

### AC2 首程三带行集冻结 ✅

- 带① repositories+system（T-504）：13 行（R6 差异修复 + X7 扩张）——R 行含 FR-157① url 前缀、FR-156① configJSON 四域 round-trip、GC/backup 路径别名；X 行含 configurations/v2 读/existence/batch 族/system info/serverTime/configuration（裁定点留痕）。
- 带② storage+security（T-505）：14 行（R9 + X5）——R 行含 propertiesXml/lastModified 两臂、users 回显字段集（gap-endpoints §1 对齐）、组成员写语义不对称、permissions v1 路径族别名、token/keypair 字段集核对；X 行含 Update Properties 三动词补全、encryptedPassword、archive entry `!` 抽取、密码过期/锁定族（容量余量条款）。
- 带③ search+老搜索（T-506）：13 行（R6 + X5 + 对账/不适用 2 行）——R 行含 K64① 大小写校准、E-09 超集口径裁定、prop 任意参数键语义、notUsedSince 参数名口径、dates/creation 400 文案族；X 行含 versions/latestVersion/latestVersionByProperties/badChecksum/archive（低优）。
- 差异修复行全部前置于扩张行；每行 A 层目标 + 置信度 + 冻结依据锚点。
- **backlog registry** 建立（矩阵 §13）：M18+ 八批（B-18a Federation 专程〔T-519 衔接〕/B-18b Lifecycles/B-18c HA/B-R2 复制别名/B-R3 系统长尾/B-R4 安全长尾/B-R5 存储长尾/B-R6 包型扩张/B-R7 插件候裁/B-R8 Distribution 深度候 Q2 翻转）。

### AC3 tech-lead 就绪度 + T-488 零重复 ✅

- **T-488 对账结论：零重复**。D07 Build-info 全部 11 行、D08 Release Bundle 全部 7 行均为**指针行**（对账态 + 实现归属，指向 build-info.md §1 / release-bundle.md §1），零行为条文复制；联动行（D01 行 17、D03 行 1/16/17）同样只登记归属。矩阵 §14 有显式对账表。
- **就绪度自评（矩阵 §15）**：三带行集齐（各在 8~15 行区间）、逐行置信度齐、158 ops 机读底稿齐、与 FR-156/157 既有票边界注明（行=行为核对层、票=实现层——派单时注明消费关系防双改）。**低置信 9 条全部为路径字面/文案逐字级，不阻塞派单**；建议 T-504/505/506 验收段预留活体对拍腿（Q10 出口①修复达成时中置信行升级）。

## 2. 关键发现（供 conductor/tech-lead）

1. **官方文档地形变化（高置信，2026-09-06 实取）**：经典 JFrog REST reference（users/groups/tokens/permissions/system/license/import-export）已从 jfrog.com/help 迁至 `docs.jfrog.com/administration/reference/*`（385 条目）；现行官方把用户/token 主推面指向 **Access 服务 API**（`/access/api/v2/users`、`/access/api/v1/tokens`），Artifactory 侧 `/api/security/*` 标注 deprecated 但仍是工具链事实兼容面——**BinFlow 以 Artifactory 路径族为 A 层对位、Access 面登记缺位**的口径已写入矩阵 D04 头注。backups/repo_layouts/mail/proxy/maintenance 等经典端点在新官方索引**无页**（仅反编译/经典条目在案）——置信度中、进待验证清单。
2. **路径别名族是兼容程主要修复形态之一**：BinFlow 19 个 ops 走自有路径（`/api/v1/system/gc|backups|maintenance`、`/api/v1/replications*`）——建议别名挂载时**并行保留自有路径**（零破坏自有控制台），带①/带② R 行已按此冻结。
3. **license 搜索腿（Q9）落位**：矩阵 D03 行 15 ⛔ + 带③ 3-13 登记行——缺位登记不伪造，与 PM 建议①一致。
4. **超集面 20 ops 全部登记为 C 层**（不冒充兼容）；其中 `POST /api/repositories/{key}/test` 与 replication test 两族在 Artifactory 为 UI 内部面（replication.md §C 旁证）——未来若挂 UI 对齐程可复用。

## 3. 待验证（低置信汇总——矩阵 §16）

V-1 经典路径字面五条（flat copy//tasks/compact/backup/maintenance）；V-2 ?list 参数族 as-built 子集；V-3 batch 方法族；V-4 keypair/token/users 官方 schema；V-5 System Info 字段集；V-6 addons 回显面；V-7 dist-tags 客户端依赖度；V-8 复制族 wire 字段对拍；V-9 统计行 tally 抽查。验证途径均为活体基线修复后 curl 探测或票内核对——不阻塞三带派单。

## 4. 自测

- `python3` 解析 fern/openapi/binflow.json：112 路径/158 ops/20 标签清点（与 AC 输入一致）。
- 矩阵四态统计二次机读复核（首轮脚本误parse → 手工逐表 tally 定稿，差异已在 §16 V-9 留痕）。
- 官方三索引 + 12 个参考页 .md 逐页实取（curl，2026-09-06），OpenAPI 路径块解析核验。
- 文件零 git 操作：`git status` 不含本票产出的意外暂存（由 conductor 收编时核）。

## 5. 移交

- tech-lead：T-504/505/506 可按矩阵 §12 三带直接拆票（实现归属已注；与 FR-156/157 的消费关系防双改）。
- PM：FR-162+（REST 跨程 FR 增补）可以矩阵 §1/§13 为底稿。
- conductor：若 Q10 出口①（容器修复）达成，矩阵中置信行升级对拍可挂在 T-504~506 验收段。
