# L023-2H — 复验残留两条修复（R3 空集合物化 + D7 位 0 尾巴）

Ticket:       L023-2H（微返工：L023-2G §9 记录的两处复验残留）P1
Role:         dev-go-core（area = internal/httpapi/build.go 一处文件）
Area:         internal/httpapi（echo/文案面——零 metadata/build 域改动）
Input:        reports/compatibility/L023-buildinfo-diff.md §9.2-R3 / §9.3-观察一；wire：l023d-wire/malformed-started-variants.log（m1 位 0 臂 a/b 对照）
Changes:      ① **R3 空集合物化**（6 单元 02/15b/16b/19b/22b/26c）：build 无 modules 时 FULL 回显**省略键**（原物化 `[]`）；module 无 dependencies 时**省略键**（Dependencies 加 omitempty）。slim 的 `modules: []` 维持（09 单元 SAME 面）；artifacts 空集**不在 R3 清单**——维持 `[]` 物化（零触碰原则）。② **D7 尾巴**：malformedAt 首位即失败（零前缀解析）时**无 malformed-at 子句**——`Invalid format: "xyz"`（a/m1 逐字；判定=rest 与 input 等长 ⟺ 位 0 断点）。
Files:        修改：internal/httpapi/build.go（invalidFormatMessage 位 0 分支 + renderBuildInfo 空 modules 省键 + buildModuleEcho.Dependencies omitempty）；测试：build_query_semantics_test.go（+TestBuildQueryEmptySetKeysOmitted 三臂 + 位 0 逐字臂）、build_rest_test.go（空 dependencies 断言翻转）。
Tests:        新增 TestBuildQueryEmptySetKeysOmitted（无 modules 省键/无 dependencies 省键/artifacts 保持 []/slim 保持 []）+ 位 0 臂并入 404 测试；存量 round-trip 空段断言对位。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/build/ -count=1（ok 83.9s）；go test ./internal/metadata/ -count=1（行为面绿——**T411PerfItemDomain/PropertyJoin 两条 P95 时序预算测试红：`item-domain P95 566.8ms > 500ms budget`，纯阈值面**；2H 对 metadata **零改动**（git status 证明——2A-2F 已由 conductor 提交，本票工作树仅 httpapi 三文件），本机连续数小时套件负载后同族套件基线全面劣化（早期 45-90s 的 build 族现跑 188-201s），P95/T411/SearchP95 属同族环境抖动）；go test ./internal/httpapi/ -count=1 -timeout=20m -run build 族（**ok 188.7s**——排除仅两条无关 P95 时序测试后全绿；广模式一次跑中 TestBuildSearchP95 红同为时序预算面、路径与 2H 零交集）；go test -race ./internal/httpapi/ -count=1 -run 'TestBuildQueryEmptySet|TestBuildQueryDetail404|TestBuildRESTUploadAndEcho'（ok 95.4s）；冒烟：隔离实例 127.0.0.1:18095（独立 data-dir，零 docker，用后清除）。
Outputs:      冒烟三臂：H1 无 modules build → detail 无 `"modules"` 键；无 dependencies module → 键集仅 `{artifacts,id}` 且 artifacts=`[]`（R3 外保持）；H2 `?started=xyz` → 400 `Invalid format: "xyz"` **无子句**（=a/m1 逐字）；H3 回归护栏 `?started=…​.000 0000` → `Invalid format: "2026-09-15T09:00:00.000 0000" is malformed at " 0000"`（=a/m2 形维持）。
Compatibility: 对照 L023-buildinfo-diff.md §9.2-R3/§9.3：两处按 wire 逐字对齐；R3 后 D07-R05/R06/R07 的 R3 挂账应可随差分复跑翻绿（处置归 conductor/compatibility-engineer）。**登记**：R3 清单只载 dependencies/modules 两键——artifacts 空集未证（A 侧样本未覆盖），维持 [] 物化未动；D7 截断输入（skeleton 中途耗尽）臂仍带空尾段 `is malformed at ""`——无样本，未动。
Security:     无新面（echo 文案与错误消息两处；内插经既有 JSON 转义路径）。
Performance:  无影响（渲染分支两处；时序预算测试红均为环境面，见 Commands）。
Risks:        ① T411×2/SearchP95 环境抖动判定建议 conductor 在静置机器上复跑一次定谳（若静置仍红则另立票——但与 2H 零代码交集）；② artifacts 空集与 D7 截断臂两处未证面（登记）。
Blockers:     无。
Next:         差分复跑（§9 同 case 集+malformed 变体）确认 R3/D7 全 SAME → D07-R05/R06/R07 翻态候选。
