# QA 报告 T-226 — V29 等价口径回归：MinIO + Artifactory OSS 容器复跑 M6 基线

- role: qa-engineer；日期：2026-08-23
- 票据：T-226 [P2] M7 等价口径回归：MinIO + Artifactory OSS 容器复跑 M6 基线（V29）（BOARD 只读）
- 口径依据：PRD milestone-7 §4.4 FR-69-AC3（V29 = M6 H01~H05 汇总断言 + H63/H67 在 M7 代码上仍绿）；ADR-0025 决策 3（MinIO≈AWS S3、Docker OSS≈真实 Artifactory，差异记录制）；M6 断言口径照 T-173（S3 腿）与 T-175/T-196（迁移腿）QA 报告
- 被测物：**commit 85da31c**（worktree 干净构建；主仓工作树当时含 T-219 在途改动，未混入）。linux/amd64：
  - `binflow-server` 23,447,656 B，sha256 `ff75c63d…d1517d`（含 `make console` 真实 SPA；版本注入 `v1.0.0-t226.85da31c`）
  - `bf-migrate` 9,197,617 B，sha256 `42e77e1d…2182c0`
  - scp 后 VM 侧 sha256sum 逐字节一致（binary 新鲜度纪律）
- 执行环境：用户 VM `lzw@172.16.58.129`（Ubuntu 24.04.4 / kernel 7.0.0-30-generic / docker 29.1.3，mirrors 已配）；MinIO RELEASE.2025-09-07T16-13-09Z（`minio/minio:latest` 容器，S3 API :19000 path-style us-east-1）；Artifactory OSS **7.84.10**（+ PostgreSQL 16-alpine，详见 §3 差异 A-1/A-2）。客户端：VM curl/docker 29.1.3；macOS npm 10.9.8 + python3.14 venv(twine 7.0.0)/pip + Playwright Chromium（借 web/node_modules）；mvn 3.9.16 容器腿（`maven:3.9-eclipse-temurin-21`，VM mirror 拉取）

## 总结论：**PASS（等价基线在 M7 代码上全绿；真实 OSS 腿存在环境级差异，已逐条归档，无 M7 改动引入的回归）**

| 腿 | 序列 | 结果 |
|---|---|---|
| MinIO（S3 等价） | H01~H05 汇总断言 | **全 PASS**（§2） |
| Artifactory OSS（迁移等价） | H62~H67（mock 源等价复跑） | **全 PASS**（§4） |
| Artifactory OSS | H62~H67（真实 OSS 源） | **环境受限**（§3：REST 面被 OSS license 门挡住，工具行为如实记录；差异 A-3/A-4） |
| FR-52 最小回执 | 上传/下载/去重 | 一行证据见 §2.1（全量吞吐归 T-227） |

---

## 1. 环境事件记录（如实）

- **VM 关机扩盘中断窗**：~11:43 用户关机扩盘（sda 20G→100G、内存 5.7→7.7Gi），VM 重启，MinIO/BinFlow 实例随关机消亡。恢复后按预案执行扩盘自愈：`sudo growpart /dev/sda 2` + `sudo resize2fs /dev/sda2` → 根分区 99G（空闲 79G）。**MinIO 腿全部证据在重启前收齐**；重启后仅 Artifactory 腿在跑，无证据丢失。sudo 凭据会话内传递，未写入任何文件。
- **会话中断**：QA agent 12:33 因 API 配额被击落，15:01 恢复续跑（conductor 已核验容器栈存活）。中断前后无状态损失（Artifactory 栈 Up 3h）。
- 主仓 HEAD 在 QA 期间由 conductor 前进到 ab5bad9+；本报告全部证据针对 85da31c。

## 2. MinIO 腿 — H01~H05 汇总断言（M6/T-173 口径）

实例：BinFlow `storage.backend=s3` → MinIO :19000 桶 `binflow-qa`，listen 0.0.0.0:18080，data_dir 本地（元数据 SQLite + blob 全在 MinIO）。起服即绿：`s3 storage backend active` 日志、`/healthz`/`/readyz` 200、`/binflow/api/v1/health`（auth）`{"status":"ok","storage":{"status":"ok"}}`、`/api/system/version` 回 `v1.0.0-t226.85da31c`（H07/G 版本注入面）。

### 2.1 H01 = M1 C 序列 P0 汇总 — PASS

建仓 200；10MiB 随机内容 PUT 201 → 下载 `cmp` 逐位一致 + sha256 相同（`29e9dee6…`）；**S3 对象键 `blobs/29/<sha256>`**（S3-01 布局断言）；item info 含 size/createdBy；**C12 去重：同内容第二路径 PUT 201 后 MinIO 对象计数 2→2 不变**、双路径 GET 均 200；checksum 搜索命中双路径；**`/api/v1/storage/stats` 200**（M6 缺陷 D-1 修复面在 M7 仍绿：`{blobs:3, logical_bytes:20971533, physical_bytes:10485773}`——logical>physical 直接可见去重）；checksum 不匹配 409（错误体含 received/actual 双值）、秒传（同摘要）201；DELETE 204 → 复 GET 404。FR-52 最小回执 = 上传 201/下载 sha256 一致/去重对象数不变，如上一行。

### 2.2 H02 = M2 D 序列 P0 汇总 — PASS

`/v2/` 匿名 401 + 认证 200；`docker login localhost:18080` Succeeded；minio 镜像 tag→push（manifest digest `sha256:a1a8bd4a…`，与 T-230 同镜像 push digest 一致——跨票交叉印证）→ rmi → **删缓存重拉** RepoDigest == push digest；`/v2/_catalog` 回 `docker-local/t226/minio`、tags `["h02"]`；**D11 去重：同镜像第二名字 push 后对象计数 13→13 不变**；D06 单体 PUT 201 + HEAD 200；D10 chunked PATCH 202 + GET 状态 204 `Range: 0-1023`；**kill -9 重启后**：healthz 200、历史制品 200、docker manifest 200、**在途会话 GET 404（Q4/V19 S3 hard-404 契约在 M7 代码仍钉死）**。未复跑：D17 podman/D19 skopeo/D08b buildx（客户端不可得，同 T-173 环境限制口径）。

### 2.3 H03 = M3 M 序列 P0 汇总 — PASS

- **mvn**（容器腿）：`mvn deploy` BUILD SUCCESS（pom+jar+metadata 上传，`settings.xml` 凭据）；全新消费者经 **virtual（libs-virtual = libs-release + maven-remote）** 同时解析本地 `t226-lib` 与远端 `junit:4.13.2` → 编译运行输出 `T226_MAVEN_OK`（M13+M45+M50 三合一：本地仓解析 + 真上游 maven-central pull-through + virtual 聚合，一次证据链覆盖）
- **npm**（macOS）：publish `+ t226-demo@1.0.1`、whoami=admin、全新目录 install + `require` → `t226-npm-ok`
- **pip/twine**（macOS）：twine upload 200；simple index `#sha256=` 片段 + `api-version=2`（PEP 629）；venv `pip install six --index-url …/pypi-local/simple` → `SIX_OK 1.17.0`；从 BinFlow 回下载 wheel sha256 `4721f391…` == 源文件 == index 片段（三对齐）
- **remote pull-through + 缓存**：`pypi-remote`（VM 出网 pypi.org 实测可达）install colorama `REMOTE_PROXY_OK 0.4.6`；第二 venv 重装 CACHE_HIT、**服务端日志 pypi.org 上游计数 4→4 不变**（M41 口径锚定）
- 未复跑：M16 SNAPSHOT 组、M44 负缓存参数化、npm-remote（时间盒；协议面由 adapter 测试 + M6 记录覆盖，非 S3 等价性差异面）

### 2.4 H04 = M4 W 序列 P0 汇总 — PASS

`/binflow/` 301→ui、`/binflow/ui/` 200、指纹资产 200（真实 SPA 构建进二进制）；session POST 200 → whoami `{"username":"admin","admin":true,"adminRole":"admin","source":"local"}`（**M7 RBAC wire 字段与 M1 admin 布尔兼容视图并存——无回归**）、错口令 401；ci-bot 建号 201 → 无授权读 403 → `POST /api/v1/permissions`（repos/includePatterns/principals）201 → 读 200、写仍 403（写越权面）；audit list 200（events 含 actor/action/detail）；**配额**：quotaBytes=800 仓首传 600B 201、二传 500B **413**、被拒路径 404（原子性）、首传可读；**GC**：dry-run（默认宽限）candidates=0 → `graceHours:0` apply `candidateCount=5 deletedCount=5`（孤儿 S3 对象实际消失，存留对象数 40→35）；**export/import（M6 D-2 修复面）**：S3 实例 `export` exit 0（35 blob/73,365,405 B/831ms/84.1MiB/s）→ 全新 data dir + 全新桶 `import --verify full` rehashed=35 → 第三实例（18084）起服：历史制品 200 且 sha256 仍 `29e9dee6…`、npm packument 200、maven jar 200、**已删路径保持 404（不复活）**、stats 恢复去重剖面（logical 146MB / physical 73MB）。未复跑：Playwright 浏览器面（REST+SPA 资产面已证）、W08 TTL/优雅重启 session（M7 面已由 T-221-qa 覆盖）。

### 2.5 H05 = M5 G 序列 P0 汇总 — PASS

版本注入 ✓（§2 开头）；docs：`/binflow/docs/` 匿名 200（**注意挂载前缀是 /binflow/docs/，根 /docs 404**——差异 d-7）；冷启动（SIGTERM 重启，热数据热桶）**980ms** < 2s 门；**1000 并发拉取：1000×200 / 0 失败**（wall 277s——2 vCPU VM 环境值，M6 断言面是零错误；T-173 同剧本 macOS 13s 属机器差）；主实例访问日志 **零 5xx、零 ERROR 行**；二进制 23.4MB < 40MB 预算（check-size 面；S3 依赖经 minio-go 入二进制为 M6 既有事实）。未复跑：G01~G18 发布产物/安全审计面（release-engineer 域，同 T-173 理由）、1GB 大文件 RSS（归 T-227 吞吐腿）。

## 3. Artifactory OSS 腿 — 环境发现（T-228 必读）

| # | 发现 | 定性 |
|---|---|---|
| A-1 | **最新版 artifactory-oss:latest（7.189.4）已移除内嵌 Derby**：起服即 `Cannot start the application with a database other than PostgreSQL`；且覆盖 `JAVA_OPTIONS` 会丢镜像默认 `-Djf.product.home` → tomcat 报 `WEB-INF/lib/bc/normal does not exist`（用 `EXTRA_JAVA_OPTIONS` 才是正解）。**当前真实部署形态 = OSS 镜像 + 外置 PostgreSQL** | 环境差异（非 BinFlow 侧）；T-228 需按此搭栈 |
| A-2 | 7.84.10 同样拒绝 Derby；PG 配置注入成功形态：**预建 1030 属主完整目录树 + `etc/system.yaml` 用 `url:` 形态**（`host/port/name` 形态不被解析，服务落回 Derby 模板占位符 `{db.home}`）+ 同型 `etc/{artifactory,access}/db.properties`。引导 ~95s ping 200 | 环境差异；compose 化建议归 T-228 |
| A-3 | **OSS license 门（真实 7.84.10 实测）**：`PUT/GET /api/repositories/{key}`（repo 配置）、`PUT/GET /api/security/users*` 全族、`GET /api/storage/{repo}?list&deep=1` 均 400 "available only in Artifactory Pro"。**免license 面**：repo 列表、FolderInfo children、制品 deploy/读、token list/create | **环境/产品边界差异**：`bf-migrate` 对真实 OSS 只能做「repo 清点（无配置）+ assert 拒绝/失败明细」；users 阶段 ListUsers 400 → 整场迁移 abort（exit 1，repo 已写入后）；artifacts 的 `?list` 400 → 若能到制品阶段会走 FolderInfo 兜底（T-196 双路径设计在此正好对上） |
| A-4 | 对真实 OSS 实跑 `bf-migrate`：dry-run exit 1 + 报告落盘（`user phase aborted … HTTP 400`）；全量 run：repos found=1 **failed=1**（repo 配置读取 400，记 item failure 不中断）→ users abort exit 1，目标零仓写入 | 工具行为如实；**建议（P2，conductor 决策）**：users 阶段对 400/401/403 降级为 warning（与 token 阶段同姿态）或加 `--skip-users`，否则 OSS 源整场不可迁 |
| A-5 | OSS token：`POST /api/security/token` 的 scope 只收 **`applied-permissions/user`（不带 `:name` 后缀）**；`applied-permissions/user:admin`、`*`、`users/*` 均 400。apiKey 端点返回设置 JSON 不发 key | 环境差异；BF reader 只需 Bearer，无碍 |
| A-6 | OSS 建仓/建用户走 **UI**（Playwright 实操成功：login→users/new 表单→alice/bob 建成）；REST 全被 license 门挡 | 环境差异；T-228 造数脚本要走 UI API 或预置 |

## 4. Artifactory OSS 腿 — H62~H67 等价复跑（mock 源，M6 口径，同一 M7 二进制）

源：VM 上 python3 mock（形状按 docs/reverse/rest-api.md §2/§3 + auth-model.md），**制品本体 = 真实 120 文件语料（4KB~256KB，manifest 记录逐文件 sha256/sha1/size）**；目标：BinFlow disk 实例 18090（fresh）。全部用例跑在同一 `bf-migrate` @85da31c。

| # | 验收标准 | 结果 | 证据摘要 |
|---|---|---|---|
| H62 | 非空目标 → 拒绝 exit≠0；`--allow-non-empty` 覆盖并记合并语义 | ✅ | 目标已含 libs-generic（本迁移先前写入）+ **全新 progress 文件** → `exit=1`，`target instance is not empty: 1 repositories (libs-generic) … pass --allow-non-empty to merge anyway`，guard-report `refused:true`、零写入零进度；`--allow-non-empty` → exit 0，guard `allowed_via_flag:true` + merge 语义全文注记，120/120 幂等重迁 |
| H63 | generic 仓 100+ 制品迁移，sha256 抽样 10 条一致 | ✅ | `artifacts: found=120 migrated=120 failed=0 bytes=4325376`；随机抽样 10/10 sha256 与源 manifest 逐条一致；（超票面：**清单 sha1/内容不符时逐条拒绝**——mock 曾误填全零 sha1，120/120 被工具的下载前双哈希闸拒掉，修复后全绿——工具内部一致性闸在 M7 代码仍有效） |
| H64 | migration_report.json 产出、成功率 100% 或失败明细 | ✅ | 四份报告（dry/拒绝/正式+resume/合并）全部在场、JSON 可解析、逐阶段 found/migrated/skipped/already_done/failed + failed_items 明细 + guard 块 |
| H65 | 迁移用户可登录 | ✅ | `--passwords-out`（0600）生成 alice/bob 口令；alice `POST /api/v1/session` → 200 `{"adminRole":"user","groups":["readers"]}`；错口令 401（PRD 备选臂：admin 持口令，与 M6 同判） |
| H66 | Token 清点即止 + 政策注记 | ✅ | `tokens: found=2 migrated=0 skipped=2` + `token values cannot be exported from Artifactory … recreate them on the target`（真实 OSS 侧 token list 同样可达且有真实元数据 2 条——A-5 佐证）；观察 d-9：**--resume 轮的报告 tokens 记 `found=0`+already-recorded 注记**（首跑计数在早前报告里），建议 resume 轮透传累计计数 |
| H67 | dry-run 零迁移 + 统计与源一致 | ✅ | exit 0；目标仓库列表 `[]`；无 progress 文件（零记录）；repos found=1 / users found=2 / tokens found=2 skipped=2 / artifacts found=120 全与源一致；dry 报告 `dry_run:true`；dry 软守卫警告（目标探测不拒绝）与 T-196 契约一致 |

附带：**resume × 守卫豁免闭环**（首跑 2 用户失败〔目标缺 readers 组——文档化前置条件〕+ 120 sha1 拒绝 → 预建组 + 修源 → `--resume`：guard `resumed_run:true` 不误拒、repos already-done=1、users 补迁 2、artifacts 120 全量，exit 0）。

## 5. 与 M6 本地基线的差异清单（逐条定性）

| # | 序列/现象 | 差异 | 定性 |
|---|---|---|---|
| d-1 | H04/W28~W32 | S3 下 export 产出**自包含包**（35 blob/73MB 随行），非 PRD H04 字面「只导元数据」 | **已知裁定**（T-201 AC③ 显式替代 PRD 读法，import 不依赖原桶）——非回归 |
| d-2 | H03/M01 | virtual 仓 wire 字段是 `repositories`（票面/直觉易写 `members`，400 报错清晰） | 实现即规格，无动作 |
| d-3 | G19 | docs 挂载在 `/binflow/docs/`，根路径 `/docs` 404 | 实现即规格（router.go /binflow 前缀内）；QA 剧本勘误级 |
| d-4 | H19/G27 | 1000 并发 wall 277s（T-173 macOS 13s） | **环境差异**（2 vCPU VM vs 工作站）；断言面零错误已过；数值对照归 T-227 |
| d-5 | H66 | resume 轮报告 tokens found=0 + "already recorded" 注记（首跑计数留在早前报告） | 工具可用性观察（P3）：建议 resume 透传累计计数 |
| d-6 | 真实 OSS H62~H67 | 全序列被 OSS license 门阻断（A-3/A-4）；users 阶段 abort 姿态与 token 阶段 warning 姿态不一致 | **环境/产品边界**（非 M7 回归）；建议票：users/repo-config 阶段降级为 warning 或 `--skip-users`；T-228 用 Pro trial 或 UI 造数 |
| d-7 | 环境引导 | latest=7.189.4 无 Derby、需外置 PG；7.84.10 PG 注入需 `url:` 形态 + 预建 1030 目录树 | 环境差异（A-1/A-2），T-228 实操手册素材 |
| d-8 | 中断 | VM 扩盘重启（§1）+ agent 配额击落 12:33→15:01 | 环境事件，证据无损失 |

**M7 改动引入的回归：零。** S3 等价五序列与迁移全序列在 M7 代码上与 M6 收官基线行为一致；M7 的新面（adminRole 兼容视图、S3 续传 hard-404、T-201/T-202/T-203 修复面）均按 §5.6 反转表的期望工作。

## 6. 自动化基线（worktree @85da31c）

- `go test -count=1 ./...`：**24/25 包 ok**；唯一 FAIL = `internal/auth TestSessionTTLAbsoluteCapWins`（负载敏感 flake：当时宿机并发跑 VM 腿 + 1000 拉取风暴）；定向 `-count=3 -run` 复跑 **ok**、整包 `./internal/auth/` 复跑 **ok (15.9s)**——非代码缺陷，建议 conductor 知悉（与 T-172 已知 flaky 家族同类）
- 存储三包：storage 127s ok / httpapi 211s ok / migrate 19.5s ok / cmd/bf-migrate 2.2s ok

## 7. 缺陷与建议（不修码，回 conductor）

| # | 级别 | 摘要 |
|---|---|---|
| B-1 | P2（建议票） | `bf-migrate` users 阶段对 ListUsers 400/401/403 硬 abort（repo-config 阶段已记 item-failure 不中断）——真实 OSS 源整场不可迁；建议降级 warning 或 `--skip-users`（A-4/d-6） |
| B-2 | P3 | resume 轮迁移报告 tokens 计数不透传首跑值（d-5） |
| B-3 | 勘误 | PRD H04「S3 下 export 只导元数据」与 T-201 裁定的自包含包口径需 PM 回写（d-1） |

## 8. 清理与 VM 收尾快照（2026-08-23 15:45）

- **已清理**：BinFlow 实例全部停止（18080/18084/18090，进程零残留）；t226-minio 容器 rm、minio 数据目录 sudo rm；mock Artifactory 进程停止；maven 容器镜像 rmi；minio/mc 镜像 rmi；推到测试 registry 的两镜像 rmi；VM 侧全部 scratch（~/t226 下 bin/脚本/配置/日志/manifest/凭据文件、/tmp 测试件）；macOS 侧 /tmp/t226-cli（含 npm/pip 测试凭据）/tmp/t226-dist 与构建 worktree 移除；`docker logout localhost:18080`。Artifactory 侧 live token 文件已删（可重签）。
- **保留（按 conductor 指示，磁盘已扩 100G）**：`t226-artifactory`（7.84.10）+ `t226-pg`（postgres:16-alpine）容器在跑（8181/8081、8182/8082，网络 t226jfrog）；数据卷 `~/t226/art-tree`（1030 属主）；镜像 postgres/minio/artifactory-oss；Artifactory 内语料（example-repo-local 120 制品 + alice/bob 用户 + 2 token 元数据）留给 **T-228**。测试口令均为会话内一次性值，未写入任何仓库文件。
- 快照：Ubuntu 24.04.4 / 根分区 99G 用 16G 空闲 79G / 内存 7.7Gi available 5.2Gi / 8080 空闲 / docker 29.1.3 mirrors 正常。

## 9. 未验证项及理由

| 项 | 理由 |
|---|---|
| AWS S3 真云（V27） | T-227 条件腿（本票口径外） |
| 1GB 上传 RSS / 全量吞吐（FR-52 深度） | 归 T-227 |
| D17/D19/D08b、M16 SNAPSHOT 组、M44 参数化、Playwright 浏览器面、W08 TTL、G01~G18 | 客户端不可得 / 时间盒内汇总口径未点名 / 已由 M7 专项 QA（T-221-qa）与 M6 记录覆盖——均非 S3/迁移等价性差异面 |
| 真实 OSS 上的 H62~H67 制品/用户实迁 | OSS license 门结构性阻断（§3 A-3/A-4，差异已归档；等价性由 mock 源腿闭环） |
