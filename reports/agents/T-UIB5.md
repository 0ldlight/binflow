# T-UIB5 — UI Phase 1 批 5：旧 CSS 退役（等价替换）

Ticket:       T-UIB5 · UI Phase 1 批 5：旧 CSS 退役 · P0（design-system-plan §6 批 5 行 / §1 G3 / §7）
Role:         dev-frontend（area = web/src 全域类消费面 + web/e2e 类断言腿）
Area:         web/src/**（utility 类消费点 + 6 个旧 CSS 文件 + 桥接层）/ web/e2e/**（类断言迁移）
Input:        docs/design/design-system-plan.md §6 批 5、§1 G3、§3.2 行高注记、§7 引用门；docs/design/console-ux.md §7（token 消费纪律，经 base.css 头注引用）；批 0-4 产出（tint 槽位消费缝、ui/badge.tsx 软底实现、golden dev.a22a4aa4）
Changes:      按类族分节：
              ① badge 族（28 个 class 消费点 → 0）：bits.tsx Badge 删除、消费面（18 文件）升格 `@/components/ui/badge`；新增 tint-* 七变体（neutral/info/success/warning/danger/pro/enterprise）逐属性复刻旧 `.badge:not()` 配方（px-[7px]/py-0.5/12px/lh1.4/font-normal/border-0 + color-mix 15%/18% 底与 78% 文字收敛——配方经桥接层 `--color-badge-*{,-soft}` 9 槽承载，tier 档复用 info/warning）；14 文件的直接 span 消费者换 Badge 或混合串工具类；tierBadgeClass/statusBadge/TASK_BADGE/recordStatus 四 helper 改返 tint 变体名；LicenseAddons 混合串按旧配方重写（(0,2,0) 曾胜工具类的实证修正）；admin outlined 徽章 border 经新桥接槽 `--color-badge-warning-outline`（62% 描边）承载，security.css 旧规则退役；ui/Badge 增 mono 属性（仅 font-mono——旧 `.badge:not()` 0,2,0 曾压过 `.mono` 字号，徽章内 mono 恒 12px）与 data-variant 语义锚
              ② mono 族（21 页面 token → 0）：`mono` → `font-mono text-[0.95em]`；与 font-mono/字号工具类同串时按 HEAD 实际级联裁决（text-dense 胜则只留 font-mono；cert 指纹 `mono font-mono` 无竞争则保 0.95em）；`.field input.mono`、`.storage-refresh-row .mono` 及 6 处页面组 CSS 死 descendant 规则删除（license/tree/search/authconfig 的 `.mono` 子选择器在 HEAD 已无命中面，实证后清）
              ③ text 族：text-2 87 token → `text-muted-foreground`（SearchPageV2 6 处同串已有等值色类，直删）；text-muted 精确 token 实测 0（既有代码全是 text-muted-foreground——票面清单里该族空集）
              ④ filter-bar 族（7 文件）：`flex flex-wrap items-center gap-3 py-3`（含 gap-2 旧并存串保持原值）；.count → `ml-auto …-aux muted-fg tabular-nums whitespace-nowrap`；filter-tail-actions/end 与 col-check 按各消费点 HEAD 实际胜出值（gap-1.5 或 gap-2）逐一迁移
              ⑤ CSS 文件：styles/governance.css（156 行）、components/dialogs.css、components/pager.css 三文件删除（pager.css 实证从未被 import——死文件；governance/dialogs 类族全部迁 TSX 工具类，`td .field-error` 唯一仍多消费的规则迁 base.css）；pkg-icon.css 删 .pkg-svg 布局规则（迁 PkgIcon.tsx `[&_svg]` 工具类，类名保留作暗底提亮选择器与 spec 锚）、仅存暗底提亮表；base.css 删 utility/badge 族 9 规则 + 头注更新；pages.css 删过滤栏族 6 规则
              ⑥ 字号别名删除：--bf-fs-aux/body/form/h3/h2/h1 六别名从 typography.css 删除；base/pages/桥接层/6 个页面组 CSS 的 var 引用全量换新阶名（fs-xs/sm/base/md/xl/2xl，值零变）；DeployDialog/SetMeUpDialog 两处内联 style 同步
              ⑦ e2e 类断言迁移（12 spec 文件 22 处）：.badge 族 → `[data-slot="badge"]`（tone 场景加 `[data-variant="tint-*"]`）/ toHaveAttribute('data-variant')；span.mono/td.mono/h3 .mono → .font-mono；.filter-bar .check-row → .check-row；另修复 t492 `td.mono` 在 HEAD 已失锚的存量腐烂（实证 HEAD DOM 为 td.font-mono）
              ⑧ 等价性修正三轮（探针驱动）：tint 变体 leading-[var] 被 twMerge 吞 → `[line-height:var(--bf-lh-xs)]`；text-xs/text-aux 在旧规则只设字号的点位注入了行高 → 全换 `text-[length:var(--bf-fs-*)]`（detail-pop summary/pre、gc-error pre、smu error/raw/token code、deploy-echo/raw、quota pct、gc grace summary）；smu 两按钮 all:unset 平价补 `[line-height:inherit]`；audit time-field 旧 (0,2,0) gap 4px 胜出修正为 gap-1；桥接层另补 `--color-border-strong` 槽（deploy-drop 虚线边等值承载）
Files:        新增：reports/agents/T-UIB5.md（本件）。
              删除：web/src/styles/governance.css、web/src/components/dialogs.css、web/src/components/pager.css。
              修改（74 文件，要点）：web/src/components/ui/badge.tsx（tint 变体+mono+data-variant）、components/layout/bits.tsx（Badge 退役）、components/PkgIcon.tsx、components/SetMeUpDialog.tsx、components/DeployDialog.tsx、lib/addons.ts、design-system/tailwind.css（11 槽）、design-system/tokens/typography.css（别名删除）、main.tsx（import 摘除）、styles/base.css（-129 行面）、styles/pages.css（-79 行面）、components/pkg-icon.css、pages/{security,repositories,admin,search} 组 CSS 8 件、页面 TSX 约 50 件、e2e 12 spec。
Tests:        e2e 类断言迁移 22 处（.badge→data-slot/data-variant、.mono→.font-mono、.filter-bar→.check-row）；四态覆盖不在本票面（纯等价替换，无态变更）；改动面 6 页截图差分双主题抽查（repos/license 暗色）；返工追加 repo-new-local 表单页（磁贴 tier 徽章三属性 computed 对照 + axe 双主题 + 0px 全页差分，见 Rework 节）。
Commands:     cd web && npm run build && npm run typecheck && npm run lint && npx playwright test <受影响 spec 集>（BASE=隔离实例）；go build -o /tmp/binflow-uib5/… ./cmd/binflow-server && …serve -c /tmp/binflow-uib5/binflow.yaml（:8091 独立 data-dir，禁 docker 未用）；git worktree /tmp/uib5-head @HEAD 构建 :8092 对照实例（data 为 8091 快照拷贝）；成对全页截图 + stdlib PNG 逐像素差分（无新依赖）
Outputs:      四门：build 绿（assert-tokens OK css 11 + tsx 104 零字面量/零色彩 token 直引；assert-i18n OK 3119 调用点/2346 键同构；vite ✓ built in 7.3s）；typecheck 绿；lint 0 error（45 条 react-hooks warning 为存量）；截图差分（同数据快照 HEAD vs 工作树，6 页 + 2 暗色抽查）：repos 0 / license 0 / gc 0 / audit 3px(Δ≤2) / users 237px(0.018%) / explorer 46px(0.0035%) —— 合计 286/7,914,240 = 0.0036%，残差逐点定位均为实例数据（users=两实例 admin lastLoggedIn 时间戳、explorer=文件 URL 内嵌 origin :8091/:8092、audit=滤波窗边界 AA），样式差分=0；e2e（BASE=隔离实例）：受影响 12 spec 全绿——permissions/users-groups/governance-monitoring/rbac/t390/replication/license-addons/auth-config/keyboard/setmeup-deploy/governance/repositories/storage_migration/styleguide/t416/t424/fr82 合计 104 passed；3 例失败均为 HEAD 对照实例同形失败（t434:84、t434:230、t492:146——存量腐烂，与本批无关）；t424 在长跑实例的数据饱和假象（184 仓超页窗）经 fresh-data 实例复验 5/5 绿
Compatibility: 锚册对照：无涉（本批不动 IA/导航/交互形态；弹窗/抽屉结构零变化）。契约漂移：无（纯样式层，无 API 面）。
Rework:     返工一轮（conductor 双端对拍发现，已闭合）：建仓表单磁贴 tier 徽章三属性不等值——根因是 tierBadgeClass 返回值改 tint-* 变体名时，RepositoryFormPage:461 把它当**裸 class 字符串**拼接（tint 配方只存在于 ui/Badge 的 cva 变体，不是 CSS 类）→ 背景丢失/文字色回退 raw #0b6bcb（T-441 对比度债复发，4.48:1 差 0.02 不过 AA）/字号 11px。消费面全排查：tierBadgeClass 共 3 消费点（LicenseAddons×2 走 variant= 正确；表单磁贴 1 处错拼）——唯一 casualty。修法（小 diff）：磁贴位换 `<Badge variant={tierBadgeClass(...)} className="border border-info font-semibold">`（enterprise 对称走 border-warning）；探针复校又捕到 font-weight 残差（磁贴标签位经 repositories.css .pkg-name=600 继承，旧配方不设字重 → font-medium(500) 不够，改 font-semibold(600) 后全等）。**不等值清单（已闭合）——pkg-tier-* 磁贴徽章 computed 对照（旧=1d553c90^ @:8092，新=修复树 @:8091，同数据快照）**：bg `color(srgb 0.0431373 0.419608 0.796078 / 0.15)` = 新同值逐字相等；color `color(srgb 0.0586667 0.35749 0.658902)`（info 78%+正文收敛 ≈#0f5ba8 系）= 新同值；font-size 12px = 新同值；另附 fw 600 / padding 2px 7px / border 1px rgb(11,107,203) 三属性亦全等（16 枚徽章全 IDENTICAL；radio 行 8 枚为 surface-1 上的裸工具类形态，两版本本就一致未动；enterprise 档在磁贴面无 DOM 实例——enterprise addon 仅 ha/xray-integration 非包类型，其分支经 LicenseAddons 页 0px 差分覆盖）。验收腿：axe（WCAG2A/2AA/2.1A/2.1AA/best-practice，include pkg-grid）旧亮/新亮/新暗三扫 serious+critical = 0/0/0；repo-new-local 全页像素差分 0/1,319,040（0.0000%）；m10 license-addons + repositories e2e 复跑 9 passed；四门复跑全绿
Security:     无新增用户内容渲染面（类名替换不改 dangerouslySetInnerHTML 用法）；无敏感信息进前端日志；XSS 面零变化
Performance:  无新依赖、无运行时逻辑变更；产物体积微降（删 3 CSS 文件 + 别名层）；大数据面（表格/树分页）行为零变化
Risks:        ① tint（旧配方）与批 4 软底（*-surface）两族徽章并存至批 6 收敛裁决——本批按等价铁律保旧配方，Styleguide 仍展示软底（未动，golden 归 conductor）；② twMerge 把自定义字号类（text-aux/dense 等）按色彩组消解——cn() 同串组合会吞字号（存量 AlertBox 即有此潜在地雷），本批 tint 变体以任意值 token 自持规避，建议后续统一修 cn 分类配置；③ 3 例存量 spec 腐烂（t434×2、t492:146 en 表头断言）未修——非本批引入，建议单独提票；④ e2e 长跑实例数据饱和（repos 184 仓）会让按 key 排序的行断言翻页失败——复跑建议 fresh data-dir
Blockers:     无
Next:         1) 徽章两族收敛裁决（批 6 或 conductor 专项：tint-* vs *-surface 软底统一，涉及 golden 翻新）；2) cn()/tailwind-merge 字号类分类修复票（extendTailwindMerge font-size 组登记 text-aux/dense/form/h3/h2——修后可清理本批的自持任意值并顺带修 AlertBox 存量地雷）；3) t434/t492 存量 spec 腐烂修复票；4) base.css/pages.css 存量类族（card/table/field/kv/page-header/field-hint…）的后续批次退役建议——本批已验证「HEAD 实际级联探针 + 成对截图差分」方法可复制
