Ticket:       L025-3A — LOOP 025 票甲：D02 配置族读面快赢 + P1（按派发单）
Role:         dev-go-core（area = internal/httpapi）
Area:         internal/httpapi（路由 + 新处理器 + 测试；未触其他包）
Input:        docs/reverse/rest-api.md §2.1（L025-1 新段落——冻结输入）：§2.1.0 端点表行 1-4/8/9、§2.1.1 configurations、§2.1.2 existence、§2.1.3 v2 配置读、§2.1.4 批读、§2.1.8 布局面；reports/agents/L025-1.md（探针结论与勘误）；公开规范补充源：jfrog.com/help 「Repository Layouts」页（2026-09-17 实取 .md 版——布局 pattern/regexp 字串的唯一合法来源）
Changes:      ① 新增 internal/httpapi/repo_config_read.go：handleRepoConfigurations（admin 分组清单——LOCAL/REMOTE/VIRTUAL/FEDERATED/RELEASE_BUNDLE 大写键按序、组内 key 升序、comma-OR 双轴过滤可叠加、无命中 200 空对象、vendor CT `…RepositoryConfigurationsList+json`、no-store、403 信封逐字 Forbidden）、handleRepoExistence（type 重复参数 OR、逗号/未知值 400 `Invalid repository type: <v>` 逐字、project 静默忽略、projectKey 回显、admin 对任意 projectKey 放行 exists:false、非 admin 403 Forbidden）、handleRepoGetV2（type 替 rclass、按 rclass vendor CT、404 信封 `The repository <key> was not found` 逐字、Content-Type（非 Accept）协商——不匹配 vendor CT 406 `Not Acceptable`、非 admin 五键部分视图 {key,type,packageType,description,url}）、handleRepoBatchGet（map 键→v1 单仓 schema（复用 repoConfigOf——自洽不变式）、缺名静默省略、逗号串=单 key、无 names 400 `Repository keys are missing.` 逐字、>100 400 `Repository item limit exceeded: {N}. Limit: {100}`）；② 新增 internal/httpapi/repo_layouts.go：内置布局表 + GET /api/admin/repolayouts 列表（{name,artifactPathPattern,layoutActions}）与单读（全字段 + repositoryAssociations 三空数组）+ 缺名 500 信封 `No value present` 逐字；③ router.go 三处挂载：repositories/configurations 与 repositories/existence（字面路径先于 {key} 前缀族）、v2/repositories/batch GET 与 v2/repositories/{key} GET（literal batch 先于 keyPairs 前缀族）、/api/repo_layouts 旧挂载双 404（集合 `Not Found`/条目 `Not found`）与 /api/admin/repolayouts 只读两动词；④ admin 门在 handler 内（CapRepoWrite 探针）以渲染规格逐字 403 信封（路由门会渲染 BinFlow 自有文案）
Files:        新增 internal/httpapi/repo_config_read.go、internal/httpapi/repo_layouts.go、internal/httpapi/repo_configurations_face_test.go、internal/httpapi/repo_config_v2_read_test.go、internal/httpapi/repo_layouts_read_test.go；修改 internal/httpapi/router.go（三处 case 插入，无既有 case 改动）。删除：无。**回填小单一（同工作树合报）**：repo_layouts.go 布局表 1/25→25/25——数据源=conductor 抓取 reports/compatibility/l025a-wire/repolayouts-reference.json（172.16.58.130:8082 参照，来源合法），25 行逐行转录（pattern 字串逐字含 terraform-provider 行首尾 \n+缩进字节、行序保持）；layoutActions 由占位空数组改为捕获的对象形态 {copy,edit,delete}（copy 全 25 行 true，edit/delete 仅 composer-default/build-default true）；maven-2-default artifactPathPattern 改参照形态（(-[classifier]) 前无空格——官方文档页的空格定案为渲染伪迹）。**回填小单二（单读面）**：单读六/七键形态对齐 conductor 单读 wire（repolayouts-single-read.json）——distinctiveDescriptorPathPattern 定型为**布尔**（true 仅 descriptor 布局五家：maven-1/maven-2/ivy/sbt/sbt-ivy）；descriptorPathPattern 仅该五家出现（逐字转录五条）；folder/fileIntegrationRevisionRegExp 25 行恒在逐字转录（多数 .*，特例 maven-2 SNAPSHOT / `SNAPSHOT|(?:(?:[0-9]{8}.[0-9]{6})-(?:[0-9]+))`、ivy/sbt/sbt-ivy `\d{14}`、maven-1 `.+`、conan `[^/]+`、vcs folder `.*`+file `[a-zA-Z0-9]{40}`）；maven-2 官方推导 file-regexp 修正为实测 `[0-9]` 形态（原 `\d` 形态废弃）；repositoryAssociations 改**用量派生**（rows 的 config blob repoLayoutRef==布局名 → 按 rclass 分桶、key 升序——对齐捕获中 simple-default 携带 example-repo-local 的机制）；捕获文件的 14 个额外探名（gradle-default/helm-default/pypi-default 等，pro 非实例布局）不进表——BinFlow 对其答与参照相同的 500 `No value present`
Tests:        新增 3 文件 9 测试函数（全部 table/子测试驱动、行为命名、票号在文件头注释）：repo_configurations_face_test.go——TestRepoConfigurationsGroupedInventory（分组/排序/字段集/comma-OR 双轴/叠加/空对象/CT/no-store/rclass 小写/无 url 无 type）、TestRepoConfigurationsForbidden（非 admin 403 信封结构逐字、匿名 401）、TestRepoExistenceProbe（无参全五类回显、单类型、重复 OR、逗号与未知 400 逐字、project 忽略、ghost projectKey 回显、非 admin 403）；repo_config_v2_read_test.go——TestRepoConfigV2ReadDialects（三 rclass schema/type 替 rclass/CT/no-store/remote url+knobs/virtual repositories）、TestRepoConfigV2NotFound（404 信封逐字）、TestRepoConfigV2ContentTypeNegotiation（vendor CT 不匹配 406、匹配 CT 200、Accept 不参与 200）、TestRepoConfigV2NonAdminPartial（五键精确集、class 字段剥离）、TestRepoBatchRead（值与 v1 单仓逐键相等、ghost 省略、逗号单 key 落空 {}、无/空 names 400 逐字、101 项 limit 400 逐字、非 admin 200）；repo_layouts_read_test.go——TestRepoLayoutsRetiredMount（双 404 文案）、TestRepoLayoutsList（回填一后扩腿：计数=25、7 个代表 pattern 逐字（含 conan 尖括号正则与 terraform-provider 行首 \n）、layoutActions 对象四臂（maven/go=copy-only、composer/build=全 true）、非 admin 403）、TestRepoLayoutGetDetail（**回填二后扩腿：四代表行（maven-2/ivy/vcs/npm）与捕获 wire 逐键全等 compact 对比——含布尔 ddp、五家 descriptor、vcs 双 regexp 不对称、npm 六键精确集且无 layoutActions 键；associations 用量派生臂（建 example-repo-local@simple-default → localRepositories=[example-repo-local]，npm 保持空）；缺名/gradle-default/pypi-default 三臂 500 `No value present` 逐字**）
Commands:     `go build ./...`；`go vet ./...`；`gofmt -l internal/httpapi/`（两轮：首轮两文件未格式化→`gofmt -w` 后空）；`~/go/bin/golangci-lint run ./internal/httpapi/...`（首轮 revive var-naming 全大写字段→改 CamelCase+JSON tag 保线上键→0 issues）；`go test ./internal/httpapi/... -count=1`；`go test ./internal/repo/... ./internal/metadata/... ./internal/auth/... -count=1`；`go test -race ./internal/httpapi/ -run 'TestRepoConfigurations|TestRepoExistence|TestRepoConfigV2|TestRepoBatchRead|TestRepoLayouts' -count=1`；隔离冒烟：`go build -o /tmp/binflow-l025a ./cmd/binflow-server` + `BINFLOW_STORAGE__DATA_DIR=/tmp/l0253a-data BINFLOW_ADMIN_PASSWORD=… BINFLOW_SERVER__LISTEN=127.0.0.1:18099 serve` + curl 16 发（本机独立 data-dir，无 docker、未触 172.16.58.130——本票自跑冒烟全部面向本机实例）。**回填一后复跑**：四门同绿；触碰面 race ok 14.474s；全包 `ok 259.567s`；冒烟二轮列表 25 行 python diff=IDENTICAL。**回填二后复跑**：`go build ./... && go vet ./... && gofmt -l internal/httpapi/`（空）；`~/go/bin/golangci-lint run ./internal/httpapi/...` → 0 issues；`go test ./internal/httpapi/ -run '…全部触碰面…' -count=1 -race` → ok 14.774s；`go test ./internal/httpapi/ -count=1` → ok 343.116s；隔离冒烟三轮（新二进制独立 data-dir+seed example-repo-local@simple-default）python 全量 diff 单读对照 repolayouts-single-read.json
Outputs:      四门：build/vet 通过、gofmt 空、golangci 0 issues、httpapi 全包 `ok … 179.055s`（回填一 `ok 259.567s`、回填二 `ok 343.116s`）、repo/metadata/auth 全 `ok`（74.9s/27.1s/36.7s）、race `ok … 29.515s`（回填二 `ok 14.774s`）。冒烟关键行：configurations→`HTTP/1.1 200 OK` + `Content-Type: …RepositoryConfigurationsList+json` + `Cache-Control: no-store`，分组体 LOCAL/REMOTE 各 1、blackedOut:true/environments:[DEV] 回显、匿名 401、非 admin 403 `{"errors":[{"status":403,"message":"Forbidden"}]}`；existence→`{"exists":true,"matchingRepoTypes":["LOCAL"],"projectKey":"default"}`、逗号 400 `Invalid repository type: local,remote`、`projectKey=ghost&project=ignored`→exists:false+全五类+回显 ghost；v2 单读→CT `…RemoteRepositoryConfiguration+json`、`Cache-Control: no-store`、hardFail:true/url 上游、404 `The repository nope was not found`、CT 不匹配 406、Accept 不匹配 200、非 admin 恰五键 `{description,key,packageType,type,url}`；批读→`smk-lib` 值=v1 单仓体、ghost 省略、无 names 400 `Repository keys are missing.`；布局→旧挂载 404 `Not Found`/`Not found` 双形态；**回填一：列表 25 行 5773 字节 python diff=IDENTICAL**；**回填二：单读全量 diff（捕获 39 名=25 成功行+14 个 500 探名）→ `ok-rows=25 err-rows=14 diffs=0` VERDICT: ALL-CAPTURE-IDENTICAL（成功行六/七键逐键全等、simple-default 的 localRepositories=[example-repo-local] 用量派生对上、14 个非实例探名双双侧 500 `No value present` 信封全等）**。插曲：回填一冒烟首轮 curl 命中前一轮残留旧进程（bind: address already in use，新实例未起）→ lsof 杀监听后复跑得正确对照；回填二冒烟 diff 脚本首轮把捕获文件的 500 探名行当成功行取——改为 HTTPError 分流比对后全等
Compatibility: 契约面对照 docs/compatibility/contracts/：无 D02 读面专属契约文件（本面以 §2.1 规格为据）。实现=规格高置信面全对齐（挂载/状态码/信封文案/CT/no-store/comma 语义/静默省略族）；BinFlow 数据模型子集面（§2.1.1 17 键、§2.1.3 v2 18/40/9 键集）按规格 null-省略姿态发已建模键、略未建模键（notes/signedUrlTtl/propertySets/downloadRedirect/cdnRedirect/xrayIndex/xrayDataTtl/projectKey）——与参照全键集有既有数据模型级漂移，差分归 conductor。漂移点/待验证清单见 Risks
Security:     三个 admin 门（configurations/existence/repolayouts 读）= handler 内 CapRepoWrite 探针（仅 system admin 通过）+ 规格逐字 403；匿名=路由 required 401 挑战。v2 非 admin=信息面收窄（五键，无 class 字段/credential 面）；批读值经 repo.Service 掩码路径（remote 密码 NFR-S14 恒不出现）；错误文案插入的 key/type 均经 writeError 信封 JSON 编码输出；日志无凭证
Performance:  无（全部为清单级读路径，复用既有 ListRepos/GetRepo；无新 I/O 形态；无测量）
Risks:        ① ~~布局表数据缺口~~ 已闭（回填一：25/25 列表逐字，diff=IDENTICAL）。② ~~单读字段缺口~~ 已闭（回填二：六/七键形态+全 25 行单读字段逐字，全量 diff=0；maven-2 推导值已按实测校正——file-regexp `[0-9]` 形态、ddp=true、descriptor 无空格；associations 用量派生对齐捕获机制）。剩余数据面小项：**未显式设 repoLayoutRef 的 BinFlow local 仓不关联任何布局**（BinFlow local 臂不注入默认值，K73 既有裁定；参照 local 列有包类型默认布局——差分若打此臂会出 maven-2-default 关联差，属既有模型级漂移非本票引入）。③ existence 第 4/5 类参数拼写 `federated`/`release_bundle` 为合理推断（参照仅探过 local/remote/virtual），按「接受并 exists:false」实现。④ v2 非 admin 部分视图仅 remote 臂有规格（五键）；local/virtual 按同集实现（url=contextURL）——规格待验证。⑤ v2 map 渲染键序=字母序（参照=Jackson 声明序；规格未钉键序，差分 normalize 应消解）。⑥ configurations face 的 description 空串照发（v1 家族姿态）；参照 null-省略方言未逐臂探过。⑦ repoType= 过滤值大小写敏感（仅小写）。⑧ 批读 >100 limit 文案=中置信（仅反编译）已按规格实现并测试钉死。⑨ 派发单二描述的「六键形态」对 descriptor 布局实为七键（+descriptorPathPattern）——以 wire 实物为准实现；捕获 JSON 无 folderPathPattern 字段（同前上报）
Blockers:     无（布局面列表+单读数据缺口均闭卷：25/25 两形态全量 diff=0）
Next:         工作树可合交（conductor 一并收编）。建议：① 票乙（batch 写族 207 状态机）以本票 repoConfig 基座继续；② D02-R05 单删 v2 报告形态复核小票（L025-1 已上报）；③ 差分腿：本票读面 ≥14 臂（403 信封/空对象/comma 双语义/三 schema/非 admin 五键/CT 406/缺名 500）+ 布局面两形态数据 diff（本票已自证 ALL-CAPTURE-IDENTICAL，差分只需复证）；④ 规格 §2.1.8 可由 reverse-engineer 按两份捕获升级「六/七键形态+ddp 布尔+associations 用量派生+14 非实例探名 500」表述（归 conductor 转派，非本票改文档）
