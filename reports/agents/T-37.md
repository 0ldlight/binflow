# T-37 工作日志 — docker token 流：/v2/token + 401 挑战 + scope 映射（FR-11）

- ticket: T-37 [P0] docker token 流：/v2/token + 401 挑战 + scope 映射（FR-11，批次 2）
- 状态: done（docker login 冒烟项受本机代理环境阻塞，见「客户端验证」，协议面 curl 全绿）
- 日期: 2026-08-18
- area: `internal/adapter/docker/`（token 子域全部新增文件 + handler.go 路由缝）+ 集成测试 `internal/httpapi/docker_token_test.go`（R7 允许）+ 三处装配点签名跟随（cmd/main.go、harness_test.go、docker_v2_test.go 两处）
- 输入: T-32.md T-37 节 AC、docs/prd/milestone-2.md v1.1（FR-11/DE-13/D04/D04b/D04c/NFR-S9/Q2/Q3 暂行口径）、DECISIONS.md ADR-0010 第 4/5 条、docs/reverse/docker-registry.md §5、T-33 交接（challenge() 窄接口 + authenticate 的 context 信号分流 + TokenPath 路由缝）

## 实现

### 1. token 端点（AC①，DE-13）

`internal/adapter/docker/token.go`：

- **GET/POST /v2/token**：query（GET）与 form body（POST，form 值优先于 query）都接受；参数 `service`/`scope`（可多值、空格分隔多 scope）/`account`/`client_id`（后两者读取不校验——规格 §5.2：权限在资源端点判）。
- 成功 200 `{"token","access_token"(同值),"expires_in","issued_at"(RFC3339 UTC),"scope"(收窄后)}` + `Docker-Distribution-Api-Version: registry/2.0`（全 /v2 响应带，DE-17）。
- 签发复用 `auth.TokenRegistry.Issue`（ADR-0010 第 4 条：不平行一套）；**TTL 取 `auth.token_default_ttl`（默认 720h），下限 1h**——非正 TTL 被 floor 到 1h，绝不落「永不过期」（本端点姿态是有限 TTL，AC①/Q3）。
- **任意有效用户可签**（双入口设计，FR-11 v1.1：管理面 `POST /binflow/api/security/token` admin-only 是另一入口）；主体 = Basic 解出的 principal 本人（token 表 username 归属调用者，审计/吊销链可依）。
- **匿名 + anonymous_access=true → 直发**（pull scope 按 ACL 收窄——匿名主体在 Can 里只有 read 规则，push/delete 自然被剔）；匿名关 → OAuth 形 401 `invalid_client` + `WWW-Authenticate: Basic realm="BinFlow Registry"`（本端点客户端在 OAuth 语义中，回 spec 体 Bearer 挑战会让它绕回死循环）。
- `offline_token=true` → 400 `invalid_request`（§6.5②定案）；`grant_type=refresh_token` → 400 `unsupported_grant_type`（non-goal）；其他方法 → 405 + Allow 头。
- **token 域非 2xx 用 OAuth 形错误体**（`{"error","error_description"}`）——与 spec 错误体、E-01 信封三格式并存按域分层（M1 三分层延续 + FR-11）。

### 2. 匿名 token 的主体（关键设计裁定）

token 表 `username NOT NULL REFERENCES users`，且**真实账号绝不能拥有公 anonymous 可获取的 token**（否则该账号任何授权都会注入每个匿名 pull）。`internal/adapter/docker/anonseed.go`：

- 合成账号 `_docker_anonymous`：`enabled=0`、非 admin、无任何授权、密码 hash 为无主随机字节的 argon2id PHC 串（行形状合法，即使有人改库翻 enabled 位也无法登录）。
- 惰性 seed（进程内 `sync.Once` + 幂等 insert，冲突即赢）；seed 失败 → 500 fail closed（匿名 token 宁可不发，不 fallback 到任何真实账号）。
- **验证器对 disabled owner 拒绝**（auth.Verify 既有规则），所以匿名 token 作 Bearer 时表现为「被拒凭证」→ 401 挑战；匿名 pull 的通行始终走 anonymous-open 规则，不经过 token。闭环方向正确：操作员若误启用该账号，匿名 token 立即全部失效（fail closed）。

### 3. 401 挑战与 scope 推导（AC②）

`internal/adapter/docker/scope.go`：

- **scope 推导**（挑战头 + 权限门共用一张表，docker-registry.md §5.1）：GET/HEAD → `repository:<repoKey>/<image>:pull`；PUT/POST/PATCH → `pull,push`；DELETE → `pull,delete`（T-32 AC②「DELETE 含 delete」）；catalog → `registry:catalog:*`（T-40 端点本体，占位路由已带）。subject 用客户端寻址的**完整 name**，客户端协商到的 token 与被拒资源一致。
- **scope 解析**：多 scope 空格分隔（strings.Fields 合并后逐 token）；三段式 `<type>:<name>:<action[,action]>`，action 合法集 pull/push/delete/*；**无效 token 容忍并丢弃**（不 400——规格 §5.2：Artifactory 也不在 token 端点逐项校验 scope；AC③ 矩阵的 invalid-scope 行落此）。
- **scope→Can 映射**（ADR-0010 第 5 条）：pull→r、push→w、delete→d、`*`→{r,w,d}；subject 首段=repoKey、余段=path（`repository:team1/acme/app:pull` → Can(team1, "acme/app", r)）；单段 subject（官方名形态）退化为 repo-key-only 提问（不发明 path）。
- **【暂行】token 响应 scope 按 ACL 收窄输出**（AC④）：admin 直通全量（Authorizer 规则 1 的镜像，不经 store 提问）；非 admin 逐 action 提问 Can，剔除未授权项；无存活 action 的 scope 整条消失；`registry:catalog:*` 对任何已认证主体授予（行级过滤归 T-40 端点，Q5）。**强制仍逐请求 Can**——收窄字段是客户端提示，不是授权。
- 挑战头不泄露内部细节（NFR-S9）：realm 只有 `<base>/v2/token`、service 固定 `binflow`、scope 是协议数据；无堆栈/内部 ID（真栈 curl 抓头验证）。

### 4. 资源端点权限门（AC② 前置落地，T-38~T-40 继承）

`handler.go` `authorizeRoute()`：name 路由过了 repo 门后，用**同一张推导表**把请求方法映射成 Can 提问。匿名写 → 带 scope 的 401 挑战；已认证权限不足 → 403 `DENIED`（spec 体，errors.go 新增码）；授权通过 → 落到 T-38+T-39 的基座 404。nil Authorizer（纯测试装配）对写 fail closed、对读随匿名开关。T-38/T-39/T-40 无需再各自推导权限映射。

### 5. 错误体三格式按域隔离（AC① + NFR-S10）

- `/v2/token` 全部非 2xx → OAuth 形（含 401/400/405/503）+ api-version 头。
- `/v2/**` 其他路由 → spec 体（既有）。
- 两者都不出现 E-01 `"status"` 信封字段（测试以字段判别断言）。
- **被拒凭证（坏 Basic/过期/吊销 Bearer）经 T-33 的 context 信号由 router 渲染**：/v2/token 上的坏凭据得到 spec 体 + Bearer realm=/v2/token 挑战（生产链真路径）——docker 客户端必须看到它能解析的 registry 平面挑战；OAuth 形 401 保留给「无凭据 + 匿名关」这一分支（客户端此时在 OAuth 语境）。测试对两分支分别断言并写明理由。

### 6. 吊销联动（D23，AC②/FR-11-AC6）

/v2/token 签发与管理面同一 token 表 → `POST /binflow/api/security/token/revoke`（按值）即可吊销；吊销后 Bearer 请求 → 401 + Bearer 挑战（spec 体）。真栈验证见下。

### 7. 装配

`docker.New` 签名扩展：`(svc, repos, authz auth.Authorizer, tokens auth.TokenRegistry, users metadata.UserStore, opts, log)`——authz/tokens/users 均可为 nil（测试装配；nil tokens → 诚实 503，nil authz → scope 收窄为空，强制不受影响）。Options 增 `TokenTTL time.Duration`。装配点：`cmd/binflow-server/main.go`（传 authSvc 双角色 + md.Users() + cfg.Auth.TokenDefaultTTL）、`internal/httpapi/harness_test.go`（同法）、`docker_v2_test.go` 两处构造跟随。R7 边界内（middleware.go 本体未动）。

## 自测（命令 + 结果）

- `go build ./...` — 通过
- `CGO_ENABLED=0 go build -trimpath -o /tmp/binflow-t37 ./cmd/binflow-server` — 通过（零 CGO 基线）
- `go vet ./...` — 零输出
- `gofmt -l cmd internal` — 空
- `golangci-lint run ./...` — **0 issues**（token.go 的 gosec G117「access_token 字段名匹配 secret 模式」以单点 nolint + 理由处置：token 本身就是该端点的响应载荷，DE-13 协议要求它上 wire；非误报但不适用）
- `go test ./... -count=1` — **12 包全 ok**
- `go test -race ./... -count=1` — **12 包全 ok**（docker 3.8s / httpapi 38.9s / storage 56.1s 为最重）
- adapter 包 12 个顶层测试全过（scope 矩阵 + 端点矩阵含 23 个子用例）；httpapi 集成 10 个顶层 + 7 个矩阵子用例全过

## 客户端验证（curl 黑盒真栈 + docker）

binary `/tmp/binflow-t37`，三场景独立数据目录/端口（18191 匿名开 / 18192 匿名关+base_url / 18193 LAN 绑定）。

场景 A（匿名开，127.0.0.1:18191；seed：docker-local 仓 + ci-bot 用户 + `ci-out/**` read 授权）：

| 断言 | 结果 |
|---|---|
| D04 `GET /v2/` | 200，body `{}` |
| D04b `GET /v2/token?service=binflow&scope=repository:docker-local/acme/app:pull,push`（Basic admin） | 200；token 64B；`token==access_token`；`expires_in:2592000`（720h）；`issued_at` RFC3339；`scope` 回显全量 |
| D04b Bearer 打 `/v2/_catalog` | 404（T-40 占位；非 401 = token 有效，AC③ 允许占位 404） |
| POST form 同参数 | 200，token 64B |
| D04c 非 admin（ci-bot）`/v2/token?scope=repository:docker-local/ci-out/**:pull` | 200，token 64B |
| D04c 同凭据 `scope=...ci-out/**:pull,push` | scope 收窄为 `...:pull`（push 被 ACL 剔除） |
| D04c 同凭据打管理面 `POST /binflow/api/security/token` | **403**（双入口对照，D3 裁决不变） |
| offline_token=true（GET） | 400 `{"error":"invalid_request",...}`（OAuth 形） |
| grant_type=refresh_token（POST） | 400（unsupported_grant_type） |
| 错误凭据（admin:wrong / no-such-user:x） | 均 401，body 不含用户名/存在性线索 |
| D23 吊销链：签发→Bearer `_catalog` 404（过认证）→revoke（200 "Token revoked"）→Bearer 复打 | **401**（+Bearer 挑战） |
| 匿名写 PUT/POST/DELETE name 路由 | 401 + `scope="repository:docker-local/app:pull,push"` / `pull,delete`（含 delete 动作） |
| 匿名 GET name 路由 | 404（过 repo 门与读授权，落 T-39 基座 404） |
| ci-bot（read-only）Bearer PUT manifest | **403 DENIED** spec 体（消息含推导 scope） |

场景 B（匿名关 + base_url，127.0.0.1:18192）：

| 断言 | 结果 |
|---|---|
| D04 `GET /v2/` | 401 |
| 挑战头 | `Www-Authenticate: Bearer realm="http://127.0.0.1:18192/v2/token",service="binflow"`（realm=base_url 拼接） |
| api-version 头 | `Docker-Distribution-Api-Version: registry/2.0` |
| `/v2/token` 裸匿名 | 401 `{"error":"invalid_client",...}` + Basic 挑战（OAuth 形） |

场景 C（LAN 绑定 192.168.1.149:18193，为 docker daemon 网络路径）：

- **完整 docker login 协商序列**（容器内模拟 daemon 行为：probe → 解析 realm/service → Basic 换 token → Bearer 复打 `/v2/`）：
  - challenge: `Bearer realm="http://192.168.1.149:18193/v2/token",service="binflow"` ✓
  - token 长度 64 ✓
  - `GET /v2/` with Bearer → **200** ✓
- **`docker login` 本体受阻（环境非协议）**：本机 docker daemon 在 VM 内、HTTP 代理 127.0.0.1:7897 系统级启用且 daemon.json 无 `insecure-registries` 项。表现：daemon 对 `127.0.0.1:18192` 报 connection refused（代理不转发本地回环）、对 LAN 地址报 "server gave HTTP response to HTTPS client"（plain-HTTP registry 未列入 insecure-registries，daemon 坚持 TLS）。修法唯一且是**用户本机 daemon 配置**（往 `~/.docker/daemon.json` 加 `"insecure-registries": ["192.168.1.149:18193"]` 并重启 Docker Desktop）——写用户配置/重启服务超出我的权限边界（安全底线：不动用户配置），故标 **blocked-环境项**，协议面以容器内全序列等效覆盖（上表）。D05 全量（login→push→pull→run）本就归 T-44，届时 QA 在可控 daemon 或 compose 环境跑（FR-14-AC1 的 compose 路径天然解决此问题）。此结论与注入说明已留待转交。

## 测试清单

新增 `internal/adapter/docker/token_test.go`（包内单测）：

- `TestScopeMatrix`（14 行表：单/多 scope 空格分隔、逗号 action 拆分、admin 全量、嵌套 subject 拆分、无斜杠 subject 退化、无效 token 容忍、未知 action 丢弃、catalog 授予/匿名拒、匿名读授予、匿名写剔除、`*` 具体化、多参数合并）
- `TestNarrowScopesFailClosed`（nil Authorizer 零授予）
- `TestDeriveChallengeScope`（7 方法行）
- `TestTokenEndpointSuite`（9 行表：GET/POST form/query 三种拼写、匿名双态、offline_token、refresh grant、405、nil registry 503）
- `TestTokenSubjectIsThePrincipal` / `TestTokenTTLFloor` / `TestEnsureAnonymousSubject`（幂等+形状）/ `TestEnsureAnonymousSubjectFailure`（fail closed）

新增 `internal/httpapi/docker_token_test.go`（真栈集成，10 顶层）：

- `TestV2TokenNegotiation`（D04b 全链 + Bearer 复打）
- `TestV2TokenPOSTForm`、`TestV2TokenNonAdmin`（D04c 三段对照）、`TestV2TokenWrongCredentials`（不泄露存在性，双分支）
- `TestV2TokenAnonymousBothModes`（D04 双态）、`TestV2TokenRevocation`（D23）
- `TestV2ScopedChallengeMatrix`（7 行：GET/HEAD/PUT/POST/PATCH/DELETE/嵌套名——完整 realm+scope 断言）
- `TestV2RouteGate`（匿名写挑战 / read-only 403 DENIED spec 体 / 授权写过门 404 / 授权读过门）
- `TestV2TokenCleanupRows`、`TestV2TokenOAuthErrorPlane`（三格式隔离）

修改：`name_test.go` / `docker_v2_test.go` 两处构造签名跟随；`harness_test.go` 装配扩展。M1 既有测试零回归（全量 12 包 ok 即证）。

## 遗留 / 交接

- **docker login 本体**：环境项（见客户端验证节），D05 全量归 T-44；建议 T-44/T-45 用 compose + insecure-registries 或可控 daemon。技术事实：BinFlow plain-HTTP 直连可用（容器内全序列已证），daemon 侧需 insecure-registries 条目——这是 docker 对所有 plain-HTTP registry 的通用要求，非 BinFlow 特例（tech-writer T-46 宜写明）。
- **T-38/T-39/T-40**：`authorizeRoute` 已在 repo 门后把权限判定做完（同一张 scope 推导表），内容 handler 只需实现协议本体；403 DENIED / 401 scoped challenge 的渲染不要重做。
- **T-40 catalog**：挑战 scope `registry:catalog:*` 已定；`registry:catalog` scope 对已认证主体在 token 端点直授，行级 read-ACL 过滤留给端点（Q5 暂行）。
- **匿名 token 语义**（architect 复审点，非阻塞）：匿名 token 作 Bearer 时因 disabled owner 被 401——设计意图（匿名通行不经 token、fail closed），若未来要求「匿名 token 可作 Bearer 通行 pull」，需在 auth.Verify 为该合成主体开特例（不建议，会让吊销语义复杂化）。
- `expires_in` 恒 720h（config 可调）；规格「待动态验证」清单中 expires_in 恒 3600 一项，BinFlow 取 config TTL 不跟进 Artifactory 常量（与 Q3 暂行口径一致）。

## 变更清单

新增：
- internal/adapter/docker/token.go（/v2/token 端点 + OAuth 形错误）
- internal/adapter/docker/scope.go（scope 语法/推导/Can 映射/ACL 收窄）
- internal/adapter/docker/anonseed.go（匿名 token 合成主体 seed）
- internal/adapter/docker/token_test.go
- internal/httpapi/docker_token_test.go

修改：
- internal/adapter/docker/api.go（Options.TokenTTL；Handler 增 authz/tokens/users；New 签名）
- internal/adapter/docker/handler.go（token 路由接 serveToken；挑战带推导 scope；authorizeRoute 权限门）
- internal/adapter/docker/errors.go（ErrCodeDenied）
- internal/adapter/docker/name_test.go（构造签名跟随）
- internal/httpapi/harness_test.go（装配扩展）
- internal/httpapi/docker_v2_test.go（两处构造签名跟随）
- cmd/binflow-server/main.go（装配传 authz/tokens/users/TokenTTL）

---

# 修复轮 T-55 — /v2/token 401 错误体一行修（PRD v1.2/C3 收口）

- ticket: T-55 [P0-min] /v2/token 401 错误体统一 OAuth 形（T-53 PM 裁定的实现侧收口）
- 日期: 2026-08-19
- 输入: docs/prd/milestone-2.md v1.2 §5.2 错误契约（C3/D3：`/v2/token` 所有非 2xx 一律 OAuth 形 `{"error","error_description"}`，资源端点维持 spec 体）+ reports/agents/T-53.md（回归断言建议：`curl -su <user>:wrong $BASE/v2/token?...` → 401 + `jq .error` 非空）

## 问题

T-37 交付态在 /v2/token 上 401/400 两不沾：400 参数错（offline_token/refresh_token）由 `serveToken` 渲染 OAuth 形；401 凭据错（错误 Basic）经 T-33 的 context 信号由 router 调 `RenderAuthFailure` 渲染——落到 spec 体 `{"errors":[{code:UNAUTHORIZED...}]}` + Bearer 挑战。v1.1 的 NFR-S10「/v2 全 spec」与 FR-11-AC7「spec 形」、§5.2 注「OAuth 形」三处打架，T-53 裁定 token 端点族全 OAuth。

## 修法（最小面）

1. `token.go` `writeOAuthError` 去掉「401 自动附 Basic 挑战」的副作用（挑战头归属调用方——两种 401 说的挑战不同，T-55 只统一 body）；签名删未用的 `*http.Request`。
2. `token.go` 新增 `renderTokenAuthFailure`：**WWW-Authenticate Bearer 挑战头逐字节不变**（realm=`<base>/v2/token`、service=`binflow`，客户端重试仍靠它）+ OAuth 体 `{"error":"invalid_client","error_description":"authentication required"}`。
3. `handler.go` `RenderAuthFailure` 路径感知：token 路由族（`/v2/token` 与子路径，同 `serveNameRoute` 的判据 `isTokenRoute`）→ `renderTokenAuthFailure`；其余 /v2 路由 → 既有 spec 体挑战（**不变**）。
4. `serveToken` 匿名关 + 无凭据分支：显式设 Basic 挑战后写 OAuth 体（行为不变，只是 header 设置从 writeOAuthError 移到调用点）。

错误码取 `invalid_client`（与无凭据分支一致、OAuth2 规范里「客户端认证失败」的正码；coordinator 允许 invalid_client 或 unauthorized 二选一）。

## 测试

- 新增 `TestRenderAuthFailureTokenPathIsOAuth`（adapter 层，4 行表：/v2/token、/v2/token/sub → OAuth 体 + Bearer 头；/v2/、name 路由 → spec 体；两分支挑战头/api-version 头同形断言）。
- `TestV2TokenWrongCredentials`（httpapi 真栈）改写：三组错误凭据（含 T-53 建议的 `ci-bot:wrong`）→ 401 + `error` 字段非空 + **无** `errors[]`/`status` 字段 + Bearer realm 挑战头逐字节相等。

## 自测

- `go test ./... -count=1` — 12 包全 ok（docker 66.8s / httpapi 166.4s）
- `go test -race ./internal/adapter/docker/ -count=1` — ok（57.0s）
- `golangci-lint run ./internal/adapter/docker/... ./internal/httpapi/...` — 0 issues；`gofmt -l` 空
- 注：修复中途 internal/metadata 短暂不可编译（并行票在途改动），等待其落定后全量复跑通过；期间观察到 docker 包一次并发调度下的偶发失败，隔离复跑（plain+race）稳定通过，与本修无因果（T-52 blob 域测试在途）。

## 真栈 curl 复验（/tmp/binflow-t55，18195 匿名开 / 18196 匿名关）

| 断言 | 结果 |
|---|---|
| `curl -su admin:wrong /v2/token?...` | 401，`jq .error` = `invalid_client`，desc = `authentication required` |
| `curl -su ci-bot:wrongpw /v2/token?...`（T-53 建议用例） | 401，同上 |
| `curl -su no-such-user:x /v2/token?...` | 401，同上（三者 body 全同——不泄露用户存在性） |
| 挑战头 | `Www-Authenticate: Bearer realm="http://127.0.0.1:18195/v2/token",service="binflow"`（不变） |
| offline_token=true | 400 `{"error":"invalid_request",...}`（OAuth 形，维持） |
| 资源端点匿名 PUT | 401 `{"errors":[{"code":"UNAUTHORIZED",...}]}`（spec 体，不变） |
| 200 路径回归 | admin 换 token 64B；Bearer 打 /v2/ → 200 |
| 匿名关 + 裸匿名 /v2/token | 401 OAuth 形 + `Basic realm="BinFlow Registry"`（不变） |

## 变更清单（T-55 轮）

- internal/adapter/docker/token.go（writeOAuthError 重构 + renderTokenAuthFailure + isTokenRoute）
- internal/adapter/docker/handler.go（RenderAuthFailure 路径感知）
- internal/adapter/docker/token_test.go（TestRenderAuthFailureTokenPathIsOAuth）
- internal/httpapi/docker_token_test.go（TestV2TokenWrongCredentials 断言改写）

---

# 修复轮 T-44 P0 三联 — ping 无条件挑战 / offline_token 容忍 / POST 表单凭据（D44-1/2/3）

- ticket: T-44 QA P0 缺陷修复（reports/agents/T-44-qa.md §2：D44-1/D44-2/D44-3；PRD 勘误 C5/C6 由 PM 侧同步）
- 日期: 2026-08-19
- 范围: ping/token 面（本票子域）；D44-4/5/6（P2）落在 manifest/uploads/catalog 域（T-39/T-38/T-40 表面，文件正被其他修复轮并行编辑）——**不在本轮触碰**，建议 conductor 按域另派

## D44-1 — ping 无条件 401 挑战（匿名开 тоже）

**修法**（handler.go `servePing`）：未认证 ping 一律 401 + Bearer 挑战（匿名开实例也一样）；认证 ping 维持 200 `{}`。`Options.AnonymousAccess` 不再影响 ping。依据：ping-缓存型客户端（moby/containers-image）只按 ping 缓存的挑战决定是否协商——ping 200 = 永不携带凭据（QA §4.1：push 401 五轮零 token 请求；错口令 login 也 Succeeded）。匿名访问改走匿名 token 全链。

**匿名 token 可用化**（D44-1 的前置，anonseed.go + blob.go `principalOf`）：
- 合成账号 `_docker_anonymous` 改 **enabled=1**（此前 disabled——token 作 Bearer 时被验签链拒绝，ping 挑战化后匿名 pull 全走这条路，必须可验证）；密码 hash 改为真 argon2id（随机 32B 秘钥即弃——任何口令都不可能通过验证，防穷举/防 PHC 解析意外）。
- `principalOf` 把该主体的 principal 映射为 nil（匿名身份）：/v2 全平面上匿名 token 的权限 = 匿名规则（anonymous_access 开时只读、写一律 401 挑战），**对该账号的任何授权行在 /v2 上被忽略**——公 anonymous 可得的 token 无法被权限行升格。映射点单点化（principalOf），authorizeRoute/canMountFrom/catalogVisible/narrowScopes/serveToken 全部经它。
- T-33 的 D04「匿名开 200」口径由 PM 勘误 C6 同步（测试 TestV2PingAnonymousOpen → TestV2PingChallengesUnauthenticated 重写）。

## D44-2 — offline_token 接受并忽略

token.go：删除 offline_token 400 分支（tokenRequest 字段与 isTruthy 一并移除）。官方 distribution token spec 定义该参数且 server MAY ignore；docker 29 挑战模式 login 的 token GET 必带 `offline_token=true`。响应永不携带 refresh_token（Q3 无 refresh 维持）。PRD §6.5② 由 C5 勘误。

## D44-3 — POST OAuth 表单凭据与 Basic 同权

token.go `formCredentials` + `authenticateForm`：POST 且中间件未从 Authorization 头解析出主体时，读表单 `username`+`password`（grant_type=password 拼写——buildkit/helm）或 `client_id`+`client_secret` 对；经 `users.Get` + `auth.VerifyPassword` + enabled 校验（该端点是全适配器唯一自行处理凭据之处——表单体在中间件之后才可解析，注释写明）。**错口令 → 401 OAuth 形（不回落匿名签发）**；同一失败哨兵涵盖未知用户/错口令/禁用（不泄露存在性）。表单值优先级：头凭据（Basic/Bearer）> 表单对 > 匿名。query 拼写的凭据被忽略（进日志/代理的泄漏面）。

## 测试与真栈验证

单测/集成（internal/adapter/docker + internal/httpapi）：
- 重写 `TestV2PingChallengesUnauthenticated`（匿名双拼法 401+精确挑战头 / 认证 200 `{}`）；`TestV2ErrorEnvelopeIsolation`/`TestV2MethodNotAllowedHasAllow`/`TestSpecErrorEnvelopeShape` 改带认证主体（未认证 ping 先挑战后 405）
- `TestV2TokenAnonymousBothModes` 升级：匿名 token 作 Bearer——**读过 gate（404 MANIFEST_UNKNOWN 非 401/403）、写 401 带 pull,push scope 挑战**
- 新增 `TestV2TokenFormCredentials`（password 对 200 且 token 可 push 过 gate；client_id/secret 对 200；错口令 401 不泄露）
- offline_token 行改「接受并忽略：200 + 无 refresh_token」；`TestV2TokenOAuthErrorPlane` 探针改 refresh grant
- `TestEnsureAnonymousSubject` 改断言 enabled=true + 真 PHC + 猜测口令不通过

真栈（/tmp/binflow-t44fix，双实例 A=匿名开 18198 / B=匿名关 18199，LAN 192.168.1.70，dind 29.7.2 `--insecure-registry` 按 QA 配置路径）：

| 断言 | 结果 |
|---|---|
| 协议预检 A：匿名 ping | 401 + `Bearer realm="http://…/v2/token",service="binflow"` |
| 协议预检 A：认证 ping | 200 |
| 协议预检 A：GET token `offline_token=true`（Basic） | 200（原 400） |
| 协议预检 A：POST form 凭据（username/password） | 200，token 64B |
| 协议预检 A：POST form 错口令 | 401 |
| 协议预检 A：匿名 token 读/写 | 读 404（过 gate）/ 写 401 带 scope 挑战 |
| docker login A 对口令 | `Login Succeeded`（exit 0） |
| docker login A **错口令** | **失败**：`unauthorized: authentication required`（原「Succeeded 空洞」修复，FR-11-AC4 复位） |
| docker push A（admin） | 成功（manifest+layers 全推） |
| docker rmi→pull→run A | `ok` exit 0 |
| docker **logout 后匿名 pull→run A** | `ok` exit 0（匿名 token 全链：POST /v2/token 200 → Bearer 读通行） |
| docker login B（匿名关，挑战模式 POST 表单+offline_token） | `Login Succeeded`（D44-2+D44-3 路径，服务端日志 POST /v2/token 200 ×2） |
| docker push/pull/run B | 全 0，run `ok` |
| **buildx** `build --push`（登录态，buildkit 真实表单流） | 推送成功；pull 回 + `docker run` 输出 `buildx-ok`（D44-3 原报告场景复位） |
| 服务端日志 | 全程无 5xx（一条 500 为客户端断连的 context canceled repo lookup，O1 断连定界域，非本面） |

## 自测

- `go test ./... -count=1` — 12 包全 ok（docker 53.5s / httpapi 88.5s / storage 90.1s）
- `go test -race ./internal/adapter/docker/ ./internal/httpapi/` — ok（38.9s / 111.3s）
- `golangci-lint run`（docker+httpapi+cmd）— 0 issues；`gofmt -l internal cmd` — 空

## 变更清单（本轮）

- internal/adapter/docker/handler.go（servePing 无条件挑战；PrincipalFrom→principalOf）
- internal/adapter/docker/anonseed.go（合成账号 enabled + 真 argon2id 随机哈希；注释重写）
- internal/adapter/docker/blob.go（principalOf 匿名映射——单点）
- internal/adapter/docker/token.go（offline_token 忽略；formCredentials/authenticateForm；principalOf）
- internal/adapter/docker/token_test.go、anonseed 断言、name_test.go（跟随）
- internal/httpapi/docker_token_test.go（重写/新增 4 处）、docker_v2_test.go（ping 三测跟随 C6）

## 遗留

- D44-4（超长 tag GET 404）/D44-5（上传状态 Location 头）/D44-6（空 tags 200）——P2，属 manifest/uploads/catalog 域且文件在并行修复轮手中，建议按域另派（D44-6 附带 PRD 校准项）
- 观察项：表单凭据解析出的主体不进 access log 的 user 字段（middleware 视角仍是 anonymous）——日志可观测性缝，归 httpapi 日志域（T-41 家族），不阻塞
