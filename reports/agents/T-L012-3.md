# T-L012-3 — dev-go-storage：mdTimestamps 递归臂升级（props-mtime 逐节点对齐）

Ticket:       T-L012-3 · mdTimestamps 递归臂升级票（LOOP 012 轨道 L012-3，rest/storage-list-params-family resolved.note LOOP 012 池项段素材）· P1
Role:         dev-go-storage
Area:         internal/httpapi（本票明示授权域：properties 写族 + ?list P2 派生消费面）；internal/storage 与 internal/remote 零改动；internal/metadata 零改动（未动 schema、未动 store 接口）
Input:        台账 docs/compatibility/known-divergence.yaml#rest/storage-list-params-family resolved.note LOOP 012 池项段（E1-E4 取证：参照递归属性写移动子项 props-mtime、直写只动该子项；BinFlow recursive=1 只记 addressed 节点致子项 mdTimestamps.properties 缺席）；T-L011-1.md Outputs 一轮（E1-E4 素材）与 Risks⑤；docs/design/architecture.md §15.3.3（?properties 族契约 + 审计 props.write/props.delete 形）；L010-list-p2-diff.md §1（mdTimestamps 形态与「never faked」姿态）；本票双端活体再取证（:8082 7.161.20 + UAT :8083 换装前后）
Changes:      ① 择案 A（propsTargets 逐 target 记审计行），弃案 B（NodeProps 加列/派生）——论证：mdTimestamps.properties 的唯一事实源已是审计派生（propModifiedTimes 查 props.write/props.delete 行），案 A 让递归写的事实（每子项确被写）落进既有管道，零 schema、零迁移、零新查询面（案 B 须 node_props 加列=迁移+接口面，票界要求「论证零迁移或转票」即出局；audit 派生 1000 行页上限为既有已登记 ponytail 天花板，案 A 不改其语义只放大触发面，注释已补）② PUT 臂：targets 循环内逐 target 在 Merge 成功后记 props.write 行——**变更条件化**（mergeChanges：新键或值集不同才记；幂等重写不记）③ DELETE 臂：同型逐 target props.delete 行 + 变更条件化（deleteChanges：drop 集合与该 target 现存键相交、或 *=形式该 target 有任何键才记）——条件化的活体依据：参照递归 DELETE 现存键全体子项移动@删除时刻（13:51:48）、DELETE 无人携带的键全体 parked、值集相同的递归重 PUT 全体 parked（13:51:46 不动）——即「行随实变更不随请求」，连带给 BinFlow 修掉一个潜在分歧（幂等重写曾移动 mtime）④ **附带修复（同路径根因）**：通配 miss 的 DELETE（如 ?properties=nosuch*，解析出的 drop 集为空且非 * 形式）此前落入 store 的「空=全删」形态，实测（UAT 换装前活体）把节点全部属性清空且递归时殃及全体子项——参照同臂 204 属性原样；修法=空 drop 集（非 * 形式）跳过 Delete 调用 ⑤ storage.go propModifiedTimes 注释补 fan-out 放大说明 ⑥ root P2 enrich 角探针（任务②）：参照臂可达——PUT 属性打仓库根=204（默认递归 fan-out 含根自身，GET ?properties 回读在），此后 ?list&includeRootPath=1&mdTimestamps=1 的「/」根条目携 mdTimestamps.properties@写入时刻；无根属性时「/」无 mdTimestamps（双端一致）；BinFlow 现行=properties 族三动词打根一律 400「invalid artifact path: path is empty」（root-not-a-node 姿态）→ 该 enrich 臂在 BinFlow **架构上不可达**（无写入面可产生根属性行），对齐需开 properties 族的根姿态新票（PUT/GET/DELETE+递归 fan-out+authz 空路径+审计 path=""〔propModifiedTimes 现跳过空 path〕），非本票 enrich 缺陷，登记转裁
Files:        修改 internal/httpapi/properties.go（PUT/DELETE 逐 target 条件化审计行 + 空集守卫 + mergeChanges/deleteChanges 助手 + recordPropsAudit 注释）、internal/httpapi/storage.go（propModifiedTimes 注释）；新增 internal/httpapi/props_recursive_mtime_test.go（行为命名，票号在文件头注释）；UAT 面：镜像 binflow:uat-l0123-a81cd053-wip-alpine(-amd64)、deploy/compose/.env.uat 版本键两行、容器 binflow-ga 换装（数据卷沿用）；worktree /tmp/binflow-l0123 已移除；无删除文件
Tests:        props_recursive_mtime_test.go 两件：TestPropsRecursiveWriteMovesChildMtimes——E1 递归 PUT 后 /x.bin、/sub、/sub/y.bin 三类条目（文件/嵌套文件夹/深层文件）mdTimestamps.properties 全在场；E2 二次递归写全体严格移动（跨秒断言）；E3 直写单子项只动该子项、兄弟逐字相等；E4a 值集相同递归重 PUT 全 parked（逐字相等）；E4b 无人携带键的递归 DELETE parked；E4c 真键递归 DELETE 全体移动；全删后键整体消失（enrich 门）；TestPropsDeleteWildcardMissKeepsProperties——直打与递归两臂通配 miss 后属性原样保留（数据丢失回归）。**红检**：stash properties.go 修复 → E1 三条目缺席 + E4c 不动 + 通配 miss 清空全红 → 还原全绿
Commands:     go build ./... && go vet ./...；gofmt -l internal/httpapi internal/storage internal/remote（空）；~/go/bin/golangci-lint run ./internal/httpapi/...（0 issues）；go test ./internal/httpapi/ ./internal/storage/... -count=1；go test -race ./internal/httpapi/ -run 'TestProps|TestStorageList|TestStorageProperties|TestMetadataFace' -count=1 solo；UAT 全链：git worktree add --detach /tmp/binflow-l0123 a81cd053 + 应用本票三文件 diff；make console && make docs && make release VER=uat-l0123-a81cd053-wip；离线镜像（DaoCloud 第三次 EOF，/tmp 修补副本：REPO_ROOT 钉 worktree + fetch_index 本地 digest 回退 LOCAL_BASE_DIGESTS，不触 deploy/）REGISTRY= PUSH=0 SMOKE=0 ARCHES=amd64 VARIANTS=alpine；docker tag -alpine 别名；.env.uat 版本键 sed；docker compose -p binflow-uat up -d --force-recreate；双端差分脚本 E1-E4+root 八臂串行
Outputs:      四门全绿（见 Commands）；UAT version=`{"version":"uat-l0123-a81cd053-wip","revision":"a81cd053"}`，~3s 健康；差分（值归一后逐臂 SAME）：E1 递归 PUT → 参照/本端三子项条目全携 mtime@写时刻（参照 14:09:15.365-.388ms 级 / 本端 14:09:28.000Z 秒级——audit 派生族既定拼写）；E2 二次写双端全体移动（17.582→17.595 / 28→30）；E3 直写双端仅 a.txt 动（sub parked 逐字同）；E4a/E4b 双端全体 parked；E4c 真删双端全体移动（24.06 / 37）；W 通配 miss 双端 204+属性 ['pk','solo'] 保留+mtimes parked（换装前 BinFlow 此臂清空全属性=修复实证）；**root 角分歧**：参照 root PUT 204→「/」携 properties@14:09:24.281Z vs BinFlow 400→「/」absent（架构性不可达，见 Changes⑥）
Compatibility: E1-E4 四臂 + 通配 miss 角与 :8082 活体逐臂对齐（变更条件化语义=行随实变更）；时间戳秒级 .000Z 维持 M16 audit 派生族定谳不翻案；mdTimestamps 键位/缺席门与 L010 §1 形态一致；architecture.md §15.3.3 审计行形状（repo/path/键清单+recursive 标记）逐行仍合——递归操作现为 N 行（每实变 target 一行），文档未钉行数，audit 面 props.write/props.delete 系 BinFlow 自有 action 名无参照对照面；root 角新分歧登记（转 conductor/architect 裁）：参照 properties 族根姿态开放（204+根属性+递归）vs BinFlow 400 path-empty
Security:     无新输入面；审计 detail 沿键摘要+recursive 标记不含值敏感面外的内容；通配 miss 空集守卫反是数据保全修复（不再误删属性）；双端 admin 口令仅经 env/命令行变量传入不落日志；UAT 绑 127.0.0.1:8083；scratch 仓双端用后删净（l0123-loc/l0123-empty/l0123-bug）
Performance:  逐 target 审计行为best-effort 同步 Append（与既有单行同机制，量随 target 数线性——递归写本就 O(target) 次 Merge 事务，审计占比小）；DELETE 新增每 target 一次 List 预读（PUT 原有 cap 检查已 List，DELETE 为变更检测新增一次索引读，主键点查）；propModifiedTimes 派生仍两查询每 action（1000 行页上限不变，fan-out 放大已注释，升级路径=metadata GROUP BY 归属 store 面）
Risks:        ① audit 派生 1000 行/action 页上限：单仓属性史超限后尾部 mtime 陈旧/缺席（既有天花板，fan-out 行加速消耗窗口——重属性史仓的 mdTimestamps 完备性归后续 GROUP BY 票）② 幂等重写不再落审计行：依赖「每次 PUT 必留痕」的下游（未见）会看不到 no-op 写；变更条件化系参照语义对齐 ③ root 角分歧未了结（转裁项，见 Compatibility）④ audit 行 Time 逐行 Now()（秒级）同秒内多 target 同戳——参照为同操作毫秒级微差，值归一下无差 ⑤ 递归属性写 BinFlow 显式 recursive=1 vs 参照默认递归的缺省语义差系 FR-89.2/决策 11.40 族既有裁量另计（台账原注维持）
Blockers:     无
Next:         ① 翻绿建议（归 compatibility-engineer）：台账 #rest/storage-list-params-family resolved.note 的 LOOP 012 池项段闭项——E1-E4 双端活体对齐证据=本日志 Outputs；如需立新账则登记「properties 族根姿态」（参照 204 开放 vs BinFlow 400，authority 缺位待裁）② root 角若裁对齐：开面收缩/扩张票（PUT/GET/DELETE 根姿态+authz 空路径+propModifiedTimes 空 path 跳过放开）③ mdTimestamps 重属性史完备性（1000 行页）：可立 metadata GROUP BY(path,max(time)) 派生面票（AuditStore.LastActionTimes 先例）④ DaoCloud 源三遇 EOF——离线配方可固化 build-release.sh OFFLINE 开关（归 devops，T-L011-1 Next⑤ 重申；本票 /tmp 修补副本即其原型）⑤ UAT 现载 uat-l0123-a81cd053-wip（a81cd053 + 本票三文件）；复位基线复用 T-L011-4 Commands
