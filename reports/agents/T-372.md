# T-372 trash 树常驻入口节点 — 工作日志

- 票：T-372 [P1] M13 B5（FR-122.1 / FR-122-AC1；L21 段）
- 状态：done（四闸门 + 全量 Playwright + axe 双主题全绿；1 腿共居负载假阳性按 flake 协议串行复跑甄别为绿）
- 日期：2026-08-31
- area：web/src/pages/artifacts/（树浏览器 + 样式）、web/e2e/（m13 新 spec + m8 树 spec 增腿）、web/scripts/anchor-audit.mjs（STOP 停词 v1.17）、docs/design/console-m8.md（推翻条款回写——票面 AC1 指定）、docs/design/console-ux.md（锚册 v1.17）；**internal/ 与 cmd/ 零触碰**（AC2：服务端契约 git diff=0——工作树里的 internal/ 测试文件 diff 全部属并行票 T-361，票前已在）

## 1. 做了什么

### 1.1 先改册（AC1 规格先行纪律，落码前完成）

- **console-m8.md §4.3**：「Trash Can 常驻节点**不建**（BinFlow 无回收站）」条款**推翻回写**——留痕要素齐：原条款删除线保留 + 推翻票号/FR/日期（T-372 / FR-122.1 / 2026-08-31）+ 推翻依据（前提「BinFlow 无回收站」被 M12 FR-106 推翻，回收站页 T-352 在案）+ 新形态（reverse §3.2 末尾常驻、最小面 = 入口跳转、可见性管理壳同门、键盘叶节点语义、锚名）。
- **console-m8.md §6.3**：线框注记补一行（树尾常驻入口——§4.3 推翻条款的兑现面；供 T-374 AC1「推翻条款落痕核查」双点落痕）。
- **console-ux.md v1.17**（锚册）：§0 修订记录 + 头部票据/状态行 + **§10.3 T-372 批 1 名锚入册**（`tree-trash-node`）+ §10.5 两行承载锚补记（/artifacts 与 /admin/governance/trash）。批块按 T-352/T-366 既有体例（锚名 + 形态描述 + 消费 spec + 变更注记）。
- **anchor-audit.mjs STOP 停词 v1.17**：`t372-trash-node` / `artifacts-tree`（批块行文引用的 spec 文件名段，非锚——v1.15 'trash-can' 同款先例）。

### 1.2 实现（FR-122.1 最小面）

- **ArtifactsBrowser.tsx**：左树末尾常驻 `TrashTreeNode`——
  - 位置：仓库行之后（reverse console-ui §3.2「末尾常驻 Trash Can」对齐）；加载/错误态照常渲染（常驻），空实例引导卡整块替换树时不渲染（常驻语义限定在树本体，批块变更注记已登记）。
  - 可见性：`canSeeAdmin = admin || readOnly`（AppShell 管理壳同门）；普通 user 不渲染（§2.2 管理面可见性纪律）。
  - 交互：点击 / Enter / Space → `navigate('/admin/governance/trash')`（**页身沿用 M12 T-352 页面，零新面、零数据请求**）；叶节点无展开语义（→/← 不响应），↑↓ 沿 `[data-tree-row]` 树行序移动（与仓库/目录行共享键盘模型）；不参与「过滤仓库」过滤域（常驻 ≠ 已加载仓库集成员）。
  - a11y：`role=treeitem` + `aria-level=1`（tree 容器内），装饰图标 aria-hidden，twisty 位空槽占位对齐栅格。
- **browser.css**：`.tree-node.trash-node` 与仓库区分隔线（Artifactory 树尾 Trash Can 同形分隔；视觉面无锚）。

### 1.3 e2e（AC2）

- **web/e2e/m13/t372-trash-node.spec.ts**（新，6 腿，全 mock 探针 off dist——trash-can.spec 同款基座）：① admin——节点常驻树尾（DOM 序 = 全部 tree-repo 行之后）+ 点击深链跳转 /admin/governance/trash + 返回复位；② 键盘——末位仓库行 ↓ 落到节点 + Enter 激活；③ 常驻语义——过滤仓库收空仓库行后节点不消失；④ readonly_admin——节点可见 → trash-readonly-note + 写动作三反断言；⑤ 普通 user——tree-root-denied + 节点反断言；⑥ axe 双主题（light/dark）serious/critical = 0。
- **m8/artifacts-tree.spec.ts 增 2 腿 + 1 反断言**（真栈）：admin——树尾 DOM 序 + 点击落 M12 页（community 形态 = trash-locked 卡钉住）；readonly_admin——同门可见；plain user 腿——节点 toHaveCount(0)。

## 2. 变更文件清单

修改：
- docs/design/console-m8.md（§4.3 推翻回写 + §6.3 注记行）
- docs/design/console-ux.md（v1.17：头部票据/状态 + §0 修订记录 + §10.3 T-372 批块 + §10.5 两行）
- web/src/pages/artifacts/ArtifactsBrowser.tsx（TrashTreeNode + 树尾挂载 + canSeeAdmin）
- web/src/pages/artifacts/browser.css（trash-node 分隔线）
- web/e2e/m8/artifacts-tree.spec.ts（+2 真栈腿 +1 反断言）
- web/scripts/anchor-audit.mjs（STOP +2 停词，v1.17 注释）

新增：
- web/e2e/m13/t372-trash-node.spec.ts

零触碰：internal/**、cmd/**（AC2 git diff=0；internal/console/dist 的 dist 拷贝为构建动作且该目录除 placeholder.html 外均不入 git）。

## 3. 自测（实际执行）

| 命令 | 结果 |
|---|---|
| `cd web && npx tsc --noEmit` | 零输出 |
| `cd web && npm run lint` | 0 problems |
| `cd web && npm run build` | ✓ built in 3.58s（relink-assets verified：无 ui-segment 资源引用残留） |
| `node scripts/anchor-audit.mjs --ledger` | **PASS**（src 家族 520 / 落点 570；registered 519 + 白名单 8 + 退役表 98；unregistered/broken 双零，retired 全在表） |
| `make console-size`（dist 拷入后） | **352,850 bytes gzip（< 5MB 预算维持；T-366 基线 352,447 + 403）** |
| `BASE=…8137 npx playwright test e2e/m13/t372-trash-node.spec.ts --project=chromium` | **6 passed** |
| `BASE=…8137 npx playwright test e2e/m13/t372-trash-node.spec.ts e2e/m8/artifacts-tree.spec.ts --project=chromium` | **16 passed (2.6m)**（含真栈 2 新腿；tree spec 既有 10 腿零回归） |
| `BASE=…8137 npx playwright test`（全量三 project，自有净实例 :8137） | **237 passed / 23 skipped (7.3m)** + 1 failed → 甄别：`m8/repositories-admin.spec.ts:101` axe 腿 = **T-352 报告登记的共居负载假阳性家族同行号复现**（repositories-admin:101 在册）；`--workers=1` 串行复跑该 spec **5 passed (42s)** = 绿。flake 协议处置，log 不 chase |

实例形态：`go build -o /tmp/bf-t372/binflow-server`（新 console dist 嵌入）+ `BINFLOW_HOME=/tmp/bf-t372/home`（临时数据目录，净实例）+ 最小 yaml（listen 127.0.0.1:8137）。

## 4. 契约漂移 / 上游缺口

无（本票零 API 消费——入口节点零数据请求，落地页 API 面 T-352 已验收）。

## 5. 遗留

1. 「内嵌沿用 M12 页面」的备选形态未取（票面给的是入口跳转/内嵌二选一，最小面取入口跳转——Artifactory 树内展开浏览属后续增强面，如需另票）。
2. 空实例（零仓库）时引导卡整块替换树 → 节点不渲染（§1.2 已述 + 批块变更注记登记；如产品要求空实例也见入口，属行为变更另裁）。
3. T-374（docs 票）将核对本票两处推翻落痕（console-m8 §4.3/§6.3）——已按其 AC1 预留双点。
