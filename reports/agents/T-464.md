# T-464 — en 双资源包填充 + 语言切换器 + 断言双语化（FR-149.2/.3/.4，断言反转⑥收口）

状态：done（待收编） ｜ role: dev-frontend ｜ 2026-09-06 ｜ M16 批次④ B15 尾票（web/ 全域）

## 1. en 全量填充（FR-149.2）

**1,971 键 × 10 域全量填充**（含本票新增 `语言` 键），落 `web/src/i18n/locales/en/*.ts`：

| 域 | 键数 | 域 | 键数 |
|---|---|---|---|
| console | 215（214 + 本票新键 `语言`） | governance | 314 |
| repositories | 393 | monitoring | 77 |
| artifacts | 209 | webhooks | 89 |
| search | 47 | admin | 149 |
| security | 330 | common | 148 |

- **术语对齐 Artifactory（两包保真）**：repo key / node / checksum / Deploy / Set Me Up / Resolve / Configure / permission target / readonly_admin / Trash Can / cron / Quartz / outbox / handler / criteria / include·exclude patterns / SNAPSHOT / StartTLS / base DN / discovery / issuer / grace / dry-run / Replicate Now / No Replication Configured 等英文术语在两包中原样（AC1 术语一致性有 e2e 断言钉死，见 §4 腿④）。
- **语义 = zh 逐义对译**（非机直译）：如「暂无最近搜索」= `No recent searches yet`（7.161 活体逐字——B-3.14 册面冻结串）；「回收站」= `Trash Can`；档位 = `tier`；兜底 = `fallback`；预留位 = `reserved`。
- **片段键（拼接半句）逐一核对源码拼接语境后定值**：约 60 处以 `（`/`）`/`，`/`。`/「`/`」` 等开闭的片段键（en 值按 JSX 固定槽序适配，如 `（以`+username+`身份自铸）` → `(minted by` + username + `for this account)`；`快滤「`+q+`」无匹配行——清空快滤恢复` → `Quick filter “` + q + `” matches no rows — clear the quick filter to restore`）。
- **brace-in-key 5 条（T-463 §8 移交）**：admin 4 条（`{0} = 用户名…` ×3 + `可填 {baseUrl} 动态回跳…`）+ repositories 1 条（`放置位置：${user.home}/…`——`${user.home}` 含点不匹配插值正则，天然安全）。en 值逐字保留 `{0}`/`{baseUrl}` 模板字面（无 vars 调用原样渲染，LDAP 过滤器模板语义不变）。
- **填充管线（一次性，考古留痕）**：手工 authored zh→en JSON 映射 → 生成器机械校验（键集与 zh manifest 双向同构 / `{var}` 占位符键值同名集 / 值非空）→ 产出 `registerEn` 目录包。校验器逮住 3 处键面笔误（`GET /api/repositories` 误写 `/api/v1/`、句号尾差、`SSO…登录` 句点）——机械闸在人工逐义对译之上的价值实证。**仓库内正典 = en/*.ts 本身**（映射不入库，防双源漂移）；后续维护直接改 en/*.ts + manifests 同步（regen 工具已保值化，见 §6.2）。

## 2. 语言切换器（FR-149.1 收口）

**位置票内定案 = 侧栏脚注**（AppShell `app-nav-footer`，模式切换与版本行之间；应用/管理两侧栏同脚注常驻）。

**定案依据（按册——7.161 活体参照不可用〔容器损坏〕，锚册无 ux 终裁行，PRD FR-149.1 候选 =「侧栏脚或 Profile」）**：
1. 与模式切换（nav-mode-switch）/版本行（nav-about）同属「全局 + 常驻 + 个人偏好」控件层；
2. 全站生效一步可达——Profile 需一次导航，且 BinFlow Profile 页无编辑态表单可挂（Artifactory 7.x 语言在 Profile 编辑态；BinFlow 无对位形态，不为单一控件翻修整页）；
3. 两侧栏同脚注渲染，任一模式可达。候 ux 复核翻案时锚随载体迁址（锚名零改）。

- **形态**：MUI ToggleButtonGroup 二档（`中文` / `English`）+ 左侧 `语言` caption；当前 locale = 选中态（ToggleButton 原生 aria-pressed）；点击 = `setLocale`（localStorage 持久化 + 整页 reload——T-463 定案 3，模块级 t() 求值点按新 locale 重求值）。
- **语言名走母语名（endonym）**：`中文` 两态恒显（语言自称不随 locale 翻译，对位 Artifactory 语言下拉 native label）——**i18n-allow 行尾豁免首例**（`LOCALE_LABEL_ZH`，assert-i18n 豁免口 T-463 预建、本票首用并在册登记）；`English` 无 CJK 天然免闸。
- **锚册**：`nav-locale` / `nav-locale-zh` / `nav-locale-en` 3 名入册（console-ux §10.5 T-464 批块 + v1.46 版本行）；anchor-audit --ledger 四断言全绿（unregistered/broken 双零）。

## 3. locale 化格式（FR-149.4——FR-144.6 的 en 变体）

| 面 | zh（维持现行，零翻新） | en（本票新增） |
|---|---|---|
| 结果表 modified（formatStamp，B-3.15 锚） | `dd-MM-yy HH:mm:ss +ZZZZ` | `MMM d, yyyy h:mm:ss AM/PM +ZZZZ`（同款本地时区 + 显式偏移，12 小时制） |
| 审计时间列（formatAuditTime） | 当日 `HH:mm:ss` / 跨天 `YYYY-MM-DD HH:mm:ss` | 当日 `h:mm:ss AM/PM` / 跨天 `MMM d, yyyy h:mm:ss AM/PM` |
| 数字（formatCount / TokensPage mintedAt） | zh-CN（三位分组同形） | en-US（显式随 locale 取义，输出同形零回归） |

**票内范围裁剪（登记）**：ISO 形助手（lib/trash formatTrashTime / SubscriptionDrawer formatMillis / 监控刷新行）维持 locale 中立——mono 场景 ISO 为 Artifactory 亦用形态，不再扩面；候 ux 复核翻案另立。

## 4. 双语 e2e（FR-149.3——断言反转⑥收口）

**新 spec `web/e2e/m16/t464-i18n-bilingual.spec.ts`（5 legs，全绿）**：

| 腿 | 断言 |
|---|---|
| ① 切换器往返 | UI 点击 zh→en（reload + lang=en + aria-pressed 翻转 + Dashboard 文案）→ reload 持久 → 返 zh（文案翻转回 + en 文案零残留） |
| ② en 抽样 ≥6 页 | 树（跨仓树 aria-label）/ 建仓表单（包类型选择步 + General Settings 分节）/ 仓库详情（Overview Tab）/ 搜索空结果（No artifacts match + {q} 插值）/ 安全（Users h2）/ 监控（Overall status）/ 治理（Audit Log h2 + 审计时间列 en 形正则）——7 页面 |
| ③ en 日期格式 | 搜索结果 modified 整格正则 `^[A-Z][a-z]{2} \d{1,2}, \d{4} \d{1,2}:\d{2}:\d{2} (AM\|PM) [+-]\d{4}$` + 反断言 zh 24h 形不再现；zh 形由 t449 既有腿钉死（零翻新） |
| ④ 术语两包保真 | zh 态 `Set Me Up` / `部署 Deploy` 原样在场；en 态 `Set Me Up` / `⬆ Deploy` 同名同形 |
| ⑤ axe 双 locale | en 树页 + 服务状态页 / zh 服务状态页 serious·critical=0（zh 全路由双主题全量腿在 m8/a11y-sweep 不重复） |

**t463 spec 随刻意变更 1 处**：腿②「骨架回落 zh 文案」断言翻新为「en 值真渲染」（`Artifact Repository Console`）——该断言锁的是骨架期临时态，填充即翻新（t463 spec 头注预留了本翻新）。**zh 全量 spec 零翻新**（断言反转⑥：默认 locale 断言维持）。

### 4.1 zh 回归抽样（净实例 127.0.0.1:18140，admin/password，`--workers=2`）

| 批 | spec | 结果 |
|---|---|---|
| 1 | t463-i18n-skeleton + m8/shell + m8/smoke + auth-shell + t449-search-stack + t457-profile-help-about | **28 passed / 0 failed** |
| 2 | artifacts + t439-form-stepper + t453-route-forms + m8/users-groups + m8/governance-monitoring + m13/t366-webhooks | **33 passed / 1 skipped（既有条件）/ 0 failed** |
| 3 | t455-perm-five-columns + t462-cron-backups + m10/properties-matrix | **17 passed / 1 failed→复跑绿** |

批 3 唯一失败 = t462-cron-backups:344 axe 腿（gc+backup 双主题）——同 spec 单腿即时重跑 **1 passed**；T-463 §3.1 已登记同款共享实例负载 axe-flake 形态（本机当时并行控制构建），非文案/结构回归。

## 5. 体积报数（NFR-P72/P73；控制对照 = 44e05ed2 净 worktree 同法构建）

| 项 | T-463 态（对照） | T-464 态 | Δ |
|---|---|---|---|
| catalogs-*.js（en 懒载 chunk） | 97,597 B raw / **35,794 B gz** | 201,641 B raw / **72,977 B gz** | **+104,044 raw / +37,183 gz** |
| dist js+css gz 合计 | 464,672 B | 502,599 B | +37,927 B（≈ catalogs +744 B 应用面：切换器 + locale 化助手） |

- T-463 投影区间 **+25~45KB gz——落点 +37.2KB，如实达标**；zh 用户零请求零字节不变（懒载 chunk 仅 en 引导触发，t463 spec 腿②钉死）。
- W37 5MB 预算内（502.6KB ≪ 5MB）。

## 6. 特殊登记

1. **新键 `语言`**（console 域）——调用点 2581→**2583** / 键 1970→**1971**（切换器 label + aria-label 两调用点）；manifests/zh/console.json 与 en/console.ts 同步。
2. **regen-catalogs.mjs 保值化（防回流灭值）**：T-463 的目录包再生成工具初版「全量清空写 `''`」——对已填充的 en 包是灭值脚枪（本票实证：首次试跑保值 0，幸有备份）。已改为**清场前快照 + TS 编译器解析逐键保值**（新键才落空串）+ 发射器对齐 JSON.stringify 双引号形态；当前树再生成 = 10 域文件逐字节恒等（catalogs.ts 仅头注更新为填充态）。
3. **verify-extraction.mjs 预期内分叉**：AppShell BEFORE 103 vs AFTER 105（`仅后: [语言, 语言]`）——零语义变化对账工具的基线是 T-463 提交，本票 +1 键 ×2 调用点为刻意新增（工具非 CI 门）。
4. **anchor-audit STOP +2**：`i18n-allow` / `t464-i18n-bilingual`（v1.46 行文假阳性——豁免口机制名与本票 spec 文件名段，同款先例 v1.37~v1.45 逐票递增）。
5. **共租机纪律**：净窗实例 18140（跑前无他测在跑）；并行 lane 的 `ci/protocol-matrix.sh` / `reports/agents/T-478.md` 在工作树内属 T-478/T-479 lane，本票零触碰。
6. 环境取证：`strings /tmp/t464-binflow-server | grep -c nav-locale` = 2 + `Artifact Repository Console` 在 embed 内（go:embed 真 embed 新 console）。

## 7. 自测证据（四门 + e2e）

| 命令 | 结果 |
|---|---|
| `npx tsc --noEmit` | **EXIT 0**（裸跑取码；tsconfig 含 e2e） |
| `npm run lint`（eslint . + assert-i18n） | **EXIT 0**——0 errors / 36 warnings（全为 T-432 ratchet 存量，零新增）；assert-i18n OK（零硬编码中文；2583 调用点 / 1971 键，en 包与 zh 清单同构） |
| `npm run build`（assert-tokens + assert-i18n + vite build） | **EXIT 0**（1m27s；relink/wire-brand 正常） |
| `node scripts/anchor-audit.mjs --ledger` | **PASS**（A1 unregistered=0 / A2 broken=0 / A3 / A4） |
| `BASE=http://127.0.0.1:18140 npx playwright test e2e/m16/t464-i18n-bilingual.spec.ts --workers=2` | **5 passed**（1.3m；两处腿修后全绿——首两轮失败腿为包类型选择步未驱动与 addInitScript 重放覆盖手动 locale，均 spec 侧问题非产物问题） |
| zh 回归抽样（§4.1 三批 15 spec） | **78 passed / 1 skipped / 1 flake（复跑绿）** |

## 8. 契约漂移

无（纯 FE 票，服务端 diff=0；二进制仅为 e2e 取证重建——embed 物即本票产物，internal/console/dist 为 gitignored 本地态）。

## 9. 遗留

- 切换器位置候 ux 复核（票内定案依据见 §2；翻案时锚随载体迁址零改名）。
- ISO 形时间助手的 locale 化扩面候 ux 裁（§3 裁剪登记）。
- AC1 全量翻转（≥6 页面已抽 7 页）+ axe 全路由 en 态的完全体归 M16 收口 QA 双 locale 抽样腿（PRD 终验条款），本票抽样面即其底稿。
