Ticket:       L000-A — P0 unknown U-STG-01 BinaryProvider 存储链取证（ARTIFACTORY FULL REIMPLEMENTATION PROGRAM, LOOP 000）
Role:         reverse-engineer（本票实例域：存储链 / binary provider）
Area:         docs/reverse/storage/binary-provider-chain.md
Input:        派发缺口 = domain-map 发现 BinaryProvider 实现类不在反编译 Java 树，filestore/S3 行为只能活体核对；参照公开规范 = docs.jfrog.com S3/cloud storage 配置页
Changes:      新建 binary-provider-chain.md（实现库定位 §2 → 链组合矩阵 §3 → 行为规格 §4 六小节 → 既有规格对账 §5 → 官方印证 §6 → 判定 §7 → 待验证 §8）；evidence/ 五份原始证据；未改任何既有规格（对账冲突以「修正」标注列于 §5，待 conductor 裁定后由本角色后续票回改旧稿）
Files:        新建 docs/reverse/storage/binary-provider-chain.md；docs/reverse/storage/evidence/{binarystore.xml.live.7.161.20.txt, binarystore-default.xml.binary-store-{core,filestore,client}-4.372.5.txt, binary-store-class-inventory.txt(288 类), e4-experiments-7.161.20.md}
Tests:        E4 活体 6 组：①基线上传→blob 落位 filestore/12/1274dc…（sha1 寻址实证）②去重三路径（全量/重传/checksum-deploy 零字节）=1 blob ③删除→204+回收站副本+引用阻断 ④prune/GC 双轮（202/200；256 目录遍历；processed 3/cleaned 0——短窗不回收实证）⑤30MB 慢速上传飞行中抓到 _pre/dbRecord*.bin 命名，掐断后 <8s 清空零残留；成功路径 _pre→b5/b5f00b… 精确落位 ⑥checksum 不匹配→409+ChecksumsInfo 明细。DB 只读 12 查（binaries/nodes/node_props/qrtz/binary_blobs/storage_multipart_uploads/node_events 族）。实验仓 audit-probe-stg01 已删（200/204），回收站前缀已清
Commands:     docker exec artifactory {cat binarystore.xml; find filestore; unzip -l/-p binary-store-*-4.372.5.jar META-INF/binarystore-default.xml ×3; unzip 常量池 strings 提取 BinaryStoreConstValues}; grep ConstantValues.java（A 树 20 命中：gc.* ×14、binary.provider.* ×4、cloud.* ×3、multipart.* ×9、temp.folder.quota ×1）; find reverse-src/artifactory -path "*jfrog/storage/binstore*" → 0 命中（A 树缺实现类确证）; psql -d artifactorydb（\dt 165 表、binaries 引用计数 LEFT JOIN、node_events 分组）; curl :8082 REST ×14（PUT/DELETE/checksum-search/prune/gc/status）
Outputs:      binary-provider-chain.md：链组合矩阵 24 模板（core 4 + enterprise 15 + cluster 5）+ provider 类型词表 20 + 行为条目 35（A1-A6/D1-D4/G1-G8/C1-C6/H1-H3/配置 2）+ 对账 10 条
Compatibility: 置信度分布：链矩阵 24 条 6 高/18 中；行为条目 29 条 18 高/8 中/3 低（A1-A6、D1-D4、G1-G8、C1-C6、H1-H3、配置 2 条）。与官方规范差异/补充：prune/status 响应结构、409 mismatch 码、checksum-deploy 必须用请求头、sharding-cluster 出厂参数族、full-db/double-shards 等未文档化模板（均标「补充官方规范」）；修正旧规格 4 处（cached-fs→cache-fs、S3→s3-storage-v3、mismatch 400→409、tmp 暂存先行→_pre 直写）
Security:     鉴权面仅一条：checksum-deploy 头缺失 400 的错误体形态（A5）；回收站 403 守卫未复测（沿用 storage-layout 旧证）；凭据仅命令行，未落盘
Performance:  时序相关：prune 全 256 分片目录遍历（本实例 2.6-4.2s）；gc.intervalSecs=86400、trashBinariesCleanup.intervalSecs=900、gcNumberOfWorkersThreads=3（MSSQL 强制 1）、redirect 阈值 200KB、cache-fs 同步线程池常量族
Risks:        ①实例共享：E4 窗口有并行 docker 拉取写入（blob 5→9 增量已用 nodes 表验明属 audit-probe-docker-remote-cache，非实验污染）②G5「短窗不回收」的原因是推断候选（事件管道/间隔门槛），非定论——规格中标 UNKNOWN ③C 类安装包（7.161.16 独立归档）本机未定位，以 B 镜像内出厂工件替代，版本差异 7.161.16 vs 7.161.20 未经独立对拍 ④二进制常量池只提取到常量名未提取默认值（C6 标中）
Blockers:     无
Next:         ①compatibility-engineer 契约化：409 mismatch、prune/status JSON、checksum-deploy 头语义、_pre dbRecord 命名 ②差分票：GC 24h 观测窗（回收资格）+ MinIO 云链抓包（redirect/预签名） ③逆向专项：binary-store-core jar 的 GCProvider 批次源与 PreTempDirCleanupRunnable 调度（需 conductor 批准对 jar 做 bytecode 级取证——本票仅取资源与常量池，未越界）④旧稿回改票：storage-layout.md/s3-storage-layout.md 四处修正落地

## 断点快照（本轮已完成，供后续续跑）
已完成: 取证面 1/2/3/4 全部；产出三件套落盘
未完成: 无（残余转 §8 待验证清单 8 条）
断点位置: 无悬挂调用链
恢复入口: 后续票从 binary-provider-chain.md §8 待验证清单第 1 条（GC 资格窗口 24h 观测）起
