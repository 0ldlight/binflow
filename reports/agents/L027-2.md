Ticket:       L027-2 — D08 release-bundle 契约固化票（contracts/release-bundle.yaml 域首文件 + matrix 六行填链）
Role:         compatibility-engineer（release-bundle 域实例）
Area:         docs/compatibility/{contracts,matrix.yaml}
Input:        docs/reverse/release-bundle.md（L026-1 活体规格 §1/§3/§10 + §8 待验证清单）；reports/compatibility/l026a-wire/ 64 件（p01–p63 + p48b，A=Artifactory pro 7.161.15 :8082，license 含 release-bundle 权益）；L026-6 实现面（internal/httpapi release_bundle*.go/bundle.go，commits 54b1d46e→695d8989）；matrix.yaml D08-R01..R06（contract_ref 全空）；reports/agents/L026-1.md Next 段（错误臂/空态可直接固化 IMPLEMENTED 建议）
Changes:
- 新建 contracts/release-bundle.yaml（域首文件，schema compat-contract/1 体例对照 buildinfo.yaml/d02-config.yaml）：7 条目全 IMPLEMENTED/high——
  1. release-bundle/assemble（R01）：校验链三臂 400 逐字（missing-AQL/not-items/语法错）+ 命中五键行形（urn/sha256/properties 数组值/size/pkg_type，无属性行 properties:{} 键不省略）+ 空命中 results[] + license 门臂（反编译单源标注不挡）；
  2. release-bundle/transaction-errors（R02）：p38–p43 六臂（JWS 400 双端点同形/裸 Jackson 400/事务路径原文回显 ×3）+ status 管理门 p54；成功形 201 不立断言（U2）；
  3. release-bundle/store（R03）：七臂有序链（管理门→projectKey 404 透传→Jackson 400→null 500 泄漏→INVALID_RB_REPO 双键 400→缺省仓解析→JWS 500 无前缀同因异码）+ OPTIONS p11 + release-bundles 系统仓自动创建副作用（p48/p48b：local/releasebundles/simple-default、列表隐藏单查可见）；三态 202/200/409 未实证不立 wire 断言（U1）；
  4. release-bundle/query-family（R04）：空态两形（bundles map 空对象/versions 恒 200 空数组非 404）+ 404 三消息族（"Bundle not found"/冒号拼接/HEAD 无 body/"Release bundle not found"）+ type 缺省 TARGET/HEAD 恒 SOURCE 不对称；有记录成功形 UNKNOWN（U9）；
  5. release-bundle/config-and-artifacts（R05）：720 出厂缺省 p06 + artifacts 404 p08 + fat_manifest 400 逐字 p47 + admin 门 p53/p55；PUT 202 臂反编译未实证不立逐字断言；
  6. release-bundle/v2-read-face（R05 伴随面）：names/received/records 空态三信封（分页三键 1000/0）+ received 单查 GET 恒 400 名字校验 quirk + records/statuses/received-DELETE 三族 404（release-bundles-v2[.evd]/-jfds 泄露）+ audit 七必填 400/405；下划线单数前缀定案；
  7. release-bundle/any-distribution-face（R06）：ANY DISTRIBUTION 非 REST 实体（名单无行 p61/单查 404 p62）+ distribution rclass 拒建 400 逐字 p63 + 权限矩阵横切面（p50–p58 user 放行/admin 门 403 族/匿名 401 族）；回退链内部语义非 REST 面不立断言。
- 文件头三段登记：pending-differential U1–U9（wire 未覆盖分支——store 三态/事务成功形/license 门臂/jfds 语义/有记录成功形等，UNKNOWN 不虚构断言）+ 候裁面 C1–C8（401 信封族/JSON 渲染风格/p33 token/source-DELETE 500/config 内存态/良构 JWS/native 面等——L027-1 差分输入只登记不预裁）+ matrix 行描述勘误候选 M1（R05 capability 未含 v2 伴随面，归 conductor）。
- matrix.yaml D08-R01..R06 六行 contract_ref 填链（条目号锚 contracts/release-bundle.yaml#<entry-id>；R05 note 内补 v2-read-face 伴随指针）；六行 note 追加 L027-2 行级 changelog；头部注释块新增 L027-2 落账段；summary.contract_ref_non_null 31→37。state/confidence/last_difftest/golden_ref 六行全部不动（absent/high/空维持——翻绿归 L027-1 差分+落账）。
Files:
- 新建 docs/compatibility/contracts/release-bundle.yaml
- 修改 docs/compatibility/matrix.yaml（六行 + 头部 changelog 段 + contract_ref_non_null 计数）
- 新建 reports/agents/L027-2.md（本日志）
Tests:
- probe 复跑门：不适用——本票零新 probe（evidence 全部引用 L026-1 既有 wire 64 件，逐件存在性校验见 Commands ③；复跑归 L027-1 差分腿）
Commands:
- ① YAML 门（本机无 pyyaml 用 ruby）：ruby /tmp/ymlcheck.rb contracts/release-bundle.yaml → "OK … entries=7"（7 条目 status/confidence 逐条打出：全 IMPLEMENTED/high）；ruby /tmp/mcheck.rb matrix.yaml → "matrix OK rows=200"（四态 88/17/63/20+superset 12、contract_ref non-null=37、last_difftest non-null=44、D08 七行逐行打出）
- ② 引用完整性门：ruby 锚点解析脚本 → D08-R01..R06 六行 cref 全 RESOLVES（条目 id 与契约文件逐一对上）；p 引用存在性 shell 扫描 → 契约内全部 p 编号在 l026a-wire/ 有件（唯 p48b 为 .json 非本扫描的 .txt 形态，单独校验 RESOLVES）
- ③ 契约面核对门（IMPLEMENTED surface 在 internal/httpapi 路由表 grep）：router.go L1532 `case rest == "release/bundle" && r.Method == http.MethodPost`、L1553/1555 `release/store` PUT+OPTIONS、L1564–1573 bundles 查询族/config/fat_manifest、L848–926 `v2/release_bundle/names|received|records|statuses` + L919 `v2/audit` POST——七条目 surface 全部路由在案
- ④ 矩阵口径门：contract_ref 31→37 前后对照（changelog 段留痕）；行数 200 不变零新行（195 冻结行集不增删）；last_difftest 44 不变（差分未跑不回填）
Outputs:
- 契约 7 条目：release-bundle/{assemble,transaction-errors,store,query-family,config-and-artifacts,v2-read-face,any-distribution-face}
- matrix 行更新 6（D08-R01..R06 contract_ref 填链 + 行级 note changelog）；known-divergence 新增 0（裁定不在本票——候裁面 C1–C8 登记于契约文件头，L027-1 差分后归档）
- probes 0（复用 l026a-wire 64 件既有证据）；fixtures/golden 0（差分未跑不采金样）
Compatibility:
- 状态机变动：新域 7 条目全 IMPLEMENTED（0 翻态——六矩阵行 absent 不动；VERIFIED 封顶待 L027-1 差分）
- 置信度分布：high 7 / medium 0 / low 0（wire 单端锚+实现单测钉；反编译单源臂〔license 门/签名 400/PUT 202〕一律行内标注来源且不立逐字断言，不抬升条目置信）
- 断言溯源：全部 expect/error_behavior 锚 l026a-wire p 件；spec §10 中置信臂（includeMetaData 元数据路径过滤/jfds 语义）按规格置信标注待验证，不升格
Security:
- 条目 authentication/权限位断言汇总：assemble=user 达校验臂（p51）；查询族=user 200（p50）；store/config/async-status/fat_manifest=admin 门 403 errors 信封 Forbidden（p52–p55）；匿名全域 401（p56–p58，B 平台 401 信封差=C1 候裁）；ANY DISTRIBUTION 不得落成可 REST 读取 permission target（§10.7 结论，条目 7 立约束）；distribution rclass REST 拒建 400（p63——防伪装配面）
Performance:
- 无 timing/p95 面断言（l026a-wire 无计时采集；纯 REST 元数据面无性能锚——与 L026-6 Performance 结论一致）
Risks:
- 单端证据：7 条目全部只有 A 侧 wire + B 侧单测钉，无活体差分——IMPLEMENTED 现值与 A 的残差（C1 401 信封/C2 渲染/C3 token/C7 v2 全局默认等）待 L027-1 裁定，存在翻 DIVERGENT 可能
- U 清单九项 wire 未覆盖（store 三态/事务成功形/有记录成功形/license 门臂等）——分布反馈到条目 expect 的「不立断言」注记，Distribution 联调环境到位前不可闭
- R05 行 capability 未含 v2 伴随面（M1 勘误候选）——contract_ref 已双锚，行文本勘误归 conductor
Blockers: 无
Next:
- 转 L027-1（differential-qa-engineer）：七条目差分输入（contract_ref 六行已可机读；候裁面 C1–C8 为差分重点清单；normalize#release-bundle 域提案随差分报告入册）；差分后 IMPLEMENTED→VERIFIED/DIVERGENT 翻态 + last_difftest 回填 + known-divergence 归档
- 转 conductor：M1 行描述勘误（R05 capability 补 v2 伴随面）；U1/U2 需 Distribution 联调环境（Q2 出口②前置）
- 跨域转派候选（非本票）：C3 internal/search token 提取、C4 metadata DeleteBundle 缝、C5 config 旋钮持久化
