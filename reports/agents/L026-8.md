# L026-8 — fern openapi spec 面英文化 + 产品化措辞

Ticket:        L026-8 对外文档国际惯例对齐第一波：fern openapi spec 面英文化+产品化措辞（生成器内中文字段英文化 + 重新生成 fern/openapi/binflow.json）
Role:          tech-writer
Area:          tools/openapi-spec/build_spec.py + d_*.py + helpers.py（生成器）；fern/openapi/binflow.json（产物）。未动 fern/docs.yml / fern.config.json（无中文 description；docs.yml 的中文是中文文档站导航页名，属站点本身语言，不在本票面）与 docs/user/
Input:         派发输入（背景=2026-09-17 用户长期规则：对外文档必须符合国际惯例、贴近产品使用、不含研发过程内容；先例 commit 027098d1）。唯一语义源=生成器现有中文字串；未参考任何新证据、未猜任何 API 行为
Changes:       7 个生成器文件全部进 spec 的中文串译为英文（info 块、servers、tag descriptions、158 个 operation 的 summary/description、参数/响应/schema descriptions、2 个可复用错误响应）；重新生成 binflow.json。代码结构、路径、键名、枚举、默认值、示例零改动
Files:         tools/openapi-spec/build_spec.py、tools/openapi-spec/helpers.py、tools/openapi-spec/d_content.py、tools/openapi-spec/d_protocol.py、tools/openapi-spec/d_search.py、tools/openapi-spec/d_security.py、tools/openapi-spec/d_system.py、fern/openapi/binflow.json（再生）
Tests:         ① python3 -m py_compile 全部通过；② build_spec.py 自带 self-check clean（无迭代标记、$ref 全解析、operationId 无重复）；③ 生成 JSON 全文 CJK 断言=0 命中；④ 对 HEAD 深度结构 diff：KEYS/LEN/VALUE 改动=0，PROSE=979，示例占位符 1 处（"<口令>"→"<password>"），另 1 处 KEYS=/paths//v2/token/get +security（见 Risks：HEAD 产物陈旧，非本票引入）；⑤ 112 paths / 158 operations / 20 tags / 54 schemas 与基线一致；行数 9920→9921（+1=该 security 键行）；operationIds 顺序与值逐一同；⑥ redocly lint（npx @redocly/cli@latest + tools/openapi-spec/redocly.yaml）：valid，70 warnings——HEAD 同一配置同为 70 warnings，零新增
Commands:      python3 -m py_compile tools/openapi-spec/*.py；python3 tools/openapi-spec/build_spec.py（输出：binflow.json: 112 path items, 158 operations, 20 tags, 54 schemas, 324286 bytes / self-check: clean）；python3 内联断言脚本（CJK 扫描 + git show HEAD:fern/openapi/binflow.json 结构对比，见 Tests）；git show HEAD:fern/openapi/binflow.json > /tmp/binflow-head.json；npx -y @redocly/cli@latest lint fern/openapi/binflow.json --config tools/openapi-spec/redocly.yaml；同命令跑 /tmp/binflow-head.json 对照
Outputs:       fern/openapi/binflow.json（对外 API tab 的 spec，英文化后）；tools/openapi-spec/*.py（生成器英文化，后续再生保持英文）
Compatibility: 口径零变更——本票纯文案面：路径、状态码、错误文案（英文逐字串）、示例、枚举、默认值经结构 diff 证明逐字节不变，matrix.yaml 不受影响、无需联动。描述中的兼容面表述（四面结构/认证三态/错误三格式/有意 404 清单）逐点翻译未增义；唯一措辞变化=「已保留 / 未实现路径（有意 404）」→「Intentionally unrouted paths (not supported, 404)」，七条理由（用 /api/v1/permissions、备份恢复仅 CLI、空间回收走 GC、docker 不走 /binflow 前缀、HA 多证不采纳、prop 单数官方拼写、flat copy/move）全数保真
Security:      无敏感默认值/内网地址引入；示例均为原有占位符（<64hex>、s3cr3t!、<password>）
Performance:   不适用
Risks:         ①【非本票引入，建议 conductor 知悉】HEAD 提交的 binflow.json 相对自身生成器源是陈旧的：源码（自 04851ac6 起）对 GET /v2/token 带 security=ANON，但 HEAD json 无该键——本次再生产物按生成器源补上 security: []（docker token 端点免预认证、凭据经 query 传递的语义，与源码意图一致），故行数 +1；如需回退旧形须改生成器而非手工改 json。② redocly 本地无安装，经 npx 临时拉包完成（与 CI 同规则集）；fern 构建链未本地跑（发布由 conductor 推送后走 .github/workflows/fern-docs.yml），spec 被 Fern 直接消费未见异常风险，但未实测渲染——「待验证」项
Blockers:      无
Next:          无后续篇目（纯 spec 面）。可选：① docs/user/api-reference.md（契约页）仍为中文，若后续做文档站国际惯例第二波可对齐术语表；② 生成器 Python 头部注释保留中文（不进 spec，零风险），如需彻底英文化另行小票

## 中→英对照表（语义复核用）

完整串级差异见 `git diff tools/openapi-spec/ fern/openapi/binflow.json`；此处覆盖高风险面（info 块全文、tags、术语基线、语义密集段落）。逐点对译、不增义。

### 术语基线（全文一致使用）

| 中文原文 | 英文措辞 | 备注 |
|---|---|---|
| 官方拼写：`GET /binflow/...` | Canonical path: `GET /binflow/...` | 每端点首句固定式 |
| errors[] 信封 | errors[] envelope | |
| 纯文本（逐字） | plain text (verbatim) | |
| 免认证 | no authentication | |
| 仓 key / 节点路径 | repository key / node path | |
| 恒渲染/恒回显 | always rendered / always echoed | |
| 缺省 | defaults to | |
| 点名 | naming / by name | |
| 干跑 | dry run | |
| 静默缺失 | silently omitted | |
| 值信封/判定体 | envelope / verdict body | |
| 档位（license） | (license) tier | |
| 旋钮 | knobs | |
| 台账 | ledger | |
| 骑同一面 | rides the same face | trash 浏览同 storage 面 |
| 404-empty 族 | the 404-empty family | |

### info 块

| 中文 | 英文 |
|---|---|
| BinFlow 制品仓库 REST API——Artifactory 兼容面 + /api/v1 自有面 + 协议接入面 | BinFlow artifact repository REST API — the Artifactory-compatible surface, the native /api/v1 surface, and protocol client surfaces |
| BinFlow 的 API 分为四个面 | The BinFlow API spans four surfaces: |
| 1. 制品的容路径（无 /api 前缀）…各协议客户端走这里 | 1. **Artifact content paths** (no `/api` prefix) … every protocol client goes through here |
| 2. 管理面 API（/binflow/api/*）：仓库 CRUD、用户/组/权限、审计、GC、Token、搜索、系统信息 | 2. **Management API** (`/binflow/api/*`): repository CRUD, users/groups/permissions, audit, GC, tokens, search, system info |
| 3. Docker Registry 根级平面…独立路由，不走 /binflow 前缀（本 spec 中该组路径以独立的根级 server 表达） | 3. **Docker Registry root-level plane** (`/v2/*`) — separately routed, not under the `/binflow` prefix (in this spec those paths carry their own root-level server) |
| 4. Webhook 事件面…订阅 CRUD/试发/排障 | 4. **Webhook event surface** … subscription CRUD, test sends, troubleshooting |
| Basic…凭据始终在请求中且正确，不存在「先访问后挑战」，一次性认证失败直接 401 | Basic … credentials must always be present and correct in the request; there is no challenge-then-retry, a failed authentication is an immediate 401 |
| Bearer Token（高 QPS/CI 推荐——校验不触发 argon2 哈希计算） | Bearer tokens (recommended for high QPS/CI — validation does not run the argon2 hash) |
| 控制台另有 binflow_session 会话 cookie…随同源请求自动发送；会话认证的非 GET/HEAD 写请求携带非同源 Origin 头 → 403 | The console additionally uses a `binflow_session` cookie … sent automatically with same-origin requests; non-GET/HEAD writes authenticated by the session that carry a cross-origin `Origin` header → 403 |
| 错误响应三种格式：… | **Three error formats**: the errors[] envelope (the primary format …); plain-text errors (user/group management domains); OAuth-style errors (token endpoint and the docker domain …) |
| 已保留 / 未实现路径（有意 404）：/api/v2/**（…用 /api/v1/permissions；钥对仓关联面…例外）、export/import（备份恢复仅 CLI）、prune/**（空间回收走 GC）、/binflow/v2/**（docker 端点不走 /binflow 前缀）、licenses 复数（HA 多证语义不采纳）、搜索族 props\|users\|artifactory\|badge（prop 是官方单数拼写，复数 props 404）、/api/flat/copy\|move | **Intentionally unrouted paths (not supported, 404)**: … `/api/v2/**` (the Artifactory v2 permissions API — use `/api/v1/permissions`; the repository key-pair association face `/api/v2/repositories/{key}/keyPairs` is the one exception), … (backup/restore is CLI-only), … (space reclamation goes through GC), … (docker endpoints do not live under the `/binflow` prefix), … (plural — the HA multi-license semantics are not adopted), the search family … (`prop` is the official singular spelling; the plural `props` is 404), and `/api/flat/copy|move` |
| servers：BinFlow 默认上下文路径（binflow.yaml 的 http.path，缺省 /binflow）——管理面、内容路径与事件面共用此前缀 | BinFlow default context path (http.path in binflow.yaml, default /binflow) — shared by the management API, content paths and the event surface |
| basicAuth：…所有 /binflow/api/* 基础路由均支持；口令哈希为 argon2id（memory-hard），高并发场景请用 Bearer Token | … Supported on every /binflow/api/* base route; password hashes are argon2id (memory-hard) — prefer Bearer tokens under high concurrency. |
| bearerToken：…校验不触发 argon2 哈希计算，性能远优于 Basic；docker token 流同表——管理面吊销对 docker token 即时生效 | … Validation does not run the argon2 hash and vastly outperforms Basic; the docker token flow uses the same table — revocations on the management API take effect on docker tokens immediately. |

### tags（20 条）

| tag | 中文 | 英文 |
|---|---|---|
| artifacts | 通用制品域——内容路径上传/下载/删除与 storage 元数据面… | Artifacts — content-path upload/download/delete plus the storage metadata face (container paths without the /api prefix + the /api/storage family) |
| artifact-operations | 制品操作族——copy/move、归档下载与解包、trash can（pro 槽 repo-operations / trashcan） | Artifact operations — copy/move, archive download and extraction, trash can (pro slots repo-operations / trashcan) |
| docker | Docker Registry（根级 /v2 平面——独立路由…401 挑战为 Bearer realm=/v2/token，随实例匿名开关不变） | Docker Registry (root-level /v2 plane — separately routed, not under the /binflow prefix; the 401 challenge is Bearer realm=/v2/token, unchanged by the instance's anonymous switch) |
| npm / pypi | npm 域…packument/发布/登录/探活；PyPI 域…PEP 503/629 simple 索引与 twine 上传 | npm domain (… packument/publish/login/ping); PyPI domain (… PEP 503/629 simple index and twine uploads) |
| search | 搜索域——…（结果按调用者权限过滤） | Search — … (results filtered by the caller's permissions) |
| repositories / reindex / webhooks | 仓库管理；reindex 管理族；webhook 订阅（…注意不在 /binflow/api 下；整族 pro 槽 webhook） | Repository management; Reindex family — conan/helm/deb/yum index rebuilds; Webhook subscriptions (… note it is not under /binflow/api; the whole family sits behind the webhook pro slot) |
| users | 用户管理…错误族为纯文本响应体 | User management … — this family's errors are plain-text response bodies |
| groups / tokens / permissions / keypairs / auth-config | 组管理；Access Token——OAuth 2.0 错误规范；Permission Target；GPG keypair 族…BinFlow 原生生成/仓关联面；认证配置面 | Group management; Access tokens — OAuth 2.0 error conventions; Permission targets; GPG key pairs (… plus BinFlow-native generation and repository association); Authentication configuration |
| system / maintenance / uploads / replication / session | 系统端点…；维护面——cron 三消费面（…台账）；分片上传（MPU）——数据端点仅纯 S3 后端…501 纯文本非 404；push 复制…全局封锁；控制台会话 | System — …; Maintenance — the three cron consumers (… ledgers); Multipart uploads (MPU) — data endpoints require a pure S3 backend; filestore/dual-write instances return 501 plain text, not 404; Push replication — … the global block; Console session |

### 语义密集段（抽核最险处）

| 位置 | 中文要点 | 英文落笔 |
|---|---|---|
| repoPut（建仓 create-only） | 只建仓——已存在 key 一律 400…零副作用（存量配置原样不动）；更新拼写只有 POST；rclass × packageType 组合门已全量退役，唯一剩余门是 license 档位 | **Create only** — an existing key always gets **400** … with **zero side effects** (the stored configuration is untouched); The only update spelling is POST …; **the rclass × packageType combination gates are fully retired**; the only remaining gate on creation is the license tier |
| repoPost（merge 三列） | 省略=保留存量；null/空串=清空（数组空值保留、对象 {} 整族复位）；显式值=覆盖（0 也是显式值，不作缺省回落） | fields **omitted** … = keep the stored value; **`null` / empty string** = clear that field (array empty values are kept, an object `{}` resets the whole family); **explicit values** = overwrite (`0` is also an explicit value, no default fallback) |
| userDelete（四道护栏） | 检查序固定：目标 404→内置 admin 400→最后 admin 400→自删 400；重复删除=确定性 404（有意非幂等）——把第二次 404 理解为「对象已被删」 | a fixed check order: Target missing → **404** …; Built-in admin → 400 …; Last admin → 400 …; Self-deletion → 400 …; **Repeated deletion = deterministic 404 (intentionally not idempotent)** — callers should read the second 404 as "already deleted" and not retry |
| npmLogin | 豁免写认证门…201 幂等再铸，不报 409；仅 packageType=npm 仓适用（generic 同路径仍 401——类型钉死防匿名写入） | **exempt from the write-authentication gate** … (**201 re-mints idempotently**, no 409); Applies to `packageType=npm` repositories only (the same path on a generic repository still 401s — the type is pinned to prevent anonymous writes) |
| qrl 三动词 | disabled 时三动词一律 400 纯文本（带显式 mode 的 POST 除外）；重启回出厂；限流只延迟不拒绝——429/408 门与本配置正交 | While the limiter is **disabled, all three verbs return 400 plain text** … (except a POST carrying an explicit mode …); a restart returns to factory defaults …; Rate limiting only delays, never rejects — the 429/408 gates are orthogonal to this configuration |
| block/unblock | 非 "true" 串=本次不动；不拦配置面；封锁生效面=事件轨+认领轨+手动触发+拉侧回源 | **any string other than `"true"` = no-op for this call**; **does not gate the configuration face**; The block covers: the event track … + the claim track … + manual triggers (run 409) + pull-side origin fetches (miss 404 naming the block) |
| sessionCreate | 登录入口对失效 cookie 豁免（cookie-tossing 免疫）；活跃不能续期（滑动续期被绝对 TTL 封顶吞没） | The login entry is exempt from invalid-cookie rejection (immune to cookie tossing); **activity does not extend it** (sliding renewal is swallowed by the absolute TTL cap) |
| uploadsComplete | 节点由客户端落，服务端只组装+登记 blob | the client lands the node; the server only assembles + registers the blob |
| replicationCreate | target_url 填目标实例裸 origin，不带 /binflow 后缀（带了会拼出 /binflow/binflow/…） | **target_url takes the target instance's bare origin** …, **without** the `/binflow` suffix (including it produces `/binflow/binflow/…`) |
| webhookCreate（投递） | HMAC-SHA256 hex 于 X-JFrog-Event-Auth；重试 5 次首试计入/固定 10s/单次 30s/仅发送失败或 ≥500（4xx 一步终态）；死信落审计 | HMAC-SHA256 hex in `X-JFrog-Event-Auth`; retries **5 including the first attempt / fixed 10s spacing / 30s per-attempt timeout / only on send failure or ≥500** (4xx is a one-step terminal state); dead letters are audited … |

### 研发过程内容清理（依 2026-09-17 用户长期规则 + 027098d1 先例，语义保留、过程指涉删除）

| 位置 | 原文 | 处理 |
|---|---|---|
| d_protocol V2 server | （ADR-0010 根级例外——不在 /binflow 前缀下） | 删「ADR-0010」内部决策号，保留 "not under the /binflow prefix" |
| d_system qrlConfigGet | 未写过回默认桶（K63 查询门的常量快照） | 删「K63」内部编号 → "a constant snapshot of the query gate" |
| d_protocol npmPackumentGet/npmUnpublish | 契约页 NE 表的…拼写与适配器文法不符——已登记漂移，本 spec 采路由验证文法 | 删契约页/漂移登记过程句，保留用户事实（pkg 与 @scope%2Fpkg 两拼写等价；rev 为占位 opaque） |
| d_system auditQuery | admin only 见契约页门注 | → "some faces are admin-only"（删契约页指涉） |

其余（Python 文件头部 Source 注释等）不进 spec，保留中文未动。
