Ticket:        L008-1a [P0] update-merge ADR 正式化（ADR-0050 收编候选稿）
Role:          architect
Area:          DECISIONS.md（ADR-0050 追加）/ docs/design/repo-update-merge.md（状态行 + §9 实现票任务书）
Input:         conductor L008-1a 派发指令 + 候选稿 docs/design/repo-update-merge.md + 十二臂证据 reports/compatibility/L007-3-update-merge-evidence.md + L006-a-b-diff.md §1.4 + DECISIONS.md ADR-0003/0012/0026/0044/0049 + docs/compatibility/matrix.yaml（D02-R03/R04 行与 LOOP006/007 收官注记）+ docs/compatibility/known-divergence.yaml（rest/repo-config-update-merge-semantics）+ internal/repo/config.go、service.go、internal/httpapi/repositories.go、keypair.go、internal/httpapi/envelope.go、web/src/lib/repos.ts（as-built 只读核对）
Changes:       ① ADR-0050 追加（案 A 定谳）：POST=merge 三列矩阵（省略=保留/null=清空/空值三列互异，数组无清空通道、对象 {}=整族复位）+ PUT=create-only（PUT-on-existing 400，BinFlow 更新系超集行为回撤）+ 显式 0=存 0 双面收口（fetch 侧 0=unset 回落链维持）+ credential 单侧清 + 范围=remote 臂+description 席位先行（local/virtual 另票）+ breaking note 单窗口 + RFC 9110 张力段（对齐事实生态，ADR-0003 优先）+ 迁移升级窗口（无数据迁移、仅行为面）。② 候选稿状态行 Proposed→Accepted（ADR-0050 收编）。③ 候选稿尾部新增 §9 实现票任务书（9.1 文件级改动 5 项/9.2 单元验收 7 条/9.3 差分臂 13 硬断言+4 探针/9.4 边界与不做）——parseRemoteConfig 基线注入 + 指针席位零新增路线（username/password 经 raw-map 键在场判缺）。④ conductor 小件追加：**ADR-0012 勘误四入册**（blob 豁免检索窗——再验证臂作用面收窄至可变引用面 manifest-by-tag；digest blob 已验证 standing 副本本地应答、零上游、行不重写不滑窗；四臂铁证 + L008-2 双端复验 0-vs-0 + 行不重写实证；manifest 控制臂窗语义不动；负缓存早退/脏行回落/X-Binflow-Cache=HIT 边界三款；known-divergence D3 条目 authority 链增 adr 指针）。注：conductor 所指 L008-2.md「勘误四建议文本」节未落盘（其 Next ② 载明建议随票回报未自写），文本由本票依 conductor 语义给定 + L008-2 Outputs + D3 台账 resolved 段起草入册，语义零偏移；并核 ADR-0050 交叉对账预登记触发条件 ①~⑤——勘误四不触碰 ①②③⑤，触发条件 ④（fetch 侧存量 0 生效语义）证据未覆盖仍开放、唯爆破面收窄（窗生效面仅及可变引用面），ADR-0050 无需 Errata。
Files:         DECISIONS.md（+40 行：ADR-0050 整节 L1375 起；+1 行：ADR-0012 勘误四（L193，勘误三之后、ADR-0013 之前）；只追加，既有正文/勘误一~三零改动）；docs/design/repo-update-merge.md（+36/-1：状态行 1 处 + §9 任务书）；reports/agents/T-L008-1.md（本日志）
Tests:         文档票无码改；编译门验证仓树健康（go build ./... 零输出通过、go vet ./... 通过，exit 0 实测）；引用门：ADR-0050 编号唯一（sort|uniq -d 空输出=零重复；0037 为历史空号、0050 顺承 0049）、任务书引用的 internal/ 路径与行号锚（config.go L211/328、service.go L2507/2576/2627、repositories.go L625/708、keypair.go L396、lib/repos.ts L182-189）逐一 grep 核对存在；表格门：ADR-0050 矩阵 6 行全 4 列（pipes=5 一致）、任务书 §9.1 表 4 列（config.go 行内代码 `\|\|` 转义已核）；证据文件存在性核过（L006-a-b-diff.md / L007-3-update-merge-evidence.md）
Commands:      go build ./... →（零输出，exit 0）；go vet ./... →（零输出，exit 0）；grep -c "^## ADR-0050" DECISIONS.md → 1（编号唯一）；grep -n "parseRemoteConfig(" internal/repo/*.go → 两生产调用点（service.go:2196 create / 2576 update）+ 零测试直调（签名扩展爆破面=2 处）；grep -n "ErrRepoExists" internal/httpapi/repositories.go → writeRepoSvcError L783 已映射 400（PUT 回撤可复用既有错误面）；grep -n "createRepo\|updateRepo" web/src/lib/repos.ts → L183 PUT / L188 POST（console 分工已合拍实证）；tail -c DECISIONS.md → 追加点=ADR-0049 软缝协议行（只追加纪律满足）
Outputs:       ADR-0050 ×1（含三列矩阵机制表 + 6 决策点 + 交叉对账 6 项 + 软缝 Errata 触发条件 5 条）；实现票任务书 ×1（5 文件改动清单 + 7 验收 + 17 差分臂）；模块接口契约 0 新增（本票纯行为面裁定，Service 接口零改动——UpdateRepo 签名不变）；API 契约 2 端点语义定案（PUT/POST /api/repositories/{key}）
Compatibility: 与 docs/compatibility/contracts/ 无冲突（repo-config 契约未冻结，规格票待立）；known-divergence#rest/repo-config-update-merge-semantics（BUG）authority 链更新为 ADR-0050（type: adr），台账/矩阵回填归 compatibility-engineer（本票不动其域）；D02-R03 L007-3 补注臂（PUT-on-existing 相抵）获 ADR 裁定锚，实现票差分后闭环
Security:      credential 族裁定——省略=保留密文行（sealed password 不动）、显式 ""/null=清、单侧清；NFR-S14 恒不回显 password 维持（参照回显密文块=既有差异账不随本票翻）；PUT-on-existing 400 化不减门（family-7 路由门照常先行，ADR-0026 机制轴零翻动）
Performance:   merge 基线=UpdateRepo 已有 current（Get 在 L2526）复用，零新增读放大；raw-map 判缺复用 validateRemoteConfigShape 已解出的 map（或等价一次解码），无二次解析
Risks:         ① 对象族两格证据缺口（显式 null、未提子键 disposition）——探针臂 A14/A15 定案，规格票冻结；② fetch 侧存量 0 生效语义未取证——Errata 触发条件④预登记；③ PUT-on-existing 非 admin 门序未取证——探针臂 A17；④ 实现票同时触 create+update 双面 0-as-absent，create 面回归（T-290 族既有测试）需全跑
Blockers:      无
Next:          ① conductor 按本任务书派 dev-go-core 实现票（含 T-80 注释勘误 L2627）；② 规格票冻结字面量（400 文案逐字/报错次序/对象族两格）；③ ADR-0026 文字级 Errata 候选（「PUT 替换既有仓臂」表述过时）待 conductor 裁量派票；④ 实现后差分复验 → 台账 resolved + D02-R03/R04 注记回填（compatibility-engineer）；⑤ local/virtual merge 实现票（virtual 参照行为待取证）
