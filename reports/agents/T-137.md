# T-137 [P0] FR-38 原生 K8s 清单（PB-06）

**Agent**: release-engineer
**Status**: done
**Date**: 2026-08-21

## 做了什么

### 文件改动

| 文件 | 变更 |
|---|---|
| `deploy/k8s/deployment.yaml` | 新建 — Deployment：replicaCount=1 / securityContext runAsNonRoot:true / env config+secret / PVC / liveness+readiness probes（distroless exec）/ resources requests+limits / terminationGracePeriod 35s |
| `deploy/k8s/pvc.yaml` | 新建 — PVC：RWO / 20Gi / storageClass 可配置 |
| `deploy/k8s/service.yaml` | 新建 — Service：type/port/nodePort 可配置 |
| `deploy/k8s/secret.yaml` | 新建 — Secret 样例：admin password + remote credentials key，含注释说明 |
| `deploy/k8s/ingress.yaml` | 新建 — Ingress 注释样例：nginx ingress class，无 rewrite 注解 |
| `deploy/k8s/kustomization.yaml` | 新建 — kustomize 构建入口 |

### 架构决策

- **安全基线**: `runAsNonRoot: true` + UID 65532（distroless nonroot）；口令经 Secret（不硬编码）；镜像字段无 `latest`
- **镜像默认 distroless**: `binflow:v1.0.0-distroless`（版本锚定，走 exec probe healthcheck-probe）
- **探针三件**: liveness（10s delay, period 10s）+ readiness（5s delay, period 5s）+ startup（注释就绪可选）
- **SQLite 单写约束**: `strategy.type: Recreate`（WAL 模式单 writer），不用 rolling update
- **Ingress 无 rewrite**: 注释样例含 `nginx.ingress.kubernetes.io/ssl-redirect` 但不含 rewrite-target（ADR-0010）

## AC 对照

| AC | 描述 | 状态 |
|---|---|---|
| G14 | deploy/k8s/ 完整：Deployment/PVC/Service/Secret 样例/Ingress 注释样例 | 通过 |
| G14 | runAsNonRoot:true / resources requests+limits / 探针三件 | 通过 |
| G14 | 镜像字段无 latest（版本锚定） | 通过 |
| G14 | env 口令经 Secret（secretKeyRef） | 通过 |
| — | kubeconform + kustomize（P1） | Pending |

## 遗留问题

- **kind 烟测**: 需要 kind 环境 `kubectl apply -f` → Pod Ready → push/pull 一腿 → delete pod 重建制品存活验证
- **kubeconform**: 需要 kubeconform 工具，本地无
- **kustomize build**: 需要 kustomize 验证