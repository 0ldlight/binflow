# T-110 工作日志 — assets 保留字增补 + session TTL 塌缩句

- 角色: architect
- 日期: 2026-08-20
- 输入: T-91 双 review 裁决意见（架构 review 遗留① + 意见：滑动续期字面 vs 实际行为矛盾；安全 review 支持保留字增补）
- 状态: done（含会话中断恢复：中断发生于日志落盘前，两处主编辑已在工作树，恢复后核对完整并补齐一处残留一致性行）

## 两处落点

### 1. ADR-0008 再增补（DECISIONS.md 一句）

repo key 保留字并集增 **`assets`** → {api, v2, docs, console, ui, assets}。理由（T-91 架构 review 遗留①）：`assets` 与 `ui` 同构被 `/binflow/assets/**` SPA 指纹资产前缀遮蔽——不保留则 `assets` 仓建仓成功但内容永不可达（静默不可用缺陷）；安全 review 同判。代码侧 `reservedRepoKeys` 由 T-91 修复轮同步（一行），本票只动 ADR。

### 2. TTL 塌缩句（ADR-0014 勘误③ 补句 + architecture.md §7.5 补句）

「双键共用一键后，滑动续期被绝对封顶**吞没**——会话必死于 `created_at + TTL`，与活跃度无关」。防止前端/QA 按「滑动续期」字面写成「活跃可续命」：实际默认配置（24h）下活跃用户也必掉线重登。落在两处：ADR-0014 勘误③末尾括号句；architecture.md §7.5 登录流条目末尾加粗句（同文源对齐）。

### 一致性收口（超出两处清单的必要最小同步，3 行）

保留字并集在 architecture.md 有三处引用，中断前已同步 §5.1 校验行，恢复后补齐 §6 DDL 注释行（原仍为五字并集）；§7.1 assets 路由行 T-108 时已备无需动。

## 边界自律

DECISIONS.md 两句（均为追加括号注，不动决策正文）+ architecture.md 四处（塌缩句 1 + 保留字并集同步 3）；未改代码；未动 BOARD.md。

## 遗留问题

无新增。`assets` 段与 repo key 的保留字清单现已闭合（T-108 遗留 1 就此解决）；T-91 修复轮的 reservedRepoKeys 一行代码归实现侧。
