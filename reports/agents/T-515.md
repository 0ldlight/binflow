# T-515 工作日志：配置格式行为规格（docs/reverse/config-formats.md 全量重写）

- **ticket_id**: T-515（Phase A / 计划票 T5「配置格式行为规格」）
- **role**: reverse-engineer（本票实例域：配置文件格式 + 加载时序 + 生效语义）
- **date**: 2026-09-24
- **area**: docs/reverse/config-formats.md
- **scope_files**:
  - 只读取证：/Users/lzw/workspace/artifactory-decompiled（7.161.x 全量反编译树；树内模块版本戳 7.161.14、XSD 链含 7.162.0 命名空间——多构建混装，已在规格头部声明）。主要涉及模块：artifactory-config（ArtifactoryHome/ConstantValues/ArtifactorySystemProperties/HaNodeProperties/ArtifactoryDbProperties/ArtifactoryConfigVersion/ArtifactoryConfigurationAdapter）、artifactory-core（CentralConfigServiceImpl/OnboardingYamlBootstrapper/RepositoryConfigBootstrapServiceImpl/RepoConfigBootstrapUtils）、jfrog-config（ConfigurationManagerImpl/ConfigWrapperImpl/JFrogHome）、jfrog-db-infra（DbProperties）、artifactory-rest（ConfigResource/ImportResource/ExportResource/SystemResource/MapToDiffConverter 消费面）、artifactory-rest-ui（ConfigDescriptorResource/Get|UpdateConfigDescriptorService）、artifactory-lifecycle（ArtifactoryHomeConfigListener）、access-server-core（AccessSecurityBootstrap/InternalAccessHome/AccessConfigKeys）、artifactory-support-core（ArtifactoryConfigFileCollector）、artifactory-api（PathValidatorUtil）
  - 写入：docs/reverse/config-formats.md（全量重写，保留并继承 T-303 binarystore 节）；本日志
- **commands_run**（实跑取证命令与命中，均只读）:
  - `ls /Users/lzw/workspace/artifactory-decompiled{,/backend}` — 模块清单（569 模块）
  - `find .../backend -name "ArtifactoryHome.java"` 等 8 次 find 定位关键类（命中 artifactory-config/common/home 等）
  - `grep -rln "db.properties" backend --include="*.java"` — 8 文件命中，全部为 DbProperties/sysConfig 体系或文档字符串，**无 etc/db.properties 文件读取**
  - `grep -rln "ha-node.properties" backend` — 仅 1 命中（ArtifactoryHome 常量定义），读取逻辑在未反编译 sysconf
  - `grep -rln "storage.properties" /Users/lzw/workspace/artifactory-decompiled -r` — **0 命中**（负证，7.161 无此文件）
  - `grep -rln "system.yaml\|SystemYaml" backend` — 无 SysConfig/SystemYaml 实现类命中（sysconf 内核未反编译的证据）
  - `grep -rn "getenv" artifactory-config artifactory-core jfrog-db-infra jfrog-config` — 仅 MetricsServiceImpl/AccessRestClientImpl/ArtifactoryVersionReader(ARTIFACTORY_PRODUCT_VERSION) 等 4 处旁路命中，配置主链无 env 读取 → env 映射在 sysconf
  - `grep -rn "JF_SHARED\|JF_ARTIFACTORY" backend` — 1 命中（日志测试 mock），佐证 JF_* 映射不在 Java 侧
  - `grep -n "importAllowedPaths|configXmlValidationEnabled|centralConfigSave|configurationManager*" ConstantValues.java` — 默认值行号取证（736-739/920-922/1164-1171/1714）
  - `Read` 全文：ArtifactoryHome(921 行)/HaNodeProperties/ArtifactorySystemProperties(363)/DbProperties(338)/JFrogHome/CentralConfigServiceImpl(分段 1000+ 行)/OnboardingYamlBootstrapper(483)/CentralConfigReader/ConfigResource(364)/ConfigDescriptorResource/ImportResource/PathValidatorUtil/ArtifactoryConfigurationAdapter/ConfigurationManagerImpl(461)/ConfigWrapperImpl(分段)/AccessSecurityBootstrap/ArtifactoryHomeConfigListener/AccessConfigKeys(节选)/AccessConfigMapUtils(节选)/ArtifactoryConfigFileCollector/ArtifactoryConfigVersion(节选)
  - 官方文档交叉：WebFetch docs.jfrog.com「Using System YAMLs」（命中层级声明：env > 服务段 > shared > 默认）、「system-yaml-configuration-file」（无插值/无 legacy 文件条款）；WebSearch 佐证 artifactory.node.* 取代 ha-node.properties
- **key_outputs**:
  - §2 etc 文件总清单（20+ 文件：角色/缺省/HA 共享/加密）+ 9 步启动加载时序
  - §4 db.properties 去向 + `artifactory.database.*` 全键默认值表（derby/hikari/100/10/1/lockingdb 三必填/AWS SecretsManager/{db.home} 占位符/HA+Derby 拒绝启动）
  - §5 ha-node.properties 去向（HaNodeProperties 键表：artifactory.node.* 8 键）
  - §6 system.properties 8 步加载算法 + 废弃键全量表（12 项）+ ConstantValues 机制（-D > 文件 > 代码默认；类型解析行为）
  - §8/§9 config.xml 家族 5 文件生命周期 + 启动 5 级回退决策（import > DB > bootstrap > 旧 config.xml > 内置默认；import 一次性改名消费）+ 版本探测/转换链（当前 XSD 3.5.11）+ **replace-vs-merge 精确语义**（POST 全量=替换，除带旧 revision 三方合并例外；PATCH YAML=合并，`~`=删键）+ 乐观并发重试参数 + 热生效/重启分面
  - §10 仓库导入 = replace（先 deleteAll 再批量插入）+ 双导入源互斥校验
  - §11 onboarding YAML（仅空实例、成功后留档+删除、许可证延迟安装）
  - §12 security 文件族 + access.security.bootstrap.yml（upsert 后删文件）+ config.xml security→Access 推送
  - §13 etc→DB 文件同步器（白/黑名单、时间戳冲突解决、.force.delete、mandatory 保护、600 权限、20s/60s/3 次）
  - §14 暴露面端点表（GET/POST/PATCH/PUT /api/system/configuration、UI configdescriptor、import/export system/repositories、密码脱敏与 access-token 例外、支持包脱敏串 21 项）
- **test_evidence**: 无——本票纯规格（未对 :8082 活体验证；参照实例属 7.161.26 与本规格源版本不同代，动态验证留给差分票）
- **compat_evidence**: 覆盖统计：端点 12（§14 表）/ 流程 14（§2.2 时序 9 步 + §8 决策 5 级 + §13 同步 3 面）/ 布局与键表规则 40+（§2.1、§4.2、§5、§6.2、§12.1）。置信度分布：高 ~62 / 中 ~12 / 低 0（低置信项全部以「未能定位/待验证」形式收敛进 §16 九项，无编造条目）。规格头部标注源版本：7.161.24 反编译树（模块戳 7.161.14 混装）+ 旧稿 7.161.16 证据
- **open_questions**（=规格 §16）: ① system.yaml vs system.properties 同键仲裁 ② ha-node.properties legacy 回退是否存在 ③ ${VAR} 插值（判负，留验证）④ JF_* 映射细节 ⑤ XML 重复元素报错文案 ⑥ binarystore 非法组合报错（T-303 遗留）⑦ config.xml security↔Access 全映射表 ⑧ repo import.json 解析失败形态 ⑨ db.properties 安装器迁移
- **decisions_made**: ① 保留 T-303 binarystore 节为 §7 继承（证据版本差异显式标注，不回改旧结论）② storage.properties/db.properties 以「负证+去向」方式入规格而非省略（ticket 点名文件不能静默丢弃）③ sysconf 内核边界单列 §1，凡涉其内部行为只引官方文档并降置信度 ④ 暴露面端点表含 UI configdescriptor（Admin Console「Config Descriptor」页即此）
- **deferred**: ① 仓库 JSON（AggregatedReposConfig）字段级 schema → 建议独立票（repo-operations.md 已有部分）② Access config.xml security 映射全表 → security 领域票 ③ logback 热加载细节 → logging 领域
- **risks**: ① 反编译树多版本混装（7.161.14 戳 + 7.162.0 XSD）——个别条目可能领先 7.161.24 基线（XSD 3.5.11 归属 7.162.0 一条已标注）；② `getBootstrapConfigXml` 反编译丢失 return 的控制流为还原语义（与官方描述一致，仍属推断面）；③ sysconf 相关三条（§3.2 层级/§3.3 env 映射/插值）为官方文档单源，置信度已降「中」；④ GET configuration 密码脱敏的「access-token 例外」行为对兼容目标影响大，建议差分票优先覆盖
- **handoff_to**: compatibility-engineer（§14 端点表 + §9.3 merge/replace 语义可直接契约化；§16 待验证项转差分票）；conductor（D1 配置格式策略裁决的直接输入：§3 优先级双轴、§8 导入语义、§13 同步器是 BinFlow 设计必须对齐或显式分歧的三个面）
- **status**: done
