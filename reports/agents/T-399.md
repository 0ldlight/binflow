# T-399 — M14 release 烟测 + UAT 随里程碑 PR（release-engineer）

- 票：T-399 [P1] release 烟测 + UAT 随里程碑 PR（BOARD B10 段；area: deploy/ + charts/ 版本收口 + CD 链；dep: 全部实现票）
- 状态：**done**（AC1 矩阵烟测六腿全绿 + Chart 1.3.0→**1.4.0** 定案 + PVC keep kind 真集群一手实证；AC2 UAT 执行面备妥——BEFORE 取证归档 + 可粘贴清单 §5，候 conductor 在 T-400 PASS 后建 develop→main 里程碑 PR 触发）
- 日期：2026-09-01（04:56 首轮被配额击落零落盘，07:07 重置后从头执行）
- 红线遵守：**零 git 写操作**（scratch 取自 `git archive HEAD`=28f5c09，只读 git diff/log 用于核查）；UAT 仅只读 SSH 取证 + 出网探测（零写、零服务变更）；未推送任何镜像/Chart/二进制（build-release.sh 以 `REGISTRY= PUSH=0` 本地止步）；对外发布红线维持。
- 被测树：**HEAD 28f5c09**（M14 全量：含 e43131d D-396-1 修复 + cdd6c0e T-398 docs）。

---

## 1. AC1

### 1.1 Chart 版本收口：**1.3.0 → 1.4.0**（bump 定案）

- **事实链**：Chart 1.3.0 由 T-376 盖章（e8aec33）；其后仅 T-394（d6395c1）改了 `templates/pvc.yaml`（keep 注解 + merge）、`values.yaml`（注释）、`NOTES.txt`（手动清卷说明）而未 bump（T-394 §4.4 显式归本票）。**树内 1.3.0 ≠ T-376 盖章 1.3.0**——同一版本号两种渲染产物。
- **定性**：非注释级——`pvc.yaml` 渲染产物变化（每持久化安装新增 `helm.sh/resource-policy: keep`），且 `helm uninstall` 行为翻转（删卷→留卷）是用户可见语义变化 → 按本票派单口径（"若仅注释级可维持 1.3.0"）**不满足维持条件，bump 1.4.0**（minor：行为新增、向后兼容，随 1.2.0→1.3.0 的里程碑节律）。
- **落地**：`charts/binflow/Chart.yaml` version→1.4.0 + `artifacthub.io/changes` 按既有「每版重写」惯例（对照 65ba574→e8aec33 先例）置顶 PVC keep 条目（含 merge 优先级与 NOTES 双路说明），保留 1.3.0 三键族条目于其后。
- **验证**：`helm lint charts/binflow` = **1 chart(s) linted, 0 failed**；四态渲染（scratch 内 1.4.0 同树）：
  - 默认：`annotations: {helm.sh/resource-policy: keep}` ✓
  - 用户注解冲突（转义 `--set 'persistence.annotations.helm\.sh/resource-policy=remove'` + `foo=bar`）：渲染 `foo: bar` + **keep 单键胜**（无重复键）✓（注：不转义点号的 `--set` 会被解析成嵌套对象报 YAML 错——helm 语法限制，非 chart 缺陷）
  - `existingClaim=mine` / `enabled=false`：模板不产出 ✓

### 1.2 部署矩阵烟测——六腿全绿（真部署真验证）

构建形态：`git archive HEAD` → /tmp/bf-t399 → `make console && make docs && make build` 全绿（wire-brand-assets 7 文件指纹化；SPA 377,629B gz；docs 4.58MB；binflow-server 39.87MB≤40 绿 / bf 15.28MB WARNING=m13 维持态 / bf-migrate 8.96MB）。

| 腿 | 结果 | 关键实测 |
|---|---|---|
| **R 单二进制**（goreleaser face） | ✅ | scratch 无 .git，按 `.goreleaser.yaml` 逐字复刻六平台产物（同 `-s -w -X main.version/-X main.revision`、`-trimpath`、CGO=0、目录包裹 + README.md、windows zip、sha256 checksums）；`make release-verify RELVER=v1.0.0` = **6/6 archives + checksums present，shasum -c 全 OK**。⚠️ **六平台聚合 103.36MB > 100MB（T-326 门）首红**——见 §6-F1 |
| **I 镜像链**（build-release.sh 本地形态） | ✅ | `REGISTRY= PUSH=0` 149s：Dockerfile.assets 原生资产级 + 双变体×双 arch 零-RUN 注入（digest 钉底座）→ 本地 `binflow:v1.0.0-{alpine,distroless}-{amd64,arm64}` 四镜像；arch config 字段四验全对；SMOKE 四镜像 readyz=OK + version=`v1.0.0`。arm64 镜像零 qemu 即组装（T-270 契约维持） |
| **L1 compose** | ✅ | prebuilt 镜像 `up -d`（compose 面 build: 段被既有镜像命中；本机 buildkit goproxy.cn 障碍〔T-376 §2〕以预构建注入绕开，全源 `--build` 链已由 T-376 落证）→ `binflow-ga Up (healthy)`；healthz/readyz OK；**favicon-996d22f6.ico 200 + immutable**；roundtrip：建仓 200 → PUT 201 → GET **cmp 字节一致** → DELETE 200 → gone 404；settings 回显 M13 键族在场；`down -v` 清净 |
| **L3 k8s 清单**（kind bf-ga，distroless） | ✅ | `kubectl kustomize` → sed 密码 → apply 4 资源 → **rollout successfully rolled out**，pod 1/1，PVC binflow-data **Bound**；port-forward → healthz OK + favicon 200 + roundtrip 全绿（200/201/IDENTICAL/del 200/gone 404）；`kubectl delete -f` 4 资源全删还原空态 |
| **L-chart PVC keep**（kind，AC1 核心） | ✅ | 见 §2 |
| **L4 systemd** | ✅ | 静态：`bash -n` install.sh OK + `--dry-run` 全走通（M13 键清单输出在场）；unit 语义面（User=/Group=binflow、Restart=on-failure、ReadWritePaths=/var/lib/binflow、ReadOnlyPaths=/etc/binflow、EnvironmentFile=-）逐行在册。活体：debian:bookworm-slim 容器，**服务用户直读 0600 env 文件被拒（Permission denied——恰证 unit 设计：manager 注入）**；root 读入 env → `setpriv --reuid/--regid/--clear-groups` 降权执行（= systemd 注入语义）→ 起服 `version v1.0.0 revision 28f5c09`；healthz OK；**env 注入实证 `.env` 的 TRASHCAN__RETENTION_DAYS=21 → settings 回显 21**；favicon 200；roundtrip IDENTICAL |
| **L5 offline** | ✅ | 按 README 布局真构 bundle（docker save 双变体 tar + **helm package → binflow-1.4.0.tgz** + k8s/compose 面 + linux amd64/arm64 二进制 + checksums + SHA256SUMS，18 文件 85M/tar 50M）；干净目录解包 → `shasum -c SHA256SUMS` **0 non-OK**；`install-offline.sh --compose`（.env：端口 18091 + retention=10）→ healthy + trashcan 回显 10 + favicon 200 + roundtrip IDENTICAL；`--helm --dry-run` → **chart 自动发现 binflow-1.4.0.tgz + 钉捆绑镜像 `--set image.repository=binflow --set image.tag=v1.0.0-distroless`**（T-376 C3 修复维持）；`down -v` 清净 |

### 1.3 helm uninstall PVC keep 真集群验证（kind 一手实证 + T-396 L14 承证复核）

**一手序列（Chart 1.4.0，kind bf-ga，ns t399-pvc）**：

```
helm install t399 charts/binflow --set image.repository=binflow --set image.tag=v1.0.0-distroless --set persistence.size=2Gi
→ deployment rolled out；PVC t399-binflow Bound（vol pvc-04c37dd6-cb4f-4798-a655-1aa59b23b3eb）
→ 注解活体：kubectl get pvc -o custom-columns=…ANN → keep
→ port-forward → 建仓 t399-pvc-keep 200 → marker 上传 201 → 读回 t399-pvc-keep-marker ✓
helm uninstall t399 --wait → release "t399" uninstalled
→ 幸存：PVC t399-binflow **Bound + 同 Volume + keep**；PV pvc-04c37dd6 **Bound**（claim 仍指 t399-binflow）；Deployment 已无
helm install（同 release 名，同参）→ STATUS deployed → rollout OK → **收养幸存卷**（同 Volume pvc-04c37dd6）
→ marker 读回 `t399-pvc-keep-marker` **cmp 逐字一致**（PVC-KEEP-MARKER-BYTE-IDENTICAL-AFTER-REINSTALL）
收场：helm uninstall → NOTES 手动清卷路径1 实跑 `kubectl delete pvc t399-binflow` ✓ → ns 删除 → kind 还原空态
```

意外加分证据：首装误设不存在 storageClass（PVC Pending）时 uninstall 同样**保留 Pending PVC**（失败路径 keep 亦生效）。
**T-396 L14 承证复核**：其 kind 腿（t396-marker、uninstall 双幸存、重装逐字读回）与本票序列逐项同构——其跑于树内未盖章模板、本票跑于 **1.4.0 盖章后**，两轮独立绿，结论闭合。NOTES.txt 的双路清卷说明在卸载后 install 输出中实见。

## 2. AC2 — SPA/favicon/manifest go:embed 接线核验（二进制内嵌面）

**接线链**（HEAD 实读）：`web/public/brand/*`（7 文件）→ vite build 落 `dist/brand/`（base 前缀 `/binflow/ui/brand/`）→ `web/scripts/wire-brand-assets.mjs` 整族搬入 `dist/assets/` 并按内容 sha1-h8 指纹化（`favicon-996d22f6.ico`）、manifest `icons[].src` 改写后整体指纹化、index.html href 重写、`dist/brand/` 删除（自校验：任一 href 未命中映射/残留即非零退出）→ `make console` cp 进 `internal/console/dist/` → `//go:embed dist` → `serveAsset`（扁平名 + `public, max-age=31536000, immutable`；`/binflow/ui/**` 恒 SPA shell 无文件特例）。

**活体核验**（HEAD 全量构建二进制，本机 serve）：

- 壳 href 六条全指纹化：favicon.ico/-16/-32/-48/apple-touch-icon/manifest → `/binflow/assets/<名>-<h8>`（与 JS/CSS 同挂载）
- 6 资产 GET **全 200**：content-type 正确（image/x-icon、image/png、application/json）+ **immutable** + 字节数与源文件逐一相等；`favicon-996d22f6.ico` 与 `web/public/brand/favicon.ico` **cmp 字节一致**
- manifest（`manifest-ddcb36c7.json`）`icons[].src` = 指纹化兄弟名（apple-touch-icon-cffceb7c.png / icon-512-55cc9eca.png）——PWA 相对解析成立
- 负臂：子目录名（`assets/brand/favicon.ico`）、裸稳定名（`assets/favicon.ico`）、遍历（`assets/../brand/…`）全 **404**（扁平名 + 内容寻址契约维持）
- 部署面贯穿：同一指纹在 **compose（alpine 镜像）/ k8s（distroless 镜像）/ systemd（goreleaser 单二进制）/ offline** 四腿全部 200 —— 内嵌面全矩阵一致
- **线上承证**：UAT（uat.13ca2d6，main PR #47 已含 T-389 品牌面）现役壳引用的即 `favicon-996d22f6.ico`/`favicon-48-bcf9f075.png`——与源资产 sha1-h8 **逐一吻合**（生产形态实例已服务同族字节）

## 3. AC2 — UAT BEFORE 取证（只读，2026-08-31T23:16Z）

```
ssh -i ~/.ssh/binflow-uat.pem -o IdentitiesOnly=yes ubuntu@52.79.109.153
host:    up 4d13h；/ 96G 用 3.0G（4%）
unit:    binflow-uat active；ExecMainStartTimestamp=Mon 2026-08-31 09:37:04 UTC
         ExecStart=/opt/binflow-uat/binflow-server serve -c /opt/binflow-uat/binflow.yaml
healthz/readyz: OK / OK
version: {"version":"uat.13ca2d6","revision":"13ca2d6","product":"BinFlow"}   ← = origin/main HEAD
binary:  /opt/binflow-uat/binflow-server 30134434B sha256 85fe3142…（.new 残留仍在——T-376 §5.4 维持态）
docs:    /binflow/docs/ 200；/binflow/ui/ 200
M14 标记 A（favicon 族）：壳内已引用 favicon-996d22f6.ico 等六指纹（main 经 PR #47 已含 T-389 品牌面）
M14 标记 B（T-398 docs）：/binflow/docs/admin/governance/ grep push-replication = 0（未含——随里程碑 PR 翻转）
M14 标记 C：/binflow/api/v1/system/settings 匿名 401（需 admin——AFTER 腿带凭据复核）
```

**T-397 遗留①（Docker Hub 直连）UAT 出网探测**：`https://registry-1.docker.io/v2/` → **401 + `WWW-Authenticate: Bearer realm="https://auth.docker.io/token"`**——UAT 侧公网可达、挑战舞步同构（T-397 本机 DNS 污染环境拿不到的数据点）。产品腿（建 docker remote 仓真拉）属写操作，归 AFTER 清单可选项。

## 4. T-396 观察项① — main/develop CI 差异核查（知悉项收口）

`git diff origin/main(13ca2d6) HEAD -- .circleci/ deploy/ci/`：**各仅 1 hunk，全部在 deploy_uat SSH 键路径**；e2e job **零差异**。

- main 持有修复形态：`UAT_SSH_KEY_B64` base64 注入（+UI 键回落）+ uat-deploy.sh 通用 `id_<type>_<fingerprint>` 匹配 + **强制 `-i` + 缺键报错**；develop 为旧形态（`fingerprints: ["$SSH_KEY_FINGERPRINT_UAT"]` 环境变量不展开问题 + 键可选）。
- **里程碑 PR 合并安全性**：merge-base=1830559；`git diff 1830559..HEAD -- <两文件>` = **空**（develop 未动过）→ 三方合并单侧变更胜出，**合并自动保留 main 修复形态**（squash/rebase 同安全——PR diff 不含这两文件）。清单 §5-6 附合并后一行复核。
- 建议登记：修复回流 develop 属整洁项非阻塞（下一里程碑窗顺手 cherry-pick），避免未来 rebase 类操作踩坑。

## 5. 触发后可执行清单（UAT AFTER——候 conductor 建里程碑 PR；逐条可粘贴）

```bash
# 0) conductor 在 T-400 PASS 后建 develop→main PR；合并触发 CircleCI（build → deploy_uat + e2e）
#    uat-deploy.sh 内建版本断言（uat.${CIRCLE_SHA1:0:7} 逐字）+ 探针失败自动回滚（备份 5 份）
SSH="ssh -i ~/.ssh/binflow-uat.pem -o IdentitiesOnly=yes ubuntu@52.79.109.153"

# 1) 换装 + 基线翻转
$SSH 'systemctl is-active binflow-uat; systemctl show binflow-uat -p ExecMainStartTimestamp'
$SSH 'curl -fsS http://127.0.0.1:8080/healthz'                                  # OK
$SSH 'curl -fsS http://127.0.0.1:8080/binflow/api/system/version'               # uat.<合并sha> ≠ uat.13ca2d6

# 2) M14 面标记（BEFORE 取证对照 §3）
$SSH 'curl -fsS http://127.0.0.1:8080/binflow/docs/admin/governance/ | grep -c push-replication'
#    期望 ≥1（BEFORE=0——T-398 治理指南随 PR 内嵌）
$SSH 'curl -fsS http://127.0.0.1:8080/binflow/ui/ | grep -oE "favicon-[a-f0-9]{8}\.ico"'
#    期望指纹名（BEFORE 已在场=PR #47 品牌面维持，非翻转标记）
$SSH 'curl -fsS -u admin:$UAT_PW http://127.0.0.1:8080/binflow/api/v1/system/settings'
#    期望 200 + M13 键族维持（folder_download 六字段=规格列 / trashcan.retention_days=14）

# 3) 制品 roundtrip（唯一允许的写：自建自删，T-376 纪律）
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X PUT \
      http://127.0.0.1:8080/binflow/api/repositories/t399-uat-smoke -H "Content-Type: application/json" \
      -d "{\"rclass\":\"local\",\"packageType\":\"generic\"}"'                  # 200
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X PUT \
      --data-binary t399-uat-smoke-payload \
      http://127.0.0.1:8080/binflow/t399-uat-smoke/smoke/t399.bin'             # 201
$SSH 'curl -fsS -u admin:$UAT_PW http://127.0.0.1:8080/binflow/t399-uat-smoke/smoke/t399.bin; echo'  # 回读 payload
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X DELETE \
      "http://127.0.0.1:8080/binflow/api/repositories/t399-uat-smoke?deleteContent=true"'  # 200

# 4) M14 功能面抽样（带凭据；replication PUT = T-405 新端点）
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X PUT \
      http://127.0.0.1:8080/binflow/api/v1/replications/99999 -H "Content-Type: application/json" \
      -d "{\"enabled\":true}"'    # 期望 404 "replication config not found: 99999"（M13 二进制无此路由——翻转标记；真实 PUT 域测归 e2e）

# 5) 可选（T-397 遗留①产品腿——UAT 出网已达〔§3〕，conductor 裁是否做写面）：
#    建 docker remote 仓（url=https://registry-1.docker.io/v2）→ docker pull 经 UAT 实拉 → 删仓
#    （写操作；BEFORE 只探测了可达性，未动服务）

# 6) PR 合并后一行复核（§4 结论的落章验证）
git fetch origin && git show origin/main:.circleci/config.yml | grep -c UAT_SSH_KEY_B64   # 期望 ≥1（main 修复形态保留）
```

UAT 数据纪律：零写操作除 §5-3/§5-5 自建自删项；不动 `/opt/binflow-uat/binflow.yaml`（operator-owned）与 data/。

## 6. 发现登记（报不自裁）

| # | 发现 | 处置态 |
|---|---|---|
| **F1** | **六平台聚合 103.36MB > 100MB（T-326 check-size 门）首红**：T-326 建 gate 时 94.13MB（单档 14.93–16.36MiB），T-329 后该 face 未再跑过；现单档 16.29–18.04MB，M12→M14 累积（console/docs/服务端功能面）+9.2MB。**单档 40MB 门绿**（max 18.04MB 压缩档；裸 binflow-server 39.87MB≤40）、运行时门全绿（T-396 footprint 13.9MB/冷启 718ms）。本轮以 gate 自带 `CHECK_SIZE_WARN=1` 降级放行 release-verify | 与 D-396-2（NFR-P61 口径）同族记账问题——**候 conductor 裁**：再基线（如 120MB）或拆分口径（单档门为主）；非 M14 突增（bf 15.28MB 与 m13 逐字节同级，server 增量正常） |
| F2 | `bf` 15.28MB 触 15MB WARNING——m13-done 同值维持态（T-396 L17 在册），非 M14 增量 | 维持登记 |
| F3 | compose 腿以 prebuilt 注入替代全源 `--build`（本机 buildkit goproxy.cn 障碍，T-376 §2 在册）——G10 全源冷构建链由 T-376 落证，镜像内容等价（同二进制同资产），compose 编排/健康/卷/roundtrip 面本轮全覆盖 | 环境限制留痕；镜像源修复或 GOPROXY build-arg 化仍为候选（T-376 §5.5） |
| F4 | CI 修复回流 develop（§4）——整洁项非阻塞 | 建议下里程碑窗 cherry-pick |
| F5 | UAT `/opt/binflow-uat/binflow-server.new` 残留（T-376 §5.4）仍在 | 维持原登记（uat-deploy.sh 一行，非必要面） |

## 7. 交付文件（工作树未提交态，均在本票 area）

```
charts/binflow/Chart.yaml          1.3.0→1.4.0 + changes 注记重写（PVC keep 条目置顶）
deploy/README.md                   矩阵第 4 行 M14/1.4.0 增补 + M14 里程碑烟测记录行
deploy/offline/README-offline.md   chart 包名示例 1.3.0→1.4.0
deploy/offline/install-offline.sh  注释示例 1.3.0→1.4.0（行为零变化）
```

（PVC keep 模板本体、NOTES、values 注释为 T-394 已落树内容，本票只做版本收口与验证，不改模板。）

## 8. 清理对账

本地：compose/offline 两栈 `down -v`（卷/网络全清，binflow-ga 容器零残留）；kind 内 k8s 基座 4 资源 + t399-pvc ns/PVC/PV 全删（还原空态，bf-ga 集群本体保留=既有资产）；systemd 容器已 rm；port-forward 全断。留存备复验（/tmp，可整删）：`/tmp/bf-t399`（scratch 树+三段构建日志）、`/tmp/t399-*.log`、`/tmp/t399-mkdist.sh`、`/tmp/binflow_offline_v1.0.0{,.tar.gz}`、`/tmp/t399-offline-test`（解包验证现场）。本地镜像 `binflow:v1.0.0-{alpine,distroless}{,-amd64,-arm64}` 留存供 conductor 复验（未推任何 registry）。UAT：零写（§3 全 GET/systemctl show/curl 只读探测）。
