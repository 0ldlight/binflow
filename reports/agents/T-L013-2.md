# T-L013-2 — K60 群零补证转写 + packument 裁定腿素材（LOOP 013 轨道 2）

```
Ticket:       L013-2 · K60 认证/会话族契约第二批（零补证转写）+ packument-latest 判别探针设计 · P1
Role:         compatibility-engineer（领域实例：npm）
Area:         docs/compatibility/contracts/npm.yaml（第二批）+ fixtures/normalize.yaml + tools/difftest/l0132/
Input:        docs/reverse/npm.md（T-393 K60 定案——npm 10.9.8 源码逐文件核对 + live 抓包 + DE 三方）；
              L012 两批独立 wire（l0121-wire 定稿 + l0122-wire 复核——L012-dist-tags-diff.md §5 零补证建议，
              T-L012-2b Next 承接）；T-394.md（login 落地 live：AC1/AC5/AC6/AC8 + 注记 1-2）；
              known-divergence.yaml#npm/packument-latest-recompute（UNKNOWN，gate 本轮）；T-L012-2b.md（遗留段）
Changes:      ① K60 群 7 条目转写入 contracts/npm.yaml 第二批（K60-1..K60-6 + §5-1 环境流量条）；
              ② normalize.yaml npm 域新增 session_mint 归一组（token placeholder / login id 值 placeholder / rev 超集记注）；
              ③ npm.yaml 首批 scope 行勘正（「K60 族归档」挂账 → 第二批覆盖）+ 双批覆盖统计；
              ④ tools/difftest/l0132/packument-latest-probe.sh 判别探针（a|b|diff 三模式——设计交付，未执行）
Files:        修改 docs/compatibility/contracts/npm.yaml（375→676 行，+7 条目）；
              修改 docs/compatibility/fixtures/normalize.yaml（npm.body.session_mint 组）；
              新建 tools/difftest/l0132/packument-latest-probe.sh（可执行，bash -n 过）
Tests:        探针自检（判定逻辑非实网执行）：合成 obs（A 重算/B 不重算 + p8 semver-max/no-recompute）跑 diff 模式
              → F1 GATE=DIVERGENT、F3 A=semver-max B=no-recompute、F2 四行投影齐出——判定分支按标准渲染（/tmp 产物即删）。
              真网零跑（票面：执行归 differential-qa，向 conductor 报备转派）
Commands:     ruby -ryaml -e 'YAML.load_file(...)' ×2 → npm.yaml OK / normalize.yaml OK；
              grep -n '^## \|^### ' docs/reverse/npm.md → §2.1(26)/§2.2(40)/§2.3(49)/§3(56)/§4(61)/§5(68)/§6(74)
              全部存在（evidence 锚核对过）；evidence 路径存在性 11/11 OK（T-394.md、diff 报告、4×curl-matrix、
              4×p1-publish 日志、npm.md）；契约面 grep：session.go servePing:42/serveWhoami:55/serveLogin:80、
              router.go npmCouchLoginExempt:1917/isCouchUserDocument:1881、route.go NE-08:6/-rev 分支:103 全命中；
              条目键面全集检查（python 脚本）：第二批 7/7 FULL（首批 4 条 error-arm 条目无 error_behavior 为
              L012-2a 既有设计——被指臂，不回改）
Outputs:      契约 7 份：npm/session-login-put、npm/session-whoami、npm/session-ping、
              npm/session-web-login-posture、npm/session-profile-token-family、npm/session-login-no409-invariant、
              npm/env-probe-npm-404；normalize 新增 1 组；探针 1 份；matrix 0 行改（建议回报）、台账 0 条改（建议回报）
Compatibility: 状态机变动（新增条目）：SPECIFIED 起点直落——VERIFIED 6 / SPECIFIED 1（零补证两源互证直上 VERIFIED
              依派单；SPECIFIED 1=profile-token-family 零活体证据）。全 npm.yaml 累计 15 条目：VERIFIED 14 / SPECIFIED 1。
              置信度分布（本批）：high 7 / medium 0 / low 0
Security:     authentication 断言面：login 双臂（gate 豁免 + body argon2id/头 principal）、whoami bearer|basic + 匿名 401、
              ping anonymous、v1/login 任意 4xx、profile 族 basic；防御臂断言：错凭据 present-but-wrong 一律 401
              绝不静默匿名、跨账户铸 token 403。凭据卫生：UAT admin 口令仅读自 gitignored .env.uat 用于只读探测，
              未写入任何交付物；探针沿 l0121 rig 惯例（wiretap env 注入 + 假 token userconfig，真凭据不落盘不进 argv 日志）
Performance:  无 timing/p95 断言面（K60 族无性能规格；ping/whoami 为轻端点——后段如立项性能契约再补）
Risks:        ① login 缺字段臂未裁定（DE 400 vs B 401——UNKNOWN 候选已回报，近不可达面）；② whoami 403 ACL 臂
              （T-394 注记 2）规格无行、A 未取证——adjudication 候选；③ K60-5 A 侧响应面零取证（差分批跑 m21-m24 格收）；
              ④ login A 侧 live wire 缺（DE 代理——差分批跑补双端 login 一腿后头面/逐字面可加密）；⑤ 探针未实跑
              （设计自检过；实跑归 differential-qa——双侧建仓前置已写入脚本头）
Blockers:     无（K60-5 无法免建仓只读取证：UAT 现存仓已拆（仅 docker-local）、建仓属写操作——留差分批跑，非阻塞）
Next:         转 differential-qa：① tools/difftest/l0132/packument-latest-probe.sh 执行（a|b|diff——向 conductor
              报备转派；F1 四分支裁定映射在脚本头）；② 差分批跑 K60 补格 m21（/-/v1/login A 侧）+ m22-m24
              （/-/npm/v1 家族双端）→ npm/session-profile-token-family 升 VERIFIED；③ 双端 login wire 一腿（头面捕获）。
              转 conductor 落账（本票不改账）：① login 缺字段 UNKNOWN；② login id/rev npm-blind INTENTIONAL
              （authority=npm.md §6 定案/T-393）；③ D12-R03 行翻 contract_ref；④ packument-latest-recompute
              依探针 F1 结果翻 BUG/resolved
```

## 断点快照（如被中断）

已完成：契约第二批 7 条目 + normalize session_mint + 探针脚本 + 全部验证门（YAML/锚/路径/路由面/键面/判定逻辑自检）。
未完成：无（票面范围全清）。
断点位置：无悬挂。
恢复入口：若 conductor 追加 K60-5 活体格，先跑 `tools/difftest/l0132/packument-latest-probe.sh` 同目录补 m21-m24 curl 格，再翻 `docs/compatibility/contracts/npm.yaml#npm/session-profile-token-family` status。
