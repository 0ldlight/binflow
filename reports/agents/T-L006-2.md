# T-L006-2 — 双审微票池五件（docker 域）

```
Ticket:       L006-2 双审微票池五件（L005-1 双审 10 条 non-blocking 归本域清偿）P1
Role:         dev-registry-adapter（领域实例：dev-package-docker）
Area:         internal/adapter/{docker,helmoci} + 票面具名的 repo 面 seam/doc 两处（internal/repo/dockervirtual.go:121-123 与 dockerremote.go:327-330——ticket 1/2 明点名，contract §7「ticket 明确允许」支）
Input:        T-L005-1-review-b.md（10 条 non-blocking 清单）；reports/compatibility/L005-c14-d1-diff.md §3.1/§3.2/§3.5（tag 形键法与差分口径）；L004-negative-cache-probe.md（M-a/M-b 臂型）；docs/reverse/helm.md §8.3（过时句定位）；docs/compatibility/contracts/docker-remote.yaml（remote-manifest-404-no-negative-cache / 两个 conditional-get 条目——unobserved_reference_arms 现状）；adapter SPI internal/repo/api.go V2VirtualPlane
Changes:      ① seam 读探针：V2MemberManifest 的 remote-成员 !ok 臂（原「no rows 即 RemoteProbeMiss」）先探 C14 引用键负缓存行——tag→<image>/tags/<tag>、digest→manifest 节点路径（与 adapter manifestMissNodePath 写侧同拼写，注释互相锚定）；fresh 行→RemoteProbeNegative（walk continue 零上游），否则维持 RemoteProbeMiss；probe 错误如实上抛 ② CacheRemoteMiss doc 改准（digest→manifest 节点路径、tag→<image>/tags/<tag>，替换「Digest-keyed paths only」过时句） ③ star 未观察臂：两个条件矩阵的 star 行加 If-Modified-Since 组合腿（IMS 单独可 304、star 在场必 200——真正锁住「任意 INM 在场封死日期臂」）+ 文件头注明 unobserved、沿用 blob 面外推 ④ 过时注释三处：adapter remote.go 404 臂与 virtual.go 404 臂的「digest-keyed paths only」改准（双键法+臂位区分）；virtual.go 冷 miss 写行注释由「seam does not re-probe tag-keyed rows yet」改为「seam probes the row on the next ask (L006-2)」⑤ 差分补臂：by-digest 冷 miss ×3+到期复问双端活体（见 Tests/Outputs）
Files:        修改：internal/repo/dockervirtual.go（!ok 臂探针+方法 doc）、internal/repo/dockerremote.go（CacheRemoteMiss doc）、internal/adapter/docker/remote.go（404 臂注释）、internal/adapter/docker/virtual.go（404 臂注释+冷 miss 写行注释）、internal/adapter/docker/client_conditional_test.go（star 两行+头注）、internal/repo/dockervirtual_test.go（新增表驱动 seam 测试）；新增：internal/adapter/docker/virtual_cold_miss_negative_cache_test.go、reports/compatibility/L006-by-digest-cold-miss-diff.md、本日志。删除：无
Tests:        repo 侧 TestV2MemberManifestColdMissNegativeRow（6 臂表驱动：tag/digest × fresh/expired/无行；行窗挂 env 冻结时钟 e.clk——真墙钟会被冻结时钟判 future）；adapter 侧 TestVirtualColdMissNegativeCache（全栈 walk：tag 冷 miss ×3 上游恰 1、digest 冷 miss ×3 上游恰 1、60s 到期重问 +1 再冻结、miss 行落成员键空间、已知 tag 穿透拉取不受污染——**跨包键法锁定**：写侧 adapter manifestMissNodePath、读侧 repo seam 拼写漂移会静默退化，本测试端到端锁死）；变异验证：stash 掉 seam 补丁后 walk 测试翻红（digest ×3 上游 3 联系 vs 1）——区分性成立；star 组合腿在「实现若评估 IMS 则 304」的假想旧实现下必翻红
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter internal/repo（空）&& ~/go/bin/golangci-lint run internal/adapter/docker/... internal/adapter/helmoci/... internal/repo/...（0 issues）；go test ./internal/adapter/docker/ ./internal/adapter/helmoci/ -count=1（ok 24.9s/17.4s）；go test ./internal/repo/ -count=1（ok 84.2s）；race 子集：docker{ColdMiss/Conditional×2/ManifestColdMiss/VirtualBranch}+helmoci TestVirtual*+repo{ColdMissNegativeRow,T365*} 全 ok；差分腿命令原文见 L006-by-digest-cold-miss-diff.md §6
Outputs:      差分关键输出：try1 :8083→404/480ms 上游 GET×1（UA binflow-remote/1.0）、:8082→404/21ms 上游 HEAD×1（UA Artifactory/7.161.20）；try2/3 双端 8-22ms 本地、上游计数冻结 1/1；60s 到期复问双端各 +1 且 BinFlow 行重写 19:36:46Z→19:37:46Z（重写时刻=复问时刻、窗长恰 60s）；404 体双端逐字节同（仅尾换行——既有归一规则内）；sqlite 行：l0062/busybox/manifests/9850fa23…|negative（digest→manifest 节点路径键法活体直证）
Compatibility: 契约对照：C14 键法（side_effects 已载 tag/digest 双拼写）本票补 by-digest 活体臂；virtual 面读探针使 walk 与直读面跨面一致（同一成员行、同一 missedTTL 窗）。可差分 surface：① /v2/<virt>/<image>/manifests/<tag-or-digest> 冷 miss 重复 ×3（virtual 面——上游计数 1、二问起本地）② 直读面 by-digest 冷 miss ×3+到期复问（本轮已跑，报告在案）。漂移点：无新增；N1（首问动词 HEAD vs GET）维持既有登记
Security:     missPath 由已验证的 image/tag（边缘 validateDockerImage/validateTag/parseV2ReferenceDigest）拼接——无新输入面；probe 走 probeRemoteV2Core（成员键空间、membership-guarded 既存路径），不触网；负缓存行键空间仍限本成员 repo 行（无跨仓注入面）
Performance:  !ok 臂新增一次 GetCache 点查（+至多一次 Nodes 点查，仅 miss 态走）——sqlite 主键查询，量级可忽略；命中负行时省一次整上游会话（净收益）。其余无
Risks:        跨包键法（adapter 写侧/repo 读侧两处拼写）靠 walk 测试行为锁定而非单一事实源——若后续某侧单改键法，测试会翻红但需人工定位；star 未观察臂已注释+建议契约注记，活体取证仍缺（需一次带 `*` 的参照捕获）；virtual 面 by-digest/tag 冷 miss 的活体差分未跑（本票差分腿为直读面——walk 级 httptest 已覆盖机制，virtual 差分待后续差分票）
Blockers:     无
Next:         ① compatibility-engineer：docker-remote.yaml 两 conditional-get 条目 unobserved_reference_arms 增 star 腿注记；remote-manifest-404-no-negative-cache 增 known_image_digest_miss 臂（evidence 引 L006 报告）② reverse-engineer：helm.md §8.3 过时句改法（建议文本见下）③ 差分票候选：virtual 面冷 miss 双端对照（walk 机制已 httptest 锁定，活体面待采）④ C14 virtual seam 缺口三处转票注记（L005-1 报告 §3.5 尾注）随本票 #1 关闭，可请 conductor 更新台账
```

## 附：reverse 文档建议文本（docs/reverse/helm.md §8.3，归 reverse-engineer，本票不直改）

现文（过时）：

> 上游 404：digest 键路径写负缓存（missedTTL 窗口内零回源应答 404）+ 过期副本续serve；**tag 404 不写负缓存**（tag 无存储键，直接 404）。

建议改为（对齐 C14/L005-1 后行为）：

> 上游 404：写负缓存（键按引用形态——digest 引用键 manifest 节点路径 `<image>/manifests/<hex>`、tag 引用键 `<image>/tags/<tag>`；missedTTL 窗口内重复 miss 零回源本地应答 404）+ 过期副本续serve；TTL 到期回源 +1 并重写行。

（§8.4 virtual 段无需改——「remote 成员先读缓存事实…NEGATIVE 跳过」的描述在 seam 补探针后于冷 miss 态同样成立。）
