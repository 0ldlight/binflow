# T-514 工作日志 — FR-153.2/.3 FE bundle 面 + Any Distribution 预置勾选（缺位解除③并腿）

- 票：T-514（M17 W7）——BundlesPage 新建 + 权限两步弹窗三预置桶勾选腿；deps T-513/T-491 已 done。
- 角色：dev-frontend ｜ area：`web/src/pages`（bundles 新页组）+ `web/src/pages/security`（勾选腿）+ 导航挂靠（AppShell/NavIcons/main.tsx——T-459 监控组先例）+ i18n 新域 + 锚册三文件回写。
- 状态：done（候收编）。分支 develop 工作树直改，零 git 操作、BOARD 零写。
- 日期：2026-09-07 ｜ 环境：nvm node v24.20.0。

## 0. area 映射核对

实际落点 = 票面 area 全覆盖；未触碰共享组件层除 NavIcons（新图标一枚——导航挂靠票内明示，T-459 先例）。**并行 lane 共租留痕**：工作树内 `internal/{httpapi,search,repo}` 的 T-511（AQL build 域）在途文件与本票零交集（git status 双清单核对）；`en/monitoring.ts`/`zh/monitoring.json` 的既有未提交改动系 T-494 收编侧 i18n 填充——本票 regen 逐键保值复核（`diff <(git show HEAD:…|sort)` 键集逐字一致，仅 11 键位置重排 = regen 工具 canonical 序，零值变化）。

## 1. 页面形态定案依据（BundlesPage）

- **导航挂靠（票内定案）**：应用模式「应用」分组第三条目 `/bundles`（制品之后）——依据链：console-ui §1.1 应用域族（Artifactory 展开族 Packages/Builds/Artifacts；OSS 7.84 册无 Distribution 不列 Release Bundles 条目）+ 官方文档归属（/ui/repobundles 属 Artifactory 应用域）→「Artifacts 域」出口；7.161 活体双坏在案（t226/7.161 均不可用——以册+官方文档为准，票面预案）。专域名词不译（Access Tokens / Webhooks 先例）；图标 `bundle` 自绘两屉堆叠（NavIcons 自绘 pulse 同款纪律）。
- **三视图一组件**（ArtifactsBrowser 先例——`/bundles`〔名单〕→ `/bundles/:name`〔版本单〕→ `/bundles/:name/:version`〔描述符〕，URL 即状态）；消费 T-513 查询族：GET /api/release/bundles（名单，200 {uri,bundles[]}）/ +/{name}（版本单）/ +/{name}/{version}（描述符 {uri,info}）+ HEAD 同址（X-Checksum-Sha256——E5 锚定面，独立 probe 行呈现 64 hex + 拷贝）。
- **四态**：loading 骨架 / error ErrorCard+重试 / 名单空态（EmptyState+插画位——服务端可见集过滤如实，不伪造 403）/ ok 三级表格（名 mono 链接、state 徽章 COMPLETE=success/INPROGRESS=warning/闭集外 default、created UTC mono）。清单大表走 useClientPager（T-451 共享 Pager 页窗）。
- **mono+拷贝**：signature（清单身份摘要占位——title 注 E6 语义）/ HEAD 校验和 / 清单行 sha256（12 字截断显示+全值拷贝）。**pending 行（sha256=''）如实呈现 —**（title 注「制品尚不在本实例」——T-513 容忍缺位政策），size=0 同 —，不伪造成校验值。
- **创建面缺位登记（不伪造）**：名单页 admin-note 引导 API（POST /api/release/bundle，显式清单形 + release-bundle 槽位门）；控制台创建面归后续票（票面「FE 主要消费查询面——创建腿可后续」）。
- t226 活体不可用（双坏在案）→ 形态以 release-bundle.md §1 端点族 + T-513 wire 为书面锚（票面明示）。

## 2. 三桶勾选腿（PermissionEditorPage / B-2.16 缺位解除）

- **预置行**：ResourceDialog 选仓步穿梭首列三预置——键 = wire 字面 `ANY LOCAL`/`ANY REMOTE`/`ANY DISTRIBUTION`（T-491 闭集），展示 = console-ui §3.8 活体拼写 Any Local / Any Remote / Any Distribution（TransferBox 新增 `label` 槽：键 ≠ 展示，锚 `perm-repo-pick-ANY LOCAL` 等 = 冻结族 `perm-repo-pick-<key>` 扩桶字面成员）；note 一行注覆盖语义（local/remote 按 class 含后建仓；ANY DISTRIBUTION = bundle 域通道按名生效）。
- **同场混选**：预置与具体仓库可同列同勾（e2e 断言四条同场 + payload repos[] 网络层对账 + GET echo 逐字 + 重开弹窗水合回填 + 勾选可撤销）。
- **m-holder 403 分支**：桶照常可勾——桶字面是客户端常量，不依赖被 403 的仓库目录端点（服务端收词 + 覆盖集按字面键终裁——T-491 §1 ManageCoverage 字面键语义进注记）。
- **注记翻新**：T-455 期两处「通配桶不建：repos[] 必须逐个指名现存仓库」admin-note 退役 → `perm-buckets-note`（两分支各一，语义注：ANY 家族互不隐式覆盖、virtual 无桶、Any Distribution 的 include/exclude 路径位 = bundle 名）；主页 chips 桶字面加 title 注记；文件头注释同步。
- **授予面覆盖断言**（e2e ②）：`ANY DISTRIBUTION` + read 授予 → 被授权用户 /bundles 名单见 bundle → 版本单（INPROGRESS 徽章）→ 描述符（signature/HEAD 校验和/快照行 sha256/pending 行 —）三级钻取 + axe 双主题；未授予用户名单空集 + 版本面/描述符面 API 直探 403 + 描述符 UI `bundle-denied` 卡。

## 3. 契约漂移（1 项——conductor 逼 architect 回写）

**T-513 报告 §1.4 端点表「GET /api/release/bundles/{name}：空可见集→404（零泄漏法）」与实态不符**：实态 = 版本面对无读门会话 **403**（`service.ListBundleVersions` 首行 canRead 拒绝即 ErrForbidden——build 族「拒绝即答案」姿态，代码 internal/bundle/service.go:462-468），404 仅两臂：CapSystemRead 持有者的真缺（e2e ④ admin 臂实测 404）或可见名零版本。描述符面同姿态（GetBundle:409-417）。名单面才是 200 过滤零 403 oracle。FE 按实态实现（版本/描述符面各设 forbidden→`bundle-denied` + 404→`bundle-not-found` 两分支），BundlesPage 文件头与 e2e 注释均留痕。**建议**：architecture §7.1 路由表或 T-513 报告勘误一行（403/404 分界），归 conductor。

## 4. 缺位解除归位登记（登记态/实态双留痕）

| 登记处 | 原（缺位态） | 现（解除态） |
|---|---|---|
| parity 册 B-2.16 | 「Any Local/Any Remote 预置未建 = 契约缺口…候裁」 | 「三预置桶 T-514 已落——B-2.16 缺位解除」（含授予面探针指针） |
| parity 册 §9A-S8 | 「Any Distribution 预置：维持缺位登记（域落地后随域解禁）」 | 「部分解禁：Any Distribution 预置 T-514 已落；Module ID/Project 列/Realm 列维持缺位」 |
| console-ux §0 v1.47 + §10.5 T-514 批 + 路由表 /bundles 行 | （无条目） | 16 名新锚 + 冻结族扩成员入册；锚册头「票据/状态」行同步 v1.47（T-464 漏更的头行顺带补） |
| anchor-audit STOP | — | v1.47 批两词（spec-pending / t514-bundles-presets——行文假阳性） |
| i18n | — | 新域 `bundles`（22 键，webhooks 一页组一域先例）+ security 域 +6/−2 键（两「不建」注记键退役） |

## 5. 双语键增量

- zh-as-key 全量过 `assert-i18n` 三闸：**OK（零硬编码中文；2,641 调用点 / 2,008 键，en 包与 zh 清单同构）**。
- 新键 en 全填充：bundles 22 + security 6（逐义对译，bundle/repo key/sha256/HEAD/pending/INPROGRESS 等术语保真）；生成走 `scripts/t463/regen-catalogs.mjs`（逐键保值）+ 手工填值。
- i18n 基建三文件 +域：index.ts I18nDomain / assert-i18n.mjs DOMAINS / regen-catalogs.mjs DOMAINS / catalogs.ts import / manifests/zh/bundles.json。

## 6. 自测（实际执行——命令 + 结果）

环境：测试实例 = `npm run build` → internal/console/dist 同形 embed + `CGO_ENABLED=0 go build -o /tmp/t514-bin/binflow-server`，scratch 净实例 127.0.0.1:**18200**（sqlite，admin/password；净窗纪律端口段）。binary strings 验证 bundles-row/bundle-detail-page/perm-buckets-note/ANY DISTRIBUTION 在 embed 内（命中 7 处）。

1. **tsc**：`npx tsc --noEmit` → 零输出（EXIT=0；裸跑取真实退出码）。
2. **lint**：`npx eslint <本票全部触碰文件>` → 0 error / 7 warning 均存量（AppShell step-up effect + refs-during-render 族、PermissionEditorPage 水合 effect——`git show HEAD:` 同文件基线同形，T-455 §5.2 已登记）。`npm run lint` 全仓 36 warning（0 error）+ assert-i18n OK，EXIT=0。
3. **build**：`npm run build`（assert-tokens + assert-i18n + vite build + relink + wire-brand）✅。
4. **锚册**：`node scripts/anchor-audit.mjs --ledger` → A2 broken=0 / A3 A4 零违例；**A1 残留 4 名 = logs-degraded / logs-truncated（T-494）+ users-columns-item-lastlogin / users-sort-lastlogin（T-492）——`git show HEAD:` 证明四锚在 HEAD 已存在**（已收编票的锚册回写欠账，非本票引入；T-492 报告「锚册待回写——conductor 收口」在案）。
5. **SPA 体积**（T-459/T-492 同口径：dist js+css gzip 合计，HEAD 基线经 git worktree）：**504,660B → 509,886B = +5,226B（5.1KB）≤ 10KB/票 ✓**。分项：BundlesPage 新 chunk 9,861B raw / gzip 3.07KB（≤10KB）；en 资源包 +4,246B raw（NFR-P73 单独登记口径）；PermissionEditorPage +809B；AppShell +165B / index +330B / api +194B / TransferBox +9B。
6. **e2e（净窗 18200）**：
   - 新面 `e2e/m17/t514-bundles-presets.spec.ts`：**4 passed**（37.9s）——① 三桶同场勾选+payload/echo/水合/撤销 ② Any Distribution 授予面覆盖探针（三级钻取+双主题 axe〔描述符〕+未授予空集/403/404 分界+bundle-denied UI）③ 导航入口+名单+双主题 axe ④ 版本单 404（admin 真缺臂）。pro license bin/bf 自铸装/卸（t461 先例；serial 单 worker 无需租约）；**bundle 无删除端点（E-26）→ 夹具 bundle 在实例长存**（uniq 防撞；一次性测试数据目录）。
   - 回归 `t455-perm-five-columns + security + m8/permissions + rbac`：**18 passed**（54.8s）。
   - 回归 `--project=m9 permissions-mholder`：**4 passed**（16.0s）；`m8/users-groups + auth-shell + console-smoke`：**11 passed**（34.2s）。
   - 回归 `m8/shell + m8/a11y-sweep`：**8 passed**（2.7m）——shell.spec 应用侧栏计数 3→4 随刻意变更（T-514 第三条目 + 图标 2→3，spec 注记在行）。
   - 首轮两红均 spec 侧笔误已修：① OK 钮在步 2 footer（步 1 是 Next——补步头直跳，t455 同链）；② 403/404 分界按实态改判（§3 契约漂移的发现源）。
7. **服务端 diff=0**（纯 FE 票——git status internal/ 零本票触碰；T-511 在途文件属并行 lane）。

## 7. 变更清单

**生产代码（9 文件 + 2 新）**

| 文件 | 一句话 |
|---|---|
| web/src/pages/bundles/api.ts（新） | 查询族四函数（名单/版本/描述符/HEAD 校验和）+ wire 类型 + 读门语义注释 |
| web/src/pages/bundles/BundlesPage.tsx（新） | 三视图一组件：四态/403 与 404 分立承载/pending 行如实/mono+拷贝/Pager 页窗 |
| web/src/components/AppShell.tsx | APP_NAV 第三条目 Release Bundles + appTitle 分支 |
| web/src/components/NavIcons.tsx | `bundle` 图标（自绘两屉堆叠） |
| web/src/main.tsx | 三路由 /bundles{,/:name,/:name/:version} 挂载 |
| web/src/pages/security/PermissionEditorPage.tsx | 三预置桶勾选腿（两分支）+ 两「不建」注记退役 + chips title + 文件头注释 |
| web/src/pages/security/TransferBox.tsx | TransferItem 增 label 槽（键=wire 字面/展示=活体拼写） |
| web/src/pages/security/api.ts | WILDCARD_BUCKETS 常量 + isWildcardBucket + 语义注释 |
| web/src/i18n/index.ts + locales/en/{bundles(新),security,catalogs}.ts + manifests/zh/{bundles(新),security}.json | bundles 域 22 键 + security +6/−2（en 全填充） |

**e2e（2 文件）**：m17/t514-bundles-presets.spec.ts（新，四腿）+ m8/shell.spec.ts（侧栏计数随刻意变更）。

**登记/工具（6 文件）**：console-ux.md（v1.47 + §10.5 T-514 批 16 锚 + 路由表行 + 头行）、console-artifactory-parity.md（B-2.16 翻正 + S8 部分解禁）、anchor-audit.mjs（STOP +v1.47 两词）、assert-i18n.mjs / regen-catalogs.mjs（DOMAINS +bundles）。

## 8. 遗留

1. **契约漂移 §3** 待 conductor 逼 architect 回写（T-513 报告「版本面 404」→ 实态 403/404 分界）。
2. bundle 创建面（控制台表单）归后续票（票面预留；名单页 API 注记引导）。
3. A1 残留 4 锚（T-492/T-494 回写欠账）——conductor 收口时顺腿入册即全绿。
4. `ANY`（Anything）第四桶维持 wire 拒收（T-491 spec-pending）——FE 桶清单同步不含。
5. t226/7.161 活体恢复后可复测 Add Repositories 预置行的呈现细形（本票以册 §3.8 高置信行为锚）。
