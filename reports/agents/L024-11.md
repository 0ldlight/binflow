# L024-11 — D01 尾差分四 BUG + 两小面（批次 2 最后一返）

Ticket:       L024-11（微返工：L024-10 尾差分 T1-T6——metadata 面三臂 + stats 真 merge + archive!/ 两小面）P1
Role:         dev-go-core（area = internal/{httpapi,repo,metadata,adapter/generic} 增量）
Input:        reports/compatibility/L024-d01-tail-diff.md（权威——§2 D-item 详单）；wire 证据 reports/compatibility/l024e-wire/{a,b}/（m09b/m10c/m10d/m14/a04/a01/a05 逐字核对）
Changes:      按报告 §2：
              1) **T4 stats 腿真 merge**（§3.1-10 低置信 no-op 证伪）：metadata.NodeStore 新增 MergeStats（StatsMerge 按字段绝对 SET——nil 字段保值，JSON-merge 非 replace-whole；folder 标记行排除；missing 行静默）；PATCH stats 臂解析数值字段（downloadCount/remoteDownloadCount；非数值静默跳过——item 7 同律），body 未命名 lastDownloadedBy 时落 **"import" 标记**（wire：downloadCount:1 → by:"import"）；目标解析与 props 臂共用（PATCH 守卫不变——m06/m07 的 400 只证 PATCH 面）。
              2) **T4 stats 回显三面**：?stats 的 uri 改 **下载形**（`<base>/binflow/<repo>/<path>`，非 api/storage 形）；六字段恒回显（lastDownloaded 改 **epoch-ms 数字**、零值不省键——`"lastDownloaded":0`）；remoteLastDownloaded 无存储列回结构零 0（登记）；lastDownloadedBy 维持 K69 manage 门。
              3) **T1 DELETE 无守卫**：metadataDeleteTarget 新解析——missing item / 非 local 仓 / 未知仓均 **204 静默 no-op**（撤 PATCH 族 400 文案借用）；401/403 门维持。
              4) **T2 其它动词**：PUT/GET/POST on /api/metadata → **405 envelope `{"errors":[{"status":405,"message":"Method Not Allowed"}]}` + `Allow: DELETE,OPTIONS,PATCH`**（非 E-26 404）。
              5) **T3 `!`-无斜杠 miss**：generic 面 notFoundMessage 在 relPath 含 `!` 时回 **`File not found.; Path: '<repo>:<archive>!<entry>'`**（冒号 Path 尾、`!` 原样；§3.3-3 的 generic/maven 同形定谳）；无 `!` 的普通 miss 维持 §1.4 文案；含 `!` 的**命中**不受影响（普通文件名路径照常 200）。
              6) **T5 成员命中六头族**：ArchiveMemberResult 增 Filename/Md5/Sha1/Sha256；服务侧缓冲成员（**32MiB 上限**——超限走流式无 checksum 头，登记）计算三 digest；handler 发六头（Accept-Ranges: bytes / Content-Disposition 双 filename 拼写 / X-Artifactory-Filename / X-Checksum-Md5/Sha1/Sha256）——checksum-suffix 臂不发（digest 文本面）。
              7) **T6 sha1 后缀 CT**：`application/x-checksum`（原 text/plain）。
              8) T7（Allow/Cache-Control 头 drop 提案）/T8（401 措辞族）未动（归 normalize/既有裁定）。
Files:        修改：internal/metadata/{api.go,substores.go}（StatsMerge+MergeStats）；internal/httpapi/metadata_patch.go（T1 守卫撤+T4 stats merge）；internal/httpapi/storage.go（stats 回显 uri/六字段/epoch 数字）；internal/httpapi/router.go（T2 405+Allow）；internal/httpapi/archive.go（T5 handler 六头）；internal/repo/archive.go（T5 结果字段+缓冲 digest+T6 CT+memberChecksumCeiling）；internal/adapter/generic/handler.go（T3 miss 族）。
              测试对位：metadata_patch_test.go（DELETE 失败臂翻 204/T2 新用例 TestMetadataOtherVerbs405/stats 臂翻 merge+回显断言）；t438_stats_test.go（statsBody lastDownloaded 改 int64+uri 下载形+remote 对断言翻结构零）；t343_archive_test.go（T3 miss 断言翻 File-not-found 形/T5 六头+sha1 值断言/T6 CT）；repo/archive_test.go（checksum-suffix CT 翻 application/x-checksum）。
Tests:        新增 TestMetadataOtherVerbs405；存量四文件对位（全部按差分权威翻转旧断言——T1 的 400 借用臂、T3 的「generic 保持 §1.4」注释与断言、T6 CT、stats no-op 臂）。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/{repo,metadata,adapter/generic}/ -count=1 -timeout=25m（ok 75.5〔archive 对位后复跑 ok〕/29.3/10.0s）；go test ./internal/httpapi/ -count=1 -timeout=25m 全量（**ok 197.1s**）；go test -race httpapi 触碰面（TestMetadata/T438/T343Member/TestStorageStats，ok 25.6s）+ repo archive 族（ok 13.4s）；冒烟：隔离实例 127.0.0.1:18092（独立 data-dir，零 docker，用后清除）。
Outputs:      冒烟逐臂（对报告 wire）：S4(=m09/m09b) PATCH `{"stats":{"downloadCount":1}}` → 204；`?stats` → `{"downloadCount":1,"lastDownloaded":0,"lastDownloadedBy":"import","remoteDownloadCount":0,"remoteLastDownloaded":0}`——与 a/m09b 六字段逐字同（uri 下载形）+ by="import"；S5(=m10c/m10d) DELETE missing item / virtual 仓 → **204/204**（B 原 400 借用臂消除）；S6(=m14a/b/c) PUT/GET/POST → **405 + `Allow: DELETE,OPTIONS,PATCH`** + envelope 逐字（B 原 E-26 404 消除）；S3(=a04) `pkg/l024e.zip!entry.txt`（裸 `!` miss）→ 404 `File not found.; Path: 'l02411-local:pkg/l024e.zip!entry.txt'`（A 形逐字——contextPath 外零差）；S1/S2(=a01/a05) 六头族+CT 经 **T343 installPro harness** 行为臂全断言（Accept-Ranges/双 filename 拼写/X-Artifactory-Filename/三 checksum 头+sha1 值核对/`application/x-checksum`）——community 冒烟实例对 archive 面诚实 403（repo-operations 闩），与 L024-8 同法（路由经真实二进制触达，行为臂由持证 harness 覆盖）。
Compatibility: 对照报告 §2 六项全落：T1（DELETE 失败臂无守卫——低置信①定谳）、T2（405+Allow——低置信④）、T3（File-not-found 族——低置信③+repo 类型疑虑关闭）、T4（stats 真 merge——升「高-活体」，报告 §3-1 spec 输入）、T5（六头族）、T6（x-checksum CT）；T7/T8 未动归裁定。**登记**：① remoteLastDownloaded/remoteLastDownloadedBy 无存储列——前者回结构零、后者维持省键（A 样本未载 by 字段，无列无样本不猜）；② stats merge 的 lastDownloaded/remoteLastDownloadedBy 字段未实现（wire 无样本——body 命名 by 的臂已实现）；③ 成员 >32MiB 走流式无 checksum 头（A 恒发——大制品臂未活体，内存上限的裁量）；④ PATCH stats 的目标守卫沿用 PATCH 族（m06/m07 只证 DELETE 面无守卫——stats-only PATCH 打 missing item 的行为未探）。
Security:     stats merge 仍走 a(annotate) 门（PATCH 403 前置不变）；DELETE 无守卫面不放大权限（no-op 只删属性，目标不可达即无操作）；405/Allow 纯路由面；成员缓冲在 32MiB 上限内（DoS 面）。
Performance:  成员命中新增一次全量读（≤32MiB 缓冲算三 digest——命中路径代价一次性；A 同为计算面）；stats merge 单 UPDATE；零其余新面。
Risks:        ① 大成员无 checksum 头臂（登记③）——差分复跑若用大制品会现差异；② stats body 命名字段臂（登记②）未证；③ metadata PATCH stats 守卫沿用（登记④）。
Blockers:     无。
Next:         ① 差分复跑（d01_tail_diff.py 同 31 单元）验证 T1-T6 全 SAME；② D01-R08/R16 翻态终判（候裁→候选）；③ 登记①-④ 候探针。
