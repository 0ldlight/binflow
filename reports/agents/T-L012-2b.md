# T-L012-2b — npm dist-tags 面实现票（LOOP 012 轨道 2b）

```
Ticket:       L012-2b · dist-tags 面实现（六项 conductor 裁定逐格修）· P0
Role:         dev-registry-adapter（领域实例：dev-package-npm）
Area:         internal/adapter/npm
Input:        reports/compatibility/L012-dist-tags-evidence.md（§2 20 格矩阵、§3 D2-D7 裁定输入、E0-2 缓存注记）；
              docs/reverse/maven-npm-pypi.md 既有钉文案（msgTagNotFound 族，本票不改其既有格）；
              本包既有 SPI（api.go 路由、packument.go 装载、virtual_packument.go recomputeLatestTag 先例、semver.go latestVersion）；
              无 docs/compatibility/contracts/npm.yaml（契约未落，票面即裁定载体——按证据报告+conductor 六项裁定实现，本日志声明此态）
Changes:      D2 serveDistTags GET/HEAD 读时重算 absent latest（latestVersion 现算；present latest 永不覆盖；纯读时投影不动存储文档）；
              D3 新 msgTagVersionNotFound（PUT 坏版本 404 文案版本位：and version:<v>）；
              D4 writeTagsLookupError 幽灵包 404 → 裸 "Not found"（dist-tag 面专属，参数 name 移除）；
              D5 单 tag/legacy PUT 坏 body → 400 "invalid dist-tag body"（剥掉 err.Error() 内幕泄漏）；
              D6 集合 PUT/POST + 单 tag POST → 405 "Method Not Allowed"（+Allow 头；删除 bulk 实建分支）；
              D7 GET dist-tags 补 Cache-Control: max-age=60；
              client_test.go M24 rm 腿加 --prefer-online（参照复证陈旧窗后的事实性适配，见 Compatibility）
Files:        修改 internal/adapter/npm/{disttag.go, errors.go, disttag_test.go, client_test.go}；
              新增 internal/adapter/npm/{disttag_recompute_test.go, disttag_wire_alignment_test.go}；
              产物 reports/compatibility/{L012-dist-tags-diff.md, l0122-wire/**}；运维态 deploy/compose/.env.uat（UAT 版本号，gitignored）
Tests:        TestDistTagReadFaceRecomputeLatest（两子格：仅删 latest / 全 tag 删空——npm ls 不再 No dist-tags found）；
              TestDistTagReadFaceKeepsExplicitLatest（显式旧 latest 存活）；TestDistTagRecomputeIsReadTimeOnly（存储文档不被投影污染+packument 面未裁定锚）；
              TestDistTagWireErrorAlignment（5 格表驱：m05/m07/m08/m09+legacy 400，含内幕泄漏反断言）；
              TestDistTagsCacheControlHeader（D7）；TestDistTagsBulkFacesRejected（m13/m14/m15 → 405+Allow+零泄漏）；
              TestDistTagsModernFamily/ScopedName/LegacyPut 既有回归保持
Commands:     go build ./... && go vet ./internal/adapter/npm/ && gofmt -l internal/adapter/npm（空）&& ~/go/bin/golangci-lint run internal/adapter/npm/...（0 issues）；
              go test ./internal/adapter/npm/ -count=1（ok）；-race -run "TestDistTag|TestDistTags|TestServeNPM|TestEscap"（ok）；
              go test ./internal/adapter/... -count=1（npm ok；helm/rpm 两处预存在失败与本票无关，stash 本票改动后仍失败）；
              docker build deploy/release/Dockerfile.alpine（工作树）→ compose 滚动 uat-l0122b-l0122；
              bash disttag-family.sh {a,b}（npm 10.9.8 15 用例经 wiretap）；bash curl-matrix.sh {a,b}；
              live 参照复证（:8082, npm 11.19.0）：dist-tag add 后立即 rm → "beta is not a dist-tag"（陈旧窗为参照自身行为）
Outputs:      CLI 腿 15 用例双端退出码向量逐 case 相同（diff _summary 零差）；notag-ls 双端均 200 {"latest":"1.1.0"} + Cache-Control: max-age=60（D2+D7 双绿）；
              badver PUT 404 文案双端逐字一致（and version:9.9.9）；ghost 404 "Not found" 双端一致；
              curl 20 格：15 格归一后逐字全同 + 5 格裁定内差异（m02 实例配置 / m08,m09 D5 / m10,m18 K60 401 大小写）——零未裁定 drift（L012-1 时为 8 格实差）
Compatibility: 见 reports/compatibility/L012-dist-tags-diff.md 终版（§3 终判分布 + §6 契约票蓝本）；可差分 surface：GET dist-tags（200 map+CC:60 / 404 Not found）、
              PUT 单 tag（201 {"ok":"created new tag"} / 404 and version:<v>）、DELETE 单 tag（200 空 / 404 and tag:<t>）、bulk 全族 405、坏 body 400 invalid dist-tag body、%2f/%2F 等价；
              漂移点：D5 400-vs-500（A 自身瑕疵，建议 known-divergence）、401 文案大小写（K60）、匿名臂（实例配置）
Security:     路径面未动（parseRoute 既有严格校验）；无 remote 代理路径；凭据卫生：wiretap env 注入+redacted 保留，本票新增的 A 侧 scratch npmrc（base64 basic）用后即删；
              参照实例仅本机 :8082、探针仓跑后双删（B ?deleteContent=true + 复核 404）
Performance:  无新 I/O；D2 重算为 O(versions) 单次 map 扫（latestVersion 既有原语，publish/unpublish 同源）；响应体不变量级
Risks:        packument 面（GET /<name>）的 latest 重算未裁定未实现（D2 证据只覆盖 dist-tags 端点族；TestDistTagRecomputeIsReadTimeOnly 钉住现状，裁定后翻断言即可）；
              max-age=60 客户端陈旧窗（add 后 60s 内 rm 读旧值）为参照同款行为——已 live 复证并在 M24 注释记录，但 npm 未来版本缓存策略变化需跟踪；
              npm 10.9.8（评估锚）+ 11.19.0（Go 套件）双版本验证；更老客户端（pre-8 legacy PUT 拼写）仅 Go 桩覆盖、未真客户端回归
Blockers:     无
Next:         契约票（compatibility-engineer）：npm.yaml 首文件以 diff 报告 §1+§3 为蓝本，钉 D5=400+中性文案并记 A 侧 500 known-divergence；
              K60-2/K60-3/§5-1 零补证转写建议已入 diff 报告 §5（免另开取证票）；packument 臂 latest 取证一小条；
              D12-R05 翻绿判定交差分批跑（本票 20 格已零未裁定 drift）；金样采集候选见 diff 报告 §6.3
```

## 补记：预存在失败与边界遵守

- `internal/adapter/rpm` TestRestPolicyKeysDriveReindexBranches、`internal/adapter/helm` TestRestEnforceSwitchesDriveUploadHook 在工作树失败；stash 本票 npm 改动后复验仍失败 → 归因 L012-3 未提交的 internal/httpapi/{properties,storage}.go 配套测试未同步（该域禁改，未动）。
- 全程未碰 internal/httpapi、未 commit、未写 BOARD.md；tools/difftest rig 零改动（family 脚本以 sed 副本重定向日志目录，原件未动）。
- UAT 现值：uat-l0122b-l0122（工作树构建，含 L012-3 httpapi + 本票 npm）；.env.uat 已注明来源轨。
