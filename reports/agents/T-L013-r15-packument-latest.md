# T-L013-r15-packument-latest — packument latest 读时重算缺失（LOOP 013 小修票）

Ticket:       LOOP 013 小修票 / 台账 npm/packument-latest-recompute（BUG）——packument 投影 latest 读时重算缺失（证据 reports/compatibility/L013-r15-packument-probes.md §3 七步）
Role:         dev-registry-adapter（领域实例：dev-package-npm）
Area:         internal/adapter/npm
Input:        docs/compatibility/known-divergence.yaml#npm/packument-latest-recompute（review_gate 即本票）；reports/compatibility/L013-r15-packument-probes.md n1-n7（DELETE latest 后端点族重算回 1.1.0 而 packument 面 latest 消失）；tools/difftest/l0132/packument-latest-probe.sh 判定标准（F1 gate）；L012-2b D2 裁定（读时重算、present latest 不覆写）
Changes:      1) disttag.go：serveDistTags 内联重算提取为 crownLatest(doc)（D2 absent-only 语义原样：latest 缺席→crown 语义最高版；present 不覆写；dist-tags 缺失时按端点族原行为补建）；2) packument.go renderPackument：渲染副本上调 crownLatest——packument 投影与端点族同源重算（一处改动，覆盖 local + virtual 走 loadPackument 的全部 GET /:name 面，SLIM 协商路径同样生效——npm install 走的就是该投影）；stored 文档不动（皇冠纯读时投影）；3) 测试翻面：TestDistTagRecomputeIsReadTimeOnly 由「断言 packument 面 latest 消失」翻为「两读面（GET /:name + GET dist-tags）删 latest 后均 crown 1.1.0 + 二次 DELETE 404 证明 store 保留删除」；TestDistTagReadFaceKeepsExplicitLatest 增 packument 面 present-latest 不覆写断言
Files:        修改 internal/adapter/npm/disttag.go（提取+复用 crownLatest）、internal/adapter/npm/packument.go（renderPackument 加 crown）、internal/adapter/npm/disttag_recompute_test.go（翻面+双面钉住）；新增/删除：无
Tests:        disttag_recompute_test.go：两读面删 latest 后同 crown（packument 面=新增覆盖）、读时投影不落盘（二次 DELETE 404）、explicit older latest 两面均不覆写；既有 DistTag/Packument/Publish/Unpublish/Serve/Virtual 全量回归
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter && ~/go/bin/golangci-lint run ./internal/adapter/...；go test ./internal/adapter/npm/ -count=1；go test ./internal/adapter/npm/ -count=1 -race -run 'DistTag|Packument|Publish|Unpublish|Serve|Virtual'；go test ./internal/adapter/... -count=1；UAT 重建：BINFLOW_VER=uat-l0132-cc55428d-npmfix docker compose -p binflow-uat --env-file deploy/compose/.env.uat -f deploy/compose/docker-compose.yml up -d --build；探针：bash tools/difftest/l0132/packument-latest-probe.sh a &&（b 侧 wiretap 无法绑 8083——binflow-ga 独占绑定 127.0.0.1:8083 而 artifactory 是通配绑定；按票面授权改手跑同序列：npm 10.9.8 直发 env 插值 npmrc + 同名 obs curl 快照）→ bash tools/difftest/l0132/packument-latest-probe.sh diff
Outputs:      四门全绿（lint 0 issues）；npm 包 -count=1 ok 30.0s、race 子集 ok 59.7s、adapter 全家 14 包 ok；探针判定表：F1 A=1.1.0 B=1.1.0 **ALIGNED（GATE 绿）**；F3 重算基准 A=semver-max B=semver-max（发布序 2.0.0→1.0.2 删 latest 后双端回 2.0.0）；p3 sanity 双端 latest=1.0.0（钉面不覆写）；b/p5 packument `{"latest":"1.1.0"}`、b/p6 端点族 `{"latest":"1.1.0"}`——两读面矛盾消除；obs 存 reports/compatibility/l0132-packument/{a,b}/obs
Compatibility: F1 分支落 resolved 素材齐备：双端同源重算同值（ALIGNED）+ F3 基准双端 semver-max 一致 + p3 钉面一致；台账翻绿建议：npm/packument-latest-recompute → resolved，authority 追加本探针 obs；contracts/npm.yaml#npm/dist-tags-latest-recompute 的 unobserved 注记可回填「重算基准=semver-max（F3 双端实测）」——归 compatibility-engineer；F2 informational 差异（不 gate，契约票素材）：顶层键 A 多 `users`；单版本字段 A 有 lastModified/scripts、B 有 _nodeVersion/_npmVersion/readme；tarball 形态剥 origin 后仅差挂载前缀
Security:     无新面：crownLatest 纯内存投影不改存储；路径校验未动；探针凭据全程 env 注入，/tmp/l0132 已清（无 npmrc/缓存残留），双端 difftest 仓已删净
Performance:  crownLatest 为 O(versions) 内存扫描（与原端点族重算同量级），发生在已有 copyDoc 渲染副本上；无 I/O 新增
Risks:        覆盖 npm 10.9.8 wire（评估锚）+ curl 直发；npm 8/9 未实测（变更面是服务端投影，客户端版本无关）；UAT 镜像含并行票未提交改动（httpapi project-filter），B 侧腿实际验证的是叠加树；环境事故一次：Docker Desktop 在镜像构建中崩溃（重建后恢复，Artifactory 重启一次后 200），不影响判定
Blockers:     无
Next:         1) 台账 npm/packument-latest-recompute 翻 resolved（conductor）；2) contracts/npm.yaml 回填 F3 semver-max 注记 + F2 informational 素材开 packument 契约票（compatibility-engineer）；3) 台账 rationale 与 l0132 探针头第 7-8 行对 TestDistTagRecomputeIsReadTimeOnly 的转述需勘误（实际断言与「同源重算」相反）
