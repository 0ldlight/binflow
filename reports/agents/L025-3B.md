Ticket:       L025-3B — LOOP 025 票乙：D02 batch 写族 + 207 状态机（按派发单）
Role:         dev-go-core（area = internal/httpapi）
Area:         internal/httpapi（路由 + 新处理器 + 测试 + 既有守卫测试的台账递增；未触其他包）
Input:        docs/reverse/rest-api.md §2.1（L025-1 冻结版）之 §2.1.5 PUT 批建、§2.1.6 POST 批改、§2.1.7 DELETE 批删 207 状态机、§2.1.0 端点表行 5-7；票甲 L025-3A（commit 7661853b）的挂载与方言基座（repoBatchItemsLimit 常量、requestMediaType/v2ConfigVendorType vendor 协商件、writeJSONBody/writeText/writeError 渲染族、seedConfigFamilyRepo 测试基建）；ADR-0050 update-merge 方言（handleRepoPost/configJSON/UpdateRepo 既有语义）；本票活体探针（172.16.58.130:8082 参照 7.161.x，2026-09-17）
Changes:      ① 新增 internal/httpapi/repo_batch_write.go：handleRepoBatchPut（批建——全有或全无：名字校验先行〔缺 key/已存在 key/批内重复逐条收集、`\n` 连接单条 message〕，创建段 federated-first 稳定分派〔规格字面，BinFlow 拒 federated rclass 故行为空转，登记候差分〕，创建期失败回滚已建前缀，201 text/plain 体=每仓 `Successfully created repository '<key>' \n\n` 逐字节）；handleRepoBatchPost（批改——resolve 先行：ghost key 或 vendor CT 过滤落空 → 404 裸文本 `No repositories found for the following keys: <a, b>`〔CT application/json 非信封〕，逐仓 CanManageRepo(write) 聚合未授权清单 → 403 裸文本 `User is not authorized to update the following repositories: <a, b>`，逐项 ADR-0050 merge〔description 键在场合并 + remote 臂 baseline 合并——handleRepoPost 同语义〕，批内失败恢复已触项快照〔官方 entire-batch-fails 姿态〕，成功 200 裸串 `Repositories updated successfully.`）；handleRepoBatchDelete（207 状态机——body=JSON 字符串数组去重保序，空/缺/坏 body → 400 `{"statusMessage":"No repository keys were provided for deletion"}` 无 reports 键；预校验整单中止臂：无删除权限 → 403 逐字 `Cannot delete repository: '<首键>', Reason: User: ('<u>') has insufficient permission to delete repositories: <键>`、blank key → 400 同形 BinFlow 自有理由、auto-trashcan → 400 服务自身 guard 文案〔以首守卫即拒的 DeleteRepo 调用收割消息，构造上零副作用〕；逐仓段：ghost → success:true `repository config does not exist`、并发中删（进程内 per-key in-flight 注册表，try-lock 失败即报）→ success:true `deletion is already in progress`、真实删除恒 deleteContent（v2 flow「and all its content」语义）+ deletedArtifactsCount=文件节点数（folder 哨兵行剔除）、成功文案 local/remote/federated=`Repository '<key>' and all its content have been removed successfully.` / virtual=`Repository '<key>' has been removed successfully.`；聚合：全 success（含 ghost/中删）→ 200 `All repositories were removed successfully`，真混合 → 207 `Some repositories failed to be removed`，全败 → 首失败仓状态码〔body 保持 reports 形，statusMessage 复用 207 文案——登记〕；真失败报告含 deleteArtifactsFailureCount 与 errors[{status,message}]）；writeBareJSONText（application/json 包裸串的专用渲染，nosniff）；addonPackageTypeMessage（checkAddonPackageType 的非写形态，批建错误聚合用）；repoBatchDeleteErrOf（sentinel→状态码映射，writeRepoSvcError 同表+StatusError 直取）；canManageRepoWrite（family-7 单仓写门的 handler 侧判定）。② router.go：v2/repositories/batch 字面路径补 PUT（route 门 CapRepoWrite——family-6 建仓臂，计数 92→93）/POST/DELETE（route 门 required-only——两动词的真门在 handler 内：POST 的逐仓聚合 403、DELETE 的预校验 statusMessage 形态）三 case，块注释重写；③ t215_guard_test.go：t215ManageGates 92→93 按台账机制追加本票理由（该守卫测试的既定协议——增量必须随表）
Files:        新增 internal/httpapi/repo_batch_write.go、internal/httpapi/repo_batch_write_test.go；修改 internal/httpapi/router.go（v2 batch 三写动词 case + 注释）、internal/httpapi/t215_guard_test.go（守卫计数台账 +1）。删除：无
Tests:        新增 repo_batch_write_test.go 11 测试函数（table/子测试驱动、行为命名、票号文件头）：TestRepoBatchPutCreates（201 体逐字节〔尾空格+空行〕、CT text/plain、空数组 201 空体〔规格待验证臂钉住〕）；TestRepoBatchPutAllOrNothing（已存在 key 整单 400 逐字+新 key 未建、双错 `\n` 连接、批内重复同文案、创建期失败回滚前缀〔virtual ghost member〕）；TestRepoBatchPutLimitsAndGates（101 项 limit 逐字、坏 body 400、非 admin 403、匿名 401）；TestRepoBatchPostMergeDialect（200 裸串+CT、description 省略保留、remote baseline 合并 url 保留+hardFail 落地）；TestRepoBatchPostMissingAndVendor（ghost 404 裸文本+零副作用、vendor CT 过滤落空同 404）；TestRepoBatchPostUnauthorizedAndValidation（plain user 403 裸文本+行未动、缺 key 400 逐字、101 limit、批中不可变类失败恢复首项快照）；TestRepoBatchDeleteEmptyArm（[]/缺 body/坏 body/混合数组四臂同 400 statusMessage-only、无 reports/非信封）；TestRepoBatchDeleteReports（ghost-only 200+报告四字段精确+去重、local 计数=2+逐字文案、virtual 逐字文案、remote 文案〔本票活体证〕、全 success 聚合）；TestRepoBatchDeletePreValidation（非 admin 403 statusMessage 逐字+仓完好、匿名 401、blank key 400、trash key 400 单报告形）；TestRepoBatchDeleteMixedAndAllFail（注入 store 失败：混合 207+失败报告全字段集〔success:false/deleteArtifactsFailureCount:0/errors[{status:500,message}]〕、全败=首失败状态码 500）；TestRepoBatchDeleteConcurrentInProgress（注入 2s 慢删除：双并发请求恰一 in-progress success 报告+恰一真实删除，时序无关判定）
Commands:     `go build ./...`；`go vet ./...`；`gofmt -l internal/httpapi/`（首轮新文件一处未格式化→`gofmt -w` 后空）；`~/go/bin/golangci-lint run ./internal/httpapi/...`（终轮 0 issues）；`go test ./internal/httpapi/ -run 'TestRepoBatchPut|TestRepoBatchPost|TestRepoBatchDelete' -count=1`；`go test -race ./internal/httpapi/ -run 'TestRepoBatchPut|TestRepoBatchPost|TestRepoBatchDelete|TestRepoBatchRead|TestRepoConfigV2|TestRepoConfigurations|TestRepoExistence' -count=1`；`go test ./internal/repo/ ./internal/metadata/ ./internal/auth/ -count=1`；`go test ./internal/httpapi/ -count=1`（全包多轮，时间线与判读详 Outputs/Risks⑧⑨：守卫计数修复前后各轮+两轮独占复跑绿）；隔离冒烟：`go build -o /tmp/binflow-l025b ./cmd/binflow-server` + `BINFLOW_STORAGE__DATA_DIR=/tmp/l0253b-data BINFLOW_ADMIN_PASSWORD=… BINFLOW_SERVER__LISTEN=127.0.0.1:18107 serve` + curl 17 发（本机独立 data-dir，无 docker）；参照探针（172.16.58.130:8082，admin，l025p- 前缀资产用毕删净）：PUT 批建〔local+remote〕、DELETE 批删〔local+remote+ghost 混合〕、configurations 全量复核 l025p 残留=0
Outputs:      四门：build/vet 通过、gofmt 空、golangci 0 issues；触碰面 11 测试全 PASS（4.761s）；race `ok 53.858s`；repo/metadata/auth 全 `ok`（143.8s/66.8s/79.0s）；全包时间线：守卫 +1 前一轮 `FAIL 603.651s`（唯一失败=t215 计数 92≠93，台账机制处理）；守卫 +1 后两轮独占复跑绿——失败行捕获轮零 `--- FAIL`、最终干净单跑 `ok 272.238s exit=0`；守卫单测 `--- PASS: TestRouteGateCapabilitySpellings`。插曲（Risks⑨）：守卫修复后有一轮全包（612.823s，起点与前一轮套件尾段重叠）挂出 argon2 栈痕迹失败，失败名被 tail 截断不可考；随后两轮独占复跑均绿——判高负载时敏毛刺，非本票行为缺陷。冒烟关键行：S1 批建二仓 `Successfully created repository 'smokc' $ / $ …'smokd' $ / $`〔sed -n l 可见尾空格+空行，与规格实测字节一致〕+ `[201 CT=text/plain]`；S2 含已存在 key → 400 信封逐字 `error when validating repository name: smoka : Repository key already exists`+smoknew GET=404；S3 POST merge → `Repositories updated successfully. [200 CT=application/json]`+GET desc/url 保留+hardFail=True；S4 ghost → `No repositories found for the following keys: smokghost [404 CT=application/json]`；S5 vendor CT → 同 404；S6 `[]` → 400 `{"statusMessage":"No repository keys were provided for deletion"}`；S7 ghost-only+去重 → 200 四字段报告+`All repositories were removed successfully`；S8 local 计数=2/virtual 文案/ghost 混合全 success 200；S9 非 admin DELETE → 403 逐字 `Cannot delete repository: 'smokrem', Reason: User: ('smoku1') has insufficient permission to delete repositories: smokrem`、非 admin POST → 403 `User is not authorized to update the following repositories: smokfin`+desc 未动、非 admin PUT → 403（BinFlow 标准文案，登记）；匿名 401。参照探针：P1 参照批建体与 BinFlow 逐字节一致（`Successfully created repository 'l025p-loc' $/$…` 201 text/plain）；P2 参照 DELETE 混合批〔local+remote+ghost〕→ 200，**remote 文案=local 形 `Repository 'l025p-rem' and all its content have been removed successfully.`（本票活体定案，原规格待验证项闭卷）**，报告字段集 {repoKey,statusMsg,deletedArtifactsCount,success} 与 BinFlow 一致、ghost 形态一致、聚合文案一致（reports 顺序参照 HashSet 不稳，BinFlow 批序——差分 normalize 域）；P3 configurations 复核 l025p 残留=[]
Compatibility: 契约面对照 docs/compatibility/contracts/：无 D02 batch 写族专属契约文件（本面以 §2.1 规格为据）。高置信面全对齐（动词映射翻案后的 PUT 批建/POST 批改/DELETE 批删、201 体逐字节、400/404/403 逐字文案、207 状态机 200/207/全败首状态码、ghost/并发中删 success 形态、报告字段集与成功文案两形态）。本票活体补证：remote 成功文案=local 形（规格原未钉，探针定案）。漂移点/待验证清单见 Risks
Security:     三动词全走认证链（匿名 401 挑战）；PUT 路由门 CapRepoWrite（family-6 建仓不委托 manage 持有者，FR-65），POST 逐仓 CanManageRepo(write) 聚合判定（fail-closed：无 ManagementAuthorizer facet 即拒），DELETE 预校验 CapRepoWrite + 规格逐字 403 statusMessage；批建输入校验复用 repo.Service 全链（键字符集/保留段/类型矩阵/虚拟成员规则），MaxBytesReader 4MiB（DELETE 1MiB）限体；credential 面：POST merge 传至 service 的 raw 键零日志、快照恢复走 masked 读形态（密码席位键在场合并，密封凭证不动，NFR-S14）；错误文案插值的 key/用户名均经 JSON 编码或 nosniff 纯文本输出（writeBareJSONText 带 X-Content-Type-Options）；无新依赖
Performance:  无（批次上限 100，逐仓线性调用既有 GetRepo/CreateRepo/UpdateRepo/DeleteRepo；deletedArtifactsCount 一次 ListByPrefix=DeleteRepo 自身同查询；无测量需求）
Risks:        ① 规格待验证清单（候差分，测试已钉实现）：真 207/全败聚合〔反编译+官方 207 文档双源，活体未触发——全败 body 的 statusMessage 复用 `Some repositories failed to be removed` 为本票取舍〕；DELETE 失败报告的 statusMsg 文案〔用 err.Error()〕与 deleteArtifactsFailureCount 值域〔恒 0——BinFlow 整仓删除无逐制品部分失败〕；blank key 中止臂的理由文本 `repository key is blank`（结构=规格高置信，文本=BinFlow 自有）；PUT 非 admin 403 走 BinFlow 标准文案（参照该臂未捕获）；PUT/POST >100 limit 文案（中置信维持，票甲已钉）；批内错误收集顺序（limit 先于缺 key）与 POST 缺 key 首错单条（不 join）——顺序无规格。② federated-first：BinFlow 拒 federated rclass，分派结构在而行为空转（低置信规格字面实现，登记）。③ 并发中删探测为进程内注册表：跨实例并发（多进程）不可见——BinFlow 单进程服务形态下充分，v1 单删路径未接入该锁（规格 D02-R05 复核票的射程）。④ reports 顺序=批序（参照 HashSet 不稳；差分应 normalize）。⑤ PUT/POST 空数组=201 空体/200 成功（参照未探，按「零项无失败」字面实现）。⑥ POST 批内重复 key 去重保首（参照 map 行为未探）。⑦ 批改 local/virtual 臂 config=caller-owned replace（BinFlow 既有 v1 姿态，与本批新无关的已登记模型级漂移）——merge 实证臂（description 列级+remote config 级）已测试钉死。⑧ 全包首轮 FAIL=t215 守卫计数（本票新增 1 个 manage 路由门），按该测试的台账机制 92→93 附理由，非行为缺陷。⑨ 一轮全包失败带 argon2 栈痕迹（两套件进程重叠的高负载时段，失败测试名被截断不可考）；随后两轮独占复跑零 `--- FAIL`/`ok exit=0`——判时敏测试环境毛刺；若 conductor 收编后再现，优先查既有 auth 时敏族而非本票面
Blockers:     无
Next:         ① 工作树可合交（conductor 收编）；② 差分腿建议 ≥12 臂：批建 201 体逐字节/已存在 key 整单 400/多错 join、批改 merge 两臂/404 裸文本/403 聚合/vendor CT、批删空 400/ghost 200/并发中删/混合 207/全败首状态码/trash 中止；③ D02-R05 单删 v2 报告形态复核小票（L025-1 已上报）与本票 ③ 并发锁射程可并票；④ 规格 §2.1.7 可由 reverse-engineer 按本票探针补「remote 文案=local 形」一句（归 conductor 转派）
