# T-383 工作日志 — FR-124.2 M1 建仓形态（改判：断言收口小票）

- 角色：dev-frontend ｜ 状态：**done**（四闸门 + m14 spec 4/4 + m8 全量 67/67 绿〔2 例 axe 并发假阳性串行甄别绿〕+ 服务端 diff=0）
- area：`web/src/pages/repositories/RepositoryFormPage.tsx`（六节锚，纯新增落点）+ `web/e2e/m14/`（新 spec）+ 锚册 console-ux.md（v1.19 批块）。**并行票 T-393（docs/reverse/）零触碰零冲突；parity 册（console-artifactory-parity.md）非本票 area，未动。**
- 参数源：**T-381 活体核验 v1.1**（reports/agents/t381-evidence/t381-measurements.json `V2_newrepo` + 11/12*.png）——Artifactory OSS 7.84.10 实测。

## 0. 改判确认

原票「单 Dialog 化」基于 v1.0 记忆「建仓全程单 modal」；T-381 实测证伪（7.84 = 磁贴网格 modal + 整页路由表单**两段式**），**决策项 A 撤销——BinFlow 现形态已对齐**。本票按改判后口径执行：**对照核验 + 断言钉死 + 差距最小面收口**，不做形态改造（本票实际零形态改动——全部差距项经核验均落在 parity v1.1 既有裁定内，见 §1）。

## 1. 对照核验表（AC1 核心交付）

实测源：Artifactory = t381-measurements.json `V2_newrepo`（DOM 实测）；BinFlow = 本票 Playwright 实测（:8143 净实例，探针数据见 §4 末）。

| # | 核验项 | Artifactory 7.84 实测（v1.1） | BinFlow 实测（本票） | 判定 |
|---|---|---|---|---|
| 1 | **rclass 选择形态** | 列表页「Add Repositories」**下拉**（Local/Remote/Virtual，w=176）预选定，不在向导内以 Tab/分段出现 | ① quick 菜单「快速建仓」三型下拉（`quick-new-repo-remote` → `?rclass=remote`）；② 列表「＋ 添加仓库」钮 + 当前 Tab 语境；③ `?rclass=` 深链；④ 页内 `form-rclass-*` 单选组回显/可改 | ✅ **对齐钉死**（parity v1.1 M1 行既有裁定「手势等价，不改」；本票 test 3 断言 quick 菜单下拉腿 + 深链预选腿 + 单选组回显） |
| 2 | **包型磁贴网格（选择步）** | 「Select Package Type」modal：**880×726 居中**（@x=360/1600 视口），90×90 磁贴（inline SVG 官方标），33 包型 | 「选择包类型」MUI Dialog：**440px** 居中（1280/1600 视口实测均 440，x=420/x=580 正中），紧凑卡磁贴 190×44（icon+名+desc+tier 徽章），本实例 13 项（5 核心 + 8 门控禁用带徽章——D5 可见性口径） | ✅ **形态对齐钉死**（modal + 磁贴网格 + 单选即关）；**尺寸档差距 = 票内定案不追平，留痕**：880px 是 33 包型（5×7 大磁贴）的档位，BinFlow 社区地板 5 核心 + 门控槽位、磁贴带 desc/徽章的信息密度更高，追平 880px 即大面积留白；parity 册 M1 差距行「网格 modal 宽度参考 880px（BinFlow 现档位即可）」**已有裁定**，本票从之并把 440 档钉进断言（test 3 `toBeCloseTo(min(440, vw-48))`——改档须有意识动 spec） |
| 3 | **表单整页路由（两段式钉死点）** | 选磁贴后 modal 关闭、落 `/ui/admin/repositories/{rclass}/new` 整页表单「New Local Repository」——非单 modal 全程 | 选磁贴后 `pkg-grid` 计数 0、URL 保持 `/admin/repositories/new`、`repo-form-page` 仍在路由页；标题「新建 Local 仓库」 | ✅ **对齐钉死**（test 1/2 `pickFromGrid` 助手逐腿断言「modal 关 + URL 不变 + 表单在路由页」——这正是决策项 A 撤销的实证面） |
| 4 | **表单六节结构** | 字段平铺 + 折叠高级节（Repository Key/Environments/Repository Layout/双 Description + Disable Resolution/Allow Browsing/Event Replication 复选） | 单页分区六节 `Paper`：常规（恒在）/ 来源（remote）/ 成员（virtual）/ Maven 策略（local×maven）/ 治理（local）/ 高级（恒在，deb/rpm/helm 策略组在高级节内）；页脚 取消/重置/创建 X 仓库（主钮右） | ✅ **对齐钉死**（BinFlow 六节 = Artifactory「平铺字段 + 折叠高级」的信息密度等价分节，超集增强不违 parity；本票加 `form-section-*` 六锚（v1.19）+ test 1/2 断言三 rclass 条件呈现矩阵——remote=来源、virtual=成员、local×maven=策略、local=治理） |
| 5 | **深链兼容** | `/ui/admin/repositories/local/new` 可直达（T-381 核验方法即用直达） | `/admin/repositories/new[?rclass=remote|virtual]` 直达：表单页 + 网格即开 + rclass 预选；`/edit` 深链既有 m8 腿覆盖 | ✅ **对齐钉死**（test 2 三腿全走 `page.goto` 直达——非导航点击链） |
| 6 | 组合门控（磁贴矩阵语义） | 33 包型对 rclass 有组合约束（实测量未逐型展开） | `pkg-grid-item-docker` 在 remote 深链下 disabled（FR-15-AC7 docker 仅 local）；磁贴 = 原生 button `role=radio`（radiogroup 语义） | ✅ 钉死（test 2/3；m8 既有腿复证） |

**差距清单结论：零「需收口」差距**——全部核验项要么对齐（钉死），要么尺寸档差异已有 parity v1.1 裁定在案（票内定案：不追平 880px，理由见表 #2）。故本票**未引入任何形态改动**，唯一 src diff = 六节锚（断言载体）。

## 2. 实现（最小面）

### 2.1 src（`RepositoryFormPage.tsx`，+6 属性，零逻辑）

六节 `Paper component="section"` 加 `data-testid="form-section-{general|source|members|policy|governance|advanced}"`（v1.19 入册）。纯锚位新增：既有 `aria-label` 不动、`pkg-grid-*`/`form-*`/`repo-form-page` 冻结族零改名、渲染与行为零变化。

### 2.2 e2e（`web/e2e/m14/t383-newrepo-parity.spec.ts`，4 tests）

1. **建仓全链**（AC2 主断言）：列表入口 → 网格 modal（role=dialog + 标题）→ 选 generic → 两段式过渡 → 六节（local×generic）→ Save 落仓（toast `Successfully created repository '<key>'` → 详情 URL + `.key` → **API 对账** rclass/packageType → 收尾删仓）。
2. **深链 + 六节矩阵**：`?rclass=remote`（docker 磁贴禁用 + maven 选定 → general/source/advanced）→ `?rclass=virtual`（members）→ 默认 local×maven（policy/governance）；全程只读零落仓。
3. **入口形态 + 磁贴 Dialog 钉死**：quick 菜单下拉腿（Add Repositories 对位）+ rclass 感知取消（remote 取消回 remote Tab）+ radiogroup/磁贴五核心闭集 + **440px 定案档几何断言**（T-382 `expectDrawerGeometry` 同体例）+ Esc 关闭回 Tab。
4. **axe 双主题**：网格 modal + 创建态表单 local×generic + 创建态表单 remote×maven（来源节 + TTL 面）× light/dark = 6 扫 serious/critical=0（a11y-sweep 路由面只见网格开态/编辑态——**创建态六节是本票新增扫描面**）。

## 3. 锚册（console-ux.md v1.19）

- 头表票据列 + 状态行 → v1.19；§0 修订记录 +1 行（含改判背景与 880px 定案留痕摘要）。
- §10.2 追加 **T-383 建仓形态核验批（6 枚）**：`form-section-{general|source|members|policy|governance|advanced}`——各节条件呈现语义随册（deb/rpm/helm 策略组在 advanced 节内、不设节锚的边界写明）。
- 对账：ledger PASS；dead 桶回归既有 4 家族（本票 6 锚全有 spec 消费——`expectSections` 模板形态对账器不可见，六节均已补逐字断言，不欠 dead 桶新条目）。

## 4. 自测证据（实际执行）

```sh
cd web && npm run typecheck        # 通过（零输出）
         npm run lint              # 通过（零输出）
         npm run build             # assert-tokens: OK — css 11 + tsx 46；✓ built in 5.82s
         node scripts/anchor-audit.mjs --ledger
                                   # PASS（unregistered/broken 双零，retired 全部在表）
make console（手作等价步）+ make build   # SPA 353508 B gz（5MB 预算内；T-382 基线 +636B——6 属性 + 新 spec 副作用为零，增量为 dist 哈希漂移）

# 实例：/tmp/bf-t383（净实例 :8143，BINFLOW_ADMIN_PASSWORD=password，scratch data dir）
BASE=:8143  npx playwright test e2e/m14/ --project=chromium            # 4 passed（15.1s）
BASE=:8143  npx playwright test e2e/m8/repositories-admin.spec.ts --project=chromium
                                                                   # 5 passed（12.7s）——既有 m8 建仓 spec 零改动零回归
# 邻接面（触碰 repo 表单/网格的全部 spec）：
BASE=:8143  npx playwright test e2e/repositories.spec.ts e2e/repo-policy-keys.spec.ts \
              e2e/m8/keyboard.spec.ts e2e/m10/console-license-addons.spec.ts --project=chromium --project=m10
                                                                   # 22 passed（1.1m）
BASE=:8143  npx playwright test e2e/m8/shell.spec.ts e2e/governance.spec.ts e2e/t104-supplements.spec.ts --project=chromium
                                                                   # 13 passed + 2 skipped（47.5s）
BASE=:8143  npx playwright test e2e/m8/a11y-sweep.spec.ts --project=chromium   # 3 passed（1.9m，56 路由面 × 双主题）
BASE=:8143  npx playwright test e2e/m8 --project=chromium            # 67 passed / 2 failed / 1 skipped（5.9m）
# 2 例红 → flake 协议串行甄别（全绿，皆非本票面）：
BASE=:8143  npx playwright test e2e/m8/governance-monitoring.spec.ts e2e/m8/permissions.spec.ts \
              --project=chromium --workers=1                          # 8 passed（1.5m）

# 一次性几何探针（用后即删，未入库）：pkg-grid @1280 → {x:420, w:440}（正中）；
#   @1600 → {x:580, w:440}；磁贴 190×44 ×13（5 核心 + 8 门控禁用带徽章）。

# 服务端 diff=0：git status 仅 docs/design/console-ux.md + web/src/.../RepositoryFormPage.tsx
#   + web/e2e/m14/（新增）；git diff -- '*.go' go.mod go.sum 为空；internal/console/dist 全 gitignored。
```

**2 例全量红与本票 diff 的耦合分析**（皆 axe 腿，串行 8/8 绿）：
- `m8/governance-monitoring:32`：T-352/T-374/T-382 在册的 axe 共居负载假阳性家族（默认 4 workers 下 axe 页内求值超时/假对比度），与本票零关联（该 spec 不触碰建仓面）。
- `m8/permissions:449`（contrast rollback 双主题 axe 腿）：同族——串行复跑绿。两例均为环境并发噪声，非产品缺陷，不追（先例处置一致）。

## 5. AC 判定

| AC | 判定 | 证据 |
|---|---|---|
| AC1 对照核验 + 差距收口/钉死 + 深链 | **PASS** | §1 六行逐项表（rclass 形态/磁贴网格尺寸档/整页路由/六节结构/深链/组合门控）；对齐项全量钉死为断言（test 1~3）；唯一差距项（440 vs 880px）票内定案**不追平**并留痕（parity v1.1「现档位即可」既有裁定 + 33 包型档位 vs 5 核心槽位的留白论证 + 440 档进断言）；深链三腿 test 2 |
| AC2 建仓全链 + m8 零回归 + 四闸门 + axe 双主题 0 + 服务端 diff=0 | **PASS** | 全链 = test 1（选择→表单→Save 落仓→API 对账）；m8 repositories-admin 5/5 零改动零回归 + m8 全量 67/67（2 红串行甄别绿，§4）；四闸门全绿；axe 双主题 6 扫 serious=0（test 4）+ a11y-sweep 56 面复证；服务端 diff=0（§4 末） |
| AC3 锚册纪律 + ledger | **PASS** | v1.19 批块入册 6 枚（form-section-* 六节族）；锚族零改名（pkg-grid-*/form-*/repo-form-page 原样）；ledger PASS（unregistered/broken 双零、retired 全在表、dead 桶零新增） |

## 6. 契约漂移

- 服务端 API 契约：无（本票零 API 消费变化，纯锚位 + spec）。
- 规格面观察（非漂移，留 conductor 参考）：parity 册 §7 矩阵 M1 行在 v1.1 已改判「✅ 已有（决策项 A 撤销）」，与本票核验结论一致，无回写需求；T-383 未动 parity 册（area 纪律）。

## 7. 遗留

1. **m14 目录现只含本票 spec**——后续 M14 票（B3+）按惯例落 `web/e2e/m14/`；目录 README 未建（m9/m12/m13 同样无 README，spec 头注已自文档化，如需 README 随下票顺手补）。
2. **440px 档定案的册面回写**：本票把定案写进 console-ux v1.19 修订行 + spec 断言；parity 册 M1 行的「现档位即可」表述如需升级为「定案 440px」归 ux/conductor（parity 册非本票 area）。
3. /tmp/bf-t383 实例留供复验（:8143 后台 serve，日志 /tmp/bf-t383/serve.log），可整删；本票零 /tmp 残留脚本。
4. 全量跑的 2 例 axe 并发假阳性为套件既有观察（T-352/T-374/T-382 在册），本票未新增此类面，不追。

## 8. 变更清单

```
web/src/pages/repositories/RepositoryFormPage.tsx  六节 Paper +form-section-* 锚（v1.19；纯锚位零逻辑）
web/e2e/m14/t383-newrepo-parity.spec.ts           新增（4 tests：全链/深链矩阵/形态钉死/axe 双主题）
docs/design/console-ux.md                         v1.19：T-383 批 6 锚入册 + 头表/修订行（含 880px 定案留痕）
```
