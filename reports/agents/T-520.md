# T-520 工作日志

```
Ticket:        T-520 [P0] virtual 仓四桶解析序设计 + ADR 勘误（role:architect，area:docs/design + DECISIONS.md）
Role:          architect
起止时间:      2026-09-25 02:15 – 02:51 CST
Area:          DECISIONS.md（ADR-0051 追加 + ADR-0012/0013 勘误行）；docs/design/virtual-four-bucket.md（新文件）
AC 清单:       ① 新 ADR 走只追加+Errata 协议处理两桶→四桶，引用 virtual-resolution.md §2 为权威依据，说明原简化失效原因（BIN-M1 priorityResolution 语义）——完成（ADR-0051；另澄清票面「ADR-0012 勘误」的实际正文载体是 ADR-0013 联动记录①，两处勘误行均落）；② 设计文档含四桶数据结构与装配算法（展开→分桶→四段拼接、嵌套 DFS+visitedKeys、key 去重、缺失静默丢弃）——完成（§2，算法经独立 scratch 编译+运行验证）；③ 实现拆分指名文件与函数（dev-go-core=internal/repo 解析序；dev-go-storage=internal/remote 投影注册表）——完成（§5，逐文件行号）；④ Maven 语义耦合点（§3.6 快照跳 cache、§5.1 合并跳 cache、§5.2 不受影响）——完成（§6）；⑤ 兼容风险清单（两桶行为依赖方、feature flag 裁量）——完成（§7，含 15 个 adapter 消费方清单与「不设 flag」决策）；⑥ 验收口径可测试不变量（locals 恒前 remotes、cache 全排本体前等）——完成（§8，I1-I12 + 开放差分项 O1-O5）。
方案摘要:      候选 A（两桶+补丁，表达不了 cache/本体分离）/ B（四桶逻辑投影）/ C（物理投影+节点迁移）→ 选 B：节点存储零迁移（缓存行留 remote 命名空间，ADR-0012 机制轴零翻动），解析层新增 cache facet（零上游 standing-copy 命中步）+ internal/remote 派生投影注册表（priorityResolution/handle* 投影继承单源）；四段序 = P-local+P-cache → P-remote → NP-local+NP-cache → NP-remote，段内 real-local 先于 cache 投影（规格 §9.1 推演序，开放差分项）；嵌套 virtual 解禁（DFS+运行时环剪断）。不设 feature flag（ADR-0050 决策 6 先例）。
产出文件:      ① DECISIONS.md +15 行（ADR-0051 全条目；ADR-0012 勘误五=引用面注记零作废项；ADR-0013 勘误=联动记录① 两桶序作废、维持项逐列）② docs/design/virtual-four-bucket.md 新建（9 节：范围/现状/算法/facet 语义/注册表/实现拆分/Maven 耦合/风险迁移/验收不变量+后续缝 F1-F8）③ 本报告。
命令与输出:    1) 算法编译门（/tmp/t520-scratch，module t520scratch）：`go build ./... && go vet ./... && test -z "$(gofmt -l .)"` → exit 0（build/vet/gofmt 三绿）；运行输出验证四段序与边界——声明 [R1,NP-local,L1,nested(→P-local L2,环 virt),R1 重复,ghost 缺失] → 序 = L2(P-local) → L1(NP-local) → R1 cache facet → R1 本体；ghost 触发 warn 被丢弃、R1 重复被去重、nested→virt 环被剪断。2) 引用门 grep：`grep -n "^## ADR" DECISIONS.md | tail -6` → ADR-0051 唯一追加于 ADR-0050 后（ADR-0037 编号缺口为既有在档预留，DECISIONS.md:802 注释说明，非本次引入）； cited anchors 全部实存：virtual.go:110（virtualMemberOrder）、service.go:2407（validateVirtualMembers）、api.go:512（VirtualMemberOrder 契约）、fetcher.go:77/938（缓存行落 remote 命名空间）、operations.go:519/521、dockervirtual.go:62/595、remoteexternal.go:66、repo_config_render.go:219/330。3) `git diff --stat DECISIONS.md` → 15 insertions, 0 deletions（只追加协议成立；worktree 内 BOARD.md 等其余 modified 文件为本会话之外的主会话/他票在途改动，本票未触碰）。4) markdown 结构自查：新设计文档 21 个标题层级规范、`awk -F'|'` 表格列异常 0。
测试四态:      设计票无代码落地：单元/集成/差分 = NOT_RUN（无实现产物，属预期）；算法 scratch 编译+运行 = PASS（见命令 1）。差分腿（mvn CLI + curl 双发）为实现票验收面，本票登记口径于设计文档 §8。
lint/格式:     scratch 包 `gofmt -l` 零输出；`go vet` 零告警；两份 markdown 标题层级与表格列数自查通过；文档语言=中文、标识符/代码=英文（规范符合）。
证据锚:        规格侧——docs/reverse/virtual-resolution.md §1「嵌套 virtual 展开/重复成员/不存在的成员/混合成员」行（高置信度）、§2 四段拼接与「cache 与 remote 不相邻/桶划分（投影继承）」（高）、§3.6「跳过所有 cache 仓」（高）、§5.1 合并遍历跳 cache（高）、§7.5 DELETE 勘误（高，本票未触及删除面，仅对账确认无冲突）、§9.1/9.5（开放项与单源风险自记）；投影侧——remote-cache-projection.md §1.1（非持久实体）、§1.2（继承表）、§1.3（-cache 后缀防护）。代码侧——见命令 2 锚清单（as-built 与设计假设一致，无漂移发现）。
风险:          ① cache 步「过期副本无条件服务」是本设计最高风险语义断言（O2，差分推翻则走 Errata 加检索窗检查）；② 规格全量条目为反编译单源（参照实例 401 凭据 pending，规格 §9.5 自记）——四桶骨架高置信度可逐行复读，但段内次序（O1）与负缓存并存态（O3）未经活体；③ virtual patterns 三层过滤（规格 §4）BinFlow 未实现——嵌套解禁后子树 patterns 剪枝缺失属既有缺口放大（登记 R6/F6，不扩本票面）；④ 15 个 adapter 消费方未处理 Facet 时退回两桶时代成员语义（兼容承诺成立但差分面会滞后，maven/npm 两处必要动作须随实现票）；⑤ 发现超出四桶范畴的规格-现状冲突一处：规格 §7.2/§7.3 要求 REST 存储面与搜索域以 cache key 形态消费 remote 内容，BinFlow 现状以 remote key 直查——属 F2/F3 后续缝非本票冲突，仅登记。
偏差:          票面称「ADR-0012 勘误」——实际两桶决策正文载体是 ADR-0013 联动记录①（其引用 ADR-0012 为「无影子投影」前提）；已按 Errata 协议双落（ADR-0013 勘误行=机制作废；ADR-0012 勘误五=引用面注记零作废项），ADR-0051 背景段显式留痕该澄清，未静默改号。其余无偏差：未 commit/push（硬约束遵守）、未写 BOARD.md、未动 Linear、未改任何代码文件。
后续建议:      ① 实现票两枚按 owns 拆分派发（dev-go-core：virtual.go/service.go/api.go/validate.go + 测试翻新；dev-go-storage：internal/remote/projection.go 注册表）——任务书=设计文档 §5 表；② dev-go-adapter 消费票（maven/virtual_metadata.go 跳 cache、npm packument 缓存去重，余 13 文件核对）；③ 差分腿设计（O1/O2/O3 三臂优先）交 differential-qa-engineer，素材=设计文档 §8；④ virtual-resolution.md §7.5 勘误（DELETE 仅删 virtual 自身存储）与 BinFlow 现状 refuseVirtualDelete（405 不透传删除）的语义对齐需单独立票裁定（INTENTIONAL 候选，authority 可引本 ADR 链）——超出本票面仅登记。
结论:          done。ADR-0051 + 双勘误行 + 设计文档三交付物齐备；编译/引用/结构三门实跑通过；两桶→四桶升级的机制、边界、风险、验收口径全部落册可溯源。
```
