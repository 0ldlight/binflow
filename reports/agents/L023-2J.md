# L023-2J — 终尾票：§10.2 三臂精确文案 + D4-R3 properties 单面

Ticket:       L023-2J（终尾：PUT started 两臂 joda 族化 + GET 值域越界臂 + properties 空集省键）P1
Role:         dev-go-core（area = internal/build/upload.go + internal/httpapi/build.go）
Area:         started 校验文案族 + echo 单面（零触碰其他面）
Input:        reports/compatibility/L023-buildinfo-diff.md §10.1（R3 单面定谳）/§10.2（六臂矩阵与 2J 三臂精确文案：p2 `Invalid format: "xyz"`、p5/p4a `Cannot parse "…": Value 13 for monthOfYear must be in the range [1,12]`）；§10.2「参照机制」段（格式错/值域错换族 + joda 字段字面）
Changes:      ① **共享 joda 族渲染器**（internal/build/upload.go 新增 StartedFormatMessage + startedLayoutBreak + startedRangeBreak）：格式错=`Invalid format: "<text>"`（有前缀追加 ` is malformed at "<尾段>"`、位 0 无子句——2H 语义迁入）；**值域错换族**=`Cannot parse "<text>": Value <n> for <field> must be in the range [<min>,<max>]`，joda 字段字面 monthOfYear/dayOfMonth（月感知上限）/hourOfDay/minuteOfHour/secondOfMinute 按时序首错上报。② **PUT 面**：Upload 的 started 校验失败臂弃自有文案 `build started "…" must be an ISO8601 timestamp…`，改 WireError{StartedFormatMessage}（干净句无 wrap 后缀）。③ **GET 面**：详情预检改用同一共享渲染器（httpapi 本地 invalidFormatMessage/malformedAt 删除）——p4a 值域臂由 `is malformed at ""` 改 Cannot parse 族。④ **§10.1 R3 单面**：无 properties 的 build 详情回显**省略键**（原 `{}` 物化；带 properties 的回显不变；slim 面不变）。
Files:        修改：internal/build/upload.go（族渲染器三函数+Upload 臂+WireError 化）；internal/httpapi/build.go（GET 预检共享化+本地助手删除+properties 省键）。测试：build_query_semantics_test.go（+TestStartedJodaFamilyAndPropertiesOmission 四臂）；upload_test.go/build_rest_test.go（旧 started 文案两处对位翻转）。
Tests:        新增四臂：PUT xyz（无子句干净句）/PUT 值域（Cannot parse 逐字）/GET 值域（同族逐字，B 的 `is malformed at ""` 漂移消除）/无 properties 省键；存量三处旧文案断言对位。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/build/ -count=1 -timeout=20m（ok 66.8s）；go test ./internal/httpapi/ -count=1 -timeout=20m build 族+新臂（ok 174.4s）；go test -race ./internal/httpapi/ -count=1 -run 触碰行为五测试（ok 120.4s）；冒烟：隔离实例 127.0.0.1:18094（独立 data-dir，零 docker，用后清除）四臂。
Outputs:      冒烟：J1 PUT `started=xyz` → `Invalid format: "xyz"`（=§10.2 p2 逐字）；J2 PUT `2026-13-45T99:99:99.999+0000` → `Cannot parse "2026-13-45T99:99:99.999+0000": Value 13 for monthOfYear must be in the range [1,12]`（=p5 逐字）；J3 GET `?started=2026-13-45…%2B0000` → 同句 400（=p4a 逐字）；J4 无 properties build → detail 无 `"properties"` 键（=§10.1）。
Compatibility: 对照 §10.2 三臂逐字达成 + §10.1 R3 单面省键——D7 六臂矩阵现 p1/p3/p4b/10b（2H）+ p2/p5/p4a（本票）全 SAME，p6 对照臂本就 SAME；D4-R3 五单元应随差分复跑翻绿（处置归差分通道）。**登记**：① 值域族为**样本归纳模板**（§10.2 明示「族」）——字段名/区间按 joda 字面与时序首错实现，仅 monthOfYear 样本逐字钉死，dayOfMonth 月感知上限/hourOfDay 等臂为 joda 忠实推断（未逐字活体）；② 偏移段（Z/±hh[:mm]）只走格式族不做值域检查（joda 偏移上限未探）；「布局通过+值域通过但严格解析拒绝」的异形（如 `+010` 短偏移）落 clause-less 兜底；③ 空 started（body 缺字段）→ `Invalid format: ""`（族推定，未活体）；④ append/promote/retention 面的 started/timestamp 校验文案未动（§10.2 仅 PUT/GET 两面——零触碰纪律；promote 面有自身 §11.5-8 逐字行在案）。
Security:     无新面（错误文案族+echo 省键；内插经既有 JSON 转义路径）。
Performance:  无影响（纯渲染分支）。
Risks:        登记①②③ 的未活体臂——差分复跑若出 joda 异形样本（Feb 30/`+010`/空串）再校；值域族实现为独立函数无既有路径耦合。
Blockers:     无。
Next:         差分复跑（六臂矩阵+R3 五单元）确认全 SAME → D07-R05/R06/R07 翻态终判。
