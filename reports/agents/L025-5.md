# L025-5 — D02 配置族差分小面（G1 大面另票）

Ticket:       L025-5（微返工：L025-4 差分 G2/G4/G5 三 BUG + G6 头族裁定）P1
Role:         dev-go-core（area = internal/httpapi 批写/读面）
Input:        reports/compatibility/L025-d02-config-diff.md（权威——§2 D-item 详单）；wire 证据 reports/compatibility/l025q-wire/{a,b}/（w02b/w05/w12 逐字）
Changes:      按报告 §2：
              1) **G4 PUT 批建非 admin 403**：路由降 required-only + handler 首行 errors 信封**裸 `Forbidden`**（与 configurations 面 403 同文案——撤 BinFlow「administrator privileges required」标准渲染）；管理判定不变（CapRepoWrite）。
              2) **G2 DELETE blank-key**：撤预校验整单中止臂（spec §2.1.7-2 被活体证伪）——blank key 落**逐仓 ghost 处理**（`""` 行 success:true `Cannot delete repository: '', repository config does not exist`，真仓正常删，聚合 200）。
              3) **G5 v1 单仓 GET 未知 key**：404 `Repository does not exist: …` → **400 envelope 裸 `Bad Request`**（v2 读的 404 信封形维持不变——v02 绿面不动）。
              4) **G6 头族**（admin/repolayouts 的 CORS+Cache-Control）：**零代码触碰**——按报告 §0 归一提案 Q1 的头 drop 集处理（判定面已 drop；补齐与否是 compatibility-engineer 的 PN 裁定项，非实现漂移）。
              5) G3（POST 批改文案）/G1（投影大缺口）/G7（开放臂）/G9（401 族）不在本票。
Files:        修改：internal/httpapi/router.go（batch PUT 路由门降级）；internal/httpapi/repo_batch_write.go（G4 handler 门+裸 Forbidden；G2 blank-key 臂撤）；internal/httpapi/repositories.go（G5 handleRepoGet 未知 key 臂）。
              测试对位：repo_batch_write_test.go（blank-key 臂翻 ghost 形断言+PUT 403 补裸 Forbidden 断言）；compat_test.go（未知 GET 臂翻 400 裸文案）；t215_guard_test.go（管理门清单 93→92——G4 门移入 handler 的清单记账）；t215_create_arm_gate_test.go / t215_rbac_routes_test.go / t217_manage_wire_test.go（「denied create 后 GET = 404」三处探针翻 400 quirk 形——语义不变：断言「仓不存在」，形态随 G5）。
Tests:        存量六文件对位（无新增面——三 BUG 各自的差分 wire 由既有断言翻转钉死）；冒烟四臂。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/httpapi/ -count=1 -timeout=25m 全量；go test -race 触碰面（TestRepoBatch/CRUD/RouteGate/T215/T217，ok 47.2s）；冒烟：隔离实例 127.0.0.1:18091（独立 data-dir，零 docker，用后清除）。
Outputs:      冒烟四臂（对报告 wire）：S1(=w02b/G5) `GET /api/repositories/no-such-key` → 400 `{"errors":[{"status":400,"message":"Bad Request"}]}` 裸逐字；S2(=w05/G4) 非 admin `PUT /api/v2/repositories/batch` → 403 `{"errors":[{"status":403,"message":"Forbidden"}]}`（admin 同 body 201 对照）；S3(=w12/G2) `DELETE ["l0255a",""]` → **200 双报告**：真仓 `Repository 'l0255a' and all its content have been removed successfully.` + `""` 行 `success:true "Cannot delete repository: '', repository config does not exist"`——与 A 活体形一致（B 原 400 预校验整单中止消除）；S4 域族全量复跑绿。冒烟产物（仓/非 admin 用户）用毕全删复核。
Compatibility: 对照报告 §2：G2/G4/G5 三 BUG 逐字/逐形闭合；G6 归一提案 drop 处理（零触碰）+ 裁定归 compatibility-engineer；spec 勘误随 G2（§2.1.7-2 blank-key 臂改 ghost 逐仓形）。**登记**：① G5 的 400 quirk 仅 v1 单仓 GET 面——v2 读 404（v02）/v1 LIST miss/batch 读 ghost 省略均维持（差分绿面零触碰）；② t215 管理门清单 93→92 是 G4 门位移的记账（门本体未删，handler 内同判定）；③ G3（POST 非 admin `Only platform/project admins are allowed to update repositories`）明确不在本票——dispatch 未列，留待下票。
Security:     G4 门降级仅文案路由（管理判定 CapRepoWrite 原样，handler 首行执行）；G2 blank-key ghost 臂无权限放大（空 key GetRepo 必 miss）；G5 纯状态/文案面。
Performance:  无影响（三处分支文案）。
Risks:        ① G5 波及三处 T215/T217「denied create 后仓不存在」探针（已翻 400 形断言——语义等价）；② G6 若裁「补齐」需另票加 CORS 头族（现 drop 姿势与差分判定一致）。
Blockers:     无。
Next:         ① 差分复跑（d02_config_diff.py w02b/w05/w12 单元）验证三 BUG SAME；② G1 投影大缺口（另票定因——账实不符上报已由差分报告承载）；③ G3 文案随下票；④ D02 批次 3 行翻态终判。
