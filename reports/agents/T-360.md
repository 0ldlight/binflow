# T-360 工作日志 — ADR-0041 决策 4 回填 + ADR-0042 D-F2 存量布局迁移方案

- 角色：architect；日期：2026-08-30；状态：done
- area：DECISIONS.md（仅此一文件 + 本报告；零 git 操作）
- dep：—（T-340 §4 / T-348 §4 登记在案；回填侧输入 = T-370 登记 + 用户终裁 2026-08-30「对齐官方锚点」+ T-364 as-built 25e8afd）

## 1. 交付 1：ADR-0041 锚点回填勘误（用户终裁执行）

形态沿用仓内勘误惯例（ADR-0006 勘误④/ADR-0012 勘误二同款：**原文存目 + 行内 errata 指针 + 独立勘误块**，不推翻决策本体）。改动七处（DECISIONS.md L879/L893/L920/L930-931/L934/L946-947）：

| # | 位置 | 修订 | 依据 |
|---|---|---|---|
| 1 | 状态行 | 追加 2026-08-30 errata/backfill 指针 | 可发现性 |
| 2 | 决策 2 | 「3xx/4xx/5xx/超时/连接错 = 可重试失败」→ **仅发送失败或 ≥500 可重试；4xx 单次终态；3xx 不跟随判终态**（原句存目作废注记） | webhook.md §5.2 retryCount 行原文「unable to send the request or … error (>= 500)」；3xx 终态 = T-364 as-built 实现形态 + V4 待验证注记（翻转点 = deliver() 分类一臂） |
| 3 | 决策 4 | 尝试上限 10 → **5（首试计入，V3 中置信注记）**；2s ×2 指数退避 cap 10min → **固定间隔 10s（无退避曲线）**；单次超时 10s → **30s（含建连/重定向/读体）**；worker 4 不翻（引擎内部常数）；最坏故障窗重算 ≤190s | webhook.md §5.2 retryCount/retryWaitMillis/timeoutMillis |
| 4 | 锚点回填位 | 追加「回填执行状态」：四项已回填；K47/K48/K49 字面量以 webhook.md 按效力序直接生效（机制条款不逐条改写）；Q4 取证在案、终裁归用户窗；Q6 开放；K47 wire 形态级分歧另立勘误 | 效力序条款（用户裁决 > webhook.md > ADR > PRD） |
| 5 | 新增「锚点回填勘误」块 | 触发票留痕（T-370 登记 → 用户终裁 → T-364 as-built 先行、零代码翻转）+ 修订四项 ①~④ + **机制条款不动清单**（outbox 双方言两表/死信与 Replay additive〔webhook.md §5.3 明示归 ADR〕/Guard 默认拒私网/worker 4）+ as-built 增补登记（限流三参数 1000s+10000+50000、排障环 10000/30s——DispatcherOptions 内部常量，零新配置键） | 勘误惯例 + BOARD T-370 裁定材料③ |
| 6 | T-362 骨架 AC-3 | envelope 逐字段断言**删除「时间戳」**——artifact 域载荷无事件时间戳（勿发明字段） | webhook.md §4 + T-358 §1-8 取证 |
| 7 | T-364 骨架 AC-2/AC-3 | AC-2 退避序列观测 → 固定间隔 10s 序列 + 4xx 不重试负面臂；AC-3 死信阈值 10 → 5 attempts | 随勘误 ①② 同步（ADR 内部自洽） |

## 2. 交付 2：ADR-0042（DECISIONS.md L954-978，Accepted）

三轴全 A，要点七条（对票 AC1/AC2 逐项覆盖）：

- **AC1-迁移时机**：启动期 sweep（bootstrap 序、监听器起前——读面结构性见不到半迁移树；崩溃中断下次重跑）。否决惰性（读路径 ×N 永久特判 + 首读竞态）与永久双读（分歧永久化、snapshot/搜索仍错）。
- **AC1-幂等**：谓词消耗型——迁移后源谓词零命中，二跑零改（AC 断言 moved=0）；单仓单事务（或分批归 T-371）。
- **AC1-回滚**：元数据备份恢复（ADR-0011 CLI；blob 面零接触 ⇒ 仅恢复 metadata.db 即无损——checksum 寻址红利）。否决台账表（一次性角落不配持久 schema）与就地逆向（逆向谓词无法区分已迁移 spec 节点与 v2 原生 spec 节点——不可安全表达，明示不做）。
- **AC1-零损硬约束（sha256 对账口径）**：① 逐节点映射折叠清单全等（path→sha256）；② 单仓节点数一致（dedup 臂豁免、conflict 恒 0）；③ blob 面零变化（不触 blob store API）。
- **AC1-K53 回填**：PRD §5.6 K53 行由 ADR-0042 关闭（登记于 ADR 决策 7；不写 PRD 文件——PM 下批改「已定案（ADR-0042）」）。
- **AC2-迁移范围**：仅 conan local 仓 / 仅包文件臂 / 仅 `…/0/package/<pid>/0/package/<pid>/<tail>` 双拼树 → 前缀重写至 `<coordinateRoot>/0/package/<pid>/0/<tail>`（规格全形 `<coordinateRoot>/<rRev>/package/<pid>/<pRev>/<file>`，v1 通道修订段字面 0）；文件夹行随子树重写。
- **AC2-后效口径**：① ref-search settings/options/requires 恢复非空（`-q` 过滤腿）；② v1 包 snapshot 键裸文件名；③ v2 包 files 清单键同归；④ 通道 GET 直达 spec 路径。附 T-371 验收锚点骨架四条（含 conan 1.66 install roundtrip 与零 conan 仓零扫描断言）。

载体定案：conan 域 sweep + repo.Service 一条窄原语（同 blob link 平移 / 系统态无权限门 / 零 observer·webhook Emit·逐节点审计 / 错误显式分类）——否决 SQL 编号迁移（分层 + 无自愈）与 CopyOrMove 复用（用户面语义污染，会向订阅者发 artifact.moved）。

## 3. 现态核对（设计前实测，非转抄）

- `internal/adapter/conan/v1.go:411-414`：`channelFileName` trim `segPackages+"/"+pid+"/"`（复数），`layout.go:57` `dirPackage = "package"`（单数）——双拼成因与 T-340 §4 一致；PUT 腿（v1.go:422-426）两修订段均 `revDefaultV1="0"`，谓词三重字面约束成立。
- `search.go:155`：ref-search 读 `pkgFile(…, "conaninfo.txt")` 规格路径——双拼树落空 ⇒ settings 恒空，机制与 T-340 实测吻合。
- `v1.go:229-236`：snapshot 以 `pkgFilePrefix` trim ⇒ 键携 `package/<pid>/` 前缀，v1/v2 清单同病确认。
- T-364 as-built（dispatcher.go:64-72）：`DefaultRetryCount=5` / `DefaultRetryWait=10s` / `DefaultAttemptTimeout=30s`——勘误为纯文档对齐确认。

## 4. 自测

| 命令 | 结果 |
|---|---|
| grep 旧规范性表述（`尝试上限 **10**`/`退避序列（2s/4s/8s`/`超 10 attempts`/`2s 起步 **×2`） | 仅命中勘误块内「原文存目」引用两处（L893/L933，惯例保留）；规范性表述零残留 |
| grep 新锚点值（首试计入/固定间隔 10s/30s/4xx 不重试/无时间戳字段/5 attempts） | 7 处在场 |
| ADR-0042 结构（状态/日期/背景/候选方案 ≥2 轴/决策/理由/后果/clean-room/T-371 骨架） | 九段全在（L954-978） |
| `git status --short DECISIONS.md`（仅查看，零 git 写操作） | M DECISIONS.md（工作树修改，未暂存未提交） |

## 5. 遗留 / 交接

1. **T-371**（下游实现票，dep 本票 + T-369）：消费 ADR-0042 决策与锚点骨架；原语形（单节点 vs 子树前缀批）与事务分批粒度票内定形。
2. **BOARD T-364 AC2「指数退避」措辞**：conductor 随改「固定间隔 10s 重试序列（4xx 不重试）」——BOARD 归主会话，本票不写（ADR 侧锚点已同步）。
3. **Q4（webhook 槽档位）/ Q6（事件覆盖界）终裁**仍开放（勘误块内已注记归属）；webhook.md V3/V4 活体验证项维持登记。
4. K47 订阅 wire 形态级校准（key/handler 单订阅模型 vs ADR-0041 决策 3 UNIQUE 约束）**未在本次终裁范围**——若 T-362 as-built 与规格有形态分歧，另立勘误（锚点回填位已留缝）。
