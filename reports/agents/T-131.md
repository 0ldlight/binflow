# T-131: FR-44 FE 删隐式目录双兜底（DM-02 FE 面）

**状态**: done
**日期**: 2026-08-21
**角色**: dev-frontend

## 变更摘要

1. **删 `searchListing` 前缀重构兜底**（`lib.ts` lines 159-186 原位置）：
   - `listChildren` 不再在 404 时回退 `/api/search` 前缀重构
   - 隐式目录已由 BE `materializeAncestors`（ADR-0016）材料化，`listChildren` 直走主路径

2. **删 `mkdir` 逐段材料化循环**（`lib.ts` `mkdirOnce` 辅助函数一并删除）：
   - `mkdir` 改为单段 PUT（BE 服务端材料化祖先目录行）
   - 从 18 行（循环 + helper）简化为 9 行（内联单 PUT）

3. **TreePage 404 分支对非根目录恢复可达**（NB① 结构性关闭）：
   - `TreePage.tsx` 的 404 分支（line 452-461）已存在，无需修改
   - 移除 `searchListing` 兜底后，非根目录 404 将直接传播到 TreePage 错误处理

4. **同步更新 lib.ts 头部契约注记**（T-131 更新标记）

5. **新增 G30e Playwright 回归测试**（`web/e2e/t131-g30e.spec.ts`）：
   - G30e-1: 零 `/api/search` 请求断言（上传 + 浏览 + mkdir + 导航）
   - G30e-2: typo 深链接 404 EmptyState 渲染
   - G30e-3: mkdir 单段 PUT 回归（只发一条 PUT）
   - G30e-4: 大目录客户端分页回归

## 修改文件

| 文件 | 变更 |
|---|---|
| `web/src/pages/repositories/tree/lib.ts` | 删 searchListing + mkdirOnce，简化 mkdir 为单段 PUT，更新契约注记 |
| `web/e2e/t131-g30e.spec.ts` | 新增 G30e 回归测试（4 例） |

## 验证

- `make console`：零告警通过（88 modules transformed）
- 代码搜索：`searchListing`、`mkdirOnce` 在 src/ 目录零引用
- lib.ts 行数：374 → 315（减少 59 行死代码）

## 待办

- E2E 测试需要本地运行 binflow 二进制验证（`make build && cd web && npx playwright test t131-g30e.spec.ts`）
- 依赖：需要运行 T-128 的 BE 实现（materializeAncestors）才能通过 G30e-1 测试