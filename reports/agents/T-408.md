# T-408 — ADR-0043 AQL 引擎架构（M15 B0 前置锚）

**状态**: done（architect，2026-09-01）
**area**: DECISIONS.md（ADR-0043）+ docs/design/architecture.md §24/§11.46-47/文件头
**dep**: —（软协作 T-407 aql.md——并行在途，aql.md 截稿时未落盘）

## 产出

1. **ADR-0043 Accepted**（DECISIONS.md:980 起）——七轴全 A：
   - 编译管线 = **两段编译**（search AST→IR；metadata IR→方言参数化 SQL——SQL 知识不越 metadata 层）
   - name/depth = **SQL 派生表达式**（`replace(path, rtrim(path, replace(path,'/','')), '')` 双方言同式；零迁移）
   - ACL = **repo 集 SQL 织入 + path 级行复核两段**（SearchScope/CanRead 新窄读缝，同一 allow() 源）
   - 资源门 = **引擎级三件**（semaphore 4 非阻塞 / deadline 10s / cap+1=1000 截断——内部常量零配置键，Q2 出口）
   - 错误面 = **E-01 单形不动**（message 单点组装含 domain/field 名；.delete()/.update() → 400 诚实拒绝）
   - WriteTimeout（T-392 收口）= **维持不设全局值**——同一 Server 承载内容面大 blob 分钟级流写，查询时长治理归 K63 deadline，不另开面
   - sha1/md5 = **blobs 惰性 join**（filter/include 引用才 JOIN；ledger 侧，T-92 同口径）
   - 含 EBNF 暂行骨架、字段→SQL 映射表（name/depth/type 派生、时间归一化比较 julianday/::timestamptz、@key→EXISTS node_props 兑现 idx_node_props_name）、软缝对齐清单十条、四票验收锚点骨架。
2. **architecture.md 搜索域增量节**：新 §24（24.1 包边界/缝接口代码、24.2 执行序图、24.3 编译映射与注入防线、24.4 ACL 织入、24.5 资源门+WriteTimeout 归位、24.6 端点与 metrics、24.7 妥协）；§11 台账 +46（path 复核行级成本）/+47（offset 无界）；文件头 M15 注记。

## 现役代码勾稽（定稿依据，非悬空设计）

- nodes 列面（001_init.sql）：**无 name/depth/type/updated_by 列** → 派生表达式 + `modified_by` 注册表标 unsupported（不加列不伪造）。
- auth targetCovers（authorizer.go）：**path 级 include/exclude 模式存在** → repo 集过滤不完备，两段织入的直接动因。
- NodeSearcher 断言形态 / nodeQuery「静态片段+参数化 args」G202 纪律 / likeSubstring ESCAPE '\' / allow() 私有（service.go:210）/ searchGate 匿名 403 / E-01 envelope.go 无 code 字段 / Server 仅 ReadHeaderTimeout（server.go:337）/ metrics 自研 Registry binflow_* 族。
- **官方文法取证**（clean-room 基准，2026-09-01）：docs.jfrog.com「AQL Query Structure and Syntax」——方法链（include/sort/offset/limit/distinct + delete/update 动作面）、`{"@license":"Apache-2.0"}` @ 属性键 + `@*` 通配、字段名大小写敏感、数字可引可不引、空 {} 合法。

## 软缝对齐（AC3——T-407 落盘后 Accepted 复核）

ADR-0043 末尾「软缝对齐清单」十条待对：最实质的一条 = **PRD 133.1 的 `{"@key":{"$eq":"license"}}` 属性形态与官方 `{"@license":"..."}` 不一致**——本 ADR EBNF 取官方形态，PRD 措辞以 aql.md 归一（旧版 @key/@value 元字段形态存在则注册表双收）。其余：checksums 路径形态、depth 口径、操作符闭集、截断头 vs 官方 limit 形态、超时 503 暂行、virtual 暂行实际存储行、folder 行默认入结果、compact 投影、400 文案逐字。分歧走 ADR 勘误段，不翻机制。

## 四票边界确认（AC3）

T-409（Parse+注册表+QueryError，纯函数）/ T-411（Planner+NodeQueryer 缝+SQL 快照）/ T-413（Engine.Run+门+SearchScope/CanRead）/ T-415（handler+E-01+metrics）——每票窄接口面可独立 table-driven 验证，粒度成立。注意：T-413 与 T-412 同触 internal/repo（不同文件：search.go/api.go vs service.go），合入序归 tech-lead。

## 验证

- 文档自检：ADR 编号连续（0043 承 0042）；architecture §24 交叉引用（§11.46/47、§7.3、ADR-0043）一致；ADR-0042 骨架零破坏（编辑残留已修复并 grep 验证单处）。
- 零 git 操作 / BOARD.md 零写。
