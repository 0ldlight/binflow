Ticket:       L008-1b — update-merge 实现票（ADR-0050 派发级；dev-go-core repo+httpapi 双域唯一实例；P0）
Role:         dev-go-core（repo + httpapi 实例）
Area:         internal/repo + internal/httpapi（任务书 §9.1 清单 5 文件内；console/keypair 零改动确认项达成）
Input:        ADR-0050（DECISIONS.md L1375-1413）；docs/design/repo-update-merge.md §9 实现票任务书（5 文件/7 单元验收/13 硬断言臂/4 探针臂）；证据 reports/compatibility/L007-3-update-merge-evidence.md（三列矩阵 + PUT-on-existing 400）；:8082 活体（2026-09-12 本票补证 A14/A15/A16/A17，见 Outputs）
Changes:      ① config.go：parseRemoteConfig 增基线模式（签名 +baseline *remoteConfig；nil=create 面维持默认起步）——url 更新面可省（基线有 url 即可，显式空白仍拒）；period 族 0-as-absent 双面移除（`|| f.value == 0` 子句删，负值拒/别名分歧拒维持——显式 0 即写 0）；username/password 非指针席位经 validateRemoteConfigShape 返回的 raw map 判键在场（缺键=保留基线、在场 null/""=清、有值=写——零新增结构席位，第三返回值 passwordSet）；contentSynchronisation 起步值由零值改基线值（省略/null=保留整族【A15 补证】、{}/显式对象=整体替换未提子键复位 false【A14 补证】——先前「未提=基线」草案按补证推翻）；chartsBaseUrl 省略=保留基线；② service.go：UpdateRepo remote 臂传存量 canonical 为 baseline（存量 unmarshal 沿审计段容忍姿态）；credential 保留语义落 remote_configs 行——passwordSet=false 时 GetConfig 取存量 sealed password 原行（create-crash 愈合窗回落空）；T-80「Full-replace semantics (the Artifactory PUT model)」注释整段勘误为 ADR-0050 merge 语义；CreateRepo 传 nil；③ httpapi/repositories.go：handleRepoPut 移除更新臂——已存在 key 且 body 带 rclass → 400 errors 信封逐字 `error when validating repository name: <key> : Repository key already exists`；无 rclass 的 PUT-on-existing 落 create 路径先撞 type 拒（A16 次序对齐：type 先于 key-exists）；create 臂（addon 门、FR-65 分门）不变；handleRepoPost description 席位 raw body 键在场判缺（缺键=传 current.Description、在场含 null/""=覆写，null 解码 ""）；repoConfig.Username/Password 改 json.RawMessage 透传（null 原样过 transport 进 config blob——typed string/pointer 都会把 null 压成缺省，credential 单侧清依赖此）
Files:        修改：internal/repo/config.go、internal/repo/service.go、internal/repo/remote_virtual_test.go（0-as-absent 建面翻转回归改写）、internal/repo/t317_smart_remote_effect_test.go（full-replace 测改 merge-on-omit）、internal/repo/t290_smart_remote_test.go（Review B2/N3：失实行名改写 + zero-ms+secs 腿按取证翻转）、internal/httpapi/repositories.go、internal/httpapi/compat_test.go、internal/httpapi/remote_browse_flag_wire_test.go、internal/httpapi/remote_metadata_ttl_wire_test.go、internal/httpapi/repo_config_roundtrip_test.go、internal/httpapi/t217_manage_wire_test.go、internal/httpapi/t327r_policy_transport_test.go、internal/httpapi/t329de_enforce_transport_test.go、internal/httpapi/t80_repo_model_test.go；新增：internal/repo/remote_update_merge_test.go、internal/httpapi/repo_update_merge_wire_test.go、tools/difftest/l0081b/update-merge-arms.sh、reports/compatibility/L008-update-merge-diff.md、本日志；零改动确认：web/src（console createRepo=PUT/updateRepo=POST 既有分工）、internal/httpapi/keypair.go（updateRepoKeypairRef 传 *current 全量，merge 下行为不变）
ReviewB 返工: ① B1（P0 数据完整性）：service.go credential 保留臂 GetConfig 错误分流——仅 metadata.ErrRemoteConfigNotFound 容忍（create-crash 愈合窗回落空），其余错误 `fmt.Errorf("remote config %q: %w", …)` 拒绝更新（同函数 UpdateConfig 十行下分流先例同式）；修复前瞬态 DB 错被吞为「无行可保」→UpdateConfig 携空 Password 覆写→sealed password 静默丢失；补单测腿 TestRemoteUpdateCredentialKeepSurfacesGetConfigFailure（remoteGetConfigFailingMD 装饰器注入瞬态错→更新拒绝+remote_configs 行逐列未动断言）。② B2（socketTimeout 别名对显式 0 语义相反）：先 :8082 四探针臂取证——socketTimeoutMillis:0 create/update 均存 0、millis:0 胜过同送 secs:30（落 0）、socketTimeoutMs/socketTimeoutSecs 非参照字段（未知面整丢落默认）；按取证 resolveRemoteAlias 重写（单侧显式 0=给定值——修掉 b 位吞 0；双给定 0 让位非零侧 yield 维持；非零分歧拒维持）+ socket 零解包删除（millis:0 不再回落 secs）；参照无意见的自有拼写（ms/secs）按 ADR 决策 3 字面统一存 0；新增 TestRemoteExplicitZeroAcrossAliasSpellings（五拼写 × create/update 双面表驱动 + yield 双给臂 + millis:0-vs-secs 优先序臂）；重建镜像 uat-l0081b2-95b4acad（DaoCloud 镜像 EOF→l0072 同式离线组装：cached assets rootfs+本地 alpine digest）:8086 双端复验 D1/D2/D3 三臂 SAME、D4 ms-alias 记录臂 DIVERGENT-BY-DESIGN（席位存在性分歧非零语义）。③ N3 随票：t290 两失实行名改写（zero keeps defaults→explicit zeros accepted、ms double zero keeps default→ms double zero is the explicit zero）+ 相邻注释 ADR 化。差分报告补 §0.5（B2 四探针+双端复验表+B1 说明）

Tests:        remote_update_merge_test.go——TestRemoteUpdateMergeMatrix（15 行表驱动：省略/null/空值/显式值 × 标量 credential/period/布尔/对象族，逐席位断言 canonical echo + remote_configs 行；含 credential 单侧清、显式 0 存 0、负值拒、别名分歧拒、url 空白拒、url 重写裁斜杠）、TestRemoteUpdateCredentialKeepSealedRow（omit=sealed 行逐字节保留、显式值重封）；repo_update_merge_wire_test.go——TestRepoUpdateMergeCredentialWire（username null 清/omit 留/写透传三层）、TestRepoUpdateDescriptionMergeWire（description 省略留/null 清/""清/值写）、TestRepoUpdateEmptyBodyKeepsAll（POST {} 200 全保）；t80 重构——PUT-on-existing 400 逐字臂+零副作用臂+无 rclass type-先序臂+POST 404 臂；既有 11 组测试翻转/转 POST 改写（见 Files）
Commands:     go build ./... && go vet ./... && gofmt -l internal/repo internal/httpapi（空）&& ~/go/bin/golangci-lint run ./internal/repo/... ./internal/httpapi/...；go test ./internal/repo/ -count=1；go test ./internal/httpapi/ -count=1；go test -race ./internal/repo/ -count=1 -run 'TestRemoteUpdateMergeMatrix|TestRemoteUpdateCredentialKeepSealedRow|TestT317SmartRemotePairMergeOnOmit|TestM02ZeroPeriodsStoreZero'；go test -race ./internal/httpapi/ -count=1 -run 'TestRepoUpdateMerge|TestRemoteVirtualUpdateREST|TestRepositoriesCRUD|TestT217ManageOnlyHolderOrthogonality'；UAT：make release VER=uat-l0081b-95b4acad → REGISTRY= PUSH=0 ARCHES=amd64 VARIANTS=alpine ./deploy/release/build-release.sh → 独立容器 :8085（不碰共享 binflow-ga）；tools/difftest/l0081b/update-merge-arms.sh（:8082 × :8085 双发对拍）；ReviewB 返工后：四门重跑（同命令）+ go test ./internal/repo/ -count=1（ok 63.776s）+ go test ./internal/httpapi/ -count=1（ok 140.889s）+ go test -race ./internal/repo/ -count=1 -run 'TestRemoteExplicitZeroAcrossAliasSpellings|TestRemoteUpdateCredentialKeepSurfacesGetConfigFailure|TestRemoteUpdateMergeMatrix|TestT290SocketTimeoutSpellings|TestT290SmartRemoteValidation'（ok 23.762s）；B2 取证：:8082 四探针臂（l0081b-z1..z5 仓，删净）；B2 复验：make release VER=uat-l0081b2-95b4acad → 离线组装（DaoCloud EOF）→ 独立容器 :8086 双端 D1-D4 臂
Outputs:      四门零告警（lint 0 issues；初跑 2 gofmt+1 revive unused-parameter 修后清）；repo ok 62.850s / httpapi ok 143.286s（-count=1 全量）；race 子集 ok 10.340s / 10.259s；UAT 镜像 uat-l0081b-95b4acad（/api/system/version 复核）；差分 13 硬断言臂全 SAME——A1 keep-set 归一化逐键同、A3 null 清、A5 "" 清、A7 {} 整族复位、A8 存 0/0/false、A9 url 保留、A10 逐字 `error when validating repository name: l0081b-m : Repository key already exists`、A11 拒后零副作用、A12 create 文案、A13 全量体双端同形、A14/A15 新取证格双端同；补证（:8082）：A14 显式对象=整体替换未提子键复位 false、A15 对象 null=保留整族（两格缺口闭合，实现按补证落——推翻任务书「先行按未提=基线」草案）、A16 无 rclass 先撞 type 拒（次序同/文案异既有族）、A17 anonymous 401 先于一切（authenticated 非 admin 腿参照密码策略拒建用户未取证，缺口留痕）；双端测试仓删净复核零残留；ReviewB 返工输出——四门零告警维持（lint 0 issues）；repo 全量 ok 63.776s（B2 翻转腿修后）/httpapi 全量 ok 140.889s；race 子集 ok 23.762s；B2 取证（:8082）：millis:0 create/update 均存 0、millis:0+secs:30 落 0（0 胜 secs）、ms:0 与 secs:30 非参照字段（落默认 15000）；B2 双端复验（:8086 重建镜像）：D1 create=0/0、D2 优先序=0/0、D3 update=0/0 三臂 SAME，D4 ms-alias=ref 30000（未知字段存量保留）/bf 0（自有席位 ADR 字面）DIVERGENT-BY-DESIGN 记录；B1 单测腿：注入瞬态错→`update under GetConfig failure = transient db failure` 拒绝且 sealed password/URL/username/TTL/SocketTimeoutMs 逐列未动断言通过
Compatibility: 台账 rest/repo-config-update-merge-semantics 建议 resolved（authority=ADR-0050：实现落地+差分复验双件齐）；matrix D02-R03 建议补注「PUT-on-existing 400 对齐闭环」、D02-R04 三列矩阵臂同笔闭环（翻绿建议回报不直改，见 L008-update-merge-diff.md §3）；对拍面五项归一化均既有族非本票引入（cs 输入拼写 nested-vs-flat、清空格 echo 形、customHttpHeaders 无席位、password 回显 NFR-S14、socketTimeoutSecs 种子拼写）；T-290 族 0-as-absent create 面翻转（ADR-0050 决策 3 联裁）——TestM02ZeroPeriodsStoreZero/metadata TTL zero 臂改断存 0，fetch 侧 0=unset 回落链未动（wire/生效分层）
Security:     credential 合并未降安全姿态：canonical 恒无 password（NFR-S14 构造性维持）；sealed 行保留路径只搬运密文不触碰明文；passwordSet 语义缺键=保留密文、显式 null/""=清（常量路径 sealPassword("")）；无新增输入面（PUT-on-existing 400 早退反减写面）；日志无凭证（沿用 WARN drop 通道）；A17 门序（family-7 门先于 400）维持 ADR-0026 姿态
Performance:  UpdateRepo remote 臂新增一次 Remote().GetConfig（仅 passwordSet=false 时，主键点查 O(1)）；handleRepoPost body 一次读全 + 两次 Unmarshal（≤1MB 上限不变）；无其他影响
Risks:        ① 存量 config 无法解码的 remote 行在基线下 url 缺失→更新需显式带 url（fail-closed，自愈路径）；② bool 席位显式 null（*bool 解码 nil）=缺省保留——参照矩阵 null=清仅实测于字符串席位，bool null 未取证（低置信，规格票素材）；③ period 族 null 同上（typed 解码不可见）；④ 对象族证据取自 7.161.20 单实例，字面冻结归规格票；⑤ local/virtual merge 未在本票（ADR-0050 决策 5 另票）；⑥ B2-D4：socketTimeoutMs 为 BinFlow 自有输入席位（参照无此字段），席位内零语义按 ADR 字面——若未来参照版本引入该拼写需重取证；⑦ B1 拒绝路径仍处 Repos().Update 之后（与既有 UpdateConfig 失败同窗的部分写姿态，重试自愈——sealed password 永不丢失，本修复的目标即此）
Blockers:     无
Next:         compatibility-engineer 裁台账 resolved + D02-R03/R04 翻绿；规格票冻结字面（A16/A17 文案与门序、cs nested 输入拼写、bool/period null 形态）；local/virtual merge 另立实现票；breaking note 双变更（PUT-on-existing 200→400、全量替换→merge）release note 首条单窗口公告归 conductor
