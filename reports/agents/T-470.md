# T-470 — 文档迁移 Fern（intake ⑫）

- 票：T-470 [P1] 文档迁移 Fern
- 指令原文：「后续 binflow 的文档迁移至 fern，注意剔除文档中迭代相关的内容，尽量精简」
- 执行：tech-writer，2026-09-04（develop 分支，零 git 提交，BOARD 零写）

## 产出

`docs-fern/`（全新目录，42 文件）：

```
docs-fern/
├── fern.config.yml          # organization: binflow + docs: binflow + 本地预览说明
└── docs/
    ├── docs.yml             # 导航树（1 tab / 7 section / 40 路径，含 icon）
    └── pages/               # 40 页 .mdx
        ├── index.mdx                        # 简介（包型矩阵/URL 拓扑/安全基线）
        ├── getting-started/quickstart.mdx   # 五分钟两条路径（自根 README 提炼）
        ├── install/          ×8             # binary/docker/compose/helm/kubernetes/systemd/offline/upgrade
        ├── integrations/     ×12            # 13 包型（helm 与 helmoci 同篇）
        ├── admin/            ×12            # console/repositories/permissions/tokens/governance/
        │                                     # backup/storage/auth/license/operations/webhooks/monitoring
        ├── search/           ×2             # properties/aql
        ├── tools/            ×2             # bf-cli/migrate-artifactory
        ├── api-reference.mdx
        └── faq.mdx
```

三原则执行情况：

1. **去迭代化**：源 docs/user 共 156 处 T-xxx 引用 + 数百处 M1x/ADR/PRD/验收基线叙述 → 产出为 **0**（唯一例外：api-reference 中 docker virtual 建仓的 400 错误文案逐字保留服务端原文 `...PRD Q4 keeps virtual docker unserved`——该串是 API 实际返回的报错，属契约保真非叙述）。删除项：各篇「适用版本 M1~M14」头、QA 复跑注记（commit/日期/报告链接）、增补速览章节头、历史注记、real-env-appendix.md 整篇（纯 QA 证据归档页）、faq 的 T-298 CD 演示标记段。
2. **精简**：源 46 文件 10,278 行 → 产出 40 页 5,263 行 = **51.2%**（≤60% 目标达成）。合并：auth ← 3 篇（auth-config+oidc+ldap）；storage ← 2 篇（storage-config+s3）；permissions ← 2 篇（groups+rbac）；operations ← 2 篇（artifact-operations+trash-can）；console ← 2 篇（console+path-map）。去重：FAQ 的客户端速查节（与接入指南重复）改为链接；FAQ「常见报错速查」大表与状态码信封表合并；api-reference 七个「M9~M15 增补速览」章节并入各域表格。
3. **信息保真**：API 端点/参数/错误码与文案/配置键/命令示例逐字保留（含 `HIT \| MISS` 转义、40 位 hex、错误信封样例、remote 仓全字段默认值表、webhook 投递常量、AQL 400 点名文案族、keypair/MPU/copy/move/trash/replication 全端点行）。

## 验证（实际执行）

- fern CLI 安装失败（受控环境）：`@fern-api/fern-cli` 与 `@fern-api/cli` 均不可装——后者传递依赖 `@fern-fern/fiddle-sdk@^0.0.193` 不在公共 registry（npm 404，日志 `2026-09-03T18_19_16Z-debug-0.log`）。按票面预案转语法自查。
- 语法自查（scratch 目录 /tmp/fern-mdx-check，已清理）：`yaml.parse` 两配置 + `@mdx-js/mdx@3` 逐页 compile 40/40 + docs.yml↔pages 双向映射 + 40 页内全部绝对路径链接解析。结果 **ALL CHECKS PASSED**（输出：`YAML OK ×2 / pages: 40, nav paths: 40 / ALL CHECKS PASSED`）。
- 去迭代化 grep：`T-[0-9]` / `M1[0-9]` / 里程碑 / `ADR-` / `PRD` / `BOARD` / 冲刺 / 迭代 —— 产出仅 1 处命中（上述错误文案原文）。

## 遗留 / 待确认

- `fern build` / `fern docs dev` 真实构建未跑（CLI 装不了）——**待确认**：可联网环境（或有 Fern 内部 registry 凭据的机器）上执行 `cd docs-fern && npx @fern-api/fern-cli docs dev` 复验；MDX 已过编译器校验，风险集中在 Fern 平台侧（如 docs.yml 字段名演化）。
- `fern.config.yml` 的 `docs:` 键按票面建议书写（organization + docs-name）；Fern 官方现行形态多为 `fern.config.json` 且 docs 配置内聚在 docs.yml——若平台侧校验拒收该键，删除 `docs: binflow` 一行即可（已留注释说明本地预览命令）。
- 发布（Fern 平台账号）由 conductor/用户后续执行，本轮未做。
- 双轨期：`docs/user/` 与 `docs-site/` 未动（零改动）；退役另裁。

## 统计摘要

页数 46→40（含 index/quickstart 新页 2、源 7 篇并入他篇、appendix 1 篇删除）｜正文 10,278→5,263 行（51.2%）｜T-xxx 引用 156→0 ｜新增 42 文件，零 git、BOARD 零写。
