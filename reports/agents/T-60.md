# T-60 — M3 PRD v1.1 校准回写 `role:product-manager`

- 日期：2026-08-19
- 输入：T-59 交付的 `docs/reverse/maven-npm-pypi.md`（306 行）+ `docs/reverse/repo-semantics.md` §7/§8 扩编（remote 六步 pull-through/四值 checksum 策略/故障矩阵/virtual 四桶序/写路由/隐藏 cache 仓；置信度高 ~102/中 ~24）
- 产出：`docs/prd/milestone-3.md` v1.0 → **v1.1**（只改该文件，边界遵守）
- 状态：done（待 conductor 核验）

## 落点清单（§0 v1.1 修订行有全量对照，此处摘要）

1. **§5.5 C1~C8 定案回写**（待校准表 → 校准记录表，四列含依据与置信度）：
   - C1 maven-metadata：服务端计算为准（四类触发时机/版本组与 SNAPSHOT 目录规则/RTFACT-6242 保护性不删/lastUpdated=计算时刻）；「XML union/max 合并」表述修正为「PUT 触发重算」——对外 AC 断言（M15/并发不丢版本）不变。
   - C2 layout：maven-2-default 六字段 pattern 定案（一手模板）；**400 码值维持暂行**（规格只见模型未见码值）。
   - C3 virtual：四桶序定案 → BinFlow 两桶简化（无 `<key>-cache` 投影，语义等价）；stale 命中即返回、真 404 才进下一成员。
   - C4 TTL 7200/1800 定案（另 socketTimeout 15s / assumed-offline 300s）；HEAD+Last-Modified 协商确证存在 → BinFlow P0 到期重取、协商列 P1；并发 miss 单飞 P1。
   - C5 `defaultDeploymentRepoRef` 别名增补 + 405 文案定案；指向非 local 前置 400（BinFlow 有意从严）。
   - C6 remote 删除不同步上游维持、置信度升高。
   - C7 PyPI 布局对齐 `{name}/{version}/{filename}`（原始名）+ `/packages/` href → **PE-03 升格兼容（子集）**。
   - C8 npm tarball 布局与 scoped 形态定案；`_attachments` 清空维持暂行（对外不可观察）。
2. **M1 勘误吸收**：snapshot policy **404→409**（ME-08/FR-16-AC7/M19 三处同步，高置信度）；includes/excludes 双值码（下载 404/上传 409）不触 M3 AC，§6.4 归档（M1 PRD 本体勘误走另行票）。
3. **Q7 定案**：npm 错误体 E-01 定案（三协议共用 `errors[]` 信封，规格 §0）；**重复 publish 409→403**（`Cannot modify pre-existing version`）——NE-01/FR-18-AC6/M26 更新，计数注的「有意偏离分支」消除（有意不兼容分支 2→1）。npm 十步校验链、`-rev` PUT 恒 200 假成功（unpublish 前置，缺则 npm unpublish 必断）、dist-tags 201 文案、ETag=包文档 sha1+304、SLIM Accept（P1）、`%2f`/`%2F` 等价全部入 FR-18 规格（新增 AC10/AC11）。
4. **Q3 定案**：PyPI 仅 sha256（规格 §3.4：pip 依赖 `#sha256=`、Artifactory 的 md5 仅无 sha256 兜底）；`md5_digest` 可缺失（twine ≥ 6.2）+ `:action` 严格 400 `unknown action` + api-version=2 头 + 无尾斜杠 302 + ETag/304 入 FR-19/PE-01/PE-02（新增 AC9，M31 补 4 条探针）。
5. **Q4 维持不动**（conductor 已转用户知悉，行内注明）。
6. **连带定案**：remote checksum 后缀不回源 404 `"Checksums are not downloadable."`（FR-20 请求流第 2 步 + 新增 AC13 + M45 探针）；上游故障默认 **404**（assumed-offline 300s 静默）+ `hardFail:true` → 502（**推翻 v1.0 默认 502**：FR-20 请求流第 5 步/AC5/AC10/M44/NFR-S15 五处同步，M44 加静默期负断言与 hardFail 对照仓）；`snapshotVersionBehavior` 三值（deployer 默认，服务端 unique 改写 P2 + 新增 AC12）；virtual priorityResolution 启用（FR-15/FR-21/RE-02/RE-07/新增 AC9）；remote 字段与默认值对齐（RE-01）；npm/pypi 域根探针与隐藏索引目录原则（metadata 表为中心）入规格。
7. 计数更新：35 端点 = 兼容 29 / api/v1 1 / 语义等同 0 / 有意不兼容 5（+RE-08 DELETE 行内分支）；顺手修正 §0 v1.0 行的「37 条」残留为 35（与 §5.2 实际行数一致）。

## 自检证据

- `rg '待校准|TBC|TODO|待 §5.5|→ 409（同版本|拒绝 → 404（拒绝）|默认 60s' docs/prd/milestone-3.md` → 0 命中（无占位、无旧口径残留：409/404/502/snapshot 404 均已定向更新）。
- 结构核对：`### FR-` 8 个（FR-15~22）、端点行 35（RE 11/ME 10/NE 8/PE 6）、`## 0~9` 章节齐——与 v1.0 骨架一致，只增改内容未动结构。
- 交叉引用抽查：M19/M26/M27/M31/M44/M45 命令期望值与对应 AC/矩阵行三方一致（409/403/404+502/302+304+action/`Checksums are not downloadable.`）。
- 两处 Edit 首次未命中（「等价/等效」「NE-08/NE-01」笔误），经 Read 回读后全部命中——无半改状态。

## 遗留 / 转交

- Q1（凭据存储）/Q2（virtual 写路由默认）仍待用户拍板；Q5 剩 refresh 参数形态、Q6 剩保留策略时机——暂行假设均可执行，不阻塞拆票。
- 规格侧待验证项已挂断言或 P2 备注：`-rev` 隐式副作用（FR-18-AC11）、`%2f`/`%2F` 等价（NE-01）、PEP 691 仓库级开关（PE-05）、unique 改写对拍（FR-16-AC12）、virtual DELETE 逐成员遍历（RE-08 注）。
- M1 PRD 本体的两处勘误标注（snapshot 409、includes/excludes 双值码）不在本票边界，建议 conductor 另派 M1 勘误小票（同 T-56 先例）。
