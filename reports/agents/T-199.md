# T-199 — 指标名文档同步（T-197 遗留①）工作日志

- 状态: done（自测全绿，待 review/qa）
- 日期: 2026-08-22
- role: tech-writer / area: `docs/user`（两文件）
- 依据: reports/agents/T-197.md §4.1（消费方清单第 1 条）、§1 AC1（D5 改名裁决）、`internal/httpapi/metrics.go` family 常量、`docs/prd/milestone-6.md` v1.3 FR-61 表

## 1. 做了什么

### AC1 旧名 → T-197 新名（4 处，与 T-197 §4.1 清单逐处对上）

| 文件:行 | 旧 | 新 |
|---|---|---|
| `docs/user/metrics/prometheus-reference.md:48` | `binflow_storage_blobs_total` | `binflow_storage_blobs` |
| `docs/user/metrics/prometheus-reference.md:49` | `binflow_storage_blob_bytes_total` | `binflow_storage_blob_bytes` |
| `docs/user/metrics/prometheus-reference.md:106`（PromQL） | `binflow_storage_blobs_total` | `binflow_storage_blobs{engine="disk"}`（顺带把裸指标写成带标签示例） |
| `docs/user/guides/s3-config.md:153` | `binflow_storage_blob_bytes_total{engine="s3"}` | `binflow_storage_blob_bytes{engine="s3"}` |

两处顺带（同文件、同主题、不越 area）：

1. **头部版本注**（prometheus-reference.md:8）：补「指标命名按 T-197 D5 校准」，与 PRD v1.3 口径互指。
2. **升级注意块**（存储表后新增 blockquote）：T-197 §4.3 明确「已起的 Prometheus 面板若引用旧名需随发版更新（docs/user 修复票一并覆盖）」——补一条面向面板/告警规则维护者的改名提示（无旧名别名，旧 PromQL 去掉 `_total` 才有序列）。旧名以「现名 + `_total` 后缀」的公式表述而非全称拼写，保证全库旧名字面 grep 清零（AC 自测硬条件）。

### AC2 全量指标名核对（逐名 grep，双向）

- **正向**（代码 7 family → 两文档命中计数）：`binflow_http_requests_total`(ref×4) / `binflow_http_request_duration_seconds`(ref×2) / `binflow_http_requests_in_flight`(ref×1) / `binflow_storage_blobs`(ref×2) / `binflow_storage_blob_bytes`(ref×1, s3×1) / `binflow_auth_logins_total`(ref×5) / `binflow_replication_tasks`(ref×2)，全部在场且拼写一致。
- **反向**（两文档出现的全部 `binflow_*` 记号 → 对照 metrics.go 常量）：7/7 OK；唯一多出的 `binflow_http_request_duration_seconds_bucket` 是 histogram 族的标准 exposition 序列后缀（`_bucket`/`_sum`/`_count`，见 `internal/metrics/metrics.go:281-285` 的发射代码），族基名即代码注册名——非文档杜撰名，不改。
- 标签与语义面核对（无需改）：method/path/status（:164）、method/path（:165）、in-flight 无标签（:156）、engine=disk|s3（:87/:136-141）、source=local|oidc|ldap（:89-91）、status 五值闭集（:96-101）与两文档表格逐项一致；「7 族」TYPE 行计数与 T-197 端到端证据一致。

### AC3 `make docs`

`[SUCCESS] Generated static files in "build"` + 拷贝 `internal/docs/dist` + `docs site size (raw total): 2.60 MB` 全过。构建报警（docker-registry 页链 `deploy/dev/README.md`、首页 getting-started 两链、governance 锚点）均为**既有问题**，涉及页面不在本票两文件内，未处理（见 §4 遗留 1）。

## 2. 改动文件

| 文件 | 改动 |
|---|---|
| `docs/user/metrics/prometheus-reference.md` | 3 处指标名 + 头部版本注 + 升级注意块 |
| `docs/user/guides/s3-config.md` | 1 处指标名（:153） |
| `docs-site/**`、`internal/docs/dist/**` | `make docs` 再生构建产物（AC3 副产品） |

## 3. 自测（实际命令与输出摘要）

```
$ grep -rn 'binflow_storage_blobs_total\|binflow_storage_blob_bytes_total' docs/user/
                                                    → 无输出，exit=1（旧名清零）
$ for n in <metrics.go 7 个 family 名>; do grep -c "$n" <两文档>; done
                                                    → 计数见 §1.AC2 正向表，拼写零偏差
$ grep -rhoE 'binflow_[a-z_]+' <两文档> | sort -u | 逐名 grep metrics.go
                                                    → 7 OK + 1 个 histogram _bucket 序列后缀（代码 :281-285 证实）
$ make docs
                                                    → [SUCCESS] Generated static files in "build"
                                                      docs site size (raw total): 2.60 MB (2725689 bytes)
$ grep -rl 'binflow_storage_blobs_total\|binflow_storage_blob_bytes_total' docs-site/build/ internal/docs/dist/
                                                    → 无输出，exit=1（构建产物旧名零残留）
$ grep -rl 'binflow_storage_blob_bytes' docs-site/build/ internal/docs/dist/
                                                    → 命中 assets/js/0942326c… 与 ac0b2547… 两个内容 chunk（新名已编译进场）
```

## 4. 遗留 / 需上游确认

1. **make docs 既有断链/断锚**（非本票引入，area 外未动）：`/binflow/docs/docker-registry` → `../../deploy/dev/README.md`；文档首页两条 getting-started 链；governance 页指向首页中文锚点。建议另立 docs 修缮票。
2. T-197 §4.2 的 **ADR/DECISIONS.md 指标名回写与 ADR 编号错位**（ADR-0021/22/23 定序）仍在 architect 侧，本票未碰（area 外）。
3. T-197 §4.5 的 M7+ 指标（`logical_bytes{repo}`/`sessions_active`/复制延迟直方图）排期归 tech-lead/product-manager，本票文档无需预写。

## 5. 规格依据

- reports/agents/T-197.md：§1 AC1（D5 裁决与两 gauge 新名）、§3（promtool exit=0 证据中的 7 条 TYPE 行——文档「7 族」计数背书）、§4.1/§4.3（本票范围与面板消费方覆盖要求）
- `internal/httpapi/metrics.go:33-41`（family 常量唯一事实来源）、`internal/metrics/metrics.go:281-285`（histogram 序列后缀发射）
- `docs/prd/milestone-6.md` v1.3：§0 修订表 v1.3 行、FR-61 行为规格 :450-462（`binflow_storage_blobs{engine}` / `binflow_storage_blob_bytes{engine}` 正式名）、FR-61-AC1 :468、K6 :767
- BOARD.md T-199 条目（:75/:513，只读）
