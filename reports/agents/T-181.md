# T-181: [P2] PRD M6 勘误（migration 旧字段名 + Q6/Q7 暂行假设回写）

- **Area**: docs/prd（仅 milestone-6.md）
- **Status**: done
- **Dep**: T-176（§7.1 契约回写版，本票上游依据）

## 做了什么

### AC ① — 迁移端点旧字段名全量清除（6 处落点）

权威依据：docs/design/architecture.md §7.1（T-176 回写版），并与实现逐字段核对一致——
`internal/storage/migration.go` L57-68 `MigrationStatusView` json tag（`running/done/total/migrated/
skipped/failed` + `error/started_at/finished_at` 三者 omitempty）；501 语义与 `internal/httpapi/
migration.go` L18-43（GET/POST 两处，Migration dep 为 nil → `writeError(501, "migration is not
configured")`；start 幂等 202 / 拒启 409）核对一致。

| 位置（v1.1 行号） | 旧 → 新 |
|---|---|
| FR-50 行为规格 L179 | `{"total_blobs","migrated","in_progress","completed"}` → 实际契约全字段（含 total/skipped/migrated/failed 各自口径 + 从未启动返回全 0 基线快照 + 旧拟名作废注记） |
| FR-50 新增两行为 L180/L181 | 补录实际契约缺失面：`POST /api/v1/storage/migration/start`（幂等 202 + 状态体 / 拒启 409）；**501 语义**（`backend≠s3` / `migration.enabled≠true` / 已置 `migration.completed=true` → 两端点 501「migration is not configured」，非 404） |
| FR-50-AC1（H12）L186 | `completed=true` → `done=true`（`running=false`） |
| FR-50-AC2（H13）L187 | `completed=true` → `done=true` |
| §5.4 H12 命令 L639/L640 | `jq -r '.completed'` → `jq -r '.done'`；新增 501 对照腿 curl（未配双写 → 501 非 404） |
| §5.2 矩阵 S3-03 行 L566 | 行为要点补实际字段清单 + 501 语义 |

### AC ② — Q6/Q7 暂行假设回写（§7 开放问题区）

- 表头改「暂行假设（v1.0；v1.1 起标注实现状态）」。
- **Q6**：改标「**暂行已实现，待终裁**（T-162 口径）」——落法 = `target_repo` 指向可写 local 仓（backing）+ 未配置写路由的 virtual 只读门面（members=[backing]），复用 M3 virtual 既有行为；注明暂行下 backing local 本身仍可被目标实例用户直写；v1.0 暂行假设（`rclass=local + replica=true`）未实施；切换位在 `pushOnce` 推送落点寻址（`PUT /binflow/{targetRepo}/{path}`），引擎骨架不动。
- **Q7**：改标「**暂行已实现，待终裁**（T-162 口径）」——checksum 一致 → 幂等成功（零传输、不打开源 blob）；不一致 → 任务终态 `status=failed`（attempts 记满防 cron 复活、completed_at 置位）、不动目标；注明与 ADR-0021 决策 4 字面（updated_at / first-write-wins / 记 skipped）及 H47 `status=conflict` 词汇的偏差（009 任务状态闭集无 conflict，暂行取 failed + conflict 文案）；切换位在 `pushOnce` 的 HEAD 分支（internal/replication/engine.go）。
- **Q10（新增）**：私有复制目标默认放行——`DenyPrivateTargets` 默认 `false`（scheme/host 校验、逐跳重检、DNS-rebinding pinning 仍生效）；建议 config 桥接票增 `replication.allow_private_target`（默认 true）。源自 T-162 假设清单第 3 条。
- 交叉指针 3 处（防正文与开放问题自相矛盾）：FR-57「目标端行为」、FR-59「冲突处理」、§5.4 H47 注释各加一句 v1.1 注，指向 §7 Q6/Q7 暂行口径。

### AC ③ — 勘误记录

- §0 修订表新增 v1.1 行（2026-08-22，T-181：migration 契约对齐实现 + Q6/Q7 暂行口径 + Q10 增补）；头部「状态」字段 v1.0 → v1.1；文脚注补 v1.1 出处与依据链。

## 改了哪些文件

| 文件 | 变更 |
|---|---|
| `docs/prd/milestone-6.md` | 上述全部（14 处编辑；本票唯一改动的既有文件） |
| `reports/agents/T-181.md` | 本工作日志（新文件） |

## 自测（实际执行，ripgrep 经 Grep 工具）

1. 旧字段名 `total_blobs|in_progress`：命中 **2 处**——L18（§0 勘误记录行）与 L179（FR-50 行为规格「旧拟名……作废」注记）。两处均为标注「作废」的勘误记载、非契约正文（沿 M1 PRD T-21 核验先例：对照记载中的旧值为有意保留）；**行为规格 / AC / 验收命令 / 端点矩阵中的旧字段名零残留**。
2. `completed`：命中 6 处——L18（勘误记载）、L178（配置键 `storage.migration.completed`，architecture §8 实键，非端点字段）、L179（作废注记）、L181（配置键 `migration.completed=true`，501 条件之一）、L846（Q7 行的 009 复制任务列 `completed_at`）。无一处是迁移端点响应字段残留。
3. 「暂行已实现，待终裁」：3 处（Q6/Q7/Q10）；`pushOnce`：2 处（Q6/Q7 各注明切换位）。
4. `storage/migration`：6 处落点（L179/L180/L186/L566/L639/L640）全部为新契约口径。
5. 改动面：全部编辑经 Edit 工具落盘于 `docs/prd/milestone-6.md` 单文件（无脚本/批处理波及面）；本 agent 未触碰该文件与 `reports/agents/T-181.md` 以外的任何文件（BOARD.md 只读未动、ROADMAP 未动）。注：仓库存在其他在途 ticket 的未提交改动，conductor 核验本票范围请按 `git diff -- docs/prd/milestone-6.md` + `git status --short reports/agents/T-181.md` 判定归属。

## 依据

- `docs/design/architecture.md` §7.1（T-176 回写版：GET/POST migration 两端点契约 + 501 语义）与 §8 config 模型（`migration.enabled/completed/concurrency` 键名核对自 internal/config）。
- `internal/storage/migration.go`（MigrationStatusView json tag，逐字段一致）。
- `internal/httpapi/migration.go`（501 文案与 start 幂等语义）。
- `reports/agents/T-162.md`「Q6/Q7 暂行假设」四条清单：前三条（Q6 落法 / Q7 冲突 / 私有目标放行）入本票；第 4 条（主密钥复用 `BINFLOW_REMOTE_CREDENTIALS_KEY`）属 NFR-S37 面，PRD 既有文本未涉及，未动。

## 遗留（供 conductor 知悉，非本票范围）

1. `ROADMAP.md` M6 条目仍写「PRD v1.0（T-148）」——本票范围禁改 ROADMAP，版本引用待 conductor 顺手更新或另票。
2. PRD §5.5 K4/K5/K6 与 §9 DoD⑤ 的 ADR 拟编号（ADR-0021=LDAP / ADR-0022=复制 / ADR-0023=Prometheus）与 DECISIONS.md 实际发布编号（ADR-0020=OIDC/LDAP、ADR-0021=复制、ADR-0022=Prometheus、ADR-0023=bf CLI）存在错位——v1.0 撰写时 ADR 尚未发布，建议随下次 PRD 修订或 ADR 索引票收口。
3. Q10 为本次新增开放问题（T-162 假设第 3 条），待用户终裁；Q6/Q7 终裁后需再回写 FR-57/FR-59/H40/H47 为定案口径。
