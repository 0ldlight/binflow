Ticket:       FE-P1 — 前端重写 Phase 1 Foundation（依赖装配 + Tailwind/tokens 桥接 + shadcn primitives + app/lib/stores 骨架 + 构建链适配）· P0
Role:         dev-frontend（area = web/src 新基座目录族；旧 MUI 应用零触碰）
Input:        docs/design/frontend-rewrite-architecture.md §0/§1/§2/§3/§5/§9/§10-P1 行；frontend-rewrite-audit.md §一.1 依赖账/§一.2 vite/§一.8 api 层/§四.2 i18n 机制/§四.4 构建链；docs/design/frontend-capability-matrix.md（背景，未动业务页面）
Changes:      ① 依赖装配 26 包（registry.npmjs.org 直指；engines 未动）；② styles/tw 层：tokens.css 双主题值自旧 styles/tokens.css 原样迁移（唯色值层，两文件并行服役不同页加载）+ tailwind.css（@theme inline 桥接 43 个语义变量 + @custom-variant dark 绑 [data-theme]）；③ components/ui 19 个 shadcn primitives 源码进仓（React 19 无 forwardRef 形态，中文 doc 注释）；④ app/ 骨架：config（UI_BASE 双真值之 #2/STORAGE_KEYS/POLL_MS/分页档）、providers（Query+Theme+Toast+Confirm 装配，不接线）、router（createBrowserRouter 空路由表 + basename）、shell（AppShell/Sidebar/Topbar + nav-model 四分组）；⑤ lib 三件：api（三入口一信封：API_ROOT/EVENT_ROOT/contentUrl + toApiError 逐行平移 + 401 监听 + body 三形态）、query（QueryClient retry=1/refetchOnWindowFocus=false + qk key 工厂 14 族）、format（bytes/datetime/number 纯函数平移，双 locale 走现 i18n）；⑥ stores 五仓（ui/command-palette/session/preferences/ai——types+空实现，PREF_KEYS 与现役 localStorage 键逐字一致）；⑦ 构建链：vite proxy 合并为整 /binflow 前缀 + /binflow/ui bypass（修 event/内容/docs 面 dev 盲区）、@tailwindcss/vite 入插件链、'@' 别名（tsconfig paths + vite resolve.alias，支撑 components.json shadcn CLI 流）、assert-tokens 增 styles/tw 豁免层（闸语义不降）；⑧ foundation.ts barrel（冒烟载体，不进生产模块图）
Files:        新增 52：web/components.json、web/src/app/foundation.ts、app/config/1、app/providers/5、app/router/1、app/shell/5、components/ui/19、lib/api/3、lib/query/3、lib/format/4、lib/utils/2、stores/5、styles/tw/2。修改 5：web/package.json、web/package-lock.json、web/tsconfig.json、web/vite.config.ts、web/scripts/assert-tokens.mjs。删除 0。旧应用零触碰：main.tsx/路由表/页面/组件/src/i18n/ 全部原样
Tests:        P1 无新 e2e spec（票面范围=基座，无可见页面）；验证走 barrel 冒烟构建（2054 模块全量编译过真 vite 配置）+ console-smoke e2e 对 dev server 2/2 绿（shell 挂载 + 深链 fallback，证明 proxy bypass 不破 SPA）
Commands:     npm run typecheck；npm run lint（eslint . && assert-i18n）；npm run build；node /tmp/fe-p1-smoke.mjs（vite JS API 以 foundation.ts 为入口）；npm run dev + curl --noproxy（四面探针）；BASE=http://localhost:5173 npx playwright test e2e/console-smoke.spec.ts --project=chromium
Outputs:      typecheck 0 错；assert-tokens OK（css 13 + tsx 86 + styles/tw 豁免 1）；assert-i18n OK（2713 调用点/2054 键，与基线逐字一致）；build 五步全绿（881 modules，3.3s）；gzip js+css = 518879 bytes = 基线 518879（零增量）；relink 2 URL+1 preload join→/binflow/assets/、wire-brand 7 文件指纹化（与基线签名一致）；smoke 构建 foundation JS 79.04KB(gzip 23.51KB)+CSS 26.37KB(gzip 6.11KB)；dev 探针：/binflow/ui/=vite shell、api=BinFlow JSON、event=E-01 401 JSON（盲区已修）、内容面=E-01 404 JSON、docs=200 html；e2e 2 passed (2.1s)
Compatibility: 契约面未触碰（无 API 消费变更）。锚册无涉（无页面改版）。唯一行为增补=vite dev proxy 扩为整 /binflow 前缀（audit §五 盲区 1 承址）：已知边界=dev 下名为 "ui" 的仓库内容路径被 SPA 段遮蔽（/binflow/ui/…），prod embed 不受影响，已注记 vite.config
Security:     无用户内容渲染面（纯基座）；XSS 面零新增；敏感信息零落日志（toApiError raw 仅存内存 Error 对象，与旧实现同姿态）
Performance:  生产 bundle 零增量（518879=518879 bytes，foundation 不在主模块图）；smoke 实测全 primitives+radix+zustand+react-query 树 gzip 23.51KB；ag-grid-community(22MB)/lucide(44MB)/zod(7.7MB) 磁盘大但零 import=零 wire 成本，P2+ 按路由懒载（§1 预算分解）
Risks:        ① lint 全树有 1 个既存 error（见 Blockers）非本票引入；② tailwind preflight 与旧 CSS 同页混载未验证（P1 不同页加载，P2 挂载策略时需裁定整页换栈）；③ vite dev 绑 [::1] 仅 IPv6 loopback（vite 默认行为，非本票变更，本地 curl 需 localhost 或 --noproxy）；④ components.json 的 shadcn CLI 未实跑验证（无网络依赖需求，drop 生成物需手工对齐 token 语义）
Blockers:     无阻塞本票。上报既存项：npm run lint 全树 1 error = src/pages/builds/api.ts:160 no-useless-catch（try/catch 仅 throw err 包装）——该文件为工作树在途未跟踪文件（builds 域他票 area），P1 零触碰；属收编该票时的哨兵修复项，非本票范围
Next:         ① P2 核心 UX 批次（router 路由表逐域落 + Login/Shell/Dashboard/Explorer，锚迁移 --ledger 硬门随开）；② relink-assets 扩展腿预研（Monaco/AG Grid worker 资产扫描——R1 首验项在引入时做）；③ 锚册侧建议：skeleton 四态缺省锚已在新 primitives 预置 data-testid（skeleton/confirm-phrase-input/confirm-accept），登记归 P2 anchor-audit 批次；④ 建议 conductor 让 builds 在途票收编时清 no-useless-catch；⑤ tw-animate-css（弹层进出场动画）留待 P2 视觉细化时拍板引入
