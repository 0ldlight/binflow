# T-384 工作日志 — FR-124.3 M3 用户/组创建（改判：断言收口小票）

- 角色：dev-frontend ｜ 状态：**done**（四闸门 + m14 spec 4/4 + m8/security/rbac/keyboard 19 + m10 7 + m9 串行甄别 6/6〔A/B 对照证明既有 flake 与本票零耦合〕+ a11y-sweep 3/3 + 服务端 diff=0）
- area：`web/src/pages/security/UsersPage.tsx` / `GroupsPage.tsx`（十处纯锚落点）+ `web/e2e/m14/`（新 spec）+ 锚册 console-ux.md（v1.20 批块）。**并行票 T-394（internal/httpapi + charts + cmd）零触碰零冲突；parity 册（console-artifactory-parity.md）非本票 area，未动。**
- 参数源：**T-381 活体核验 v1.1**（`reports/agents/t381-evidence/t381-measurements.json` `V6_forms` + 30/31/32/34-*.png）——Artifactory OSS 7.84.10 实测。

## 0. 改判确认

原票「用户/组创建 modal 化」（决策项 B）基于 v1.0 记忆「Artifactory 创建 = modal」；T-381 实测证伪（7.84 用户/组创建 = **整页路由表单** `/ui/admin/management/users/new`、`/groups/new`），**决策项 B 撤销**。parity 册 M3 差距行 v1.1 已裁定「BinFlow 列表页内建表单与路由页表单的差异属同档形态，可保持」。本票按改判后口径执行：**对照核验 + 断言钉死 + 差距票内定案留痕**，不做形态改造（本票实际零形态改动——唯一 src diff = 十处 data-testid 纯锚落点，六节锚新增 + 四枚页脚锚复役）。

## 1. 对照核验表（AC1 核心交付）

实测源：Artifactory = t381-measurements.json `V6_forms`（DOM 实测）；BinFlow = 本票 Playwright 实测（净实例 :8144，最终构建）。

| # | 核验项 | Artifactory 7.84 实测（v1.1 V6） | BinFlow 实测（本票） | 判定 |
|---|---|---|---|---|
| 1 | **创建入口形态** | users/groups 列表页 `New user` / `New Group` 钮 → 路由页表单 | 列表页「＋ 新建用户」/「＋ 新建组」钮 → `user-form`（aria-label="新建用户"）/ `group-form`（aria-label="新建组"）就地展开；**非 modal**（表单开态页内 `[role=dialog]` 计数 0——正是决策项 B 撤销的实证面）；L4 入口门 = admin-only 渲染（readonly_admin 无创建钮，m8 users-groups 既有腿承载） | ✅ **对齐钉死**（入口 = 列表页按钮同形 + 创建动作非 modal；test 1/2 断言入口→表单开态 + dialog 反断言） |
| 2 | **整页路由 vs 列表页内建** | 专属路由 `/ui/admin/management/users/new`、`/groups/new` 整页表单；保存后回列表 | 同路由就地：URL 保持 `/admin/security/users`、`/admin/security/groups`，**列表上下文保持在场**（users-table/groups-table 与表单同屏）；保存（或取消）后表单收拢回列表面（对位 Artifactory「保存后回列表」）；编辑面：用户 = 路由页 UserDetailPage、组 = 同内建编辑态 | ✅ **同档形态，票内定案不追平（留痕）**：parity v1.1 M3 行既有裁定「可保持」；理由 = 列表上下文不消失 + 少一跳导航（P1 信息密度），且 BinFlow 用户编辑已是路由页——创建内建/编辑路由的两档各有承载；test 1/2 把「URL 不离列表 + 列表在场 + 收拢回列表面」钉进断言（改形态须有意识动 spec） |
| 3 | **表单结构（用户面）** | 字段：User Name / Email Address / Password / **Retype Password** + 复选项（Administer Platform / Manage Resources / Can Update Profile / Disable UI Access / Disable Internal Password）；页脚 **Cancel（最左 x=294）/ Reset / Save（右）** | 四节（v1.20 节锚）：**用户设置**（用户名\*/Email\*/角色三值下拉）/ **选项**（启用复选）/ **口令**（初始口令\*）/ **相关组**（双列穿梭）；页脚 取消（outlined 最左）/ 重置（outlined）/ 创建用户（contained 主钮右）——**几何序 Cancel < Reset < Save 钉死**（boundingBox，V6「Cancel 最左/Save 右」同形体例）；必填门 = Save 置灰直到用户名/Email/口令齐（reverse §4.6，对位 Artifactory blur 校验 + Save 门） | ✅ **对齐钉死**（核心字段集 name/email/password + 页脚三联 + 必填门）；**超集不违 parity**：角色三值下拉（M7 FR-66 闭集）+ 相关组穿梭（Artifactory Related Groups 对位增强）。**差距两枚票内定案**：① 创建态无 Retype Password——管理员代设初密场景的编辑页有 `user-form-password2` 确认口令（console-m8 §6.9 线框「口令/确认口令」注记即挂编辑态），创建态不补（零形态改动主预期 + 差一档可即时编辑重置）；② 复选集（Administer Platform 等 5 枚）vs 三值角色 = RBAC 模型差（FR-66 / rbac-model §5 有意不跟进 Artifactory 布尔矩阵），非 parity 缺口 |
| 4 | **组面** | Group Name / Description / **External ID** + 复选项（含 **Automatically Join New Users to this Group**）+ 成员用户选择列表（admin/anonymous/carol/dave）；页脚 Cancel/Reset/Save | 两节（v1.20 节锚）：**组设置**（组名\*/描述）/ **成员**（双列穿梭 = Artifactory Users 双列的对位形态，勾选即入已选列）；页脚三联同形同序 | ✅ **对齐钉死**（组名/描述 + 成员选择列表 + 页脚三联）；**External ID / Auto Join 不建 = console-m8 §6.10 既有裁定**（「无外部组模型」——组实体仅 {name, description}，milestone-4 §组实体）；spec 反断言表单内不出现二者字样 |

**差距清单结论：零「需收口」差距**——核验项要么对齐（钉死为断言），要么已有裁定在案（parity v1.1「同档形态可保持」/ console-m8 §6.10「External ID / Auto Join 不建」），其余为模型差或票内定案（创建态无 Retype，理由见表 #3）。故本票**未引入任何形态改动**，唯一 src diff = 十处锚（六节新增 + 四枚页脚复役）。

token 面（V6c）：不在本票口径内（票面 AC 无 token 项；parity 批 2 ⑤ Tokens 页真身承接），未触碰。

## 2. 实现（最小面）

### 2.1 src（UsersPage.tsx / GroupsPage.tsx，+10 属性，零逻辑）

- 六节锚（v1.20 入册）：`user-form-section-{settings|options|password|groups}`（创建表单四节 div）+ `group-form-section-{settings|members}`（组表单两节 div；编辑态组权限矩阵节沿用 group-perm-matrix 既有锚）。
- 页脚四锚复役（v1.9「零 spec 消费」退役，本票页脚三联断言消费——T-382 smu-close/smu-done 复役同款流程）：`user-form-cancel` / `user-form-reset` / `group-form-cancel` / `group-form-reset` 落回取消/重置按钮。
- 渲染与行为零变化；`users-*` / `user-form-*` / `groups-*` / `group-form-*` 冻结族零改名。

### 2.2 e2e（`web/e2e/m14/t384-usergroup-parity.spec.ts`，4 tests）

1. **用户创建全链**（AC2 主断言）：列表入口 → 内建表单开态（非 modal + URL 不离列表 + 列表在场）→ 四节结构与字段归属 → 必填门（置灰→放行）→ 组穿梭入组 → Save 落库（toast `用户 X 已创建` + 表单收拢 + 新行含组）→ **API 对账**（GET 全量回显：name/email/adminRole=user/enabled/groups）→ 收尾级联清理。
2. **组创建全链**：入口 → 两节结构（含 External ID / Auto Join 反断言）→ 必填门 → 成员穿梭（勾选用户入已选列）→ Save 落库（toast `组 X 已创建（成员 +1）` + 新行）→ **API 对账**（E5 `?includeUsers=true`：userNames === [成员]——逐用户组集替换落盘的事实源复核）→ 收尾。
3. **形态钉死**：页脚三联双表单同形（DOM 序 + boundingBox 几何序 Cancel<Reset<Save + Save contained/其余 outlined）；Reset 语义（回初值、不提交、必填门复灰）；Cancel 语义（表单收拢回列表面、URL 不动）。
4. **axe 双主题**：用户创建态开态（四节 + 穿梭就绪）+ 组创建态开态 × light/dark = 4 扫 serious/critical=0（a11y-sweep 路由面只见列表闭态——**创建态开态是本票新增扫描面**）。

净实例零组时 groups 页呈空态而非表——组腿以背景组 API 备料保证「列表上下文在场」断言确定性（组表本体渲染条件）。

## 3. 锚册（console-ux.md v1.20）

- 头表票据列 + 状态行 → v1.20；§0 修订记录 +1 行（改判背景 + 六节锚 + 页脚复役 + e2e 新面摘要）。
- §10.3 追加 **T-384 批块**：六节锚（含字段归属语义）+ 页脚四锚复役注记。
- §10.6 退役总表：摘除复役四枚（rows 留「曾列本表，T-384〔v1.20〕复役并回归在册」注记，T-382 同款）；总表计数 prose 修正 98→**92**（含 T-382 已复役两枚的历史欠账一并结清）。
- 对账：ledger PASS；unregistered/broken 双零；dead 桶回归既有 4 家族（`${rowTestidPrefix}-matrix` / `groups-sort-name` / `node-props-cancel` / `user-form-role-*`）——本票 10 锚全有字面 spec 消费，**dead 桶零新增**。

## 4. 自测证据（实际执行）

```sh
cd web && npm run typecheck        # 通过（零输出）
         npm run lint              # 通过（零输出）
         npm run build             # assert-tokens: OK — css 11 + tsx 46；✓ built in 4.50s
         node scripts/anchor-audit.mjs --ledger
                                   # PASS（unregistered/broken 双零；dead 桶 4 家族=既有；retired 全部在表）
# 手作等价 make console（rm assets + cp web/dist）→ SPA gz 353543 B（5MB 预算内；T-383 基线 353508 → +35B = 十属性的哈希级增量）
make build（go build binflow-server，embed 最终构建）

# 实例：/tmp/bf-t384（净实例 :8144，BINFLOW_ADMIN_PASSWORD=password，scratch data dir；最终构建重启后复跑）
BASE=:8144  npx playwright test e2e/m14/ --project=chromium          # 8 passed（43.1s）= T-384 4/4 + T-383 4/4 零回归
BASE=:8144  npx playwright test e2e/m8/users-groups.spec.ts e2e/security.spec.ts \
              e2e/rbac.spec.ts e2e/m8/keyboard.spec.ts --project=chromium
                                                                   # 19 passed（2.9m）——m8 用户组面 + 键盘（user-form 锚消费方）
BASE=:8144  npx playwright test e2e/m10/properties-matrix.spec.ts --project=m10
                                                                   # 7 passed（50.8s）——m10 用户邻接消费面
BASE=:8144  npx playwright test e2e/m9/users-groups.spec.ts --project=m9 --workers=1
                                                                   # 6 passed（2.4m）——m9 E2/E3/E5 视图腿（见 §4.1 flake 处置）
BASE=:8144  npx playwright test e2e/m8/a11y-sweep.spec.ts --project=chromium
                                                                   # 3 passed（1.6m）——全路由面 × 双主题 serious/critical=0

# 服务端 diff=0：git status 本票足迹 = docs/design/console-ux.md + web/src/pages/security/{UsersPage,GroupsPage}.tsx
#   + web/e2e/m14/t384-usergroup-parity.spec.ts（新增）；git diff -- '*.go' go.mod go.sum charts cmd internal 为空。
# docs-site 的 M/D 噪声为本轮会话开始前的工作区既有状态（快照在案），非本票产物。
```

### 4.1 m9 users-groups N01 偶发红 — A/B 对照证明与本票零耦合

并行/串行全文件跑约 1/3 轮出现 N01（users 或 groups 腿）`data.length ≤ 3` 预算断言红（Received 5）。**诊断**（临时诊断 spec 打出请求清单，用后即删）：多出的 2 枚 GET = `/binflow/api/repositories` + `/binflow/api/v1/storage/stats`——**仪表盘落地的惰性取数**在 `loginAs` 返回、tracker 挂载之后才发出，污染了 N01 的页面级请求预算（tracker 挂在登录页与目标页 goto 之间）。**A/B 对照**：手工摘除本票全部十处属性 → 重建 console + 独立二进制 → 净实例 :8145 复跑同一诊断循环，**第 4 轮出现同一签名红**（`["/repositories","/v1/storage/stats",…5 项]`）→ 该 flake 为**既有、机器负载敏感的 spec 级竞态**，与本票 diff 零耦合（本票 src diff = 十个惰性 data-testid 属性，不触碰任何请求行为）。按 flake 协议串行复跑甄别绿（6/6）。**修复建议（越区，不本票代做）**：m9 spec 的 tracker 挂载改「先 goto 目标页等仪表盘惰性取数沉降再挂 + reload」，或预算口径排除仪表盘路径——conductor 可拆一行测试基建票。

（过程注记：A/B 收尾的 `pkill -f binflow-server serve` 误伤了 T-383 留档的 :8143 实例；已用原 data dir（/tmp/bf-t383，未动）重启恢复，curl 200 在案。）

## 5. AC 判定

| AC | 判定 | 证据 |
|---|---|---|
| AC1 对照核验 + 对齐钉死/差距定案 | **PASS** | §1 四行逐项表（入口形态/整页路由/表单结构/组面）；对齐项全量钉死为断言（test 1~3：非 modal 反断言、URL 不离列表、四节字段归属、页脚三联几何序）；差距项全部有既有裁定或票内定案留痕（内建 vs 路由 = parity v1.1「可保持」；External ID/Auto Join = §6.10；创建态无 Retype + 复选集 vs 三值角色 = §1 #3 定案） |
| AC2 创建全链 + 既有 spec 零回归 + 四闸门 + axe 双主题 0 + 服务端 diff=0 | **PASS** | 全链 = test 1（用户：入口→表单→Save→API 对账）+ test 2（组：含 E5 成员对账）；既有面 = m8 users-groups/security/rbac/keyboard 19 + m10 7 + m9 串行 6/6（唯一红为 §4.1 A/B 归因的既有 flake，非回归）；四闸门全绿；axe 双主题 4 扫 serious=0（test 4）+ a11y-sweep 全路由面复证 3/3；服务端 diff=0（§4 末） |
| AC3 锚册纪律 + ledger | **PASS** | v1.20 批块入册 6 节锚 + 页脚 4 锚复役（含 §10.6 摘除与 92 条计数修正）；锚族零改名（users-*/user-form-*/groups-*/group-form-* 原样）；ledger PASS（unregistered/broken 双零、retired 全部在表、dead 桶零新增） |

## 6. 契约漂移

- 服务端 API 契约：**无**（本票零 API 消费变化，纯锚位 + spec；对账腿消费的 GET users/{name}、GET groups/{name}?includeUsers=true 与 ADR-0030 E3/E5 契约逐字一致）。
- 规格面观察（非漂移，留 conductor）：parity 册 M3 行「MUI 映射」写「路由页分节表单（Paper 节）」，BinFlow 用户/组表单实际载体是 card/form-section CSS 分节（MUI 化波次未及此两页）——映射描述与实现有代差，回写归 ux/conductor（parity 册非本票 area）。

## 7. 遗留

1. **m9 users-groups N01 请求预算 flake**（§4.1）：既有、A/B 归因在案；建议一行测试基建票收口（tracker 挂载时机或预算排除口径）。
2. parity 册 M3「MUI 映射」单元格与本两页实现载体的代差描述（§6）——回写归 ux/conductor。
3. Tokens 面（V6c / parity 批 2 ⑤）不在本票口径，未触碰——Identity Tokens 表 + 生成区仍为 BinFlow 侧缺口（在册）。
4. 复验实例状态（会话收尾核正）：/tmp/bf-t384（:8144）与 T-383 的 /tmp/bf-t383（:8143）两实例在本票报告落档**之后**随 agent 会话结束被宿主回收，serve.log 均为优雅停机（"webhook dispatcher drained / binflow stopped"）——**data dir 完整保留**，复验一行重启即可：`BINFLOW_SERVER__LISTEN=127.0.0.1:8144 BINFLOW_DATA_DIR=/tmp/bf-t384/home/data BINFLOW_ADMIN_PASSWORD=password bin/binflow-server serve`（:8143 同款换路径）；全部自测证据均在实例存活期采集（§4 时序在案）。/tmp/bf-t384-ab（A/B 对照 data dir）与 /tmp/bf-t384、/tmp/bf-t383 均可整删。
5. T-394 共写面（UsersPage/GroupsPage 若被其触碰）——本票十锚均为 data-testid 属性，合并冲突面极小；其票面 area 为 internal/httpapi + charts + cmd，理论零交叠。

## 8. 变更清单

```
web/src/pages/security/UsersPage.tsx            创建表单四节 +user-form-section-* 锚 + 页脚 cancel/reset 复役锚（纯锚位零逻辑）
web/src/pages/security/GroupsPage.tsx           组表单两节 +group-form-section-* 锚 + 页脚 cancel/reset 复役锚（纯锚位零逻辑）
web/e2e/m14/t384-usergroup-parity.spec.ts       新增（4 tests：用户全链/组全链/形态钉死/axe 双主题）
docs/design/console-ux.md                       v1.20：T-384 批 6 节锚入册 + 页脚 4 锚复役摘除 + 头表/修订行/计数 92
```
