# T-205 — docs 断链修缮（T-199 遗留①）工作日志

- 状态: done（自测全绿，待 review/qa）
- 日期: 2026-08-22
- role: tech-writer / area: `docs/user`（三文件各一处 hunk）
- 依据: reports/agents/T-199.md §4 遗留1、reports/agents/T-146-qa.md §构建警告（三条修复建议）、BOARD.md T-205 条目（:90-91，只读）

## 1. 做了什么

### 基线复测（改前）

`npm run build`（docs-site，与 `make docs` 构建段等价）告警与 T-199 记录逐条对上，**无新增**：

```
broken links（3 条，2 个源页）:
- /binflow/docs/docker-registry -> ../../deploy/dev/README.md (resolved as: /deploy/dev/README.md)
- /binflow/docs/ -> getting-started/binary.md
- /binflow/docs/ -> getting-started/docker.md
broken anchors（1 条）:
- /binflow/docs/admin/governance -> /binflow/docs/#客户端接入每协议一篇
```

### AC1 docker-registry 页越树链接

`docs/user/docker-registry.md:304`（下一步节）：

- 旧：`[deploy/dev/README.md](../../deploy/dev/README.md)` —— 目标文件在仓库里**存在**，但在 Docusaurus 内容根（`docs/user/`）之外，站内永远不可达（T-146-qa 判「跨仓库路径无法解析」；改指 `deploy/README.md` 同样越树、同样断——AC 给的两个选项里「改指 deploy/README.md」不可行，取「改指存在页」路线）。
- 新：`[Docker 运行](install/docker.md)、[docker-compose 部署](install/compose.md)（开发/评估用 compose 形态另见仓库内 \`deploy/dev/README.md\`，非文档站页面）` —— 站内链接承接「部署形态与端口/持久化」语义（T-146-qa 建议方向）；仓库文件改用反引号代码格式引用，与本页 :245 及 install/compose.md、install/k8s.md、install/offline.md 的既有引用惯例一致。

### AC2 首页 getting-started 两断链

`docs/user/README.md:7-9`：

- 旧：`[5 分钟上手（单二进制）](getting-started/binary.md) — M1` ×2 —— `getting-started/` 目录从未创建（T-129/T-141 内容矩阵缺口，T-198 已记录「他人文件的既有悬链」）；实际内容落在 `install/binary.md` / `install/docker.md`（T-146-qa 给出的实底）。
- 新：改指 `install/binary.md` / `install/docker.md`；组标题补注「（两条最快路径，完整步骤见下方『安装指南』）」明示与安装指南的目的地重合是有意设计；条目注从过时的里程碑标签（— M1）换成可验证结果（下载 → `serve` → `/readyz` 200 / `docker run` → `/readyz` 200，即两页头部的复跑口径）。
- 未选择新建 `getting-started/` 两页：快速开始内容与两篇安装指南完全同源，另立两页会造成双份待维护副本；QA 建议即为改路径。

### AC3 governance 锚点失效

`docs/user/admin/governance.md:326`：

- 旧：`[接入指南](../README.md#客户端接入每协议一篇)` —— README 里「客户端接入（每协议一篇）」是导航列表的加粗条目而非标题，锚点目标不存在（且中文锚点跨页脆弱）。
- 新：`[接入指南](/integrations)` —— 指向 sidebar（docs-site/sidebars.js:26 `slug: '/integrations'`）生成的「客户端接入」类目索引页（build/integrations/index.html 实存）；根绝对路径由 Docusaurus 自动加 baseUrl 前缀，渲染为 `/binflow/docs/integrations`。链接文本后补「（docker/maven/npm/pypi/generic 各篇）」保留原锚点想表达的枚举语义（T-146-qa 建议 B 方案）。

### AC4 全量扫描

改后 `make docs` 全站构建：`grep -ci broken` 日志计数 **0**（改前同类计数 4 条目/3 处问题）——存量清零且未引入新断链。另手工 grep `docs/user/` 内 `\]\((\.\./\.\./` 越树链接模式：无残留；其余跨页锚点（api-reference/console/faq/backup-restore 等十余处 `xxx.md#锚点`）经构建器校验全部有效（改前基线即无告警）。

## 2. 改动文件

| 文件 | 改动 |
|---|---|
| `docs/user/docker-registry.md` | :304 一行（AC1） |
| `docs/user/README.md` | :7-9 三行（AC2） |
| `docs/user/admin/governance.md` | :326 一行（AC3） |
| `docs-site/build/**`、`internal/docs/dist/**` | `make docs` 再生构建产物（AC4 副产品） |

注：工作区里 `docs/user/README.md` 的「专题指南」段扩充与 `docs/user/faq.md:97` 的 OIDC/LDAP 行是**其他在途票的未提交改动**（本票开工前已在工作区），非本票产物，未触碰。

## 3. 自测（实际命令与输出摘要）

```
# 基线（改前）
$ cd docs-site && npm run build        # 日志存 /tmp/t205-baseline-build.log
  → [SUCCESS] Generated static files in "build"
  → broken links 3 条 + broken anchors 1 条（清单见 §1，与 T-199 §4.1 逐条一致）

# 改后（规范命令）
$ make docs                            # 日志存 /tmp/t205-after-build.log
  → [SUCCESS] Generated static files in "build"
  → docs site size (raw total): 2.60 MB (2726606 bytes)
  → grep -ci "broken" /tmp/t205-after-build.log  → 0

# 三处修复的渲染证据（build 与 internal/docs/dist 双侧核过）
$ rg -o 'href="[^"]+"[^>]*>5 分钟上手[^<]*' docs-site/build/index.html
  → href="/binflow/docs/install/binary">5 分钟上手（单二进制）
    href="/binflow/docs/install/docker">5 分钟上手（Docker）
$ rg -c '接入指南' docs-site/build/admin/governance/index.html  → 1（dist 同）
  rg -o 'href="/binflow/docs/integrations"' 同页 → 命中（baseUrl 前缀正确）
$ rg -c 'href="[^"]*deploy' docs-site/build/docker-registry/index.html
  → 无匹配 exit=1（越树 href 清零；页内 deploy/dev/README.md 仅以 <code> 文本出现 ×2）
```

排查备注：构建页 HTML 含 NUL 字节（`@easyops-cn/docusaurus-search-local` 的关键词高亮标记），致 BSD grep 静默失败/`file` 报 data——用 rg/strings 复核；HEAD 版本（`git show HEAD:...` 58197 字节含 3 个 NUL）同样存在，系既有构建行为，非本票引入。

## 4. 遗留 / 建议

1. **建议主会话裁决**：`docs-site/docusaurus.config.js:30` 的 `onBrokenLinks: 'warn'`（注释约定「内容矩阵补齐后 flip to throw」）。断链现已清零，具备 flip 条件，但 docs-site 构建配置按 ADR-0011 归 tech-writer 之外管辖，本票未动。
2. 快速开始与安装指南暂共用页面（本票 AC2 取 QA 建议的改路径方案）；若后续想要独立的 5 分钟极简页（≤5 步、免平台分支），另立内容票，勿混入修缮票。
3. 工作区在途未提交改动（README 专题指南段、faq OIDC/LDAP 行等）与本票同文件不同 hunk，提交时请注意分票归因（见 §2 注）。

## 5. 规格依据

- reports/agents/T-199.md §4.1（存量 3 处清单与「另立修缮票」建议）
- reports/agents/T-146-qa.md 构建警告节（三条断链的实底与修复建议）
- reports/agents/T-129.md（onBrokenLinks=warn 的由来）、T-142.md:95、T-171.md:24、T-198.md:77（getting-started 从未落地的历次记录）
- docs-site/sidebars.js:19-35（客户端接入类目 slug=/integrations）、docs-site/build/integrations/index.html（路由实存）
- BOARD.md T-205 条目 :90-91（AC ①-④，只读）
