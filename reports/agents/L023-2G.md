# L023-2G — L023-2F 返工后差分复验（build-info 域重放门全量重跑）

Ticket:       L023-2G（复验票：L023-2F 8 BUG 修复后的差分重跑）
Role:         differential-qa-engineer
Area:         tools/difftest/l0232d/（重放，零新工具）+ reports/compatibility/L023-buildinfo-diff.md §9 复验段 + contracts/buildinfo.yaml 现值刷新
Input:        L023-2F 产出（dev.c6f6c71a 重部署，含 D1-D7/D10 八 BUG 修复）；首轮差分基线（L023-buildinfo-diff.md §0-§8 + 48 判定单元）；A 参照不变（7.161.15 :8082）；jf 2.122.0
Changes:      ① 重放门全量重跑 buildinfo_diff.py（环境重建四测试仓——首轮清场已删；harness 一处修复：22c 聚合串匹配 bug〔路径夹在仓名与箭头之间致恒假阴性，wire 实证两轮双端搬迁均成功——首轮 22c 的 DIVERGENT 判定本身成立因当时 B 真未搬，本轮起归为 harness 修正〕）；② 记录级探针：malformed started 变体 ×3（D7 rest 计算近似）+ D10 批删 repo 内插 B 侧探针；③ jf 四真腿重跑（jf-legs.sh 的 A 配置行修正为平台根 URL）；④ §9 复验段 + 契约蓝本 11 条目现值刷新。
Files:        修改：reports/compatibility/L023-buildinfo-diff.md（追加 §9 复验段）；docs/compatibility/contracts/buildinfo.yaml（binflow_state/status 刷新：promote DIVERGENT→VERIFIED 等）；tools/difftest/l0232d/{buildinfo_diff.py（22c 修复）,jf-legs.sh（A 配置 URL 修正）}；wire 新增 malformed-started-variants.log + 各 case 覆盖为复验值。新增：本日志。
Tests:        48 判定单元：**SAME 34 / DIVERGENT 14**（首轮 24/24）；复跑门 pass2==pass3 判定集逐 case 一致。D-item 对账：D1/D2/D3/D5/D6/D10 → **SAME**（8 BUG 修复全部双端确认）；D4 → 主体 SAME + **新残差 R3**（空集合物化：B 回 `modules:[]`/`dependencies:[]`，A 省略键——6 单元，2F 修复带出，BUG-minor）；D7 → 3/4 臂 SAME + 1 边缘臂（`started=xyz` 位 0 失败：A 无 malformed-at 子句/B 恒发）；D8/D9 维持（预期内，候裁）；D11 维持（jf publish 后置步仍 400 AQL property——2F 范围外）。jf 真腿：upload/promote/discard **三腿双绿**（promote exit=0+真搬迁+promoted 豁免幸存集 ['/9','/7'] 双端逐位一致——D3 属性通道成立）；publish 腿 B 仍 exit=1（D11+version=dev 债）。
Commands:     python3 tools/difftest/l0232d/buildinfo_diff.py（pass1-3）；bash tools/difftest/l0232d/jf-legs.sh；malformed 探针 curl ×3 变体×双端（l023d-wire/malformed-started-variants.log）；D10 探针 POST /build/delete buildRepo=void（回显解析仓确认）；ruby -ryaml load contracts/buildinfo.yaml →「YAML OK entries=11」；清场复核（A 404/B 零 l023d/四仓净/void 404/jf config 除）。
Outputs:      §9 复验段（9.1 D-item 对账表/9.2 残余三族/9.3 两处近似记录级观察/9.4 D07 终版/9.5 处置）；契约蓝本现值（VERIFIED 6 / DIVERGENT 5）。**D07 翻态终版**：R01/R02/R03 **翻 ✅**；R04/R08/R10 **候裁态**（仅 D8 label 挂账）；R05/R06/R07 不翻（R3 空集合物化 + D7 边缘臂）；R09/R11 面外维持。
Compatibility: 回归对照（vs 首轮）：**fixed 8**（D1/D2/D3/D5/D6/D10 全量 + D4 主体 + D7 主体）/ 仍在 2（D8/D9 候裁）/ 新增 1 族（R3 空集合物化——2F 修复副作用，BUG-minor）+ 1 边缘臂（D7 m1）。jf promote 真腿从 exit=1 → exit=0 翻绿。
Security:     全程 admin 腿；凭据命令行传递；jf config 用后删；无新越权面。
Performance:  每轮 ~40s，零超时零 flake（三轮判定稳定）。
Risks:        ① R3 为 2F 修复带出的新面（多发空集合 vs 省键）——修复方向应与 D5 同理（omitempty/nil 判定），小票可并；② D7 近似的位 0 失败臂差单臂文案（400/信封/前缀同）；③ D3 属性通道的对抗臂（属性部分缺失/多仓同 checksum）未差分——jf 主链已绿，登记后续探针；④ D10 仍无双端活体（A projects 门结构性阻挡），B 侧 wire + 源码口径双证。
Blockers:     无。
Next:         ① R3+D7 边缘臂合并小票（echo 收尾）；② D8/D9 候 compatibility-engineer 裁定后 R04/R08/R10 可翻；③ D11（AQL property）→ T-511 提权决策；④ 契约蓝本入册评审（现值已刷新）。
