# T-274 — T-267 锚退役误杀回填 + anchor-audit spec 正则补形 + §10.6 表修正

- 角色: dev-frontend ｜ 票据: T-274 [P0]（T-272-qa §七 DEFECT-1，option a 最小面）｜ 日期: 2026-08-25
- area: `web/src`（19 族回填）+ `web/scripts/anchor-audit.mjs`（spec/src 抽取正则补形）+ `docs/design/console-ux.md` §10.6
- 基线: 起手 HEAD `b11bdc2`（3295181 的 81 处 testid 删除仍在案）；Go 面零触碰（T-273/T-275 并行改动在工作树但不属本票）
- **过程异常记录**：05:42 起 API 配额击落、07:11 后恢复续作——击落时点处于只读侦察阶段（QA 报告 + 3295181 diff + e2e 引用形态盘点），恢复后 `git status web/` 核对确认零半成品落盘，全部产物为恢复后单次连续写入。

## 1. 根因复述与独立复核

T-272-qa §七 DEFECT-1 三点根因链本票全部独立复核成立：

1. **正则盲区（根因①）**：`anchor-audit.mjs` 旧 spec 正则
   `data-testid="([^"$]+)"` 只认双引号字面且排除 `$`，不识别
   `[data-testid^=…]` 前缀形——e2e 真实两大主流形态（模板串
   `[data-testid="repos-row-${key}"]` 与前缀选择器）全部隐形。
2. **无 shell 执行（根因②）**：T-267 按 dead 桶做「零消费」清理 → `3295181`
   删 81 处 testid（25 文件），其中 19 个活族被误杀。
3. **陈旧 dist 逃逸（根因③）**：T-268 三轮绿跑在 pre-T-267 console 上。

独立复核（scratch 脚本，修复后正则对 `web/e2e` 全量 + `3295181^`↔HEAD src 家族差集）：
**删除 98 族中 19 族仍有活消费**，与 QA 的 19 族口径精确一致；A4 违例集（回填后）
逐条列出同样恰为 19 族，两向自洽。

## 2. 变更一：anchor-audit spec 抽取正则补形

`web/scripts/anchor-audit.mjs`——单正则改六形态 `REF_RES`：

| 形态 | 例 | 旧正则 |
|---|---|---|
| S1 属性选择器（三引号 × `^=/$=/*=` × `${}` 模板段） | `[data-testid="repos-row-${key}"]`、`[data-testid^="repos-row-"]` | 隐形 |
| S2 getByTestId（含模板） | `getByTestId(\`user-form-group-${g}\`)` | 部分可见 |
| S3 `testid:` 选项形 | `testid: 'x'` | 可见 |
| S4 `toHaveAttribute('data-testid', 值)` | usage-fanout 排序腿两处 | 隐形 |
| S5 `toMatch(/^前缀-)` | keyboard 腿 activeElement 前缀断言 | 隐形 |
| S6 `(not.)toBe(\`前缀-${…}\`)` 窄门（前缀含连字符） | keyboard 腿负向比对 | 隐形 |

- 纯透传形（值以 `${` 开头，helper 的 `[data-testid="${anchor}"]`——锚名来自调用方
  实参）显式跳过：不可解析，计入只会制造伪 broken。
- 前缀形（尾连字符）归一 `前缀-*`（`specFamily`），与 dead/broken 前缀命中语义一致。

**顺带发现并修复同根 src 侧盲区**：`const testid = \`repos-usage-${repoKey}\``（变量
模板赋值，渲染位 `data-testid={testid}`）——src 扫描与 spec 扫描**双向隐形**，运行时
锚真实存在（QA round 1 相关腿本就通过）。src 正则并入 `\s*` + 模板值并收该形态
（原 prop 缺省正则随之合并，避免同落点双计）；`repos-usage-*`（T-253「已用」列）
从未入册 → §10.6 补录（修复后 A1 曾报 unregistered，即此）。

### spec 引用计数前后对照（19 族逐族；同六形态正则，旧=旧正则可见数）

| 族 | 修复前可见 | 修复后可见 |
|---|---|---|
| repos-row-* | 0 | **33** |
| user-form-group-* | 0 | 9 |
| perm-repo-pick-* | 0 | 5 |
| quota-bar-* | 0 | 5 |
| group-form-member-* | 0 | 3 |
| perm-matrix-remove-* | 0 | 3 |
| repos-deploy-* | 0 | 2 |
| repos-delete-* | 0 | 2 |
| quota-row-* | 0 | 2 |
| quota-edit-* | 0 | 2 |
| storage-row-* | 0 | 2 |
| repos-setmeup-* | 0 | 1 |
| group-manage-badge-* | 0 | 1 |
| perm-manage-badge-* | 0 | 1 |
| perm-repo-remove-* | 0 | 1 |
| quota-input-* | 0 | 1 |
| quota-save-* | 0 | 1 |
| dashboard-audit-row-* | 0 | 1 |
| user-form-role-* | 0 | 0（见 §4 裁量） |

- **repos-row 33/33 与 QA 口径精确吻合**（29 选择器形 + 2 `toHaveAttribute` 值 +
  1 `toMatch` 前缀 + 1 `not.toBe` 模板）——「修复后工具看得见」自证成立。
- 合计 75 处引用自隐形转可见（QA 报 65 为其口径下计数；差值 = 值断言三形态与
  前缀归一口径差，不影响族级判定）。
- user-form-role-*：spec 的 8 处引用全部落在 select 级静态锚 `user-form-role`
  （该锚一直存活），option 级确无直接消费——QA 清单按「user-form-*」族群断言
  纳入 19 族，本票按票面「git revert 式恢复」保真回填，**处置留 conductor**
  （若再退役须走新流程；当前进 dead 视图桶，非 ledger 违例）。

## 3. 变更二：19 族 src 回填（21 落点 / 9 文件）

对照 `git show 3295181^:web/src/...` 逐点恢复 testid 属性（JSX 属性顺序有微差，
语义零差异；不动 T-267 其余清理——真死锚保持退役）：

| 文件 | 回填锚 |
|---|---|
| pages/repositories/RepositoriesPage.tsx | `repos-row-${repo.key}` / `repos-setmeup-` / `repos-deploy-` / `repos-delete-` |
| pages/governance/QuotasPage.tsx | `quota-bar-` / `quota-row-` / `quota-input-` / `quota-save-` / `quota-edit-`（cancel/error 两族维持退役） |
| pages/monitoring/StorageSummaryPage.tsx | `storage-row-${r.key}` |
| pages/DashboardPage.tsx | `dashboard-audit-row-${i}`（map 回调恢复 `(ev, i)`） |
| pages/security/UserDetailPage.tsx | `user-form-role-${r}`（option）/ `user-form-group-${g}`（itemTestid） |
| pages/security/UsersPage.tsx | `user-form-group-${name}`（itemTestid） |
| pages/security/GroupsPage.tsx | `group-form-member-${u}`（itemTestid）/ `group-manage-badge-${r.group.name}` |
| pages/security/PermissionsPage.tsx | `perm-manage-badge-${t.name}` |
| pages/security/PermissionEditorPage.tsx | `perm-repo-pick-${k}` ×2（itemTestid）/ `perm-matrix-remove-${cellKind}-${name}` / `perm-repo-remove-${r}` |
| pages/security/TransferBox.tsx | 仅注释（itemTestid 调用方恢复传参的过时表述修正） |

回填落点数与 `3295181^` 的 19 族清单精确对齐（git diff +21 行 data-testid/itemTestid）。

## 4. 变更三：console-ux.md §10.6 修正（v1.9.1）

1. **退役总表 7 行摘除 19 族**（20 表记名——`perm-matrix-remove-{user,group}` 两记
   对应 src 一族；118 → 98 条），摘除行原因列就地注明「T-274 查明系审计盲区
   误杀、已回填」——回归在册。
2. 新增**回填记录（v1.9.1）**段（非表体形态——`tableCol1Families` 只解析 `|` 行
   首列，回填记录若用表体会重触发 A4）：19 族逐域清单 + 逐族可见消费数 +
   `repos-usage-*` 补录（双向隐形漏网，T-253 落地）。
3. 口径定义 spec 口径行补六形态；新增**工具局限史**条款：「工具的判定域 = 其
   解析形态的并集，域外引用对统计隐形而『零消费』会被当成处置依据——退役处置
   前必须先对工具可见性本身自证（全文本 grep 前缀非零即停手核查形态）」。
4. §0 修订记录新增 v1.9.1 行。

## 5. 验收证据

| 门 | 命令 | 结果 |
|---|---|---|
| 锚 ledger | `node web/scripts/anchor-audit.mjs --ledger` | **PASS**（A1 unregistered=0 / A2 broken=0 / A3 retired 全在表 / A4 退役表∩src=∅）；src 363 家族 402 落点 / spec 452 家族 1842 引用 / 册 540；退役表 98 |
| console 构建 | `make console && make build` | 绿（197,250 B gzip js+css）；bin 07:19 与内嵌 dist 同刻；served 页面 chunk 实测含 `repos-row-` 等回填锚（T-268 陈旧 dist 教训规避） |
| web build | `npm run build`（assert-tokens + vite + relink） | 绿 |
| web lint | `npm run lint`（含 anchor-audit.mjs） | 0 告警 |
| web typecheck | `npx tsc --noEmit` | 0 错 |
| e2e 全量默认并发 | 全新 scratch `:8080`（BINFLOW_HOME=/tmp/bf-t274，HEAD bin）+ 双 seed（m8 树 10291/10291 @76.6s；m9 verification green）→ `npx playwright test`（4 workers 裸跑） | **round 1: 176 passed / 0 failed / 5 skipped（3.7m，exit 0）；round 2: 176 passed / 0 failed / 5 skipped（3.8m，exit 0）——连续两绿**（QA §六「建议 2 轮」兑现） |
| 日志归档 | /tmp/bf-t274/ | e2e-round{1,2}.log / audit-final-head.txt / ledger-final.txt / server.log |

环境清理：:8080 QA 实例验证毕已停（lsof 复核 0 监听；18080 用户实例全程未动）；
工作树本票产物 = web/src 10 文件 + anchor-audit.mjs + console-ux.md（Go 面改动属
T-273/T-275 并行票，非本票）。

## 6. 遗留与移交

- **user-form-role-\* option 级零直接消费**（dead 视图桶第 3 员）：保真回填所致，
  是否再退役走 conductor（新流程：先自证工具可见性，见 §10.6 工具局限史）。
- dead 视图桶另两员（`${rowTestidPrefix}-matrix` / `groups-sort-name`）为 T-267 期
  既有视图态，未动。
- QA 的 65 处引用计数与本票 75 处存在口径差（值断言三形态 + 前缀归一），族级
  判定一致；已在 §2 注明。
- e2e skipped 3→5（QA round 1 基线 3）：多为 env-skip（t104 1GB RSS 腿等设计内），
  0 failed 不受影响；did-not-run 8 员全部恢复执行（181 腿全量两轮皆然）。
