# L023-1 — build-info 域规格核对（absent-roadmap 批次 1 首票）

Ticket:       L023-1（LOOP 023 批次 1 首票：build-info 域规格核对——D07 11 行 P1）
Role:         reverse-engineer（本票实例域 = build-info REST/语义面）
Area:         docs/reverse/build-info.md（§11 增补 + §0/§1/§2/§9/§10 勘误修订）
Input:        派发领域 = D07 build-info 11 行（matrix.yaml 1961-2091 行逐行核对）；缺口来源 = absent-roadmap 批次 1（BinFlow 零底座新域）；参照公开规范 = JFrog REST 参考页（T-488 期已实取）+ 产品内嵌一手 openapi.yaml

Changes:      按序：
              1) 盘点既有规格（docs/reverse/build-info.md，T-488 2026-09-06 稿）× matrix D07 11 行 → 覆盖度表（见 Outputs）；
              2) 定位实现源：T-488 误判「build REST 实现在闭源模块」——本轮发现 OSS 7.161.16 树 `build-handler/build-handler-service/` 即完整服务端实现（REST 资源层 @Path("build") + command + service + DAO）→ 勘误 E13；
              3) 通读 REST 资源层 + 8 个 command + 7 个 service + DAO 排序 SQL + 比较器 + 工具类，提取六个票面点名语义面深规格；
              4) 活体取证 172.16.58.130:8082（pro 7.161.15）：PUT×5 / GET 三跳×8 / append×1 / promote×3 / retention×2 / DELETE 族×4 / 404 族×3——全部探针后 deleteAll 清理 + 空态复验（零残留）；
              5) 落盘：build-info.md 增补 §11（勘误总表 E1-E13 / 11 行覆盖度 / 六语义面深规格 / 取证锚点 / 实现票拆分建议），同步修订 §0 软缝④⑥⑦⑧⑨、§1 端点表 8 行、§2.1/2.2/2.4/2.5、§3.1、§5、§7、§9 待验证清单（关闭 #2/#4/#6/#7，新增 #8-#10）、§10 勘误。

Files:        修改 docs/reverse/build-info.md（唯一规格文件；无新建——增补进既有域名文件）；新建本报告。

Tests:        活体抽验（参照实例 :8082，全部通过、全部已清理）：
              - PUT /api/build → 204 + X-Checksum-Sha256: 393cb351…（E1）；重复同坐标 → 204 覆盖；同号不同 started → 204 多 run 并存（号单 ['/1','/1']）；name=".jfrog" → 400 `Build name must not start with '.'`
              - GET /api/build 空态 → 404 `No builds were found`（E2）；有条目 → 顶级 uri 绝对 + ?buildRepo=artifactory-build-info；lastStarted 入 +0200 出 +0000（UTC 归一）
              - GET /{name} 号单 → started 严格倒序 3→2→1（待验证 #4 关闭）；GET 详情 started 原样 +0200、durationMillis 回显 0、artifactoryPrincipal=admin
              - append 同 id 模块 mod-a → GET 回显两个 mod-a 条目（E5 证伪「按 id 合并」）；append 404 未逐字复验（源码单源，文案以源码为准标高——源码+官方 openapi 双载文案差异已登记 E4）
              - promote status-only → 200 messages=[{level:"INFO",…}]（E3 大写证实）；targetRepo=no-such-repo → 404 `Cannot find target repository by the key 'no-such-repo'`；failFast + 假 checksum 制品 → 400 `Unable to find artifacts of build 'l023-probe' #1 from artifactory-build-info repo: aborting promotion.`（E12）；statuses wire = {status, comment, timestamp, timestampDate, user}（repository/ciUser 省略）
              - retention count=0 → 400 `Max count retention needs to be a positive number`；count=1（async 不传=同步口径）→ 204 且删 build 2、留 3（count 名额）+ 留 1（promoted 豁免）——即删性（E7）+ promoted 豁免双证
              - DELETE buildNumbers=3,99 → 200 `The following builds have been deleted successfully: 'l023-probe#3'.\nWarning - the following builds could not be removed: '99'.\n`（E6 have）；deleteAll=1 → `All builds 'l023-probe' under 'artifactory-build-info' have been deleted successfully`；404 族三文案逐字（No build was found…/Unable to find build…/Cannot find a build by the name…）

Commands:     源码定位：
              grep -rln "BuildResource\|BuildAddon\|BuildServiceImpl" reverse-src/artifactory/backend --include=*.java（→ 零命中，确认反编译树无实现）
              find reverse-src/artifactory/backend -type d -path "*org/artifactory/build"（→ 仅 papi 模型）
              grep -rln "_START_\|_EXT_" reverse-src/ （→ 仅前端 locale 噪声；docs/、PRD、jfrog OSS 树 build-handler 同零命中）
              find ~/Downloads/jfrog-artifactory-7.161.16/build-handler -path "*main*" -name "*.java"（→ resource/command/service/dao 全套 60+ 文件清单——T-488 误判勘误的直接证据）
              通读：BuildPublicResource.java（300 行）/ BuildCreateCommand（86）/ BuildAddModulesCommand（53）/ BuildPromoteCommand（78）/ BuildRetainCommand（54）/ BuildDeleteCommand（154）/ BuildRunComparators（117）/ BuildCreationService（242）/ BuildPromotionService（433）/ BuildRetentionService（181）/ BuildDeletionService（333 节选）/ BuildReadService（372 节选）/ BuildDatabaseService（965 节选）/ BuildsDao.java（排序 SQL 节选）/ BuildServiceUtils.java / PromotionResult.java / BuildsDeletionModel.java + reverse-src StatusEntryLevel.java（level 枚举四值）
              活体：curl 探针 26 次（命令与关键响应全文见 Tests；凭据 admin/**，仅 shell 变量传递未落盘；探针 build 名 l023-probe 用后 deleteAll×3 清理并复验空态 404）
              公开规范：WebSearch JFrog docs "_START_ _EXT_"（零命中）；回执复核（WebFetch）：官方 Build-Info JSON 旧 URL 301→404、现役页 docs.jfrog.com/artifactory/docs/build-integration 逐字零命中、jfrog/build-info README 零命中——#8 升格定案（详见规格 §9 #8）

Outputs:      规格文件 docs/reverse/build-info.md——11 行覆盖度（L023-1 前 → 后）：
              R01 GET /build：已规格（表级）→深规格；R02 PUT：已规格→深规格；R03 号单：已规格→深规格（倒序落定）；R04 DELETE：已规格→深规格（文案/怪癖）；R05 详情：已规格→深规格（statuses/slim/diff）；R06 append：部分→深规格+勘误（E5 不合并）；R07 promote：已规格→深规格（九步执行序）；R08 批删：部分→深规格（6 字段 body）；R09 rename：已规格（面外）→微补强；R10 retention：已规格→深规格+勘误（E7 即删）；R11 docker promote：已规格（§2.6 面外）→维持未再核。
              语义流程 9 节（§11.1 勘误 13 条 / §11.3 号分配与上传改写 / §11.4 append / §11.5 promote 九步 / §11.6 retention 两遍删序 / §11.7 批删集合 / §11.8 排序语义 / §11.9 锚点 / §11.10 拆票建议）；布局规则 0 新增（§3.2 既有维持）；待验证清单净变化：关闭 4 条（#2/#4/#6/#7）、新增 3 条（#8 _START_/_EXT_ UNKNOWN、#9 patternArtifacts、#10 async 缺省口径）。

Compatibility: 置信度分布（§11 新增条目）：高 ~52 条（源码+活体双源）/ 中 4 条（append 404 文案映射、buildRepo 后缀校验文案、名清单序向、多 run 同号单删怪癖——单源控制流读法）/ 低 0。与官方规范的差异/补充：三条实现为准（DELETE has→have、retention 不立即删→即删、GET 空态 200→404）+ 一手 openapi 自身九处失真（E1-E4/E8/E9/E11、批删字段缺 buildRepo、slim 未载）——全部显式登记 §11.1，供 compatibility-engineer 契约化时以「实现真值」为 golden 锚。软缝④（append 合并键）与软缝⑥（retention 时机）答案被证伪/修正——**ADR-0045 Accepted 期复核输入，需上报 conductor**。
Security:     坐标 hidden 校验（name/number 去前导空白后以 . 开头 → 400——防 buildinfo 仓隐藏目录绕权，RTDEV-94003 对位）；build 数据面 403 文案族六条逐字（§7）；无 read 权 build 列表静默过滤；retention 执行层无 delete 权=宽容跳过（与 REST 层 403 断言分层——实现时勿混淆两层）；promote 需 annotate 权标注属性；User-Agent 前缀 ArtifactoryBuildClient/<2.0.11 触发参数手动解码（遗留兼容面，攻击面登记）。
Performance:  retention async 缺省同步（E9 源码口径）；async=true 走后台 work queue（workers=10）；promote 状态更新走删除重建全量 JSON（大 build 的放大系数——存储配额 413 门在链上）；批删按 dbOperationsBulkSize 分片 IN 查询；build 搜索 maxBatchSize=5000；manifest 大小限 700MB。
Risks:        ① 源码 7.161.16 vs 活体 7.161.15 差一档——本轮抽查点零分歧，但未抽查面（rename/promote timestamp 非法路径/patternArtifacts）存在版本间漂移可能；② 「多 run 同号每次至多删一个」是控制流读法（中置信），契约化前建议活体双 run 同号删除复验；③ retention async 缺省已按源码定案=同步（E9：JAX-RS 原语 false；活体两解兼容），与 openapi/官方 true 相悖——低风险时序差异（功能面零分歧），契约化按源码口径；④ `_START_`/`_EXT_`——回执复核后定案：官方文档（Build-Info Integration 现役页 + build-info README）亦零命中，#8 已升格为「四源零命中定案 + 官方邻接真值」（env 采集纯客户端：--collect-env/--env-include/--env-exclude 缺省排除敏感表；服务端只存储零展开——官方锚 docs.jfrog.com/artifactory/docs/build-integration）；⑤ 名清单序向（SQL 升序字面 vs 直觉最近活跃在前）待多 build 活体判向。
Blockers:     无（T-488 期的「实现不可达」与「活体不可用」双阻碍本轮均解除）。
Next:         ① 转派 compatibility-engineer：§11 勘误 13 条 → D07 契约族 + golden 锚（错误文案逐字/204+checksum 头/level 大写/即删性）；② 上报 conductor：软缝④⑥答案翻案（ADR-0045 复核输入）+ `_START_`/`_EXT_` 出处回执请求；③ 实现票拆分建议已内嵌 §11.10（票 A CRUD 地基 → 票 B append+promote ∥ 票 C 批删+retention）；④ 后续规格票候选：patternArtifacts/diff 响应形态活体补全（#9 + #5 残余）。

---

## 附记：L023-2D 规格回写（2026-09-15 小尾巴票）

差分收官报告 `reports/compatibility/L023-buildinfo-diff.md` §7 的 7 项修正（活体双端证据推翻本票源码单读）已逐条回写 `docs/reverse/build-info.md`，勘误编号续 E14-E20（§11.1）+ 正文同步修订：

| # | 修正 | 回写落点 |
|---|---|---|
| ① | promote 400 两面性：E12 abort 异常路径 = errors[] 信封（REST 异常映射器）；流程完成路径（timestamp 非法/failFast 警告集齐）= 400+messages body；成功零消息 = `{"messages":[]}` 空数组 | E14 + §1 promote 行 + §2.4 + §11.5-9 |
| ② | 制品收集双通道：manifest（originalDeploymentRepo+path 全集直查）→ 否则 AQL build 属性三键；checksum 仅 verify；path/originalDeploymentRepo = echo 保真键（空串也回） | E15 + §11.5-5 + §3.1 artifact 行 |
| ③ | slim=true：properties 键整体省略（非置 null） | E16 + §1 详情行 |
| ④ | retention count 门 400 Content-Type = application/json（body 仍纯文本句——产品错标为参照真值） | E17 + §1 retention 行 + §11.6-1 |
| ⑤ | GET 详情 404 精确字面：无 started 子句句尾一空格；有子句逗号前一空格 | E18 + §1 详情行 |
| ⑥ | 名清单序向 = 升序（双活体收口，中→高） | E19 + §11.8 + §9 新增已解项 #11 |
| ⑦ | 自定义 buildRepo：写路径 = projects 门（project 须存在；仓不存在/project 不存在/缺后缀三因共用 does not exist 文案）；读路径无门（不存在仓读 = 404 No builds were found） | E20 + §11.3（附 BinFlow 无 projects 域的承接面裁定登记 D9） |

证据锚：`reports/compatibility/l023d-wire/{a,b}/`（重点 a/18、a/21、a/22a、a/08、a/09、a/24a）+ 差分报告 §3/§7；文档头已加「L023-2D 回写」注记、§11.9 已补取证锚段。本附记仅为规格回写留痕——D-item 分类裁定与 matrix 翻态（报告 §6）不在本角色职权。
