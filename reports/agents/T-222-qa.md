# QA 报告 T-222 — M7 验收 II：续传四腿 + 控制台 Playwright + 性能回归（V12~V20）+ M1~M6 全 P0 复跑

- ticket: T-222 [P1]（FR-66 V12~V14 / FR-67 V15~V20 / NFR-P33/P34 / M1~M6 P0 硬门槛 / 全仓 race）
- QA: qa-engineer，2026-08-23
- 被测物：main @ `b8e5fcf`（工作树除本票 scripts/m7-resume-probe.sh 修复外干净）
- **击落-恢复说明**：12:28 被 API 配额上限击落，15:01 配额重置后续跑。盘上状态（探针新鲜度修复 + 全部日志）完整保留，恢复后 `git diff scripts/` 核对以盘上为准，无工作损失。
- 方法：真实栈一次性实例（全部临时端口，未触碰用户实例 127.0.0.1:18080 / PID 36174）+ dind 27.5.1 拓扑（docker 客户端腿，T-45/T-216 先例）+ MinIO（backend=s3）+ 真实客户端（docker CLI 27.5.1〔dind〕/ mvn 3.9.9 + JDK 21.0.12〔/tmp/t67-tools〕/ npm 10.9.8 + node 22 / twine+pip〔现场 venv〕/ curl 8.x）。
- **binary 新鲜度纪律**：本轮开跑时 `bin/binflow-server` 即处于陈旧态（`internal/docs/dist` 比二进制新 10 分钟——陷阱当场复现），探针新鲜度修复首跑即强制重建；此后所有起栈均用重建后的二进制；docker 腿镜像 `binflow:t222` 由当前源码 `docker build -f deploy/dev/Dockerfile` 现场构建（exit=0，含真实 console 构建）。

## 一、任务 0（必修）：探针「binary 存在即复用」陷阱 — ✅ 修复并验证

修复（`scripts/m7-resume-probe.sh`，+24 行）：

- 默认路径（`$ROOT/bin/binflow-server`）或任何 `$ROOT` 下的二进制：`find cmd/ internal/ go.mod/ go.sum/ tools.go -newer $BIN` 发现任一构建输入比二进制新 → 打印陈旧文件路径并强制 `make build`；否则静默复用。`internal/` 覆盖 `console/dist` 与 `docs/dist` 两个 embed 面。
- `$ROOT` 之外的显式二进制：操作者显式选择，打印 `freshness NOT checked` 提示后照用。

验证：

- `bash -n` / `sh -n` / `/bin/dash -n` 全过（POSIX 四壳口径内三壳在本机可得）。
- **陷阱咬合实证**：开跑时 `internal/docs/dist/metrics/prometheus-reference/index.html`（mtime 1787456119）> `bin/binflow-server`（1787455359）→ kill 臂首跑输出 `stale binary: '…prometheus-reference/index.html' is newer …; rebuilding via make build` → 重建后全链 GREEN。
- **新鲜复用实证**：sigterm 臂紧随其后运行，零重建输出（静默复用），全链 GREEN——两分支都走到。
- 双臂 GREEN EXIT=0 见下节 V15/V16。

## 二、V12~V20 结论表

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| V12 | Playwright admin UI 设 alice 为 readonly_admin → API 回显 | ✅ | `e2e/rbac.spec.ts` V12 腿 `3 passed (24.0s)` 中 7.4s：UI 角色下拉落值 + toast + API 回显 `adminRole`（snake）+ reload 回显 + `user.role.change` 审计对账 |
| V13 | readonly_admin 控制台只读走查 + 服务端 403 兜底 | ✅ | V13 腿 14.3s：五页可见 + 只读徽章/注记 + 写入口无 UI 路径 + 同会话重放四类写请求全 403（`administrator privileges`）+ 读面同时存活 |
| V14 | 权限页 manage 复选往返 | ✅ | V14 腿 8.3s：勾 manage → diff 确认 → 保存 → API 回显 `users[carol]=['read','manage']`（r/w/d/m 序）→ reload 复选保持 |
| V12+ | 全套 Playwright（--workers=2） | ✅ | **85 passed / 3 skipped / 0 failed (1.8m)**，与 T-218 基线（85/3/0）完全一致。首跑 2 failed（governance audit 超时 + t146 G19b-1）为本 QA 自造环境干扰：后台全仓 race 套件抢 CPU（T-218 已登记该腿本机负载敏感）+ t146 未导出 `ADMIN_PW`（其 spec 缺省 `docsqa-test-pw-146` 与实例口径不符）——净机 + `ADMIN_PW=password` 复跑全绿，非产品缺陷 |
| V15 | kill -9 腿续传全链 GREEN EXIT=0 | ✅ | `make test-m7-resume`：POST 202 → PATCH 512KiB Range: 0-524287 → kill -9 → boot #2 → **GET 204 + Range: 0-524287** → PATCH 202 Range: 0-589823 → PUT 201 → GET blob 200 → bitwise 一致（sha256 `b4640694…`）→ `GREEN … EXIT=0` |
| V16 | SIGTERM 干净重启腿（ADR-0028 口径）GREEN EXIT=0 | ✅ | `make test-m7-resume-sigterm`：同链全绿（sha256 `1a0d5c55…`），`SIGTERM delivered; graceful exit (compose restart 口径)` → 重启后 GET 204 + Range: 0-524287 → 续块收尾逐位一致 → `GREEN … EXIT=0`（T-229 Close 保留语义在真进程上维持） |
| V17 | 错位腿：416 + 权威 Range（空 body）；伪造 digest 400 | ✅ | 重启后 `Content-Range: 0-65535`（offset 已 524288）→ **416**；**逐字节复核**：`Range: 0-524287` 精确、`Location: /v2/m7-legs/img/blobs/uploads/<uuid>` 与 grant 返回逐字节相同、`Content-Length: 0` + body 实测 0 字节。会话在 416 后存活：正确续块 202 + PUT 201 + 下载逐位一致。新会话伪造 digest PUT → **400 DIGEST_INVALID**（T-209 语义零回退）；失败 commit 后 GET 404（存储契约：Commit 无论成败终结会话） |
| V18 | 过期腿：回填 expires_at 过去 → 重启 sweep 清行清目录 + 404 | ✅ | PATCH 4KiB → SIGTERM 停机（rows=2、uploads/ 两目录）→ sqlite `UPDATE upload_sessions SET expires_at=<1min 前>` → 重启：**目标行 1→0、目标 `uploads/<uuid>/` 目录消失**；未过期的另一会话（伪造 digest 腿残留）按 ADR-0028 正确保留。GET 与 PATCH 过期 URL 均 **404 `{"errors":[{"code":"BLOB_UPLOAD_UNKNOWN",…}]}`** |
| V19 | MinIO（backend=s3）docker push/pull 全绿 + hard-404 契约原样 | ✅ | MinIO 容器 + `backend=s3`（endpoint/use_path_style/桶 binflow）。dind 27.5.1：login Succeeded → push v1/v2（digest `3d782922…`/`c08bf5dd…`）→ rmi → pull 回 digest 逐位一致 → `docker run` 内容正确（v1-content/v2-content）→ manifest inspect OK → tags/list + _catalog 绿。**S3 重启恒 404 实腿**：持久卷保 metadata，PATCH 256KiB → crash 重启（docker rm -f + 同卷重启）→ GET/PATCH upload URL 均 404 `BLOB_UPLOAD_UNKNOWN`（multipart 状态在 S3 服务端，DU-04/Q4 有意不做）。`go test -race -run TestS3ResumeSessionNotSupported ./internal/storage/` → **PASS**（契约未改） |
| V20 | M2 D 序列 P0 双份（本地 filestore + S3） | ✅ | **disk 臂**：push/pull digest 一致 + run + manifest inspect + tags/list `{"name":"m2d/app","tags":["v1","v2"]}` + `_catalog` + HEAD manifest 200（`Docker-Content-Digest` + schema2 Content-Type）。**S3 臂**：同序列全绿且 digest 与 disk 臂逐位相同（跨后端一致性）。⚠️ PRD FR-67-AC6 提及的 Helm OCI 子腿未复跑：oras/crane 本机不可得（T-172 置备后已清理），docker-CLI OCI 面（schema2 manifest + blob + catalog）已覆盖，如实标注不猜 |

## 三、NFR P33/P34

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| P33 | 1000 并发拉取零错误且 P95 与 M6 基线偏差 <10%（G27 口径） | ✅（零错误 + 吞吐两臂均优于基线；P95 对比不可计算 → AC 歧义标记，见观察 O-2） | G27/T-147 承载口径（P50×20 波 = 1000 请求，1MB×50 blob 预热，本机净机）：**匿名** 1000/1000 全 200、0 失败、5.3s（187.2 req/s）、P50 3.7ms / **P95 6.8ms** / P99 9.8ms；**认证（admin Basic）** 1000/1000 全 200、0 失败、28.9s（34.6 req/s）、P50 864.6ms / **P95 2748.2ms** / P99 5002.2ms。M6 基线（T-172 G27）：匿名 38.8s、认证 44.1s、零 5xx——本轮两臂分别快 **86%/34%**（单向向好，无任何 >10% 回退）。认证臂 P95 由 Argon2 主导（T-172 D-1 既有姿态；均值 28.9ms/req vs M6 44.1ms/req 已改善） |
| P34 | 重启后首条可服务请求 <2s（含会话注册表重建） | ✅ | 25 个在途上传会话（各 1MB 部分数据）在册 → kill -9 → 重启 → **首条 200 blob GET 0.34s**（预算 2s）；重启后 RSS 82.6MB。1000 在途会话子句：T-216 lazy 重建使引导期重建成本恒为零（注册表仅在首次触碰时物化 id+offset）——结构性满足，本轮以 25 会话实测引导不受在册会话拖累佐证 |

## 四、M1~M6 全 P0 序列复跑（硬门槛，分批）

四个批次、一次性实例、每腿独立断言；**合计 192 腿全 PASS**。批次日志 `/tmp/t222/m1m2.log / m3.log / m4m6.log / m5.log`。

### 批次 1 — M1 C 序列 + M2 D 序列（84/84）

- **M1**：C01~C21 core——ping/readyz/version/health；五类型建仓 200 + PUT 既有=update 200；PUT 201 + sha256 回显 + GET 逐位；HEAD/ETag 304；Range 206/416（`Content-Range: bytes */65536` 逐字）；checksum-deploy 四态（hit 201 / well-formed miss 404 / 无 sha 头 400 / body-PUT 摘要不符 **409** errors[] 信封）；404 信封；`/artifactory/**` 404；匿名读默认开 + 匿名管理面 401；token 创建（access_token/token_id）/可用/吊销后 401/幂等吊销 200；用户建/查/错口令 401；name 搜索 + checksum 搜索（`/api/search/checksum?sha256=`，dedup 命中 zero-copy）；stats；删除 204 + 删后 404。
- **M2**：D01~D23——`/v2/` 401 + Bearer realm→/v2/token；token GET（admin）/带 token 200；错凭据 OAuth `invalid_client`；匿名 pull-scope token；monolithic POST ?digest= 201 + blob GET 逐位；分块 PATCH/PUT-with-body 收尾 201；manifest PUT 201 + `Docker-Content-Digest` 精确 + by-tag/by-digest GET 逐位；tags/list；_catalog；HEAD manifest；tag 覆盖 v2；**by-tag DELETE 405（digest-only，T-40 R2 口径）**；manifest 删除 202；删光后 tags/list 404 NAME_UNKNOWN + digest-only 推送 tags:null（T-52 两态）；`?n=1` + `Link rel="next"`；blob DELETE 405；跨协议 dedup（generic PUT 同字节 → **物理 blob 恰一份**）。

### 批次 2 — M3 M 序列真实客户端（33/33）

- **maven**（mvn 3.9.9 + JDK 21）：1.0.0/1.1.0 两轮 deploy（altDeploymentRepository + server 凭据）exit 0 ×2；jar/pom GET 200；`.sha1` 旁车内容与本地 sha1 一致；`maven-metadata.xml` 双版本合并 + `<release>1.1.0</release>`；**virtual 解析**（local 成员命中自有制品 + remote 成员 pull-through junit 4.13.2）`dependency:resolve` exit 0，junit jar 经 BinFlow 缓存落 m2；`X-Binflow-Resolved-From: maven-remote-central` 头在位。
- **npm**（10.9.8）：`.npmrc` `_auth` 形态 publish `+ t222-demo@1.0.0`；packument 200 含版本；净目录 install exit 0 + `node -e require` 可执行；dist-tag add stable + dist-tags 视图可见。
- **pypi**（现场 venv twine/pip，Python 3.14）：手工构建规范 wheel → twine upload exit 0；simple 页 200 含 `#sha256=` href + `api-version=2` meta；`pip install --index-url` + import 往返输出 `t222-ok`。

### 批次 3 — M4 W 序列 + M6 H core（55/55）

- **M4**：组 PUT 201 / POST 部分更新 200 / 回显；成员经 user-PUT `groups`（回显 + email 回显 + `groups:["nope"]` 400）；组授权继承（jane/john 经 devs 写 201、nick 无授权写/读 403）；并集（user read + group write 双向可用）；**降权即时生效**（demote 后写 403、读仍 200，无重启）；引用组删除 409 → 解引用后 200；条目 `?permissions` 视图（`/api/storage/{repo}/{path}?permissions`，principals+字母集）；includes/excludes（include 内 200 / exclude 外 403，匿名关实例）；**W38 CSRF**：session cookie + 恶意 Origin 写 403、无 Origin 写 201（Basic/Token 臂不受影响的口径）；配额（800B+400B vs 1024：首传 201、二传 **413** 含 quota 文案、被拒文件原子不留、usage 端点）；审计 actor/action 过滤 + limit>1000 400 + 非 admin 403。
- **M6**：`/metrics` 200 + Prometheus 格式 + binflow 系列；**T-208 禁用 seam**（john 自铸 token 先活 403-authenticated → `enabled:false` → **同 token 401** + basic 401 → 复启恢复）；`GET /api/v1/session` whoami 200；replication status/list 200；migration 未配置 501（既定语义）。
- ⚠️ M6 OIDC/LDAP 容器腿（H01~H05）未本轮复跑：T-224 已在 `830bc9d`（PASS 8/8，2026-08-23）覆盖 V21~V26 + Q11 四护栏；`830bc9d..b8e5fcf` 增量 = T-220 技术债（auth ldap.go/oidc.go lint 重构 + storage session.go N3 ctx 修复 + sql 行尾），HEAD 上 auth/storage 单测与全仓 race 全绿。按「不猜」原则如实记录为相邻覆盖 + 增量分析，未跑容器腿。

### 批次 4 — M5 G 序列 core（20/20）

- 冷启动（ping 计时）< 2s；`/binflow/docs/` 匿名 200 + HTML；深层上传 a/b/c/d.bin 201 + 隐式父级 info 200 + 父目录 ?list；GC dry-run/`--apply` exit 0 + **被引用 keep.bin 幸存**；**export→import 往返**（manifest 在位、import exit 0、keep.bin 可读且逐位一致、repo 配置存活）；1GB 流式 PUT/GET 全 200、**RSS 增量 133MB < 256MB**（macOS 页缓存噪声下的保守读数，1GB 上行 5.8s）；G27 性能面已由 P33 承载。
- G01~G17 发布产物矩阵（goreleaser/镜像分发/Helm/K8s/systemd/离线包）属 release-engineer 烟测域，同 T-172 口径不在本地 filestore 验收面复跑。

### 批次 5 — 全仓自动化

- `go test -race -count=1 -timeout 20m ./...`（净机后台）：**23 包 ok、0 FAIL、RACE_EXIT=0**（httpapi 230s、repo 332s、storage 266s 均在限内，零 race 告警）。

## 五、缺陷

**无（P0~P2 均无）。**

### 非缺陷登记（观察项，交 conductor 知悉/路由）

- **O-1**（行为记录）：digest-mismatch 的失败 Commit 会留下 `upload_sessions` 行 + `uploads/<id>/` 目录直至 TTL sweep 回收（内存注册表条目即时移除，URL 即刻 404）。与 ADR-0028「sweep+TTL 是唯一回收路径」一致，非缺陷；排障文档可补一句「失败收尾的残行靠 TTL 回收」。
- **O-2**（AC 歧义 → 转 PM/conductor）：NFR-P33 要求「P95 与 M6 基线偏差 <10%」，但 M6 G27 基线（T-172）只归档了时长与错误数，**未归档 P95**——分母不存在。本轮以同口径吞吐对比（两臂均优于基线 ≥34%）+ 零错误判定达标，并记录 M7 新 P95 基线（匿名 6.8ms / 认证 2748ms）供后续验收用作分母；建议此后性能基线归档 P50/P95/P99。
- **O-3**（条件腿如实标注）：Helm OCI（PRD FR-67-AC6 字面）与 oras/crane 客户端腿未复跑——本机客户端不可得；docker-CLI OCI 面已全绿。需要时按 T-172 先例现场置备 oras。
- **O-4**（口径备忘，本轮实证的 wire 契约）：`X-Checksum-Deploy: true` + `X-Checksum-Sha256` 四态；by-tag manifest DELETE 405；T-52 两态 tags/list；permission target wire 键为 `repos`/`includePatterns`/`excludePatterns`（数组）；条目权限视图在 `/api/storage/{repo}/{path}?permissions`；CSRF Origin 校验仅 session 臂；**匿名读是地板**——`anonymous_access: true` 时被 pattern 拒绝的已认证读仍 200（W12 类断言需匿名关实例）；用户建号必填 email。均为契约内行为，供后续 QA 剧本校准。
- **O-5**（测试环境教训，非产品）：Playwright governance audit 腿对 CPU 争用敏感（T-218 已登记）——本 QA 首跑在全仓 race 后台下 2 failed，净机复跑 85/3/0 全绿。后续验收避免性能/全套测试与重负载并行。

## 六、环境与清理

- dind 拓扑全拆：`t222-dind`/`t222-minio`/`t222-srv-s3*` 容器、`t222net` 网络、`t222-meta` 卷、`binflow:t222` 镜像、dind 内推送镜像与 login 凭据（`docker logout`）全清；`docker ps -a`/`images` 中 t222 资源归零。
- 一次性实例全部停杀（含击落前后残留的 v17v18 两进程，已 kill -9）；`ps` 无 `binflow-server serve -c /tmp` 残留。
- 临时数据目录全删；`/tmp/t222` 仅保留日志与脚本（260K）供复核，可随时清除。
- **用户实例 127.0.0.1:18080（PID 36174）全程未触碰**；kind 集群与 buildkit 容器未动。
- 凭据（t222-* 口令、MinIO 密钥）仅存在于临时目录与命令行，已随清理销毁，零落盘。

## 七、结论

**PASS**——V12~V20 全绿（含逐字节 Location/Range 复核）、P33/P34 达标（O-2 口径注记）、M1~M6 P0 复跑 192/192 全绿、全仓 race 23 包零告警、任务 0 探针修复双分支实证。零缺陷；5 条观察项已登记路由。
