# T-126 工作日志 — M5 工程 ticket 拆解

- ticket: T-126 [P0] M5 工程 ticket 拆解 `role:tech-lead`
- 状态: done（待主会话审核录入 BOARD.md）
- 日期: 2026-08-21
- 输入: docs/prd/milestone-5.md（v1.1：FR-34~FR-47、PB/DC/DM 13 条端点矩阵、G01~G35、§5.5 K1~K3 待校准、§6.4 债务归置主表 8/2 + 速裁 12、§7 Q1~Q4 已用户定案、§8 剧本十条、§9 GA DoD 七条）、reports/agents/T-88.md（M4 拆票先例）、reports/agents/T-119.md（ADR-0016 定案 + BE/FE 实现草案两票——本拆解照录不重议）、BOARD.md（只读：M4 全 done，deploy/dev 有 Dockerfile〔T-106 console 阶段〕+ compose + relink-assets，docs/user 11 篇〔console/docker-registry/faq/README + admin×4 + integrations×3〕，cmd/binflow-server/main.go:57-58 version/revision="dev" 占位实存，.goreleaser 不存在、docs-site/ 不存在、internal/docs 不存在）、.github/workflows/ci.yml（console node20 构建步先例）、Makefile（check-size 40MB 预警已有）、DECISIONS.md ADR-0011（embed 主交付/工作流细则/脚手架先行条款）
- git 序核实: T-125（PRD v1.1，§7 四项用户定案回写）已落——R 系列无 PRD/ADR 正面冲突待裁决（M4 R1 形态的门槛不存在）；ADR-0016（T-119）已定案，实现票可直接派。

## 结论

21 张票（T-127~T-147），P0 x18 / P1 x2 / P2 x1；首波可并行 4 张（T-127/T-128/T-129/T-130），最大波宽 4；共 7 批。关键路径发布链 T-127→T-132→T-136/T-137→T-139→T-143→T-147（7 票 7 批），文档链 T-129→T-141→T-146 与债务链 T-128→T-131→T-134→T-144 汇入 T-147。

---

## 〇、口径总则（三硬约束 / Q1~Q4 写死 / M1~M4 复用清单 / K1~K3 处置）

**PRD 内嵌三硬约束（全部进票）**：

1. **FR-44 FE 硬依赖 BE 合入**：T-131 `dep:T-128`（先删兜底会回归 404——T-119 草案明文）；连带 T-134（同 `web/src/pages/repositories/tree` 目录）排 T-131 后串行。
2. **docs-site 脚手架先于文档矩阵票**：T-129（批 1）→ T-141/T-142（批 4/5）——ADR-0011 后果条款「类似 T-7 先行」。
3. **Q3 条件腿 `dep:用户环境`**：G05（windows 锁运行时）与 G15b（systemd 真机）在 T-138/T-145 票面标注；到位前按 T-106 §2.5 降级口径（静态验证 + 记录 + 逐条用户确认），到位后补跑不受 m5-done 阻断（PRD §9-③）。

**Q1~Q4 用户定案写死（不重议）**：Q1 公共渠道（GitHub Releases + ghcr.io；Chart 仓库首选 GitHub Pages 形态、终形随 K2）→ T-127/T-132/T-147 标注「推送前逐项用户确认、凭证用户注入不入库；本地归档为发布前置校验面」；Q2 HPA 默认 disabled + maxReplicas≤1 拦截 → T-136 AC；Q3 见上；Q4 VER=v1.0.0 双 tag → T-127 版本注入基准 + T-147 G35 收口。

**K1~K3 处置**：K1（Docusaurus 构建细节）/ K2（镜像与 Chart 供应链细节）→ T-130 架构终裁微票（批 1，顺带收编 §6.4 #6 architecture §7.1 两行补遗——P2 纯文档项并入防散票）；K3（离线包内部布局）→ T-139 票内细化（PRD §5.5 原文归 release-engineer 产物票），不立微票。**T-129 按 K1 暂行形态开工、T-132 本地 tag 先行**——终裁推翻时的改动面收敛在配置层/脚本常量（R5）。

**M1~M4 能力复用清单（零重做，票内不再实现）**：

| 复用面 | 来源 | M5 消费方式 |
|---|---|---|
| deploy/dev Dockerfile 多阶段（node 自建 console → go build embed）+ HEALTHCHECK/非 root/CGO 零 | T-17/T-106 | T-132 双变体 GA 构建链克隆该模式（独立布局 deploy/release，K3）；dev 形态不动 |
| compose 命名卷/:? 口令/restart/healthcheck + 反代直通片段（D-106-2 修复后） | T-17/T-45/T-106 | T-135 GA compose 升级（资源 limits 样例 + nginx profile） |
| relink-assets 自检门禁（懒 chunk 路径重写） | T-120 | T-129 docs 站静态资产若需路径处理同法（Docusaurus baseUrl=/binflow/docs/ 原生支持，预计零改） |
| 烟测剧本与降级口径（五协议 roundtrip / restart 持久 / down -v 零残留 / 不可达形态记录） | T-45/T-105/T-106 | T-135~T-139 烟测 + T-145 QA 全量复跑 |
| putNode 五落库路径唯一汇点 + folder 臂（ensureFolderLedger）+ pruneEmptyParents + 005 纯 SQL backfill 先例 | T-12/T-95 + ADR-0016 | T-128 materializeAncestors 复用 folder 臂语义 + 007 照 005 模板 |
| FolderMarkerSHA 占位契约 + SnapshotChecksums 按值排除 | T-124 | T-128 AC④ 回归锚（007 哨兵 blob 不误伤 export） |
| TokenRegistry / audit Append / NFR-S3 脱敏链 / W23 词表断言 | T-11/T-93/T-104 | T-133 只加两挂点 + 指纹 detail，词表常量两词 |
| storageNode 三面汇点（item info/?list/?permissions） | T-14/T-97 | T-140 uri 基址族单缝修正 |
| Playwright 基建 + route abort 手法 + request 监听断言 | T-89/T-104 | T-146 G18 离线上下文 / T-131 G30e 零 /api/search 断言 |
| health 只增字段 + 启动结构化日志 | T-14/T-16 | T-127 version 字段注入（cmd main.go:57-58 占位实存） |
| QA 基线脚本与四序列（C/D/M/W） | T-18/43/74/76/103~105 | T-144 §5.6 反转表更新后复跑 |
| Makefile 单入口 + CI 同源 + check-size 40MB 预警 | T-7/T-89 | T-127 release/release-snapshot 目标 + 发布 job；T-129 make docs |

---

## 一、票据列表（T-127 ~ T-147，共 21 张）

### 脚手架与架构终裁（批 1）

**T-127** [P0] goreleaser 发布基线 + 版本注入贯通（FR-34 工程面，PB-01/PB-02） `role:devops-engineer` `area:.goreleaser.yaml、Makefile（release/release-snapshot/check-size 扩展）、cmd/binflow-server（version/revision 变量与 --version/启动日志）、internal/httpapi（health version 字段）、.github/workflows/ci.yml（发布 job）、.gitignore（dist/）` `dep:—（M4 基线）`

AC:
① G01 主体：`.goreleaser.yaml` 六平台（linux/darwin/windows × amd64/arm64）全 `CGO_ENABLED=0`；产物 `binflow_<VER>_<os>_<arch>.tar.gz`（windows `.zip`）内含 binflow-server（.exe）+ LICENSE + README 摘要 + `binflow_<VER>_checksums.txt`（sha256）；`make release-snapshot`（`goreleaser release --snapshot --clean`）退出码 0，`shasum -a 256 -c` 全 OK；构建前资产就位（before hooks 或文档注明先 `make console`〔+T-129 合入后 `make docs`〕——embed 占位策略沿 T-89 二选一并注明）；goreleaser 为构建侧工具**不入 go.mod**（NFR-S31）
② G02 版本注入：ldflags 注入 `main.version/main.revision`（git short sha）；`--version` 输出 `binflow-server <VER> (<revision>)`；启动结构化日志首行 version 同值；`GET /api/v1/health` 增顶层 `version` 字段（**只增字段**，§6.3）与 `--version` 同值；裸 `go build` 回退 `dev (dev)`（现状语义零变更，测试钉死）；VER 基准 v1.0.0（Q4）
③ check-size 扩展 + 发布目标：逐平台压缩产物 ≤ 40MB 断言（超限 WARN→exit 1 可配，QA G03 消费）；`make release` 本地归档形态（dist/ + checksums 照做，作为发布前置校验面）；**真发布到 GitHub Releases 的凭证与推送不在本票**（Q1：逐项用户确认、凭证用户注入——T-147 G35 收口）；CI 增发布流水线 job（dry-run 校验产物存在性，零凭证）；darwin 本机 + linux/amd64 容器 `serve` `/readyz` 200 自测（G04 腿，docs 200 断言在 T-129 合入后生效——票内注明）

W/G 映射：G01/G02/G03/G04（docs 腿除外）。
review：单 reviewer（构建链与 CI 面）。

**T-128** [P0] FR-44 BE：putNode 祖先 folder 行材料化 + 007 回填迁移（DM-02，ADR-0016） `role:dev-go-core` `area:internal/repo（service putNode/pruneEmptyParents）、internal/metadata（migrations 007）` `dep:—（ADR-0016 已定案；实现照 reports/agents/T-119.md 草案票 1，不重议）`
review：**双 reviewer（正确性 + 架构）——硬性要求（GA 前最后一个行为变更面 + 存量库迁移）**。

AC:
① 材料化（ADR-0016 要点 1~3）：putNode 内抽 `materializeAncestors`——path 每个祖先段经既有 folder 臂写（ensureFolderLedger/新行或幂等刷 UpdatedAt、size 0），**祖先先于目标行落库**（崩溃窗口至多残留良性空 folder 行）；祖先为派生状态：不判权、不过 governance pattern、不记审计、quota/usage delta 0；file 与 folder 目标均材料化（空仓 mkdir 链 `a/b/c/` 的 `a/`、`a/b/` 服务端建——FE 逐段循环可删的 BE 前提）
② 007 回填（要点 4——最大坑）：`007_folder_rows_backfill.{sqlite,postgres}.sql` 先 `INSERT OR IGNORE` 哨兵 blob 行（sha256=64×'0'，size 0——FK 前置，直接插 folder 行必撞 FK）再递归 CTE 拆既有 node path 推祖先集 → folder 行 INSERT OR IGNORE（created_by=''；created_at/updated_at 取该目录最年轻子代值，票内定并测试确定性）；双方言、幂等（重跑 changes()=0 断言）；文件体内零事务语句（T-10 M9 红线）；老库升级路径单测（006 库迁移后隐式目录 GET 200）
③ 行为变更与回归（G30a~d BE 面 + FR-44-AC6）：深层单上传（≥4 段）后逐级隐式目录 `GET /api/storage/<repo>/<dir>/` 200 FolderInfo（真实 created/lastModified）、`?list` 200；删唯一子文件 → 空祖先链被 pruneEmptyParents（GET 404）、有兄弟子代不剪、**显式 mkdir 的空目录同样被剪**（repo-semantics §4 参考语义断言）；重复上传零新行 + 祖先 mtime 刷新、created/createdBy 保留；同名 file+folder 共存回归；docker push 后 image 目录可浏览 + **docker 套件全量复跑零断言依赖现状**；governance 拒绝上传零祖先残留（门在 putNode 前）；并发同新目录上传无 unique violation；export/import 往返含 folder 行与哨兵 blob（W28/W30 复跑）+ **T-124 面回归**（mkdir 实例 export manifest 零占位——FolderMarkerSHA 按值排除不受回填影响）；搜索索引对 folder 行显式排除维持（search 域零变更）；**存量「隐式目录 404」断言（httpapi/generic 两侧）随本票同批更新并注明来源**（§5.6 反转表，T-88 R5 同款）；remote engine 不跟随（§11.20 维持，零改动）

**T-129** [P0] docs-site Docusaurus 脚手架：build 链 + embed + /binflow/docs 挂载（FR-41 工程面，DC-01） `role:devops-engineer` `area:docs-site/（新：docusaurus 配置/侧栏/i18n 骨架）、internal/docs（新：go:embed + Handler）、Makefile（docs/docs-size 目标）、.github/workflows/ci.yml（docs 构建步）、web/e2e（docs 冒烟 spec）` `dep:—（M4 基线）`
【K1 暂行形态开工：本地索引全文搜索（Docusaurus 内建方案）/ self-host 静态资产零 CDN / zh 唯一 locale（i18n 骨架就位）/ 版本化 docs/v1.x；T-130 终裁推翻时改动面收敛在 docs-site 配置层（R5）】

AC:
① 站点与工作流（ADR-0011 工作流细则）：`docs-site/`（Docusaurus 3）以 `docs/user/` 为内容源聚合构建——**tech-writer 只写 docs/user/*.md、不碰 docs-site/**；frontmatter 兼容子集（title/description/sidebar_position）既有 11 篇回查通过；baseUrl `/binflow/docs/`；`make docs` = build → 复制进 `internal/docs/dist`（与 console web/dist→internal/console 同构）+ `docs-size` 报告 gzip 增量（预算 ≤15MB，超限 WARN 不阻断——fallback 走 ADR-0011 须经用户确认，票内只报告）；docs-static tar 可选输出（`docs-static_<VER>.tar.gz` 同一 build 产物，P1）
② 挂载与匿名（DC-01）：`internal/docs` 包 go:embed + Handler 挂 `GET /binflow/docs/**`——**匿名只读**（anonymous_access=false 实例同样 200，与 /healthz 同类产品自描述面）；无任何写面；repo key 保留字 `docs` 既有（ADR-0008 并集）零改动；静态资产指纹缓存策略（immutable）+ shell no-cache；段内路径不越段
③ 离线与 CI（NFR-S29 前提）：零 CDN/零第三方脚本/零字体外链（self-host 资产落盘）；Playwright 冒烟 spec（route abort 一切外部 host → 首页 200 + 中文搜索可用——正式 G18 归 T-146）；CI 增 docs-site node≥20 构建步（npm audit --audit-level=high 同 web 口径）+ `make build` 无 node 环境可编译（dist 占位策略沿 T-89 二选一并注明）；`make docs` 前后二进制体积增量记录进票面日志（G21 前置数据）

review：单 reviewer（构建链与 embed 边界；Makefile 与 T-127 目标段不重叠——R1）。

**T-130** [P0] K1/K2 架构终裁 + §7.1 两行补遗（§5.5 待校准 + §6.4 #6） `role:architect` `area:DECISIONS.md（ADR-0011 增补 + 镜像供应链 ADR）、docs/design/architecture.md、docs/prd/milestone-5.md（§5.5 回写）` `dep:—`
review：conductor 核验直收（文档票，M4 T-108/T-122 先例）。

AC:
① K1 终裁（ADR-0011 增补）：Docusaurus 搜索索引方案（内建本地索引 vs 外挂）、self-host 资产落盘布局、fallback 触发线（超 15MB 的处置步骤）、docs-static tar 内容——四项定案回写 ADR-0011 + PRD §5.5 K1 行（DC-01 置信度中→高）；**若推翻 T-129 暂行形态，须给出收敛改动清单**（预计限 docs-site 配置层）
② K2 收口（新 ADR 或 ADR-0011 同族增补）：ghcr.io 命名空间（org/镜像名）、浮动 tag `<VER>` 指向 distroless 定案、distroless 基镜像 tag 锚定、**Chart 仓库形态**（Q1 首选 GitHub Pages——终形定案）；渠道家族已用户定案（GitHub Releases + ghcr.io）不重议；PRD §5.5 K2/PB-03/PB-05 置信度回写
③ §6.4 #6 顺带：architecture §7.1 补两行（PUT /api/security/password、/api/v1/permissions CRUD——T-122 遗留，GA 文档面复核点，防 FR-41 API 参考与架构文档失真）

### 债务收编 BE/FE 与镜像（批 2）

**T-131** [P0] FR-44 FE：删隐式目录双兜底（DM-02 FE 面） `role:dev-frontend` `area:web/src/pages/repositories/tree、web/src/lib（契约注记）` `dep:T-128（**硬依赖 BE 合入后**——PRD 三硬约束之一；先删兜底会回归 404）`
review：单 reviewer（删码面 + e2e 证据）。

AC:
① 删双兜底（T-119 草案票 2）：删 `searchListing` 前缀重构兜底（listChildren 主路径 200 直接消费）+ 删 mkdir 逐段材料化循环（改单段 PUT——BE 已服务端材料化）；TreePage 404 分支对非根目录恢复可达（**NB① 结构性关闭**）；lib.ts 头部契约注记同步删
② G30e：Playwright——隐式目录主路径浏览**不再发 `/api/search` 请求**（request 监听断言）；typo 深链 404 态正确呈现；mkdir 单段上传回归；大目录分页回归（T-100 面零回归）
③ 既有 e2e 套件全绿（tree/search/upload 三链）+ `make console` 构建零告警

**T-132** [P0] FR-35 Docker multi-arch 镜像双变体（PB-03） `role:release-engineer` `area:deploy/release/（新：alpine/Dockerfile、distroless/Dockerfile、buildx 脚本）` `dep:T-127（构建链与版本锚定）`
【K2 暂行：本地 tag 先行（`binflow:<VER>-alpine/-distroless/` 浮动），ghcr.io 命名空间待 T-130；**推送前逐项用户确认、凭证用户注入不入库**——本票只到本地 tag + manifest 断言】
review：**双 reviewer（安全 + 架构/供应链）——硬性要求（K2 供应链面 + 最小面断言）**。

AC:
① G06 multi-arch：buildx `--platform linux/amd64,linux/arm64` 构建两变体退出码 0；`docker buildx imagetools inspect` 两变体 manifest list 均含两平台；构建沿 T-106 先例**镜像内 node 阶段自建 console 与 docs 站**（不依赖构建机本地状态；docs 构建步在 T-129 合入后纳入——票内注明两阶段口径）；CGO_ENABLED=0 维持
② G07 变体契约：alpine 基底（含 shell）与 `gcr.io/distroless/static-debian12:nonroot`；`EXPOSE 8080`、USER 非 root、`HEALTHCHECK /readyz`、data 默认 `/var/lib/binflow`；两变体（amd64 本机）起容器 `/readyz` 200 且 HEALTHCHECK 转 healthy；alpine 腿 `docker exec id -u` 非 0；distroless 腿 `--entrypoint sh` **失败**（无 shell 最小面断言）
③ G08/G07b/AC5：容器内 `/binflow/ui/` + `/binflow/docs/` 200、console 资产指纹与宿主 `make console` 产物一致（T-106 §2.2 手法）；qemu arm64 distroless `/readyz` 200（P1 腿，qemu 不可得降级记录——R8）；两变体压缩尺寸记录（alpine ≤60MB / distroless ≤40MB 参考线，超限归因不阻塞）；dev 形态镜像（deploy/dev）零改动断言

**T-133** [P0] FR-45 token 签发/吊销落审计（DM-03，D-104-2 修复） `role:dev-go-core` `area:internal/httpapi（token 族挂点）、internal/audit（词表常量）、internal/auth（sha256 指纹 helper）` `dep:—（M4 基线；错开批 1 的 httpapi/health 文件——T-127 后派发即无冲突）`
review：单 reviewer（审计面 + 防噪声口径）。

AC:
① G31a/G31b：`POST /api/security/token`（client_credentials）200 后 `token.issue` 落审计（actor / token 指纹 sha256 前 8 位**非明文** / 过期时长）；revoke → `token.revoke`（actor / 被吊销 token 指纹）；被吊销 token 重放 401；detail 经 NFR-S3 脱敏链（grep token 明文零命中）；REST 面全覆盖（form/JSON 双形态）
② G31c 防噪声口径（产品定案）：`/v2/token` 短 TTL 会话 token **不产生** `token.issue`（运行态凭据，类同 web_sessions 不入备份的 §11.19 同族——挂点选型须结构上隔离，不能靠调用方自觉）；docker login / Bearer 协商后 audit 无新 token.issue 断言
③ 词表回归（FR-45-AC4）：M4 W23 词表断言循环扩 `token.issue`/`token.revoke` 两词全过；M4 FR-29 定案词表全集维持零变更；table-driven（签发/吊销/明文零泄漏/docker 隔离四矩阵）

### 部署矩阵与 FE 收官（批 3）

**T-134** [P1] FR-46 docker 视图数据源定案落地 + 控制台帮助入口（DC-02） `role:dev-frontend` `area:web/src/pages/repositories/tree（docker 特化）、web/src（AppShell 帮助入口一行）` `dep:T-128（storage 面数据完整度——ADR-0016 后 image 目录可浏览）,T-131（同目录串行）`
【数据源裁决已产品定案（PRD FR-46）：storage 面 `GET /api/storage/{repo}/{path}` children——session 凭据原生可用；**零新端点**；maven/npm/pypi 特化视图 M6+】

AC:
① G32a/G32b：docker 仓树渲染 manifest digest 行 + tag 徽标（数据源 storage children；tag node M2 起落 nodes 行）；Playwright session 凭据登录 + **request 监听断言零 `/v2/*` 请求**（数据源口径证据）；UI 树 tag 集与 `crane ls`（或 curl+Bearer tags/list）输出一致（数据等价抽样）
② DC-02：控制台「帮助」入口链到 `/binflow/docs/`（AppShell 一行，P1——G19b 腿归 T-146）；四态/骨架屏沿 console-ux 既有基元
③ FR-46-AC3 文档联动挂点：console 篇「docker 视图数据源 = storage 面」一句口径归 T-142（本票提供措辞事实核对）；tree 域 e2e 全绿零回归

**T-135** [P0] FR-36 docker-compose GA 产物（PB-04） `role:release-engineer` `area:deploy/compose/（新）` `dep:T-132（GA 镜像构建链）`
review：单 reviewer（部署正确性 + 烟测证据）。

AC:
① G09：`deploy/compose/docker-compose.yml`（具名卷 / `restart: unless-stopped` / healthcheck / `:?` 强制口令 / 资源 limits 注释样例 / 可选 nginx 反代 profile——D-106-2 修复后片段 `location = /binflow` 直通 + `/v2/` 不 rewrite）+ `.env.example` 完整；`up -d --build` → healthy → 五协议烟测（dind docker login/push/pull + generic/maven/npm/pypi 各一腿）→ `compose restart` → 制品 GET 200 + 旧 session whoami 200 → `down -v` 卷/网络零残留（T-106 §2.2 口径复跑）
② G10 15 分钟口径：clean clone + 计时从零到 `/readyz` ≤ 15 分钟（镜像构建计入——镜像内自建 console/docs 是文档路径一部分；超时归因写进报告，文档问题转 tech-writer——R9）；`docker compose config -q` 通过 + `.env.example` 与 compose 引用变量一一对应（无缺注）
③ 反代 profile（P1）：`--profile nginx` 起栈 → `/binflow` 301 相对路径 + `/v2/` 401 挑战直通（T-106 §2.3 断言复跑）；dev compose（deploy/dev）维持不动断言

**T-136** [P0] FR-37 Helm Chart（PB-05） `role:release-engineer` `area:charts/binflow/（新）` `dep:T-132（镜像 tag 锚定）`
review：单 reviewer（schema 拦截与 ingress 直通面）。

AC:
① G11：Chart 全对象（Deployment replicaCount=1 / PVC RWO / Service / Ingress 可选 / ConfigMap + Secret 口令分离——`BINFLOW_ADMIN_PASSWORD` 不入 ConfigMap / Probes liveness=/healthz readiness=/readyz / resources 样例 / values.schema.json / NOTES.txt 取口令与端口提示）；`helm lint` 0 错误；`helm template` 渲染对象齐全；HPA 默认不渲染；NOTES.txt 渲染非空；Chart 版本随 <VER>
② G12 单副本拦截（Q2 定案 + architecture §9 硬约束）：`--set replicaCount=2` → 安装失败（schema maximum:1，错误信息含单副本约束与 HA=M6+ 说明）；`--set hpa.enabled=true,hpa.maxReplicas=2` 同样失败（HPA 模板默认 disabled，启用强制 maxReplicas≤1）
③ G13/G13b/G13b-AC5：kind（本地 bf-ga，Q3 定案不依赖用户环境）`helm install` → Pod Ready → port-forward `/readyz` 200 → dind NodePort docker push/pull 一腿 + generic PUT/GET → `kubectl delete pod` 重建后制品 GET 200（PVC 持久化）；ingress 渲染断言——同 host 下 `/` 与 `/v2/` 均直通、**无 rewrite 注解**（ADR-0010）；可达环境 curl `/v2/` 401 挑战；`helm upgrade` patch 版回归（P1 记录项）；helm-docs/README values 表（P1）

**T-137** [P0] FR-38 原生 K8s 清单（PB-06） `role:release-engineer` `area:deploy/k8s/（新）` `dep:T-132（镜像引用形态）`
review：单 reviewer（安全基线静态断言）。

AC:
① G14：`deploy/k8s/`（Deployment / PVC / Service / Secret 样例 / Ingress 注释样例）；kind 上 `kubectl apply -f` → Pod Ready → `/readyz` 200 → push/pull 一腿 → delete pod 重建制品存活
② 静态断言：`kubectl get deploy -o yaml` 含 `runAsNonRoot: true`、resources requests+limits、探针三件；镜像字段无 `latest`（tag 文档变量锚定）；env 口令经 Secret
③ `kubeconform -strict` 通过 + `kustomization.yaml` 可 `kustomize build`（P1）

### 收尾产物与文档内容（批 4）

**T-138** [P1] FR-39 systemd 单元 + 安装脚本（PB-07） `role:release-engineer` `area:contrib/systemd/（新：binflow.service、install.sh）` `dep:T-127（checksums 产物形态）`
【Q3 定案：真机腿 `dep:用户环境`——到位前按 T-106 降级口径静态先行（记录 + 用户确认），到位后补跑不受 m5-done 阻断】
review：单 reviewer（脚本健壮性；供应链面由 T-143 G16 复核兜底）。

AC:
① G15 静态面：`binflow.service`（Type=simple / User=binflow / ExecStart=/usr/local/bin/binflow-server serve -c /etc/binflow/binflow.yaml / Restart=on-failure / ReadWritePaths=/var/lib/binflow / TimeoutStopSec≥30 对齐优雅停机——architecture §9）；容器内 `systemd-analyze verify` 零 error；`shellcheck install.sh` 零告警
② install.sh：system 用户创建、目录布局（/etc/binflow /var/lib/binflow /usr/local/bin）、二进制下载 + **checksums 校验失败即中止**、单元安装、`systemctl enable --now`、`--dry-run` 支持、卸载路径文档化；依赖仅 bash + curl + sha256sum + systemctl
③ G16 校验和防线 + G15b 条件腿（`dep:用户环境`）：沙箱内篡改二进制后跑安装脚本 → 校验步失败退出（退出码非 0、零文件落地）；真机腿（systemctl start → active → /readyz 200 → restart 制品存活 → is-enabled=enabled → stop 优雅停机行 + 退出码 0）环境到位后补跑，未到位按降级口径记录并经用户确认

**T-139** [P0] FR-40 离线安装包（PB-08） `role:release-engineer` `area:deploy/offline/（新：install-offline.sh、README-offline.md、打包脚本）、Makefile（offline-bundle 目标——批 1 后无 Makefile 并行冲突）` `dep:T-127（linux 二进制+checksums）,T-132（双变体镜像）,T-136（chart）,T-137（k8s 清单）`
【K3 本票细化：内部布局与 install-offline.sh 语义（幂等/失败清理/kind 导入分支）】

AC:
① G17：`binflow_offline_<VER>.tar.gz` 结构——`images/binflow-<VER>.tar`（docker save 双变体 multi-arch 一并）/ `charts/binflow-<VER>.tgz` + `k8s/` 副本 / `binaries/`（linux amd64+arm64 与 checksums）/ `install-offline.sh`（docker load → 可选 kind 导入 / compose 起 / helm install 三分支，幂等 + 失败清理）/ `README-offline.md` / 包内 `SHA256SUMS` 全过 + 外层 tar sha256 记录；docs 不单独附带（embed 随二进制/镜像；ADR-0011 fallback 生效时附 docs-static tar——联动标注）
② G17b 零外网语义：隔离网络环境（独立 docker network / 断网 VM，QA 注明手法——镜像与包预置后断外网或 `--network none` 前置 load）从包起 compose 或 kind 栈 → `/readyz` 200 → docker push/pull + generic PUT/GET 成功，全程零外网请求；包体积与件数记录（P2 记录项）
③ install-offline.sh `shellcheck` 零告警 + `--dry-run`（或等价检查模式）；打包目标进 Makefile 单入口

**T-140** [P2] FR-47 storage uri 基址族修正（DM-01，O-106-1 + O-4 同族） `role:dev-go-core` `area:internal/httpapi（storage.go uri 拼接单缝）` `dep:—（错开批 1 httpapi 文件即无冲突）`
review：单 reviewer（形状冻结面——GA 后 breaking，一次修对）。

AC:
① G33a：storage REST 族（item info / `?list` / `?permissions`）uri 基址统一——`uri = <scheme://host[:port]>/binflow/api/storage/{repo}/{path}` 形态（以 docs/reverse/rest-api.md §3 规格形为准，高置信度）；**repo key 恰出现一次**（现状双重嵌段）；顶层 uri 与 `files[].uri` 同基；`curl <uri>` 200 自洽
② G33b：`?permissions` uri 与 item info uri 拼接规则一致（同族断言）；形态对照 rest-api.md §3 记录进票面
③ 回归：M4 W21（?permissions）与 W12（树消费）断言零破坏；FE 树/搜索零改动（FE 不消费顶层 uri——T-100 核实，QA 复核注记归 T-144）

**T-142** [P0] FR-41 内容（下）：API 参考 + FAQ 扩编 + 既有篇目增补 `role:tech-writer` `area:docs/user/api-reference.md（新）、docs/user/faq.md、docs/user/integrations/maven.md、docs/user/admin/{governance,backup-restore,console}.md（增补，不动 install——T-141 area）` `dep:T-129（脚手架先于内容——三硬约束之二）,T-128（prune 行为定案——backup-restore 口径依赖）`

AC:
① API 参考篇（FR-41 行为规格第 4 类）：Artifactory 兼容端点子集总表（E/DE/ME/NE/PE/SE/SR 域汇总，来源各里程碑 PRD 矩阵——含 T-130 补遗的 §7.1 两行面）+ `/api/v1` 自有端点 + 三凭据面（Basic / Token / session）+ 错误信封三分层（errors[] / 纯文本 / OAuth）**各至少一例**（FR-41-AC6 硬项）
② 增补四硬项（FR-41-AC6）：maven 篇补 mvn ≥ 3.8 `maven-default-http-blocker` 注意事项（O-106-2 收编——T-106 修正账⑤ 实证）；governance 篇补 `graceHours:0` 运维告警（N6 收编——busy 实例 apply+graceHours:0 可致在途上传偶发失败）；backup-restore 篇补「删文件后空目录自动清除是预期行为」口径（ADR-0016 prune 语义联动 FR-44，**含显式 mkdir 空目录同剪**）；console 篇补「docker 视图数据源 = storage 面」一句（FR-46-AC3，措辞与 T-134 事实核对）
③ FAQ 扩编（GA 条目：15 分钟口径 / 体积预算 / 单副本约束与 HPA / 与 Artifactory 差异对照）；全部命令在批 4 前产物上抽样复跑退出码 0；中文文档、命令与标识符英文；frontmatter Docusaurus 兼容子集（T-129 回查口径）

### 内容收口与安全审计（批 5）

**T-141** [P0] FR-41 内容（上）：安装指南 7 形态 + generic 接入 + 升级篇 `role:tech-writer` `area:docs/user/install/（新 7+1 篇）、docs/user/integrations/generic.md（新）、docs/user/README.md（导航）` `dep:T-129（脚手架硬前置）`【终验腿挂部署产物票：install 各篇命令须在 T-135~T-139 done 产物上复跑——起草可先行，终验命令逐篇 exit 0；QA 篇目清单核对归 T-146】

AC:
① 安装 7 形态篇（bare 二进制 / Docker / compose / Helm / 原生 K8s / systemd / 离线包）——每篇含前置要求、步骤（按产物实际形态）、`/readyz` 验证、卸载/回滚；**按文档从零到 /readyz 计时 ≤15 分钟口径自测一遍并记录**（G10 联动；超时归因反馈产物票或本篇优化）
② generic(raw) 上传下载接入篇（新增第 5 接入篇：curl PUT/GET/checksum-deploy/Range）；升级与版本说明篇（P1：v1.0.0 起步、export/import 迁移路径引 backup-restore、跨版本 metadata 迁移链 001~007 说明性口径）
③ 全部命令在 T-135~T-139 验收产物上抽样复跑退出码 0（SYSTEMD/windows 腿按 Q3 降级口径标注）；中文、frontmatter 子集、docs/user/README 导航更新

**T-143** [P0] FR-42 安全审计（G23~G26 + 报告） `role:security-auditor` `area:reports/（安全审计报告归档）、验收（扫描矩阵）` `dep:T-127,T-129（docs-site npm audit 面）,T-132（镜像扫描对象）,T-139（离线包 checksums 面）`

AC:
① G23/G25 依赖与密钥面：`govulncheck ./...` 零 High/Critical（数据库时点记录）或豁免清单齐备（每条含理由与期限）；`go mod verify` OK；`npm audit`（web/ + docs-site/）High 修复或豁免记录（构建面依赖不入运行时二进制，NFR-S31）；gitleaks 全仓扫描零命中（含历史）；goreleaser/docusaurus/trivy 等构建工具与运行时依赖隔离登记（ADR-0005 准入记录）
② G24 镜像面：trivy 双变体 × amd64/arm64（qemu/manifest 两种手段之一，注明）`--severity HIGH,CRITICAL --exit-code 1` 零发现；alpine apk 升级处置（转 release-engineer 执行并复扫闭环）；distroless 面 zero-shell 复核（G07 断言引用）
③ G26 + 报告：负断言抽样集复跑全绿（SSRF 16 直连变体抽查 / 路径穿越变体抽查 / CSRF 跨源 403 / 匿名矩阵——引用既有 W/M 序列编号，报告列明复跑项）；export 产物敏感面复查（NFR-S22 0700+密文同等保管）；FR-45 联动复核（token.issue/revoke 可查、detail 零明文——G31 同断言）；新增 deploy/+charts/ 面 lint（helm lint / kubeconform / shellcheck 引用各票结果）；CI 与构建脚本零凭据硬编码；**安全审计报告归档**：分级发现表 + 豁免清单 + 结论（「可 GA」或阻塞项——阻塞 = Critical 未修）；任何 Critical 修复后复扫闭环

### QA（批 5~7，实例串行/端口隔离）

**T-144** [P0] QA：债务收编全链 + 四里程碑回归基线（剧本 1/5） `role:qa-engineer` `area:验收（G30~G33 + G34 回归）` `dep:T-128,T-131（FR-44 BE+FE）,T-133（token）,T-134（docker 视图 P1 腿）,T-140（uri P2 腿——若未 done 按延后标注执行）`

AC:
① G30a~G30e 全链：深层上传逐级 200 / prune 三态（空链剪、兄弟不剪、显式 mkdir 剪）/ 007 幂等与老库升级腿 / export-import 往返含 folder 行 + T-124 零占位面 / Playwright 零 /api/search 请求 + typo 404 态；G31a~c（token 审计三断言含 docker 隔离）；G32（docker 视图 + crane 对账，P1）；G33（uri 基址族三断言，P2）
② G34 回归基线（GA 硬门槛）：**先按 §5.6 反转表更新断言**（隐式目录 404→200 / --version dev→<VER> / /binflow/docs 404→200 / docker image 目录可浏览 / W23 词表扩两词 / M3 M16c 注记过时处理），后 M1 C / M2 D / M3 M / M4 W 四序列 P0 复跑全绿；docker/mvn/npm/pip 真实客户端各一条链零回归；每条附原始输出，任何 5xx/非预期码打回附票号
③ FR-44-AC6 收口面：docker 套件全量复跑（image 目录行为变更面）+ governance 拒绝上传零祖先残留 + 并发同新目录无 unique violation；报告 `reports/agents/T-144-qa.md`

**T-145** [P0] QA：发布矩阵与部署形态全量（剧本 2/3 + 条件腿） `role:qa-engineer` `area:验收（G01~G17b 发布/镜像/部署面；独立实例端口段 18160+）` `dep:T-127,T-132,T-135,T-136,T-137,T-138,T-139`
【Q3 条件腿 `dep:用户环境`：G05（windows serve + export 并发第二 export/gc 退出码非 0——LockFileEx 互斥，T-96 遗留收口）与 G15b（systemd 真机）——环境到位前按 T-106 降级口径记录 + 用户确认，到位后补跑不受 m5-done 阻断】

AC:
① 二进制面：G01（产物+checksums 全 OK）/ G02（版本注入三面同值 + 不可跑平台 strings 抽查）/ G03（六平台 ≤40MB 表格化 + docs embed 增量记录）/ G04+G04b（darwin+linux/amd64 可跑腿 + qemu arm64 P1）/ G05 条件腿（降级口径记录）；G07（FR-34-AC7 bare 形态五协议烟测 + console/session 链复跑）
② 镜像与部署面：G06/G07/G07b/G08（含 distroless 无 shell 反断言）/ G09+G10（compose 全链 + 15 分钟计时）/ G11~G13b（Helm lint/拦截/kind/PVC/ingress）/ G14（K8s 清单 + 静态断言 + kubeconform P1）/ G15+G16（systemd 静态 + 校验和防线沙箱）/ G17+G17b（离线包结构/校验/零外网起栈 + G18 联动腿：断网浏览器 docs 可用——正式归 T-146）；NFR-P25 形态启动收敛 ≤60s 逐形态记录（口径报告注明）
③ 条件腿处置 + 报告：G05/G15b 按 Q3 定案执行（到位实测 / 未到位降级记录 + 逐条用户确认挂 GA 总矩阵）；任何 5xx/非预期码打回附票号；报告 `reports/agents/T-145-qa.md`

**T-146** [P0] QA：文档中心（剧本 4） `role:qa-engineer` `area:验收（G18~G22 Playwright + curl；与 T-145 独立实例并行，端口段隔离）` `dep:T-127（G21 体积对账需 release-snapshot 全产）,T-129,T-141,T-142,T-134（G19b 帮助入口腿）`

AC:
① G19/G19b：`make docs` exit 0；embed 后 `GET /binflow/docs/` 200；匿名关实例匿名 GET 同 200；Playwright 点帮助图标 → URL 含 /binflow/docs/（P1 腿）
② G18/G20：Playwright 离线上下文（route abort 一切外部 host）——首页 / 中文搜索（「配额」「离线」两关键词）/ 正文页全可用，**零外网请求断言**（NFR-S29）；五类导航逐类点达 ≥1 页 + 篇目清单逐篇 200（安装 7 形态 + generic 接入 + API 参考三处新增为硬项——按 T-141/T-142 篇目表核对）
③ G21/G22：docs embed 增量 ≤15MB（make docs 前后二进制差）与六平台压缩产物 ≤40MB 对账（G03 交叉）；fallback 未触发或触发经用户确认记录；版本下拉含 v1.x + 搜索命中跳转正确页（P1）；报告归档

**T-147** [P0] QA：性能基准 + GA 总矩阵 + 发布清单（剧本 7/8/10） `role:qa-engineer` `area:验收（G27~G29 + G35；GA 收口）` `dep:T-143,T-144,T-145,T-146`

AC:
① G27/G28 性能（PRODUCT 成功标准全量兑现）：1000 并发混合拉取（REST GET 为主 + docker pull 循环，预热 ≥50 blob，会话/连接复用口径报告注明）退出码全 0、零 5xx、零客户端错误；≥5 分钟持续负载曲线（P1）；冷启动 <2s 三腿（darwin / linux-amd64 / 条件 windows）；空载 RSS <100MB；1GB 流式 <256MB（M1 口径复跑）；**checksum 去重断言**（跨五协议同 blob 单物理份 + stats 对账——AC7/D16/C07 手法复跑，FR-43-AC5）
② G29/G29b 基准报告：矩阵齐全含记录项（REST PUT/GET 吞吐 P95 / 搜索 P95 / export-import 吞吐 / **GC dry-run/apply 吞吐基线**——CLI 双遍 mark 数据采集供 §6.4 #10 M6+ 决策，零优化 / docs/console 首屏）；与 M4 基线（T-105）对比 P95 无 >20% 回退（回退项归因）
③ G35 GA 总矩阵 + 发布清单：FR-34~FR-42 每条 AC × 部署形态 × PASS/FAIL/降级三态汇总表（GA 验收唯一索引）；降级腿逐条引用 Q3 用户确认记录；**发布物 sha256 清单产出**（六平台 + checksums + 离线包 + docs-static tar）；发布执行建议（Q1 渠道：GitHub Releases + ghcr.io + Chart 仓库随 K2）——**推送动作归 conductor + 用户逐项确认，本票止于清单**（安全底线）；报告 `reports/agents/T-147-qa.md`（M5 QA 总报告 + GA DoD §9 第 1/2/5 条结论）

---

## 二、分批派发表（宽度 ≤4，批内 area 不重叠）

| 批次 | 票 | 宽度 | 说明 |
|---|---|---|---|
| 1 | T-127 / T-128 / T-129 / T-130 | 4 | 脚手架 + 架构终裁最前（T-130 收敛 K1/K2 窗口）；T-127 与 T-129 同为 devops 但 area 逐文件不重叠（.goreleaser/Makefile-release 段 vs docs-site/internal/docs/Makefile-docs 段——**Makefile 追加式双写，派单注明目标段边界，冲突则 conductor 串行收编**，R1）；T-128 与 T-127 均触 httpapi 但文件不重叠（storage/repo+migrations vs health.go） |
| 2 | T-131 / T-132 / T-133 | 3 | FE 删兜底（T-128 合入后——硬约束一）；镜像（T-127 后）；token 审计独立（httpapi token 族与批 1 health.go 文件级不重叠——派发时确认 T-127 已收口） |
| 3 | T-134 / T-135 / T-136 / T-137 | 4 | FE docker 视图（T-131 同目录串行后）；部署三产物并行（deploy/compose vs charts/ vs deploy/k8s 互不重叠，均 dep T-132） |
| 4 | T-138 / T-139 / T-140 / T-142 | 4 | systemd（contrib/systemd）；离线包（deploy/offline + Makefile 新目标——批 1 后无并行冲突）；uri 修正 P2（httpapi/storage.go——批 1/2 后无冲突）；文档内容下半（API/FAQ/增补——T-129/T-128 就位） |
| 5 | T-141 / T-143 / T-144 | 3 | 安装文档（产物票全 done 可终验）；安全审计（扫描对象齐）；QA 第一段（债务+回归——被测面全冻结） |
| 6 | T-145 / T-146 | 2 | QA 双线：发布矩阵（docker/kind 面）与文档中心（Playwright 面）**独立实例 + 端口段隔离并行**（T-106 先例 18160+；若 conductor 判污染风险则串行，T-146 顺延） |
| 7 | T-147 | 1 | 性能 + GA 总矩阵 + 发布清单（收口票；发布推送归 conductor+用户） |

review 强度汇总：**双 reviewer（硬性）= T-128（正确性+架构：FR-44 行为变更面 + 存量迁移）、T-132（安全+架构/供应链：K2 镜像供应链面）**；单 reviewer = T-127、T-129、T-131、T-133~T-142；T-130 文档票 conductor 核验直收；QA/审计票（T-143~T-147）按 M1~M4 惯例无 code review，报告 + conductor 复现核验（T-143 的扫描结论由 T-147 抽样复跑交叉验证）。

**关键路径**：T-127 → T-132 → T-136/T-137 → T-139 → T-143 → T-147（发布链 6~7 票 7 批）。次链：T-129 → T-141 → T-146 汇入 T-147（文档链）；T-128 → T-131 → T-134 → T-144 汇入 T-147（债务链）。尾部 T-143~T-146 四票收敛于 T-147 为 GA 收口固定成本。

---

## 三、风险清单（R1~R10，供 conductor 转交）

- **R1（批 1 派发边界）Makefile 双写**：T-127（release/release-snapshot/check-size 扩展）与 T-129（docs/docs-size）同批并行均追加 Makefile 目标——段不相交、纯追加，合并冲突概率低；派单注明各自目标段，若 git 冲突 conductor 串行收编（≤10 行追加，无语义耦合）。
- **R2（构建链耦合时序）docs embed 与 goreleaser**：T-127 先行时 internal/docs 未存在，release-snapshot 产物不含 docs 增量（G03/G21 不完整属预期）；T-129 合入后自动携带（embed 经 go:build 无配置耦合）；**G03/G21 体积对账必须在两者合入后执行**（归 T-146）；docs 增量超 15MB 或总量超 40MB 触发 ADR-0011 fallback 须用户确认——预留 fallback 微票位（不占本 21 票额，触发才立）。
- **R3（断言反转时序，M4 R5 同款）**：隐式目录 404→200、--version dev→<VER>、/binflow/docs 404→200、docker image 目录可浏览——存量断言由对应票（T-128/T-127/T-129）**同批更新并注明来源**，防 CI 批 1~4 中途红；T-144 终判 + §5.6 反转表为唯一锚。
- **R4（FR-44 时序硬约束）**：T-131 必须在 T-128 合入后派发（先删兜底会回归 404）；T-134 又串行 T-131（同目录）——BE 延期则 FE 链整体顺延，结构不变。
- **R5（K1/K2 终裁窗口）**：T-129 按 K1 暂行形态、T-132 本地 tag 先行——T-130（批 1）终裁若推翻，改动面预计收敛在 docs-site 配置层与 deploy/release 脚本常量；若 K1 推翻搜索方案属结构性改动（如换外挂索引），conductor 评估追加微票而非改 T-129 范围。**T-132 的 ghcr.io 推送面（命名空间/浮动 tag）在 T-130 终裁前不执行**——本地 tag 与 manifest 断言先行不受阻。
- **R6（Q3 条件腿）**：G05（windows 锁运行时）/G15b（systemd 真机）`dep:用户环境`——到位前 T-106 降级口径（静态验证 + 记录 + 逐条用户确认），到位后补跑不受 m5-done 阻断（PRD §9-③）；K8s 面全部 P0 由本地 kind 承担，不依赖用户环境（Q3 定案）。
- **R7（发布动作安全底线）**：ghcr.io / GitHub Releases / Chart 仓库推送 = 对外发布——**逐项用户确认 + 凭证用户注入不入库、不入 CI 明文**；全部票（T-127/T-132/T-147）止于本地归档/清单，推送动作归 conductor + 用户（DoD 第 7 条）；`m5-done` + `v1.0.0` 双 tag 由主会话执行。
- **R8（qemu/binfmt 依赖）**：arm64 腿（G04b/G07b/trivy arm64 扫描）依赖 qemu/binfmt——Docker Desktop 默认可用、Linux 裸机需注册；不可得时降级为 manifest list 断言 + 记录（P1 腿不阻塞）；QA 报告注明手法（T-145/T-143）。
- **R9（15 分钟口径归因）**：G10 计时含镜像内 console/docs 自建（文档路径的一部分，T-106 先例）；超时归因转 tech-writer 文档优化或产物票构建缓存，不是 compose 产物缺陷——防误打回 T-135。
- **R10（P2/条件腿与 M6+ 台账，供录板不占票额）**：本拆解 P2 = T-140（FR-47，m5-done 前补收）；P1 条件腿 = G05/G15b（Q3）；**M6+ 债务沿 PRD §6.4 速裁表录板**：remote docker FetchError、FE 工程债批量、audit/搜索 path 过滤、导航 499/WARN、users DELETE、T-96 NB 池、web_sessions 周期清扫、docker blob GC 候选（#5）、GC 双遍 mark 优化（#10）、immutable tag/retention、包管理器分发、en locale、Prometheus/OTel、S3 后端。

---

## 输出契约

- 状态: done
- 产出: /Users/lzw/dev-center/reports/agents/T-126.md（本文件，票单唯一全文）；BOARD.md 录入由主会话执行
- 统计: 21 张票（P0 x18 / P1 x2 / P2 x1），首波可并行 4 张（T-127/T-128/T-129/T-130），最大波宽 4，共 7 批
- 依赖链: 关键路径 T-127→T-132→T-136/T-137→T-139→T-143→T-147（发布链）；次链 T-129→T-141→T-146 与 T-128→T-131→T-134→T-144 汇入 T-147
- 风险/待澄清: R1~R10；无 PRD/ADR 正面冲突（M4 R1 形态的派发门槛不存在）；R5（K1/K2 终裁窗口）与 R7（发布安全底线）为 conductor 执行约束；Q3 条件腿与 M6+ 台账按 R6/R10 录板
