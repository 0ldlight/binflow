Ticket:       L026-6 — D08 release-bundle 六矩阵行实现（D08-R01~R06，absent → implemented）
Role:         dev-go-core（area 实例：internal/httpapi 为主 + internal/bundle 域扩展）
Area:         internal/httpapi（release_bundle*.go 文件族 + 路由注册）+ internal/bundle（系统仓/配置旋钮域模型）
Input:        docs/reverse/release-bundle.md §10（live-verification 各小节）+ §1 表 + §8/§9；wire 证据 reports/compatibility/l026a-wire/p01-p63 + p48b；docs/compatibility/matrix.yaml D08-R01~R06；ADR-0032 家族档位门；架构 §26.2 source-only 裁定
Changes:
- R01 装配面（release_bundle_assembly.go）：POST /api/release/bundle 重写为 AQL 装配探针——校验链（missing-AQL 400 / not-items 400 / Jackson 裸词 400）→ D03 引擎跑 AQL → 五键命中形（urn/sha256/properties/size/pkg_type），props 经 NodeProps 单独水合，pkg_type 按 repoKey 缓存取仓储 packageType 首字母大写
- R02 事务三段错误臂（release_bundle_transaction.go）：open/async-close/close/close-status 四路由；缺 JWS 分隔符 500（前缀文案）、路径回显语义（消费首段回显余段）、transaction 404 家族、良构 JWS → 签名 400 终态；status 面 handler 侧 bare "Forbidden" 管理门（p54）
- R03 store 面（release_bundle_store.go）：七臂有序链——管理门 → projectKey 404 透传（反引号嵌入）→ Jackson 解析 → signedJwsBundle null 500 泄漏 → storingRepo 校验 400 INVALID_RB_REPO 双键信封 → 默认仓解析 + release-bundles 系统仓自动建（bundle/system.go EnsureSystemRepo，幂等）→ 裸 JWS 解析 500（无前缀）→ 签名 400；OPTIONS 预检 200 "OPTIONS, PUT" + Allow 头；repositories LIST 隐藏 releasebundles 仓（p48b，单键 GET 仍可见）
- R04 查询族（bundle.go 重写）：names map（{"bundles": {name: [versions]}}）、versions 恒 200 []、描述符 GET/HEAD/status/artifacts 三套 404 文案族（"Bundle not found" / name:version not found / "Release bundle not found"）、?type= 投影锁定设计（source→SOURCE 记录，其余→恒空 TARGET）、HEAD 无信封 404 + X-Checksum-Sha256、DELETE main 恒 TARGET 404、/source/ DELETE 缺记录 404 / 有记录诚实 500（见 Risks）
- R05 配置面 + v2 读面（release_bundle_config.go / release_bundle_v2.go）：GET/PUT /api/release/bundles/config（默认 720、202 纯文本回显、负值 400）；fat_manifest 非 list.manifest.json 400 p47 逐字、合规名 404 UNKNOWN；v2 面（names/received/records 分页回显/statuses/单查三族 404 含 release-bundles-v2 与 -jfds 泄漏）/api/v2/audit 七字段校验 400 + 405 + 完整体 E-26
- R06 权限面（测试文件落证据）：POST 路由门 required-only（p51 普通用户可到校验臂）；管理面 handler 侧 bundleAdminGate bare "Forbidden"（p52-p55）；ANY DISTRIBUTION 非 REST 实体（单查保持 404 p62）；distribution rclass REST 建 400 p63 逐字（先于键存在性检查）
- 档位门：写面走 RequireAddon（同族 requireWebhook 先例），community 403 + X-Binflow-License-Required；读面永不过档（D1）
- metrics：删除 observeBundleCreate（POST 不再建记录），bundleCreates 家族指标保持注册
Files:
- 新增 internal/bundle/system.go（系统仓常量 + RepoStore 缝 + EnsureSystemRepo + 720 旋钮）
- 新增 internal/bundle/system_test.go
- 新增 internal/httpapi/release_bundle_assembly.go / release_bundle_transaction.go / release_bundle_store.go / release_bundle_config.go / release_bundle_v2.go
- 新增 internal/httpapi/release_bundle_assembly_test.go / release_bundle_transaction_test.go / release_bundle_store_test.go / release_bundle_config_test.go / release_bundle_v2_test.go / release_bundle_permissions_test.go
- 修改 internal/bundle/service.go（repos RepoStore + cleanupHours 默认 720 + cfgMu）
- 修改 internal/httpapi/bundle.go（查询族重写，删 handleBundleCreate 家族）
- 修改 internal/httpapi/bundle_wire_test.go（重写：空态 wire 表 + 种子记录面 + 门矩阵 + 缝断路）
- 修改 internal/httpapi/router.go（release 族路由 + /api/v2/release_bundle/ 面 + v2/audit；OPTIONS 无门）
- 修改 internal/httpapi/server.go（bundle.WithRepoStore 接线）
- 修改 internal/httpapi/repositories.go（distribution rclass 400 + list 隐藏 releasebundles）
- 修改 internal/httpapi/metrics.go（删 observeBundleCreate）
- 修改 internal/httpapi/t215_guard_test.go（manage 门账本 91→90：-1 L026-6，p51 裁定取代 T-513 的 +1；骑在 L026-7 的 -1 之后）
- 删除 无
Tests:（全部 table-driven，票号在文件头注释）
- system_test.go：EnsureSystemRepo 幂等/竞争容忍/shape；720 默认与旋钮
- bundle_wire_test.go：p01-p12 空态全表 + HEAD 无信封；种子记录下 type 投影/三 404 族/HEAD 忽略 type/DELETE 两投影；401/零泄漏/只读/Any Distribution 通道；community 403+头、pro 校验臂；metrics 暴露
- release_bundle_assembly_test.go：校验链全臂；五键命中形（props {} 与水合、includeMetaData 无副作用、空 []）；非管理员可达校验（p51）；无引擎 503
- release_bundle_transaction_test.go：p38-p43 六臂 + 良构 JWS 签名 400；status 管理门 p54；community 三端点 403
- release_bundle_store_test.go：七臂链序（含双键信封键序）、系统仓建+单 GET 可见+列表隐藏、良构 JWS 400、community 门；OPTIONS 预检（匿名 200）
- release_bundle_config_test.go：720/PUT 202 回显/round-trip/负值 400；管理门 p53；fat_manifest p47 逐字 + 404 UNKNOWN 臂 + p55
- release_bundle_v2_test.go：p13-p26/p60 全表（无 null 渲染）；audit 七字段反引号全序/部分体/405/完整体 E-26；community 403 与 405 路由事实
- release_bundle_permissions_test.go：p50/p51 用户矩阵；匿名 401 全族 + OPTIONS 例外；Any Distribution 404；distribution rclass p63 逐字（现有键同文案、federated 不被吞）
Commands:
- go build ./... && go vet ./... && gofmt -l internal/httpapi internal/bundle
- golangci-lint run internal/httpapi/... internal/bundle/...
- go test -count=1 ./internal/bundle/
- go test -count=1 -timeout=30m ./internal/httpapi/
- go test -race -count=1 -run 'TestBundle|TestV2Bundle|TestV2Audit|TestReleaseBundle|TestAnyDistribution|TestDistribution|TestEnsureSystemRepo|TestCleanupPeriod' ./internal/httpapi/ ./internal/bundle/
Outputs:
- build/vet/gofmt：BUILD/VET/GOFMT CLEAN（无输出即无违规）
- golangci-lint：0 issues.
- bundle：ok github.com/lzwzzy/binflow/internal/bundle 17.536s
- httpapi 全量：首跑 FAIL TestRouteGateCapabilitySpellings（router.go carries 90 manage gates, want 91——p51 路由门降档后账本未跟上，已修 t215_guard_test.go 91→90 并补账本条目）；复跑 ok github.com/lzwzzy/binflow/internal/httpapi 838.781s（--- FAIL 计数 0，全绿）
- 定向 -race：ok github.com/lzwzzy/binflow/internal/httpapi 246.534s / ok github.com/lzwzzy/binflow/internal/bundle 41.231s
Compatibility:
- 契约缺失：matrix D08-R01~R06 contract_ref 均空，D08 契约票未开——本票以 docs/reverse/release-bundle.md §10 live-verification + wire p01-p63/p48b 为唯一依据（规格置信度高直实现；中/低项见 Risks）
- wire 对齐：六行全部 probed 臂逐字落死（状态码/CT/信封键序/文案）；p44 双键信封、p45 null 泄漏、p46 Jackson、p47/p48b/p49-p63 逐字
- 平台级已接受漂移（非本票引入）：JSON 渲染 Go 风格 `"key": value` vs 参考 Jackson 风格 `"key" : value`（writeError/writeJSONBody 平台姿态，difftest normalize 吸收）；401 文案 "authentication required" vs "Authentication is required"（平台级）；权限列表面预设差异（T-253/T-254 已冻结）
- 本票登记漂移：AQL 解析错误 token 提取（p33：BinFlow QueryError 子查询 token 与参考不一致——internal/search 域，非本 area，测试只钉前缀形态）
Security:
- 输入校验：family 体上限 8MB（MaxBytesReader）；路径段 percent-decode 失败诚实 400；grammar 外路径拒绝路由；repo key 走既有校验
- 权限：读面服务端过滤零泄漏（不可见名与不存在不可区分）；写面档位门 + 管理面 bare Forbidden；distribution rclass 拒建防伪装配面；匿名全族 401（OPTIONS 预检语义例外，p11 wire 依据）
- 凭证：本面无密码/token 处理；JWS 仅做形态校验不落日志；日志无凭证字段
- DELETE：/source/ DELETE 有记录路径诚实 500 不删数据（metadata 无 delete 缝——宁可拒绝不误删）
Performance:
- 无新热路径测量需求（纯 REST 元数据面，无批量/流式）；pkg_type 按 repoKey 请求内缓存；names map O(名×版本) 与 store 同阶；EnsureSystemRepo 幂等（已存在路径零写）
Risks:
- UNKNOWN 未实现（wire 未覆盖，不猜）：store 签名后三态 202/200/409（§3.1，探针实例亦不可达）；事务 201 成功形；fat_manifest 合规名成功臂（现 404）；v2 audit 完整体成功形（现 E-26 404）；v2 records ?limit/?offset 自定义回显（现工厂值）
- spec-pending（成功形未捕获）：names map 行形状（版本字符串最小读法）；versions 行形状；描述符 GET 成功形（BinFlow-native）；artifacts 成功形（BinFlow-native）
- 已知缺口：/source/ DELETE 有记录 → 诚实 500（metadata.BundleStore 无 delete 缝，需 metadata 票）；配置旋钮仅内存态（重启回 720，ponytail 注释在 system.go，需持久化票）；config PUT 负值 400 文案 BinFlow-native 未探针
- jfds 仓语义中置信度（§10.6）：文案逐字但置备语义未验证
Blockers: 无
Next:
- 差分可达性登记：本面在 dev community 档被档位门挡（写面 403）——差分票需 pro 模拟档腿才能打到写臂
- matrix D08-R01~R06 状态 absent→implemented + 本报告漂移条目归档：交 compatibility-engineer
- 后继票建议：① metadata DeleteBundle 缝（补 /source/ DELETE 成功臂）；② 配置旋钮持久化；③ internal/search QueryError token 提取对齐（跨域）；④ D08 契约票（把 §10 固化为契约+golden）；⑤ 全局 /api/v2 未知路径默认（p21 /api/v2/keys 参考答 "Not Found" vs BinFlow notImplemented——v2 面整体裁定，本票只 scoped 到 release_bundle 前缀）；⑥ UI 消费面（L026-2 依赖本票新形状）
- L026-7 与本票共编 t215_guard_test.go 账本（各自 -1 均已记账，无冲突）
