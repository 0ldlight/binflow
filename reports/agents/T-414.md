# T-414 工作日志 — FR-135.2/135.3 FE 列选器三页推广 + member-pop hover 对比度清账

- 角色：dev-frontend ｜ 状态：done（四闸门绿 + 新 spec 5/5 + 定向回归 59 绿 + a11y-sweep 双主题 serious=0 + **净实例全量 284 passed / 1 failed〔在册假阳性家族，串行 3/3 绿甄别〕/ 24 skipped（11.7m）** + m9 usage-fanout 净实例 4/4 + ledger PASS + 服务端 diff=0 + SPA +2,183B ≤10KB）
- area：`web/src`（users/groups/search 三列表页 + pages.css member-pop）+ `web/e2e/m15/t414-columns-promo.spec.ts`（新）+ `web/e2e/m14/t387-l1-columns.spec.ts`（④腿口径更新）+ 锚册 console-ux.md v1.27 + anchor-audit STOP。**零 BE 依赖**；RepositoriesPage 行面零改动（member-pop 是 pages.css 一行色变，TSX 未触）。

## 1. AC1 三页列选（T-387 spec 形态复用）

- **共享层零重造**：直接消费 `lib/columnPrefs`（useColumnPrefs + ColumnDef，T-387 交付面）；
  菜单壳按 T-387 留痕「页内直挂换字面量锚」——三页各 ~55 行 Menu JSX，不抽公共组件。
- **per-page 键**：`binflow-console-cols-{users,groups,search}`（T-387 定式延展；读回按当页
  列集清洗 + 至少一列守卫〔最后一列 aria-disabled + toggle 层双保险〕+ 隐私模式退化会话内
  ——共享层自带，零新逻辑）。
- **列集 = 既有真实列闭集（无端点列不伪造）**：
  - users 六列：用户名/Email/组/角色/Status/操作（Realm/Last Login/Admin 布尔列 §6.9[1] 不建）；
  - groups 四列：组名/权限数/成员数/操作；
  - search 五列：仓库/路径 / 语义/大小/修改时间/sha256（SR-01 全量 E-09 背书；无 checksums
    时 sha256 列如实呈现 —，不弃列）。
- **操作列视角门控（票内定案）**：users/groups 的操作列仅 admin 在场——非 admin（readonly_admin）
  视图下列集/菜单项/偏好 id **同步剔除**（不伪造点了没反应的空控制）；readHidden 按当页列集
  清洗使隐藏项自动失效。repos 页 replications 列「菜单恒在、渲染按 Tab」先例不动（本票两页
  是角色门控非 Tab 门控，取列不在场即不入册形）。
- **落位**：users/groups 两页原无工具栏搜索面——新增 `.filter-bar` 单独承载
  `.filter-tail-actions.filter-tail-end` 尾组（pages.css 基元 T-387 已入 main.tsx 全局）；
  search 页尾组缀既有过滤输入后。列显隐只做 `{cols.isVisible(id) && <TableCell>…}` 整列
  条件包裹，单元格内层 JSX 逐字节未动。

## 2. AC2 member-pop hover 对比度（T-391 L-a 清账）

- T-391 §5 L-a 原文：`.member-pop summary` 常态即 accent——行 hover 行底（MUI action.hover 叠
  --bf-bg：亮 #e9ebed / 暗 #25292f）上亮色 4.42:1 < AA 4.5，与 `.row-link:hover` 同缺陷类，
  「同配方收口即可（一行 color-mix）」。
- 落地（pages.css 一行 + 注释）：`color: color-mix(in srgb, var(--bf-accent) 88%, var(--bf-text))`
  ——亮 5.08:1 / 暗 6.12:1（行 hover 行底），常态底上更高（无回归面）；现货 token 派生零新
  token，assert-tokens 对 color-mix var(--bf-*) 明示放行（build 内过门）。
- 承证双腿（spec test 4）：① **computed 回归腿**——summary 计算色 ≠ 裸 accent（配方被退役
  即红；hexToRgb 归一比对，computed 采样入 attachment）；② **hover 态 axe 双主题
  serious=0**——光标停行上（行底在身）+ 浮层开态全页扫 × 亮/暗。

## 3. 锚册（console-ux.md v1.27）+ 对账

- 头表票据列/状态行 → v1.27；§0 修订 +1 行；§10.5 路由表 `/search` 与
  `/admin/security/{users|groups|…}` 两行承载锚补记 T-414 批。
- §10.5 新增 **T-414 批 28 名全静态锚**：`{users|groups|search}-columns{,-menu,-reset}` 三件套
  + `users-columns-item-{name|email|groups|role|status|actions}`（6）+
  `groups-columns-item-{name|perms|members|actions}`（4）+ `search-columns-item-
  {repo|path|size|modified|sha256}`（5）。列项锚 `anchor:` 属性字面量落码。
- **锚族冻结**：三页既有列锚（users-sort-*/groups-sort-*/search-* 族）零改名；T-387 的
  repos/audit 列选锚零触碰。
- anchor-audit STOP +v1.27 假阳性 7 词（member-pop/row-link〔CSS 类钩子〕、color-mix〔CSS
  函数〕、binflow-console-cols-{users,groups,search}〔localStorage 键〕、t414-columns-promo
  〔spec 文件名段〕）。
- 对账：**ledger PASS**（A1~A4 全绿）；dead 桶回到既有 5 家族（${rowTestidPrefix}-matrix /
  groups-sort-name / node-props-cancel / tree-empty-virtual〔T-416 将消费〕/ user-form-role-*），
  **本票零新增 dead**——28 锚全有 spec 消费（spec 侧选择器全字面量，t387 纪律沿承；首轮我曾
  用模板透传形 helper 致 8 锚 dead，已重构为字面量数组入参）。
- t387 spec ④腿口径更新：「users 页零列选锚」→「repos/audit 列选锚不越界到 users 页」+
  users-columns 在场断言（三页推广后口径改写，断言语义不弱化）。

## 4. 自测证据（实际执行；净实例 = /tmp/bf-t414-fresh :8174，手作 make console + make build 二进制）

```sh
cd web
npx tsc --noEmit                    # 通过（零输出）
npx eslint <6 个改动 ts/tsx + anchor-audit.mjs>   # 通过（零输出；pages.css 不在 eslint 域）
npm run build                       # assert-tokens OK（css 12 + tsx 50）；✓ built in 5.27s；
                                    #   wire-brand-assets: 7 brand files；relink verified
node scripts/anchor-audit.mjs       # unregistered/broken 双零
node scripts/anchor-audit.mjs --ledger   # PASS（retired 全部在表；dead 5 家族全既有零新增）

git diff --stat -- '*.go' go.mod go.sum   # 空（服务端 diff=0）

SPA 预算（js+css gzip 合计）：377,868 B（改动前基线，本票开工首build实测）
  → 380,051 B = **+2,183 B（+0.58%，≤10KB 预算内）**

# e2e（BASE=净实例或脏实例如注）：
BASE=:8173 npx playwright test e2e/m15/t414-columns-promo.spec.ts --project=chromium
                       # **5 passed（23.9s）**——①users 全生命周期（开合 aria/Esc 回焦/弃勾
                       #   ±1/守卫 dispatchEvent 实证/复位存回 []/reload 持久/三键互不染）
                       #   ②groups 列选+持久+复位+键互不染 ③search 列选+URL 承 q reload 持久
                       #   ④member-pop computed 配方回归腿 + hover 态 axe 双主题 0
                       #   ⑤三页菜单开态 axe 双主题 0
BASE=:8173 npx playwright test e2e/m14/ e2e/m8/users-groups.spec.ts e2e/m8/auxiliary.spec.ts e2e/repositories.spec.ts
                       # 59 passed + 1 skipped（2.8m）——m14 全目录 + 用户组 + 搜索/仪表盘腿
                       #   + 仓库全链（含 virtual member-pop 腿 :282）
BASE=:8173 npx playwright test e2e/m8/a11y-sweep.spec.ts
                       # 3 passed（9.5m）——全路由 × 双主题 serious=0（三改动页在扫面内）
BASE=:8174 npx playwright test e2e/m9/usage-fanout.spec.ts --project=m9 --workers=1
                       # 4 passed（1.2m）——净实例（脏实例 :8173 上同 spec 3 红 = 夹具残留，
                       #   见 §5 环境注记）
BASE=:8174 npx playwright test
                       # **284 passed / 1 failed / 24 skipped（11.7m）**——唯一红 =
                       #   m8/governance-monitoring:32（在册 axe 并发负载假阳性家族，
                       #   T-387 §4/T-389 §4 同款）
BASE=:8174 npx playwright test e2e/m8/governance-monitoring.spec.ts --workers=1
                       # 3 passed（41.4s）——串行甄别绿，假阳性定性成立
```

## 5. 环境注记（本轮 e2e 的两段弯路，供 conductor 复验时避坑）

1. **宿主机过载**：全量第一/二轮跑在 load 450~630 的共享宿主机上（VMware/虚拟化 XPC 等外部
   进程），33 红 + 15 did not run（clipboard/shell/tree 等全隅，与 diff 零耦合的负载超时）；
   配额窗重置后 load 回落（11~22），净实例全量即 284/1/24。
2. **数据目录夹具残留**（e2e README 既有口径：「同数据目录连跑的夹具残留同害，T-374 §3-1」）：
   脏实例（两轮全量 + 定向的 /tmp/bf-t414 :8173，~200 个夹具仓）上 m9 usage-fanout 串行仍
   3 红——axe analyze 在页内 90s 不归；**同一二进制换净实例 :8174 即 4/4 绿**。终验请沿
   T-387/T-391 的净实例口径。
3. spec 首轮 2 处自缺陷在案（已终版修正）：①全显态点 reset 项被 Playwright 可点性拒发
   （aria-disabled——t387 在册教训，改「先弃再复位」）；②菜单开态 axe 扫撞 MUI Menu 渐入
   过渡（半透明 Paper 混合底假报 contrast，users/groups/search 三页同形 Menu 只有 search 撞上
   = 时序竞）——开菜单后 +300ms settle（a11y-sweep 250ms「卡片独立到达」先例同款）。
4. /tmp 实例：:8173（脏）已停；:8174（净，/tmp/bf-t414-fresh）留验在跑，可整删：
   `kill $(lsof -ti :8174) && rm -rf /tmp/bf-t414 /tmp/bf-t414-fresh`（按端口精确杀）。

## 6. AC 判定

| AC | 判定 | 证据 |
|---|---|---|
| AC1 三页列选开合/持久/复位（T-387 形态复用 + 键定式 + 无端点列不伪造 + 其余列表页不受影响） | PASS | §1（共享层复用零重造 + 三键清洗/守卫/退化全继承）；spec ①②③ reload 持久与三键互不染断言；t387 ④腿改写后绿；59 定向含 m14 全目录零回归 |
| AC2 member-pop hover ≥4.5:1 双主题（T-391 配方） | PASS | §2 一行 color-mix（亮 5.08 / 暗 6.12）+ spec ④ computed 回归腿 + hover 态 axe 双主题 serious=0 |
| AC3 四闸门 + axe 双主题 serious=0 + 服务端 diff=0 + SPA ≤10KB | PASS | §4：tsc/eslint/build（含 assert-tokens）/anchor-audit+ledger 全绿；a11y-sweep 全路由双主题 + 本票 spec 两腿 axe 0；go diff 空；+2,183B |

## 7. 契约漂移

无（零 API 消费变化；纯 FE 票）。

## 8. 遗留 / 观察

1. T-419（AQL 模式）消费面就绪：其「结果表复用既有列框架与列选器（T-414 交付面）」——
   search 页 COLUMNS/菜单壳即模板；建议其列项锚沿 `search-columns-item-*` 族扩展（AQL 专属
   列如 query 耗时另立 id），锚册随票 bump。
2. users/groups 的操作列视角门控是票内定案（列不在场即不入菜单）——若后续 readonly_admin
   面 QA 想反向核对「admin 隐藏操作列后切 readonly 视图」的偏好 id 清洗行为，spec ①的
   dispatchEvent 形态可直接复用。
3. 环境两条（§5.1/§5.2）建议 qa 终验（T-421）知悉：宿主机负载窗口 + 数据目录残留——都是
   在册家族，本票两段弯路是新的实证样本。
4. 观察项（非本票）：MUI Menu 渐入期扫 axe 的假报面（§5.3②）若日后有更多菜单开态 axe 腿，
   可考虑把 settle 姿势沉进 expectA11yClean 的调用约定（e2e README 一行），本票未扩面。

## 9. 变更清单

```
web/src/pages/security/UsersPage.tsx                列选器三件套（六列闭集 + admin 视角门控）+ 整列条件渲染
web/src/pages/security/GroupsPage.tsx               同上（四列闭集 + admin 视角门控）
web/src/pages/search/SearchPage.tsx                 同上（五列闭集；尾组缀过滤输入后）
web/src/styles/pages.css                            member-pop summary 一行 color-mix（T-391 配方）+ 注释
web/e2e/m15/t414-columns-promo.spec.ts              新增（5 tests）
web/e2e/m14/t387-l1-columns.spec.ts                 ④腿口径更新（锚不越界 + users-columns 在场）
web/scripts/anchor-audit.mjs                        STOP +v1.27 假阳性 7 词
docs/design/console-ux.md                           v1.27（T-414 批 28 锚 + 修订行 + 头表 + 路由表两行）
reports/agents/T-414.md                             本日志
```
