# T-223 工作日志 — M7 文档 I：RBAC 指南 + 上传续传说明

- 角色：tech-writer ｜ area：`docs/user + docs-site`（未越界：未动 BOARD.md、未 git 提交、未触其他 agent 在途文件）
- 日期：2026-08-23 ｜ 状态：done（待 conductor 核验）
- 权威依据：ADR-0026 终版（角色模型/manage 派生/adminRole wire/readonly_group 键）、ADR-0028 终版（Close 保留语义/三径对称/sweep+TTL 唯一回收）、PRD v1.1 §4.1/§4.2（Q1/Q2/Q3 已定案口径，Q4 S3 暂行不纳入如实标注）；实测形态对齐 T-221-qa.md（PASS 16/16）与 T-216/T-229 探针。

## 一、产出清单

### 新增

| 文件 | 内容 |
|---|---|
| `docs/user/admin/rbac-roles.md`（~250 行，sidebar_position 44，进「管理指南」导航） | ① 三层模型总览（角色/target m/target rwd）；② 角色三值 + **角色×能力矩阵表**（8 行：管理读/管理写/数据读/数据写删/manage 派生/角色字段写/自助面/docker push）；③ readonly_admin 全域只读**短路**语义（target 不参与、组合=无效非非法、含实测 curl）+ 读面 11 项清单（migration 过门 501 注记）+ 变更面 403 含 GC dry-run；④ **adminRole wire**：分配（PUT 201/POST 200）、回显（单查/whoami/session）、**冲突 400**（`conflicting 'admin' and 'adminRole' fields…`）、非法枚举 400（含 kebab 拒绝）、**同 Token 即时生效**三段（403→200→403）；⑤ **manage 动作与覆盖集**：三步授权下放（POST create-or-replace 201、无 PUT /{name} 路由注记）、覆盖集 ⊆ 规则、**替换 = body ∪ 存量并集**（B1 吊销洞）、部分交集/空集 403、能做/不能做表、**manage ≠ 内容读写**正交腿（repo detail 200 / GET·PUT 制品 403 / usage 200∨臂）；⑥ `user.role.change` 审计（实测事件形态）；⑦ oidc/ldap `readonly_group` 映射键 + env 拼写 + admin_group > readonly_group > user 阶梯；⑧ 常见报错对照 6 行 |
| `reports/agents/T-223.md` | 本日志 |

### 修改

| 文件 | 变更 |
|---|---|
| `docs/user/docker-registry.md` | 新增「**大层上传中断续传（跨重启）**」节（token 说明之后）：ADR-0028 口径——三径 kill-9/SIGTERM/compose restart **对称可续传**、sweep+TTL（24h）唯一回收 + 停机保留清单 INFO 日志；**S3 后端 hard 404 限制**（Q4 暂行不纳入，如实标注）；**docker CLI 自身不续传**注记（规格级续传面向探针/自管脚本，curl 形态）；完整 7 步 curl 链（POST→Location→PATCH 512KiB→重启→**GET 204+Range**→错位 **416 空 body+权威 Range**→续块 202→PUT digest 201→回读逐位 cmp）；行为边界表 5 行；常见报错对照 +2 行（416 错位处置 / 404 BLOB_UPLOAD_UNKNOWN 三因）；头部适用版本与复验证据注记更新 |
| `docs/user/admin/groups-permissions.md`（权限页+用户页，同一篇承载） | 模型句动作集扩 `manage`（正文+三步流注释）；用户维护表 `GET users/{name}` 行加 `adminRole` 回显；新增「M7 角色字段」小节（adminRole 闭集/等价映射/即时生效/审计/越权，指向 RBAC 指南）；`?permissions` 字母集 r/w/d→**r/w/d/m**；有意不兼容表「组无 admin 位」行补「组不能授予角色」；下一步加 RBAC 链接；头部适用版本 M7 增补注记 |
| `docs/user/api-reference.md` | 头部适用版本 M1~M4→M1~M7 + M7 增补说明；DE 表**补 `GET /v2/{name}/blobs/uploads/{uuid}` 状态腿行**（204+Range、M7 跨重启存活）+ PATCH 行 416 语义；SE 表 users 三行补 adminRole（仅 admin 可写）+ permissions 三行补 manage；仓库管理域表六行逐条补 manage 持有者边界（建仓臂/删仓 admin-only 不下放）；系统端点表 readonly_admin 读面 + usage ∨-臂 + GC 含 dry-run 403；`/api/v1` 自有端点表同步；纯文本错误示例补 adminRole 冲突 400（实测原文）；下一步补 RBAC 链接 |
| `docs/user/admin/governance.md` | 审计词表安全族补 `user.role.change`（M7，detail 含 user/old/new） |
| `docs/user/README.md` | 导航：管理指南补 RBAC 页条目；docker 条目补「大层上传跨重启续传」+ M2/M7 版本标注 |
| `docs-site/sidebars.js` | 「管理指南」items 补 `admin/rbac-roles`（导航收录新页）；头注 position 注释 40-43→40-44 |

### 构建产物（随仓库，先例 build/ 入库；不提交，留 conductor）

- `docs-site/build/**` + `docs-site/.docusaurus/**`：`make docs` 全量刷新（git status 70 文件：改动 + 旧 asset hash 删除/新 hash 加入；sitemap.xml 含 `admin/rbac-roles`；sidebar JS chunk 含 RBAC 条目——已 grep 证实）。
- `internal/docs/dist/`：make docs 复制刷新（gitignored 占位策略不变）。

## 二、验证（全部实际执行）

### 1. 文档命令真实性（本机 scratch 实例 127.0.0.1:18223，当前工作树 `make build` 产物，2026-08-23）

RBAC 面（写入 rbac-roles.md 的命令与输出逐条复跑）：

```
PUT users/alice +adminRole=readonly_admin        → 201；GET 回显 {"admin":false,"adminRole":"readonly_admin"}
POST users/alice {"adminRole":...}               → 200（部分更新臂）
admin=false+adminRole=admin                      → 400 conflicting 'admin' and 'adminRole' fields…（原文入文档）
adminRole=root                                   → 400 unknown adminRole "root" (supported: user, readonly_admin, admin)
alice 读面抽查 repositories/users/permissions/audit/storage-stats → 200×5；storage/migration → 501（过门）
alice 变更面：配额写/revoke/GC dry-run           → 403×3
数据面短路：GET 未授予仓制品 200 / PUT 403 / DELETE 403
同 Token 即时生效：user 403 → 升 readonly_admin 200 → 降回 403（无重启无换发）
audit?action=user.role.change                    → detail {user,old,new} 三事件（newest-first）
V07：admin 建 t-app(manage wire)=201 → carol 编辑增 dave:[read]=201 → dave GET lib.a=200 / PUT=403
V08：t-other(disjoint)=403、t-mix(部分交集)=403、repos=[](空集)=403、建仓=403、users 写=403、
     全局列表=403、单仓详情=200
B1：carol 同名替换 t-ent（body⊆、存量⊄）        → 403（并集规则）
V09：carol2 仅 manage——repo detail=200、GET 制品=403、PUT 制品=403、usage(app-local)=200、
     usage(other-local 越界)=403
permissions 回显 manage 字母（groups app-admins:[read,write,delete,manage]）；
?permissions 视图字母集 carol2=["m"]（admin 视角）；组单查=200（同族门）
alice 自铸 token=200（限本人）；为他人铸=403；alice 改 bob 角色=403
whoami/session：{"admin":false,"adminRole":"readonly_admin"}
```

续传面：

```
make test-m7-resume            → GREEN EXIT=0（kill -9 臂全链：GET(post-restart) 204 Range: 0-524287 →
                                 PATCH 202 Range: 0-589823 → PUT 201 → GET blob 200 逐位一致）
make test-m7-resume-sigterm    → GREEN EXIT=0（SIGTERM 臂同形全绿）
自有 curl 链（18223 实例）：POST→Location；PATCH 512KiB→202 Range: 0-524287；SIGTERM 停机（serve.log
  出现 "preserving unexpired upload sessions…count":1）；重启后 GET→204+Range: 0-524287；
  错位 PATCH Content-Range: 0-65535→416 + Content-Length: 0 + Location + Range: 0-524287（权威）；
  续块 524288-589823→202；PUT ?digest→201；GET blob→200 cmp 逐位一致；
  过期腿：sqlite 回填 expires_at 过去→重启→404 {"code":"BLOB_UPLOAD_UNKNOWN"}（sweep 清行）
```

### 2. make docs（两次全量 + 链接检查）

```
make docs → docusaurus build SUCCESS；docs site size (raw total): 2.72 MB (2851342 bytes)（预算 15MB 内）
npm run build 2>&1 | grep -iE "broken|warn|error"（滤 docsDir/blogDir 噪音）→ 零输出（无断链/锚点失配，
含 rbac-roles.md → docker-registry.md#大层上传中断续传跨重启 中文锚点）
构建产物核验：build/admin/rbac-roles/index.html 存在；sitemap.xml 含 admin/rbac-roles；
root index.html 含「RBAC 角色与仓库级管理员」；docker-registry build 页含续传节；
groups-permissions/api-reference/governance build 页含 adminRole / user.role.change（grep -a 计数核实）
```

### 3. 环境与清理

scratch 实例（18223 + /tmp/t223-docs）已停已删；未触碰他 agent 实例（18218=T-218、18473=T-216 未动）；
未写任何密钥入仓库文件（口令仅存在于临时实例 env，随清理销毁）。

## 三、口径对齐说明（防漂移）

- readonly_admin 数据面：**全域只读短路**（ADR-0026 决策 1 / T-214①），未写 PRD v1.0 已否决的「同权走 target」；
- 「跨重启续传」**不限「异常中断后」**（ADR-0028 对外措辞第 3 点）；sweep+TTL 唯一回收路径写明；
- S3 续传：**不适用 + 404 形态**如实标注（Q4 暂行，未写「计划支持」）；
- docker CLI 不主动续传：按 PRD FR-67 客户端注记原文口径（中断后从头重传，规格级续传以 curl 形态）；
- permission target 编辑臂 = **POST create-or-replace（201）**，明确「无 PUT /{name} 路由」（T-217/PRD 骨架勘误非缺陷，按实际 wire 写）；
- wire 拼写：字段 `adminRole`（camel）/ 值 `user|readonly_admin|admin`（snake）/ DB 列 `role`（ADR-0026 决策 6）。

## 四、遗留 / 待确认

1. **FAQ 增补（readonly 边界/S3 404 原因）归 T-225**（M7 文档 II）——本票未动 faq.md，避免与 T-225 area 重叠；faq 的 Artifactory 对照表如需「角色」行建议随 T-225 一并。
2. **控制台只读态/角色下拉的操作说明**归 T-218 验收后补（console.md 现未描述角色 UI——服务端 API 面已齐，UI 描述待 T-218 落定再写，避免写未验收的界面行为）。
3. docs/user/README.md 的「CI 集成」「安装指南」等占位行未动（与 M7 无关）。
4. step-up（FR-68）文档明确不在本票（T-225）。
