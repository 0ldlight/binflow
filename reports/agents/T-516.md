# T-516 工作日志 — remote cache 投影与 virtual 解析序规格（Phase A / 计划票 T9）

- **ticket_id**: T-516（Phase A / 计划票 T9「remote cache 投影与 virtual 解析序规格」）
- **role**: reverse-engineer（本票实例域：remote-cache 投影 + virtual 解析序）
- **date**: 2026-09-24
- **area**: `docs/reverse/remote-cache-projection.md`（新建）+ `docs/reverse/virtual-resolution.md`（新建）
- **scope_files**:
  - 产出：`docs/reverse/remote-cache-projection.md`、`docs/reverse/virtual-resolution.md`（本日志外仅此两文件）
  - 只读分析：`/Users/lzw/workspace/artifactory-decompiled/backend/`（artifactory-core / artifactory-config / artifactory-rest / artifactory-rest-ui / artifactory-addon-rest / artifactory-api 六模块 30+ 文件；反编译目录零写入）
  - 交叉引用（未改动）：`docs/reverse/repo-semantics.md`、`maven-npm-pypi.md`、`remote-browsing.md`、`aql.md`
- **commands_run**（实际执行；路径相对 `backend/`，反编译根 `/Users/lzw/workspace/artifactory-decompiled/`）:
  - 定位与轮廓：`find .../backend/artifactory-core -name "RepositoryServiceImpl.java"`；`grep -n "public|@Path" artifactory-rest/.../RepositoriesResource.java`（7 端点）；`wc -l VirtualRepo.java`（574 行）
  - cache 投影：`grep -n "LocalCacheRepo|localCacheRepo" RemoteRepoBase.java`（18 命中）；`grep -n "localCacheRepositoriesMap|getCachedRepoConfigs" RepositoryServiceImpl.java`；`sed -n '4144,4290p' RepositoryServiceImpl.java`（rebuildRemoteAndCacheRepositories 全文）；`cat LocalCacheRepoConfig.java` / `RemoteCacheRepoTypeConfig.java` / `DbCacheRepo.java`（全文）；`grep -n "isCache|isManaged" RepositoryConfigServiceImpl.java`（6 命中）；`sed -n '440,480p'/'1690,1715p' RepositoryConfigServiceImpl.java`（-cache 创建防护 400 两处）
  - 删除/重命名：`grep -n "removeRepository" RepositoryServiceImpl.java`；`sed -n '114,240p' RemoveRepositoryServiceImpl.java`；`cat RepoRemovalStatus.java`（REPO_NOT_FOUND=404 文案）；`grep -n removeRepositoryFlowV2Enabled ConstantValues.java`（默认 TRUE）
  - 访问面：`grep -n "getTargetRepository" UploadServiceUtils.java` + `sed -n '333,348p' UploadServiceImpl.java`（404 文案）；`grep -rn "is a cache repository" mover/RepoPathMover.java`；`grep -n isCache AuthorizationServiceBase.java`（ACL 去 -cache 求值）；`grep -rn '"-cache"' artifactory-rest-ui`（14 命中：树/搜索/SetMeUp）；`sed -n '60,160p' RootFetchByRepositoryType.java`
  - 写入/读取/TTL：`sed -n '745,880p' RemoteRepoBase.java`（downloadAndSave 单飞全链）；`grep -n "missedRetrievalsCache.put" RemoteRepoBase.java`（3 命中 630/1296/1567）；`cat CacheExpiryImpl.java`（包型 checker 注册表）；`grep -n getCacheAge FileResource.java`
  - 清理：`sed -n '140,220p' ArtifactCleanupServiceImpl.java`；`cat ZapArtifactsResource.java` + `sed -n '53,101p' ZapServiceImpl.java`
  - virtual 序：`cat VirtualRepositoriesResolver.java`（四桶全文）；`sed -n '60,190p' VirtualRepositoryConfigResolverImpl.java`；`grep -n "getRepoConfigsByKeysInSpecificOrder" -A 20 RepositoryConfigServiceImpl.java`（LinkedHashSet 去重+声明序重排）；`cat VirtualResolverRequestFilter.java`；`sed -n '185,260p' RepoBase.java`（accepts 三特例）；`sed -n '74,330p' ArtifactoryVirtualDownloadStrategy.java`（三种解析模式全文）
  - Maven/npm：`sed -n '84,200p' MavenMetadataInterceptor.java` + `cat MergeableMavenMetadata.java`；`sed -n '95,130p' PomInterceptor.java` + `sed -n '40,100p' PomTransformer.java` + `sed -n '85,120p' MavenPomInterceptor.java`（上传侧仅一致性校验——修正了一处草稿误写）；`grep -n npmVirtualCache ConstantValues.java`（418-421/619-621）；`cat NpmVirtualCacheCleaner.java`
  - 聚合/统计：`sed -n '324,420p' RepositoryBrowsingServiceImpl.java`（合并覆盖规则）；`grep -n updateDownloadStatsIfNeeded StatsServiceImpl.java -A 10`；`sed -n '440,475p'/'725,745p' SyntaxSearchService.java`（remote→-cache 映射）；`grep -n undeploy VirtualRepo.java`（§7.5 勘误证据）
  - 版本核验：`for d in .../pom.properties; do grep -h version= ...; done | sort | uniq -c`（JFrog 模块 82×7.161.14、8×access 7.191.14；README 声明发行包 7.161.24）
  - 活体探查：`curl /artifactory/api/system/ping`（192.168.120.38:8082 → 200）；`curl -u <redacted> /api/system/version`（→ 401 Bad Credentials，凭据不可用）；localhost:8082 → 连接失败。**零活体实证，全部条目为反编译单源**。
- **key_outputs**:
  - `remote-cache-projection.md`：7 节——投影派生（无配置记录/命名 `-cache`/storeArtifactsLocally 门控/继承与固定字段表）、`-cache` 后缀创建 400 防护、删 remote 级联清 cache、直查/直下/直传 404/copy-move 拒绝/ACL 经 remote key 映射/UI cached 分组+树映射、写入路径 7 规则、TTL/负缓存 7 规则、清理（unusedArtifactsCleanupPeriodHours 批删 + zap=过期标记非删除）、待验证 5 条
  - `virtual-resolution.md`：9 节——成员序（声明序可配/双层去重/locals 恒前于 remotes/环剪断）、四桶精确装配（cache 入 local 桶/远端抑制/EDGE_MIXED）、请求级 9 步流程（virtual 缓存先查→精确匹配→[RELEASE]/快照模式→403 透传→本地 vs 缓存裁决→404 删缓存联动）、三层过滤时机（virtual 剪全局→嵌套剪子树带布局翻译→成员内拒绝）、Maven（metadata 合并逐规则/pom 清洗三策略与节点清单/仅下载侧）、npm（缓存仓去重/600s TTL/双 TTL 清理器/成员失效联动；逐字段合并标 UNKNOWN 引既有规格）、聚合（浏览合并 local 覆盖 remote 展示值/REST 首持有者/搜索域 remote→cache 映射/统计落 cache 仓路径+跳过条件）、**§7.5 勘误**、待验证 5 条
- **test_evidence**: 无——本票纯规格票（活体参照实例认证不可用，见 commands_run 末条；两文件中的「高」均为反编译代码逐分支可读，非动态验证）。
- **compat_evidence**: 源版本口径=7.161.24 反编译（README 声明；模块内嵌 pom 多标 7.161.14，已在两份规格文件头注明该偏差）；官方文档交叉印证仅覆盖概念名（priorityResolution/pomRepositoryReferencesCleanupPolicy/unusedArtifactsCleanupPeriodHours 字段存在性），流程级行为全部为代码补充官方规范，两文件内逐条标注「此条补充官方规范」。
- **open_questions**:
  1. `GET /api/repositories/<K>-cache` 直查响应 JSON 形态（规格 §2.1 标中）
  2. 四桶序在极端穿插声明下的最终序活体对拍（virtual-resolution §9.1）
  3. cleanup 统计基准字段与回收站行为（remote-cache §7.2）
  4. AQL 侧 remote→cache 域映射与 UI 语法搜索口径是否一致（virtual-resolution §9.3）
  5. v1 删除流（v2 开关关）下 DELETE `-cache` 的行为差异
- **decisions_made**:
  1. 两文件定位为 repo-semantics.md §7/§8 的深化增量而非重写，交叉引用不复制内容
  2. npm 包文档逐字段合并未在本仓定位到实现（闭源 addon 段）→ 按票规标 UNKNOWN 并引 `maven-npm-pypi.md` §2.6 既有口径，不扩写
  3. 「locals 恒前于 remotes」由两段代码（分桶收集 + 四段拼接）合成推演 → 标高但列入待验证清单第 1 条供差分腿复核
  4. 修正草稿期一处误写：pom 清洗策略仅下载侧生效（MavenPomInterceptor 上传侧只做坐标一致性校验），已改写后落盘
- **deferred**:
  1. PyPI/Docker/Go 等其余包型的 virtual 聚合差异（maven-npm-pypi.md §2/§3 已有骨架，本票未扩）
  2. `NestedRealRepositoriesWithRoutes` 的路由结构对 UI 结果归属展示的完整影响面（仅摘搜索域映射）
  3. EDGE_MIXED 边缘档的 cache 剔除行为细节（BinFlow 无此档，仅一行记档）
- **risks**:
  1. **规格冲突上报**：`repo-semantics.md` §8.2「DELETE virtual 下路径=逐成员查找实际持有者删除」与本票证据冲突——7.161.24 代码中 `DELETE /{virtual}/{path}` 仅删 virtual 自身存储（聚合缓存），不删成员制品（virtual-resolution §7.5，置信度高）。该行原本就标「中/待逆向增补」，请 conductor 裁定是否由本票顺带勘误旧稿（效力序：规格票 > 旧稿中置信度行；我未直接改旧稿）。
  2. 活体实例（192.168.120.38:8082，基准 7.161.26）凭据不可用 + 本地 :8082 宕机 → 全部条目书面单源；按角色规则，「高」按「反编译逐分支可读」口径给出，动态风险已在两份规格的待验证清单中逐条登记，建议下一轮差分腿优先覆盖 §7.5 勘误与 §3 第 9 条（404 删 virtual 缓存联动）。
  3. 版本标注偏差：ticket 声称「7.161.24 为主，部分 7.161.20」，实测模块 pom 为 7.161.14（发行包 README 为 7.161.24，未见 7.161.20 痕迹）——已在两文件头按 ticket 要求标注并加注实测偏差，不影响行为结论，但差分对拍时以 7.161.26 活体为准的复核价值更高。
- **handoff_to**: compatibility-engineer（两规格 → 契约化素材：cache 投影生命周期断言、四桶序断言、pom 清洗三策略矩阵、统计归属断言）；differential-qa-engineer（待验证清单 10 条 → 差分腿选题，尤其勘误项）
- **status**: done
