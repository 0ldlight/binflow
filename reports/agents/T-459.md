# T-459 工作日志 — 监控面 + 导航分组（盘上遗产续作收尾）

- **票**：T-459 [P2] FE FR-145.5/.6b 监控面 + 导航分组/侧栏过滤（M16-SPLIT 批次④ B12）
- **area**：`web/src/pages/monitoring`（新页）+ SystemInfoPage 归位 + AppShell 导航
- **执行**：前任 agent 中途亡故（无报告文件，盘上留完整足迹 + 探针证据）；本任 finisher 盘点 → 验证 → 补缺 → 收尾。**本任零新增 src 改动**——遗产经全量闸门验证后即为终态；本任产出 = 验证证据 + 缺口补齐（报告/临时抽查腿）+ 记账。
- **服务端 diff = 0**（纯 FE 票；消费端点闭集 = 既有四读面）。

## 1. 遗产盘点表（前任笔迹 vs 本任补齐）

| 文件 | 状态 | 笔迹 | 本任处置 |
|---|---|---|---|
| `web/src/pages/monitoring/ServiceStatusPage.tsx`（新，252 行） | 遗产完整 | 前任 | 验证通过（tsc/eslint/e2e 8 腿/axe） |
| `web/src/pages/monitoring/SystemLogsPage.tsx`（新，289 行） | 遗产完整 | 前任 | 同上 |
| `web/src/pages/monitoring/SystemInfoPage.tsx`（迁入 + 注记翻新） | 遗产完整 | 前任 | 验证通过；**改名事件已被 09d9311d（T-461 提交）卷入**——见 §5 |
| `web/src/main.tsx`（三 lazy + 路由重排 + 四旧深链 replace 窗） | 遗产完整 | 前任 | 验证通过 |
| `web/src/components/AppShell.tsx`（ADMIN_NAV 重排 + 管理态顶栏过滤框 + ⌘K 随模式 + admin-filter-empty） | 遗产完整 | 前任 | 验证通过 |
| `web/src/components/NavIcons.tsx`（pulse/article 两枚） | 遗产完整 | 前任 | 验证通过 |
| `web/src/lib/api.ts`（getSchedules + ScheduleStatus 投影） | 遗产完整 | 前任 | 验证通过（curl 对账 wire 形态一致） |
| `web/e2e/m16/t459-monitoring-nav.spec.ts`（新，8 腿） | 遗产完整 | 前任 | **两轮 8/8 全绿** |
| 既有 spec 翻新 ×11（auth-shell/governance/m13×2/m16×2〔t447+t457 settle 400ms〕/m8×3/security/storage_migration） | 遗产完整 | 前任 | 本任全量跑绿（§3） |
| `docs/design/console-ux.md` v1.43 / `console-artifactory-parity.md` v1.12 / `web/scripts/anchor-audit.mjs` STOP v1.43 | **已被 09d9311d 提交** | 前任执笔 | 本任复核 + `--ledger` PASS |
| `reports/agents/t459-probe/`（7.161.20 活体探针 ×6 面） | 遗产完整 | 前任 | 报告引用（§2） |
| `reports/agents/T-459.md` | **缺位** | — | **本任补齐（本文件）** |
| a11y 三态抽查（L2/过滤空态/system-info user 态） | 缺证据 | — | **本任临时腿补测，全绿后即删**（§3-4） |

## 2. 形态与契约定案（前任定案，本任复核认可）

**AC1 监控三页**：
- Service Status（`/admin/monitoring/status`）：总体徽标 + 子系统三行 = `GET /api/v1/health`（system:read）；版本 = `GET /api/system/version` 同源（useVersion 缓存）；调度服务节 = `GET /api/v1/system/schedules`（T-450 台账只读投影，cron 编辑面归 T-462）。实例 URL = `window.location.origin + /binflow`（呈现事实源）。**Uptime 无端点——如实缺位注记不伪造**；根级 `/metrics` 为 Prometheus 抓取面（Basic-auth 族，console 结构性不消费——/healthz 同款口径）。
- System Logs（`/admin/monitoring/logs`）：**载体票内定案：日志源 = 审计跟踪（`GET /api/v1/audit`）**。尾随 7s 倒计时 + Pause/继续 + 立即刷新（7.161 三件套逐一对位，探针 s1）；过滤 = 窗口客户端子串（hint 明示边界，服务端精过滤在审计页）；下载 = 当前窗口 Blob 导出 .log（BinFlow 无 Support Zone 如实换形）。7.161 的 Service/Node/LogFile 三选择器降为单源说明行 `logs-source`——不伪造选择器。
- SystemInfoPage 归位 `/admin/monitoring/system-info`：路由迁移，`settings-*` 冻结锚零改名；健康卡保留为实例信息摘要（运行面主承载移 Service Status）。旧深链经路由表一轮 replace 折入。

**AC2 导航分组（parity B-2.18）**：监控组扩为服务节点组六页（存储/服务状态/系统日志/系统信息/维护〔GC〕/备份恢复——后三页归位迁址）；Webhooks 归常规组；治理组收四页；管理侧栏 16→18 条目。认证组子项形态：7.161.20 活体六子项（探针 s4）中 HTTP SSO/Crowd·JIRA/SCIM 三域不存在（FR-92 闭集 = LDAP/OAuth/SAML）——单页三页签维持，缺位登记不伪造。**Search Admin Resources 过滤框位置按活体勘误落管理态顶栏**（探针 s3：x=1156 y=48 w=340——A1-5；票面原词「侧栏」系 7.84 经典口径）；活体上过滤无可观测效果，BinFlow 过滤真生效（客户端子串窄化 + 整组隐藏 + 无匹配注记 + Esc 清词）系对经典语义的承载，差异留痕 parity v1.12。

## 3. 自测（命令 + 输出摘要）

环境：node v24.20.0；scratch 净实例 `/tmp/t459-data`（127.0.0.1:**18110**，sqlite，admin/password；`make console` 同法 embed + `go build -o /tmp/t459-binflow-server ./cmd/binflow-server`，binary strings 验证 `status-page`/`logs-page`/`admin/monitoring/status` 在 embed 内）；跑前净窗（`ps aux | grep -cE 'playwright|go test'` 命中仅 grep 自身 + 他会话 playwright-mcp，零在跑测试）；共租机全程 `--workers=2`。

- **tsc**：`npx tsc --noEmit` → **0 错误**。
- **eslint 触碰面**（17 文件）：**0 error** / 10 warning——AppShell 6 条与 HEAD 同款同位（setState-in-effect + Menu anchorEl ref 模式，行号平移，非本票新增）；SystemLogsPage 2 条与 StorageSummaryPage 既有数据获取模式同款（warning 级为仓内既有容忍面）。
- **build**：`npm run build` → 成功（5.89s，relink/wire-brand 校验通过）。
- **锚册**：`node scripts/anchor-audit.mjs --ledger` → **PASS**（unregistered/broken 双零）。
- **新 spec**：`BASE=http://127.0.0.1:18110 npx playwright test e2e/m16/t459-monitoring-nav.spec.ts --workers=2` → **8 passed ×2 轮**（39.2s / 59.4s）——health/version/schedules 对账 + user L2 + 行对账/尾随重取/停拍零请求/手动 +1 + 过滤窄化/空态清除/下载 .log + limit=200 换档请求对账 + 分组六页断言 + 四旧深链折入 + 过滤框全链（18 条目/Esc/⌘K/模式让位）+ axe 双主题。
- **被牵动 spec 全量**（11 个）：auth-shell + governance + t366-webhooks + storage_migration → **28 passed**（52.8s）；m8 shell + governance-monitoring + security + t366-consumer → **22 passed**（1.8m）；t447 + t457（T-461 轮两 axe 红腿——前任已加 settle 400ms 处置半透明色采样假性炸裂）→ **10 passed 含两 axe 腿绿**（44.6s）；a11y-sweep（31 路由 × 双主题 = 62 扫描，含监控组四路由）→ **3 passed**（2.4m）。
- **a11y 三态抽查监控三页**（本任临时腿，双主题，serious/critical=0，测后即删）：status/logs 的 user L2 无权限态 + logs 过滤空态（29.7s 绿）；system-info 的 user 态（健康卡 403 驱动 L3 隐藏 + 实例卡可见）（8.7s 绿）。加载态为瞬态无稳定断言窗，四态纪律以默认/空/错三态落闸（默认态由 a11y-sweep + 新 spec axe 腿覆盖）。
- **SPA 预算**（gzip js+css 合计，`make console-size` 同口径；基线 = HEAD worktree 仅修 main.tsx 断链导入复原 T-461 应然态，其 gzip 合计 410,949B 与 T-461 报数严丝合缝）：**410,949 → 416,768 = +5,819B（5.7KB）≤ 10KB/票 ✓**。raw 口径参考：dist 全量 +14,502B（新 chunk ServiceStatusPage 6,230B gz 2,329B / SystemLogsPage 5,634B gz 2,778B + AppShell +1,743B + index +900B）。
- 收尾：实例已停（18110 零监听）、worktree/临时文件清零、`git status` 与开工同构（无本任残留）。

## 4. 契约注记（漂移登记）

- **System Logs 的日志源缺口（登记不伪造）**：BinFlow 无服务进程日志（slog 文件）的 REST 尾随/下载端点（internal/httpapi 全量无日志面）——本票按「零新端点优先」以审计跟踪承载；真要 7.161 形态的 Service/Node/LogFile 三选择器须 **PM FR 增补 + BE 新端点族**（建议票已由 parity v1.12 登记）。
- **Uptime 缺口**：health/version 均不含进程启动时间——status-uptime-gap 如实注记，不伪造。
- **wire 对账**：`/api/v1/system/schedules`（camelCase 投影）与 `/api/v1/audit`（newest-first + limit）实测与 api.ts 类型一致——无漂移。

## 5. 收编注意事项（conductor）

- **09d9311d（T-461 提交）卷走了本票三件**：SystemInfoPage 改名（R100 admin/→monitoring/，致 **HEAD 单独 tsc 断链**——main.tsx 仍 import `./pages/admin/SystemInfoPage`）+ console-ux v1.43 + parity v1.12 + anchor-audit STOP v1.43。**本票工作树（main.tsx 导入修正 + 全部 src/e2e）合入后即自洽**——T-459 提交务必整树收编，勿再拆分。
- 工作树内 `web/src/pages/monitoring/SystemInfoPage.tsx` 的未提交 diff 为注记翻新（路由迁址说明），与已提交的改名是同一票的两半。
- 建议票（非本票范围）：日志尾随/下载端点族（§4）；调度 cron 编辑面归 T-462 既定。

## 6. 结论

AC1~AC4 全数落定（AC3 SPA +5.7KB 在预算内；AC4 四闸门 + axe 双主题 + 三态抽查全绿）。状态：**done，待收编**。
