# T-396 — M14 QA 中期回归工作日志

- 票：T-396 [P1] QA 中期回归（L01~L19 首跑 + M1~M13 P0 双形态抽样 + 归属审计 + 改判预核实 + E1~E7 复核 + 四闸门/race/资源门 + 漂移处置态核对）
- 角色：qa-engineer ｜ 日期：2026-09-01 ｜ 被测：**HEAD `a5eebe0`**（develop；`git archive HEAD` 全量 scratch `/tmp/bf-t396` 自建——`make console`（npm ci 同锁）+ `make docs` + `make build`，零工作树污染、零 git 写操作）
- 结论：**FAIL（唯一红面 = 缺陷 D-396-1，M9 回归面 spec 漂移 3 腿确定性红）**——其余全绿：L01~L19 首跑全绿、P0 抽样双形态全绿、归属审计 100% 票号、改判预核实绿、E1~E7 零倒退、四闸门 + race 33 包 + 资源门三连全绿、漂移池 2 清 6 在册处置态明确。D-396-1 为 spec 迁移缺失非产品行为缺陷（NFR-P39 数据面预算 3≤3 未破），修复面一行。
- 环境注记：`bin/bf license issue` 铸的 30 天 pro 文档在 **HEAD 原味二进制上验签必败**（工作树开发签发钥公半 `47e286d4…` ≠ 内嵌验签钥 `9e4449a7…`，T-281 首发三步流程第 2 步「换 verifykey.go 常量重编译」未走过）——pro 形态腿用 **scratch 内换钥重编的 QA 专用二进制**（`bin/binflow-server-pro`，仅 /tmp，不入库）；license 安装契约 = **raw 文档体**（JSON 包裹体按坏文档拒，非漂移）。

## 0. 编排（scratch 实例 + dind + kind，全部已清）

| 实例/资源 | 端口/形态 | 用途 | 状态 |
|---|---|---|---|
| community | 8096（HEAD 二进制 + console + docs 嵌入，seed-m8 10291 节点） | e2e 全量 / L13 npm / L14 v3-flat REST / 漂移探针 / 通用 roundtrip | 已停 |
| A / B | 18421/18422（community，B 带 REMOTE_CREDENTIALS_KEY） | L12 docker remote 全链 | 已停 |
| A2 / B2 | 18424/18425（pro，QA 换钥二进制 + REST 装 license） | pro 形态抽样（helmoci + license 面） | 已停 |
| dind 29.7.2 | `--feature containerd-snapshotter=false` + MSS 1300 clamp + insecure-registry 两端口 | docker push/pull 真客户端 | 已 rm |
| kind bf-ga v1.36.1 | ns `t396-pvc`，local-path 2Gi PVC | L14 helm uninstall PVC keep **真集群腿** | release+PVC+ns 已删净 |

---

## 1. AC1 — L01~L19 首跑（命令照 PRD §5.5 骨架）

| L | 面 | 判定 | 实测证据（HEAD scratch） |
|---|---|---|---|
| L01 | 活体核验承证 | ✅ | `reports/agents/t381-evidence/` 27 件 + `t402-evidence/` 11 件在档；parity 册 **v1.2**（V1~V8 回写 + R 系）+ package-icons README **v1.2** 在树；「以核验为准」残留仅在诚实降级位（V6c token 明文面板子项），零静默升格维持 |
| L02 | D1 抽屉化 | ✅ | `e2e/m8/setmeup-deploy.spec.ts` + keyboard + m9/oidc-stepup 定向 **15 passed + 2 skipped（armed 腿按探针 skip——票面 T-382 armed :18094 4/4 在档，终验 T-400 复跑）**；几何断言在 spec（50vw 档/1600→800 票内实证）；`smu-*` 锚族 ledger PASS（见 AC3） |
| L03 | M1 断言收口 | ✅ | `e2e/m14/t383-newrepo-parity.spec.ts` 4/4（两段式对照/深链/440px 磁贴档/M2·M3 建仓序列零回归） |
| L04 | M3 断言收口 | ✅ | `e2e/m14/t384-usergroup-parity.spec.ts` 4/4（非 modal 反断言/页脚三联/E5 编辑路由页/RBAC 零回归） |
| L05 | L2 有意偏离维持 | ✅ | `web/src` 全树 `MoreVert` grep=**0**；仓库行动作位 = Set Me Up + Deploy（generic/maven）**无删除无 ⋮**（RepositoriesPage.tsx:738-757）；删除走详情危险区（m8 spec 绿） |
| L06 | Tokens 真身 | ✅ | `e2e/m14/t386-tokens-page.spec.ts` 8/8；`PlaceholderPage` grep=0；一次性明文/吊销/readonly 臂全绿 |
| L07 | L1 列选/刷新 | ✅ | `e2e/m14/t387-l1-columns.spec.ts` 5/5（per-page localStorage 持久/reload 保持/全选复位/刷新取数） |
| L08 | F2/N2 | ✅ | `e2e/m14/t388-f2n2.spec.ts` 6/6（插画槽双态 + 18 条一级条目 16px mono 图标，V5 口径） |
| L09 | 品牌六用例 | ✅ | `e2e/m14/t389-brand.spec.ts` 3/3；静态核：favicon.ico **MS Windows icon resource 3 icons**（16/32/48 BMP-in-ICO）+ PNG 16/32/48/180/512 全在（`web/public/brand/`）+ manifest PWA + docs-site navbar `lockup-horizontal/srcDark` 双主题（docusaurus.config.js:101-103）；`◆` 残留 grep=0；SVG `<text>`/`font-family` grep=**0**（35 资产文件）；INTER-LICENSE 不适用（K56 手工勾画定案）路线正确 |
| L10 | 图标四消费点 | ✅ | `e2e/m14/t390-pkg-icons.spec.ts` 5/5；`CLIENT_PKG_META` **字符图标**（▫⬢⌬⬒⬓）grep=0（常量本体仅存 id/label/desc 元数据——AC 的退役对象是字符图标）；30 枚 mono/brand 双版在树；`--bf-pkgicon-*` 提亮档在 tokens.css（K61） |
| L11 | a11y/FE 债 | ✅ | T-391 color-mix 配方在树（pages.css:59-66 注释含亮 5.08/暗 6.12 实测值）；`web/e2e/README.md` 两行注记在档（§1 纯净 community 前提 / §2 dind snapshotter）；axe 双主题随全量 sweep 绿 |
| L12 | docker remote 全链 | ✅ | live：community 建仓 **200** / docker virtual **400 逐字**（"virtual docker repositories are not supported (docker serves local and remote…)"）；dind push digest `sha256:96e9340a…` → **经 remote pull RepoDigest 全等**；blob 逐出腿（rmi+prune 后重拉）上游 GET **+2**（config+layer 逐 blob 回源）→ 再拉 **delta 0 冻结**；**停上游**：已缓存 tag dind 拉成功、未缓存 tag **404 MANIFEST_UNKNOWN + upstream unreachable 摘要，零 5xx**；Bearer 全舞步 = 单测 `TestT392RemoteBearerChain`（race 轮绿）+ BinFlow 闭上游 Basic 凭据链票内 #6 在档（本轮未重演 mock realm 腿——T-392 一次性 mock 未入库，登记见 §4 观察③） |
| L13 | npm legacy login | ✅ | 真客户端 npm 10.9.8：expect 伪 TTY `npm login --auth-type=legacy` → **Logged in on http://127.0.0.1:8096/binflow/api/npm/npm-local** → `.npmrc` 改写 `_authToken` → whoami **admin** → publish **+ t396-qa-pkg@1.0.0** → install+require `t396` → cache clean 重装 require `t396`；服务端行 `PUT …/-/user/org.couchdb.user:admin 201 user=anonymous`；**负臂实证 K60-1 钉死**：无 npm 仓实例上同端点 401（建 npm-local 后即绿——类型钉死不外溢） |
| L14 | 小票包 | ✅ | **PVC keep 真集群腿**：kind 上 helm install（PVC Bound + `helm.sh/resource-policy: keep` live）→ 容器内建 `t396-marker` 仓 + 制品 → **helm uninstall 后 PVC/PV 双幸存**（同 Volume `pvc-9ae02833` Bound）→ 重装同 release → **marker 制品读回 `t396-pvc-keep-marker` 逐字**；helm lint 0 failed + 四态 template（默认 keep / 用户注解冲突 keep 单键胜 / existingClaim 不产出 / disabled 不产出）+ NOTES.txt 手动清卷双路在档；启动日志措辞 live（`"config":"built-in defaults (no custom binflow.yaml; environment overrides applied)"`）；**v3-flat 409**：`BINFLOW_T341_CURL_E2E=1 BINFLOW_T337_CURL_E2E=1 go test -run 'TestV3LiveCurlMatrix|TestLiveCurlMatrix'` 双 PASS——四臂逐字：异字节 w-only **409**（"A package with the provided ID and version already exists"）/ 同字节 w-only **409**（翻转本腿）/ d 覆盖 **201** / 新包 **201**；D-10 v2 腿维持 409 |
| L15 | 全量回归 + 归属 | ✅（归属面）/ **❌（m9 面 = D-396-1）** | e2e 全量 **265 passed / 7 failed / 32 skipped（6.3m）**——4 红为本 scratch 未嵌 docs 的环境 gap（补 `make docs` 后 t146 定向 **8/8 绿**）；**3 红为 m9/usage-fanout 确定性失败 → 缺陷 D-396-1（§2）**；服务端/FE 归属审计见 AC2 |
| L16 | （归 T-400 终评） | — | E1~E7 豁免复核本轮已做（AC2）；矩阵逐格终评不在本票 |
| L17 | NFR | ✅（附 D-396-2 记账发现） | race 全树 **33 包全绿**（internal 30 + cmd 3，httpapi 596s/repo 582s 最重，零 DATA RACE）；footprint **vmmap 13.9MB@ready**（预算 100MB）+ 冷启动 **718ms**（<2s）——`footprint-probe.sh --expect` GREEN；check-size：binflow-server **35.28MB/40MB** 绿、bf 15.28MB 触 15MB **WARNING**（m13-done 同为 15.28MB，维持态非 M14 增量；六平台聚合 face 归 T-399/T-400 release 腿）；SPA gzip **377,642B / 5MB 硬门 7.2%**——**累计增量 +24,792B 超 NFR-P61 ≤10KB 行**（详见 §2 D-396-2） |
| L18 | 文档实测 | ✅ | remote-virtual.md docker remote 专节（M14 banner + `/v2` url 形态 + live 证据行）+ helm.md（install/helm.md）卸载留卷节（按名/按标签双删法 + 三条边界）+ api-reference npm couch 族 +2 行（L81/82 含幂等再铸不 409）+ FAQ M14 增补两问在树；本轮实测命令与文档口径一致（npm login 命令照 docs/user/integrations/npm.md 形态起） |
| L19 | replication ①② | ✅ | ①锚定承证：parity v1.2 R1~R10 + `t402-evidence/` 11 件；②实现：`e2e/m14/t404-replication-crud.spec.ts` 8/8 + **T-404×T-405 联合腿 live**：POST 201 全形回显 → `PUT /binflow/api/v1/replications/{id}` **200 + GET 投影原形**（enabled 翻转 + updated_at bump）→ 缺 enabled **400 逐字** / 未知 id **404**（"replication config not found: 99999"）/ 非数字 id **400** / 匿名 **401** / 非 admin **403** / 审计词 `replication.config.update`（detail enabled/name）→ DELETE 204。T-404 的「PUT 形状假定」与 T-405 as-built **逐项吻合**（漂移池关闭） |

**m14 定向套件**：`npx playwright test e2e/m14/` = **42 passed + 1 skipped**（t383×4 / t384×4 / t386×8 / t387×5 / t388×6 / t389×3 / t390×5 / t404×8）——与 T-404 报告数字一致。

## 2. 缺陷与发现

### D-396-1 [P1]（唯一 FAIL 面）— `e2e/m9/usage-fanout.spec.ts` ambient 白名单未随 T-404 第 8 列迁移：3/4 腿确定性红

- **复现**（HEAD `a5eebe0` scratch，:8096 seed 实例）：

  ```sh
  cd web && BASE=http://127.0.0.1:8096 npx playwright test e2e/m9/usage-fanout.spec.ts:139 --project=m9 --workers=1
  # ✘ admin: used column hydrates from ONE usage batch … (3.5s)
  #   Error: ambient shell bootstrap pinned to the known pair
  #   (got: GET /api/v1/session | GET /api/v1/replications | GET /api/system/version)
  # 同样红：:204（readonly_admin）/ :228（user u8）——单腿隔离复跑同错，串行不绿 ⇒ 非在册 flake 家族
  ```

- **根因**：T-404 仓库列表第 8 列在 local Tab 无条件 `listReplicationConfigs()`（RepositoriesPage.tsx:406）→ 页面数据面新增 `GET /api/v1/replications`；spec 的 ambient 过滤正则 `/\/api\/(repositories|v1\/storage\/usage)/`（usage-fanout.spec.ts:130）未把它归入数据面 → 落进「ambient 恰好两枚」精确白名单 → 三腿全红。
- **产品面判定：无缺陷**——NFR-P39 数据面预算 = repo list 1 + usage 1 + replications 1 = **3 ≤ cap 3 未破**；缺的是 spec 归类（一行正则补 `v1\/replications`）。
- **影响**：M1~M13 P0 回归面在 HEAD 全量必红（本轮全量 3 红即此）；**T-404 报告「m9 21 passed + 1 skipped」在 HEAD 不可复现**（票面自测声明与树不符，需 conductor 知悉）。
- **建议归属**：T-404 追加小修（spec 一行 + 引一句 NFR-P39 口径注记）；P1。

### D-396-2 [P2·记账] — NFR-P61「SPA gzip 增量 ≤10KB」按里程碑累计口径已超（+24.2KB）

- 实测：m13-done 基线 **352,850B**（/tmp/bf-m13 同法复 build 复核，与 T-391 报告 352,872 差 22B = dist 哈希漂移）→ HEAD **377,642B**，**累计 +24,792B**。逐票增量（T-382 +577 / T-383 +636 / T-386 +4,937 / T-387 +3,244 / T-388 +2,155 / T-390 +5,725 / T-404 +6,159 / T-389 …）各自 ≤10KB 上报，**累计口径无人看过**——正是「预算累计核对」要抓的形态。
- 拆分：**图标+logo 资产计入面 = 9,457B**（30 枚 pkg-icons 运行时剥注释/`<title>` 后 gz 实算）**≤10KB 达标**；brand SVG 3,766B（部分以独立 immutable 资产走 serveAsset 不入 JS bundle）；其余 ~11KB+ 为功能代码本体（Tokens 页/列选器/插画槽/replication 节）。
- **判定**：按 P61 括号注（「30 枚图标 tree-shaken + logo 资产计入」）的**资产口径 = 达标**；按字面**总增量口径 = 超 2.4×**。二义需 conductor 裁：建议 T-400 前把 P61 明确为「资产计入 ≤10KB + 功能代码走 5MB 硬门（现 7.2%）」，或重定基线。非功能缺陷。

### 观察项（P3，不需票或已有归属）

1. **main 侧 CI 修复未回流 develop**：`.circleci/config.yml` + `deploy/ci/uat-deploy.sh` 在 m13-done（PR #45 合并后 main 上的 `e43c780` IdentitiesOnly 修复）有、HEAD 无——`git log m13-done..HEAD` 零提交触及（tag 非 HEAD 祖先）。**T-399 UAT 腿若从 develop 起构建需知悉**（UAT SSH 间歇 Permission denied 的已修形态只在 main）。
2. parity 册 **D1 行仍标 △**（v1.2 亦未翻）——T-382 遗留 2 在册（ux/conductor）；N2/L1/F2 行 △ 属 T-400 L16 终评节奏，非遗漏。
3. T-392 的 **mock Bearer realm 上游为一次性脚本未入库**——本轮 Bearer 舞步以单测（race 轮 `TestT392RemoteBearerChain` 绿）+ 票内 live 证据承证；建议后续票把 mock registry 夹具化（候选池）。
4. `bf license issue` 默认 `--key ./binflow-license-private.pem` 铸的文档对**原味 HEAD 二进制必然 LICENSE_INVALID**（验签钥常量未换）——T-281 首发三步流程第 2 步从未执行；QA/pro 腿现况 = 各自换钥重编（T-373 与本轮同姿势）。建议 conductor 在 release 面登记（首发换常量 or 测试验签钥注入缝）。
5. m8 README §1 运行口径只提 `make console && make build`——t146-docs 四腿需 `make docs` 嵌入（本轮首跑 4 红全为此）。README 一行可省后来者 10 分钟（归 T-398 docs B 顺笔候选）。

## 3. AC2 — P0 抽样 / 归属审计 / 改判预核实 / E1~E7

### 3.1 M1~M13 P0 双形态抽样

| 面 | 形态 | 结果 | 证据 |
|---|---|---|---|
| docker | community live（dind） | ✅ | §1 L12（remote 全链 + 三态矩阵 + 降级） |
| docker local/virtual | community REST | ✅ | local 200 / virtual 400 逐字（文案含 "serves local and remote"——T-392 翻转后形态） |
| helmoci（M13 回归面） | **pro live** + community 负 | ✅ | pro A2/B2：helmoci local/remote 建 200；helm 4.2.4 push `Digest sha256:2ede952f…` → **经 remote pull Digest 全等**（首拉上游 GET 恰一轮 manifest+2 blobs → 重拉 **delta 0**；values 内容读回逐字）——K54 共享缝与 docker remote 改动**共存零回归**；community 负臂：helmoci 建仓 **400 逐字**（"license tier 'community' < 'pro'"） |
| npm | community live | ✅ | §1 L13 全链 |
| nuget（T-378/T-401 面） | 真栈 curl | ✅ | §1 L14 双矩阵（v2 409 维持 + v3/flat 四臂 409/201） |
| generic | community live | ✅ | PUT 201×2（同字节双节点）→ 读回 sha256 全等（36e14b8a…）；**blob 去重**：checksum 寻址 `blobs/36/36e14b8a…` 恰一份 |
| license 门 | community + pro | ✅ | community 负臂见 helmoci；pro 安装面 live（REST raw 体 201 + GET 回显 tier=pro/daysToExpiry=29）；license 单测族随 race 绿 |
| maven/pypi/goproxy/cargo/conan/rpm/deb | 套件承证 | ✅ | M14 服务端零增量（见 3.2 归属——这些 adapter 无 M14 diff）；race 全绿（各 adapter 包 ok）+ e2e 全量对应 spec 绿；真客户端 live 腿以 T-373（2026-08-31 m13 基线）承证，T-400 全矩阵复跑 |
| **m9 usage-fanout（回归面）** | — | **❌** | **D-396-1**（§2）——抽样面唯一红 |

### 3.2 契约归属审计（`git diff m13-done..HEAD -- internal/ cmd/ charts/`）

m13-done 非 HEAD 祖先（main 侧 PR #45 合并点）→ 按 T-373 同款树对树口径 + 提交映射。**服务端 28 文件 = 4 提交 100% 归属 M14 票号，零孤儿**：

| 提交 | 票 | 面（文件数） |
|---|---|---|
| `30de921` | **T-392** | internal/repo/{api,service,validate}.go + 9 测试（docker remote 矩阵翻转 + 断言翻转 7 处）+ adapter/docker/remote_pull_test + adapter/helmoci/remote_test + httpapi/{compat_test,t80_repo_model_test}（12 文件） |
| `d6395c1` | **T-394** | httpapi/router.go（couch 门）+ t394 测试×2 + adapter/npm/session.go（注释）+ cmd/binflow-server/main.go + t394_config_wording_test + charts/{pvc.yaml,values.yaml,NOTES.txt}（9 文件） |
| `ab01e3d` | **T-401** | adapter/nuget/{flat.go,flat_test.go,v3live_curl_test.go,client_e2e_test.go}（4 文件，area 四文件纪律相符） |
| `b2769a3` | **T-405** | httpapi/{replication.go,router.go,t405_replication_put_test.go,t215_guard_test.go}（4 文件；internal/replication **零改动**） |

FE 面（`git diff m13-done..HEAD -- web/src`，69 文件 +3331/-367）：10 提交全 M14 票号（T-382/T-383/T-384/T-386/T-387/T-388/T-389/T-390/T-391/T-404）；T-385 无提交（票面撤销正确）。**m13-done..HEAD 仅有的 area 外差异** = §2 观察①（main 侧 CI 两文件，develop 零提交触及，非 M14 归属缺口）。go.mod/go.sum/Makefile/scripts/tools.go 零 diff。

### 3.3 改判面预核实（三改判不重翻，只核「在树 + 零形态改动」）

| 改判 | 预核实 | 证据 |
|---|---|---|
| T-383（决策项 A 撤销）零形态改动 | ✅ | `git show 15795cc -- web/src` = RepositoryFormPage.tsx **+9/-7 全部为 data-testid 节锚**（form-section-* 六枚）+ 注释——零逻辑零结构；断言在树（t383 spec 4/4 绿，含「非全程单 Dialog 反断言」与 440px 磁贴档） |
| T-384（决策项 B 撤销）零形态改动 | ✅ | `git show f32127f -- web/src` = 纯节锚/页脚三联复役锚（group-form-{cancel,reset}/节锚）——零形态；t384 spec 4/4 绿（非 modal 反断言 + URL 不离列表） |
| T-385（V4 撤旗）无 ⋮ 维持 | ✅ | web/src `MoreVert` grep=0；行动作位仅 Set Me Up/Deploy；删除仍走危险区输入确认（m8 spec 绿）——「菜单无删除」缺席精神维持，未倒退 |

### 3.4 E1~E7 豁免复核（豁免倒退 = 缺陷）

| # | 豁免 | 复核 | 证据 |
|---|---|---|---|
| E1 | 删除不进行内/无一键删 | ✅ 维持 | 行尾零删除动作（MoreVert=0 + 动作位枚举）；危险区 + 输入 key 确认维持；**Artifactory 直删对照实证在册**（V4/V8）——更严立场未倒退 |
| E2 | keyset「加载更多」 | ✅ 维持 | 「加载更多」×10 处；无页码控件引入 |
| E3 | 仓库类型 badge 中性色 | ✅ 维持 | `RepositoriesPage.tsx:711` `<Chip className="badge neutral">` |
| E4 | 权限编辑器整页 + 测试器/diff | ✅ 维持 | PermissionEditorPage + pathmatch/targetdiff 在树，m8 permissions spec 绿 |
| E5 | 用户/组编辑路由页 | ✅ 维持 | UserDetailPage 路由页在树；t384 spec 编辑态断言绿 |
| E6 | 中文文案 + 英文术语 | ✅ 维持 | 抽样：表头「包类型 / Repository Key」混排如册 |
| E7 | toast 右下锚位（再议） | ✅ 维持 | ToastContext Snackbar 容器右下堆叠形态 + error 常驻（代码注释 §3.5 口径在树）；未迁就顶部居中——待用户推翻信号，无倒退 |

**豁免倒退：0。**

## 4. AC3 — 四闸门 / race / 资源门 / 漂移处置态

### 4.1 四闸门（scratch HEAD）

| 闸门 | 结果 |
|---|---|
| typecheck | ✅ 零输出 |
| assert:tokens（随 build） | ✅ `assert-tokens: OK`（css 12 + tsx 50；vite 4.03s） |
| anchor ledger | ✅ `PASS（unregistered/broken 双零，retired 全部在表）`（锚册 v1.25 对账） |
| lint（web + go） | ✅ eslint 零输出；golangci-lint **0 issues** |

### 4.2 race 全树 + 资源门

- `make test`（-race，20m 上限）：internal **30 包 ok**（最重 httpapi 596s / repo 582s / auth 324s）+ cmd **3 包 ok**（复跑补全 make 输出被 tail 截断的 cmd 面）——**33/33 绿，零 FAIL 零 DATA RACE**。
- footprint-probe `--expect`：**vmmap 13.9MB@ready（≤100MB）+ 冷启动 718ms（<2s）GREEN**（T-392 时点 11.9MB/767ms——docs+console 嵌入增长 ~2MB，远在线内）。
- SPA/预算：见 D-396-2；check-size 单平台面见 §1 L17。

### 4.3 契约漂移登记池——处置态核对（live 复核）

| # | 漂移项 | 处置态（本轮核实） |
|---|---|---|
| 1 | **T-386：`POST /api/security/token` username=不存在 → 500（应 400）** | **在册未修，live 复证仍 500**（`{"error":"invalid_request","error_description":"token creation failed"}`）；有效 admin 200 / 空 body 200（自身铸币）。候后端小票（M14 收口窗或 M15 池，BOARD T-386 迟报在案）——非 M14 回归 |
| 2 | **T-401：docs/user/integrations/nuget.md L161「同 id/version 且字节相同 → 幂等 201」陈旧**（T-378 起过时；T-401 后同字节 w-only=409） | **在册未修，HEAD 仍原文**（§重复推送臂表第 3 行）——T-401 建议的 tech-writer 小票未立；T-398（docs B）AC 面不含 nuget.md，**需 conductor 裁归属**（建议 T-398 扩一行或立微票） |
| 3 | T-382：armed OIDC 起法缺 `BINFLOW_REMOTE_CREDENTIALS_KEY` | **已清**——oidc-config.md:102 在场（T-397 清偿） |
| 4 | T-384：parity 册 M3 行 MUI 映射（Paper）与实现（card/form-section）代差 | **在册未回写**（ux/conductor，parity 册 area） |
| 5 | T-386：console-ux §3.6.2 token 门写「admin（D3）」/ §9-R6「无列表」注记 | **在册未回写，live 复证**：非 admin（m8-e2e-user）铸 token **200**、匿名 401（as-built required-only）——ux-designer 册内文字，conductor 转交在案 |
| 6 | T-390：package-icons README §6.4 暗底口径错误 | **已清**——K61 定案 + README v1.1→v1.2 在树 |
| 7 | T-404：PUT 形状假定（在飞时） | **已清**——T-405 落地 + 本轮联合腿 live 逐项吻合（§1 L19） |
| 8 | T-391 L-b：m9 seed 并行撞（usage-fanout 家族背景） | 维持在册——**注意与 D-396-1 区分**：本轮 3 红是新的确定性成因（ambient 白名单），串行不绿 |
| 9 | T-394 遗留（whoami 403 读面 ACL / npm≥11 loginCouch 漂移 / 缺字段 400-vs-401 逐字） | 维持在册（各随既有归属窗） |
| 10 | T-392 遗留（RE-11 双计数源 / dind PMTU / WriteTimeout 姿势） | 维持在册（ROADMAP 备稿） |
| 11 | T-404：GET /api/v1/replications 无 per-repo 过滤 | 维持（客户端过滤契约注记，非缺陷） |

## 5. 遗留与移交

1. **D-396-1**（P1）：T-404 追加小修 usage-fanout ambient 正则一行 + T-400 前复跑 m9 全量（同时建议 conductor 核 T-404 票面 m9 声明的不可复现性——非追责，流程校准）。
2. **D-396-2**（P2 记账）：NFR-P61 口径二义裁决 + T-400 资源门按裁决后的口径出数。
3. nuget.md 三处陈旧的归属票（§4.3-2）。
4. 观察项 1/4/5（§2）随 T-399 / release 面 / T-398 各自吸收。
5. armed OIDC 真 IdP 腿本轮未重演（T-382 armed 证据 + 社区腿绿承证）——T-400 终验全量补。
6. 清场：dind 已 rm、kind ns/PVC 已删净、五实例已停；`/tmp/bf-t396`（含 QA 换钥 pro 二进制与全部 serve 日志/探针产物）与 `/tmp/bf-m13`（SPA 基线树）留存备复验，可整删。
