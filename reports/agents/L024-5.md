# L024-5 — 搜索族/AQL 差分 6 BUG + setItemProperties 新断链（jf 主链收官）

Ticket:       L024-5（L024-4 差分 6 BUG + 新断链 setItemProperties——jf build-publish 主链 exit=0 收官票）P0
Role:         dev-go-core（area = internal/{repo,metadata,search,httpapi} 增量）
Input:        reports/compatibility/L024-search-aql-diff.md（权威——§2 逐 case/§3 D-item 详单）；wire 证据 reports/compatibility/l024d-wire/{a,b}/（b03/b04/b04b/l03/v01 逐字核对）；docs/reverse/rest-api.md §3 L120（setItemProperties 规格）
Changes:      按报告 §3：
              1) **L4 badChecksum 双面重做**（b03/b04/b04b）：语义=**DB client-vs-server 对比**（V-z 实扫证伪关闭）——迁移 028（sqlite+postgres）nodes 增 client_md5/client_sha1/client_sha256 三列（''=未申报）；Node 结构+Put/Get/ListByPrefix/SearchByPath/usage 侧 nodeUpsertStmt 全链贯通；putNode 增 declared 参（PutWithOptions 传 expect/PutLandedBlob 传 ref/docker 清单臂零值——register），幂等重传臂同步刷新申报值；AuditChecksums 重写为纯 DB 对比（missing=bad、不实扫字节、真坏 blob 申报匹配不旗标）；行形改**平铺** `{uri,serverMd5,clientMd5}`（sha1/sha256 同构键名）+顶级 `limitReached:false`（V-z 嵌套形作废）。
              2) **L1 SNAPSHOT 行字面**：collectVersions 改「目录段字面投影，永不展开」（`1.1-SNAPSHOT` 目录 → `1.1-SNAPSHOT` integration:true；时间戳名目录 → 字面 integration:false）；展开助手 uniqueSnapshotValue 删除。
              3) **L2 v 非通配无连字符直拼**：ArtifactVersion 增 SnapshotTS/SnapshotBuild（行内最新 unique 文件解析）；latestVersion 非通配臂改 `v + <ts> + "-" + <N>` 无分隔拼接（`1.120260915.192244-1`，V-ab 关闭）。
              4) **L3 分隔符一字节**：/api/versions listFiles 行分隔 `,` → `, `（`}, {`）。
              5) **L6 毫秒截断**：节点行时钟 nodeNow()（`.000` 定宽毫秒，仅 node 行——blob/audit 等保持 RFC3339 秒；RFC3339 解析器天然接受分数位）。
              6) **L8 setItemProperties 新断链闭合（双根因）**：①**路由根因**——Go 1.17+ net/url ParseQuery 遇 `;` **整查询报错** → `r.URL.Query()` 空 → 路由看不到 `properties` 键（jf 实发 `?properties=build.name=x;build.number=y`）——路由改 rawQueryHas（'&' 容错分割）；②**解析根因**——parsePropertiesQuery 对分隔只收 `,`，jf 的 `;` 对被吞成单键垃圾值——改 `,`/`;` 双分隔（无 '=' 段续前键的多值律维持，与官方 doc 形 `k2=v1;v2` 自洽）；另加 path-matrix 形（`PUT /api/storage/<path>;k=v`）peel 臂（差分 pass4 夹具形）。handler 本体（FR-89.2）已在——纯路由/解析接线。
              7) **L5 property.key key-only 投影**：OutputField 增 PropForm（pair/key/value）；include("property.key")→`{"key":k}` 单键对象、include("property.value")→`{"value":v}`（推论形态）；**无属性行省键**推广到全部属性投影拼写（原仅 bare include）。
              8) **jf 链尾债随票收口**：/api/system/version 对未 stamp 版本（""/"dev"）回落数值形 **1.0.0**（ldflags stamp 仍覆盖）——jf strconv.Atoi("dev") 尾错自 T-508 起杀全部命令 exit code，本票验收（exit=0）的最后一块。
              9) L9（401 措辞族）既有裁定引用——未动；L7（normalize 提案）归 compatibility-engineer。
Files:        新增：internal/metadata/migrations/{sqlite,postgres}/028_node_client_checksums.sql；internal/httpapi/search_diff_semantics_test.go（三用例：latestVersion 直拼/属性双载具/property 单键投影）。
              修改：internal/repo/search_badchecksum.go（DB 对比重写）；internal/repo/search_versions.go（字面投影+快照件+助手删）；internal/repo/service.go（putNode declared 参+nodeNow 时钟）；internal/metadata/{api.go,substores.go,search.go,substores_usage.go}（client 三列贯通）；internal/search/plan.go（PropForm）；internal/httpapi/search_versions.go（L2 臂+L3 分隔+L4 平铺渲染）；internal/httpapi/properties.go（双分隔解析+peelStorageMatrix）；internal/httpapi/router.go（rawQueryHas+matrix 路由）；internal/httpapi/search_aql.go（PropForm 渲染+省键推广）；internal/httpapi/system.go（版本回落）。
              测试对位：search_badchecksum_test.go（DB 语义重写：申报匹配+真坏不旗标/平铺+limitReached）；search_versions_test.go（字面行+L2 直拼表+全字面拼写臂改 404 登记）；versions_by_properties_test.go（`}, {` 分隔）；aql_property_projection_test.go（@* 省键）；aql_endpoint_test.go（窗口族 propless 省键）；**properties_test.go（T-493「REST 只认逗号」契约翻转——raw `;` 开对为 204+双值，%3B 内容语义不变——差分权威覆盖旧裁定**）；repo/search_versions_test.go（内核字面+快照件）；metadata 四处 rewind 测试助手补 028 列 DROP（ALTER 无 IF NOT EXISTS，t212 先例）。
Tests:        新增三用例+存量五文件对位；冒烟 S1-S8（隔离实例 127.0.0.1:18093）。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/{repo,metadata,search,storage}/ -count=1 -timeout=25m 串行（ok 140.6/91.7/81.1/95.1s）；go test ./internal/httpapi/ -count=1 -timeout=25m 全量（**ok 174.5s**——含 T-493 分号契约翻转等五文件对位后）；go test -race httpapi 触碰面七测试（ok 13.5s）+ repo 内核/put 面（ok 55.8s）；冒烟+jf 真腿。
Outputs:      冒烟逐臂（对报告 wire）：S1(=v01) versions `[('2.0-20260916.120000-1',False),('1.1',False),('1.1-SNAPSHOT',True),('1.0',False)]`——字面 SNAPSHOT 行（L1）；S2(=l03) latestVersion v=1.1 → `1.120260916.130000-1`（L2 无连字符直拼）；S3(=p02) listFiles `}, {` 分隔（grep 计 1）；S4(=b03) 无申报语料全旗标平铺行 `{uri,serverMd5,clientMd5:""}`+`limitReached:false`；S5(=b04) **申报 X-Checksum-Md5 的文件不旗标（真坏字节不触发——DB 对比语义）**、未申报文件旗标，DB 列复核实存（client_md5=申报值）；S6(=q01 回归) created `…:48.134Z` 毫秒（L6）+props {key,value} 对形+无属性行省键；S7(=q03) property.key → `[{'key':'build.name'},{'key':'build.number'}]` key-only、property.value → value-only、无属性行省键（L5）；S8 **jf 2.122.0 全链 exit=0**：upload（build 标注）→ build-publish（PUT build 落库 + **Setting properties 步 204**〔query 分号对经 rawQueryHas+双分隔解析〕）→ `buildInfoUiUrl` 回显；属性步产物全量（build.name/number/timestamp+vcs.branch/revision/url 七键）+ promote 200（D3 属性通道消费回归）。version 面 `{"version":"1.0.0","revision":"dev","product":"BinFlow"}`。
Compatibility: 对照报告 §3：L1/L2/L3/L4/L5/L6 逐字/逐形对齐；L8 双根因修复（路由 ParseQuery 语义 + 解析双分隔——**两者都是本票现场实证**：单修任一根因 jf 仍 404 或落垃圾值）；L9/L7 未动（裁定通道）。**登记**：① badChecksum uri 键按仓出现差异的观察注记（报告 §3-L4）未解——恒带 uri 实现，待反编译复核；② property.value 投影为推论形态（报告明示「推论待活体」）；③ matrix 臂的 docker 清单节点申报零值（client 空 → badChecksum 会旗标 docker 制品）——A 侧行为未证；④ version 1.0.0 回落是产品版本声明（BinFlow 自有版本，非伪称 Artifactory）——ldflags stamp 优先，正式发布 stamp 后自然接管；⑤ listFiles 分隔修正连带把「单行形」样本推定到多行——报告 p02 样本即多行（2 行），一致。
Security:     rawQueryHas/双分隔解析是纯解析面（参数化存储不变）；badChecksum 仍 admin-only+ACL 过滤；setItemProperties 走既有 a(annotate) 门+ValidatePropKey/Value；版本回落不暴露额外信息。
Performance:  badChecksum 从 O(总字节) 实扫降为 O(节点) 索引读+每节点一次 blob 主键查——显著提速；毫秒时钟仅格式化差异零成本；rawQueryHas O(查询串)。
Risks:        ① 混合精度时间戳（存量秒级行 vs 新毫秒行）的字符串序——节点行无 keyset 游标（blobs 分页不受影响），AQL 排序走域内字段比较器——低风险登记；② docker 制品申报零值面（登记③）；③ matrix 臂的非属性 path-';' 段会被误判回落原样（peel 保守：无 '=' 即原样）——文档化在 peelStorageMatrix 注释。
Blockers:     无。
Next:         ① 差分复跑（search_aql_diff.py 同 case 集）验证六 BUG 全 SAME + jf 链双端一致；② D03-R10/R11/R13/R12 翻态候选；③ L7 normalize 提案归档；④ uri-per-repo 观察注记反编译复核（可转 reverse-engineer 小票）。
