# L026-7 工作日志 — dev-go-core（internal/httpapi 权限门位）

```
Ticket:       L026-7（LOOP 026 波 2：m-holder/非 admin repo 读位投影面对齐 A——L025-7 FE 双臂降级的后端根治）
Role:         dev-go-core（area 实例：internal/httpapi）
Area:         internal/httpapi 权限门位（router.go 读面门 + 投影测试翻转）；未碰 repo_config_render.go / release_bundle* / web/
Input:        reports/agents/L026-5.md §任务三（A 侧活体规格草案，置信度高，2026-09-17 双夹具 wire）+
              reports/compatibility/l026r-wire/{a-holder-*,a-noperm-*} 8 件 + b-holder-* 6 件对照 +
              known-divergence rest/m-holder-repo-read-faces（L026-5 登记，UNKNOWN 待实现）；docs/reverse/ 无独立成文条目（以 L026-5 草案为唯一事实源）
Changes:      ① router.go：GET /api/repositories 列表门 routeAuth{required,manage:CapRepoRead} → {required:true}；
              GET /api/repositories/{key} 单仓门 routeAuth{required,repoManage:read} → {required:true}（注释块改写为 L026-5 定谳口径）。
              ② repositories.go：handleRepoGet/handleRepoList 注释更新（"plain-user arm stays behind the route gate, registered for ruling"
              → 定谳落地；列表注释补「任意认证 entries 与 admin 逐字节同」）。handler 逻辑零改动——投影分叉本就在 handler
              （canManage(CapRepoWrite) 判 admin 全量 vs 非 admin partialConfigMap 四/五键分 rclass 面），列表 handler 本就全量；
              repo.Service 的 GetRepo/ListReposFiltered 仅 requireAuthenticated，门位放宽即足量。
              ③ configurations 面（handler 内 CapRepoWrite→403 Forbidden 信封）与制品下载/写面权限门零改动（写门 403 由新测试+既有套件钉住）。
              ④ handlePermissionListManage（permissions.go:754）未触碰——NFR-S49 零区分性属 permissions filter 面，与本票解耦。
Files:        修改：internal/httpapi/router.go（两处门位+注释）、internal/httpapi/repositories.go（两处注释）、
              internal/httpapi/t215_guard_test.go（t215ManageGates 92→91、t215RepoManageBits 9→8，注释记 L026-7 裁量）、
              internal/httpapi/t215_rbac_routes_test.go、internal/httpapi/t217_manage_wire_test.go、internal/httpapi/compat_test.go、
              internal/httpapi/routes_compat_test.go、internal/httpapi/t215_create_arm_gate_test.go、internal/httpapi/repo_config_projection_test.go（断言翻转，见 Tests）。
              新增：internal/httpapi/repo_read_access_test.go。删除：无。
Tests:        新增 TestRepoReadFacesOpenToEveryAuthenticatedUser（repo_read_access_test.go，票号在文件头注释）：
              双 persona（manage-only holder=真实 target wire 授 m / 零权限用户）× v1 单仓覆盖内外（键集恰 {key,rclass,packageType,description}+值断言）
              × v2 单仓覆盖内外（恰 {key,type,packageType,description}）× 列表（与 admin body 逐字节同比）× configurations
              （403 恰一条 {status:403,message:Forbidden} 信封）+ 写面反钉（create/update(覆盖外)/delete 双 persona 全 403——读面开门不带配置写权）。
              既有断言翻转七处（供 conductor 复核）：
              1) t215_rbac_routes_test.go TestT215RoleMatrixReadFace 两行 plainUser 403→200（list+单仓）；
              2) t217_manage_wire_test.go TestT217ManageHolderBoundaryLegs：GET /api/repositories as carol 403→200（注释改 L026-7 口径）；
              3) compat_test.go TestRepositoriesAuthMatrix：non-admin list/single-repo 403→200 两行（注释去"reads included demands admin"）；
              4) routes_compat_test.go TestD2D3AdminReadPlaneMatrix：表加 nonAdmin 期望列，repo list/single-repo 两行→200，stats/health 维持 403；
              5) t215_create_arm_gate_test.go TestT215ManageHolderCreateArmStaysAdminOnly：plain 用户 v1 GET 403→200（原"no m, control leg"判别腿退役，
              判别性检查移交新测试键集断言；mholder 200 行保留）；
              6) repo_config_projection_test.go TestRepoConfigKeyFacesNonAdmin："v1 plain user still gated"(403) 子测翻为
              "v1 plain user same per-rclass partial"（local 4 键/remote 5 键(url)/virtual 5 键(repositories)，与 v2 同族）；
              7) t215_guard_test.go 两计数常量 91/8。
Commands:     （主树，L026-6 churn 落盘前）go build ./... && go vet ./internal/httpapi/ && gofmt -l internal/httpapi/ → 全过（exit 0）；
              go test -count=1 -timeout=30m ./internal/httpapi/ → ok 235.745s（全包）；
              go test -race -count=1 -run TestRepoReadFacesOpenToEveryAuthenticatedUser → ok 3.828s。
              （隔离复核：L026-6 并发改 internal/bundle+httpapi/release_bundle_* 致主树暂不可编译，git worktree HEAD+本票 diff 于 /tmp/l0267wt 复跑四门）
              go build ./... → BUILD-OK；go vet ./internal/httpapi/... + gofmt -l 空 → VET-FMT-OK；
              golangci-lint run internal/httpapi/... → 0 issues.；
              go test -count=1 -timeout=30m ./internal/httpapi/ → ok 225.694s；
              go test -race -count=1 -run TestRepoReadFacesOpenToEveryAuthenticatedUser ./internal/httpapi/ → ok 4.321s。
              靶向（主树）：TestRepoReadFacesOpenToEveryAuthenticatedUser|TestT215RoleMatrixReadFace|TestT217ManageHolderBoundaryLegs|
              TestRepositoriesAuthMatrix|TestD2D3AdminReadPlaneMatrix|TestRepoConfigKeyFacesNonAdmin|TestRouteGateCapabilitySpellings 全 PASS。
Outputs:      四门全绿两遍（主树一遍、隔离 worktree 一遍，见 Commands）；翻转套件定向全 PASS；投影守窄面
              TestRepoConfigKeyFacesNonAdmin（L025-6 4/5/5 键）原样绿（全包 run 内）；L026-3 blob 回显面在全包 run 内绿。
Compatibility: 对齐 L026-5 §任务三 A 侧规格（置信度高，holder+noperm 双夹具活体）：v1 单仓四键投影（覆盖内外同形）、
              v2 单仓同族四键、列表全量 entries 与 admin 逐字节同、configurations 403 维持——B 侧原分歧两面（v1 覆盖外 403、列表 403）
              已按 A 翻正。known-divergence rest/m-holder-repo-read-faces（L026-5 登记 UNKNOWN）事实状态由本票实现变更
              （A 形态已实现）——条目状态字段更新归 compatibility-engineer（docs/compatibility/ 非 dev 可写），本报告 Next 已列。
              漂移点：无新增；unknown-key v1 GET（plain 用户可达后）维持 handler 既冻结的裸 400 quirk（L025-5/diff G5），未在 wire 内、未造新行为。
Security:     信息暴露面变化=列表面（五键 entry）+四/五键投影对任意认证用户可见——A 同形口径（Artifactory 语义下列仓不算敏感；
              制品读仍受仓权限 Can(r) 管，配置写/删仍 admin/manage-coverage 门外）。新测试反钉写面三腿（create/update/delete）双 persona 403，
              证明读面开门未带配置权。凭据面：列表/投影不涉密码 token；remote url 投影走既有 masked canonical config（NFR-S14 不变）。
              NFR-S49：handlePermissionListManage 零改动，permissions filter 面零信息面回归。
Performance:  无（纯门位删除，删两处 O(1) 鉴权调用；无 timing 断言面）。
Risks:        ① readonly_admin（BinFlow 原生角色，wire 无对应 persona）在 v1 单仓仍走 partialConfigMap 投影（canManage(CapRepoWrite)=false）
              ——与改动前一致，非本票变更面。② FE L025-7 双臂降级（web/src/repos.ts）本票不动，退役归后续票——列表 403 分支已成死臂（无害）。
              ③ L026-6 并发中主树瞬时不可编译属其在途状态，非本票引入；本票 diff 已在隔离 worktree 全绿复核。
Blockers:     无。
Next:         转 compatibility-engineer：known-divergence rest/m-holder-repo-read-faces 状态翻 IMPLEMENTED/RESOLVED（本票证据）+差分腿复跑；
              转 conductor：L025-7 FE 双臂退役票（web/src/repos.ts 读位合并 403 容错双臂可拆）；BOARD 收编本票后建议同轮跑 e2e 回归。
```
