# T-L001-1 — docker remote DIVERGENT 族清偿（LOOP 001 轨道 L001-1）

```
Ticket:       L001-1 · docker remote DIVERGENT 族清偿 · P1（清单含 P1×2 / P2×4 / P3×1 / D21）
Role:         dev-registry-adapter（领域实例：dev-package-docker）
Area:         internal/adapter/docker（+必要测试扩散至 internal/adapter/helmoci 测试、internal/httpapi 测试——票面明示合法）
Input:        reports/compatibility/L000-docker-remote-diff.md（DIVERGENT 逐 case）+ L000-docker-remote-evidence.md（E1/E4/E5/E6/E7 为契约，E4 原样为准）；docs/reverse/docker-registry.md 既有结论；internal/adapter/docker 既有 SPI 走读
Changes:      ① errors.go 新增 Artifactory 通用错误模型渲染器（statusFormBody/writeStatusFormError，pretty 2-space）② token 坏凭据三臂（RenderAuthFailure/表单凭据失败/匿名关闭）改 E1-4/E1-5 原样 status 形态，参数类 400 保留 OAuth（未观察）③ manifestUnfound(image)——message 静态 "The named manifest is not known to the registry."、detail.manifest=镜像名（E6-1）④ blobUnfound——message 静态 "blob unknown to registry"、detail.blobSum（E6-2）⑤ serveRemoteRoute 写臂改 refuseRemoteWrite：uploads POST/PATCH/PUT 与 manifest PUT/DELETE 三文案 400（E5-1..3），未观察组合保留 405+Allow ⑥ 新增 fetchList（attempt/dance 复用 + Link rel=next 翻页、上限 3=E8 默认）+ serveRemoteTagsList（实时上游聚合、pretty、name=上游回显名、失败→tags:null）+ serveRepoCatalog（/v2/<repoKey>/_catalog 路由拦截、上游聚合、失败→空列表 200=E7-2）⑦ 挑战 service=host 回显：bearerChallenge 合一构造（challenge + renderTokenAuthFailure 两处），Host 空回退 ServiceID ⑧ 测试对齐与新行为测试（见 Tests）
Files:        修改 internal/adapter/docker/{errors,token,handler,remote,catalog,virtual,api}.go、internal/adapter/docker/{token,remote_pull,catalog}_test.go、internal/adapter/helmoci/remote_test.go、internal/httpapi/{docker_v2,docker_token}_test.go；新增 internal/adapter/docker/remote_tags_catalog_test.go、remote_unfound_shapes_test.go；deploy/compose/.env.uat（BINFLOW_VER→uat-l0011-f80c46aa，gitignored）；reports/compatibility/L000-docker-remote-diff.md（追加复验段）；删除无
Tests:        新增 remote_tags_catalog_test.go：实时聚合（上游加 tag 即时可见 + 上游计数逐次递增）、Link 三页合并字典序、tags 降级 null、_catalog 聚合/空/死上游、路由门（非 remote 404/NAME_UNKNOWN/405/嵌套不劫持）；remote_unfound_shapes_test.go：tag/digest/blob 三 404 体逐字段对照 E6；重写 TestT392RemoteWriteRefusal（400×4 文案 + blob DELETE 405 保留腿）、helmoci TestRemoteWriteRefusal 同构、TestRenderAuthFailureTokenPathIsArtifactoryForm（E1-5 体 + host 回显）、TestV2TokenWrongCredentials（E1-5/E1-4 + CLI 指纹前提）、TestTokenEndpointSuite 匿名关闭臂
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter internal/httpapi（空）&& ~/go/bin/golangci-lint run ./internal/adapter/docker/... ./internal/adapter/helmoci/...（0 issues）；go test ./internal/adapter/docker/ ./internal/adapter/helmoci/ ./internal/repo/ ./internal/httpapi/ -count=1；UAT 重建 make console && make docs && make release VER=uat-l0011-f80c46aa && build-release.sh + compose up -d --force-recreate（卷沿用）；重放 curl 序列（tags/_catalog/token 401/manifest 404/blob 404/禁推三端点/ping）+ docker login 坏/好密码 + docker pull t2
Outputs:      tags/list → 200 pretty {"name":"busybox","tags":["t1","t2"]}（缓存仅 t1，上游日志实收 GET /v2/busybox/tags/list 200）；_catalog → 200 {"repositories":["busybox","hello-world"]}；token 坏凭据 → 401 {"errors":[{"status":401,"message":"Bad Credentials"}]}，docker CLI 渲染 unknown: Bad Credentials（E1-5 客户端指纹逐字，修复前 unauthorized: authentication required）；manifest 404 → detail{"manifest":"busybox"}+静态 message；blob 404 → detail{"blobSum":...}；禁推三端点 400 三文案逐字；ping 挑战 service="localhost:8083"；pull t2 wire digest 两侧一致 sha256:1cfa4e2b…+二次 HIT
Compatibility: 8 项修复 7 项 SAME（C15/C16/C02/C12/C13/C11/C01，逐案证据进 diff 报告复验段）；D21 BLOCKED（修复点 internal/httpapi/repositories.go repoConfigOf/repoListItemOf，非 adapter 域，转 conductor）。可差分 surface：/v2/<repo>/<img>/tags/list（200 pretty 聚合）、/v2/<repo>/_catalog（200 聚合/空）、/v2/token 坏凭据 401、manifest/blob 404 体、禁推三端点 400、ping/token 挑战 service 回显
Security:     fetchList 翻页走 remote.Client Request.URL（每跳 SSRF 复筛，未绕过防护）；catalog 路由拦截经 authorizeRoute 读门 + NAME_UNKNOWN 不可区分 posture；路径解析沿用 parseV2Name 前置校验（../ 与保留前缀）
Performance:  tags/_catalog 每次实时上游往返（Artifactory E7-1 同姿势，无本地缓存读）；列表体走 buffered 4MB 上限（超限走降级空/列，不 OOM）；无其他影响
Risks:        未观察角落自决（已在代码注释+diff 报告声明）：tags 失败→"tags":null 拼写、禁推未观察组合保留 405、token 参数 400 保留 OAuth、pretty 缩进 Go 2-space（Jackson 空白未取证）；**unfound 新形态（C12/C13）经共享构造函数无声传导至 VIRTUAL 面（virtual.go:89/:287 调用点随签名/形态更新）——E6 证据仅采自 remote 仓，virtual 面同形态属推断（证据报告 §8 E1 走读：渲染位于共享 v2 REST 层非 remote 专有），非直接观察；已以 TestVirtualUnfoundBodyCarriesRemoteShape 钉住 tag+blob 两臂**；docker.io 的 library/ 归一化（E2-6）未实现（不在票面，聚合 name=上游回显名已按证据）；httpapi router.go:291 兜底挑战 service 常量残留（生产不可达）
Blockers:     D21（越界：internal/httpapi 域；一行级修复点位已定位，需转票）
Next:         D21 转 httpapi 域票；C05/C06/C10（缓存布局/marker）差分票待 remote-cache-v2 设计票管辖释放；C01b/C03/C14 待裁项走 authority；金样采集候选：禁推三 400 体与 token 401 体（错误形态已稳定）
```

## 补充：UAT 重放环境记录

- 镜像 `binflow:uat-l0011-f80c46aa-alpine`（linux/amd64，prebuilt 注入链）；容器 binflow-ga 重建，卷 `binflow-uat_binflow-data` 沿用（repo audit-probe-docker-remote + t1 缓存保留——正是聚合缺口的现场证据）。
- 上游 registry:3（difftest-upstream，127.0.0.1:5588，busybox:t1,t2 + hello-world:latest）重放后已 rm -f；宿主 busybox/hello-world 原镜像未动（仅解引用 probe tag）。
- UAT 侧 repo 与缓存保留（后续差分直接消费，同 L000-F 口径）。
