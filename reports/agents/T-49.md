# T-49 工作日志：Artifactory OSS 工程结构参考规格

- ticket: T-49
- 角色: reverse-engineer
- 日期: 2026-08-18
- 状态: done（待主会话 review/qa）
- area: docs/reverse/oss-structure.md（只读 OSS 源码 + 只读 reverse-src，无代码改动）

## 产出

`docs/reverse/oss-structure.md`——工程结构参考规格（区别于既有五份行为规格）：

1. **模块地图**：51 个 pom（37 实体模块 + 14 聚合）按 5 域归类，每模块一句话职责 + BinFlow 包映射；附双向对照速查表（BinFlow internal/* → OSS 模块）。
2. **分层思想**：从 pom 内部依赖提取的 L0–L5 依赖图；四条 SPI 接缝（addon 接口族 / Jersey 包扫描含 pro 专有包 com.jfrog.ph·bh / 许可证管理器 / interceptor 链）；REST 三层分离（resource→api 接口→core 实现）与 web.xml filter 链；JAXB 描述符集中配置 + postinit v100..v119 版本迁移链。
3. **M3 导航**：npm/PyPI 协议实现在 pro（batch2-protocol com/jfrog/ph，2184 文件）；OSS 侧骨架 = RepoLayout 描述符（base/config）、Maven 元数据（core/maven）、**50 个 MetadataProvider 统一注册表（core/metadata/service/provider，M3 最有价值）**、remote/virtual 完整类层次与解析器（core/repo/virtual 等，OSS 未闭源）。
4. 结构启示 6 条（per-protocol 三件套、接口两粒度、成对桥解环、拦截器扩展点、配置版本化、事件 schema 双语言）。

## 关键证据摘要

- 依赖图：51 pom 逐个解析 dependencies（artifactId 内集），无臆造。
- SPI：web/rest JerseyApplication 的 packages() 同时扫 org.artifactory 与 com.jfrog.ph/bh/build/distribution/cs——pro 挂 REST 的接缝直接可见（双源：pro batch2 有对应资源类）。
- CoreAddonsImpl（backend/core）implements 60+ addon 接口 = OSS 桩，pro 覆盖。
- addon 61 子包中无 npm/pypi 独立接口 → 7.x 协议走 ph 路线，M3 票不应按 addon 老路拆。
- 计数核对过：MetadataProvider=50、addon 子包=61、filter 类=14（注册名 13，4 个重复注册）、web/rest Resource 类=121。

## 待验证（低强度，已列入规格 §5）

- storage/config 空壳 pom 的真实用途
- CoreAddonsImpl 被 pro 覆盖的具体 bean 装配机制
- web/rest 新增域 devopsagent/mfe（rest-api.md 未覆盖，M4 再补）

## 自测

- 结构断言全部来自 find/grep 实际输出；未修改 reverse-src/ 与任何 Go 代码。
