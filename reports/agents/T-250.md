# T-250 — M9 守护基线与种子脚手架（RBAC 矩阵契约基线冻结 + 扇出种子 + e2e/m9 骨架）

- 角色：devops-engineer；area：`scripts/m7-rbac-matrix.sh（契约基线模式）+ scripts/m7-rbac-matrix.baseline/.whitelist + web/scripts/seed-m9.mjs + web/e2e/m9/`（playwright.config.ts 仅加 m9 项目段——e2e 骨架交付的一部分）
- 日期：2026-08-24；状态：done（四项 AC 全实证）
- 依据：ADR-0030（新增不破坏，Accepted）/ architecture §14.5-1（守护测试第 1 条——本票落地者）/ PRD milestone-9.md N01~N07（FR-78/79 数据底座）/ BOARD M9 B1
- 纪律：未动 BOARD.md、未 git 提交、未碰 Go 源码（T-255 地盘）；Makefile 与 ci.yml 零改动（既有 `make test-m7-rbac-matrix EXPECT=1` 入口原样复用）

## 变更清单

| 文件 | 内容 |
|---|---|
| `scripts/m7-rbac-matrix.sh` | 升级「期望文件 + 空差异白名单」契约基线模式：`--record`（清档实例实测冻结基线；已存在文件须 `--force` 且按 §14.5-1 须先有 ADR）/ `--expect` 双裁决——verdict A 保留 M7 PRD 语义表（语义下限，兼防「从坏 build 重录基线」）/ verdict B 新增基线逐格 diff + 白名单放行 + WL-STALE 死条目检测；`--baseline`/`--whitelist` 可覆写（负向测试用）；`--record` 与 `--expect` 互斥 |
| `scripts/m7-rbac-matrix.baseline` | **冻结的 M8 尾态契约基线**：57 格（3 角色 × 12 读行 + 7 写行），commit `6a9efb9`、清档临时实例实测、`--record` 生成 |
| `scripts/m7-rbac-matrix.whitelist` | 差异白名单，**初始为空登记**（仅格式说明头）——M9 期任何 (角色, 面, 行, 状态码) 偏离须显式登记（条目须带 T/ADR 注释）才放行；死条目 WL-STALE 同样红 |
| `web/scripts/seed-m9.mjs` | M9 种子（CLI + 可导入模块）：50 仓（m9-r00~49）/ 20 用户（u1~u20，`pw-u<N>-123`）/ 10 组（m9-g01~10，每组 2 人）/ 4 覆盖集 target（t-u8-r：u8 对 10/50 仓 r；t-in：u9 manage 于 m9-r00/01；t-out：u9 仅 read 于 m9-r02/03；t-grp：组 m9-g01 manage——E9 组臂）；幂等（PUT 汇聚 + target create-if-absent）；内置验证（计数 + u8 部分读探针 + u9 m 覆盖探针） |
| `web/scripts/seed-m9.d.mts` | 上述 .mjs 的 TS 声明（seed-m8.d.mts 同款纪律；SeedClient 类型自 seed-m8 导入） |
| `web/e2e/m9/support/seed.ts` | spec 侧种子封装：re-export + `m9Client()`（BASE/ADMIN_USER/ADMIN_PW 口径同 m8） |
| `web/e2e/m9/{usage-fanout,users-groups,permissions-mholder,oidc-stepup}.spec.ts` | 四个占位 spec（`test.skip` + 填充票号注记：T-253/258、T-251/252/257、T-254/259、T-260） |
| `web/playwright.config.ts` | +`m9` 项目段（testMatch `m9/**.spec.ts`）；chromium 项目 testIgnore m9/（防双跑）；裸 `npx playwright test` 两项目全跑、每 spec 恰一次 |

## 1. RBAC 矩阵契约基线冻结（「新增不破坏」机器闸）

**冻结来源**：工作树 Go 源干净（M8 尾态，最后 Go 提交 `5e3f3a4` T-249）；`bin/binflow-server`（08:40 构建）新鲜。`--record` 自启清档临时实例（随机端口 + 丢弃式 data_dir）实测生成。

**基线内容**（57 格全表，矩阵渲染与文件一致）：

| 面 | readonly_admin | user | admin |
|---|---|---|---|
| R01~R11 读 | 200 | 403 | 200 |
| R12 storage/migration | 501 | 403 | 501 |
| W01~W07 写 | 403 | 403 | 200（W02/W04=201 create 臂） |

**正路验证**（归档日志：`/tmp/t250-expect-1.txt`、`/tmp/t250-expect-2.txt`、`/tmp/t250-make-expect.txt`，逐字输出节选）：

```
$ ./scripts/m7-rbac-matrix.sh --expect          # 清档实例，两次
m7-rbac-matrix: 0 deviations — PRD table clean, contract baseline clean (0 whitelisted)
EXIT=0
$ make test-m7-rbac-matrix EXPECT=1             # Makefile 唯一入口同口径
m7-rbac-matrix: 0 deviations — PRD table clean, contract baseline clean (0 whitelisted)
MAKE EXIT=0
# 跨次运行矩阵渲染逐字节一致（diff 空）——确定性成立
```

**负向验证**（临时副本，已提交文件零触碰）：

| # | 场景 | 结果 |
|---|---|---|
| NEG1 | 篡改基线副本 R12 501→200（M7 表容忍 200，只有 verdict B 能抓） | exit 1：`DEV readonly_admin read R12: baseline 200, got 501`（verdict A: 0, B: 1） |
| NEG2 | 错记白名单（登记 200 而实际偏离是 501） | exit 1：DEV + WL-STALE 双红——闸门拒绝描述错误的登记 |
| NEG2b | 正确登记 `readonly_admin\|read\|R12\|501 # 注释` | exit 0：`WL ... (whitelisted deviation)`，`(1 whitelisted)`——显式登记才放行 |
| NEG3 | 白名单挂死条目（偏离从未发生的 `user\|read\|R01\|200`） | exit 1：`WL-STALE user read R01: entry expects 200, observed 403 (baseline 403)` |
| NEG4 | `--record` 覆写已存在基线（无 `--force`） | exit 2 拒绝 + 文件逐字节未动 |

`sh -n` 通过；`-h` 帮助渲染新用法完整。

## 2. seed-m9.mjs（FR-79 数据底座）

清档临时实例（`BINFLOW_ADMIN_PASSWORD=password`，62479 端口，跑毕已停并清理）：

```
$ node scripts/seed-m9.mjs --base http://127.0.0.1:62479        # 首跑 2s
seed-m9: 50 repos / 20 users / 10 groups / 4 targets in 2s
targets: 4× created；verify.ok=true
  u8 探针:  granted 404（过鉴权、缺文件）/ denied 403（未授权仓）
  u9 探针:  covered 201（族 4 覆盖集臂放行 t-in 替换）/ uncovered 403（t-out 越界）
$ node scripts/seed-m9.mjs --base ...                            # 幂等复跑 2s
targets: 4× present；verify 逐字段与首跑一致；EXIT=0
```

u9 探针走的是 **M8 尾态既有语义**（permissions.go 族 4 例外：`CapSecurityWrite ∨ repos ⊆ manage 覆盖集`）——种子顺带自证 u9 的 m 位真实生效，E6（T-254）在此之上做过滤列表。

**票面实证命令**（curl/jq 原样）：

```
$ curl -su admin:password .../api/repositories | jq length
50                                    # ≥50 ✓
$ curl -su admin:password .../api/security/groups  | jq '[.[]|select(startswith("m9-g"))]|length'
10
$ curl -su admin:password .../api/security/users   | jq '[.[]|select(test("^u[0-9]+$"))]|length'
20
$ curl -su u9:pw-u9-123 .../api/system/version -o /dev/null -w '%{http_code}'
200                                   # PRD N 序列密码口径可用
$ curl -su u8:pw-u8-123 .../m9-r49/probe.txt -o /dev/null -w '%{http_code}'
403                                   # 排除仓零可达（N06 负向 grep 的底座）
```

## 3. e2e/m9 骨架

```
$ cd web && npx playwright test --project=m9 --list
  [m9] › m9/oidc-stepup.spec.ts:14:6 › oidc-stepup: ... (fills with T-260)
  [m9] › m9/permissions-mholder.spec.ts:16:6 › permissions-mholder: ... (T-254/T-259)
  [m9] › m9/usage-fanout.spec.ts:14:6 › usage fanout: ... (T-253/T-258)
  [m9] › m9/users-groups.spec.ts:16:6 › users-groups: ... (T-251/T-252/T-257)
Total: 4 tests in 4 files
$ npx playwright test --project=chromium --list | grep -c 'm9/'
0                                     # 基础项目零拾取 m9 —— 无双跑
$ npx playwright test --list | tail -1
Total: 159 tests in 35 files          # 155 既有 + 4 m9
```

## 4. 质量门禁

- `node --check scripts/seed-m9.mjs` 通过；`--plan-only` 输出 50/20/10 + 4 target 全清单。
- web 全量 `npm run lint`（eslint .）零告警；`npm run typecheck` exit 0（新 .d.mts + e2e/m9 strict 下编译干净）。
- 未跑 playwright 全量执行（占位 spec 无断言；实例联调归后续填充票）。用户常态实例（18080 M8）零触碰。

## 遗留 / 移交

1. **CI 接线未做**（area 纪律：ci.yml 不在本票 area）：闸门本地口径已就绪（`make test-m7-rbac-matrix EXPECT=1`）。§14.5-1「偏离即 CI 红」的接线建议随 T-272 终验或 conductor chore 裁量——matrix 在 CI 约 +30s（自启临时实例）。
2. 白名单使用口径已写入文件头：条目须带 T/ADR 注释；改基线文件本身须先 ADR + `--record --force`。
3. t-grp（组 m 覆盖集）是为 E9 组臂预置的数据；T-254 若不需要可留作不动产（不参与 u9 覆盖集，零干扰 N07 断言）。
4. 种子未上传内容文件（usage 行 usedBytes=0 起步）；若 T-253/T-258 要非零 usage 行，扩展 `seedM9` 加 PUT 内容即可（m8 的 putFile 模式现成）。
