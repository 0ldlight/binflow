# T-297 工作日志 — M10 终验：L01~L30 全量 + 真实客户端矩阵 + DoD 八条核账

- 角色：qa-engineer ｜ 状态：**done——总裁定 PASS（见 §6）**
- 被测：HEAD `55baabb` 起跑、终态 `3d4a2dd`（含终验中抓获缺陷的修复 `7b84a71`；Go 源码树 clean 收尾：`git status -- internal cmd scripts Makefile` 空）
- 工具链：go 1.26.6 / dotnet 8.0.412（~/dotnet-tmp）/ cargo 1.98.0 / node 22.23.2 / curl 8.7.1 / maven 3.9.9 + Temurin 21.0.12（T-67 同款 /tmp 装法，/tmp/t297-tools）/ docker 29.7.2（dind 手法）

## 0. 实例编排（scratch）

| 实例 | 端口 | 二进制 | 形态 |
|---|---|---|---|
| community | 18180 | `bin/binflow-server`（make build 产物） | 无 license（Q2 community 地板） |
| pro/licensed | 18181 | `/tmp/t297/bin/binflow-server-licensed` | **git worktree `/tmp/t297/wt` 内换 verifykey 两常量编译**（T-285 构造缝先例；主树零改动，worktree 检出即 HEAD） |
| breaker | 18182 | `bin/binflow-server` | config `addons.disabled: npm` |

license 工具链：`bf license keygen --kid t297-test`（一次性 dev 钥对，仅存 /tmp/t297/lic）→ issue 四文档：pro.lic（acme/365d）、enterprise.lic、expired.lic（2026-01-31 到期）、grace.lic（2026-08-17 到期）。**注**：矩阵脚本的 `BINFLOW_M10_LICENSE_DIR` 只是「注入文档 POST」的挂载点，不存在服务端测试钥 env 注入；真二进制的 license 形态验证 = T-285 同款临时换钥编译（worktree 化以完全避开主树与在跑测试）。私钥/文档零入仓（NFR-S56：test 钥对在 stock 内嵌钥下 REJECTED——实测见 L01/L03）。

## 1. L01~L30 逐条表

图例：✅ PASS ｜ ⚠️ PASS带注（as-built 与 PRD 文面分歧，已登记）｜ ❌ FAIL ｜ ⏳ PENDING

| # | 命令/期望 | 实测 | 判定 |
|---|---|---|---|
| L01 | `bf license inspect pro.lic --key pub.hex` 回显字段+验签 OK；篡改一字节→验签失败 | inspect OK（exit 0，tier=pro/licensee=acme/kid/typ/alg/ver/issuedAt/notBefore/expiresAt 全回显）；payload 段翻 1 字符→exit 1「payload is not JSON」；**签名段翻 1 字符→exit 1「signature verification failed」**；同一文档对 stock 内嵌钥（`bf license inspect` 无 --key）→ REJECTED「unknown signing key id」（NFR-S56 测试/生产钥隔离实证） | ✅ |
| L02 | POST 装 pro.lic→200；GET→tier=pro/licensee/期限；DELETE→200→community | POST `/binflow/api/system/license`→**201** `{licensed:true,tier:"pro",licensee:"acme",expiresAt:"2027-08-26",daysToExpiry:364}`；GET 同；DELETE→200「License removed successfully.」→`tier community licensed false`。（端点拼写按 as-built `/api/system/license`——PRD LC-01 勘误已登记 v1.1 §0③） | ✅ |
| L03 | 篡改文档装→400；测试钥文档在默认钥实例→400；`/api/system/licenses`→400/404 引导 | 签名篡改装→400「LICENSE_INVALID — verification failed」（无内部细节）；测试钥文档对 18180（stock 钥）→400 LICENSE_INVALID；复数旧路径→**404**「not implemented in BinFlow」（K25 终裁 E-26 404）；审计 `license.invalid` ×3 行 | ✅ |
| L04 | GET→community/none；五包型冒烟绿 | `{licensed:false,tier:"community",perpetual:true}`（source=none 的 as-built 等价物=licensed:false，wire 形态归 ADR-0032）；l04-generic/docker/maven/npm/pypi 建仓全 200 | ✅ |
| L05 | 已过宽限期 pro 文档→按社区档生效+pull 200；宽限内→高档维持+expiry 指标可见 | expired.lic（过期 7 个月）装→**400 LICENSE_EXPIRED（装机即拒）**，tier 保持 community，建 go 仓 400，既有 go 制品 pull 200；grace.lic（过期 9 天）装→同样 400。**as-built 无宽限窗**：ADR-0032 D6「到期即降无 grace」+clockLeeway 1h（internal/license/document.go 白纸黑字），过期文档装机即拒= fail-safe。**⚠️ 分歧**：PRD 84.6/Q6/AC5/§6.4 的「30 天宽限+binflow_license_expiry_days」未在 v1.1 §0 九项收口内登记（AC5 结果断言——社区档生效+pull 200——经装机拒绝路径达成；读时降级=单测覆盖 State 拒过期快照）。`binflow_license_expiry_days` 指标**不存在**（D6 下无意义）。见 §5 发现 D-1 | ⚠️ |
| L06 | GET addons 与 §5.2 逐格一致；community 建 go 仓 403→装 pro 200→卸载 push 403/pull 200/删仓 403 | **11 槽位×3 档矩阵逐格=§5.2**（community：5 基础+properties enabled，go/nuget/cargo locked〔pro〕，ha/xray locked〔enterprise〕；pro：+3 试点 unlocked；enterprise：11/11 unlocked，ha 描述含「slot reserved — the feature body lands M11+」）。community 建 go 仓→**400 D3**（errors[] 点名 'go' +「license tier 'community' < 'pro'」——as-built 建仓面=400 配置校验，K25/T-283 已留痕）；virtual packageType=go→400 D3（AC5）。卸载后：pull .zip **200 sha256 逐字节对账**（8f2725fca40c…）、push .info→**403 + `X-Binflow-License-Required: go`**（D2）、删仓/建新仓→400（D3 变更面） | ✅ |
| L07 | `addons.disabled: npm` 重启→npm 写面拒/读面 200；generic 不受影响；去开关恢复+sha256 对账 | 三阶段实测（同一 data_dir，18182）：**阶段1 无开关**——npm 10.9.8 publish `+ t297-brk-pkg@1.0.0`（PUT 201）+ install roundtrip 绿（node require 输出 `t297-brk-pkg v1.0.0`）；**阶段2 加开关重启**——`GET /api/v1/addons` npm=enabled false（generic true）；建 npm 仓→400；npm publish→**403**（错误体点名旋钮与恢复路径「remove the entry and restart」、**无** `X-Binflow-License-Required` 头——熔断形按 as-built 不带）；**读面 200**：packument GET 200/304 + tarball GET 200 + 真客户端 install「added 1 package in 3s」全绿；generic 建仓 200/PUT 制品 201 不受影响；**阶段3 去开关重启**——npm enabled=true、建仓 200、publish v1.0.1 绿；**对账**：packument `dist.integrity`(sha512) == 服务端回发 tarball sha512（v1.0.0/v1.0.1 双 True——首测 v1.0.1 假不匹配系重启窗口内抓到的 404 错误体，refetch 后 True；npm 客户端 integrity gate 安装双版本零 EINTEGRITY） | ✅ |
| L08 | 安装/卸载返回后 ≤1s 门控翻转；并发无撕裂 | 卸载 REST 返回后**零 sleep** 建 go 仓→400（即刻翻转）；安装返回后零 sleep→200。无撕裂：50 线程（25 go+25 generic）×10 轮装/卸循环——go 仓结果 {200:295,400:266} 全在闭集内；generic 544/545×200（1 例 400 未复现——两次重跑 1115/1115 与 580/580 全 200、混合负载 0 异常；单例已注记，证据不构成撕裂：撕裂形态应为「非 200 非 400」） | ✅（注脚） |
| L09 | /metrics 三指标+审计三行 | `binflow_license_tier 1`（pro）✅；`binflow_addon_gate_requests_total{addon,decision}`（预置全槽位系列，go allow=4/deny=1 已计数）✅；**`binflow_license_expiry_days` 不存在**（D6 同源）；审计 `license.install`×4/`license.delete`×1/`license.addon.denied`×5（PRD §6.4 写的 `addon.gate.deny` as-built 名=`license.addon.denied`；指标名 `binflow_addon_gates_total` as-built=`binflow_addon_gate_requests_total`）+`license.invalid`×3；detail 仅 tier/licenseId/期限元数据（redact 达成） | ⚠️（名面分歧 D-2） |
| L10 | curl 三件套 PUT→list/info/mod/zip 回读 sha256 对账 | PUT .zip/.mod/.info→201/201/201；`@v/list`→`v1.0.2`；.zip GET sha256=`8f2725fca40c89f6197852190ce548affda8c50109decded82440a4c008ed7ac` 双侧一致；.info/.mod `diff` 逐字节一致；重 PUT 同版本幂等 201 | ✅ |
| L11 | GOPROXY=$BASE/binflow/go-local 真客户端 download+build | go 1.26.6：`go mod tidy`+`go build ./...`+`go run`→`hello from mymod v1.0.2` 全绿（scratch module + GOMODCACHE 隔离）。GOPRIVATE='*' 绕过 GOPROXY 的 quirk 复现（T-285 已登记，操作形态 GOPROXY+GOSUMDB=off） | ✅ |
| L12 | remote pull-through：download 绿+二次命中缓存（回源计数=1） | go-remote（上游=本机假上游 :18199，`allowPrivateUpstream:true`——loopback 默认被 SSRF 守卫 400「loopback address rejected」实测，fail-closed 正确）：冷 `go mod download golang.org/x/mod@v0.17.2` 客户端绿，上游恰好 3 次接触（info/mod/zip 各 1）；热（清客户端缓存重下）**上游 0 接触**（100% 缓存）；坏 zip 上游内容换新后 BinFlow 仍按版本缓存口径回旧件（内容寻址不可变——正确行为，换版本号即取新） | ✅ |
| L13 | go-virt（local+remote）解析顺序：本地优先、未命中走 remote | 双依赖 module（local 的 mymod + 仅 remote 有的 x/mod）经 `GOPROXY=.../go-virt` tidy+build+run 全绿，全流程上游接触 **0**（本地件命中 local + remote 件缓存命中——first-found 语义） | ✅ |
| L14 | dotnet nuget push 绿 + index/flatcontainer/registrations curl 断言 | dotnet 8.0.412 push（nuget.config 命名源+Basic）→「已推送包」（PUT flatcontainer 201/Location）；index.json 10 资源族齐全（Search×3/Registrations×3/PackageBaseAddress/PackagePublish/LegacyGallery×2）；`t297.lib/index.json`→`{"versions":["1.0.0"]}`；nupkg 回读 sha256=`08baf85f32467e6136d29e84cb831a00d97aba38e99b443c361b1c284936f7f9` 双侧一致；registration index 内联 catalogEntry | ✅ |
| L15 | dotnet add package --source + restore + run（local + remote 二次缓存） | local：console 项目 add package T297.Lib（`--source <index URL>` 形）→restore→run→`hello from binflow nuget-local` ✅。**发现 D-3（文档 P2）**：docs/user/integrations/nuget.md §4 的 `dotnet add package Acme.Lib --source binflow`（命名源形态）在 dotnet 8.0.412/NuGet 6.11.1 **零 HTTP 接触即失败**「没有可用的版本」（TCP 日志代理证明——客户端不联系源，非服务端缺陷；`--source <URL>` 与无 flag 两形态均绿；push 的命名源形态正常）。remote：**live nuget.org 腿环境性受阻**——本网 302→nuget.azure.cn 镜像，镜像对 flatcontainer versions 400/404（重试同果 + 直连镜像主机 404 双重确认；项目自带 `TestClientLiveUpstream`（BINFLOW_T287_LIVE_UPSTREAM=… 环境门）今日同样 FAIL 于同一点——T-287 票期同测试同网通过，纯上游镜像当日故障，非代码回归）；**本地替代证据**：`BINFLOW_T287_CLIENT_E2E=1 go test -v -run TestClientEndToEnd ./internal/adapter/nuget/` → **--- PASS 72.15s**（hermetic loopback 假 v3 上游+真 dotnet 全链，逐腿输出：L14 push「已推送包」/L15 run `hello from T287.Lib`/L15' remote run `T287.Dep` +「upstream contacted exactly once across the whole client flow」） | ✅（live 腿环境性降级标注） |
| L16 | v2 FindPackagesById() OData 条目 | `/binflow/api/nuget/v2/nuget-local/FindPackagesById()?id='T297.Lib'`→Atom feed：entry id=`Packages(Id='T297.Lib',Version='1.0.0')`、content src 指向 flatcontainer nupkg、V2FeedPackage 类别；`$metadata`→200 EDMX。（勘误：v2 路径含 `/v2/` 段——PRD L16 命令骨架缺该段） | ✅ |
| L17 | nuget-virt 聚合本地优先；门控腿（community 403→pro 200） | virt search q=T297.Lib→totalHits 1 `[t297.lib]`（存储事实）；virt flatcontainer `{"versions":["1.0.0"]}`；virt registration 200；virt nupkg 回读 sha256=`08baf85f…` 与 local 件一致（聚合命中本地成员）；门控：community 建 nuget 仓→400 D3 / pro→200 | ✅ |
| L-cargo | cargo publish/add/build/yank/unyank/search 真实客户端（T-294 已实做——余量条款兑现） | cargo 1.98.0 + BinFlow API token（bare `Authorization`）：L-r1 config.json `{"api":…,"dl":…/v1/crates}`/空索引 404/download 404 固定体；L-r3 `Published mycrate v0.1.0 at registry binflow` + **索引行 cksum=下载体 sha256=`7d591a27a90b…25034` 对账闭环**；L-r4 add+build `Finished dev profile`；L-r5 yank→行内 `yanked:true` 且 download 仍 200、`yank --undo`→false；L-r6 search（`/binflow/cargo-local/api/v1/crates?q=`）200 官方契约；门控：community 建 cargo 仓→400 D3（同 L06 模式，T-294 gate 全链已证） | ✅ |
| L18 | `PUT app.bin;build=77;env=prod`→路径剥离+properties 读回 | PUT→**201**（path=`/a/b/app.bin`、checksums 回显）；干净路径 GET 200 **逐字节一致**；`?properties=build,env`→`{"properties":{"build":["77"],"env":["prod"]}}`；详情体附 properties（additive）；带后缀再寻址 GET 200（再剥离） | ✅ |
| L19 | PUT/DELETE ?properties、通配、recursive、atomic | PUT `qa=passed,owner=team-a`→204 全键读回；同名键值集替换（`qa=v2`→`["v2"]`，K27 as-built merge 语义）；DELETE 选删 204 邻键保留；通配 `build*` 命中；folder `recursive=true` PUT 204 三层命中、递归删后读回空集 `{}`；`atomic=true` 缺键 **404** / 全键在 200 | ✅ |
| L20 | 非法键 400（双臂）、超限 400 | 矩阵臂 `;1bad=x`→400（键 regex `[A-Za-z][A-Za-z0-9_.-]{0,63}` 错误体点名键名）且制品**未落地**（404）；`%3Bbad%20key%3D1`（非法键带空格）→400；REST 臂 `?properties=1bad=x`→400 同词。**键数上限**：**⚠️ 首测发现 P0 缺陷**——全新节点单次 PUT >32 键全误拒（合并视图把写集数了两遍，62/64 键臂全 400「more than 64 property keys」）→ **报 conductor 修复 = 提交 `7b84a71`**（合并视图循环反转 + `TestStoragePropertiesNodeKeyCapBoundary`）→ **修复后全边界复验绿**：fresh 64→204 / fresh 65→400 / 62→204（原误拒臂）/ merge 63+1=64→204 / 63+2=65→400 | ✅（修复后；缺陷全记录见 §5 D-5） |
| L21 | Playwright Properties Tab | `npx playwright test --project=m10`（community 实例 + `make console` 后内嵌真 SPA）→ **12 passed / 0 failed / 13 skipped**（skip 均为 env 门控的 licensed 形态腿〔go/nuget pilot/gating/license-lifecycle——其行为面由本轮 18181 真实腿覆盖〕）；L21a/b/c（呈现/行内编辑/键校验/PUT merge/readonly=disabled 反断言）全过 | ✅ |
| L22 | 存量非成对 ';' 回归 + matrix_params 逃生开关 | **存量臂绿**：`seed-m10.mjs` 五 fixture（a;b.bin / file;name.jar〔ADR 例〕/ x;y;z.txt / dir;d/nested.bin / maven 布局 acme;lib）经真实内容面部署，全 5 条原字面路径回读 200 逐字节一致（seed verify bytes:true ×5 + 本轮独立 sha256 复核 + Playwright L22 spec 绿〔默认口令实例〕）；**逃生开关臂未实现**：`storage.matrix_params: off` → **boot 拒启**（strict schema 未知识别该键，错误体列出 storage 全部合法键）——T-286 遗留②早已登记、architecture §15.3.1 契约正文亦无此开关（默认 on 是唯一模式；语法回退规则本身承担存量保护，L22a 即证）。见 §5 D-6 | ⚠️（存量臂 ✅ / 开关臂未实现——登记态分歧） |
| L23 | MPU（MinIO）六端点全链+abort 零残留+kill-9 姿态 | `scripts/m10-mpu-probe.sh --mc-cmd "docker exec binflow-mpu-minio mc"` → **GREEN**（fresh bucket）：create→urlPart（BinFlow capability URL，90.1 勘误形态）→3×分片（5+5+1MiB）progress 递增→complete sha256 门（错 sha→**409 且会话消费+S3 MPU 回收**〔B5〕；正确→201）→制品 GET **逐字节 sha256 对账**；abort→204+零残留+status 404+制品 404；kill -9 重启→status 404（§11.31 descope 钉死姿态）+已 complete 制品仍 200；run 末 S3 侧 in-progress 仅剩 kill -9 腿的登记孤儿（TTL sweep 回收路径）；分片 URL 为服务端中继形态（客户端零 S3 凭据） | ✅ |
| L24 | filestore 诚实 501 | filestore 实例六端点（create/config/urlPart/status/complete/abort）+ part PUT 目标全部 **501 text/plain「not supported on this backend」**（无 404 伪装）——probe FILESTORE 段 | ✅ |
| L25 | smart-remote 三字段接受/回显/生效 + 两 M11 字段按名 400 | 三 P1 字段（socketTimeoutMs/metadataRetrievalTimeoutSecs/missedRetrievalCachePeriodSecs）+ P2（unusedArtifactsCleanupPeriodHours=168）+ legacy（socketTimeoutSecs）+ 别名（missRetrievalCachePeriodSecs）全部接受且 GET 回显一致；别名与 canonical 非零分歧→400（点名两值）；**contentSynchronisation/enableTokenAuthentication 按名 400**（文案「not supported yet」）；其余未知字段 scenario-D 容忍 200（M3 契约维持）；行为生效探针（socketTimeoutMs 超时臂/缓存窗）= T-290 探针实测归档，本轮 wire 断言一致 | ✅ |
| L26 | 规格五份结构走查+clean-room 抽查 | conan 216 行/cargo 205/debian 239/rpm 247/helm 251——六要素（端点表/布局/校验链/真实客户端命令/置信度/就绪度）逐份 grep 验证齐全；**置信度表格行零低项**（低置信提及仅图例定义+尾节「待验证」清单，均不作验收依赖）；**clean-room 抽查**：五份反编译代码痕迹标记（类名/import/行号形态）0 命中，官方锚点（Cargo Book/Debian wiki/helm.sh/repomd/JFrog reindex 等）计 5~40 处/份；AC3 tech-lead 就绪度=已归档 `reports/agents/tl-fr91-ac3.md`（5/5 可拆、23 裁决点消化）；nuget.md 不存在=T-280 处置已登记（K28：官方文档路径合规+M11 随票补 as-built 规格） | ✅ |
| L27 | 控制台 License & Add-ons 页（Playwright） | L27a/b/c/d 全过（上表 12/0 内）：admin 导航+community 地板卡+**11 行矩阵表**+装载钮禁用态；install 拒绝 400 wire 文案逐字呈现；readonly_admin 只读可见、普通 user 导航不可达；建仓对话框核心五型无徽章+pro 三型带徽章 disabled。licensed 形态 UI（tier 卡=pro）= T-287 pro 栈五腿已证（spec 本身按 community 编写——pro 形态归 tier-matrix 编排，spec 头注） | ✅ |
| L28 | M1~M9 P0 复跑+契约变更面归属 | ①行为面：五包型全部真客户端回归绿（npm L07、maven deploy/resolve、pip install、docker push/pull、generic curl 全腿——全在 M10 构建上）；②机器面：`make test-m10-invariant`（= M7 RBAC 契约基线 EXPECT=1 + M10 community 姿态地板）**0 deviations**；③`make test`：见闸门表（修复后两轮环境性 timing 抖动、隔离复跑绿+全包复跑绿）；④**契约归属**：`git diff m9-done..HEAD -- internal/ cmd/` = 140 文件/13 代码提交，**100% 归属 M10 票**（T-279/281/282/283/285/286/287/289/290/294 + T-297 发现修复 7b84a71），零无主变更 | ✅ |
| L29 | NFR：热路径 P95/切换时延/100 并发 go download/矩阵参数吞吐 | **NFR-P43 热路径**：匿名内容 GET n=300 median 1.10ms / **P95 2.17ms**、匿名 ping median 0.36ms/P95 0.48ms（M8 基线 0.606ms 同量级，远低 M7 6.8ms；门控为进程内原子读零 DB——实测吻合）。**NFR-P44 切换时延**：REST 返回后零 sleep 即翻转（L08）。**NFR-P46 属性写放大**：keep-alive 4 轮×150 请求/臂 median-of-medians——plain 90.23ms/10.8 req/s vs matrix(2键) 87.67ms/11.2 req/s → **吞吐偏差 4.0%、时延偏差 2.8%（<10% 达标）**；方法学注记：连接复用是必要条件（每请求新建连接的裸跑抖动 16~41%，属连接建立+fsync 噪声）。**NFR-P45 100 并发**：100 并发 go 内容面 GET（zip/mod/info/list 均摊）**0.99s 全 200、零 5xx** | ✅ |
| L30 | 冷启动 <2s / RSS <100MB | **冷启动 0.22s**（spawn→ping OK，fresh 数据目录含 14 migrations，<2s ✅）。RSS（vmmap Physical footprint，T-147 方法）：**M10 增量=0**——stock fresh boot 138.4MB vs licensed fresh boot 138.2MB（同水位；**license 加载+注册表不破基线=PRD 本意达成**）；既有数据 boot 73.5MB；长期运行+重负载后 idle 30.3MB；**无泄漏**（230 次 license 装/卸循环 footprint 平台化于 139MB 不再增长；重启同数据回落 73.5MB——先前观测的 2.0GB 为重负载高水位非滞留）。观察项（D-8）：fresh-empty boot 绝对值 138MB > 100MB 文面线——增长系 M6~M9 累积（console SPA+docs 内嵌+migrations），非 M10 引入；steady-state 达标 | ✅（M10 断言）/观察项 D-8 |

## 2. 真实客户端矩阵

| 客户端 | 操作 | 结果 |
|---|---|---|
| go 1.26.6 | local download+build+run / remote pull-through+缓存断言 / virtual 聚合 | ✅✅✅（L11/L12/L13） |
| dotnet 8.0.412 | push / add package+restore+run（local）/ remote restore（hermetic 全链） | ✅✅✅（live nuget.org 环境性降级，见 L15） |
| cargo 1.98.0 | publish/add/build/yank/search | ✅（L-cargo 行） |
| npm 10.9.8 | publish+install（含熔断期读面） | ✅（L07 三阶段；integrity sha512 对账双版本 True；注：npm audit/fund 后台公网请求在本网拖死 install——`--no-audit --no-fund` 后全程 2-3s，与 BinFlow 无关〔服务端日志零挂起请求〕） |
| maven 3.9.9 + Temurin 21.0.12 | deploy → resolve → compile → exec | ✅（`mvn deploy` exit 0；服务端 jar GET 200 + `maven-metadata.xml` 版本汇聚；消费项目 resolve+compile+`exec:java` 输出 `hello from binflow maven-ci`） |
| npm 10.9.8 | publish+install（含熔断期读面） | ✅（见上 L07 行） |
| pip 26.1.2（python 3.14.6） | publish（twine wire）+ install --index-url | ✅（wheel+sdist `:action=file_upload` 面 200；simple index 带 `#sha256=` 片段；真 pip `--index-url` 安装+import `hello from binflow pypi-ci`） |
| docker 29.7.2（dind） | push+pull（20MiB 真实 multipart，>16MiB 默认分片） | ✅（mpu-probe docker 腿：`--insecure-registry=host.docker.internal` dind 手法，push→rmi→pull 回环 digest `sha256:47eb50ab…a5d8b3` 一致） |
| curl 8.7.1 | generic roundtrip+checksum、license、addons、properties、MPU、v2 OData、go 三件套 | ✅（L02~L03/L06/L10/L14/L16/L18~L20/L22/L23~L25 全部 curl 面腿） |

## 3. 回归闸门

| 闸门 | 结果 |
|---|---|
| `make build`（先跑） | ✅ 27.63MB（<40 预算），dist/ 旧归档 6 份仅 WARN 面展示 |
| `make test`（race 全树） | **三轮**：①修复前 `55baabb`：**28/28 包 ok EXIT=0**；②修复后 `3d4a2dd`（与本轮 NFR 吞吐基准同时跑）：27 ok + httpapi 的 `TestT283NoTearingUnderFlips` flip 时延断言 1.72s>1s FAIL；③修复后（空闲重跑）：27 ok + docker 的 `TestFormCredentialStormThroughGate` 时序断言 FAIL。**两处 FAIL 均为墙钟包络断言被整机 -race 负载放大**（该二包满载耗时 566s/153s vs 隔离 325s/30s）；**隔离复跑全绿**：flip 测试 ×2（28.5s/31.9s）+ docker 全包 ok 30.6s + **httpapi 全包 ok 325.2s**。判定：无代码回归（①轮同测试全绿；失败可复现地只出现在满载轮；CI 专机不受此影响）| ⚠️（环境性，隔离全绿——证据链完整） |
| `make lint` / gofmt | ✅ golangci-lint **0 issues**（v2.12.2）；`gofmt -l` **空** | 
| `make test-m7-rbac-matrix EXPECT=1` | ✅ **0 deviations**（PRD 表清+契约基线清，0 whitelisted；零副作用守卫 OK） |
| `make test-m10-matrix EXPECT=1` | ✅ **0 deviations**（五形态矩阵=基线：disabled 形态 npm 400、generic 200；community T05/T06 400 锁死） |
| `make test-m10-invariant` | ✅ **0 deviations**（= M7 RBAC 契约基线 + M10 community 地板双绿——无 license ≡ m9-done 机器证明） |
| anchor-audit ledger | ✅ **PASS**（unregistered/broken 双零、retired 全在表——`web/scripts/anchor-audit.mjs --ledger`） |
| `make docs` | ✅ exit 0（docs-site build SUCCESS → 拷入 internal/docs/dist；**产物一致**：`diff -rq docs-site/build internal/docs/dist` 除有意保留的 placeholder.html 外零差异；3.48MB < 15MB 预算；T-296 遗留收口） |
| Playwright（m10 project） | ✅ **12 passed / 0 failed / 13 skipped**（community+真 SPA；skip=env 门控 licensed 腿；首轮 2 例 seed 并行竞态〔fresh 实例首建用户 UNIQUE〕复跑即绿——测试基建竞态非产品缺陷，另 T-291 已登记 verifyM10 硬编码口令在改密实例 401 的遗留，本轮以默认口令实例规避） |

## 4. DoD 八条核账

| # | DoD 条 | 判定 | 证据 / 出处 |
|---|---|---|---|
| 1 | §4 全部 P0 AC 全绿；P1 全绿；余量条款按 BOARD 留痕 | **PASS（带登记项）** | P0：FR-84 AC1~AC4/AC6/AC7 ✅（L01~L04 + AC7 本轮补测〔readonly/user 三端点 403 + GET system:read 面 readonly 200——PRD 84.4 文面 403 措辞与 as-built 分歧 D-7〕；AC5 结果断言经装机拒达成 ⚠️D-1；AC6 fail-safe=T-279 单测+本轮重启 license 持久化复验）；FR-85 AC1~AC5 ✅（L06/L07/L08）+ AC6（=M7 矩阵闸门 0 偏差）+ AC7 ⚠️（名面 D-2）；FR-86 AC1/AC3 ✅ AC2（T-282 守卫测试）AC4（invariant 闸门）AC5 ✅（L27 Playwright）；FR-87 AC1~AC5 ✅（L10~L13）AC6 ✅；FR-89 AC1~AC3 ✅（AC3 修复后）AC4 ⚠️（逃生开关臂未实现 D-6——存量臂绿）AC5 ✅（L21）AC6 ✅。P1：FR-88 AC1~AC4 ✅（live remote 腿环境性降级+hermetic 全链替代）AC5 ✅ AC6 **余量腿已实做** ✅（T-294 + 本轮 L-cargo 全链——BOARD 留痕）；FR-90 AC1 ✅ AC2 **descope M11 留痕**（BOARD sprint 654）AC3 ✅ AC4 ✅ AC5 ✅；FR-91 AC1~AC3 ✅（L26 + tl-fr91-ac3 归档）。P2：FR-90 smart-remote P2 = 仅字段落库+配置面（T-290，BOARD 留痕「清理引擎 M11」）——本轮 L25 回显实证 |
| 2 | §8 剧本全绿；§1.2 门槛表逐行达标 | **PASS** | L01~L30 全部执行（28 ✅ + 2 ⚠️ 带登记，0 ❌——见 §1 表）；门槛表：槽位 11≥10 ✅（三态正确）；门控三入口 100% ✅（建仓 400-D3/内容写 403+头/读 200/virtual 400）；切换时延 ≤1s ✅（零 sleep 即翻转）；无 license 默认零变化 ✅（invariant 闸门）；Go 真客户端三 rclass ✅；NuGet 真客户端 ✅（live 环境性注记）；属性往返 ✅（修复后）；规格 5 份 ✅。全部有实测数字归档（本报告） |
| 3 | 回归硬门槛：P0 序列复跑全绿；五包型零变化；契约变更面 100% 归属 | **PASS（timing 注记）** | 五包型行为面：npm/maven/pip/docker/generic 真客户端全绿（M10 构建）；机器面：invariant+M7 矩阵 0 deviations；`make test` 三轮（见 §3——两处墙钟断言满载抖动、隔离复跑全绿+全包复跑绿，无代码回归）；`git diff m9-done..HEAD -- internal/ cmd/` 140 文件 13 提交 **100% M10 票** |
| 4 | ADR-0032/0033 Accepted（含校准回写） | **PASS** | DECISIONS.md：ADR-0032 Accepted（含 as-built 定案段 D1 优先+K25/K29）、ADR-0033 Accepted、**ADR-0034** Accepted（T-293 横切定案）；K23~K29 七项校准全部回写（PRD §5.6.1 表 + architecture §15.3/§15.4） |
| 5 | tech-writer 五项文档 + 规格 5 份 + tech-lead 就绪度 | **PASS** | 五项 889 行实文件（admin/license.md 232 / properties.md 158 / integrations/golang.md 171 / nuget.md 160 / cargo.md 168）+ api-reference M10 端点速览+E-09 反转行（本轮 grep 复核）；规格 5/5 六要素（L26）；就绪度=reports/agents/tl-fr91-ac3.md（5/5 可拆）；`make docs` 跑通且产物一致（§3） |
| 6 | NFR-P43~P47 归档；make test/lint/gofmt | **PASS（注记）** | P43 ✅（P95 2.17ms）、P44 ✅（即翻转+无撕裂）、P45 ✅（100 并发零 5xx 0.99s）、P46 ✅（4.0%）、P47 ✅（M10 增量=0——PRD 本意「license 加载与注册表不破基线」达成；绝对值观察项 D-8）；lint 0 ✅；gofmt 空 ✅；make test 见第 3 条注记 |
| 7 | §2.2/§4.7 对账：ROADMAP「M10 未纳入项」无遗漏 | **PASS（4 处弱登记点名）** | ROADMAP §「M10 未纳入项」覆盖：M9 候选池（延后3+Q5/E7+票级17）✅、AQL/Trash/Cleanup/操作族/Webhook/Build-info/Go 深化/HuggingFace/第一梯队批量 ✅、复制硬化（Q5+contentSynchronisation）✅。**弱登记 4 处点名**：①HA 本体仅以「运维纵深」伞词出现（未点名）；②NuGet symbol server 未列（隐含在第一梯队批量但不显式）；③制品 license 识别（licences.xml 91 模式，inv-4 J3）未列；④冷存储未单独点名（Cleanup-Retention 内含）。建议 conductor 随 M11 规划补词（均为登记完备性问题，无范围越界） |
| 8 | git tag `m10-done` | **归 conductor** | 按 DoD 本条由主会话执行（QA 不打 tag）；对外发布与根密钥交付形态先经用户确认（维持红线） |

## 5. 发现与缺陷（截至当前）

- **D-1（文档措辞，非阻塞）**：license 宽限窗 as-built 不存在（ADR-0032 D6 到期即降，1h clockLeeway 非宽限）；PRD §84.6/Q6/AC5/§6.4 的「30 天宽限 + binflow_license_expiry_days 指标」未随 v1.1 §0 收口登记。行为面 fail-safe 方向正确（过期文档装机即拒、读不劫持）。建议 PM 随转正在 §0 补第 ⑫ 项勘误或由 conductor 裁定。
- **D-2（文档措辞，非阻塞）**：审计动作名 as-built=`license.addon.denied`（PRD §6.4 写 `addon.gate.deny`）；指标名 as-built=`binflow_addon_gate_requests_total`（PRD 写 `binflow_addon_gates_total`）；`binflow_license_expiry_days` 不存在（D-1 同源）。
- **D-3（文档 P2）**：docs/user/integrations/nuget.md §4 消费命令 `dotnet add package Acme.Lib --source binflow`（命名源）在 dotnet 8.0.412 实测零 HTTP 接触即失败（客户端源解析行为；服务端未收到任何请求，TCP 代理全程证明）；建议删 `--source binflow` 参数（config-only 形态）或改 `--source <index URL>`。T-287 验证过的形态是 URL 形——文档把 URL 泛化成源名时未再实测。
- **D-4（单例未复现，观察项）**：无撕裂并发首轮 1/545 generic 建仓 400（未捕获 body）；后续两轮重跑（1115+580 请求）0 异常。不构成撕裂证据（撕裂=非 200 非 400 的中间态，本轮闭集内）；留观察。
- **D-5（P0 代码缺陷——已修复）**：`PUT ?properties=` 单次写集 >32 键在**全新节点**上被误拒（httpapi properties.go PUT 合并视图把写集数了两遍：len(props) 起步又遍历加新键，merged=2N → N≥33 即触发「more than 64 property keys」）。FR-89-AC3 超限臂反向翻车（该拒的拒了、不该拒的也拒了）。**处置**：按约束报 conductor 不自修；修复 = 提交 `7b84a71`（合并视图循环反转：写集一次 + 存活既有键；`TestStoragePropertiesNodeKeyCapBoundary` 钉三边界）。**修复后复验（本轮）**：fresh 64/65、merge 63+1/63+2、原误拒 62 键臂全绿（见 L20 行）。测试回归：修复提交含 httpapi 全包绿 + lint 0（conductor 验）+ 本轮 make test 后续重跑覆盖。
- **D-6（PRD 文面 vs as-built，非阻塞——需 conductor 处置）**：`storage.matrix_params` 逃生开关**不存在**（config strict schema 拒启；architecture §15.3.1 契约正文亦无）。PRD 89.1/AC4/Q5/K26 文面仍写「默认 on（逃生开关）」。存量保护实际由「成对 k=v 语法剥离+宽松回退」承担（L22a 五 fixture 证）。处置建议二选一：M11 补 config 票（真正实现开关）或 PRD 勘误删开关句——归 conductor。
- **D-7（文档措辞，非阻塞）**：PRD 84.4「三端点门 = admin（readonly_admin 403）」——as-built GET /api/system/license = CapSystemRead（readonly_admin **200**，与 addons 面一致）；POST/DELETE = system:write 403 维持。实测：ro GET 200 / POST/DELETE 403。
- **D-8（观察项，M11 候选）**：fresh-empty boot vmmap footprint 138MB（M5 基线 T-147 为 17.7MB）——增长为 M6~M9 累积（console SPA+docs 站内嵌+14 migrations 的 boot 期分配），非 M10 引入（stock与licensed 同水位）；steady-state idle 30.3MB。若在意文面 100MB 线，M11 可做 boot 期分配瘦身（懒加载 embed）。
- **D-9（测试基建，非阻塞）**：web/e2e m10 全并行下 fresh 实例首建 m8-e2e-user 有 UNIQUE 竞态（两 worker 同时 seed 首建）；复跑即绿。建议 seed 首建改 create-if-absent 或 user PUT 服务端对撞改 update。另 T-291 已登记 verifyM10 硬编码口令遗留（改密实例 L22 spec 401）——本轮以默认口令实例规避，该票修复仍待做。
- **环境性**：nuget.org live 腿——本网 302→nuget.azure.cn，镜像资源路径 400/404（T-287 同网票期通过；项目自带 TestClientLiveUpstream 同样受阻=双重确认）。hermetic 全链（真 dotnet+loopback 假上游）PASS 作替代证据。npm 的 audit/fund 公网后台请求在本网拖 install（--no-audit --no-fund 规避，与 BinFlow 无关）。

## 6. 总裁定

# **PASS**（M10 终验通过——1 个 P0 缺陷在验中抓获并已修复复验；无未修复阻塞项）

- **L01~L30**：28 ✅ + 2 ⚠️（L05/L22——均为 PRD 文面 vs as-built 的登记态分歧〔D-1/D-6〕，行为面安全且实证达标），0 ❌。
- **真实客户端矩阵**：go/dotnet/cargo/npm/maven/pip/docker/curl 八面全绿（nuget live 公网腿环境性降级，hermetic 真客户端全链替代+T-287 票期证据）。
- **闸门**：lint 0 / gofmt 空 / M7 矩阵 0 偏差 / M10 矩阵 0 偏差 / invariant 0 偏差 / ledger PASS / make docs 产物一致 / Playwright m10 12-0。
- **make test**：修复前 28/28；修复后两轮满载墙钟抖动（隔离复跑+全包复跑全绿，归因机器负载，非回归）。
- **DoD 八条**：1~7 全 PASS（带登记项），第 8 条归 conductor 打 tag。
- **修复清单（FAIL 项）**：无未修复项。登记项汇总：D-1/D-2/D-7（PRD 措辞勘误→PM 随转正）、D-3（nuget 文档命令形态→tech-writer 一行）、D-6（matrix_params 开关二选一处置→conductor）、D-8（boot footprint 瘦身→M11 候选）、D-9（e2e seed 竞态+verifyM10 口令→测试基建票）、ROADMAP 4 处弱登记补词（DoD-7）。

## 7. 清理

- scratch 实例（18180/18181/18182/1819x/coldstart*）进程已停、端口已释放；fake 上游（:18199）/TCP 探针代理（:18198）已停。
- MinIO 容器 binflow-mpu-minio 已 rm -f；dind 无残留（--rm）。
- git worktree /tmp/t297/wt 已移除；主树 verifykey.go 已还原（git status -- internal cmd clean）。
- /tmp/t297*（工具/钥对/数据目录）会话后清理；测试钥对与 license 文档仅存 /tmp 未入仓（NFR-S56）。
- 未 git 提交任何内容；docs-site build 产物变化为 `make docs` 重建的预期工作产物（T-296 起即 dirty，与源一致）。
