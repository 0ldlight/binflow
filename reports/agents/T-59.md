# T-59 工作日志：M3 三协议 + remote/virtual 逆向规格

- ticket: T-59
- 角色: reverse-engineer
- 日期: 2026-08-19
- 状态: done（待主会话 review/qa）
- area: docs/reverse/maven-npm-pypi.md（新）、docs/reverse/repo-semantics.md（§7/§8 扩编 + §9/勘误）；只读 reverse-src 与 OSS 源码，无代码改动

## 产出

1. **docs/reverse/maven-npm-pypi.md**（清单 M3 项，新建）：
   - Maven 2：端点表（存储命名空间 + `/api/maven/calculateMetadata|generatePom`）、RepoLayout 六字段 + maven-2-default 正则（一手 config 模板）、snapshot 上传改写三行为（unique 的 buildNumber/timestamp 推导链）、maven-metadata.xml 服务端计算（触发时机表 + 版本组/SNAPSHOT 目录两套内容规则 + RTFACT-6242 不删保护）、checksum 伴随（含 remote 永不回源 checksum 的私有行为）、virtual 合并摘要、官方差异 5 条。
   - npm：30 行端点表（`/api/npm/{repoKey}` 全表面，含 scoped `%2f` 变体与 `-rev` 假成功端点）、存储布局（`{name}/-/*.tgz` + `.npm/` 元数据旁车 + `npm.*` 属性）、publish 十步校验链（含 403 republish 拒绝与 sha512/sha1 双校验）、SLIM/FULL Accept 协商、remote tarball URL 四分支重写、virtual putIfAbsent 合并 + `<v>-cache` 缓存（TTL≥600s 才启用 + 120s 单飞锁）、官方差异 6 条。
   - PyPI：端点表（`/api/pypi/{repoKey}`，含 devpi 风格 scoped 形态与 302 补尾斜杠）、simple HTML 逐字节形态（api-version=2 头、anchor 模板、私有 `rel="internal|external"`）、ETag 折迭哈希算法、upload multipart 字段表（`:action=file_upload` 严格校验、md5_digest 可缺失）、哈希算法选择表（索引 `#sha256=` 优先 / 上传仅 md5）、`.pypi/` 布局 + PEP 503/427 双 normalize、virtual 格式回退合并 + 8h miss 负缓存、官方差异 6 条。
   - 对 BinFlow M3 校准建议 7 条 + 待验证 5 条。
2. **docs/reverse/repo-semantics.md 扩编**：
   - §7 remote：配置字段默认值表（20+ 字段，OSS 一手默认）、pull-through 六步流程（负缓存→本地缓存→HEAD 探测→过期回发→assumed-offline 静默）、单飞下载与并发回退、四值 checksum 策略表、上游故障矩阵。
   - §8 virtual：四桶搜索序（priority local/cache → priority remote → 非 priority local/cache → 非 priority remote）、写路由（defaultDeploymentRepoRef / 405+`Allow: GET`）、**per-protocol 聚合差异表**（Maven 现算不缓存 / npm 缓存合并 / PyPI 收集合并）、`<key>-cache` 隐藏仓布局。
   - §9 增补 M3 差异清单；**两处 M1 勘误**（includes/excludes：下载 404/上传 409；snapshot policy 拒绝确定 409）；关闭 M1 待验证 #4（`-cache` 命名已验证）；新增 M3 待验证 3 条。

## 关键证据摘要

- npm 端点与发布链：`c.j.ph.npm.resource.NpmResource`（JAX-RS 注解全量）、`NpmPublishCommand`（校验顺序/错误码逐分支）、`NpmDistTagCommand`、`NpmLocalMetadataBaseCommand`（ETag=包文档 sha1、tarball URL 三级解析兜底）、`NpmRemoteBaseCommand#replaceTarballUrl`（四分支）、`NpmVirtualMetadataMergerHelper`（缓存路径/TTL/锁）、`NpmGetPackageMetadataController#mergeNpmPackageMetadata`（putIfAbsent）。
- PyPI：`PypiResource`（端点全量）、`PypiFormRequest`+`PypiResourceUtils`（表单字段/`:action`/302）、`PypiHtmlIndexEntry`（anchor 模板与转义）、`PypiEtagResolver`（折迭哈希）、`PypiUtils`（`.pypi/` 路径族 + 双 normalize + sha256/md5 选择）、`PypiSystemProperties`（miss 负缓存 28800s/50000 默认值）、`PypiGetPackageIndexController`（格式回退合并）、`PypiRegistry`（packagereroute：pypi.org→`/api/pypi/` 前缀映射）。
- Maven（OSS 源码）：`MavenMetadataCalculator`（版本组/快照两套规则、RTFACT-6242）、`MavenMetadataCalculationInterceptor`（触发时机同步/异步表）、`UniqueSnapshotVersionAdapter`（buildNumber 推导与 classifier/pom 复用）、`MavenNaming`（unique snapshot 正则、metadata 命名）、`MergeableMavenMetadata`+`MavenMetadataInterceptor`（virtual 合并、foundByPriority 短路）、`MavenResource`（REST 端点与错误文案）、`config-templates/artifactory.config.xml`（maven-2-default 正则一手）。
- remote/virtual（OSS 源码）：`RemoteRepoBase#getInfo/internalGetInfo/getRemoteResource/downloadAndSave/getChecksum`（流程主干）、`RemoteRepositoryRepoTypeConfig`（默认值一手）、`ChecksumPolicy*` 四实现、`VirtualRepositoriesResolver#assembleSearchRepositoriesList`（四桶）、`VirtualRepositoryRepoTypeConfig`+`VirtualCacheConfig`（defaultDeploymentRepoRef、virtualRetrievalCachePeriodSecs=600）、pro `UploadServiceImpl#sendInvalidTargetRepositoryError`（405+Allow: GET 逐字）、`RepositoryServiceImpl`（`key+"-cache"` 拼接）。
- 错误信封：pro `PackageExceptionMapper` + `ErrorResponse` → `{"errors":[{status,message}]}`；`PhExceptionMapper` 状态码族。
- 官方规范交叉（WebSearch 佐证，写入规格头部引用）：Apache Maven metadata、npm registry HTTP API（RESOURCES/package-metadata）、PEP 503/592/691、PyPI Upload API（含 twine≥6.2 移除 md5_digest 的兼容性要点）。

## 置信度分布（按规格条目计，约数）

- maven-npm-pypi.md：高 ≈ 72 / 中 ≈ 18 / 低 0（未定性的不写）。
- repo-semantics.md §7–§9：高 ≈ 30 / 中 ≈ 6 / 低 0。
- 两处 M1 勘误均由代码显式常量/构造参数支撑（409、404/409 双值）。

## 遗留 / 待验证

- 规格内「待验证清单」共 8 条（M3 协议 5 + repo 语义 3），集中在：`-rev` PUT 无副作用确认、PyPI JSON simple 仓库级开关、unique snapshot 对拍、virtual DELETE 遍历、上游 5xx 分支、cache 仓直访权限。
- npm/PyPI 的 SSO（`/-/v1/login` web auth）与 audit/attestations 端点仅列表面，M3 无需实现，未深挖。
