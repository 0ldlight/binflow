Ticket:       T-L003-4 LOOP 003 收口前两小修 — repo 侧 TTL 双消费接线（ADR-0012 勘误三补全）+ E7 backup 成功臂 cmd 接线；P0/P1
Role:         dev-go-core（领域实例：internal/repo + internal/httpapi/backup 接线 + ticket 明示的 cmd 3 行）
Area:         internal/repo（config.go / dockerremote.go + 测试）；cmd/binflow-server/main.go（E7 的 Deps.BackupRunner 一行，ticket 明示"cmd 接线"即本修）
Input:        派发令（LOOP 003 收口前两小修，补丁草图= reports/agents/T-L003-1.md Compatibility 字段，Reviewer A 已验可落地）；internal/remote/ttldefaults.go（单点解析 DefaultContentTTLSecondsFor / ResolveContentTTLSeconds，引擎侧 fetcher.go:1303 loadRepo 已消费）；reports/compatibility/L003-storage-admin-diff.md E7 臂（成功臂 = Deps.BackupRunner seam 触发 + 200 text/plain，cmd 接线未落）
Changes:      ① internal/repo/config.go：parseRemoteConfig 建仓默认 retrievalCachePeriodSecs 由硬编码 7200 改调 remote.DefaultContentTTLSecondsFor(packageType)（不复制常量；显式值仍由既有 0-as-absent 循环直通）；删除已无引用的 defaultRetrievalCachePeriodSecs 常量并更新注释块（lint unused 门驱动）
              ② internal/repo/dockerremote.go 两处兜底接单点：remoteUpstreamCore 的 contentTTL 兜底（原 7200）改 remote.ResolveContentTTLSeconds(cfg.ContentTTLSeconds, row.PackageType)——row 在作用域内已有；remoteContentTTL（cache 行窗口的读点）改经 GetConfig+Repos().Get 取 explicit 与 row.PackageType 后调 ResolveContentTTLSeconds（行读取失败时 pkgType 空串落 generic 兜底）。注：派发令列"460 附近"一处，落地时发现 136-139 同病（同一 7200 兜底、仅 docker/helmoci 可达、正是包型默认目标域）一并接单点——root-cause 口径
              ③ cmd/binflow-server/main.go newAssembledServer：Deps 装配尾部加 deps.BackupRunner = &backupRunner{cfg, md: stack.md, log}（schedule_wiring.go 既有载体，exportSnapshot 同核——one-carrier law；scheduler Register 处保持原构造，无状态载体双实例同核无漂移）。E7 成功臂由 503 honest（seam nil）转为 200 "backup 'k' scheduled" + detached Run
              ④ 测试三件套（新建 remote_content_ttl_test.go，行为命名）+ t392 旧断言翻新（见 Tests）
Files:        新增：internal/repo/remote_content_ttl_test.go、reports/agents/T-L003-4.md（本日志）
              修改：internal/repo/config.go、internal/repo/dockerremote.go、internal/repo/t392_docker_remote_test.go、cmd/binflow-server/main.go
              删除：无（常量 defaultRetrievalCachePeriodSecs 随 ① 移除）
Tests:        remote_content_ttl_test.go 三套 table-driven（外部包 repo_test，公共 seam）：
              - TestRemoteCreateContentTTLDefaultPerPackageType（7 例）：建仓不传 TTL → docker 落 21600、maven/npm/generic 落 7200；显式 3600/7200(legacy)/21600 直通不改写；每例另断言 remote.ResolveContentTTLSeconds(stored, pkg) == want——建仓写值是引擎解析的不动点（建仓→引擎读数一致的逐例证明）。helmoci 无行：svc.CreateRepo 合法集（validate.go 静态五型）不含 helmoci，其解析腿由下两套覆盖，建仓分派分支与 docker 同一函数
              - TestRemoteUpstreamContentTTLResolution（4 例）：docker/helmoci 行 0（未设/手毁）→ RemoteUpstream.ContentTTLSeconds 21600；显式 7200/3600 直通；每例断言与单点解析等值
              - TestRemoteContentCacheWindowMatchesEngineResolution（2 例）：LandRemoteBlob 落地后 GetCache 窗口（ExpiresAt-FetchedAt）== ResolveContentTTLSeconds(stored, docker)——docker 行 0 → 窗口 21600，显式 7200 → 7200
              t392_docker_remote_test.go：TestT392DockerRemoteCreateCommunity 的 content TTL 断言由 7200 翻为 21600（旧钉子是被勘误三推翻的平铺默认，注释注明勘误三与单点来源）
Commands:     go build ./...；go vet ./internal/repo/... ./internal/httpapi/... ./cmd/binflow-server/...；gofmt -l internal/repo internal/httpapi cmd/binflow-server（config.go 曾报对齐，gofmt -w 后复跑为空）；$(go env GOPATH)/bin/golangci-lint run internal/repo/... internal/httpapi/... ./cmd/binflow-server/...；go test ./internal/repo/ -run 'Remote|TTL|Docker' -count=1；go test ./internal/httpapi/ -run 'TestStorage' -count=1；go test ./cmd/binflow-server/ -count=1；go test ./internal/repo/ ./internal/remote/ -count=1；go test ./internal/httpapi/ -count=1；go test -race ./internal/repo/ -run 'TestRemoteCreateContentTTL|TestRemoteUpstreamContentTTL|TestRemoteContentCacheWindow|TestT392' -count=1
Outputs:      build/vet rc=0；gofmt -l 输出空；golangci-lint "0 issues."；repo 指定套件 ok 11.2s（改前基线 ok 12.1s）；httpapi TestStorage ok 6.4s（基线 ok 7.7s）；cmd 全包 ok 16.2s；repo+remote 全包 ok 69.8s / 35.4s；httpapi 全包 ok 144.4s（T-L003-1 期挂账的 4 个 TestV2* 失败已随 L003-2 在途改动愈合，全套绿）；race 子集 ok 5.5s
Compatibility: TTL 三口径端到端成立证明（建仓→引擎读数一致）：口径一 建仓默认——CreateRepo(docker, 无TTL) → remote_configs.content_ttl_seconds=21600（TestRemoteCreateContentTTLDefaultPerPackageType 实测），引擎读数= fetcher.go:1303 loadRepo 的 ResolveContentTTLSeconds(21600,"docker")=21600，写读同值（测试内逐例断言不动点）；口径二 显式直通——显式值经既有 0-as-absent 循环原样落列，引擎 explicit>0 原样返回（3600/7200/21600 三例实测）；口径三 存量不回改——无任何写回路径/迁移，存量 7200 行引擎按 explicit 保留（"docker explicit legacy 7200 is never rewritten" 实测），未设/手毁行(<=0)两侧（repo remoteContentTTL 与引擎 loadRepo）经同一 ResolveContentTTLSeconds 落同一包型默认。E7：错误臂（缺 key 500 信封/disabled 500）不动；成功臂由 503 → 200 text/plain "backup 'k' scheduled" + detached export（对齐 L003-storage-admin-diff.md E7 臂与 T-L003-1 Compatibility 草图），diff 报告 E7 PARTIAL 的"cmd 接线未落"一条就此清偿
Security:     无新输入面；backup 端点门禁/信封不动（handler 仅从 seam-nil 503 臂转为 seam 实装，权限臂原有）；TTL 解析无凭证接触；无日志脱敏面变化
Performance:  remoteContentTTL 新增一次 Repos().Get 读（sqlite 主键读，落地路径每 blob 一次，量级不变）；无其他影响，无测量
Risks:        ① service.go:2264/2631 注释仍写"product default (7200)"（描述 ContentTTLSeconds 列写值）——本票边界未含 service.go，注释陈旧但代码正确，建议后继票顺手更新 ② UpdateRepo 全量替换语义下，docker remote PUT 不带 retrievalCachePeriodSecs 会把存量 7200 重规范为 21600——与 T-80 全量替换先例同构（改前同样会把任意值重写为 7200），非新风险但口径值得在契约票里写明 ③ helmoci 建仓路径因 svc 合法集不可测，引擎侧 remote/ttl_revalidate_test.go 的包型表 + 本票解析面测试共同覆盖
Blockers:     无
Next:         ① conductor：E7 臂差分复验（装配实例 curl POST /api/system/storage/backup?key= 期望 200 流式调度行）② 契约票：TTL 三口径（建仓包型默认/显式直通/存量保留 + PUT 全量替换的重默认语义）冻结进 contracts/ ③ service.go 两处陈旧注释随下一张 internal/repo 票顺手更新 ④ LOOP 003 收口
