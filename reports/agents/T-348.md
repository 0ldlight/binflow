# T-348 工作日志 — FR-112.2/112.3 规格回写 + 升置信 + D-G/D-H 校验

- 角色：reverse-engineer；日期：2026-08-29；状态：done
- area：`docs/reverse/cargo.md` + `docs/reverse/conan.md` + `docs/reverse/repo-operations.md`（顺带票面第 3 项）；纪律：共享工作树零 git 操作；reverse-src 只读未改
- 前置素材：T-318（cargo virtual as-built + 遗留#1 回写清单）/ T-316（CG-2 全表 + §8.1）/ T-308（conan D1/D2/D5/D7/D8 登记）/ T-312（remote/virtual as-built + 遗留②）/ T-340（D-F 修正 + D-F2 登记）/ T-343（V-1 定案）/ T-339（V-3 无新证据确认）

## 1. 回写清单

### 1.1 cargo.md（FR-112.2）

| # | 位置 | 内容 | 依据 |
|---|---|---|---|
| C1 | §8 virtual 行 | 整行 as-built 回写：索引首见去重（键 = name + 忽略 build metadata 的 vers，行字节逐字保留 + SemVer 排序）/ 合并校验器（ETag=渲染体 sha256、Resolved-From=首见成员）/ download first-hit 每请求解析 / yank 双持有者（local 翻转、remote 405、未知 404）/ search local 事实行 + remote 标记路径首见胜并集 / config.json 合成自指 virtual、写 403 / publish 写路由三别名 + 未配置 C5 405（Artifactory 400 → BinFlow RE-08，差异登记）/ 裸写先换址再收敛 / 成员失败容忍策略 | T-318 §1.2/§6-1 + 差异 2/3/4/6 |
| C2 | §8.1 | 表改题「remote/virtual 实现差异登记（T-316/T-318）」+ 新增 **R-7**（virtual search max_version 取首见成员行，不随后成员抬升） | T-318 差异 4 |
| C3 | §12 裁决① | 标记已裁（首见去重，T-318；真机侧证 cargo 1.98 合并索引解析+构建） | T-318 §3 |
| C4 | 待验证清单 1 | 关闭（BinFlow 不产重复行，混合行集真机侧证——比原验证目标更强形式） | T-318 §3 |
| C5 | §10 S5 | 中（去重低）→ 高（as-built）；注 Artifactory 裸拼接不采纳 | T-318 |
| C6 | §5.3 表头 | 补精确代码锚点：类 4/5 的 200+warnings 载荷 = `CargoLocalRepoHandler` 中 `Failed to publish with error '<msg>'` 拼装入 warnings（本轮 reverse-src 复核定位，batch2-protocol/org/jfrog/repomd/cargo/rest/handler/CargoLocalRepoHandler.java:183） | 本轮复核 |

**§5.3 十一类锚定表核对结论（票面第 2 项）**：逐行对照 T-316 §1.1 实现清单与 as-built 列——11 行全部一致（类 1/3/6 权限族 401/403+信封；类 2 无 409 臂覆盖；类 4/5 200+warnings.other 且副作用失败不翻转；类 7 500+信封；类 8 409 硬错误/404 unfound 双臂；类 9-11 同形），零偏差，无需修改；仅补 C6 锚点强化。

### 1.2 conan.md（FR-112.3 升置信批 + D-F 联动 + T-312 as-built）

| # | 条目 | 内容 | 依据（升置信出处） |
|---|---|---|---|
| N1 | D1（§1+§2） | 握手三端点**双前缀**（{v1,v2}/ping、{v1,v2}/users/{authenticate,check_credentials}）；conan 2.31.2 实测走 `v2/users/authenticate`（原「必走 v1」口径修正，社区 issue #17001 即此事）；§1 补「握手类无关、remote/virtual 同服务」（消除 §1/§2 原矛盾） | T-308 §3 真机 + T-312 D1（remote login 真机）；置信度 高 |
| N2 | D2（§3.1 search 行，顺带） | 尾缀 `/*` 为客户端修订通配、服务端须剥离后匹配 | T-308 D2（conan 2.31.2 活体：不剥离 0 命中）；T-308 遗留#2 明列交 reverse-engineer |
| N3 | D5（§3.3 reindex 行） | Artifactory **异步**语义升格显式代码锚（reindexRepo 日志 "Starting asynchronously re-index"、AQL 候选扫描后逐包事件入 work queue，REST 200 不等完成）；BinFlow as-built **同步** + 文案 `Calculated Conan index for repository '<key>' (path '<p>'): N revisions reindexed.`（无规格锚，as-built 固化）——差异登记 | 反编译 ConanAddonImpl/ConanServiceImpl（本轮复核）+ T-308 D5 |
| N4 | D7（§4 + S7） | `conan.recipe_hash` / `<ref>/search` 的 `recipe_hash` = **rrev**（hash 模式下 RREV 即 recipe 摘要；原「解析 conaninfo.txt 所得」括注矛盾修正——conaninfo.txt 不含 recipe hash） | T-308 D7 as-built + 测试固化 + 语义推导；置信度 高 |
| N5 | D8（§3.2 v1 DELETE 行） | **读法翻转**：~~latest 修订链~~ → **坐标根整树删（全部修订）**。双证：① Artifactory `LocalConanHandler.removeRecipe` 对 `getRecipePath(user/name/version/channel)`（无修订段）直接 repoService.delete，权限门 canWrite → 403；② conan 1.66 参考实现 remove_conanfile → delete 整个 revisions 根（controller docstring 明言 "remove all revisions…"）。v2 无修订 DELETE 同型互证（`getRecipePathForRemove`：rRev 空 → 坐标根）。**BinFlow as-built（latest 链）登记为分歧，待翻转票（T-308 预估一行改动+断言）** | 本轮 reverse-src 复核（batch2-protocol/org/jfrog/repomd/conan/rest/conans/LocalConanHandler.java:311 + common/ConanPaths.java:27）+ /tmp/t340-venv conan 1.66 参考实现源码；置信度 高 |
| N6 | D-F（§3.2 packages/delete 行） | 补幂等语义注记：混批 [live,stale] → 200 空 body（无树 pid 静默跳过）；未知 ref 404 `Path not found`；非法 pid 400。**三方对照**：Artifactory 对无树 pid 答 404 `Couldn't find packages for deletion`（先到 pid 已删——「删树成功但回 404」在 Artifactory 同在）且跨全部 rRev；参考实现静默 200（跨全部 rRev）；BinFlow 幂等 200 + latest 模型不动（差异登记） | T-340（D-F 修复 + conan 1.66 真客户端）+ 本轮反编译复核（LocalRepoHandlerCommon.removeAllPackagesByPackageId：rRev="*" 枚举、空候选 → 404）；置信度 高 |
| N7 | T-312 遗留（§7 两行） | remote/virtual as-built 回写：引擎链/UpstreamPath 翻译/marker 文档（.files.json/.search.json）逐字服务/search 不代理 404（差异登记：cargo 已实现查询代理，conan 未跟进）/写拒绝 405/缓存落点自身命名空间（Artifactory -cache 仓惯例不采纳）；virtual 归并去重/清单并集/pid 行并集/search 仅 local 成员/写路由 C5 405（Artifactory 400 → BinFlow RE-08）/写登记尾成员定向防拷贝 | T-312 §1/§5 + conan 2.31.2 真机（remote install 全链 / virtual 聚合读 / routed 写归并立即可见）；置信度 中 → as-built 高 |
| N8 | §10.1 + §1 | CN-1 终裁状态注记（v1 全量十七端点 + files 通道已落地，conan 1.66 真机；原「不做完整 v1」保留为沿革） | BOARD CN-1 终裁 + T-308 |
| N9 | 待验证清单 1/2/3 | 全部关闭：①`_` 占位（conan 1.66 wire 恒 `_/_` + 2.31.2 链）②virtual install 全链（T-312 真机）③latest 延迟（BinFlow 同步登记，真机立即可见；Artifactory 异步维持代码观察） | T-340 §3 / T-312 §3 / T-308 §3 |

### 1.3 repo-operations.md（顺带票面第 3 项）

| # | 条目 | 内容 |
|---|---|---|
| R1 | V-1 定案同步（§0 行 8 / §4.2 / §7 / §8 四处） | explode 成功码定案 **201 空体**（规格优先级：官方文档 > 反编译隐式默认；无活体 Artifactory 可达，按文档定案留痕，异值再翻转）；BinFlow as-built 201 + `X-Binflow-Exploded-Files: <n>` 计数头（BinFlow 新增） |
| R2 | V-3 注记（§1.5） | 无新证据（T-343 未触及 copy/move 面；T-339 按代码证据取大写实现并测试固化）——**维持待验证**，Artifactory live 验证仍开放 |

### 1.4 D-G/D-H 校验（AC2）

- **grep 零残留**：`暂不可用`/`声明性 local-only`/`不用 200+errors`/`暂未交付`/`证书动作未落` 在 docs/user/ 全部零命中（T-329-DGH 已回刷，本轮复核通过）。
- **api-reference SAML 三端点**：`saml/config/key/public`、`.../regenerate`、`POST saml/key` 三行在档（docs/user/api-reference.md L279-281）。
- **docs/user/integrations/cargo.md ↔ reverse 面一致性逐项对照**（D-G 校验核心）：virtual 去重键/首见行逐字/SemVer 序/Resolved-From/max_version 首见（R-7）/yank 双持有者/DELETE 405 不传播/publish 未配置 405 措辞、remote 409 硬错误与 404 unfound 双臂/TTL 缓存窗口/config.original.json、CG-2 双轨报错对照、手写索引 D-5 收敛与「删后重发」两步顺序——**与回写后的 reverse §8/§8.1/§5.3 全部一致，零修正项**。
- `make docs`：**SUCCESS**，4.00 MB（4189229 字节，与 T-329-DGH 轮逐字节相同——reverse 规格不进用户站，零断链零内容漂移）。

## 2. 置信度变化统计

| 类别 | 数量 | 条目 |
|---|---|---|
| 中 → 高（as-built/活体） | 5 | cargo §8 virtual、cargo S5、conan §7 remote、conan §7 virtual、conan S11 |
| 中 → 高（读法修正后重立） | 1 | conan §3.2 v1 DELETE（latest 链 → 整树删，双证） |
| 高维持 + 证据补强/补臂 | 5 | cargo §5.3（精确代码锚）、conan §2 authenticate（双前缀活体）、conan §3.1 search（尾缀剥离活体）、conan §3.3 reindex（异步代码锚）、conan §3.2 packages/delete（三方取证 + Artifactory 404 臂） |
| 修正（原文矛盾消除） | 2 | conan §4/S7 recipe_hash 来源（→ rrev）、conan §1↔§2 握手矛盾（类无关服务） |
| 低/待验证 → 关闭 | 4 | cargo 待验证 1；conan 待验证 1/2/3 |
| 分歧定案 | 1 | repo-operations V-1（201，四处同步） |
| 维持待验证 | 2 | V-3（level 大小写）、Artifactory `.timestamp` 覆盖阈值默认值 |
| 新登记差异 | 4 | cargo R-7（virtual search max_version 首见）、conan D8 BinFlow latest 链 as-built（待翻转票）、conan packages/delete 三方差异（Artifactory 404 臂 / 跨 rRev）、conan remote search 不代理（cargo 已跟进对照） |

## 3. 本轮新取证（票日志之外的增量证据）

- cargo 类 4/5 wire 串代码定位：`CargoLocalRepoHandler.java:183`（`Failed to publish with error '%s'`）。
- conan D8/D-F 反编译直读：`LocalConanHandler.removeRecipe`（getRecipePath 无修订段 + canWrite 门）、`LocalRepoHandlerCommon.removeAllPackagesByPackageId`（rRev="*" 枚举、空候选 404）、v2 `ConanV2LocalRepoHandler.getRecipePathForRemove`（rRev 空 → 坐标根）互证。
- conan 1.66 OSS 客户端内置参考服务器源码（/tmp/t340-venv，T-340 遗留环境仍可用）：`rest/controller/v1/delete.py`（docstring "remove all revisions"）、`service/common/common.py`（remove_packages 跨全部 rRev）、`store/server_store.py`（remove_conanfile → delete_folder(revisions 根)）。
- conan reindex 异步链：`ConanAddonImpl.reindexRepo` → `ConanServiceImpl.reindexRepoAsync`（AQL 扫描 + 事件入队）。

## 4. 遗留 / 建议 conductor 派发

1. **conan D8 翻转票（P2，dev-go-core 小票）**：v1 `DELETE conans/<ref>` 由「删 latest 链」翻转为「坐标根整树删」（T-308 预估一行改动 + 断言；v2 无修订 DELETE 需同步核对实现口径——规格 §3.1 该行本就写「全部修订」）。
2. **conan remote search 代理**（P3 候选）：cargo 已用查询键标记路径实现上游 search 代理，conan 维持 404 诚实文案——如需对齐可复用同手法。
3. D-F2（v1 files 通道路径双拼，T-340 §4 登记）仍开放，归 FR-110.1 邻域。
4. 纪律说明：本轮手工编辑仅 `docs/reverse/*.md` 三文件 + 本报告；`make docs` 产生的 docs-site/build、internal/docs/dist 为构建工作产物（T-328/T-329-DGH 谱系同源），非手工编辑面。

## 5. 自测命令

| 命令 | 结果 |
|---|---|
| grep D-G/D-H 旧措辞（docs/user/ 全树） | 零命中（exit 1） |
| `make docs` | SUCCESS，4.00 MB（4189229 bytes），断链告警 0 |
| 编辑后表格行管道数抽查（cargo/conan 各改动行） | 列数全部与表头一致 |
