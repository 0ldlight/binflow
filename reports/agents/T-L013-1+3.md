# T-L013-1+3 — 弹窗走查批 + 21 Pro 屏补拍（LOOP 013 合并票）

```
Ticket:        T-L013-1+3 [P0] 弹窗走查批 + 21 Pro 屏补拍 + 管线收口（dev-frontend，单浏览器串行）
Role:          dev-frontend（area: tools/penpot-sync/ + docs/reverse/frontend/parity-capture/ 产物域）
Input:         states-gaps.md（Phase A 12 行台账）；penpot-catalog.json missing 21 屏；capture.mjs 既有模式（系统 Chrome 通道/原生 setter 登录/SCREENS_CHUNK/settle ping 门）；run-log domSummary（顶栏按钮 "Platform/Administration/A"=头像锚）；T-PENPOT-A 套件交接
Changes:       ① dialogs-walkthrough.mjs（新）：11 项 gap 逐个 UI 触发取证（WALK_GAPS 子集重跑 + 台账合并重写 states-gaps.md）；② capture.mjs +2 env（SCREENS_SLUGS 精确补拍防覆盖好 PNG / SKIP_GAPS 防补拍跑清掉台账）；③ 三轮 DOM probe 定选择器（AG-Grid 行 kebab "Row actions"、el-drawer #release-bundle-name/version/signing-key、el-form-item 标签定位表单域、侧栏过滤框 placeholder="Search"）后按证据收敛脚本；④ build-spec.mjs DIALOG_OWNERS/STATE_OWNERS 增 13 弹窗 + 7 状态映射；⑤ inject.mjs openWorkspace/login 适配 Penpot 2.17（h2 locator-filter 防竞态、项目内新文档 = a:has-text("+ 新文档") 非 testid、登录重试一次）；⑥ README.md 补 Phase B 走查手册节
Files:         新增 tools/penpot-sync/capture/dialogs-walkthrough.mjs、reports/agents/T-L013-1+3.md；修改 capture.mjs / build-spec.mjs / inject.mjs / capture/README.md；产物增量 docs/reverse/frontend/parity-capture/{screenshots/screens +13, screenshots/dialogs +13, screenshots/states +8, states-gaps.md 重写, walkthrough-results.json, dom-snapshots/walkthrough-*.json +5}；tools/penpot-sync/{penpot-spec.json, penpot-catalog.json} 重建、verify/ +4 截屏。删除：probe-dom.mjs/probe3.mjs/probe4.mjs（一次性诊断，证据存 dom-snapshots）
Tests:         无 e2e spec（本票为参照实例取证票，产物即证据）；自证链 = walkthrough-results.json 逐 gap status + 清单对账 + inject FINAL STATUS 行 + verify 页清单
Commands:      造数：curl PUT /artifactory/api/security/users/audit-probe-user (201)、PUT /api/build (204)；补拍 ×3：PHASES=login,screens,catalog SKIP_GAPS=1 MAX_SHOTS=30 SCREENS_SLUGS=<7+7+7> node tools/penpot-sync/capture/capture.mjs；走查 ×3 轮：ARTIFACTORY_USER=admin ARTIFACTORY_PASSWORD=… [WALK_GAPS=…] node tools/penpot-sync/capture/dialogs-walkthrough.mjs；spec：node tools/penpot-sync/build-spec.mjs；注入：PENPOT_USER/PASS（eval 解析 ~/.credentials.env，勿落盘）PENPOT_PROJECT=binflow [PENPOT_FILES_URL=…] node tools/penpot-sync/inject.mjs inject|verify
Outputs:       走查台账 12 行=cleared 8 / partial 2 / open 2（+bonus retention 表单 cleared）；屏 21 访全、新 PNG 13（8 屏指纹同形去重 duplicateOf 记账）；spec 10 页 73→106 板（+33）、图 70→103、catalog captured 48→61/69；注入 FINAL STATUS: DONE pages=10 boards=106 images=103 in 39s；verify 页清单 10/10 在 + 4 截屏（56-142KB 非空白）
Compatibility: 契约面新证据（reverse 票输入）：repositories 列表=AG-Grid 非 table（.ag-row + hover 出 "Row actions" kebab）；release bundle 创建入口在 /ui/builds 非 release-bundles 页；创建器=el-drawer 双步（1 Release Bundle Details / 2 Builds Selection）+ 签名密钥必选（实例 keypair=[] → 流程硬阻塞）；用户表单域靠 el-form-item 标签（无 placeholder）；7.161.20 无主题切换控件（profile 仅 Identity Tokens/SSH keys 两 tab）
Security:      凭据零落盘（Artifactory 走 env；Penpot 从 ~/.workspace 外 mode-600 文件 eval 进 env，命令行与产物无值）；登录错误态 0 新增尝试；audit-probe- 资产三轮 cleanup 后 REST 复核零残留（repos/users/builds 全空、bundles none）——记录于 walkthrough-results.json cleanup 字段
Performance:   单浏览器串行全程；补拍按 3 块（7/7/7）+45s 冷却；ref 中途 502/503 两次 → 停等恢复（一次 ~3min 自愈、一次 JVM 256%CPU/5.2GB 打满宿主后退出 255 并拖垮 Penpot，Docker 环境整体重启后 Penpot 复活）
Risks:         ① ref 实例当前 DOWN：本票收尾时 artifactory 容器 tomcat 启动失败（日志 ${jf.product.home} 未展开致 lib 目录缺失），docker restart 一次未愈（503 持续）——实例级故障需人工介入（可能是 docker 环境重启后 env 注入丢失）；② Penpot 残留两个空壳项目（新建项目 1/2，inject 竞态失败 + 一次清理未遂反造一个）——空项目无数据，dashboard 选项菜单手工可删；③ 8 屏去重靠指纹（标题+Tab+列头），如需逐屏独立板可 SCREENS_SLUGS 强制补拍；④ spec/board 为静态证据板，非可编辑组件树
Blockers:      无（两项风险均为环境项，不阻塞本票交付物）
Next:          ① ref 实例修复（人工查 docker 环境重启后 compose env 注入，${jf.product.home} 变量展开失败）；② Penpot 手工删两空项目；③ dark-mode 锚定建议：BinFlow 深色模式以自有 token 基线为准（7.161.20 无 UI 切换控件实证，walkthrough-probe2.json）；④ release bundle 流程完整取证需先在实例配 GPG keypair（安全配置变更，须用户/架构师拍板）；⑤ sso-login-variants 需实例接 SSO 才可清（永久 open 或从台账降级为 out-of-scope）
```

## 断点快照（如执行会话中断）

- 已完成：三项任务全部收口（走查台账 8/12 cleared；21 屏补拍 + 13 新 PNG；spec 重建 106 板；Penpot 重注入 DONE + verify 通过）。
- 残余（环境项非票项）：ref 实例 DOWN（tomcat lib 路径 env 未展开）；Penpot 两空壳项目待手工删。
- 未 commit（票面纪律）；BOARD 未写（票面纪律）。
