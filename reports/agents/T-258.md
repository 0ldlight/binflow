# T-258 工作日志 — repos 页「已用」列单请求注水（E1 usage 批量消费，~171 请求 → ≤3）

| 项 | 值 |
|---|---|
| 票据 | T-258 [P0]（BOARD M9 B5；AC = PRD FR-79 / NFR-P39 / N06 扇出面） |
| 角色 | dev-frontend |
| 日期 | 2026-08-24（17:02 被 API 配额击落 → 21:05 后配额恢复续作，见 §0） |
| area | `web/src/pages/repositories/RepositoriesPage.tsx`（已用列注水）+ `repositories.css` + `web/src/lib/repos.ts`（E1 数据源）+ `web/e2e/m9/usage-fanout.spec.ts`（填肉）+ additive 类型/再导出面（`scripts/seed-m9.d.mts`、`e2e/m9/support/seed.ts`）。**security 零触碰、Go 源码零改动、BOARD 零改动、未 git 提交** |
| 状态 | done（本票 4 腿 e2e 全绿 ×5 轮 + 四绿 + gzip 54.4% 维持；全量 --workers=1 中本票面零失败——树内 T-257 在途改动致 2 例 users-groups 失败 + t146 例门，均非本票，见 §4） |

## 0. 击落—恢复说明

17:02 配额中断时处在只读调查阶段；恢复后 `git status web/` 核对：**零半成品代码**
（被击落发生在首次写入前）。21:05 后从实现零起点续作，票面 AC 全量收尾。另注：
本票与 T-257（users/groups N+1 退役）同树并行——T-257 全程在途（其
`web/src/pages/security/*` 与 `e2e/m9/users-groups.spec.ts` 未提交改动在共享树上持续
演进，两次全量之间其 spec 从占位长到 +336 行）；本票对其文件零触碰，§4 逐一归属。

## 1. 交付物

### 1.1 `web/src/lib/repos.ts` — E1 批量数据源

- `RepoUsageRow`（repo/usedBytes/quotaBytes + counts 形态 `nodeCount?`/`updatedAt?`）
  与 `getUsageBatch(includeCounts = false)` → `GET /api/v1/storage/usage[?include=counts]`。
- 按 T-253 遗留节建议：**默认无参全量形态**（50 仓一趟拿全；可见集服务端过滤，前端
  不判权）；counts 仅 tooltip 需要，同样单请求。`getRepoUsage`（单仓）原样保留
  （RepoDetailPage 消费面，不动）。
- `updatedAt` 语义注记随类型钉死（配置时刻，不当「最新制品时间」用——UI 不呈现该值）。

### 1.2 `RepositoriesPage.tsx` — 已用列注水（四态）

- `useUsageBatch(enabled)`：页面级**恰一次**批量请求（`?include=counts`）→
  `Map<repoKey, RepoUsageRow>` 索引；`enabled = 列表 status === 'ok'`——列表 403
  （普通 user L2 卡）/失败时**不发孤儿请求**；列表重载（删除/DeployDialog 上传收尾）
  走 loading→ok 变迁，用量随之刷新（不做跨挂载缓存：上传后陈旧值比多一趟请求更糟）。
  闭包 alive 旗标（useAsync 同款，晚到响应丢弃）。排序/筛选是纯前端态，**零触发**。
- `UsageCell` 改 prop 驱动（退役每行独立 `getRepoUsage`）：ok = mono 值 +
  `N 个文件` tooltip（E1 counts 部分收编 T-99 登记的「制品/缓存」列信息缺口——tooltip
  承载，不破坏既有列集）；加载 = 暂 `—`；失败 = 灰显 `—` 的**重试按钮**（title 携
  服务端 message + aria-label，键盘可达，stopPropagation 防触发行导航）；virtual 恒
  `—`；批量缺行 = `—`（不伪造 0）。全状态带 `repos-usage-{key}` 锚。
- 页头契约注记同步改写（逐仓拉取 → 单请求注水）。

### 1.3 `repositories.css` — `.usage-failed`

失败态重试按钮（text-2 非 muted、不用 opacity——axe color-contrast 纪律，
`.row-del` 同款）；只引 var(--bf-*)。

### 1.4 additive 面（类型/再导出，非功能性）

- `scripts/seed-m9.d.mts`：补 T-253 已导出但未声明的 `usageSeedBody`/
  `USAGE_SEED_PATH`/`seedUsageContent`（e2e TS 世界 strict 需要）。
- `e2e/m9/support/seed.ts`：再导出 `usageSeedBody`（本票 spec 的对账 oracle 与种子
  同源，杜绝公式复制漂移）。

### 1.5 `e2e/m9/usage-fanout.spec.ts` 填肉（4 腿，替换 T-250 占位）

| 腿 | 覆盖 |
|---|---|
| admin | 冷整页载入（goto 稳定后挂计数器再 reload——计数恰一个首屏）：**usage 批量恰 1 + 列表恰 1 + 退役逐仓形态零出现 + 数据面 ≤3 硬上限**；环境请求白名单钉死（whoami+version 恰一对，任何未知 /api/** 请求即失败）；50 行全注水非 `—`（无占位残留）；排序（asc/desc 首行翻转换锚）+ 筛选（10 行/回 50 行）**零再触发**（计数前后相等）；API 对账抽样（首/中/尾 vs `GET /api/v1/storage/usage` 原文 + 确定性种子字节 `usageSeedBody(key).length=29` → `29 B`）；axe（repos-page，serious/critical=0） |
| readonly_admin | 同一冷载入同帽注水 50 行（E1：admin/readonly 全量）+ 同 ≤3/恰 1/逐仓 0 + wire 对账全 50 键在集 + 只读注记可见 |
| user（u8） | repos 页 = L2 无权限卡（表格零渲染）+ 冷屏钉死（whoami+version+403 列表恰 3，**usage 面零请求**——门控注水实证）；可见集差异如实：u8 自身批量（用户可见视图将消费的 wire）200 且**恰** m9-r00..m9-r09 十行、usedBytes 全 29 |
| 四态 | route-mock 三段（挂起→500→透传）：挂起 = 列暂 `—`（表已渲染）；500 = 灰显重试**按钮**（role=button + title 携服务端 message 逐字 + axe）；重试点击 = 值恢复 `29 B` 且 URL 不变（重试不触发行导航——stopPropagation 实证） |

计数器为 spec 内自持 `trackApi`（page.on('request') 过 `/binflow/api/` 前缀；派单称
「沿 T-257 同款」，但 T-257 的计数器在其在途 spec 内、落盘时其面尚为占位——本票按
既有 page.on('request') 先例（repositories.spec 门控腿）自立同形 idiom，未建共享
helper（m9 spec 区域隔离，避免与 T-257 撞面）。

## 2. 自测证据

### 2.1 四绿（终态）

```sh
cd web
npm run typecheck   # tsc --noEmit 零输出
npx eslint .        # 零输出（exit 0）
npm run build       # assert-tokens OK；vite ✓ built in 1.52s；relink-assets 2 full URL(s) verified
# gzip（dist/assets js+css 逐文件求和）：194,862 B = NFR-P36 358,400 B 的 54.4%——维持
#   （T-260 基线 194,215 B；共享树含 T-257 在途 security 页改动，本票净增约 +0.6 KB，
#    RepositoriesPage chunk 9,066 B）
make console && make build   # console 载荷 195,918 B；bin/binflow-server 27.5 MB（go:embed 全链）
```

### 2.2 真栈 e2e（scratch 实例 :18096，seed-m8 全量 10291 节点树 + seed-m9）

```sh
node scripts/seed-m9.mjs --base http://127.0.0.1:18096
  # 50 repos / 50 usage files / 20 users / 10 groups / 4 targets in 5.4s；verify ok:true
  # （E1 探针：admin 50 行全 29B；u8 恰 10 行零泄露）
node scripts/seed-m8.mjs --base http://127.0.0.1:18096   # tree verified 10291/10291 in 77.4s

BASE=:18096 npx playwright test e2e/m9/usage-fanout.spec.ts --project=m9   # 4 passed
  # 稳定性：连跑 4 轮全绿（14.6s/14.0s/19.1s/22.9s）零 flake
BASE=:18096 npx playwright test e2e/repositories.spec.ts --workers=1       # 4 passed（m8 生命周期回归面）
BASE=:18096 npx playwright test --project=m9 --workers=1
  # 12 passed / 1 failed / 2 skipped——失败 = T-257 在途 N02 腿（§4），本票 4 腿 ✓
```

### 2.3 全量 `--workers=1`

```sh
BASE=:18096 npx playwright test --workers=1
  # 首轮（本票 3 腿态）：161 passed / 2 failed / 6 skipped (9.3m)
  # 终轮（本票 4 腿态）：161 passed / 3 failed / 6 skipped (10.1m)
```

- 终轮 3 失败逐一归属（§4）：t146 例门（非回归，下方对清）+ T-257 在途 2 例
  （m8 users-groups readonly 腿 + 其自建 m9 N02 腿；两次全量间其树在动）。
- t146 例门对清（匹配口令实例 :18097）：

```sh
BASE=:18097 ADMIN_PW=docsqa-test-pw-146 npx playwright test e2e/t146-docs.spec.ts
  # 8 passed（T-242/T-260 同款登记：例门锁专属口令，通用实例必败形态）
```

- 6 skipped = 环境门控腿（3 dind/docker-CLI + 2 oidc armed 探针 + m9/t104 条件腿，
  与 T-260 口径一致）。合并 t146 对清后有效口径 **169 passed / 0 非在途失败 / 6 env-skip**。

### 2.4 服务端 N 序列对账（curl 直证，:18096 存续期）

```
GET /api/v1/storage/usage            → 200 bare array 50 行，usedBytes 全 29
GET /api/v1/storage/usage?include=counts → 行含 nodeCount:1（folder 排除）
（u8 形态在 spec 内断言：恰 10 行 + 40 键负向零出现——与 T-253 §3 一致）
```

## 3. 契约漂移与口径注记

1. **「首屏 XHR ≤3」的计面口径**（无漂移，钉口径）：PRD NFR-P39「repos 列表页首屏
   XHR ≤3」实测整页冷载入 = whoami + **version** + 列表 + usage 批量 = 4 个 /api/ XHR。
   AppShell 顶栏版本徽章（`useVersion`，模块级缓存每文档恰一次）是**所有路由首屏共有的
   环境请求**，非 repos 页扇出预算成员（users 页 ≤2 同理不可能含环境对）。e2e 采双层
   断言：页面数据面（repositories+usage 面）≤3 硬上限 + 恰 2 分解；环境请求白名单钉死
   为恰 {whoami, version}——比裸 ≤3 更强（任何新增未知请求即红）。若 conductor 欲把
   version 挪出首屏（懒加载/会话级缓存），属 AppShell 公共组件变更，须单独提票。
2. **E7（m-holder/user 的 repos 过滤列表）延后 M10** 的 UI 事实如实呈现：u8 打开
   repos 页 = L2 无权限卡（CapRepoRead 闭集），已用列注水零请求；其可见集差异在
   spec 内以「u8 自身批量 wire 恰 10 行」对账呈现（该 wire 即未来用户可见视图的消费面）。
3. E1 其余逐项对齐：bare array（空 `[]`）、counts 两字段恒渲染、`updatedAt`=
   配置时刻（UI 不呈现）、未知 include 值 400（未触发）。零漂移。

## 4. 全量失败的归属证据（非本票面）

- `e2e/m8/users-groups.spec.ts:201`（两轮均红）：strict-mode violation——
  `[data-testid="user-row-…"] .badge` 现解析到 2 元素：角色徽章 + **`user-status-*`
  「启用」徽章**。后者仅存在于 T-257 未提交的 `UsersPage.tsx` diff（该文件上次提交
  = T-244 `74f4f08`；`git diff` 可见 Status 列新增）。修法（收窄选择器或改断言）在
  T-257 面（e2e/m8/users-groups.spec.ts 是其消费面邻接）。
- `e2e/m9/users-groups.spec.ts:148 N02`（仅终轮红，首轮同 spec 12 腿全绿）：T-257
  spec 本身未提交在途（+336 行 vs T-250 骨架），两次全量之间其树在动。腿内容（u7
  状态徽章/编辑器回显）全在其 area。
- `t146-docs G19b-1`：例门口径非回归（§2.3 已对清 8/8）。
- 本票文件（RepositoriesPage/repos.ts/usage-fanout.spec）在两轮全量中零失败。

## 5. 遗留

1. T-99 登记的「制品/缓存」「更新时间」两列缺口：nodeCount 已可经 E1 counts 承载
   （本票以 tooltip 收编信息面）；是否升格为实体列归 ux/architect 裁量（本票遵
   「不破坏既有列集」不建列）。
2. §3-1 的 version 环境请求若要出首屏：AppShell/useVersion 公共组件票。
3. m9 计数器 idiom 目前 spec 内自持两份（本票 + T-257 在途）；收敛为
   `e2e/m9/support/requestCounter.ts` 共享件的候选，待 T-257 落定后归并票裁量。

## 6. 环境清理

:18096 / :18097 实例已停（探活 000 双证）；/tmp/t258 保留（yaml/日志/数据证据）。
