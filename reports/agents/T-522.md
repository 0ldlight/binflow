# T-522 — CI 修复三件：OpenAPI 漂移门 + env 注释漂移 + nightly UI 检查

**Ticket:**        T-522 [P1] CI 修复三件：OpenAPI 漂移门 + env 注释漂移 + nightly UI 检查（area: ci/ + .circleci/）
**Role:**          devops-engineer
**起止时间:**      2026-09-25 02:20 – 02:52 (+0800)
**Area:**          CI 质量闸门链（Makefile spec 目标 / CircleCI build+nightly / GH Actions ci / ci/ui-smoke.sh）
**AC 清单:**
- AC1 OpenAPI 漂移门进现有 CI 链，失败信息可读（diff 摘要），生成命令与本地一致（`make spec-check` 单一入口，双 CI 面同命令）——已交付
- AC2 env 注释漂移逐处修正并列清单——已交付（全仓 grep 命中恰 1 处，`.circleci/config.yml:16`）
- AC3 nightly UI 检查仓内可承载部分（nightly workflow 加 `ui_smoke` 腿 + 单源脚本 `ci/ui-smoke.sh`）；CircleCI UI 侧动作进「用户待办」——已交付
- AC4 本报告 15 字段含漂移门本地实跑证明（篡改→红、还原→绿）——已交付

**改动摘要:**
1. **OpenAPI 漂移门**。`fern/openapi/binflow.json` 是生成物（生成器 `tools/openapi-spec/build_spec.py`，python3 零依赖，权威源顺序见其文件头：docs/user/api-reference.md → internal/httpapi 路由交叉核对 → handler 线上事实）。新增 Makefile 目标 `spec`（重生成）与 `spec-check`（重生成 + `git diff` 门，失败打 `--stat` + `-U0` 截 80 行 diff 摘要 + 修复指引）；`dev` 目标前置 `spec-check`（<1s 最快失败的本地闸门）。CI 挂载：CircleCI `build` job checkout 后第一步（双 shard 各自跑，幂等）+ GH Actions `ci` job checkout 后第一步——双面同一命令 `make spec-check`，无第二口径可漂移。
2. **存量漂移先行归零（前置条件）**。落门前实测：HEAD 上生成器输出与已提交 artifact 已漂移——e0061292（2026-09-20，console 本地化/去品牌化提交）手改了 binflow.json 四处文案（去 "Artifactory" 品牌词）但生成器未同步。门若直接落地主分支立红。按 e0061292 已确立的去品牌化方向把生成器四处文案对齐已提交 artifact（见下方偏差段：tools/openapi-spec/ 属 tech-writer 域，最小 4 行修正，留 conductor 复核）。对齐后重生成字节一致（324286 bytes，`git diff` 零输出）。保留未改的两处合法 "Artifactory"：`X-Artifactory-Filename`（字面 HTTP 头名）与 "the Artifactory v2 permissions API"（e0061292 自己保留的拼写）。
3. **env 注释修正**。`.circleci/config.yml` 头部 Prerequisites 注释说 `$UAT_SSH_FP`——该变量名从未存在；实际 add_ssh_keys 步骤读 `$SSH_KEY_FINGERPRINT_UAT`（同文件 deploy_uat job 内）。修正注释并注明 T-522 更正。全仓 grep `UAT_SSH_FP` 仅此 1 处（`UAT_SSH_KEY_B64` 是另一个真实存在的变量，deploy_uat 的主路径，不动）。
4. **nightly UI 检查**。CircleCI `nightly` workflow（trigger_source == scheduled_pipeline 门）原只有 race_full + protocol_leg×10；新增 `ui_smoke` job（executor go、checkout + `bash ci/ui-smoke.sh`）。脚本单源 `ci/ui-smoke.sh`（与 protocol-matrix.sh 同款 env 驱动形态，内置 https→plain-HTTP 过渡探针）：6 项检查——healthz 200 / `/binflow` 301→`/binflow/ui/`（CE-01 挂载契约）/ shell 200 含 `id="root"`（e2e smoke spec 同款锚点 W01）/ shell 首个 module script 指纹包真实可取（区分 placeholder 与真 console 的判别项，抓 broken-embed/asset-rot 类）/ docs 面 200 / version 端点 200。刻意不是 Playwright 全量（那是 e2e job 域，已知红 spec 债 L025-2/L026-4 参数门关着）；smoke 红意味着部署/配置腐化而非 spec 债。既有腿零改动（uat 链、protocol 矩阵、race_full、缓存、超时均原样；结构断言见 Commands 段）。
5. GH 面不加 nightly schedule：nightly 是 CircleCI 既有归属面（protocol-matrix.yml 头注释「automatic post-deploy lane lives on CircleCI」+ 2026-08-28 one-CI-surface 指令），GH 侧曾因 Actions billing 全红（2026-08-28 观测），复制一条夜间面=双跑成本+可能死面。

**产出文件清单:**
- `Makefile` — 新增 `spec` / `spec-check` 目标 + .PHONY + `dev` 前置 spec-check（+31/-3）
- `.circleci/config.yml` — env 注释修正（L16 区域）；build job 漂移门步骤；新 `ui_smoke` job；nightly workflow 挂 ui_smoke（+40/-1）
- `.github/workflows/ci.yml` — ci job 漂移门步骤（+10）
- `ci/ui-smoke.sh` — 新脚本（0755，与 protocol-matrix.sh 同权限）
- `tools/openapi-spec/build_spec.py` / `d_content.py` / `d_security.py` — 4 处文案对齐已提交 artifact（+5/-5，见偏差段）
- `fern/openapi/binflow.json` — 未改（重生成后与提交版字节一致）

**实际执行命令与关键输出:**
- 漂移门三段证明（本地实跑）：
  1. 同步态过门：`make spec-check` → `binflow.json: 112 path items, 158 operations, 20 tags, 54 schemas, 324286 bytes` / `self-check: clean` / `spec-check: fern/openapi/binflow.json in sync with tools/openapi-spec/`，exit=0
  2. 篡改无关字段（生成器 contact.url 加 `/tampered`）→ `make spec-check` → 打印 `=== FAIL: fern/openapi/binflow.json drifted ... (T-522) ===` + `--stat`（1 insertion, 1 deletion）+ `-U0` diff（`-"url": "...buildwithfern.com"` / `+"url": "...buildwithfern.com/tampered"`）+ 修复指引，`make: *** [spec-check] Error 1`，exit=2
  3. 还原后过门：恢复 contact.url → `make spec-check` → in sync，exit=0；`git status` 中 binflow.json 无改动（字节级还原）
- YAML 语法门（改后全量四文件）：`python3 -c "import yaml; ..."` → `.circleci/config.yml / .github/workflows/{ci,protocol-matrix,fern-docs}.yml` 全部 `YAML OK`。（过程留痕：初版 ui_smoke job 名含未加引号的 `UAT: ` 触发 PyYAML `mapping values are not allowed here`——已引号修复，此为本次改动自查出的唯一语法错）
- `actionlint .github/workflows/ci.yml` → clean（本机 /usr/local/bin/actionlint）
- `circleci config process/validate`：本机未装 circleci CLI（`command -v circleci` 空）——未跑，用 PyYAML + 下条结构断言替代，属已知缺口
- CircleCI 结构断言（python + PyYAML）：build 步骤含 `OpenAPI spec drift gate`；`ui_smoke` job 在位；`nightly.jobs == [race_full, ui_smoke, protocol_leg]`；`uat.jobs == [build, uat_approval, deploy_uat, protocol_leg]` 且 when 门不变（`!= / == "scheduled_pipeline"`）——全过
- `bash -n ci/ui-smoke.sh` + `shellcheck ci/ui-smoke.sh` → clean
- ui-smoke 负例（placeholder console，本机 boot `./bin/binflow-server serve`）：`BINFLOW_BASE=http://127.0.0.1:8080 bash ci/ui-smoke.sh` → `FAIL console bundle: no module script in the shell (broken build/embed)`，其余 5 项 OK（腿不互相遮蔽），exit=1。过程修掉一个真 bug：无匹配时 `grep | head` 管道在 `set -euo pipefail` 下静默杀脚本（grep exit 1 + head 早关 SIGPIPE 风险）→ 改 `grep -o -m1 ... || true`，失败信息现在可读
- ui-smoke 正例（`make console` + 重建二进制 + 重启）：同命令 → 6×OK（含 `OK console bundle (index-C0mLg4dy.js): 200`），`ui-smoke: GREEN (6/6 checks)`，exit=0
- env 驱动探针分支（对常驻 UAT 只读 GET）：`bash ci/ui-smoke.sh` → `ui-smoke base: https://uat.binflow.org` → GREEN 6/6（https 探针直连成功，未走回退分支；常驻实例 bundle hash 与本机 HEAD 构建一致 index-C0mLg4dy.js——部署面与 HEAD 同步的旁证）
- `make help` 列出 spec / spec-check / dev；`make -n dev` 首步即 `python3 tools/openapi-spec/build_spec.py`
- 临时进程/目录已清理（server pkill、/tmp/binflow-ui-smoke 删除、:8080 释放）

**测试四态:** 漂移门（同步绿/篡改红/还原绿）= PASS×3；ui-smoke（placeholder 红/真 console 绿/UAT 绿）= PASS×3；YAML+actionlint+shellcheck+bash -n = PASS；circleci config process = NOT_RUN（CLI 不在本机）；CI 真实运行观察（首轮触发时长/分片/缓存）= NOT_RUN（硬约束禁触发，见用户待办收尾项）

**lint:** yaml 校验与 actionlint/shellcheck 如上全绿；本票无 Go 代码改动（go vet/lint 面无涉）

**证据锚:** 本报告 Commands 段全部为可复制重放命令；漂移门三段与 ui-smoke 三跑是本票核心证据；UAT 实跑输出为 2026-09-25 02:5x (+0800) 一次性采集

**风险:**
- cimg/go:1.26-node 内 python3：cimg 基础镜像族预装 python3，但本票未能真实起 CircleCI 容器验证（禁触发）；若缺失，门会在首跑以清晰报错暴露（fail loud，不静默）
- nightly 全窗（race_full/protocol_leg/ui_smoke）仍处休眠——等项目级 schedule trigger 创建（L025-2 起挂起的用户动作，本票用户待办重申）；ui_smoke 与既有两腿同休眠同激活
- 漂移门依赖 `git diff`：CI checkout 均为全克隆可用；本地开发者若在 detached/浅克隆极端形态可能误报（提示信息已含修复指引，`make spec` 恒可消除）
- ui_smoke 探针对 UAT_DOMAIN 的 DNS 依赖：uat.binflow.org 当前解析正常（实测 https 直连过）；回退分支（plain-HTTP IP）本机未实测走到（https 即成功），逻辑与 protocol-matrix.sh 同款已在线上验证过的形态

**偏差:**
- 触及 `tools/openapi-spec/*.py`（tech-writer/conductor 域，本角色 owns 之外）：4 处文案修正，方向=把生成器对齐 e0061292 已提交的去品牌化 artifact（非新内容决策，是把既有决策补进生成源）；不此举则门落地主分支立红、「还原后门过」证明无法成立。请 conductor/tech-writer 复核这 4 行（diff 见上）。
- `dev` 目标前置 `spec-check`：Makefile 属本角色域，但改变了所有开发者的 dev 循环行为（多一个 <1s 闸门）；属加法不加语义改变，列此留痕。
- 会话中途工作区出现他源未提交文件（`fern/translations/en/pages/*`、`docs/design/virtual-four-bucket.md`、`reports/agents/PR-150-review-a.md`）——并行会话产物，本票未触碰。

**用户待办（CircleCI UI 侧，本角色不碰）:**
1. **创建项目级 nightly schedule trigger**（L025-2 起挂起，本票重申）：Project Settings → Triggers → 每日 03:17 UTC、branch main。此动作同时激活 race_full + protocol_leg×10 + 新 ui_smoke 三腿。
2. **org context 迁移**（T-473 注册项，仍未执行）：UAT_MATRIX_BASE/USER/PASSWORD、UAT_HOST/USER/HOME、SSH_KEY_FINGERPRINT_UAT 迁入 org context（如 binflow-uat）并挂到 deploy_uat/ui_smoke/protocol_leg——fork 不继承、轮换一处。注意引用缺失 context 会红每一条 run，须先建后引。
3. 首轮真实触发后回看：build job 漂移门步耗时（预期 <5s）、ui_smoke 6 检查全绿、nightly 三腿并发形态——job 摘要回填本票或后续票。

**结论:** 三件交付齐：漂移门双面落地且本地三段实跑证明（红/绿俱证），存量漂移已归零并留复核偏差项；env 注释唯一漂移点已修正；nightly UI 冒烟以单源脚本 + nightly workflow 腿形态落地，placeholder 负例与真 console 正例俱证，常驻 UAT 实跑 6/6。未 commit/push/触发 CI/碰 UI（硬约束遵守）。状态：done（待 conductor 收编 + 用户待办 3 项）。
