# QA 报告 T-243 — M8 中期回归：W 锚迁移首轮 + M1~M7 契约冻结审计

- 日期：2026-08-24 ｜ role：qa-engineer ｜ HEAD `6158f53`（与派发一致）
- 工作树核查：Go/web 产品源码**零在途改动**（脏文件仅 docs-site 构建产物），无需 worktree 兜底。
- 审计基线：`m7-done`（`5c34194`）→ HEAD，65 commits。
- 冻结依据：ADR-0029 决策 4 + architecture §13.1 五不变量；锚册 console-ux §10 v1.6。
- 环境：本机新鲜构建（`make console && make build`，二进制新鲜度守卫空）；被测实例 127.0.0.1:8080（scratch 数据目录，已停并清理）；用户实例 18080 全程未触碰。

---

## 一、契约冻结 git diff 审计（硬闸）——结论：**PASS，零违约**

`git diff m7-done..HEAD --stat -- internal/ cmd/` **非空**：26 文件 +954/−205，但**全部**来自 2 个 B1 债券票（排期内、板上登记）：

| commit | 票 | 性质 |
|---|---|---|
| `684e71c` | T-231（D-1 缺陷修复） | internal/client 客户端 URL 转义 + cmd/bf 打印 URI |
| `b3b3c06` | T-233（FR-77 债券包） | bf-migrate --skip-users + CI 超时 + 测试 dialer 收敛 |

逐文件核对（8 个非测试文件全部为 CLI/迁移工具面，零服务端文件）：

| 文件 | 判定 |
|---|---|
| cmd/bf-migrate/main.go、cmd/bf/main.go | CLI 工具（--skip-users 旗标；打印 URI 转义）——非契约面 |
| internal/client/{client,artifact}.go | **客户端** URL 构造（EscapePathSegments 单一构造点）；import 面核查：仅 internal/migrate 与 cmd 消费，服务端（httpapi/storage/auth）零引用 |
| internal/migrate/{doc,migrate,reader,report}.go | 迁移工具 --skip-users 降级 + 转义收敛——非契约面 |
| internal/auth 6 个 _test、internal/httpapi 5 个 _test | **纯测试基建**（dialer 闭包收敛为 2 个构造器）；断言行变更 grep = **0**（预存在 M1~M7 REST 测试零断言改动） |
| 其余 8 个 _test（client/migrate/cmd） | T-231/T-233 自身新功能测试（新增，非改动） |

五不变量逐条（§13.1）：

1. **服务端契约零改动 ✅**：REST wire 处理器（security.go/permissions.go/groups.go/server.go 等）diff 为空；web 端点清单 m7-done vs HEAD **23 条归一化路径完全一致**（新增 0 / 删除 0，apiJSON/apiText 调用面全量 diff）；lib/api.ts 与 AuthContext.tsx **逐字节未改**。抽查：T-237 `adminRole`（security.go:558/616/897）与 `enabled`（:617/898，T-208 指针语义）、T-241 `repos`/`includePatterns`（permissions.go:40-41）——wire 字段均为 m7-done 既有、UI 消费与文档口径一致（adminRole 闭集单通道、授权折叠纯前端零新端点）。
2. **挂载与资源前缀 ✅**（live 实证）：`GET /binflow/` → 301 → `/binflow/ui/`；SPA 段 200；`/binflow/assets/index-BZMt72m7.js` → `Cache-Control: public, max-age=31536000, immutable`；vite base `/binflow/ui/`、relink-assets.mjs、internal/console 源码均零改动。
3. **go:embed 链 ✅**（live 实证）：本次 web 构建 → relink（2 full URL 重写、ui 段引用存活 0）→ 二进制直接服务本次产物。
4. **session/CSRF ✅**（live 实证）：`Set-Cookie: binflow_session=…; Path=/binflow; Max-Age=86400; HttpOnly; SameSite=Lax`；whoami 回 `adminRole: "admin"`；X-BinFlow-Console 习惯头在未改的 api.ts。
5. **范围红线 ✅**：AppShell 导航 grep 无 Xray/Pipelines/Build-info 入口（含禁用态）。

外围：Makefile/.github 仅 `TEST_TIMEOUT=20m`（FR-77 AC3）；DECISIONS.md 为 ADR-0029 转正文本。

> 例外通道核查：T-231/T-233 均为 B1 排期票（FR-77 = PM FR），非 UI 票私改后端——不构成 §13.1.1 违约信号。

## 二、W 锚迁移首轮（console-ux §10 v1.6 对账）——结论：**PASS（带登记债）**

方法：src 377 归一化锚（34 文件）× e2e 全量 testid 引用（getByTestId/testid:/[data-testid] 三形态，含 support 助手），家族模板双向匹配。

| 对账面 | 结果 |
|---|---|
| 断言完整性 | **spec 引用 0 断链**——所有被断言锚在 src 实存（含 itemTestid 回调/三元构造的动态族） |
| 冻结 242 锚 | **零改名**；除显式退役外全部仍在 src。退役登记核实：`user-form-admin`（v1.5）、`repos-filter-{type,package}`、`form-prev`/`form-next`（v1.6）——src 均已移除 ✅。**缺口**：`settings-password` 随改密迁址被移除，仅 §10.5 表行隐式登记、无显式退役条目（缺陷 D-3） |
| T-235 壳 10 锚 | 7 消费（nav-mode-switch/topbar-breadcrumb/quick-set-me-up/quick-new-user/quick-new-perm/menu-edit-profile/quick-new-repo-remote）；死 3：quick-new-group、quick-new-repo-local、quick-new-repo-virtual |
| T-236 树 14 锚 | 主面消费（tree-repo-*/tree-context-*/tree-readonly-note/tree-root-denied 等）；死：browser-toolbar、tree-empty-instance、tree-footer-stats、tree-manage-repos、node-tab-general、node-tags |
| T-237 批次 | users-sort-name/user-form-{role,enabled,password2}/transfer-*/user-perm-matrix/group-perm-matrix 消费；死：users-sort-{email,groups,role}、groups-sort-{name,perms,members}、group-perms-<X>、user-form-{reset,cancel}、group-form-{reset,cancel} |
| T-239 17 锚 | search-recent-*/profile-*/not-found-*/login-docs 消费（本批最干净） |
| T-240 24 锚 | repos-tab-*/pkg-grid-*/repos-delete/repos-pager/repo-tab-*/repo-quota-{input,save} 消费；死：repo-edit-link-config、repo-goto-tree、repo-quota-{cancel,error}、repos-sort-package、form-cancel |

**死锚清单（册上有、spec 无消费者）**：src 侧共 **75 锚无任何 spec 消费**，其中 63 枚册上有、12 枚未入册。溯源抽查（node-copy/tree-refresh/upload-drop/form-summary/toast-stack/repo-cmd 族等）在 **m7-done 即无消费者**——死锚主体是历史「先落锚后补断言」欠账，M8 未丢失既有断言覆盖，但新批次继续增厚（新锚消费率约 7~8 成）。

**未入册锚（src 实存、§10 全文无登记，违反「先入册再落码」）**：`browser-intro`、`migration-readonly-note`、`perm-res-back`、`repo-advanced-card`、`perms-sort-{users,groups,repos,patterns}`、`storage-{empty,partial,progress}`、`storage-summary-blobs`。其中 **T-238 存储批整体未回册**：§10.5 `/admin/monitoring/storage` 行仍写 `placeholder-page（新页归 T-238）`，页面已落地但锚族（含已消费的 storage-summary/storage-table/storage-total-row/storage-refreshed-at）从未入册。

## 三、M7 语义回归抽样（新壳下）——结论：**PASS**

| 腿 | 结果 | 证据 |
|---|---|---|
| RBAC 角色下拉（V12） | ✅ | e2e 全量内绿：UI 置 readonly_admin → API 回 snake `adminRole` + 审计轨迹 |
| readonly 只读态（V13） | ✅ | 管理页可见、无写入口、重放写服务端 403 |
| manage 第 4 列（V14） | ✅ | 权限矩阵勾选 round-trip、r/w/d/m 序回显 |
| 即时生效 + 三角色矩阵 | ✅ | `make test-m7-rbac-matrix`：R/W 全行 admin=200/201、user=403、readonly 读 200 写 403；零副作用守卫 OK |
| step-up 默认 off | ✅ | 源码 default false（config.go load.go:674，ADR-0027 决策 6）；被测实例无配置文件跑默认 |
| 续传双臂 | ✅ | `make test-m7-resume`（kill -9）GREEN、`make test-m7-resume-sigterm` GREEN——重启后 Range 续块、PUT 收尾 201、blob 逐位一致（sha256 各自验证） |

## 四、全量 e2e + Go race 抽样——结论：**PASS**

- **e2e（真二进制 + seed-m8，--workers=1）**：139 tests → **136 passed / 0 failed / 3 skipped，6.4m，exit 0**。3 skipped 为既有条件跳过（docker dind 腿守卫），与 T-232 记录形态一致。seed-m8：10,291 节点树 79.4s 验证、角色用户 201。
- **go test -race -timeout 20m**（Go 侧冻结故按票抽样）：`./internal/storage/...` ok 83.8s；`./internal/httpapi/...` ok 172.8s。

## 五、SPA 预算与门——结论：**PASS**

| 门 | 结果 | 证据 |
|---|---|---|
| NFR-P36 gzip ≤ 350KB | ✅ 183,571 B（52.4% 预算） | `make console` 报告（js+css）；较 T-234 基线 155KB 增 28.6KB（六域重排合理增量） |
| 二进制 < 40MB | ✅ 26 MB | bin/binflow-server 27,224,784 B |
| assert:tokens | ✅ | 单独复跑 OK（3 组件 css 零硬编码色值）且为 build 前置 |
| axe 核心页抽样 | ✅ | 7 腿全绿：artifacts 浏览器+详情、治理 5 页+存储概要+系统信息、登录页、仓库管理域、亮/暗双主题 serious=0 |

---

## 缺陷清单（无 P0/P1；全部 P2 文档/覆盖债）

| # | 级 | 摘要 | 复现/证据 | 建议 |
|---|---|---|---|---|
| D-1 | P2 | T-238 存储批锚整体未入册；§10.5 路由表行未回写 | §10 全文无 storage-* 锚；`/admin/monitoring/storage` 行仍 `placeholder-page` | 补 §10.5 增补批（8+ 锚）+ 路由行回写，归锚册勘误票 |
| D-2 | P2 | 4 枚散在未入册锚 + perms-sort 族 4 枚 | browser-intro / migration-readonly-note / perm-res-back / repo-advanced-card / perms-sort-{users,groups,repos,patterns} | 同上补册或退役 |
| D-3 | P2 | `settings-password` 退役仅隐式登记 | §10.2 仍列名，src 已无（改密迁 /profile 为 profile-password） | 仿 T-240 显式退役条目 |
| D-4 | P2 | 死锚无守卫：75 枚 src 锚零 spec 消费（63 册上、12 未入册）；新批次消费率 ~75-80% | 本报告 §二；主体 m7-done 已死（非 M8 丢失覆盖） | 建议锚册增「死锚登记」节 + 新票锚消费下限 AC |

## 总结论：**PASS**

契约冻结硬闸零违约（internal/cmd 改动全部为 B1 债券票的 CLI/迁移/测试基建面，五不变量 live + diff 双重实证）；W 锚首轮对账 0 断链、冻结锚零改名；M7 语义、全量 e2e、race 抽样、预算门全绿。遗留为 4 条 P2 文档/覆盖债，建议入锚册勘误票（可搭 T-242/T-244 顺带收口）。
