# T-398 [P1] 文档 B：收口前文档增量（M14）

- 角色：tech-writer ｜ area：`docs/user/` + `web/e2e/`（README 一行）｜ 状态：**done**（AC1/AC2/AC3 全过；全部命令本机实跑，见 §2）
- 纪律：零 git 操作；不碰 web/src、docs/prd、BOARD、docs/design（登记项见 §4）；产品代码 diff = 0（`git status` 非 docs-site 改动 = 本票 7 文件，§5）。
- 日期：2026-09-01

## 1. 产出清单（7 文件）

| 文件 | 增量 |
|---|---|
| `docs/user/integrations/nuget.md` | **AC1 三处陈旧修正**：①「重复推送臂」表重构——v2/v3 两面同语义、**字节盲**双列表（同字节/异字节同判；v2 面 `Package already exist: <deployPath>`、v3 面 **官方逐字** `A package with the provided ID and version already exists`；d 覆盖 201；remote v2=400/v3=405；virtual 两态），删去陈旧的「同字节幂等 201」行（L161，T-378 起即过时）；② 常见报错 push 409 行同步（两面文案 + 处置含 `--skip-duplicate`）；③ 步骤 3 服务端行为 bullet 补字节盲 + d 覆盖例外；banner 加 M14 对齐句（T-401）与 curl 矩阵四臂腿沿革。附 `dotnet nuget push --skip-duplicate` 客户端提示（409 是该 flag 唯一跳过的状态码） |
| `docs/user/admin/governance.md` | **AC1 replication 最小节**（docs/user 此前无复制管理文档——按票面「无则 admin/ 下补最小节」落本文）：新增「复制（push replication）」整节——引擎语义（事件驱动 + 1min sweep、按包型选推送面、目标 first-write-wins、6 次退避）、配置 CRUD curl 块、字段/门/错误形态/审计/SSRF 十行表、启停语义（停即不入队/在途跑完/恢复 sweep 排空/无需重启）、观测面 status 示例（实测响应形状）、控制台两入口与预留位如实标注；banner 加 M14 增补句与复验声明 |
| `docs/user/api-reference.md` | **AC1 api-reference PUT 行**：新增「M14 增补速览（T-397/T-398）」节——replication 域五行表（GET/POST/**PUT**/DELETE/status；PUT 行含数值 id 寻址、enabled 必填、round-trip 容忍、200 回 GET 投影、五类错误形态；注记 DELETE 按 name 双寻址并存）+ PUT curl 示例；`/api/v1` 自有端点表补 replications / replication/status 两行；banner M14 句扩为三项 |
| `docs/user/console.md` | **AC1 replication 控制台面**：仓库管理节三处（列表 local Tab Replications 列 + 行级 Run 深链；编辑页 M14 Replications 节——启停开关/编辑=删除+重建/预留位恒禁用/remote·virtual 不适用；详情页 Replications Tab 摘要卡）+ 治理复制行补 M14 指针。**顺笔（T-386 遗留②文档侧）**：Access Tokens 三处「占位页」陈旧行（树图/域节/边界表）更新为 M14 真身页描述，并正面写对铸币门口径——「**铸币门 = 任何已认证会话**（Q11：与 Set Me Up 同端点同门，非 admin 仅能自铸）」；边界表行改为「无令牌清单端点（R6）」的真实边界 |
| `docs/user/artifactory-path-map.md` | 复制（Replication）行对齐 M14：BinFlow 侧补仓库编辑页 Replications 节；差异列改「REST 自 M6 全量可用（无 license 门）；cron/sync 字段族为预留位恒禁用（事件驱动引擎）」 |
| `docs/user/README.md` | 治理页导航行补「复制配置 CRUD 与启停〔M14〕」 |
| `web/e2e/m8/README.md` | **AC1 前提行**（T-396 观察⑤）：§1 构建命令 `make console && make build` → `make console && make docs && make build` + 两行注记（t146-docs 腿依赖嵌入文档站，缺它首跑 4 红全为此）。落点说明：观察⑤原文即指 m8/README.md §1（运行口径所在）；父 `web/e2e/README.md` 只做指路（「运行口径见 m8/README.md §1」）无需重复改 |

sidebars.js 零改动（无新页面，全部增量落既有挂载页——侧栏核查见 §3）。

## 2. 实测取证（全部本机实跑，2026-09-01；二进制 bin/binflow-server 当日 04:32 构建，`find cmd internal go.mod go.sum tools.go -newer bin/binflow-server` 为空证新鲜）

### R 腿：replication REST + 启停引擎语义（scratch 实例 :8171，净数据目录，BINFLOW_ADMIN_PASSWORD=password，BINFLOW_REMOTE_CREDENTIALS_KEY=base64 值——明文钥会 fail-fast 逐字 `auth config master key: replication credentials: …: base64: illegal base64 data at input byte 4`）

| # | 操作 | 结果 |
|---|---|---|
| R1 | 建 local 仓 + `POST /api/v1/replications`（带 target_password） | **201** 回显 11 字段配置行（凭据零回显；`max_items_per_push` 缺省落 1000；`enabled` 缺省 true） |
| R2 | `PUT …/replications/1 {"enabled":false}` → 再 PUT 全形 round-trip `{"enabled":true,…}` | 双 **200**；`updated_at` 随翻转刷新；round-trip 其余字段被忽略不误改；响应 = GET 投影同形 |
| R3 | PUT 错误臂 ×4 | 缺 enabled **400** `enabled is required (true or false); no other field is editable on this face yet`；非数字 id **400** `id must be a positive integer`；未知 id **404** `replication config not found: 99999`；匿名 **401** `authentication required` |
| R4 | POST 错误臂 | 未知源仓 **400** `replication config references an unknown repository "no-such-repo"`；重名 **409** `replication config name "push-prod" is already taken` |
| R5 | 非 admin（user dev1）PUT / readonly_admin（ro1）三面 | dev1 **403** `administrator privileges required`（路由门文案）；ro1 GET 列表/status **200**×2、POST **403** |
| R6 | `DELETE …/replications/push-prod`（按 name）→ 再删 | **204** 无体 → **404** |
| R7 | 引擎事件驱动：配置指死目标 → 上传制品 → `GET /replication/status?limit=3` | 落库即入队：events 出现该 node_path（`in_progress`，attempts 随退避爬升）；targets 计数行 `in_progress:1` |
| R8 | **停用即停入队**：PUT enabled=false → 再上传一制品 → status | 新制品**不出现**在 events（仅存量两条）——停用语义实证；恢复 PUT 200 |
| R9 | 清理 | 配置 DELETE 204；实例按端口杀（8171 down），数据目录删除 |

### N 腿：nuget 重复臂四臂（真 curl 真栈——T-401 同款矩阵，本票复跑取证）

| # | 命令 | 结果 |
|---|---|---|
| N1 | `BINFLOW_T341_CURL_E2E=1 go test ./internal/adapter/nuget/ -run TestV3LiveCurlMatrix -v -count=1` | **ok 1.923s**；Q6 四臂 live 逐字：admin 种子 **201** → livewriter（w-only）异字节 **409** `A package with the provided ID and version already exists` → livewriter **同字节 409**（翻转本腿）→ admin（持 d）异字节覆盖 **201** → livewriter 新包 **201** |
| N2 | `BINFLOW_T337_CURL_E2E=1 go test ./internal/adapter/nuget/ -run TestLiveCurlMatrix -count=1` | **ok 1.629s**；v2 面 D-10 腿维持：admin 种子 201 → livewriter **同文件**重推（同字节）**409** `Package already exist: live.a/1.0.0/live.a.1.0.0.nupkg` |

（nuget.md 新表即按此两面证据写；环境无 dotnet SDK，`--skip-duplicate` 断言沿 T-401 L14' 夹具口径。）

### D 腿：构建与侧栏

| # | 命令 | 结果 |
|---|---|---|
| D1 | `make docs`（编辑后两轮） | **SUCCESS** ×2；docs site size 4.58 MB；build 输出 grep `broken\|error` 计数 **0**（零断链零错误） |
| D2 | 三个新锚点在产物 HTML 中核对（python 抽 id） | governance `复制push-replication` ✓ / console `仓库adminrepositories` ✓ / api-reference `m14-增补速览t-397t-398` ✓；跨页 href percent-encoding 形态逐对在案 |
| D3 | 侧栏 | 无新页面、无 frontmatter 改动 → sidebars.js 零改动即挂载（T-397 同口径） |

## 3. AC 判定

| AC | 判定 | 依据 |
|---|---|---|
| AC1 nuget 三处 + replication 控制台/API 增量 + ProfilePage 口径一行 + e2e README 前提行 | **达成** | nuget.md 三处（§1 首行）+ banner；replication 增量四面（governance 最小节 + api-reference PUT 行与域表 + console 三处 + path-map/README 导航）；ProfilePage 口径——**核查结论：console.md 及 docs/user 全域无「管理面签发需管理员」对应表述**（grep 证），文档侧反而顺笔在 console.md Access Tokens 行正面写对 Q11 口径（铸币门 = 任何已认证会话），并把 T-386 后陈旧的三处「占位页」描述更新为真身；web/e2e 前提行落 m8/README.md §1（§1 末行说明落点） |
| AC2 make docs SUCCESS 零断链 + 侧栏核查 + 全部命令实测（nuget 四臂 curl 可复跑） | **达成** | §2 D1~D3 + R1~R9 + N1/N2（四臂 curl 即两条矩阵 test 命令，可直接复跑） |
| AC3 README 双语核查（M14 进行中——无需翻完成态） | **达成** | README.md L70「M14 (in progress)」段与里程碑行、README.zh-CN.md L51/L73 均为进行中态，双语对称无误——不翻完成态（收官归 conductor）；本票增量粒度（复制配置面/nuget 码位）未达 README 段落粒度，不扩写 |

## 4. 登记不越界（本票 area 外，原文传递）

| # | 事项 | 归属 |
|---|---|---|
| 1 | **ProfilePage.tsx L124 FE 文案**「管理面签发需管理员」与 Q11 as-built 不一致（文档侧已无对应表述，见 AC1）——原文：`CI 与脚本请使用 API Token（管理面签发需管理员；实例开启 step-up 时非 admin 自铸需二次口令——见文档）。` | FE 文案票（一行改「管理面铸币走 Access Tokens 页 / Set Me Up，任何已认证会话可自铸」类口径） |
| 2 | T-390 遗留：parity 册 M1 行「定案 440px」需按磁贴卡面复活后的新形态刷新 + M3「MUI Paper」代差描述 | ux/conductor（docs/design 域） |
| 3 | K56/品牌：`docs/design/brand/{logo,package-icons}/README` 的生产化状态（v1.2 已落）；文档站无需新页（README 双语品牌行 T-397 已加，本轮复核仍在） | ux（如需册内更新）；文档站无动作 |
| 4 | T-405 遗留①（复核仍在）：`docs/design/architecture.md` §7.1 路由表缺 `PUT /binflow/api/v1/replications/{id}` 行 | architect 域（conductor 文档环） |
| 5 | T-396 观察⑤的另一半：scratch 复现者除 `make docs` 外还需纯净 community 实例（父 README §1 已有两行注记在案，无缺口） | 无动作 |

## 5. 遗留 / 待确认

1. **nuget dotnet 腿**（L14' `--skip-duplicate`）实现机无 SDK——沿 T-401 遗留③：`BINFLOW_T287_CLIENT_E2E=1` 有 SDK 环境即验；文档中该提示按 T-401 断言基线写（409 是该 flag 唯一跳过的状态码）。
2. governance.md 复制节示例 `source_repo=repl-local` 为示例名（实测用 `repl-src`，命令形状逐字一致）；示例假设 local 仓已存在——约束已在字段表「source_repo」行写明。
3. console.md Access Tokens 三行更新属**顺笔超出票面字面清单**（T-386 遗留②只点名 ProfilePage 口径一行）：判定依据 = 三处「占位页」在 T-386 合并后即为不实陈述，收口前文档增量留陈旧行违背本票目的；如 conductor 认为越界可单独回退该三行（其余不动）。
4. T-397 遗留不变项：Docker Hub 等公网 registry 直连待公网环境验证；docker virtual 开矩阵归 conductor。
5. `BINFLOW_REMOTE_CREDENTIALS_KEY` 需 base64 值——oidc-config.md armed 起法一行（T-397 修订）未提 base64 约束，明文钥 fail-fast 文案见 §2-R 腿开场。本轮未改该页（超出票面清单），登记后续文档票顺笔（或 conductor 指定微票）。

## 6. 变更清单

```
docs/user/integrations/nuget.md      AC1 三处 + banner + --skip-duplicate 提示
docs/user/admin/governance.md        复制（push replication）最小节 + banner M14
docs/user/api-reference.md           M14 增补速览（replication 域表含 PUT 行）+ v1 自有端点表 +2 行 + banner
docs/user/console.md                 仓库管理节 M14 Replications 三处 + 治理行指针 + Access Tokens 三行（T-386 陈旧）
docs/user/artifactory-path-map.md    复制行 M14 对齐
docs/user/README.md                  治理导航行 + 复制字样
web/e2e/m8/README.md                 §1 make docs 前提行（T-396 观察⑤）
reports/agents/T-398.md              本日志
```
