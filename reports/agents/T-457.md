# T-457 — Profile 自助 Identity Token / SSH 缺位 + ? 帮助下拉 + About 版本弹窗

状态: review（四闸门全绿 + 新 spec 6/6 + 回归批全绿 + ledger PASS + SPA +3,898B ≤10KB）
角色: dev-frontend · area: web/src/pages/ProfilePage + web/src/components/AppShell（+ 消费 spec/锚册/parity 册）
日期: 2026-09-04 · 分支 develop（未 commit——等收编）

## 0. 交付面（AC 对照）

| AC（票面） | 落地 |
|---|---|
| Profile identity token 生成 + 一次性明文（7.161 形态） | `profile-token` 卡升格为自助签发真身 + 生成弹窗族 15 锚；明文只在 done 面板存活、关弹窗即卸载丢弃（无持久层持有）；服务端只存指纹（§9-R6 语义延续） |
| 即时可用 curl 断言 | t457 spec 腿①：铸出令牌真发 API 请求 **双臂**——Bearer 臂（`Authorization: Bearer` GET /api/v1/session = 200 且主体吻合）+ **curl `-u` Basic 臂**（面板内即用样例的口令位形态，auth-model §3.6 双臂；样例文本断言含 `用户名:令牌`）；吊销收尾后再对账 401 |
| SSH key 增删 | **后端无 SSH 公钥端点——契约缺口不伪造**（internal/httpapi 全量 grep 零 SSH 面；仅 E-17/E-18 token 两端点）。Profile SSH 卡如实缺位（`profile-ssh-gap` 注记 + 零表单，spec 结构反断言：卡内零 button/input/form/table）。缺口登记 console-ux §9-**R11**；parity 册 B-1.8 SSH 腿注记 |
| ? 帮助下拉四项 | Documentation（/binflow/docs/ 外链）/ Online Training（无对应服务——禁用占位 + 行内注记，不伪造外链）/ Release Notes（实链 /binflow/docs/install/upgrade「升级与版本说明」）/ About（版本弹窗）。`topbar-help` 锚语义翻新零改名（链接→下拉触发钮） |
| About 版本弹窗 | 消费 /api/system/version（version/revision/product 三值 mono + 品牌位；失败 = — 不伪装 Q4）；双入口 = help-about + **侧栏脚注 vdev 行升格**（`nav-about`，nav-version 锚不动）。spec 与同刻端点值逐字段对账 |
| Access Tokens 持久化牵 L1 重评（A7 联动） | 票内留痕：服务端令牌清单端点（§9-R6）落地 → Tokens 页会话台账转持久列表 → L1 列选/刷新推广（T-468 条件池同域）。console-ux §10.5 T-457 批块留痕注 ③ + parity B-1.8 as-built ③ |

## 1. 变更清单（本票笔迹）

| 文件 | 变更 |
|---|---|
| web/src/pages/ProfilePage.tsx | 重写 token 卡（自助签发真身 + 生成弹窗 + step-up 两臂 + 即用 curl 样例）+ SSH 缺位卡；password 卡零变化（锚冻结） |
| web/src/components/AppShell.tsx | topbar-help 链接→? 下拉（四项）；About 版本弹窗 + 侧栏脚注 vdev 升格入口（nav-about）；import 增补 |
| web/e2e/m16/t457-profile-help-about.spec.ts | 新 spec（六腿，见 §3） |
| web/e2e/t134-g32.spec.ts | DC-02 翻新：topbar-help 链接断言 → 下拉 Documentation 项断言（锚/路径不变） |
| web/e2e/t146-docs.spec.ts | G19b-1 同款翻新 |
| web/scripts/anchor-audit.mjs | STOP 增 v1.42 六枚假阳性（step-up / link-in-text-block / t134-g32 / t146-docs / t457-profile-help-about / t457-probe） |
| docs/design/console-ux.md | v1.42（修订行 + §3.5 帮助下拉/About 两全局元素条款 + §9-R11 + §10.5 T-457 批 27 锚块 + §10.5 T-239 块「编辑档案」行批注） |
| docs/design/console-artifactory-parity.md | B-1.8 / B-2.17 两行 as-built 回写（翻正·已落〔token 腿〕/ 活体差异留痕） |
| reports/agents/t457-probe/ | 7.161.20 只读取证（见 §5） |
| internal/console/dist | make console 同步（收编时 conductor 常规重建即可） |

**非本票笔迹但同工作树在案**（conductor CI 修复 lane，收编时勿归 T-457）：cmd/binflow-server/main.go、internal/adapter/helm/{handler,harness_test}.go、fern/docs.yml、fern/pages/api-reference.mdx。

**交错披露处理（conductor 通报的 finisher 笔迹）**：ProfilePage 行内 goto 链接的 MuiLink + underline 形态（与我的 axe 修复同意图同配方——LoginPage link-in-text-block 先例）已吸收保留；五文件逐一重读核对，无半成品/孤儿态。t134/t146 现值即本票语义翻新（**非** CI 环境敏感断言修复——不属 T-475 的 t443/t449/t451 修复族，无可复用形态，T-475 无涉）。

## 2. 四闸门（自测命令 + 关键输出）

```
cd web && npx tsc --noEmit                          → （零输出）0 error
cd web && npx eslint src/pages/ProfilePage.tsx src/components/AppShell.tsx \
        e2e/m16/t457-*.spec.ts e2e/t134-g32.spec.ts \
        e2e/t146-docs.spec.ts scripts/anchor-audit.mjs
                                                    → 0 error / 6 warning（AppShell：
                                                      HEAD 5 warning 同数在册 + 1 新增
                                                      828:27 react-hooks/refs——help
                                                      Menu anchorEl=ref.current，与既有
                                                      session 菜单（746:27）同款模式）
cd web && npm run build                             → ✓ built（ProfilePage 11.52kB/
                                                      gzip 4.57kB；AppShell 29.89kB）
SPA 预算（gzip js+cs 合计；基线 = git stash push 本票两 src 文件 → 重建 → pop 复原）：
  BASELINE 407,265B → FINAL 411,163B = +3,898B（3.81KiB）≤ 10KB/票 ✓
node scripts/anchor-audit.mjs --ledger              → PASS（unregistered/broken 双零，
                                                      retired 全部在表；dead 桶 4 家族
                                                      与本票前完全一致——零新死锚）
```

## 3. e2e（端口纪律：本票实例 = scratch 数据目录 + 127.0.0.1:18098——非常规端口留痕；机器共租期查 `ps aux | grep -c '[g]o test'` = 0 才起跑；kill 仅按端口精确）

新面 `web/e2e/m16/t457-profile-help-about.spec.ts` 六腿（BASE=http://127.0.0.1:18098）：

```
✓ profile token: admin self-mint chain — one-time plaintext, immediately usable
  (Bearer + curl Basic), gone after close      （Bearer+Basic 双臂 200/主体吻合 →
   关闭/重开/刷新明文不可再取 → 吊销收尾 200 → 死亡对账 401）
✓ profile token: plain user self-mint — capped TTL set, no on-behalf field,
  subject is self                              （TTL 闭集无「永不过期」/无代人签发/
   主体对账/内联 500 错误〔mock 一次性〕）
✓ profile token step-up: 401 required -> inline password form; invalid ->
  verbatim ADR text; retry mints               （mock 拦截腿：ADR 逐字 + 第三次真铸
   + step_up_password/expires_in 载荷对账——t386 ⑤ 同构）
✓ profile ssh: honest gap note, no fabricated add/delete form
✓ help dropdown: four items, honest disabled gap, About dialog matches
  /api/system/version                          （同刻端点值逐字段对账 + nav-about
   第二入口 + Esc 回焦）
✓ axe: profile page, help menu, and About dialog clean in both themes
                                               （双主题 × 页/菜单展开/弹窗三态）
6 passed
```

回归批（同实例）：

```
auth-shell + security + m8/auxiliary            → 20 passed
m8/a11y-sweep + t134 + t146                     → 17 passed（a11y sweep 双主题全路由
                                                  5.4m 绿——首轮失败系实例被 shell 回收
                                                  后 connection refused，与产品无关）
m14/t386-tokens-page                            → 22 passed + 1 skipped（armed 实例腿
                                                  默认 skip——设计态）
m8/shell                                        → 纳入终验批（见下）
终验批（最终二进制 = src 定稿后整链重建）：t457 6 + m8/shell + t134 全 + t146 全 +
  auth-shell                                    → 30 passed（2.2m）
终验批②：m8/auxiliary + t386 + security        → 22 passed + 1 skipped
```

修正过程留痕：① 首轮 spec 引用了不存在锚（profile-ssh-add 反断言）——锚册 broken 硬门不可引死锚，改结构断言（卡内零交互件）；② `profile-token-goto` href 断言初版写路由相对值——react-router basename 解析为 /binflow/ui/... 全路径，修正；③ 连续断言中弹窗已开时重复点生成钮会被 modal 拦截——helper 改 `mintFromOpenDialog`（开窗时机归调用方）；④ footer 升格钮初版带 `.nav-item` 类会击穿 m8/shell 的 `.nav-item` 计数（3）——footer 工具项非导航目的地，摘类零 spec 改动。

## 4. 锚册/parity 增量

- **console-ux v1.42**：T-457 批 **27 名新锚**（profile-token-* 弹窗族 15 + profile-ssh{,-gap} 2 + help-{docs,training,release-notes,about} 4 + nav-about 1 + about-* 6）；语义翻新零改名 3 枚（topbar-help / profile-token / nav-version）；0 退役；§9-R11 新增。STOP 同步 6 枚假阳性。
- **parity 册**：B-1.8 → **翻正·已落（token 腿）+ 缺位登记（SSH 腿）**；B-2.17 → **翻正·已落（T-457）+ 活体差异留痕**。

## 5. 7.161.20 活体取证（reports/agents/t457-probe/，只读零写入）

- **帮助下拉**（probe4）：触发钮 aria-label "Get Help and Learn"（data-cy help-menu-button）；菜单实为 **JFrog Documentation（jfrog.com/help）/ JFrog Academy（academy.jfrog.com）/ Navigation Tour** 三项——**无 About / Release Notes**（经典 UI 成员，见 7.84 逆向 §3.x）。BinFlow 四项集 = PRD FR-145.6a 定案（Documentation→自有文档站、Academy→Online Training 映射、Release Notes/About 经典成员回填）——差异不追随，留痕 parity B-2.17。
- **Profile 页**（reverse console-ui §3.18 高置信，7.161.20 活体因 onboarding 遮罩 + 模块加载不可达未直证）：Authentication Settings = Generate an Identity Token；Identity Tokens 表（Description/Token ID/Issued At/Expiry）；SSH = Add New SSH Key + 别名/签名两列。BinFlow 侧：明文一次性形态对齐；**表不进 Profile**（服务端只存指纹无清单端点 §9-R6——管理面 Tokens 页会话台账是该语义既有承载；弹窗内呈 token_id/有效期供审计对账）；SSH 腿缺位（R11）。
- 探针脚本/截图：probe.mjs~probe6.mjs + probe4/5/6-evidence.txt + s1-help-open.png 等。

## 6. 契约注记（conductor → architect 回写面）

1. **SSH 公钥端点缺位（本票登记的契约缺口）**：BE 无 `GET/PUT/DELETE /api/security/ssh_keys` 类面——console-ux §9-R11 兜底「Profile SSH 卡如实缺位」。落地后 FE 补增删表（锚名已预留语义位）。
2. **令牌清单端点缺位（既有 §9-R6 延续）**：Access Tokens 页台账仍是会话内存态；持久化落地牵 L1 列选器重评（A7 联动——本票留痕）。
3. **认证双臂确认（无漂移）**：铸出令牌 Bearer 臂与 Basic `-u user:token` 臂（口令位回落 token 校验 + 主体大小写不敏感匹配）实测双通——与 auth-model §3.6 / SetMeUp 文档口径一致；Profile 弹窗的 curl 样例即 Basic 臂形态。

## 7. 遗留

- OIDC 会话的 Profile 签发：诚实降级引导（引导至 Set Me Up resume 链——ADR-0027 决策 8，与 TokensPage 同形态不私建第二条链）；真实 OIDC 腿属 m9 oidc-stepup 域。
- lint warning +1（AppShell 828:27 react-hooks/refs——MUI Menu anchorEl=ref.current 与既有 session 菜单同款模式；0 error 过闸，模式级清理归共享层票）。
- About 弹窗「License 详情见管理面」为指引文案（License & Add-ons 页既有）。

## 8. 复现

```sh
export PATH="/usr/local/opt/go/bin:$HOME/.nvm/versions/node/v24.20.0/bin:$PATH"
make console && make build
BINFLOW_SERVER__LISTEN="127.0.0.1:18098" BINFLOW_STORAGE__DATA_DIR=/tmp/t457-scratch/data \
  ./bin/binflow-server serve &
cd web && BASE=http://127.0.0.1:18098 npx playwright test e2e/m16/t457-profile-help-about.spec.ts
```
