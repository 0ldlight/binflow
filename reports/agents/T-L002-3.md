Ticket:       T-L002-3（LOOP 002 轨道 L002-3）——prune/gc 管理面行为规格票（storage-v2 REFACTOR 前置：终案 202 异步 + status 报告 JSON）
Role:         reverse-engineer（本票实例域：storage 管理面 / REST 维护端点族）
Area:         docs/reverse/storage/prune-gc-admin.md
Input:        派发领域「prune/gc 管理面」；缺口来源 LOOP 002 终案裁定（该面 REFACTOR）；参照 binary-provider-chain.md G 组两轮实证 + docs.jfrog.com PUD/GC REST 文档
Changes:      新建规格（端点表 E1–E10 / PruneRequestModel+PruneStatusReport 字段级 schema / 流式体语义 / prune 生命周期六步 / GC 引擎迭代节奏 / 调度面含 cron 出厂值收口 / BinFlow 十条差异 D1–D10 / 待验证 V-1–V-8 / 取证命令摘要）；storage/README.md 索引加一行并收口 §2.2-6 cronExp 挂账
Files:        新建 docs/reverse/storage/prune-gc-admin.md；修改 docs/reverse/storage/README.md（索引+挂账收口）；未触碰 binary-provider-chain.md
Tests:        活体 :8082（7.161.20）共 14 腿：prune/status 三态全捕获（finished/running 55 采样/stopped）；prune/start 202×3（空体/dryRun/startFromDirectory）+并发 412 实证；prune/stop 202×2（运行中/空闲）；gc 200 流式体 `.`×2；optimize 202 空体；backup 坏 key 500 errors 信封；size text/plain 数字；GET config XML gcConfig=`0 0 /4 * * ?`；无凭证 401×2
Commands:     A 树：grep -rln "system/storage" reverse-src/artifactory/backend（命中 2：StorageResource/UI StorageResource）；find … FilestorePrunerJob（partial 树 1 命中）；sed/grep StorageServiceImpl(GcSchedulerHandler/scheduleTrashBinariesCleanupGCJob/callManualTask)、TaskServiceImpl(checkCanStartManualTask 737-790)、GCServiceImpl(startGarbageCollect/IterationResult)、ConstantValues(gc* 14 键)、GcConfigDescriptor、artifactory.config.xml(gcConfig 模板)；diff 双树 StorageResource（功能一致）。B 活体命令全录规格 §8
Outputs:      规格文件 1（覆盖：端点 10+UI 5、语义流程 4 组 16 步、schema 2 个/调度规则 5 条）；差异清单 10 条（D1–D10）；待验证 8 条（V-1–V-8，其中 V-8 承接 G5/G7 不重复立项）
Compatibility: 置信度分布 高 14 / 中 9 / 低 0（E1–E4/E6/E8/E9、schema 主干、cron 收口为高=反编译+活体+官方三源；E5/E7/E10、UI 面、AoL cron、V 系列为中）。与官方规范差异/补充：PruneStatusReport 26 字段 schema（官方无 schema）、流式体钉 200 语义、prune 并发 412 改写、GC 20 轮 FULL 节奏默认值（官方记载 20 与反编译互证）、gcConfig cron 出厂值——均标「补充官方规范」
Security:     端点族全 @RolesAllowed(admin,ha)；无凭证 401（活体）；AoL 非 dashboard 403；UI 面 401 鉴权门未破（V-6）。无越权新面
Performance:  gc 同步阻塞（请求持到完成，活体 0.017s 空载—秒级；gc.maxRunTimeMinutes=360 上限）；prune job 异步（256 目录秒级）；prune/stop 异步标记；trash cleanup 出厂不启用；AoL cron 错峰 hash
Risks:        ① 活体实例事故（详见 Blockers 复盘）：compress 探测时段容器 Exited(0)，叠加宿主机 DHCP 换 IP（192.168.1.70→149）致 router 无法 rejoin，全栈瘫痪 ~8 分钟；已修复（见下）并恢复 200；compress 的真实 HTTP 形态降为反编译单源（V-1），不再复测该端点。② backup 信封两源分歧（V-2）。③ PruneRequestModel 两键名未实证（V-3，库类不可达）。④ gc 并发 409 与 status=error 态未活体（V-4/V-7）。⑤ UI 面全单源（V-6）
Blockers:     无（规格面）。**运维事件披露（需用户知悉）**：参照实例 artifactory 容器在 compress 探测后退场；根因=system.yaml 里 shared.node.ip 写死 192.168.1.70 而宿主机现网 IP 已变 192.168.1.149，router join 回连检查超时。已做恢复性修复：容器内备份 /opt/jfrog/artifactory/var/etc/system.yaml.bak-l0023 后将该 ip 改为 .149 并 docker restart；实例现已 200 正常。探测全程未建 audit-probe 资产、未写任何仓库数据；prune/gc 触发属维护面例行操作，终态报告=完整 256/256 finished（与探测前等价）
Next:         ① compatibility-engineer 契约化 D1–D4（L002 终案：POST 202 + status 报告 schema 对齐 26 字段集）；② V-2/V-3/V-5 差分腿；③ V-6 UI 会话型取证（浏览器流量）；④ G5/G7 回收资格窗口专项（binary-provider-chain §8-1）
