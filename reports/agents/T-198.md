# T-198 — README 中文版 + 内容刷新到 M6

- 票据：T-198 [P1·用户需求] README 中文版 + 内容刷新到 M6（BOARD.md 只读）
- 角色：tech-writer
- 范围：`README.md` / `README.zh-CN.md`（仅此两文件；reports/agents/ 本日志）
- 日期：2026-08-22

## 变更

1. **`README.md` 全量刷新**（268 → 341 行），定位段从 M1「kernel
   foundation / Generic local only」改写为 M6 现状：
   - 云原生制品仓库、架构对标 Artifactory、**五协议**（Generic / Docker
     Registry v2 含 OCI+Helm-oras / Maven / npm / PyPI）、local / remote /
     virtual 三仓型、内嵌控制台。
   - M6 企业层一段：OIDC/LDAP SSO、S3 后端+在线迁移（双写/断点续传）、
     push 复制（事件驱动+退避+cron 兜底）、Prometheus /metrics、bf CLI、
     bf-migrate。
2. **新增 `README.zh-CN.md`**（326 行），与英文版逐节等价（定位/链接表/
   快速开始/五步链/M6 能力/安全/配置/运维/开发/状态），中文行文 + 命令
   本体英文。两文件顶部互链语言切换行（English | 简体中文）。
3. 快速上手五步 curl 流程保留（M1 链路仍有效，实测复跑），`make build`
   产物说明更新为三二进制（binflow-server + bf + bf-migrate）。
4. 新增「Beyond curl」节：控制台入口、`auth/methods` SSO 探测示例、S3/
   复制/指标/CLI/迁移工具各一条要点 + 指向 docs/user/guides/ 六篇与
   metrics/prometheus-reference、install/、integrations、api、faq。
5. 链接表更新：+M6 PRD（milestone-6.md v1.2）、+帮助文档中心入口、
   +迭代报告入口；Makefile 目标表补 `make docs`；internal 包清单更新
   （+remote/replication/migrate/metrics/client）；运维节补 export/import。
6. 过时表述清零：删除「M1 delivers the kernel foundation」「M1 serves
   plain HTTP only」「the M1 security surface」「Quick start (M1)」等；
   保留的 M1 字样均为正当历史引用（「M1 acceptance chain」「M1–M5 done」）。

## 自测证据

### 1. 运行时验证（当日 HEAD 预构建 bin/binflow-server，临时数据目录
/tmp/bf-t198，端口 18080；本机 8080 被另一 dev server 占用）

五步链路（AC 要求保留的 curl 流程）逐条实跑：

```
curl -s :18080/binflow/api/system/ping                 → OK
PUT /api/repositories/generic-local                    → Successfully created repository 'generic-local'
PUT /binflow/generic-local/acme/hello.txt              → checksums.sha256 == 上传前本地 shasum -a 256
GET /binflow/generic-local/acme/hello.txt（匿名）      → diff 一致 + sha256 一致
```

M6 面实跑：

```
curl -s :18080/metrics | head                          → Prometheus 0.0.4 文本，
  binflow_auth_logins_total{source="ldap"/"local"/"oidc"} + binflow_http_request_duration_seconds 等
curl -s :18080/binflow/api/v1/auth/methods             → {"password":true,"oidc":false,"ldap":false}
curl -s -o /dev/null -w %{http_code} :18080/binflow/ui/ → 200
curl -su admin:password :18080/binflow/api/v1/health   → {"status":"ok","version":"dev",storage/metadata ok}
bf --version                                           → bf v1.0.0-snapshot.00f73e7 (00f73e7)
BF_BASE_URL=… bf repo create cli-demo                  → Repository 'cli-demo' created.
BF_BASE_URL=… bf artifact upload …                     → sha256 + uri 回显
BF_BASE_URL=… bf token create | head -1 所得 token     → Bearer 调 /api/repositories 200
binflow-server gc（BINFLOW_HOME 临时目录）             → gc: mode=dry-run grace=24h0m0s … candidates=0
```

注：README 示例中 bf 凭据环境变量用 `BF_BASE_URL`（非 PRD 草拟名
BF_SERVER）——以 `cmd/bf/config.go` 实现为准（--server 旗标 >
BF_BASE_URL / BINFLOW_SERVER_URL > profile）。

### 2. 链接核对清单（两文件 21 条相对链接逐一可达，0 MISS）

README.md 与 README.zh-CN.md 各 21 条（grep 提取 → `[ -e ]` 判定）：
PRODUCT.md / ROADMAP.md / BOARD.md / DECISIONS.md /
docs/prd/milestone-6.md / docs/user/README.md /
docs/design/architecture.md / docs/reverse/ / reports/ /
README.zh-CN.md（或 README.md 反向）/ docs/user/console.md /
docs/user/api-reference.md / docs/user/faq.md /
docs/user/install/ / docs/user/admin/backup-restore.md /
guides/{oidc-config,ldap-config,s3-config,bf-cli,migrate-artifactory}.md /
docs/user/metrics/prometheus-reference.md —— 全部 OK。
（未链接 docs/user/getting-started/：该目录不存在，docs/user/README.md
里的引用是他人文件的既有悬链，不在本票范围。）

### 3. 过时表述 grep 清零

```
grep -nE "kernel foundation|M1 delivers|M1 serves|M1 requirements|\
M1 security surface|Quick start \(M1\)|under active development \(M1\)|\
single static binary, zero external dependencies, serving" \
README.md README.zh-CN.md        → 无匹配（exit 1）
```

### 4. 行数

```
wc -l README.md README.zh-CN.md → 341 / 326（原 README.md 268）
```

### 5. make docs 不受影响（静态证明，未执行）

`make docs` = `cd docs-site && npm ci && npm run build` + 拷贝进
internal/docs/dist。Docusaurus docs 插件 `path: '../docs/user'`
（docusaurus.config.js:71），sidebar 的 doc id `README` 解析为
docs/user/README.md——仓库根 README.md / README.zh-CN.md 不在构建输入
树内。未实际执行的原因：该目标会重写 internal/docs/dist/ 与
docs-site/build/（范围外文件，属在途他人工作区）。

### 6. 其他核对

- compose 卷名 `binflow_binflow-data`：deploy/dev/docker-compose.yml
  `name: binflow` + volume key `binflow-data` → 前缀拼接结果仍成立。
- deploy/nginx/ TLS 模板（ssl.conf.template）存在，安全节引用成立。
- internal/ 包清单以 `ls internal/` 实测为准（15 包）。
- 服务端子命令集（serve/gc/export/import）核对
  cmd/binflow-server/main.go usage 常量。

## 遗留 / 待确认

- 无「待确认」项写入 README。一处事实性取舍：`make build` 当前在本人
  验证窗口内因**他人未提交的在途文件** internal/replication/push_docker.go
  （untracked，T-195 多协议复制）语法错误而失败——README 按已提交基线
  （00f73e7，`bin/` 预构建二进制 08:39 产出）描述构建行为，不反映瞬态
  工作区状态；该问题归 T-195，不属本票。
- README 中文版锚点 `#配置`：GitHub 对中文标题生成的 anchor 可达；
  其他渲染器（若未来接入 docs-site i18n）需复核——docs-site 当前唯一
  locale 为 zh 且不收录根 README，暂无影响。
