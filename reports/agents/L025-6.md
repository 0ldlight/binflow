# L025-6 — LOOP 025 G1：config 四层键面全渲染（批次 3 深水收官）

Ticket:       L025-6（LOOP 025 G1：config 四层键面全渲染——批次 3 深水收官票）P1
Role:         dev-go-core（area = internal/httpapi）
Area:         internal/httpapi（渲染层；路由与门未动，storage/repo 服务层未动）
Input:        docs/reverse/rest-api.md §2.1.9 键面实测锚 + §2.1.1–2.1.4 五面规格；wire=reports/compatibility/l025q-wire/a/（c01/b01/w06b/v01/v05）；L025-4 差分报告 G1（B 现状=统一 5 键投影+local 带 url，账实不符）；architecture.md §routeAuth 家族表（v1 GET=repoManage 家族 7）
Changes:      ① 新增 repo_config_render.go：参照实测键位表（cfgSeat 键+实测默认值）× 六面（v1/configurations×local/remote/virtual，v2 三面=同键集 type 方言），renderConfigSeats 渲染器（行三列 key/packageType/description、blob 值优先、未建模键按 wire 默认值形、password 恒 ""、environments 别名回退 stages、contentSynchronisation 平铺→嵌套重塑、virtual repoLayoutRef 条件键）+ partialConfigMap 非 admin 分 rclass 投影。② handleRepoConfigurations 改键位表渲染（18/46/12，删旧 11 键 entry 结构）。③ handleRepoGet（v1）拆 admin/非 admin：CapRepoWrite→全量 61/102/50（local 无 url、无 configuration 回显）；路由家族 7 门内非 admin→rclass 方言 partial。④ handleRepoBatchGet 逐仓 admin→全量/非 admin→partial。⑤ v2 面 v2ConfigMap 改键位表渲染（18/46/12 type 方言），v2PartialConfigMap 改分 rclass（local 4 键无 url、remote +url、virtual +repositories——修 B「local 带 url」账实不符）。⑥ 删 repoConfigOf/blobBool 与「configuration」GET 回显。⑦ 补空白 wire：参照 172.16.58.130:8082 只读探针（资产 l025r-{loc,rem,virt,u} 用后删净，复核 400/404）——新定谳 v1 remote 全量=102 键、v2 remote 46/virtual 12 交叉证、非 admin 四形态（v1/batch=rclass 方言 local 4 键 / remote +url / virtual +repositories；v2=type 方言同构）。
Files:        新增：internal/httpapi/repo_config_render.go（键位表+渲染器）；internal/httpapi/repo_config_projection_test.go（四层键集精确断言+值形）。修改：internal/httpapi/repositories.go（handleRepoGet 拆分，删 repoConfigOf）；internal/httpapi/repo_config_read.go（configurations/v2/batch 三面改键位表渲染，删 blobBool）；internal/httpapi/repo_batch_write_test.go、repo_config_roundtrip_test.go、repo_url_echo_test.go、remote_browse_flag_wire_test.go、remote_metadata_ttl_wire_test.go、t290_smart_remote_wire_test.go、t327r_policy_transport_test.go、t329de_enforce_transport_test.go、t355a_conan_forced_auth_transport_test.go、t80_repo_model_test.go、t95_governance_test.go（configuration 回显读位迁移：A 键集内字段→顶层；BinFlow 专键→list 面 configuration 回显，新增测试助手 listConfigurationOf）。删除：无文件；删代码=repoConfigOf/blobBool/repoConfigurationEntry/v2PartialConfigMap 旧体。
Tests:        新增 TestRepoConfigKeyFacesAdmin（8 面×键集精确：v1 61/102/50、v2 18/46/12、batch=v1、configurations 18/46/12、local 无 url）；TestRepoConfigKeyFacesNonAdmin（v2/v2-batch 分 rclass 4/5 键、v1 plain-user 仍 403 家族 7 钉住）；TestRepoConfigFaceValueShapes（wire 默认值形逐键、blob 透传、contentSynchronisation 嵌套重塑+平铺键贯穿、cargoInternalIndex virtual true/local false、virtual repoLayoutRef 未设省略/已设渲染、stages 别名→environments）。既有 11 个测试文件迁移读位（键集内顶层直读；quotaBytes/byHash/defaultDeploymentRepo/socketTimeoutSecs/metadataRetrievalCachePeriodSecs 等 BinFlow 专键改读 list 面 blob 回显）。
Commands:     go build ./... && go vet ./... && gofmt -l internal/httpapi/ →「BUILD+VET+FMT OK」；~/go/bin/golangci-lint run internal/httpapi/... →「0 issues.」；go test ./internal/httpapi/ -count=1 → ok 183.595s；go test ./internal/repo/ ./internal/metadata/ -count=1 → ok 71.933s / ok 19.619s；go test -race ./internal/httpapi/ -count=1 →（见 Outputs 末行）；隔离实例冒烟：go build ./cmd/binflow-server → BINFLOW_HOME=/tmp/l025smoke 127.0.0.1:18099 起、镜像仓 l025s-{loc,rem,virt}+非 admin l025s-u 建、13 面 curl 取体 → python 逐层键集 diff（脚本内嵌本日志 Outputs）→ pkill 收净；参照探针：curl :8082 建/读/删 l025r-*（复核实录见 Changes⑦）。
Outputs:      冒烟键集 diff 全 SAME：configurations LOCAL 18/REMOTE 46/VIRTUAL 12；v1 local 61 / remote 102 / virtual 50；batch 三仓=v1；v2 18/46/12；非 admin v2 local 4 / remote 5 / virtual 5；非 admin batch local 4 / remote 5 / virtual 5——18/18 键名集合全等（ALL KEY SETS EQUAL）。值形差仅三类：实例态键（key/repositories/description 随仓名）、hexPublicKey（A=实例 PEM 451 字节，B=""，已登记）、其余全等。参照探针定谳：v1-rem 102 键 / v2-rem 46 / v2-virt 12 / v1-loc-nonadmin 4 键 {description,key,packageType,rclass} / v2-loc-nonadmin 4 键（无 url）/ v2-virt-nonadmin 5 键（含 repositories）/ batch-nonadmin=rclass 方言同构。清场复核：l025r-* 三仓 GET=400（未知键）、l025r-u GET=404。race 见下。
Compatibility: 四层落地对齐 §2.1.9 锚：①configurations 18/46/12（rclass，local/virtual 无 url）②v1+批读 61/102/50（local 无 url——修 G1 主漂移）③v2 18/46/12（type 方言）④非 admin 分 rclass 4/5 键（修「统一 5 键+local 带 url」账实不符；v05 remote 臂逐键同）。契约对照：docs/compatibility/contracts/d02-config.yaml 六 DIVERGENT 条目的渲染面本票闭合（读侧），翻态归 compatibility-engineer 复跑差分。新 wire 定谳待回填：v1 remote 全量=102 键（§2.1.9 表「≥46」升格）、非 admin 四形态（§2.1.3「非 admin 仅 5 键」需改「分 rclass 4/5 键」）、§2.1.3 virtual「仅 9 键」笔误→12 键（活体 c01/v2-virt 双证）——归 reverse-engineer/conductor。漂移登记：hexPublicKey 值形（实例态，键名在集）；v1 plain-user 门（A=200 partial，B=403 家族 7——架构钉住未擅改，候裁）；BinFlow 专键自 v1 详读面消失（见 Risks FE 项）。
Security:     password 恒渲染 ""（NFR-S14，渲染器硬编码不查 blob）；探针参照凭证仅本次会话使用、未落盘任何新文件；冒烟实例临时目录收净；用户实例（用户自有 binflow-server 进程）核对命令行后未触碰；非 admin 投影片面收窄（少于修复前）——4/5 键无任何配置细节，负例测试断言 hardFail/配置键不出现（TestRepoConfigV2NonAdminPartial 既有）+ 新键集断言即负例。
Performance:  无新增 IO；渲染为纯内存键位表遍历（≤102 座/仓），冒烟与单测无感知差异。无。
Risks:        ① FE 读位断裂（本票最大外溢）：GET /api/repositories/{key} 不再回「configuration」与 BinFlow 专键（quotaBytes/byHash/deb 族/defaultDeploymentRepo/allowPrivateUpstream/socketTimeoutSecs/metadataRetrievalCachePeriodSecs/chartsBaseUrl/yumGroupFileNames），web/src/lib/repos.ts（getRepoDetail.buildLocalQuotaBody/RepositoryFormPage/QuotasPage/RepoDetailPage/ExplorerPage）读 d.configuration.* 将得 undefined——已知写路径隐患：buildLocalQuotaBody 以 priorityResolution:false 兜底，配额行内编辑会把存量为 true 的 local 仓翻成 false（POST merge 方言保字符串字段、不保显式布尔）——需 dev-frontend 票改读顶层+list 面，conductor 裁序。② v1 plain-user 403 vs A 200 partial：家族 7 钉住，未擅改（test 钉 403）；候 architect 修表后一票翻门。③ 键位表为「实测样本值」——A 侧 null 省略致同 rclass 键数随仓浮动（如 projectKey 有项目才出现、virtual repoLayoutRef 设才出现）；本表按未设样本钉死，设态臂已按 L006-A 证（repoLayoutRef 条件渲染）；projectKey BinFlow 无项目域恒不出现。④ 值形未逐键对齐面：hexPublicKey（登记）；contentSynchronisation 部分对象渲染时未提子键=false（与 A replace 语义一致，parseContentSynchronisation 文档）。⑤ 探针原始体在 /tmp/l025r-wire/（会话临时）——键集已永久钉进 repo_config_projection_test.go 常量与 repo_config_render.go 键位表；建议 compatibility-engineer 把 13 探针体收编 l025q-wire 体例。
Blockers:     无
Next:         ① conductor：FE 读位迁移票（dev-frontend，优先级高——见 Risks① 写路径隐患）；② architect：家族 7 表修法裁定（v1 GET 门 required-only + handler 分投影，探针已备）；③ reverse-engineer：§2.1.9/§2.1.3 回填（v1 remote 102、非 admin 分 rclass 4/5 键、virtual 12 键）；④ compatibility-engineer：d02-config 差分复跑（normalize 键序后 18 面应全绿）+ 探针体收编 + matrix D02 三行翻态；⑤ 本票不 commit（纪律），主会话四门绿后统一收。

## 附：冒烟键集 diff 命令关键输出（python，2026-09-17）

```
SAME configurations/LOCAL         got= 18 want= 18
SAME configurations/REMOTE       got= 46 want= 46
SAME configurations/VIRTUAL      got= 12 want= 12
SAME v1 local (61) / v1 remote (102) / v1 virtual (50)
SAME batch local / remote / virtual（=v1 各键集）
SAME v2 local (18) / v2 remote (46) / v2 virtual (12)
SAME nonadmin v2 local (4) / remote (5) / virtual (5)
SAME nonadmin batch local (4) / remote (5) / virtual (5)
ALL KEY SETS EQUAL
```

race: 触碰面定向 ok 22.917s（TestRepoConfigKeyFaces/FaceValueShapes/V2/BatchRead/Configurations/URLEcho/T290/M02）；全包 `go test -race ./internal/httpapi/ -count=1 -timeout 25m` → **ok 920.002s**（首跑 601s 撞 go 默认 10m 包超时——套件在 -race 下超默认时限，非失败非数据竞争；加时限重跑定谳）。

## 附二：未建模键清单+处置（渲染=wire 实测默认值形；blob 同名键存则透传，前向兼容）

处置统一：**键名在集、值照 A 实测默认**；无「wire 没有的键形」遗留（v1-remote/非 admin 两处空白面已按票面授权探针补齐定谳，非猜测）。唯一值形漂移=hexPublicKey（A 实例 PEM vs B ""，实例态键）。

- **local 61 中 39 键未建模**：blockPushingSchema1, cargoAnonymousAccess, cargoInternalIndex, cdnRedirect, ddebSupported, debianTrivialLayout, dockerApiVersion, dockerProjectId, dockerTagRetention, downloadRedirect, enable{Bower,Chef,CocoaPods,Composer,ComposerV1,Conan,Debian,Dist,Docker,Gems,GitLfs,Normalized,Npm,NuGet,Puppet,Pypi,Vagrant}Support(17), encryptStates, forceNugetAuthentication, forceP2Authentication, maxUniqueTags, mlRepoLayout, notes, propertySets, signedUrlTtl, suppressPomConsistencyChecks, terraformType, xrayDataTtl, xrayIndex
- **remote 102 中 80 键未建模**：上述 local 族 + 远端族 allowAnyHostAuth, authUrl, blockMismatchingMimeTypes, bowerRegistryUrl, bypassHeadRequests, composerRegistryUrl, curated, customHttpHeaders, disableProxy, disableUrlNormalization, externalDependenciesEnabled, fetchJarsEagerly, fetchSourcesEagerly, gitLabResolveSubgroups, gitRegistryUrl, hexPublicKey, includesPattern/excludesPattern（remote 臂不存，渲染 **/* 与 ""）, localAddress, offline, passThrough, podsCdnUrl, podsForceModifyIndex, podsSpecsRepoUrl, propagateQueryParams, pyPIRegistryUrl, pyPIRepositorySuffix, rejectInvalidJars, remoteRepoChecksumPolicyType, retrieveSha256FromServer, sendContext, shareConfiguration, storeArtifactsLocally, synchronizeProperties, terraformProvidersUrl, terraformRegistryUrl, vcsGitProvider, vcsType
- **virtual 50 中 45 键未建模**：上述包型族 + virtual 族 artifactoryRequestsCanRetrieveRemoteArtifacts, cachingLocalForeignLayersEnabled, forceMavenAuthentication, hideUnauthorizedResources, keyPair（BinFlow 的 keyPairName 是 local deb/rpm 签名引用，另一概念）, pomRepositoryReferencesCleanupPolicy, resolveDockerTagsByTimestamp, useNamespaces, virtualRetrievalCachePeriodSecs, priorityResolution/environments/includesPattern/excludesPattern（virtual 臂不存）
- 已建模透传键（渲染真值）：local=repoLayoutRef/blackedOut/priorityResolution/environments(stages)；remote=url/username/retrievalCachePeriodSecs/missedRetrievalCachePeriodSecs/socketTimeoutMillis/metadataRetrievalTimeoutSecs/unusedArtifactsCleanupPeriodHours/assumedOfflinePeriodSecs/hardFail/enableTokenAuthentication/contentSynchronisation(平铺→嵌套重塑)/listRemoteFolderItems/repoLayoutRef/blackedOut/maxUniqueSnapshots/archiveBrowsingEnabled/priorityResolution；virtual=repositories/repoLayoutRef(条件键)；password 恒 ""（NFR-S14，不查 blob）
