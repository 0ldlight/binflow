# T-L014-2 · Maven 双 BUG：unique-snapshot 服务端改写 + checksum 旁车物化对齐

Ticket:       L014-2 · maven 双 BUG 票（LOOP 014，dev-registry-adapter maven 域唯一实例）· P0
Role:         dev-registry-adapter（领域实例：dev-package-maven）
Area:         internal/adapter/maven
Input:        reports/compatibility/L013-maven-evidence.md §1（18 臂矩阵+根因聚类）；docs/reverse/maven-npm-pypi.md §1.3/§1.4/§1.5；docs/compatibility/matrix.yaml D12-R08；本票活参照探针（artifactory-ux :8082 7.161.20，wire 落 reports/compatibility/l0142-wire/）
Changes:      ① BUG1 服务端 unique-snapshot 改写：新增 snapshot.go（calculator 上的 adjustUniqueSnapshot/adjustCompanionTarget + snapshotDirFacts/tripOf/coordinateFile），PUT -SNAPSHOT 工件名改写为 {module}-{baseRev}-yyyyMMdd.HHmmss-N[-clf].{ext}，201 Location/存储节点/计算器触发全部指向改写名；改写模型 17 项参照观测全吻合——主工件=领队 mint(now, trip.buildNumber+1)（同坐标现存文件 N≥候选则原位复用覆盖）、pom/classifier=跟随者（并入目录 trip；空目录 mint(now,1)）；侧车目标走 companion 规则（现存同坐标文件名优先）② BUG1 配套：ParseRepoConfig 缺省 snapshotVersionBehavior 由 deployer → **unique**（A6 wire 定案：参照未设字段的仓改写）；显式 deployer/non-unique 保持原名；未知拼写随缺省走 unique ③ BUG2 checksum 旁车 registration-only：putSidecar 不再落任何 .sha1/.md5 存储项（保留 >1024B 409、目标缺失 404、client-checksums 失配 409、metadata 目标容忍四门），201 Location 指向**目标工件**（参照 wire：d2 Location=jar 无 .sha1 尾）；GET 旁车路径照旧回服务端计算值 ④ 测试：snapshot_rewrite_test.go（wire 级+钉死时钟确定性两级）、TestSidecarStates 加不落项/Location 断言、provider_test 默认值断言、calc_test 非 unique 臂移入具名仓、共享 fixture 增 maven-{nonunique,deployer,unique} 三仓 ⑤ **Review 轮（REQUEST_CHANGES 两 blocking）**：B1 权限门穿透——trip 读取原走 svc.Get 被 ActionRead 门拦，write-without-read 部署者每趟降级 cand=1 原位覆盖上一构建；修法=trip 改从**未设门 facts 推导**（tripOf：newestUnique(poms) 否则 newestUnique(all)——与 recalcVersionDir 的 head 选取同式，存储行拿不到 XML 内容故弃 NodeReader.Get 方案；推导值与服务端计算的存储文档恒等〔每次 unique 落盘同步重算〕且永不会更陈旧），snapshotState 全链不再持 principal；B2 virtual 寻址盲区——改写原以 virtual key 查 facts（无节点→复用分支恒死→同 buildNumber 双时间戳无限累积、行为取 virtual 配置）；修法=handlePut 预取写目标（routeTargetOf 镜像 repo.virtualRouteTarget 的容错别名读，defaultDeploymentRepo 三别名首个非空），全 maven 链（ME-08 门/checksum 策略/改写/侧车目标查找）用 member key+member 行配置，PUT 与 Location 渲染保持客户端拼写的 virtual key，未路由 virtual 改写拒绝（服务 C5 405 不变），非 local 行强制 behavior=non-unique 兜底；⑥ fern/pages/integrations/maven.mdx snapshotVersionBehavior 行同步（默认 unique+改写语义）⑦ **第三轮（Review B 两 blocking）**：F1 pom 跟随规则分岔——活参照定案（probe f1a/f1b/n5/n6 落 l0142-wire/probe/）：**pom 永不并入当前 trip**（trip=pom 源时 N=trip+1、ts 取该 N 现存文件〔补领队 trip〕否则 now；无 pom 目录则退化为跟随者）；连带定案 tripOf 改**仅 pom 源**（参照 metadata 存在性⟺pom 存在，废 max-file 兜底）与无 metadata 目录 follower 取**listing 首个 unique 文件**（probe n3：低 trip 而非最大 N；BinFlow 上该分支因同步重算实际不可达，登记 probe-log.md）；F2 证据链断点——补录 reports/compatibility/l0142-wire/probe-log.md（round-1 17 项+review 轮 7 项观测→实现分支映射表+N1 素材节），测试注释改引真实路径；N4 未知拼写补 provider_test 行
Files:        新增 internal/adapter/maven/snapshot.go、internal/adapter/maven/snapshot_rewrite_test.go、internal/adapter/maven/snapshot_routing_test.go、tools/difftest/l0142/{maven-replay.sh,maven-judge.py,build-image-offline.sh}、reports/compatibility/l0142-wire/probe-log.md；修改 internal/adapter/maven/{config.go,put.go}、internal/adapter/maven/{maven_test.go,calc_test.go,provider_test.go}、fern/pages/integrations/maven.mdx；wire 证据 reports/compatibility/l0142-wire/{a,b}/maven/（129 文件×2 侧）+ probe/（F1 原始 hdr/body）。删除：无
Tests:        TestSnapshotRewriteWire（缺省仓改写/原 404/改写名 200/无 pom 目录 pom 跟随/F1 pom 重 PUT 开新 trip/二趟 N=3/同坐标原位复用/classifier 并入 pom trip/already-unique 直通/non-unique+deployer 原名/显式 unique）；TestSnapshotRewriteDeterministic（钉死时钟 8 步 trip 表：领队 mint→pom 无 pom 目录跟随→pom 重 PUT 开新 trip〔f1a〕→领队 mint N+1→第三 pom 补领队 trip〔f1b〕→classifier 并入当前 trip〔n5〕→同坐标复用→already-unique）；TestSnapshotSidecarRewriteAndRegistration（companion Location=改写主文件/无 .sha1 节点/改写名旁车 GET=计算值/-SNAPSHOT 旁车 GET 404/错值 409）；TestSidecarStates 扩（不落项+Location=目标）；**Review 轮新增**：TestSnapshotRewriteWriteOnlyPrincipal（B1：write-only 主体〔PermissionTarget CanWrite-only〕pom;pom 序列 N=1→2、trip-1 字节幸存、该主体 metadata GET 403 证门存在）、TestSnapshotRewriteVirtualRouting（B2：virtual+defaultDeploymentRepo 三趟经 virtual key 编号 1→跟随同 trip→2、文件落 member 三件、virtual key 零节点、Location 渲染 virtual 拼写、未路由 virtual 405 不变）；provider_test 增 N4 未知拼写行（"never"→unique）；全套 ./internal/adapter/maven -count=1 -race 通过；四门+全包清查通过（internal/client 两用例在前置提交 b4de1ef5 即红——T-231/T-233 域既有失败，独立 worktree 复核，非本票回归）
Commands:     go build ./... && go vet ./...（空输出）；gofmt -l internal/adapter（空）；$HOME/go/bin/golangci-lint run ./internal/adapter/...（0 issues）；go test ./internal/adapter/maven/ -count=1（ok 17.4s，F1 后）+ -count=1 -race（ok 61.9s，含 B1/B2/F1 全用例）；go test ./... -count=1 终轮全包（38 包 ok；internal/client 两用例失败=前置 b4de1ef5 即红，独立 worktree 复核归属 T-231/T-233）；bash tools/difftest/l0142/maven-replay.sh {a,b}（双端 129 wire 文件，UAT=binflow:uat-l0142r2-bcf27bfa-alpine）；python3 tools/difftest/l0142/maven-judge.py（23/3）；mvn 3.9.16 -Dmaven.resolver.transport=wagon deploy（双侧 exit=0 BUILD SUCCESS）；参照探针 round-1 ×17 + F1 轮 ×7（probe-log.md）
Outputs:      终判定表 **一致 23 / 差异 3**（26 臂=原 18 臂+B7/B8/D5+B9/B10 复用臂+F1a/F1b/F1c pom 规则臂；tools/difftest/l0142/maven-judge.py 输出，wire=reports/compatibility/l0142-wire/{a,b}/maven/ 各 129 文件，UAT=binflow:uat-l0142r2-bcf27bfa-alpine @:8083）：A6/B1/B2 ✓（缺省仓 pom→SNAPTS-1、unique 仓 jar→SNAPTS-1、jar 1/pom 同 trip/二趟 2）；**B9/B10 ✓**（第 3/4 次领队 PUT 双端同坐标原位复用，与 b3 同 Location）；**F1a ✓**（pom 重 PUT 1→2 双端）；**F1b ✓**（pom 后于领队→双端同形 pom2==jar 的 (ts,2)）；**F1c ✓**（classifier 并入本端 pom2 trip）；E2 ✓（mvn 腿 snapdir 3 项=3 项）；D5/B7/B8 ✓；剩余差异恰为三 UNKNOWN（A4 XML 形态 / C1 手 PUT 可见性 / C3 参照自丢 metadata）。过程教训：重放脚本输出禁接 `| head`（SIGPIPE 杀半程脚本留脏数据——b 侧曾污染为 N=7，deleteContent 清仓重跑后恢复）；judge 跨端比较须归一化时间戳或改端内一致性（F1c 修正）
Compatibility: 契约对照结论：L013 差异 7 臂 → 4 臂翻绿（A6/B1/B2/E2），三 UNKNOWN 维持待裁（素材见 Risks）；可差分 surface（供 differential-qa-engineer）：PUT -SNAPSHOT 工件（三态仓×已 unique 名）Location 形态、pom 重 PUT/领队后 pom 的 trip 规则（F1a/F1b/F1c）、复用臂（B9/B10）、侧车 PUT 状态码+Location+listing 无项、mvn deploy 后 snapdir item 集、group/snapdir metadata 语义、checksum 策略四臂、并行双臂。**漂移点上报**（spec conflict，交 compatibility-engineer）：spec §1.3「ts 优先取 build.timestamp property」与 wire 矛盾——7.161.20 mint 路径两次实测均用服务端 now（build.timestamp 仅落为节点 property，携带形态=deploy 矩阵参数；epoch-millis 等形态未探——N1 素材已归 probe-log.md 供下轮裁决票）；本实现从 wire
Security:    路径穿越：改写名仅由 Layout 解析字段重组（Module/BaseRev/classifier/ext 均来自已验证路径段），无新输入面；SSRF：无 remote 交互新增；认证：**Review B1 后改写全链不再触发放行读取**（trip 从 NodeReader facts 推导，无 svc.Get）——write-without-read 部署者与全权部署者行为一致，权限面不再影响编号；routeTargetOf 仅读 virtual 行自身 config（管理面已返回数据），无新查询面
Performance:  每次 -SNAPSHOT PUT 增一次目录 ListByPrefix + 一次 metadata 读（version 目录直接子节点，规模=<该版本文件数>，与 FR-17 recalc 同量级）；无大制品影响（流式不缓冲）；无
Risks:        ① checksum-deploy（X-Checksum-Deploy）路径未做改写（参照该路径行为未探，零传输面少见，留后继票）② mvn 版本覆盖：mvn 3.9.16 wagon 腿验证；Maven 2 真客户端不可用（raw -SNAPSHOT PUT 由 curl 复刻）③ 参照 admin 面拦截器（recurrent failures）偶发重试，wire 均终态落盘 ④ UI 建仓默认 deployer 与 REST 缺省 unique 不一致（web/ 出域，fern 已随票同步）⑤ routeTargetOf 为 repo.virtualRouteTarget 的镜像（别名三元组+首非空容错）——seam 未导出的妥协，漂移面=别名集合（T-71 起稳定）；若 repo 侧导出 seam 应回收镜像 ⑥ calc.go isSnapshotTypedMetadata（RTFACT-6242 探针）仍走 svc.Get 带触发主体读门——fail-safe 方向（读失败→保留文档）且内容读取无可推导替代，未随 B1 改；登记为独立既有面 ⑦ 三 UNKNOWN 素材：**A4**——modelVersion="1.1.0" 属性+尾部 <version> 元素+snapshot 块子序（lastUpdated 在前）；新证：mvn 3.9.16 对三偏差全容忍（双端 deploy 全绿）；**C1**——BinFlow 201 后同步重算（bogus 1s 不可见）vs 参照逐字回放至下次触发；终态等价；**C3**——新根因假说：参照版本目录 metadata 生成**要求 pom 存在**（本票 jar-only 目录实测参照 metadata 404，BinFlow 从任意文件生成 574B）——C3 的「参照自丢」很可能同根（竞态目录仅 jar），建议 reverse-engineer 补规格条目后重分类
Blockers:     无
Next:         ① D12-R08 翻绿建议：state partial→compatible（依据=l0142-wire 18/3 判定表+差分 surface 清单），由 compatibility-engineer 落 matrix.yaml；三 UNKNOWN 建议在 docs/compatibility/known-divergence.yaml 单列（A4 附 mvn 容忍新证）② spec §1.3 build.timestamp 漂移裁决票（compatibility-engineer）③ reverse-engineer 补「版本目录 metadata 需 pom」行为规格（C3 重分类前置）④ web 建仓表单默认值与 REST 缺省对齐票（dev-frontend；fern 已随本票同步）⑤ 后继：X-Checksum-Deploy 的 snapshot 改写行为探针；B1 修复的 B 轨互补——若 B 轨（ResolveMeta 谓词链）发现交集面，本包 facts 推导口径不变可直接复用 ⑥ 复用臂探针已入 maven-replay.sh（b9/b10 第 3/4 次领队 PUT）与 maven-judge.py（B9/B10 行），下轮 replay 直接出数 ⑦ repo 侧可考虑导出 virtual 写路由 seam（routeTargetOf 镜像回收，architect 票）
