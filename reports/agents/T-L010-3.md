# T-L010-3 — ?list 规格刷新（LOOP 010 轨道 L010-3，reverse-engineer）

Ticket:       L010-3 ?list 规格刷新票（rest-api.md:119 与 api-inventory.yaml:53 旧语义翻案）
Role:         reverse-engineer（本票实例域：REST 表面 ?list 参数族行为规格）
Area:         docs/reverse/rest-api.md / docs/reverse/api-inventory.yaml
Input:        派发缺口：两处 ?list 行仍记 root-400/相对 uri/无七参校验/无形态五项，与 L008-3 实测（reports/compatibility/L008-list-params-diff.md，32 臂）及 L009-2 实现（reports/agents/T-L009-2.md）相抵；证据等级：实测报告 > 旧规格行。参照公开规范：JFrog Get Storage Item Information（docs.jfrog.com/artifactory/reference/getstorageitem）。
Changes:      ① rest-api.md:119 表行整体翻新（七参全列 + 整数解析语义 + 根可列 + 文件目标新文案 + vendor CT 指针）；② 表后新增 T-L010-3 勘误块 11 条（触发与校验/空值缺席/递归语义/listFolders/includeRootPath/mdTimestamps/statsTimestamps/includePropertiesMd5/形态五项/根与错误体裁/校验次序），逐条置信度与证据臂号；③ §6 官方差异补充面 bullet 翻新（原「根目录 400 文案」bullet 作废，改为七项补充面清单）；④ 待验证清单新增 4 条（校验次序参照侧取证、根条目 lastModified 形态、sha2 在列存疑）；⑤ api-inventory.yaml storage-item 资源 list 行同步（七参 path + note 摘要）+ evidence 行加 L008-3 32 臂出处。
Files:        修改 docs/reverse/rest-api.md（4 处）、docs/reverse/api-inventory.yaml（2 处）；新建 reports/agents/T-L010-3.md（本日志）。
Tests:        活体抽验 6+2 臂（参照 :8082，只读 GET）：depth=2147483648 → 400 envelope `For input string: "2147483648"`；depth=-2147483649 → 400 同文案；depth=2147483647 → 200（边界内过）；depth=（空）→ 200；depth=%20 → 200；deep=&listFolders=%20 → 200（多参空白组合）——前六臂全部命中预期，把原「中（推导）」的 int32 界与空值缺席两项升「高（活体+反编译双证）」。另：仓库根可列、vendor CT、created 逐请求推进（4 个 200 臂 created 各异：11:13:32/11:13:40/11:13:55/11:15:36）、顶层 uri 裸仓 key 均随臂顺带再证。补充探针（includeRootPath 根条目 lastModified 形态 + 坏参×文件目标次序）未回收——参照实例在六臂取证完成后停止响应（ping 超时，exit 28），未尝试重启（只读探查授权不含服务操作），对应条目留待验证清单。
Commands:     grep -rn "?list|listFolders|Cannot list" docs/reverse/（命中 rest-api.md:119,165 / api-inventory.yaml:53 / rest-compat-matrix.md 行59,62,356,430）；sed -n '365,400p' reverse-src/artifactory/backend/artifactory-rest/org/artifactory/rest/resource/artifact/ArtifactResource.java（getQueryParameterAsInt：containsKey+isNotBlank 短路→Integer.parseInt；writeStreamingFileList 七参全列；IllegalArgumentException→BadRequestException——Review A/B 两处推导的代码锚逐字坐实）；curl 参照 :8082 六臂（见 Tests）；WebSearch+WebFetch 官方 getstorageitem 页（记载六参 list/deep/depth/listFolders/mdTimestamps/includeRootPath + FileListItem 字段 + vendor CT + "Requires a non-anonymous privileged user"；**未记载** statsTimestamps/includePropertiesMd5/根行为/整数校验文案——补充面标注依据）。
Outputs:      规格文件 2（rest-api.md 勘误块 11 条规格行 + 表行 1 + §6 bullet 1 + 待验证 4 条；api-inventory.yaml 行 1 + evidence 1）。覆盖统计：?list 参数族端点 1 / 语义流程 11 / 形态规则 5（形态五项并入第 9 条）。
Compatibility: 置信度分布（勘误块 11 条）：高 10 / 中 1（校验次序——反编译代码序 + BinFlow 侧测试，参照侧未活体取证）。与官方规范差异/补充 7 点：statsTimestamps、includePropertiesMd5 两参未见于官方页；根可列、非整数/越界 400 文案、空值缺席、文件目标 400 文案、Cannot-list-root 仅无仓段——均标「此条补充官方规范」。官方页印证面：六参记载、FileListItem uri 前导斜杠示例、size "-1"、vendor CT、匿名禁止。
Security:     匿名 → 403（反编译 AuthorizationRestException "This resource is available to authenticated users only." + 官方 "Requires a non-anonymous privileged user" 双证）——规格行保留「仅认证用户」前置；参照凭据仅进程内环境变量使用，未落盘。
Performance:  created=请求时刻墙钟（逐请求推进，非文件夹创建时刻）已入规格第 9 条；Content-Type vendor 媒体类型同条。无其他时序/缓存阈值面。
Risks:        ① sha2 在列存疑：旧规格记 `sha2?`，L008-3 证据面只到 sha1 逐字级——表行保留 `sha2?` 原样并进待验证清单第 6 条，未擅自删改；② 校验次序（勘误块第 11 条）为代码序推导，参照侧未取证；③ rest-compat-matrix.md 行 5/行 356 的「?list 参数族待核对 ◐」状态已由 L008-3+L009-2 收敛，但该文件非本票派发域未动——建议 conductor 派翻态（或并入 docs/reverse 域下票），matrix.yaml/known-divergence 翻绿归 compatibility-engineer（T-L009-2 Next 已列建议）；④ 参照实例本轮尾段失联（六臂完成后 ping 超时），补充探针两臂未回收，不影响已落盘条目置信度；实例恢复后可 5 分钟内补齐两臂。
Blockers:     无（reverse-src 在位；领域代码可定位）。
Next:         ① LOOP 010 合并差分覆盖待验证第 4/5 条（校验次序臂 + includeRootPath 根条目 lastModified 字段形态——后者若逐字节对拍发现 1970 零时刻差需 BinFlow 侧再修，见 T-L009-2 Risks ①）；② sha2 在列取证（可并入 ① 差分臂）；③ rest-compat-matrix.md ?list 行翻态票；④ P2 三参（mdTimestamps/includePropertiesMd5/statsTimestamps）实现票规格已备（勘误块 6/7/8 条），includePropertiesMd5 的跨系统等值性仍待 compatibility-engineer 先定属性规范序列化 md5 规格（T-L009-2 Next ① 同款提醒）。

## 断点快照
（票已完成，无断点。补充探针 bgpbzh3pk 若后续回收：includeRootPath 根条目 lastModified 形态 → 待验证第 5 条；坏参×文件目标次序 → 勘误块第 11 条升置信度。）
