# T-L017-1 — pypi 双 BUG 实现票（D1 requires-python 三源 / D7 坏元数据索引拒入）

Ticket:       T-L017-1 pypi 双 BUG 实现票（LOOP 017 / 轨道 L017-1 / P0）
Role:         dev-registry-adapter（领域实例：dev-package-pypi）
Area:         internal/adapter/pypi（+ 契约 D1/D7 复验翻记 + 受波及测试 fixture）
Input:        reports/compatibility/L016-pypi-evidence.md（19 臂，D1/D7 判 BUG）；docs/compatibility/contracts/pypi.yaml（anchor-attrs/badwheel-matrix 两 DIVERGENT 条目）；docs/reverse/maven-npm-pypi.md §3.2（锚点模板/属性序/HTML 转义）§3.3（上传字段族）§4.5（无 sidecar、由制品事实重建索引）；normalize#pypi（fixtures/normalize.yaml L103-132——锚点属性为断言目标不归一）；wire 金样 l016-wire/a/pypi/{p2-pkg-page,y1-index-after,i1-root-t0}.body
Changes:      ① D1：simple 包页锚点渲染 data-requires-python（值 HTML 转义，`&gt;=3.8` 逐字同 A；属性位于 href 闭引号后、锚文本前）——派生源=制品自身元数据（wheel `*.dist-info/METADATA`、sdist `PKG-INFO`），索引渲染时按文件打开解析（distmeta.go：zip 走 ReaderAt 只读中央目录+METADATA 成员；tar.gz 流式扫至深度≤2 的 PKG-INFO，成员数/解压量/头块三重上限防炸弹）；twine 表单源与文件元数据恒同值（twine-raw.req 亲证其值源自 PKG-INFO），三源渲染结果与 A 一致；legacy JSON（D9 未裁）不动——requires-python 同源逻辑落在 simple 面：PEP 691 JSON face 补 `requires-python` 字段（omitempty） ② D7：索引准入对齐「入库不入索引」——`.whl` 解析不出 `*.dist-info/METADATA`（非 zip/伪字节[L015 P4]或无 METADATA 成员[L016 b2]）→ 存储照常（上传 200、双下载面可达）但 simple 包页与根索引均拒入（「不入任何索引」）；坏名/坏版本维持现状（双端同放行）；坏 sdist 不入拒集（无取证臂，证据 bounded，测试注明） ③ blob 读取走 `h.st.Open(sha256)`——绕开 svc.Get 的 markDownload（索引渲染绝不计下载统计）；virtual 的 local member 聚合同过一遍 enrichment（虚拟视图不重新污染）；remote member 页面解析（parseSimpleHTML/JSON）顺带转发 requires-python 使重映射无损 ④ 受波及 fixture 修正：3 处旧测试用伪字节 wheel 断言索引在场（simple_test×2/upload happy path×1）+ replication pypi 腿——全部改真 wheel zip
Files:        新增 internal/adapter/pypi/distmeta.go（元数据派生+准入）、distmeta_test.go（解析器单测+真 wheel/sdist fixture 构造器）、requires_python_render_test.go（D1 渲染 5 测）、badmeta_admission_test.go（D7 准入 3 测）；修改 internal/adapter/pypi/simple.go（indexEntry.requiresPython/anchorAttrs/渲染/JSON face/projectNames 准入）、virtual_simple.go（共享渲染/成员 enrichment/属性转发）、simple_test.go、upload_test.go（fixture 真化）、internal/replication/engine_protocols_test.go（fixture 真化——越界最小修正，见 Risks）；契约 docs/compatibility/contracts/pypi.yaml（头注+anchor-attrs/badwheel-matrix binflow_state 复验翻记+L017 证据行+pep691 note）；deploy/compose/.env.uat（UAT 版签本地文件，gitignored，沿 L012 先例）；工具 tools/difftest/l017/{fixtures,replay.sh}（复跑取证 scratch，不 commit；replay.sh 注明 hand-off differential-qa-engineer）
Tests:        TestWheelFacts/TestSdistFacts/TestScanRequiresPython（单测×3：有/无 Requires-Python、badmeta 无 METADATA、伪字节、坏 gzip、大小写/折行/首现/空行界）；TestRequiresPythonWheelAnchor/SdistAnchor/Escaped/JSONFace/VirtualMember（D1：HTML 转义逐字、PKG-INFO 无表单字段派生、注入转义、PEP 691 omitempty、virtual 面）；TestBadmetaWheelAdmission/OnlyWheel404/TestSdistWithoutPkgInfoStaysIndexed（D7：三态矩阵+下载面不受影响+根索引同拒+证据 bounded sdist）；回归：pypi 包全量 + httpapi + replication + adapter 全域 + TestClientEndToEnd（真实客户端腿）
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter（空）&& ~/go/bin/golangci-lint run internal/adapter/... internal/replication/...（0 issues）；go test ./internal/adapter/pypi/ -count=1（ok 7.6s）；go test ./internal/adapter/pypi/ -count=1 -race（ok 34.6s）；go test ./internal/adapter/... ./internal/httpapi/... ./internal/replication/... -count=1（全 ok）；BINFLOW_T70_CLIENT_E2E=1 …/l017venv go test -run TestClientEndToEnd（PASS）；docker compose -p binflow-uat --env-file .env.uat up -d --build（uat-l0171-7c8bfaba-alpine）；ARTI_AUTH=… replay.sh a / BF_AUTH=… replay.sh b（twine 7.0.0+pip 26.2.1，/tmp/l017venv 重建钉版）
Outputs:      双端包页（l017-wire/{a,b}/pypi/p2-pkg-page.body）三锚点逐字同属性：`data-requires-python="&gt;=3.8"`（whl=t2 表单源、tar.gz 1.0.0=t1 sdist、1.0.1=y1 无表单字段的 PKG-INFO 服务端派生源），sha256 fragment 双端同（23b359a2…/e23567a2…/3b5e7b6f…）；twine exit=0 ×6（双端各 3 腿）；pip 过滤腿双端同选 rptest-1.0.1、跳过 Requires-Python:>=3.99 的 1.0.0（Saved …/dl/rptest-1.0.1-py3-none-any.whl 双端）；D7 b2 臂双端同形：upload 200 + hello16 页字节不变（A 725B/B 885B 前后相等）+ /simple/nol016/ 404（A 74B/B 127B——消息体差已知记录级）；b1/b3 臂维持 L016 现值（D6 不动）
Compatibility: 契约对照：anchor-attrs 条目 D1 面=VERIFIED（L017-1 复验，binflow_state 记录；条目级仍 DIVERGENT 仅因 D2 rel 未裁）；badwheel-matrix 条目 D7 面=VERIFIED（同上，D6 未裁故条目级 DIVERGENT）——粒度说明：schema 一条目一 status，D1/D7 各与未裁面（D2/D6）同居一条目，故以 binflow_state 翻记而非整条翻绿。normalize：无需新规则——属性为既有 html.wire_format: compare 的断言目标（绝不归一），双端转义形逐字同构，无归一诉求。可差分 surface（供 differential-qa-engineer 批跑）：GET /simple/{name}/（锚点属性集=断言面）×{twine sdist/wheel 上传后,curl 无表单字段 sdist 后,badmeta wheel 后}；GET /simple/（根索引「仅坏 wheel」项目不列）；GET /simple/{name}/ Accept JSON（requires-python 字段）；pip download 版本选择腿。漂移点：D2 rel/D3 packages/ 模板/D6 索引键/D8 重复上传/D10 Location 形态——全部维持，未越票面
Security:     路径校验不变（上传段校验+NormalizeRelPath 双防线原样）；元数据解析三重上限（METADATA 头块 1MiB/sdist 成员 64+解压 8MiB/Scanner 行 64KiB）防炸弹与恶意头；st.Open 按 List 已见的 hex sha256 取 blob（无客户端可控路径）；HTML 转义复用 htmlEscape（属性值注入面有专测）；SSRF/remote 面未改
Performance:  索引渲染成本=项目文件数线性（wheel=zip 中央目录+单成员 ranged 读；sdist=头部流式解压至 PKG-INFO），有 ponytail 注记天花板：巨型项目页可感知时改上传时持久化 pypi.requires.python（NodePropStore 缝——当前未接，构造器签名在 cmd/httpapi 域不可动）；-race 全绿；无缓冲无新增锁
Risks:        ① 表单独有源不可达：curl 伪造「表单 requires_python=有 + 文件元数据=无」时 BinFlow 渲染无属性（A 渲染表单值）——未取证臂、真实客户端不可达，记录级残差（契约 binflow_state 已注） ② 坏 sdist（无 PKG-INFO）不拒入——证据 bounded 的刻意不对称，若后继取证 A 同样拒入 sdist 则一改一个判定函数 ③ .zip 形 sdist / .egg 未解析（无属性、照常索引）——无取证 ④ internal/replication/engine_protocols_test.go fixture 真化=area 外最小修正（越界申报：D7 改变索引行为使旧伪字节 fixture 必然红，仅改 fixture 字节、不改断言语义） ⑤ 客户端版本覆盖：twine 7.0.0/pip 26.2.1（venv 钉版）；twine <6.2 发 md5_digest 的旧版行为未在本轮复跑（上传面未改，风险低）
Blockers:     环境事故一次（非票内）：Docker Desktop 第四次僵死（L015/L016 同款）→ Artifactory 外部 Postgres 容器（postgres:16.8，:5432）随 freeze exit 255 → access 服务 Flyway 连库失败 → ref 全灭 ~45 分钟。处置：Docker Desktop 全量重启 + docker start postgres + artifactory 容器二次重启 → ping 200 恢复。B 侧 UAT 镜像构建被中断一次（build 完成、容器未换）→ compose up 二跑换入 uat-l0171-7c8bfaba-alpine。建议 conductor 知会 devops：ref 实例对外部 DB 容器的依赖应入 runbook（L015 §4 runbook 补一行：freeze 恢复后先查 postgres 容器再重启 artifactory）
Next:         ① 差分票：L017 replay 已留 l017-wire 双端 + tools/difftest/l017/replay.sh（可并入批跑；replay.sh/tools 目录属 differential-qa-engineer，本票 scratch 用后可收编或删） ② 六笔 UNKNOWN 仍待 conductor 裁（D2 rel/D3 模板/D5 PEP691/D6 索引键/D8 重复上传/D9 legacy JSON/D10 Location）——D9 若裁「做」，requires_python 字段直接复用本次 distmeta 派生源（同源已备） ③ 金样候选：l017-wire 双端包页（D1 对齐后形态）+ pip 过滤腿日志 ④ runbook 补丁（见 Blockers）
