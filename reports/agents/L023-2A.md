# L023-2A — build CRUD 五件 + 模型底座（对位 §11.3 勘误契约）

Ticket:       L023-2A（LOOP 023 批次 1 票 A：build CRUD 五件 + 模型底座）P0
Role:         dev-go-core（area 实例 = internal/build + internal/httpapi + internal/metadata 增量）
Area:         票面分配 internal/buildinfo —— **实际落位 internal/build（见 Changes 首条重大偏差说明）**
Input:        docs/reverse/build-info.md §1（端点表）/§3.1-3.2（字段集与表族）/§11.1（勘误 E1-E13）/§11.3（AC 锚：上传/查询族深规格）/§11.7（DELETE 语义）/§11.8（排序语义）；L023-1 报告（勘误与 26 活体探针）；既有代码 internal/build（T-507~T-510）+ internal/httpapi/build.go + metadata 024 表族
Changes:      按序：
              0) **重大偏差裁决（需 conductor 知悉）**：派单前提「BinFlow 零底座新域、新包 internal/buildinfo」与仓库现状不符——T-507~T-510（2026-09-07，commits ddf21194/ead1cfe2/62eeaf30/7b1d5777，均为 HEAD 祖先）已交付完整 build 域：internal/build 六文件 + 024 迁移 + httpapi 五 handler 挂载 + webhook/audit。新建 parallel 包会与既有路由/表族/事件总线冲突。本票改为**对既有域做 §11.3 勘误对位**（票面 AC 语义全部落地），未新建 internal/buildinfo。matrix D07 行的 state:absent 是 STALE 落账（实现从未回填），非代码缺失——建议 conductor 批次收口时翻绿。
              1) PUT 上传链（§11.3）：204 + X-Checksum-Sha256 响应头（E1，sha256 取自**重写后存储 manifest**——与参照同为「对存储文档取值」口径，参照会对 JSON 重序列化故其 sha 非请求原文，BinFlow 同构）；坐标 hidden 400（name/number 去前导空白〔Java trim ≤U+0020 集〕后以 '.' 开头→逐字 `Build name must not start with '.'`/`Build number must not start with '.'`，wireError 类型保证 wire 上无 sentinel 后缀）；artifactoryPrincipal 服务端覆写为认证用户（存储前重写 payload 并重序列化，rider 字段全保留）；部分 checksum 补全（artifact/dependency 行恰有 1-2 个 digest 时经 BlobStore 反查补齐三值，全有/全无/查无均跳过——§11.3 rewrite 2）；同号不同 started 并存与覆盖（先删后建的语义由「整体替换」承担：唯一性四元已隔离 run，观察面等价—— promotions 刻意保留，见 Risks ③）。
              2) 查询三跳（§11.3/§11.8）：顶级 uri 全部改绝对 URL + ?buildRepo=<解析仓>（contextURL 同源）；行 uri 维持相对；GET /build 空态（含全过滤）404 逐字 `No builds were found`（E2）；GET /{name} 404 逐字 `No build was found for build name: <name>`；详情 404 逐字 `No build was found for build name: <n>, build number: <n>[, build started: <ts>]`（started 子句仅在调用方传 ?started= 时拼接）；列表族 lastStarted/started 回显 UTC 归一（存储 canonical 已保证）、详情 started 回显 payload 原样时区（+0800 入 +0800 出——renderBuildInfo 不再覆盖 payload 的 started，仅缺省兜底）；durationMillis 缺省回显 0；statuses 回显补 timestampDate（epoch-ms）且 repository/ciUser 空时整体省略（§3.1 wire 形态）；?slim=true → modules []/properties null；名清单排序改按各名最新 run 日期（ORDER BY MAX(started)，§11.8 升序字面——中置信序向，见 Risks ⑤）；号单维持 started 严格倒序 + 同号多行并列；?buildRepo= 全查询族生效（缺省 artifactory-build-info，不再跨仓混排）。
              3) DELETE 单删（§11.7，新面）：路由 DELETE /binflow/api/build/{name}（build 前缀 case 内 method 分支，深路径维持 E-26）；?buildNumbers= CSV、?artifacts=0|1、?deleteAll=0|1；无号且非 deleteAll → 400 逐字 `Please provide at least one build number to delete`；部分命中 200 text/plain E6 逐字（have + Warning 段 + 段尾 `.\n`）；deleteAll 独立文案（无尾点）；404 两分支（名不存在 `Unable to find build '<name>'` / 号全不存在 `Unable to find the given build numbers`）；多 run 同号每次至多删一个（最新 started 先删，中置信源码读法已按规格实现并测试固化）；d 门先于存在查找（无 oracle）；artifacts=1 连带制品删（carrier 缺席时跳过——retention 同姿势）；每 run 一条 build.delete 审计 + webhook deleted 事件（T-510 同址法则）。
              4) append 404 对位（E4，本票被 writeBuildError 重构波及的最小修）：`The build <name>:<number> is not found` 逐字（openapi 的 `Build-Info not found` 作废）。append 的 E5 语义（列表拼接不合并）**未动**——票 B 范围。
              5) metadata 增量：BlobStore.GetByMd5（md5 反查，无索引走 ledger scan——SearchByChecksum md5 臂同姿势）；ListBuildNames 排序改 MAX(started)。
Files:        新增：internal/build/delete.go（DELETE 服务面：DeleteRuns/deleteRun）；internal/httpapi/build_put_semantics_test.go、build_query_semantics_test.go、build_delete_semantics_test.go（三件行为命名测试，票号在头注释）。
              修改：internal/build/upload.go（hidden 校验/payload 重写/checksum/digest 补全 seam）；internal/build/service.go（digests seam + ListBuildNamesRepo + ListBuildNumbers repo 参）；internal/metadata/api.go（GetByMd5 接口 + 注释两处）；internal/metadata/substores.go（GetByMd5 实现）；internal/metadata/substores_builds.go（名清单排序）；internal/httpapi/build.go（PUT 204+头/查询三跳 uri 与 404/详情回显/slim/DELETE handler）；internal/httpapi/router.go（DELETE 路由）；internal/httpapi/server.go（WithBlobs 装配一行）。
              测试对位：internal/build/upload_test.go（payload 断言改重写契约+checksum）；internal/build/build_acl_test.go（ListBuildNumbers 签名补参）；internal/metadata/builds_store_test.go（名清单新序）；internal/metadata/blob_sha1_test.go（+TestBlobGetByMd5）；internal/httpapi/build_rest_test.go、build_rest_curl_test.go、build_promote_test.go、build_webhook_test.go、search_build_test.go（200→204、绝对 uri、新 404 文案——存量断言对位，无删用例）。
              删除：无。
Tests:        新增 14 用例：build_put_semantics（hidden 4 臂逐字/principal 覆写/同号多 run 并存+倒序/覆盖整体替换+checksum 变化/部分 checksum 补全〔artifact sha1-only 与 dependency sha256-only 双向〕）；build_query_semantics（三跳绝对 uri+buildRepo 串/自定义 buildRepo 空态 404/UTC 归一 vs 原样时区分野/durationMillis 0/statuses wire 含 timestampDate 与 nullable 省略/slim/详情 404 started 子句两态）；build_delete_semantics（部分命中 E6 逐字整串/全命中段/deleteAll 文案+清场 404/400 无号/404 两分支/403/同号多 run 两次删/artifacts 旗标）；metadata TestBlobGetByMd5。存量 build 族全部用例随契约对位改期望后全绿。
Commands:     go build ./... && go vet ./... ；gofmt -l internal/build internal/metadata internal/httpapi（空）；make lint（golangci-lint 0 issues）；go test ./internal/build/ ./internal/metadata/ -count=1；go test ./internal/httpapi/ -count=1（237.7s 全量 + 最终代码复跑）；go test -race ./internal/build/ -count=1；go test -race ./internal/metadata/ -count=1；go test -race ./internal/httpapi/ -count=1 -run 'TestBuildPut|TestBuildQuery|TestBuildDelete|TestBuildREST|TestCurlCompatBuild'；冒烟：go build -o /tmp/l0232a-binflow-server ./cmd/binflow-server && BINFLOW_STORAGE__DATA_DIR=/tmp/l0232a-data BINFLOW_SERVER__LISTEN=127.0.0.1:18099 BINFLOW_ADMIN_PASSWORD=... serve（隔离实例，零 docker、零 172.16.58.130 接触，用后进程杀除+文件清理）。
Outputs:      四门：build/vet 0；gofmt 空；golangci-lint 0 issues；internal/build ok（-race 28.3s）；internal/metadata ok（-race）；internal/httpapi ok 237.733s（全量）+ 新用例全 PASS。冒烟（隔离实例 curl，逐断言）：S1 空态 `404 {"message":"No builds were found"}`；S2 PUT→`HTTP/1.1 204 No Content` + `X-Checksum-Sha256: aa70b4d6…e9cf`；S3/S4 同号新 started 与新号 PUT 均 204；S5 号单 `[('/1','04:00+0000'),('/2','03:00+0000'),('/1','02:00+0000')]`（严格倒序+同号两行）；S6 名单 uri=`http://127.0.0.1:18099/binflow/api/build?buildRepo=artifactory-build-info`、lastStarted=+0000（+0800 入）；S7 详情 started=`12:00:00.000+0800` 原样、principal=admin、durationMillis=0；S8 覆盖 204 且 properties env dev→staging；S9 `.jfrog`→400 逐字 `Build name must not start with '.'`；S10 DELETE buildNumbers=2,99→200 `The following builds have been deleted successfully: 'smoke-app#2'.\nWarning - the following builds could not be removed: '99'.\n`；S11 deleteAll→`All builds 'smoke-app' under 'artifactory-build-info' have been deleted successfully`（无尾点）+ 复验空态 404。
Compatibility: 契约对照（docs/compatibility/contracts/ 无 D07 build 契约——L023-1 Next① 已列转派项）：实现以 §11 勘误为 golden 逐条对齐——E1（204+头）/E2（空态 404）/E6（have+Warning+尾换行+deleteAll 无尾点）/E4（append 404 文案）四点已活体格式逐字复刻（冒烟 S2/S1/S10/S11 与 Go 测试双保险）；§11.3 四条服务端改写（principal/checksum 补全/hidden/同号多 run）落地；§11.8 排序（倒序高置信落定、名清单日期序中置信按 SQL 字面）。**登记漂移**：① X-Checksum-Sha256 取值口径 = BinFlow 存储 manifest（principal 重写+Go 序列化）的 sha256，参照是对其自身重排文档取值——「对存储文档取 checksum」语义同构，字节面必然不同（无漂移风险面：客户端以响应头回读对账不受影响）；② diff 参数维持拒绝（Builds Diff 面外+响应形态 UNKNOWN §9#5，现有 400 文案为 BinFlow 自有）；③ append 合并语义仍为 by-id merge（E5 证伪，票 B 翻案）；④ promote/retention 的 ErrBuildNotFound 仍渲染旧文案 `Build-Info not found`（writeBuildError 兜底臂，票 B/C 对位）；⑤ 403 文案族（§7 六条逐字）未对位——超出本票 AC 面，候票 B/C 或对齐小票。
Security:     hidden 坐标校验防 buildinfo 布局点目录绕权（RTDEV-94003 对位）；d 门先于存在查找（无 oracle）；列表零泄漏过滤维持（不可读名静默剔除→404 不可区分）；错误文案注入面：E6/404 文案内插 name/number——writeText 带 nosniff+text/plain（G705 姿势），name 经 ValidateBuildName（无控制字符）后进入；密码/凭据零涉及；smoke 口令仅 shell 变量、实例与临时文件用后即清。
Performance:  无新测量面。改写成本：PUT 增加一次 map 反序列化+重序列化（CI 元数据 KB 级，32MiB 上限维持）；checksum 补全仅对 1-2 digest 行各一次 blobs 索引查询（md5 臂为 ledger scan，与既有 SearchByChecksum 同姿势——内容去重表有界）；名清单 GROUP BY MAX(started) 走既有索引面。无影响写「无」之外如上。
Risks:        ① **派单/仓库状态错配**（Changes 0 条）——若 conductor 坚持独立 internal/buildinfo 包需重新裁决；matrix D07 11 行 state:absent 为陈旧落账待翻绿。② 「多 run 同号每次至多删一个」为源码控制流读法（§11.7 标注中置信）——已按规格实现+测试固化，契约化前建议活体双 run 删除复验。③ 覆盖臂 promotions 保留（upsert 语义）vs 参照「先删后建」字面（会连 promotion 历史一起删）——票 A 观察面（GET 三跳）无差异，promotions 存续是 §2.4 append-only 律的保守读法；UNPROBED，登记候契约探针。④ 名清单序向（升序字面）中置信——§11.8 已标注待多活体验证。⑤ statuses[].timestamp 维持 RFC3339（参照为 yyyy-MM-dd'T'HH:mm:ss.SSSZ 形态）——timestamp 格式属 promote 写入面，票 B 对位时统一；timestampDate 已按 epoch-ms 落地。⑥ payload 重序列化改变存储字节形（键序 Go 字典序）——echo 语义等价、X-Checksum-Sha256 随之稳定，但若有外部依赖原始字节的消费者会观察到变化（BinFlow 无此消费者）。
Blockers:     无（四门全绿、冒烟全过；Changes 0 条偏差已如实上报候裁）。
Next:         ① conductor 裁决 Changes 0（包落位 + matrix D07 翻绿时机）；② 票 B（append E5 拼接翻案 + promote §11.5 九步 + statuses timestamp 格式 + promote/append 403 文案族）；③ 票 C（批删 POST /build/delete body 6 字段 + retention §11.6；retention Execute 与 delete.go 的 deleteRun 有 ~20 行同构可届时收敛）；④ compatibility-engineer：D07 契约族以本票冒烟输出为 golden 候选（204+头/空态 404/E6 文案/UTC 归一分野）。
