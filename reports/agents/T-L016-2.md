# T-L016-2 — npm ghost-publish 守卫深水票 工作日志

Ticket:       T-L016-2 npm ghost-publish 守卫深水票 / P0（LOOP 016 轨道 L016-2）
Role:         dev-registry-adapter（领域实例：dev-package-npm）
Area:         internal/adapter/npm
Input:        reports/compatibility/L015-expansion-probe.md §1 N3/N4/N4'/N4''（ghost 403 vs 201+latest 劫持首证）+ l015-probe-wire/{a,b}/npm/（n4/g2 逐字 wire）；docs/reverse/maven-npm-pypi.md §2.2/§2.3 步骤 4（tarball 路径已存在 → 403）+ §2.7-5；docs/compatibility/contracts/npm.yaml（同域先例 schema）；docs/design/architecture.md §5.4.2（packument 单节点存储）
Changes:      ① 守卫谓词改 attachment 路径语义：publishWithTarball 的 tarball 路径由 tarballPath(name, version) 改为 attachmentTarballPath(name, _attachments 首键)——路径冲突→403（消息仍引声明版本，L015 N4 证据形）；ghost 版本因 403 先于 merge 而不入索引、latest 不劫持。② attachment 名入存储路径的安全门：按段判定（空段/点段拒，scoped 附件名合法含斜杠）→400。③ mergePublish 接收真实 tarball 路径（dist.tarball 指向字节实际落点）；versionDistTarball 对已存相对路径 verbatim 渲染（原 canonical 回退废止——错配发布下 canonical 路径无字节，渲染即断链）。④ 新探针 tools/difftest/l016/npm-ghost-guard.sh（三错配臂+G4 对照臂，真实 npm publish 1.0.0 前置）。⑤ 契约 npm/publish-ghost-guard 入列。
Files:        修改：internal/adapter/npm/publish.go、npmname.go、packument.go、escaping_test.go；docs/compatibility/contracts/npm.yaml；deploy/compose/.env.uat（BINFLOW_VER→uat-l0162-cd9cb770，gitignored 本地件）。新增：internal/adapter/npm/publish_ghost_guard_test.go、tools/difftest/l016/npm-ghost-guard.sh；reports/compatibility/l016-probe-wire/{a,b}/npm-ghost/（wire 证据）。删除：无。
Tests:        publish_ghost_guard_test.go 四测试：G1 错配冲突 403 逐字文案+索引仅 1.0.0+latest 不劫持+无 ghost 路径节点；G2 混合 versions append 201；G3 无冲突错配 201+字节落 attachment 路径+dist.tarball 渲染 attachment 路径+版本规范路径 404；attachment 穿越六形 400 零落盘。escaping_test.go T-261 face-4 翻面（-rev echo 后渲染 stored rel verbatim——L016-2 语义决策，原 pin 注记即为此决策预留）。存量 publish/serve/disttag/unpublish/reindex 全绿无翻面。
Commands:     go build ./... && go vet ./...（空）；gofmt -l internal/adapter（空）；~/go/bin/golangci-lint run ./internal/adapter/npm/...（0 issues）；go test ./internal/adapter/npm/ -count=1（ok 45.5s）；go test ./internal/adapter/... -count=1（14 包全 ok）；go test ./internal/httpapi/... -count=1（ok）；go test ./internal/adapter/npm/ -count=1 -race -run 'TestPublish|TestUnpublish|TestServe|TestEscap'（ok 52.1s）。探针：ARTI_AUTH='admin:JFrog@2026' bash tools/difftest/l016/npm-ghost-guard.sh a；BF_AUTH='admin:ZwLkTGWKw3w6PaSFPD20' bash tools/difftest/l016/npm-ghost-guard.sh b。UAT 重建：make release VER=uat-l0162-cd9cb770（budget 117.16/120MB 过）→ l0142 离线装配法组 binflow:uat-l0162-cd9cb770-alpine → docker compose -p binflow-uat --env-file deploy/compose/.env.uat -f deploy/compose/docker-compose.yml up -d。
Outputs:      探针 side=a（ref）：setup×4 真 npm publish 1.0.0 全成；g1 403 144B `Cannot modify pre-existing version '9.9.9', aborting upload for: 'l016-gh1-pkg'`；g2/g3/g4 均 201 0B；四包读回（+2s 与 +分钟双读一致，排除异步物化）均 versions=[1.0.0] latest=1.0.0；g3-tarball-777 200 29B（attachment 路径有字节）、-999 404。side=b（修复后 UAT）：g1 403 154B 同消息逐字（信封 indent 差在 normalize_indent 域）；g2/g3/g4 201 `{"success":true}`；g1 读回 [1.0.0] latest=1.0.0（劫持收口）；g3-tarball-777 200 29B、-999 404 与 A 同形。修复前 B（L015 证据）：g1 面=201+9.9.9 入索引+latest=9.9.9。
Compatibility: 契约 npm/publish-ghost-guard（VERIFIED）：G1 403 状态+逐字文案+side_effect_invariants（索引/latest/ghost 节点）；G2/G3 201+attachment 路径存储面（GET 200/404 双端同形）。守卫面可差分 surface=PUT /{name}×4 臂+GET packument+GET ×2 tarball 路径（tools/difftest/l016/npm-ghost-guard.sh 可复跑）。漂移点（非守卫面、不裁）：A 对 crafted 最小文档永不物化投影、B 恒物化——G4 对照臂证明与错配无关，归 L015 §5 既有 UNKNOWN（最小文档门槛字段，已升 conductor），本票不猜不补。T-261 face-4 翻面为语义决策非漂移。
Security:     attachment 名是入存储路径的客户端 body 数据：按段安全判定（空段/. /.. 拒）→400 零落盘，六穿越形单测钉死；scoped 附件名（@scope/name-1.0.0.tgz 合法含斜杠）不误伤。versionDistTarball 消费面审计：unpublish 删除路径恒取路由派生（layout 防御圈内），stored rel 仅用于匹配与渲染，无穿越放大。无 remote 代理改动；认证透传未动。
Performance:  守卫多一次 svc.List（tarballExists 既有路径，原步骤 4 已有，零新增 IO）；大制品仍内存态处理（publish 既有姿态），无影响。
Risks:        错配版本的真实 npm unpublish 端到端未测（npm 客户端按 SERVED dist.tarball 取文件名回 DELETE——G3 臂下应正确解析到 7.7.7 路径，仅单测级覆盖）；attachment 穿越 400 臂 A 侧未取证（契约注记不立对齐断言）；客户端版本覆盖=npm 10.9.8 锚（探针腿）+11.19.0（本机，未参与探针）；crafted 投影差保留至后段投影票。
Blockers:     无（Docker Desktop 整体僵死一次：daemon containers/json 502——处置=全量重启+手工 docker start postgres artifactory，binflow-ga/penpot/control-plane 自愈，沿 L015 §4 runbook；探针中途 000 系此事故，非协议判定）
Next:         ① crafted 最小文档门槛字段二分票（对 real-npm 文档逐字段裁，G4 素材已备）——建议 conductor 派后段投影票；② 金样候选：g1 403 逐字文案+attachment 路径存储面（l016-probe-wire/a/npm-ghost）；③ 差分批跑：npm-ghost-guard.sh 并入 l016 批跑入口随 L016 差分 job 复跑。
