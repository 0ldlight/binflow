# T-235 [P0] 双模式壳与路由重排 — 工作日志

| 项 | 值 |
|---|---|
| 票据 | T-235（B2 串行票——B3/B4 全部页面票的挂载点） |
| 角色 | dev-frontend |
| 日期 | 2026-08-23 |
| 依据 | docs/design/console-m8.md §1/§2（施工图）、docs/reverse/console-ui.md §1（行为）、ADR-0029（Accepted）、docs/prd/milestone-8.md FR-71 |
| area | web/src/components/AppShell.tsx + web/src/main.tsx + web/src/styles/base.css（壳样式）+ e2e/m8 + docs/design/console-ux.md §10 |

## 0. 击落-恢复说明

- **18:46 被 API 配额上限击落**（conductor 20:01 通报）。击落点：base.css 壳样式两处
  edit 已落盘（盘上事实），typecheck/lint/build 尚未跑。
- **20:01 恢复**：先 `git diff web/src/` 核对盘上状态（AppShell.tsx / main.tsx /
  base.css 三文件在途、内容完整），确认无半写；重跑 typecheck/lint/build 三绿后续写
  其余交付物（e2e 迁移与改锚、壳 spec、文档回写、全量验证）。未重写任何已落盘文件。

## 1. 交付物

### 1.1 双模式壳（AppShell.tsx 全量重构；旧导航形态删除）

```
AppShell（/login 独立布局之外的所有路由）
├ 侧栏 224px（三阶最暗层；nav-item 32px；active 左 2px accent 指示条）
│ ├ 品牌：BinFlow ◆
│ ├ [应用模式]（/dashboard /artifacts /search /profile）
│ │   └ 应用：仪表盘 /dashboard · 制品 /artifacts（2 条目）
│ ├ [管理模式]（/admin/**；admin / readonly_admin）
│ │   ├ 仓库：仓库 /admin/repositories（→ local Tab）        ┐
│ │   ├ 用户与权限：用户 · 组 · 权限 · Access Tokens          │
│ │   ├ 治理：审计日志 · 维护（GC） · 配额 · 复制 · 备份/恢复   │ 12 条目
│ │   ├ 监控：存储（T-238 占位页）                            │
│ │   └ 常规：系统信息                                        ┘
│ ├ 底栏模式切换：nav-mode-switch（button + aria-current；应用态显「管理」/
│ │ 管理态显「返回应用」；admin / readonly_admin 可见，普通用户不渲染）
│ └ 许可行：BinFlow v<version> · 单二进制制品仓库（nav-version 锚原样）
└ 主区
  ├ 顶栏 48px：面包屑 topbar-breadcrumb（管理模式：如「仓库 / <key> / 编辑」）
  │   / 页面标题（应用模式）· 搜索制品 topbar-search（⌘K//）· 帮助 topbar-help
  │   · 主题 topbar-theme-toggle · 用户菜单（session-toggle）
  │     └ [仅全量 admin] 快速建仓（Set Me Up / 新建 Local·Remote·Virtual 仓 →
  │        /admin/repositories/new?rclass=…）+ 新建用户/组/权限
  │        [全角色] 编辑档案 /profile · 切换主题 · 登出（确认对话框）
  └ 内容区 max-1440px（Outlet；页面组件原样挂载，pages/** 零改动）
```

- **readonly_admin**：「管理」入口可见 + 会话徽章「只读」（session-readonly-badge
  锚原位；M7 §7.3 语义保留）；用户菜单**不渲染** Quick 写入口（L4 预收敛）。
- **普通用户**：无模式切换项；`/admin/**` 直链保持**应用侧栏**（L1 不泄管理分组），
  页面自身 403 → L2 无权限卡（governance.spec 既有断言原样通过）。
- **旧形态退役**：NavGroup.disabled/ticket 占位分支、badge-soon 渲染、
  `.nav-item.disabled` / `.badge-soon` CSS、pageTitle 旧映射全部删除。
- base.css 增补：nav-mode-switch / app-nav-license / topbar-breadcrumb 系 /
  menu-label / menu-sep / topbar-session（菜单向下展开）。**迁移抓出的真缺陷**：
  session-toggle 原用 `--bf-sidebar-text`（深底侧栏色系），迁驻顶栏后亮色下
  1.02:1——axe 双主题回归抓出，`topbar-session` 作用域改 `--bf-text` 修复。

### 1.2 路由表 + 20 条旧路由 redirect（main.tsx）

新路由表按 console-m8 §1.3/§1.4（页面组件原样挂载，无页面内部改动）：

- 应用：`/`（index → /artifacts）、`/dashboard`、`/artifacts`（T-236 占位）、
  `/artifacts/:key/*`（现役树页——URL 即状态 + `?focus=` 深链参数形态就位，
  T-236 消费）、`/search`、`/profile`（现挂设置页组件，T-239 拆分）。
- 管理：`/admin` → `/admin/repositories` → `/admin/repositories/local`；
  `{local|remote|virtual}` 三 Tab 路由 + `new` + `:key` + `:key/edit`；
  `/admin/security/{users[/:name]|groups|permissions[/new|/:name]|tokens}`；
  `/admin/governance/{audit|gc|quotas|replication|backup}`；
  `/admin/monitoring/storage`（T-238 占位）；`/admin/general/settings`。

**旧路由 redirect 映射清单（20 条，客户端 replace；Q3 终裁 M9 移除）**：

| # | 旧路由 | 新路由 |
|---|---|---|
| 1 | `/` | `/artifacts` |
| 2 | `/repositories` | `/admin/repositories/local` |
| 3 | `/repositories/new` | `/admin/repositories/new` |
| 4 | `/repositories/:key` | `/admin/repositories/:key` |
| 5 | `/repositories/:key/settings` | `/admin/repositories/:key/edit` |
| 6 | `/repositories/:key/tree/*` | `/artifacts/:key/*`（含 #7 深层 splat 变体） |
| 7 | `/repositories/:key/tree/<深路径>` | `/artifacts/:key/<深路径>` |
| 8 | `/security/users` | `/admin/security/users` |
| 9 | `/security/users/:name` | `/admin/security/users/:name` |
| 10 | `/security/groups` | `/admin/security/groups` |
| 11 | `/security/permissions` | `/admin/security/permissions` |
| 12 | `/security/permissions/new` | `/admin/security/permissions/new` |
| 13 | `/security/permissions/:name` | `/admin/security/permissions/:name` |
| 14 | `/security/tokens` | `/admin/security/tokens` |
| 15 | `/audit` | `/admin/governance/audit` |
| 16 | `/governance/gc` | `/admin/governance/gc` |
| 17 | `/governance/quotas` | `/admin/governance/quotas` |
| 18 | `/governance/replication` | `/admin/governance/replication` |
| 19 | `/governance/backup` | `/admin/governance/backup` |
| 20 | `/settings` | `/admin/general/settings` |

实现形态：8 条 `<Route>` 承载 20 条映射——`security/*`、`governance/*`、
`repositories/*` 前缀平移各覆盖一族 + tree/settings 两条专门改写。改写作用于
**raw pathname**（前缀/后缀替换而非 params 重组）+ 查询串原样保留：percent-encode
形态不二次编解码（T-231 矩阵通道），树页 `?focus=` 深链锚定经 redirect 后仍选中
（shell.spec 断言 node-detail 到达）。

### 1.3 e2e

- **`web/e2e/m8/shell.spec.ts`（新）**：① admin 应用侧栏（2 条目 + 切换项）→
  键盘切管理模式（五分组 12 条目逐项可达 + 面包屑）→ 键盘切回（aria-current
  翻转）；② readonly_admin 管理模式可达 + 无 Quick 写入口 + Esc 关菜单；
  ③ 普通用户无切换项 + `/admin/**` 直链应用侧栏 + L2 卡；④ Quick 动作全键盘
  （含 `?rclass=` 参数形态）；⑤ 20 条旧路由映射表驱动 redirect（URL 落新路由 +
  页面锚到达 + 查询串保真腿）。
- **theme-smoke 迁入（T-232 遗留①收口）**：`src/styles/theme-smoke.spec.ts` →
  `e2e/m8/theme-smoke.spec.ts`（登录改用基座 `loginAs`；axe 循环走新路由）；
  `src/styles/playwright.styles.config.ts` 删除（现役 config testDir 已覆盖）。
- **既有 spec 路径断言同票更新**（ADR-0029 决策 6 口径：testid/交互断言沿用、
  路径断言随路由表改）：auth-shell（登录落点 /artifacts + dashboard goto +
  tokens 占位改路由断言）、artifacts / repositories / security / governance /
  t104-supplements / t131-g30e（URL 正则更新）、rbac V13（安全→用户与权限分组
  名 + 先切管理模式）、login LDAP（仪表盘改 SPA 内导航——route mock 会话经不起
  整页刷新）。**锚断言零改动**（242 锚保全，见 §1.4）。

### 1.4 文档回写（docs/design/console-ux.md v1.4）

- §0 修订记录 v1.4 行；§10.4 Tokens 行注记更新（`nav-item.disabled` 计数断言
  退役 → 真实路由 + placeholder-page）。
- **§10.5 新增**：M8 新路由 ↔ 既有锚保全映射表 + T-235 壳新锚 10 枚入册
  （nav-mode-switch / topbar-breadcrumb / quick-set-me-up /
  quick-new-repo-{local|remote|virtual} / quick-new-{user,group,perm} /
  menu-edit-profile）。锚量实测：`grep -rn "data-testid" web/src/` =
  **293 落点 / 29 文件**（HEAD 基线 283 + 壳净增 10：AppShell 10→20；删 0 改 0）。
  v1.2 的 242 为动态族计一口径的历史快照（ADR-0029 转正勘误脚注同源）。
- e2e/m8/README spec 清单增补（theme-smoke 迁入 + shell.spec）。

## 2. 自测证据（全部实跑）

```sh
cd web
npm run typecheck          # tsc --noEmit —— 零输出（绿）
npm run lint               # eslint . —— 零输出（绿）
npm run build              # assert-tokens OK + vite build + relink-assets verified
npm run assert:tokens      # OK — 3 个组件 css 零硬编码色值
# 体积门（make console 输出）：
#   console SPA payload (gzip, js+css): 160249 bytes（≈156.5KB ≤ 350KB 维持；
#   T-234 基线 155KB，壳增长 +1.5KB）

# 真栈 e2e（二进制新鲜度纪律：make console && make build 后 20:05 重建）
./bin/binflow-server serve -c /tmp/t235/binflow.yaml   # scratch 数据目录
cd web
npx playwright test e2e/m8/shell.spec.ts --workers=1   # 5 passed（15.3s）
npx playwright test e2e/m8 --workers=1                 # 18 passed（33.5s）
npx playwright test e2e/t146-docs.spec.ts --workers=1  # 8 passed（docsqa 口令实例）
BASE=… ADMIN_PW=docsqa-test-pw-146 npx playwright test --workers=1
                                                       # 103 passed / 3 skipped / 0 failed（3.8m，exit 0）
```

- 全量统一环境腿：`BASE=http://127.0.0.1:8080 ADMIN_PW=docsqa-test-pw-146`
  （t146 缺省口令 + t104-perf 的 base 探测都吃 env——一次跑全绿）；
  **--workers=1**（T-232 实证的 gc graceHours=0 并行竞态兜底口径）。
- 3 skipped = 条件跳过（clipboard 授权门 + 环境门腿），非失败；0 failed。
- 首轮全量 4 处失败逐一定位：① axe 对比度（真缺陷，§1.1 已修）② rbac V13
  分组改名（断言更新）③ login LDAP 落点（断言更新）④ t146/t104-perf 环境前置
  （env 腿）；复跑全绿。首轮另有 governance audit 一例顺序性闪烁，单跑与终轮
  全量均绿（gc 竞态家族，--workers=1 兜底口径内）。
- **AC5 服务端零改动审计**：`git status --porcelain internal/ cmd/` 为空、
  `git diff --stat internal/ cmd/` 为空（internal/console/dist 为 gitignored
  构建产物，非源码面）。

## 3. 与票面/规格的偏差登记（spec 优于票面括注）

1. **「Proxies 占位」未实现**：console-m8 §1.2 明列 Proxies 不建，§1.3 全图五分组
   = 仓库/用户与权限/治理/监控/常规（无 Proxies）；ADR-0029 决策 5 禁影子入口
   （含禁用态占位）。按施工图执行。
2. **顶栏「范围下拉占位」未实现**：console-m8 §2.1 顶栏元素清单无范围下拉
   （BinFlow 单实例无范围面；§1.2 对搜索类型下拉的「单值不渲染」同一裁剪口径）。
3. 搜索漏斗图标未上顶栏：§2.1 标注 P2 polish，归属 /search 页过滤面板（T-239 域）。
4. Set Me Up 菜单项暂指 `/artifacts`（向导对话框归 T-242，树页头按钮归 T-236）；
   新建用户/组 Quick 项暂指列表页（现役页内联创建表单；/new 路由形态归 T-237）。
5. `/profile` 现挂设置页组件（含改密块；页面不动纪律），T-239 拆分。

## 4. 契约漂移

无。零 API 改动（AC5 diff 为空）；全部交互为现役端点的前端重排。

## 5. 遗留

- 旧路由兼容窗口的移除时点 = M9（Q3 终裁；redirect 表集中在 main.tsx 一处，
  移除时删 8 条路由 + shell.spec 映射腿即可）。
- `/artifacts` 根与 `/admin/monitoring/storage` 为占位页（T-236 / T-238 地盘）。
- 页内旧路径链接（pages/** 内 `to="/repositories/…"` 等）经 redirect 兜底全通，
  待 B3/B4 域票顺手改写为新路径（非本票 area，不越界）。
- governance audit 一例顺序性闪烁（gc 竞态家族）已按 --workers=1 口径覆盖，
  根治归 T-232 登记的后续票。

## 6. 变更清单

```
web/src/main.tsx                          路由表重排 + LegacyRedirect（8 路由/20 映射）
web/src/components/AppShell.tsx           双模式壳重构（旧导航形态删除）
web/src/styles/base.css                   壳样式（mode-switch/license/breadcrumb/menu；删 disabled 族）
web/src/styles/theme-smoke.spec.ts        删（迁 e2e/m8）
web/src/styles/playwright.styles.config.ts 删（随迁）
web/e2e/m8/shell.spec.ts                  新（三角色×双模式 + 20 redirect + 键盘）
web/e2e/m8/theme-smoke.spec.ts            新（自 src/styles 迁入）
web/e2e/m8/README.md                      spec 清单增补
web/e2e/{auth-shell,artifacts,repositories,security,governance,t104-supplements,t131-g30e,login,rbac}.spec.ts
                                          路径断言随路由表更新（锚断言零改动）
docs/design/console-ux.md                 v1.4：修订行 + §10.4 注记 + §10.5 新增
reports/agents/T-235.md                   本日志
```
