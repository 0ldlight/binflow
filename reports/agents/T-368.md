# T-368 工作日志 — FR-118 旋钮聚合一票（folderDownloadConfig 六字段 + trashcan.retention_days）

- 角色：dev-go-core ｜ 状态：**done（area 内全绿；cmd/main.go 装配两处按 M11/M13 纪律移交 conductor，精确 diff 见 §5——不接线则两旋钮为死键，T-366 教训）**
- area：internal/config + internal/repo（旋钮消费点，本票零产品改动——缝已备）+ internal/httpapi（REST 回显臂）。未碰 web/、conan、storage 测试面、deb/race 面（T-361 并行在改，工作树中其未提交 diff 与本票零交集）。
- 零 git 操作（并行票据共享工作树）。

## 1. 做了什么

### 1.1 旋钮一 folder_download（FR-118.1，六字段）

- **internal/config**：新 section `folder_download`（raw 段 / build 臂 / defaults / apply-env 臂 / Validate 五处齐——T-366 死键教训的三处全齐再加校验）。默认 = repo-operations.md §2.1 规格列逐字（enabled=false / enabled_for_anonymous=false / 1024MB / 5000 / 10 / empty=false）。
- **消费点**：`repo.ConfigureFolderDownload`（M12 T-343 已备）不动；cmd 装配映射六字段（§5 diff，字段名编译器对齐）。
- **REST 回显臂**（internal/httpapi）：新端点 **`GET /binflow/api/v1/system/settings`**（system:read 门，read-only，其余动词落 E-26 404）——回显两旋钮族的解析值。**KNOB-SCOPED 裁量**：不是 config dump，永不携带 secret/DSN/路径（addons/license 面的「诚实最小只读」为设计天花板）。web 表单面后置（票面 AC1 口径）。
- **L14 断言开关化**：M12 关态断言（403 `Download Folder functionality is disabled.` 逐字）**原样维持**（t343 测试未动仍绿 + 本票新增 config-plane 默认臂再钉一遍）；开态新断言五臂：开→200+zip 逐字节对账 / echo 与执行为同源一致 / 匿名关→401 逐字（step-1 先于一切）/ 匿名开+security.anonymous_access=true→匿名 200（step-5 仍是裁判——子开关开不了实例关的仓）/ max_files=1→step-7 400 逐字 / empty-dirs 开→空目录条目入包。

### 1.2 旋钮二 trashcan.retention_days（FR-118.2）

- **internal/config**：新 section `trashcan`（仅 `retention_days` 一键；**`trashcan.enabled` 被 strict schema 拒绝**——capture 开关不是配置面，走 trashcan license 槽，票面口径）。默认 **14 = M12 as-built**（默认不变红线）；YAML 0 = 「默认」哨兵（引擎 <=0 回退 14，GCHoldTTL 同款语义）；负数拒启。
- **cron 消费**：`repo.TrashEngine` 的 `TrashEngineOptions.RetentionDays`（M12 T-345 已备，`e.days` 每遍 cutoff 消费）——产品代码零改动，cmd 装配把 cfg 值递进去（§5 diff）。
- **AC2 时钟夹具**（internal/repo 新测）：`retention_days=1` → 23h 条目留 / 25h 条目清 + `trash.retention` 审计行（actor=system-trash）；零哨兵臂（0→14d 窗口，2d 条目留）。13/15 天默认臂 = M12 `TestTrashRetentionWindow` 原样重跑绿。

## 2. 六字段与 Artifactory 锚对照（K52 票内定案留痕）

规格锚：docs/reverse/repo-operations.md **§2.1**（config 描述符+模板双证，高置信）＋ config-formats.md §2 folderDownloadConfig 行 ＋ inv-2-surface.md §3 配置族行 ＋ inv-3-protocols.md §141。

| Artifactory（§2.1 字段名） | 默认 | BinFlow config.yaml 键（本票定案） | env 形态 |
|---|---|---|---|
| `enabled` | false | `folder_download.enabled` | `BINFLOW_FOLDER_DOWNLOAD__ENABLED` |
| `enabledForAnonymous` | false | `folder_download.enabled_for_anonymous` | `BINFLOW_FOLDER_DOWNLOAD__ENABLED_FOR_ANONYMOUS` |
| `maxDownloadSizeMb` | 1024 | `folder_download.max_download_size_mb` | `BINFLOW_FOLDER_DOWNLOAD__MAX_DOWNLOAD_SIZE_MB` |
| `maxFiles` | 5000 | `folder_download.max_files` | `BINFLOW_FOLDER_DOWNLOAD__MAX_FILES` |
| `maxConcurrentRequests` | 10 | `folder_download.max_concurrent_requests` | `BINFLOW_FOLDER_DOWNLOAD__MAX_CONCURRENT_REQUESTS` |
| `enabledEmptyDirectories` | false | `folder_download.enabled_empty_directories` | `BINFLOW_FOLDER_DOWNLOAD__ENABLED_EMPTY_DIRECTORIES` |
| （trashcanConfig.）`retentionPeriodDays` | 14 | `trashcan.retention_days` | `BINFLOW_TRASHCAN__RETENTION_DAYS` |

**K52 键名族裁定**：字段集照 §2.1 逐字；YAML 拼写取 BinFlow config.yaml 全库 snake_case 惯例（config-formats.md trashcan 行 `retentionPeriodDays→retention_days` 为在档先例）。Artifactory camelCase 拼写（`maxDownloadSizeMb` 等）被 strict schema 拒绝并有表测钉死（死键姿态：复制来的键快速失败而非静默不生效）。三限额 0=unlimited（消费端零值语义）；env 整数键一律正整数（0 只能 YAML 拼写——`auth.hash_concurrency` 既有规则）；负值一律拒启。热更新（§2.1 reload 即时生效含并发 resize）维持 M12 已登记分歧：重启/预服务生效（`ConfigureFolderDownload` seam 注释在案）。

## 3. 变更清单

| 文件 | 性质 | 一句话 |
|---|---|---|
| internal/config/api.go | 改 | Config 增 `FolderDownload`/`Trashcan` 两 section + 两类型（含 K52/默认红线注释） |
| internal/config/config.go | 改 | 默认常量 1024/5000/10/14 + env 键族 splitEnvKey 臂（3 bool + 4 int） |
| internal/config/load.go | 改 | raw 两段 + build 臂 + defaults() + setEnvValue 臂（T-366 三处齐） |
| internal/config/validate.go | 改 | 三限额与 retention_days 非负域校验（拒启） |
| internal/config/t368_knobs_test.go | 新 | 表测双形态：默认红线/YAML/env/env 胜出/strict schema（camelCase 拒绝）/负值拒启/env 非正数拒绝 |
| internal/httpapi/system_settings.go | 新 | `GET /api/v1/system/settings` 回显臂（knob-scoped 裁量注释） |
| internal/httpapi/router.go | 改 | 路由注册（system:read；其余动词 E-26 404） |
| internal/httpapi/t215_guard_test.go | 改 | manage 门清点 56→57（guard 要求的表内增补，注释留痕） |
| internal/httpapi/t368_knobs_test.go | 新 | echo 三族（默认/自定义/角色门+只读面）+ L14 开关化五臂 + **默认等同钉**（mapped config 默认 == repo.DefaultFolderDownloadConfig / 14） |
| internal/repo/t368_retention_knob_test.go | 新 | retention_days=1 时钟夹具（23h 留/25h 清+审计）+ 0 哨兵臂 |
| reports/agents/T-368.md | 新 | 本日志 |

（internal/repo 产品代码零改动：两消费缝 M12 已备，本票只补消费侧证明。）

## 4. 自测（实际执行）

| 命令 | 结果 |
|---|---|
| `go build ./...` + `go vet ./...` | OK，零告警 |
| `golangci-lint run ./internal/config/... ./internal/httpapi/... ./internal/repo/... ./cmd/...` | **0 issues**（中途抓过一枚 SA9004 常量组类型注记，已修） |
| `gofmt -l internal/config internal/httpapi internal/repo` | 空 |
| `go test ./internal/config/ -count=1` | **ok**（含新 t368 表测 7 函数全绿） |
| `go test ./internal/repo/ -count=1` | **ok ×3 连跑**（首轮见一次红，属 T-361 在改的满载预算臂 flake 族——本票对 internal/repo 仅新增测试文件、零产品 diff，且 trash/archive/操作族定向 `-run 'TestTrash|TestArchive|TestT368|TestBigTree' -count=2` 双跑全绿） |
| `go test ./internal/httpapi/ -count=1` | **ok**（首轮红 = t215 门清点 57≠56，guard 如设计拦截——表内增补后全绿） |
| `go test ./cmd/... -count=1` | ok（bf / bf-migrate / binflow-server） |
| M12 基线维持证据 | `TestT343FolderDownloadREST`（关态 403 逐字原断言）+ `TestTrashRetentionWindow`（13/15 天臂）+ `TestTrashCapture*` 全 PASS |

## 5. conductor 接线精确 diff（未落盘，按 M11/M13 纪律移交；**不接则两旋钮死键**）

`cmd/binflow-server/main.go`（openStack 内，trash 织入段 ~L1203）：

```go
	// The folder-download knob (M13 T-368, FR-118.1): the resolved
	// folder_download section drives the six-field configuration (the
	// default column IS the M12 as-built; restart-effective).
	repo.ConfigureFolderDownload(svc, repo.FolderDownloadConfig{
		Enabled:                 cfg.FolderDownload.Enabled,
		EnabledForAnonymous:     cfg.FolderDownload.EnabledForAnonymous,
		MaxDownloadSizeMb:       cfg.FolderDownload.MaxDownloadSizeMb,
		MaxFiles:                cfg.FolderDownload.MaxFiles,
		MaxConcurrentRequests:   cfg.FolderDownload.MaxConcurrentRequests,
		EnabledEmptyDirectories: cfg.FolderDownload.EnabledEmptyDirectories,
	})

	// The trash can (M12 T-345 / M13 T-368 FR-118.2): capture stays
	// spec-default enabled; the retention window consumes
	// trashcan.retention_days (default 14 = the M12 as-built).
	repo.ConfigureTrash(svc, repo.TrashConfig{Enabled: true, RetentionDays: cfg.Trashcan.RetentionDays})
	repo.AttachTrashGate(svc, trashcanGate{reg: addonsReg, ev: licenseMgr})
	trashEng, err := repo.NewTrashEngine(repo.TrashEngineOptions{
		Store:         md,
		Audit:         auditLog,
		Gate:          trashcanGate{reg: addonsReg, ev: licenseMgr},
		RetentionDays: cfg.Trashcan.RetentionDays, // was repo.TrashDefaultRetentionDays
	})
```

（替换现行 `repo.ConfigureTrash(svc, repo.DefaultTrashConfig())` 与 `RetentionDays: repo.TrashDefaultRetentionDays` 两行；前置注释同步改。映射字段面与 httpapi 测试 `t368FolderFromConfig` 逐字段一致——该测试即接线的钉子。）

## 6. 规格依据

- repo-operations.md **§2.1**（六字段+默认，config 描述符+模板双证，高置信）——字段面与默认值唯一基准；§2.2 执行序（step-1 401 / step-6 403 / step-7 400 逐字文案）维持 M12 实现不动。
- config-formats.md §2（`trashcanConfig` 行：BinFlow 映射 `trashcan: {enabled: true, retention_days: 14}`——snake_case 先例；folderDownloadConfig 行 BinFlow 映射原为「M4 —」，本票即该映射的落地）；§4 `send.overwrites.to.trashcan` 不在本票面。
- storage-layout.md §5 / inv-1-core.md §B（保留期 14 天高置信）；inv-2-surface.md §3（trashcanConfig enabled/14d/allowPermDeletes——enabled 不旋钮化理由：票面+license 槽口径）。
- architecture.md §11.45（两缝已备/字段+「cmd 装配一行」处方——本票按方抓药）；PRD milestone-13.md FR-118/AC1-AC3/K52；T-356 L14/L17 如实登记为翻转基线。
- 低置信度处理：§2.1 热更新条款维持 M12 已登记分歧（重启生效，seam 注释）；`jfrog-support-bundle` 仓豁免等 §2.2 次级条款不在本票面，未擅自定型。

## 7. 遗留 / 上游确认

1. **conductor 接线**（§5 diff，两处）：不接线则 REST 回显有值但服务端恒关/恒 14——T-366 死键形态。建议随本票 qa 前落。
2. **architecture.md §7.1 路由表 + §15 端点行**（新 `GET /api/v1/system/settings`）与 **§11.45 条目翻转**（旋钮已落）——architect 台账（T-324 先例：增补行归 architect）。
3. **Helm chart**：`charts/binflow/values.schema.json`/`configmap.yaml` 的旋钮键族（replication 先例在 values.schema.json:180）——release-engineer 面。
4. **文档翻转**（trash-can.md「恒 enabled/14d」已知边界表 / api-reference「配置旋钮未落」自注 / artifact-operations.md / 新端点入册）——T-375 tech-writer（dep 本票）。
5. naming 留痕：端点取 `/api/v1/system/settings`（对齐 /api/v1/system/{gc,cleanup,license} 族）；若 T-375 文档面希望别名（如 system/config）需回本票加路由——当前无此需求。
