# T-240 工作日志 — 仓库管理域重排：三 Tab 列表 + 包类型网格向导 + 分组表单 + 详情自有页 + 删仓强确认（FR-73/UI-08/09）

| 项 | 值 |
|---|---|
| 票据 | T-240 [P0]（BOARD B4 第一波，原与 T-241 并行——后改串行，见 §7） |
| 角色 | dev-frontend |
| 日期 | 2026-08-24（开工 2026-08-23 晚；22:48 API 配额击落，01:02 后恢复续作） |
| area | `web/src/pages/repositories/{RepositoriesPage,RepositoryFormPage,RepoDetailPage}.tsx + RepoDeleteConfirm.tsx + repositories.css`（不含 tree/——T-236 地盘未动）+ e2e 迁移/新增 + console-ux §10 回写 |
| 状态 | done（四绿 + 全量 --workers=1 T-240 范围 124/0；t146 与 T-241 WIP 例外归类见 §3） |

## 0. 击落-恢复说明（conductor 备案）

22:48 配额击落时五文件已落盘（列表/表单/详情三页 + 共享删仓模块 + 域 css），详情页
Write 是最后一个成功操作。01:02 恢复后核对盘上状态与上下文记忆一致，零重写、零分叉；
后续完成：typefix（getRepositories 归属 lib/api、RepoDeleteTarget 适配）、既有 spec
迁移、新 m8 spec、锚清单回写、全量自测。恢复期间工作树另含 **T-241 被串行前的在途
改动**（security 两页 + security.css + security.spec/permissions.spec）——非本票产物，
本票未触碰一字（见 §7）。

## 1. 交付物

### 1.1 三 Tab 列表（RepositoriesPage 重写，console-m8 §6.6）

- Tab = 子路由（`/admin/repositories/{local|remote|virtual}`，路由表 T-235 已备，本票
  消费）：`repos-tab-{local|remote|virtual}`（Link + aria-current）+ 页头「N 个仓库」
  计数（repos-count 锚沿承）+ 右上「＋ 添加仓库」（仅全量 admin，L4）。
- 列集：Repository Key（mono 链接 + 拷贝）│包类型│类型│上游/成员（remote URL 截断
  mono、virtual 成员 details 浮层沿 T-99 形态）│已用（usage 逐仓独立到达，virtual 恒 —，
  失败降级不伪造）│描述│行尾删除入口（admin）。列头排序 key/包类型（asc→desc→none，
  T-237 表格基准同款：点击挂 th 热区 + aria-sort）；底部计数行「显示 1 – N / 共 N 项」
  （repos-pager）。
- 四态：骨架 8 行 / Tab 空态（admin 建仓 CTA 带类型化提示；非 admin「仓库由管理员
  创建」）/ 过滤空态（清除过滤）/ 错误卡 + 重试；**普通 user 403 → L2 无权限卡**
  （GET /api/repositories = CapRepoRead，T-236 定案：admin 与 readonly_admin 全量）。
- readonly_admin：全量可见 + `repos-readonly-note` + 无创建/删除入口（rbac.spec V13
  断言原样绿）。

### 1.2 建仓向导 + 单页分区表单（RepositoryFormPage 重写，§4.4/§6.7）

- **进页弹包类型网格**（C7 五项：Generic/Docker/Maven/npm/PyPI，图标+说明；docker
  在 remote/virtual 下禁用——FR-15-AC7）：`pkg-grid` / `pkg-grid-item-*` /
  `pkg-grid-cancel`（取消 → 回对应 Tab，零写请求）。?rclass= 预选（Quick 建仓入口
  形态，shell.spec 断言的 URL 不变）。
- 三步向导收敛为**单页分区式**（§4.4 定案）：常规设置（rclass/包类型单选〔编辑态
  锁定〕+ key* 实时校验 + 描述）→ 来源（remote：URL*/凭据/私网上游+黄条）→ 成员
  （virtual：多选+↑↓调序+默认部署仓，联动清空 bug 沿修）→ Maven 策略（local+maven）
  → 治理（local：quota/patterns）→ 高级（remote TTL×4 + hardFail + 优先解析）。
  右栏实时摘要（form-summary 锚不变）；底部 取消/重置/创建|保存（必填未满足禁用，
  formValid 全表单门控——步骤门控的扁平化，零坏请求不变）。
- **门（CanManageRepo 实测语义，router.go/rbac.go）**：创建 = CapRepoWrite（仅全量
  admin；非 admin 直链 L2 预收敛省 doomed 请求）；编辑 GET/POST 走单仓管理面——
  readonly_admin 全字段禁用 + `repo-form-readonly-note`（**T-218 债收口**：文案从
  错位的「repo:write」改为「CanManageRepo write，服务端 403 兜底」）；普通 user 持
  该仓 manage（覆盖集内）即可编辑（GET 过即覆盖集内，403 → L2 带 manage 指引）。
  UI 不自行判定覆盖集（§7.10）。
- 契约保全：编辑全量替换预填/整体提交、remote password 永不回显（留空=清除已明示）、
  400/409/413 服务端原文行内（form-error）——T-95/T-99 语义零改动。

### 1.3 详情自有页三 Tab（RepoDetailPage 重写，§6.8）

- **概要**：头部 key+rclass/packageType badge+拷贝 + 动作区（浏览制品→/artifacts/<key>
  〔repo-goto-tree〕、编辑配置→/edit〔repo-edit-link，readonly 不渲染〕）+ 接入命令
  块（P3，repo-commands/repo-cmd-* 锚不变）+ 统计（usage 水位条 repo-usage-bar）+
  remote 上游卡 / virtual 成员卡（锚不变）+ 危险区（仅全量 admin，repo-danger-zone）。
- **配置**：治理卡（repo-governance-card 迁此 Tab，锚不变、操作流 +1 步 Tab 切换——
  既有 spec 已同步）+ **quota 行内编辑**（repo-quota-{input,save,cancel,error}：
  保存瞬间重取详情重组完整 body 只覆写 quotaBytes——全量替换保全，QuotasPage 同源
  逻辑本域自持副本）+ 高级字段卡 + 编辑器入口。
- **Replications**：OSS 同款降级位——`repo-repl-degraded` 卡 + `repo-repl-goto` 指向
  全局复制页（T-159/T-180 承载，本仓级不建第二入口）。
- 身份注记：readonly_admin `repo-detail-readonly-note`（quota 禁用/无危险区/无编辑
  入口）；m-holder `repo-manage-note`（GET 已过=覆盖集内：quota 与编辑器开放、删除
  仍 CapRepoWrite 不渲染）。

### 1.4 删仓强确认（RepoDeleteConfirm.tsx，§4.6/C9——列表行与详情危险区共用）

- 文案基线：「将永久删除 `<key>` 仓库及其全部制品。制品不可变，删除没有撤销。」+
  □ 同时删除内容（deleteContent，非空仓必须勾选）+ **输入 key 确认**（P5 强确认，
  有意增强）。
- 两段流沿 T-99：非空仓未勾内容 → 400 原因（含 holds N node(s)）带回对话框原样呈现
  + 预勾选重试；成功 toast（服务端原文）+ 回该仓型 Tab + 列表 reload（列表行入口）。
- 锚全部沿承：repo-delete-{content,confirm-key,reason} + confirm-dialog/accept。

### 1.5 e2e

- **新** `web/e2e/m8/repositories-admin.spec.ts`（5 腿）：①建仓向导全链（?rclass=
  预选/网格组合禁用/Enter 键盘选定/创建+API 对账/取消零写请求+网格 axe）②三 Tab
  子路由 + 逐类行 + 排序三态 + 计数/计数行 + 过滤空态清除 + 行链接 Enter + 页 axe
  ③行级删仓强确认（输错禁用/两段流/toast/行消失/API 404 对账）④readonly 腿（全量
  可见/写入口无/quota 禁用/无危险区/表单禁用+T-218 注记文案断言 CanManageRepo 且非
  repo:write/写重放 403/读面存活+详情 axe）⑤m-holder 腿（覆盖集内：详情可达+身份
  注记+quota 行内改写对账+编辑器提交；覆盖集外：详情/写重放 403 收敛 L2 带 manage
  指引；列表 403 L2；创建臂 403=FR-65 V08 边界）。
- **迁移**（锚不变、操作流随设计演进）：`e2e/repositories.spec.ts`（4 腿：网格拾取
  替代步骤 1、form-next×n 删除、门控断言改 form-submit、virtual 行移 virtual Tab、
  governance 卡 +1 次 Tab 点击）；`e2e/t104-supplements.spec.ts` W10/W10b 两腿同款
  迁移。
- 退役锚在全部 spec 中 grep 归零（form-prev/form-next/repos-filter-type/
  repos-filter-package 仅存于注释性说明）。

### 1.6 锚清单回写（console-ux §10.5，v1.6）

新增 24 枚（repos-tab-*/repos-sort-*/repos-delete-<key>/repos-pager/pkg-grid 族/
form-{cancel,reset}/repo-form-readonly-note/repo-tab-*/repo-quota-*/repo-detail-
readonly-note/repo-manage-note/repo-repl-*/repo-edit-link{,-config}/repo-goto-tree）；
退役 4 枚（repos-filter-{type,package}、form-prev/form-next）+ 两条变更注记
（governance 卡入配置 Tab +1 步；编辑态 form-key 输入框改锁定展示）。T-99 冻结锚
零改名。

## 2. 挂载缝说明（conductor 注意）

**零缝**：路由表（T-235 已备三条 Tab 路由 + new/:key/:key/edit）、AppShell、main.tsx
一字未动；tree/ 子目录未动（tree.css 仍由 ArtifactsBrowser 引用，T-236 地盘）。
commands.ts 未动（T-242 的 Set Me Up 对话框族依赖它做指令源）。

## 3. 自测证据

```sh
cd web
npm run typecheck        # tsc --noEmit — 零输出
npx eslint .             # 零输出（含全树——T-241 WIP 文件同过）
npm run build            # assert-tokens: OK（3 css 零硬编码色值）+ vite ✓
                         # 域 chunk gzip：RepositoriesPage 3.08K / RepositoryFormPage 6.93K
                         # / RepoDetailPage 4.70K
make console && make build  # SPA payload gzip = 183,492 bytes（预算 5MB，W37）——
                         # T-236 基线 170,229 后的增量含 T-237~T-239 全部新增页；
                         # 本票域内净增约 +6K gzip
```

e2e（真栈：`make build` 新鲜二进制，scratch 数据目录 /tmp，独立实例
127.0.0.1:18101，`BINFLOW_SERVER__LISTEN`/`BINFLOW_STORAGE__DATA_DIR` 定位；admin
口令走默认 password）：

```sh
BASE=…:18101 npx playwright test e2e/m8/repositories-admin.spec.ts   # 5 passed
BASE=…:18101 npx playwright test e2e/repositories.spec.ts            # 4 passed
BASE=…:18101 npx playwright test e2e/t104-supplements.spec.ts        # 4 passed / 2 skipped(dind)
BASE=…:18101 npx playwright test --workers=1 <全量 25 文件：除 T-241 WIP
             permissions.spec 与 t146-docs>                          # 124 passed / 3 skipped / 0 failed (5.3m)
# 全量不作排除的裸跑（27 文件）：134 passed / 3 skipped / 2 failed——
#   ① m8/permissions.spec.ts 腿 1：T-241 在途 spec 断其未完工页面的
#      color-contrast（其自身修复项，非本票回归，见 §7）；
#   ② t146-docs G19b-1：实例口径（ADMIN_PW 环境变量实为
#      BINFLOW_ADMIN_PASSWORD；docs-QA 专用口令需专用实例）——
#      BINFLOW_ADMIN_PASSWORD=docsqa-test-pw-146 新鲜实例复跑 8/8 passed
#      （T-236 已登记的同款姿态，非回归）
# 3 skipped = docker CLI/dind 不可用的既有降级腿（t104/t134），与 T-232 基线一致
# --workers=1 维持（T-232 登记的 gc graceHours=0 并行竞态兜底不变）
```

axe（spec 内联，serious/critical = 0）：repos-page / repo-detail-page / pkg-grid
（对话框开态）三面。

## 4. 契约漂移与裁定记录

- **端点零漂移**：全部交互走现役端点（GET/PUT/POST/DELETE /api/repositories*、
  GET /api/v1/storage/usage/{key}、POST /api/v1/permissions〔仅 e2e 授 m〕）。
- **沿承缺口**（T-99 登记，本票维持不伪造）：列表项无节点计数/更新时间 → §6.6 线框
  「制品/缓存」「更新时间」两列不呈现；remote assumed-offline 无状态端点 → ⚠ 标记
  不建。若 architect 要补列，需先扩 E-04 回显体。
- **线框有意分歧**（记录备查）：§6.6[2] 称 key 列「链接进编辑页」；本票链接进**详情
  自有页**（§6.8/§1.2 裁定 BinFlow 保留自有详情页，且 T-236 跨仓树右键「在仓库管理
  中打开」即指向详情页——直进编辑页会把它变孤页）。详情页头部给显眼「编辑配置」
  入口，操作流等价 +1 跳。
- **普通 user 列表 403**：按 router 真相呈现 L2（T-236 已上报的 §2.2 措辞漂移，
  architect 回写待办——本票不重复立项）。

## 5. T-218 仓库域债收口（本票 AC⑤）

- 病灶：仓库设置页 readonly 文案称「仓库配置修改是管理面写操作（repo:write）」——
  与实测门不符（编辑走 CanManageRepo(repo, write)，repo:write 是创建/删除臂）。
- 收口：编辑/详情两处 readonly 注记统一为「保存走单仓管理面写（CanManageRepo
  write），服务端 403 兜底」；创建态文案保留 repo:write（该臂实测即 CapRepoWrite，
  语义正确）。e2e 断言注记含 CanManageRepo 且不含 repo:write。

## 6. 过程记录（坑与修法）

1. **22:48 击落**：API 配额上限，五文件已落盘后中断；恢复后盘上状态与记忆核对一致，
   续作零分叉（§0）。
2. **axe color-contrast（serious）**：`.row-del` 用 opacity .65 弱化把 --bf-text-2
   压破 4.5:1（T-236 tree.css 同款坑）——去 opacity，密度靠字号；顺手把空表头
   `<th aria-label/>`（minor）改实体「操作」文案。
3. **SortTh 热区**：点击挂内部 button 时 th 整格点击落空（spec 点击 th 中心）——
   回到 T-237 形态：点击挂 th（冒泡同路径，键盘仍经 button）。
4. **m8 spec 种子次序**：m-holder 腿的授 m target 引用 m8-e2e-user——loginAs（幂等
   预备）必须先于 POST target。
5. **serve 实例配置**：`ADMIN_PW`/`BIND` 等直写环境变量无效——实为
   `BINFLOW_ADMIN_PASSWORD` / `BINFLOW_SERVER__LISTEN` / `BINFLOW_STORAGE__DATA_DIR`
   （首启误落 :8080+仓根 ./data，已杀进程并清目录，零残留）。
6. **t146 实例口径**：ADMIN_PW 默认值 docsqa-test-pw-146 与通用实例不符——专用口令
   专用实例 8/8（T-236 同款先例）。
7. **make 在 web/ 下无 target**：T-236 记过的坑复发一次（静默失败）——make 一律从
   仓库根执行。

## 7. 与 T-241 的串行交叠（conductor 备案）

T-241（权限 target 编辑器）原与本票并行，其在途改动发现构建隐患后被 conductor 改为
**串行排队**——但其盘上产物仍在工作树：`web/src/pages/security/{PermissionsPage,
PermissionEditorPage}.tsx` + `security.css` + `e2e/security.spec.ts`（W33/W33c 改写）+
新 `e2e/m8/permissions.spec.ts`。本票**未触碰上述任一文件**；证据面影响：
- 其 m8 spec 4 腿中 3 腿已绿，腿 1（two-step dialog 全链）栽在其自建对话框的
  color-contrast(serious)——§6 坑 2 同类，属 T-241 自修项，非本票回归。
- 本票全量绿的口径 = 全量 27 文件减 permissions.spec（T-241 WIP）与 t146（实例
  口径，另证 8/8）= 25 文件 124/0。
- 建议 conductor：T-241 复工时先修其对话框对比度，再全量复跑（其 spec 已就位，页面
  收尾即可全绿）。

## 8. 遗留与移交

- **pages.css 退役样式**：`.stepper`/`.form-layout` 旧分步样式族因表单单页化成为死
  CSS（约 60 行；pages.css 是共享层，本票 area 不含，未清）——后续共享层小票顺带。
- **quota 行内编辑与 QuotasPage 的 buildLocalQuotaBody 重复**：governance 页与
  repositories 页各持一份同源字段集重组逻辑（跨 area 不 import；抽 lib/repos.ts
  共享 helper 属共享层变更，单独小票）。
- **列表「制品/缓存」「更新时间」两列**：等 E-04 回显体扩字段（契约漂移节）。
- Set Me Up 入口（页头/详情）归 T-242——详情概要 Tab 动作区已留位。
- 树虚拟滚动、badge.warning 亮色对比度等 T-236/T-237 遗留不在本票范围。
