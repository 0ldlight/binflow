# T-268 — e2e 默认并发恢复：--workers=1 权宜解除（FR-80-AC3）

- role: devops-engineer ｜ area: Makefile + scripts/ + web/package.json + docs workers=1 清理面 + ci.yml（e2e job 登记）+ 顺带项（BASE 探针 support 助手）
- dep: T-255/T-256（GC 根治 + CI 压力步，均 done）｜ 并行：T-267（console-ux/web 锚属性，互不相交；验证期互为 co-tenant 负载，见 §6）
- 状态: done（待 review）

## 1. 解除面盘点（AC3 grep 铁证）

```
$ grep -rn 'workers=1' Makefile scripts/ web/package.json docs/
Makefile / scripts/ / web/package.json   → 0 命中（可执行层从无此约定——
  package.json "e2e": "playwright test" 本就无旗标）
docs/design/architecture.md:1827/1862/1870/1942-1944 → 竞态史/根治描述/已执行
  顺序注记（历史注记）
docs/prd/milestone-9.md:35/54/76/108/167/174/180/311/356/416 → 问题陈述与本 AC
  自身定义（点时性需求记录）
```

结论：**无生效约定残留**。PRD:356 的严格变体（`| grep -v '#'` 期空）会命中
PRD 自身文本——AC 作者未预期 PRD 也在 grep 面内；按 AC3 主句「仅剩历史注记
（无生效约定）」判读通过。Makefile/scripts/package.json 零命中、`npm run e2e`
与裸 `npx playwright test` 均并发执行 = 约定已死。

web/playwright.matrix.config.ts:14 的 `workers: 1` **不在清理面且非本权宜**：
T-104 跨浏览器观测覆盖层（WebKit/Firefox record-not-gate）出生即串行，与
T-232 GC 竞态无关，保留（登记）。

## 2. 恢复形态（config 层，playwright.config.ts）

- `workers: 4` 显式钉住 = AC 下限。为何不用 Playwright 默认 cpu/2：本套件是
  **CPU 型**（axe-core 在页内 evaluate 跑），不是 cpu/2 假设的 I/O 型——本机
  16 逻辑核默认 8 workers 时 52 面 a11y-sweep 被饿到 8.3m（连 480s 都不够，
  见 §4 诊断轮）；且 cpu/2 在 4-vCPU CI 机会给出 2 < AC 下限。钉 4 跨机确定，
  裸命令无旗标，三轮全绿（§5）。派发令明示「config 或 CLI 默认」——落 config。
- 全局 per-test timeout 30s → 180s：串行时代预算。axe 腿在并发下实测 33s+
  （T-263 登记的负载脆弱在并发下复现——派发令第 4 项裁决：上调并登记）。
- a11y-sweep 300s→600s、keyboard 90s→480s（多扫描腿自设预算，对齐量级）。

## 3. BASE 探针（T-266 登记项 ③，落 support 助手）

新文件 `web/e2e/support/base-probe.ts`，挂 `playwright.config.ts` 的
globalSetup：任何 worker 起跑前 GET `$BASE/binflow/api/system/version`
（E-03 匿名 200），断言 `product === "BinFlow"`；通过则打一行归属 banner
（含 version/revision，T-266 §3.1 的旧 binary 事故一眼可辨）。

自测三臂：

```
BASE=127.0.0.1:8099（无服务）  → "unreachable … start it first"  起跑即中止
BASE=127.0.0.1:8098（python http.server，非 BinFlow）→ "is not a BinFlow
  instance (HTTP 404, product=null) — Refusing to run"  exit 1 零测试执行
BASE=127.0.0.1:8090（本仓实例）→ "ownership verified" banner + 套件正常跑
```

验收期实弹价值：本轮 8080 端口恰被他人实例占用（PID 18474，未动）——T-266
事故同款场景下探针把「静默打错实例」变为起跑即红。

## 4. 诊断轮记录（并发恢复抖出的独占实例假设，逐一定性）

R1–R7（实例一，8 workers 诊断期）+ accA ×3 次未竟，每轮失败集合收敛后归零。
失败定性全部为 **spec 层独占实例假设**（非产品缺陷、非 GC 竞态复发）：

| 腿 | 形态 | 定性与修法 |
|---|---|---|
| m8/theme-smoke axe×2 | 30s 超时（33.9s） | T-263 登记负载脆弱并发复现 → 全局预算 180s |
| m8/keyboard axe | 90s→180s 两轮仍超（1.7m/3.1m） | 重表页 8 扫描 → 480s |
| m8/a11y-sweep | 300s→480s 仍超（5.1m/8.2m） | 52 面 × axe，随机 co-tenant 负载 → 600s |
| m8/governance-monitoring admin | ① axe 30s ② 存储行「———」③ **汇总对账 1,452/1,601 跨读偏斜** | ①全局预算 ②行断言 30s ③精确相等改单调对账（UI ≥ API 读值；并行写者跨读单调，wiring 错位仍红） |
| m8/governance-monitoring readonly | quota 行 not-found 5s | 异步聚合表行渲染 >5s → 30s |
| m8/auxiliary 仪表盘 | 审计行 not-found 2/2 复发 | 卡片窗口 = getRecentAudit(8)，并行事件流挤出目标行——等待无解 → 「补种新唯一文件 + 重读」×6 重试，断言对象始终是窗口内可见 upload 行，深链语义不弱化 |
| governance gc 腿 | dry 148 / apply 149 | 候选集在 dry→apply 间是活的：并行可增垃圾（>）也可为旧候选补引用被 hold 集合法豁免（<，正是 ADR-0031 语义）→ 精确相等改 ≥1（本腿孤儿确实回收）+ 审计自洽不变 |
| m8/repositories-admin | ① axe 30s ② toast not-found ③ 三 tab 行 not-found | ①全局预算 ②toast 5s TTL 瞬态撞慢重定向 → toast 先 URL 后（断言全保留）③行断言 30s |
| t146-docs G18-4 | inputValue "" 2/2 复发 | Docusaurus hydration 在 fill 后重置输入框 → refill 重试至值存活 |
| t146-docs G18-3 | 源码含 docusaurus.io 外链 | 路由未匹配过渡兜底（自定义 404 是 code-split chunk，争用下滞后）被一次性 content() 捕到 → 取样前等兜底外链清零 |

**GC 竞态零复发**：全部 10 轮日志 grep `blob not found` = 0；t134-g32（受害
spec，serial 文件）在三绿轮中与其他 3 worker（含 grace=0 gc apply 腿）同实例
并发 18/18 绿——T-232 竞态形态下零 500 零误删，反向验证 T-255/T-256 根治。

## 5. 验收（AC2/AC3）——三轮连绿

真栈 scratch（`BINFLOW_HOME=/tmp/bf-t268b`，:8090）+ 双 seed
（seed-m8 树 10291/10291 实落 75.0s；seed-m9 verification green），
裸 `npx playwright test`（config 并发，无 CLI 旗标）：

```
A: 176 passed / 0 failed / exit 0（6.3m，4 workers，181 tests）
B: 176 passed / 0 failed / exit 0（7.1m）
C: 176 passed / 0 failed / exit 0（8.0m）
```

passed 三轮一致（181 − 5 环境门 skip = 176：1GB 上传腿、dind×2、oidc armed、
console 段探针——既有口径）。NFR-P42 吞吐（≤4m 量级，归档不设门）：6.3~8.0m，
受验证期 co-tenant 负载影响，安静机按诊断轮外推 ~4m 量级。专项：t134-g32
定向 6/6 绿。探针 banner 每轮首行在案（/tmp/t268-acc{A,B,C}.log）。

## 6. 环境事故与披露

- 首个 fresh 实例起服失败未察觉（`pkill -f` 匹配命令行，env 不在内 → 旧实例
  未死、新实例 bind 失败退出、幂等 seed 打进旧实例）。发现路径：fresh home
  树「验证」仅 264ms。处置：按端口杀**自己的**旧实例重起，真 fresh（75s 实落）
  后才开验收轮。8080 他人实例（PID 18474）全程未动。
- 验证期 repo 级 typecheck/lint 两度红：`UserDetailPage/UsersPage itemTestid`
  与 `DashboardPage 'i'` ——均为并行票 T-267 在飞 src 改动（非本票面）；
  收尾时其已落定，门禁全绿（lint 0 / tsc 0）。T-267 验证负载同时是本票
  a11y-sweep 预算观测中的 co-tenant 变量（§4 已计入）。

## 7. 契约漂移与越界披露

- 零后端/产品代码改动（internal/ 与 web/src/ 未触）。
- **越界披露**：§4 表中 9 个 spec 文件的腿级加固在派发 area（Makefile/scripts/
  package.json/docs/ci.yml）之外——AC3「三轮连绿」的直接前置，每处均为
  等价/放宽等待形态（断言语义保留，方向与理由逐条登记于 §4 与代码注释），
  无一弱化产品不变量；GC 腿的精确相等→≥1 是对「活候选集」的语义修正而非
  竞态掩盖（审计自洽断言原样）。请 review 对该扩面追认或退回。
- ci.yml 注释刷新（顺序硬规则已兑现 → 常设双向耦合）+ 登记：**CI 无 e2e
  job**（本地/QA 姿态既有事实，本票未新增）；GC 压力步是 CI 侧的竞态代理门。

## 8. 遗留 / 移交

1. a11y-sweep 预算随实例数据增长（52 面 × 长寿命实例大表）——600s 是当前
   观测上界 +22%；若 QA 长寿命实例再超，考虑按路由拆分该腿（结构性，归
   QA 债票）。
2. `playwright.matrix.config.ts` workers:1（T-104 观测层）保留如故；若后续
   需要跨引擎并发观测再议。
3. BASE 探针只覆盖主 config（matrix 覆盖层无 globalSetup）——观测跑法偶发
   打错实例的概率与危害都低，登记即可。
4. docs-site build 产物（git status 中 docs-site/build/** 的既有未提交变更）
   非本票面，归 conductor/T-271 流程。

## 9. 变更清单

```
web/playwright.config.ts                 workers 钉 4（AC 下限）+ 全局 timeout 180s + globalSetup 探针 + T-268 口径注释
web/e2e/support/base-probe.ts            新增：BASE 归属探针（E-03 product 断言 + banner + 双臂拒跑文案）
web/e2e/m8/theme-smoke.spec.ts           axe 两腿预算注记（走全局 180s）
web/e2e/m8/keyboard.spec.ts              axe 腿 90s→480s
web/e2e/m8/a11y-sweep.spec.ts            300s→600s
web/e2e/m8/governance-monitoring.spec.ts 存储行/汇总对账（30s 窗口 + 单调对账 + 重试取样）+ readonly 行 30s
web/e2e/m8/auxiliary.spec.ts             审计行深链腿：补种重读 ×6 重试
web/e2e/m8/repositories-admin.spec.ts    toast/URL 断言次序对调 + 三 tab 行 30s
web/e2e/t134-g32.spec.ts                 tag 徽标 5s→15s
web/e2e/t146-docs.spec.ts                G18-4 refill 重试；G18-3 兜底外链清零后取样
.github/workflows/ci.yml                 压力步注释：顺序规则兑现 → 常设耦合 + 无 e2e job 登记
docs/design/architecture.md              §14.5-5 顺序项标记已执行（T-268/2026-08-25，现状与耦合口径）
reports/agents/T-268.md                  本日志
```
