# L027-4 工作日志 — L025-7 FE 双臂退役 + BEHAVIOR-CONTAMINATED 三腿复检（断点续作收编）

```
Ticket:       L027-4（LOOP 027 波 1 票④）：L025-7 FE 双臂退役（读位改 L026-3/L026-7 后端根治面）+ L026-2 三条 BEHAVIOR-CONTAMINATED 复检旗销旗
Role:         dev-frontend（前会话实现段 15:32–16:28 完成，16:28 后中断）；conductor 断点接管收编（本会话——验证+报告+提交）
Area:         web/src/lib/repos.ts + web/src/pages/repos/RepositoryFormPage.tsx + web/src/i18n/{locales/en/common.ts,manifests/zh/common.json} + web/e2e/{repositories,repo-policy-keys,m8/repositories-admin}.spec.ts
Input:        L026-3 appendUnmodeledBlobKeys（3472e5bb——离表存储键原样随每个渲染面回显）+ L026-7 读门放开（404c44ca——v1/v2 单仓+列表对任意认证开放，列表 admin 逐字节同）+ L025-8 blobEchoDenyKeys 五键（恒不随渲染面回显，列表面 blob=唯一注册读位）+ L026-2-ledger 三腿复检旗（repo-policy-keys :120/:201/:233——L026-3 行为变化可能污染渲染面，按警示不按现行行为重锚）；票指令=双臂退役+写路径保险承接。
Changes:      ① getRepoDetail 双臂退役：L025-7 的「v1 面 ∪ 列表面全量 blob 合并（403 容错降级）」改为 v1 详读面一跳直取（L026-3 后离表键已在面上）；唯一存留第二读位=remote 仓补读列表面、只提取 LIST_FACE_ONLY_KEYS 五键（L025-8 deny 键唯一读位——非全量合并、无容错、清单读失败整体响亮失败）。403 容错臂随 L026-7 开门退役。② 写路径保险承接：isNarrowRepoFace（详读面无 environments 座位=非 admin 窄投影判定——admin 全量面每面 withEnvironments 垫底恒含）+ buildLocalQuotaBody 窄面 throw + RepositoryFormPage gateReason 编辑态窄面 Save 恒禁用并说明（L025-7「读位不完整宁可响亮失败」的退役后防线）。③ i18n 新键一枚（窄投影拒绝说明，en/zh 双侧）。④ 三 spec 重锚：repo-policy-keys mock 体补 environments:[]（admin 全量面恒在座位——mock 须与真栈全量面同形，三腿复检旗据此销旗：L026-3 后离表策略键随详读面回显，mock 平铺策略键形=真栈面形，dev-3c0c3a87 活体已验）；m8 readonly_admin 腿改窄投影断言（governance 卡「（不限）」缺键呈现 + sessionApi 直证 quotaBytes undefined + 配额输入禁用）；repositories.spec L025-7 腿读位重锚（对账改 v1 详读面顶层平铺）+ 新增 deny 键读位回归腿（allowPrivateUpstream/socketTimeoutSecs 置位→编辑器预填真值→未触碰保存不翻转）。
Files:        修改 7 文件（见 Area）；新增本报告。web/dist（16:02，含本票改动的前会话 npm run build 产物——mtime 晚于最后 src 改动 15:40，gitignored 不入树）。
Tests:        新增/重锚腿：repositories.spec「L025-7 腿（read seat = v1 detail face）」重锚 + deny 键回归腿；m8 readonly_admin 窄面腿重锚；repo-policy-keys 三腿（BEHAVIOR-CONTAMINATED 旗）mock 重锚。四态：mock 腿零服务器依赖（page.route 全拦截）；真栈腿需隔离实例。
Commands:     验证环境=conductor 接管会话 VM（与实现段宿主不同——见 Risks③）：node scripts/assert-tokens.mjs；node scripts/assert-i18n.mjs；闭包型检查 node node_modules/typescript/bin/tsc -p <outputs>/tsconfig.check.json（include=两 src 改档 / 三 spec 改档+repos.ts，extends 项目 tsconfig）；npm i --no-save @rollup/rollup-linux-x64-gnu @esbuild/linux-x64（Linux 原生腿补装，mac 侧 node_modules 无损共存）；浏览器腿=npx playwright install chromium/headless-shell（落 ~/.cache/ms-playwright）+ 本地 mock 版本服务绕 base-probe + node @playwright/test/cli.js test e2e/repo-policy-keys.spec.ts。
Outputs:      assert-tokens OK（css 11+tsx 104，零硬编码——10.2s）；assert-i18n OK（3122 调用点/2348 键 en/zh 同构——新键双侧入账，41.4s）；闭包型 tsc 两轮全净（src 档 2m3s / spec 档 1m6s——唯一报错为 extends-异地配置伪影：ImportMeta.env/.glob 缺 vite/client types、@types/node 缺失，与项目内构建无关；改动文件自身及其完整 import 闭包零类型错）；vite build=前会话 16:02 dist 产物在案（npm run build 含双断言闸+vite+relink+wire-brand 全链——dist mtime 晚于全部 src 改动=改后树构建通过的证据）；运行时 e2e=本环境不可达（Risks③）。
Compatibility: 对照 L026-3/L026-7/L025-8 三裁定实现，契约面零新增（纯消费既有面）；v1 详读面键平铺形态（L025-6）与 deny 键零回显约束（L025-8）均维持；窄投影判定锚=environments 座位恒在性（L026-3 注记）。m-holder 场景=L026-7 开门后详读面可达（窄投影四键），专键读位=面键+列表面 blob 双源齐备——L025-7 报告 Next① 的 /api/v1 投影面提票诉求由 L026-7 后端已兑现，FE 双臂退役即其收尾。
Security:     无新增用户内容渲染面；password 仍只写不读；窄面 Save 禁用=防未触碰键翻缺省的数据完整性防线（非提权面）；LIST_FACE_ONLY_KEYS 提取不扩大读面（列表面本就对认证开放）。
Performance:  getRepoDetail 请求数：local/virtual 2→1（v1 一跳）；remote 维持 2 但第二读收窄为 blob 提取（无全量合并解析）；m-holder/普通 user 全量清单扇出消除（L025-7 报告 Performance 遗留清偿）。
Risks:        ① 运行时 e2e 欠账：三 spec 未在本环境跑过（chromium 在 conductor VM 沙盒内起立即 SIGSEGV——沙盒套沙盒限制；远程 dev 宿主 19:0x 起失联）；真栈腿依赖的 L026-3/L026-7 行为已在 main CI 绿——红面风险集中在断言漂移非行为破坏。② 全量 tsc/eslint 未整跑（VM 单调用时限 178s）；闭包型检查覆盖全部五个改档的完整 import 图，未覆盖=未改档的存量文件（无改动面）。③ 实现段与收编段异宿主：实现在前会话（Mac 直跑环境），收编在 conductor VM——dist/断言产物可复现，闭包型 tsc 为收编段新跑。④ repo-policy-keys mock 的 environments:[] 假=admin 全量面（若未来非 admin mock 需另做窄面形）。
Blockers:     无阻塞项；欠账面见 Risks①②——候 Mac 侧下一次全量跑清偿（run_e2e=false 门下建议手动三 spec：npx playwright test repositories.spec.ts repo-policy-keys.spec.ts m8/repositories-admin.spec.ts——隔离实例配方见 L025-7 报告 Commands）。
Next:         ① Mac 侧三 spec 复跑清偿（欠账销账后本票升 done 全额）；② conductor：远程宿主 172.16.58.130 失联事件入环境账（恢复后差分/probe 复核）；③ twMerge 字号修票（UI 余债）维持候池。
```

## 附：断点续作纪要（conductor 接管记录）

- 前会话（LOOP 027 波 1）在 16:28 后中断：web/ 七文件在途未提交、无 L027-4 报告、无 loop-state 收编事件。本会话 17:52 接管：diff 全审（双臂退役+窄面保险+三 spec 重锚的实现段完整且自洽）→ 环境探测（远程双实例先活后失联）→ 分片验证（asserts/闭包 tsc/dist 证据链）→ 本报告+提交。
- 环境事件：172.16.58.130 整机 18:0x 前可达（ref 401/dev healthz 200 实测）、19:0x 起 ping 100% loss——参照/dev 双实例同时失联，非 BinFlow 侧故障；恢复义务归 conductor 环境账（影响 nightly 观察 09-18 03:17 UTC 的地面复核腿）。
- 验证方法论沉淀：conductor VM 单调用 178s 时限+进程不跨调用存活——大闸门（全量 tsc/eslint/vite/e2e）改「闭包型 tsc+断言闸+dist mtime 证据」组合拳；chromium 需 headless-shell 变体（full chromium 不够）；base-probe 可用本地 mock 版本服务满足（product:"BinFlow" 即可）——但浏览器 SIGSEGV 仍封死运行时腿。
