# T-L012-2a — npm 契约首文件（compatibility-engineer）

```
Ticket:       LOOP 012 轨道 L012-2a —— npm 契约首文件（contracts/npm.yaml，compat-contract/1）
Role:         compatibility-engineer（npm 域实例）
Area:         docs/compatibility/（contracts/ fixtures/ + matrix.yaml + known-divergence.yaml）
Input:        reports/compatibility/L012-dist-tags-evidence.md（wire 序列 + 20 格矩阵 + l0121-wire/ 原始捕获）
              + conductor 六项裁定（D2-D7 全按参照对齐收；D5=400+中性文案不复刻参照 500；D6 bulk 面 405 化
              ——BOARD.md sprint tick 2026-09-13「6 UNKNOWN 裁定全对齐收」在案）
Changes:      ① 新建 contracts/npm.yaml（npm 域首文件，8 条目，沿 docker-remote.yaml 单文件多条目结构）：
              GET（%2f/%2F 等价 + D7 Cache-Control max-age=60 + 匿名姿态注记）/ PUT happy（201 ok 文案，
              IMPLEMENTED）/ PUT 不存在版本（D3 版本位）/ DELETE（200 空 + 再删 404 逐字，IMPLEMENTED）/
              幽灵 404（D4「Not found」）/ 坏 body（D5 对齐后形态：400 + path_pattern 反向门 + proposed_literal）/
              latest 读时重算（D2，SEQUENCE 条目）/ bulk 收窄（D6 三臂 405）。
              ② normalize.yaml 新增 npm 域（wire_format=normalize_indent——pretty/compact 归一、ok_space、
              信封措辞组 auth401、anonymous_arm=authed_only）。
              ③ known-divergence.yaml 新增 npm 域 6 条：5 BUG（D2/D3/D4/D6/D7 对齐收）+ 1 INTENTIONAL
              （D5 400-不复刻-500，authority=conductor_ruling）。
              ④ matrix.yaml：D12-R05 落 contract_ref + last_difftest + note「契约化在途」；头部 changelog
              增 L012-2a 段。行态维持 partial（冻结行不改判；6 面待实现+批跑，行 scope 另含版本 CRUD 未契约化）。
Files:        新建 docs/compatibility/contracts/npm.yaml、reports/agents/T-L012-2a.md；
              修改 docs/compatibility/fixtures/normalize.yaml、docs/compatibility/known-divergence.yaml、
              docs/compatibility/matrix.yaml
Tests:        无 probe 新增（本票证据消费方：L012-1 已含复跑门——pass2 定稿、pass1 family 退出码向量零差 +
              curl 矩阵 byte-identical；E0-5 判别探针在纯 d301cf31 容器复判）。本票为契约化票，无新活体实验
Commands:     YAML 门（四文件全过，输出见下）；行数门 grep -c row_id: = 200（冻结行集无增删）；
              引用完整性门：6/6 divergence_ref 锚 grep 命中（known-divergence.yaml#npm/… 逐条 HIT）；
              14/14 evidence 文件存在（l0121-wire/{a,b}/*.log + curl-matrix-{a,b}.txt + L012 报告）；
              契约面核对门（IMPLEMENTED 条目）：internal/adapter/npm/route.go:16-17（routeDistTags/routeDistTag
              路由种类）+ route.go:120-146 parseDistTagsRoute + api.go:229-232（serveDistTags/serveDistTag 分派）
              ——dist-tags 端点族路由 grep 实得
Outputs:      契约 8 份（npm/dist-tags-get、-put-tag、-put-nonexistent-version、-delete-tag、-ghost-404、
              -put-bad-body、-latest-recompute、-bulk-reject）；matrix 行更新 1（D12-R05，行态不变）；
              known-divergence 新增 6；normalize 新域 1（npm）
Compatibility: 状态机：首建 8 条目 = IMPLEMENTED 2（put-tag/delete-tag——现值已符，批跑后升 VERIFIED）
              + DIVERGENT 6（get/put-nonexistent-version/ghost-404/put-bad-body/latest-recompute/bulk-reject
              ——全部待 L012-2b）；DISCOVERED→SPECIFIED 8（经 L012-1 证据直接落 SPECIFIED+，其中 2 面
              IMPLEMENTED）；台账分类：BUG×5 + INTENTIONAL×1（D5）
Security:     契约 authentication 断言：全族 type=basic role=user（差分臂）；匿名 PUT=401（m10，message 入
              措辞组）；匿名 GET=实例配置差非协议面（差分认证臂——normalize auth.anonymous_arm）。
              D5 path_pattern 反向门兼防 internal 路径泄漏（/internal/、goroutine）
Performance:  无 timing 断言（dist-tags 族全本地元数据操作，L012-1 无时延面观察；不立无源断言）
Risks:        ① D2 重算基准（semver 最大 vs 最近发布）证据态不可分辨——按「最高版本」立，分辨探针列差分
              批跑候选（先发 2.0.0 再发 1.0.2 后删 latest）；② D7 Cache-Control 值源（固定 vs 配置）未证，
              按活体值 60 立 literal，发现可配降 pattern；③ 头面证据边界：wiretap 未逐项落盘响应头，除
              Cache-Control 外无头断言——差分批跑捕获后补；④ 同值幂等重 PUT 直发臂未活体（wire 为客户端
              短路），按 m06 同面推断待批跑补；⑤ proposed_literal（invalid dist-tag body）为实现建议非
              参照 verbatim——终字由 L012-2b 落地后契约升 verbatim
Blockers:     无（六项裁定齐备，authority 链完整：conductor_ruling + BOARD sprint tick + L012-1 证据）
Next:         转差分（differential-qa-engineer 批次输入）：L012-2b 实现落地后对 contracts/npm.yaml 全 8 条目
              批跑（curl 矩阵 20 格 + npm CLI 10.9.8 腿；含 D2 分辨探针、bulk 不实建复核 m19/m20、
              同值幂等直发臂）；L012-2b 实现票输入=6 条 ledger 的对齐面清单；转 reverse-engineer：无规格缺口
              （L012-1 证据面充分；头面逐项捕获属差分批跑装备增强非规格缺口）
```

## 验证输出存档

YAML 门（2026-09-13）：

```
$ ruby -ryaml -e 'YAML.safe_load(File.read(f), aliases: true)' × 4
docs/compatibility/matrix.yaml OK
docs/compatibility/contracts/npm.yaml OK
docs/compatibility/fixtures/normalize.yaml OK
docs/compatibility/known-divergence.yaml OK（entries=37，npm 域 6）
```

行数门：`grep -c "row_id:" matrix.yaml` = **200**（收票时点不变——冻结行集无增删，D12-R05 仅加 note/refs）。

引用完整性门：divergence_ref 6/6 锚命中（`^- id: npm/…$` 逐条 HIT）；evidence 文件 14/14 存在。

契约面核对门（IMPLEMENTED 依据）：

```
internal/adapter/npm/route.go:16: routeDistTags    // -/package/<name>/dist-tags
internal/adapter/npm/route.go:17: routeDistTag     // -/package/<name>/dist-tags/<tag>
internal/adapter/npm/api.go:229: case routeDistTags: h.serveDistTags(...)
internal/adapter/npm/api.go:231: case routeDistTag:  h.serveDistTag(...)
```
