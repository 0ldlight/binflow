# QA 报告 T-221 — M7 验收 I：RBAC 角色×能力全表 + manage 派生 + 真实客户端（V01~V11）

- ticket: T-221 [P1]（FR-64 V01~V06 / FR-65 V07~V11 / 矩阵归档 / 负面矩阵 / M1+M4 回归）
- QA: qa-engineer，2026-08-23
- 被测物：main @ `e7b581e` 之后工作区（T-212/215/217 已全量提交的 RBAC 面）
- 方法：真实栈双实例（宿主 binary 127.0.0.1:18091 + 容器化实例 127.0.0.1:18120〔docker daemon 在 VM 内，宿主 loopback 不可直达——T-45/T-175 先例〕）+ 真实客户端（docker 29.7.2 / mvn 3.9.9 + JDK 21.0.12（/tmp/t67-tools，T-84 留存工具链）/ npm 10.9.8 node 22.23.2）+ curl 全腿。
- **binary 新鲜度**：起栈前 `rm -f bin/binflow-server && make build`，构建完成 10:29:26，起栈 10:29:29（3 秒差，无陈旧制品；/tmp/t67-tools/binflow-server 陈旧二进制未触碰）。容器镜像 `binflow:t221qa` 由**当前源码** `docker build -f deploy/dev/Dockerfile` 现场构建（exit=0）。

## 结论表

| # | 验收标准 | 结果 | 证据摘要 |
|---|---|---|---|
| V01 | adminRole 分配（wire/回显/sqlite 落值） | ✅ | PUT `api/security/users/alice` +`adminRole=readonly_admin` → **201**；GET 回显 `"adminRole": "readonly_admin"`（camel 键、snake 值，ADR-0026 决策 6）；sqlite 直查 `users.role`：`alice|readonly_admin|0`、回填 `admin|admin|1`。集合路由 POST 建号 +adminRole 亦 201 且回显（roa2）。**AC 字面偏差（非缺陷，已登记 PM 回写 T-215 遗留①）**：PRD 骨架 `POST /{name}` 建号 → 实为部分更新臂，缺用户 404 `User not found`；form 编码 POST → 400（JSON-only 契约） |
| V02 | readonly_admin 读面 11 端点全通 | ✅ | alice 逐腿：repositories 列表/单查、v1/health、users 列表/单查、groups、v1/permissions、v1/audit、v1/replication/status、v1/replications、v1/storage/stats 全 **200**；`v1/storage/migration` → **501** `migration is not configured`（裸实例「过门 501」判，RB-03） |
| V03 | 变更面全拒 + 零副作用 | ✅ | alice 12 写腿：建仓臂 PUT repositories/alice-repo、PUT users/bob、DELETE groups/devs、POST+DELETE v1/permissions、token revoke、GC **dry-run 与 apply 两腿**、配额写 POST repositories/generic-local、组写 PUT → 全 **403**。零副作用：bob/repo 配置（含 quota）/group/permissions 列表四实体 **cmp 字节一致**；alice-repo 仍 404。PRD 字面 `POST /api/repositories`、`PUT /api/v1/permissions/t1` → **404**（路由本不存在，T-214 P8/T-217 已登记；等效真实臂全 403） |
| V04 | 同 Token 升降即时生效 | ✅ | victor(user) 自铸 token → GET users **403** → admin POST adminRole=readonly_admin（200）→ **同一 token 重放 200** → 降回 user → **再 403**。全程无重启、无换发 |
| V05 | 越权与冲突 | ✅ | alice 改 bob 角色 403；alice 改自己 403；`admin=false`+`adminRole=admin` → **400** 纯文本（conflicting 'admin' and 'adminRole' fields…）；普通用户 victor 带 adminRole 建号 403；非法枚举 `adminRole=root` → 400（supported: user, readonly_admin, admin）；bob/alice 角色未被篡改（sqlite 复核） |
| V06 | `user.role.change` 审计 | ✅ | `GET /api/v1/audit?action=user.role.change` 4 事件全形：actor=admin、detail{user,old,new}——alice/roa2 建号 old="" new=readonly_admin、victor 升 old=user new=readonly_admin、降 old=readonly_admin new=user |
| V07 | carol（r/w/d/manage 经组）派生链 | ✅ | admin POST t-app（groups app-admins:[read,write,delete,manage]）→ 201，回显含 manage 字母；carol POST 同名 target 增 `users.dave:[read]` → **201**；dave GET `app-local/lib.a` → **200**（内容逐字）；dave PUT → 403（只读对照）。**AC 字面偏差（非缺陷，T-217 遗留②已登记）**：permissions 无 `PUT /{name}` 路由，编辑臂 = POST create-or-replace（201） |
| V08 | 边界腿 | ✅ | carol：建仓臂 PUT brand-new **403**（后核 404 未建出）；t-other（disjoint）403；t-mix（部分交集 [app-local,other-local]）403；**空集 repos=[] 403**；子集对照 [app-local] 新名 t-sub **201**；PUT users/dave 403；token revoke 403；GET repositories 列表 403 / 单仓 app-local 200；dave 授权存活 200、dave email 未变 |
| V09 | 正交腿（carol2 仅 manage） | ✅ | t-mgmt（users carol2:[manage]，无 r/w/d）：GET repositories/app-local **200**、POST 编辑 t-app（⊆覆盖集）**201**、PUT 制品 **403**、GET 制品 **403**。配额写 POST quotaBytes=1048576 → 200 且 `configuration.quotaBytes=1048576` 落值 |
| V09+ | usage ∨-臂翻转（T-217 新增语义） | ✅ | carol2（仅 m）GET `/api/v1/storage/usage/app-local` → **200**（单臂时代 403）；carol（r 经组）200（W26b 臂不变）；victor（无授权）403；carol2 usage(other-local 越界) 403 |
| B1 | 同名越界替换回归（T-217 返修后） | ✅ | admin 建 t-ent（repos=[other-local]，dave:[read]）→ carol POST t-ent（body=[app-local] ⊆ 覆盖集、存量 ⊄）→ **403 + 权限清单 cmp 字节不变**；对照 t-in（存量 ⊆）同形替换 **201**；dave 在 other-local 的既有 read 事后 GET 制品 **200** 未被吊销 |
| V10 | 零回归（M1 C22/C27 + M4 W17~W21） | ✅ | C22a/b/c 全绿（201/400/403/201/403/204）；C27 匿名关实例（18092）四腿 401+WWW-Authenticate/200/200/403 全中；W17 组 CRUD、W18 成员+bad group 400、W19 继承（201/403/403）、W19b 即时生效 403、W19c 并集（GET 200 经 user + PUT 201 经组 + 双授 delete 204）、W20 删除保护 409→解引用→200→jane groups 联动 []、W21 `?permissions` 回 jane[r]+devs[r,w]（T-113 键值方向）。注：W20 首跑 409 为本 QA 夹具残留引用（union-t/devs-rw 重建），清引用后全绿——非产品缺陷 |
| V11 | 真实客户端（manage 位不动摇协议） | ✅ | carol（app-admins r/w/d/manage）：**docker** login Succeeded → push `myimg:t221` 全层 Pushed（digest sha256:5c88e740…）→ rmi → pull digest 一致；**mvn** deploy exit=0（pom/jar/sha1/maven-metadata.xml 全 200）+ 全新 m2 `dependency:get` exit=0 落 com/acme/t221-app jar+sha1；**npm** whoami=carol → publish `+ t221-demo@1.0.0` exit=0 → packument 200 → 全新目录 install exit=0 且可执行。**readonly_admin docker 臂**：alice login 后 push → 逐请求 denied（DENIED, repository:app-docker/roa-img:pull,push）；原始 `/v2/token` 臂：alice push-scope token 发 blob upload **403**、PUT manifest **403**；pull-scope token GET manifest **200**；carol 对照 202 —— token 臂与 REST 臂同源 |
| 矩阵 | 观察模式 + EXPECT=1 双跑归档 | ✅ | `make test-m7-rbac-matrix`（观察）与 `EXPECT=1 make test-m7-rbac-matrix` 双跑，**0 deviations**，guard 四段 OK（见下表） |
| 负面 | readonly_admin × 全写端点 / user × 全管理端点 | ✅ | 矩阵 W01~W07（建仓/用户写/组删/权限写/revoke/GC dry-run/配额写）readonly_admin 列全 403；user 列 R01~R12+W01~W07 全 403。另采：readonly_admin 数据面全域只读短路（GET 未授予仓制品 200、PUT 403、DELETE 403 且制品存活）、自铸 token 200（Q11 族8）/ 为他人铸 403 |
| 回归 | 全仓自动化 | ✅ | `go test -race -count=1 ./...` → **23 包 ok、0 FAIL、exit=0** |

## 矩阵归档（EXPECT=1 裁决跑，BASE=127.0.0.1:50990 一次性实例）

| endpoint | readonly_admin | user | admin |
|---|---|---|---|
| R01 GET api/repositories | 200 | 403 | 200 |
| R02 GET api/repositories/m7-matrix | 200 | 403 | 200 |
| R03 GET api/v1/health | 200 | 403 | 200 |
| R04 GET api/security/users | 200 | 403 | 200 |
| R05 GET api/security/users/m7user | 200 | 403 | 200 |
| R06 GET api/security/groups | 200 | 403 | 200 |
| R07 GET api/v1/permissions | 200 | 403 | 200 |
| R08 GET api/v1/audit | 200 | 403 | 200 |
| R09 GET api/v1/replications | 200 | 403 | 200 |
| R10 GET api/v1/replication/status | 200 | 403 | 200 |
| R11 GET api/v1/storage/stats | 200 | 403 | 200 |
| R12 GET api/v1/storage/migration | 501† | 403 | 501† |
| W01 PUT api/repositories/m7-matrix-create | 403 | 403 | 200 |
| W02 PUT api/security/users/m7bob | 403 | 403 | 201 |
| W03 DELETE api/security/groups/m7-sac-grp | 403 | 403 | 200 |
| W04 POST api/v1/permissions | 403 | 403 | 201 |
| W05 POST api/security/token/revoke | 403 | 403 | 200 |
| W06 POST api/v1/system/gc（dry-run） | 403 | 403 | 200 |
| W07 POST api/repositories/m7-matrix-w（配额写） | 403 | 403 | 200 |

† R12 =「过门 501」（裸实例无双写；user 列 403 证明是门在裁决）。guard：m7bob unchanged / repo config unchanged / group unchanged / create-arm repo still absent —— OK。**verdict: 0 deviations**。

## 客户端矩阵

| 客户端 | 操作 | 结果 |
|---|---|---|
| docker 29.7.2 | login（carol）/ push / rmi+pull（digest 一致） | PASS |
| docker | readonly_admin push（CLI + /v2/token 双臂）/ pull | PASS（push 逐请求 403；pull 200） |
| mvn 3.9.9 + JDK 21 | deploy（altDeploymentRepository）/ dependency:get（全新 m2） | PASS（exit=0 ×2，产物 sha1 链核验） |
| npm 10.9.8 | whoami / publish / install（全新目录） | PASS（exit=0 ×2，包可执行） |
| curl | V01~V10 + 负面 + 数据面短路全腿 | PASS |
| pip | 未涉及（本票无 pypi 腿） | 未涉及 |

## 缺陷

**无（P0~P2 均无）。**

非缺陷登记（PRD 骨架字面 vs 路由契约，开发两票已登记、待 PM v1.2 回写，本轮实证复核一致）：
1. V01/V03 骨架的 `POST /api/security/users/{name}` 建号 → 实为部分更新臂（缺用户 404）；建号臂 = PUT /{name}（201）或集合 POST。form 编码 400（JSON-only）。
2. V03 骨架的 `POST /api/repositories`、`PUT /api/v1/permissions/t1` 路由不存在（404）；等效真实臂（PUT 建仓臂 / POST create-or-replace）均 403。
3. V07 骨架的 `PUT …/permissions/t-app` 200 → 实为 POST create-or-replace 201（无 PUT /{name} 路由）。

AC 歧义标记：无新增（上述三条为已登记项的复核确认）。

## 环境与清理

- 宿主栈 127.0.0.1:18091（/tmp/t221）、C27 栈 18092、容器 t221qa（18120）——测毕全停全删；docker logout ×2、推送镜像与 `binflow:t221qa` 清除、/tmp/t221 与 race 日志清除。
- 未动他人产物：/tmp/t67-tools（T-67 留存工具链，仅引用）；ps 中 t216-ac-legs 实例属 T-216，未触碰。
- 全程未写任何密钥进仓库文件；凭据（t221-admin-pw 等）仅存在于临时目录与命令行，已随清理销毁。
