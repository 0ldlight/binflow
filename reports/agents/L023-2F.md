# L023-2F — build-info 差分 8 BUG 修复（D1-D7/D10；D8/D9 未动候裁）

Ticket:       L023-2F（LOOP 023 返工票：build-info 差分修复——D1/D3 为主力）P0
Role:         dev-go-core（area = internal/build + internal/httpapi + internal/metadata 增量）
Area:         与 L023-2A/B/C 同域（internal/build）
Input:        reports/compatibility/L023-buildinfo-diff.md（最新权威——与 docs/reverse/build-info.md 冲突处以报告为准）；wire 证据 reports/compatibility/l023d-wire/{a,b}/（a/18、a/21、a/22a、a/02、a/09、a/10a-d、a/16b、a/19、a/20、a/26c 逐字核对）；差分工具 tools/difftest/l0232d/buildinfo_diff.py（case 20/22a 请求形核对）
Changes:      按报告 §3：
              1) **D1（高）promote 400 两面性拆分**：E12 abort 臂（制品不可解析：record-only 无地址+属性通道空/节点缺失/源仓消失/搬迁 manifest 缺失）从「400+messages body」改为 **errors[] 信封**——refuse() 直接抛 400 StatusError（`Unable to find artifacts of build '<n>' #<num> from <buildRepo> repo: aborting promotion.` 逐字经 writeBuildError 信封渲染）；**流程完成面维持 400+messages**（invalid timestamp〔case 21 逐字〕/failFast 集齐 warning 的 finalizeStatus）——票 2B 的 errAbortPromotion+degraded 机制保留给该面。status-update 跳行不再出现在 abort 臂（硬错误路径自然跳过）。
              2) **D2（中）messages 置空**：删全部自造 INFO 汇总行——通用汇总（`Promotion of … completed, N artifact(s) migrated (moved)`）、artifacts=false 臂、dry-run 臂、docker 闭包臂；成功 = `{"messages":[]}`（a/22a）；lenient 警告面 = 单 WARNING 行（a/19）。moveVerb/targetLabel 死代码随删。
              3) **D3（高·功能性）制品收集双通道**：①**manifest 通道**——artifact 的 originalDeploymentRepo+path 对（§3.1 第六 wire 键）为**直查地址**：resolveArtifactNode 改 originalRepo 优先（兼容旧「path 首段为仓」split）；promote 走查时**实时直查**（存储 FK 仅是上传时快照——文档先于制品发布的次序合法，a/22a 即此形）；②**属性通道**（无 manifest 地址时的回退）——metadata.NodePropStore 新增 FindByProps（node_props 索引面 EXISTS 连查，接口形态=消费侧 PropertyFinder seam，aql.md property REST 面维持面外）；按 build.name/build.number 双键查节点（jf rt upload --build-name 标注集），按 path 等值/后缀匹配无地址行，经 carrier 真搬迁（源仓消失=warning 跳过；docker/类型不匹配行不属该通道）。两通道全 miss 才落 E12。
              4) **D4（中·echo）**：迁移 027（sqlite+postgres）build_artifacts 增 wire_path/original_repo 两列；BuildArtifact 增 WirePath/OriginalRepo；echo 六键全保（空串回显——结构体去 omitempty）、path=文档原样值、originalDeploymentRepo 非空时随行；PUT/append 存取贯通。
              5) **D5（低·echo）**：slim 的 properties **键省略**（非 null，a/09）；statuses 条目 comment 空→键省略（a/16b）；buildRetention 块回显补默认三字段（count 缺省 -1/deleteBuildArtifacts false/buildNumbersNotToBeDiscarded []，a/26c）。
              6) **D6（低·文案）**：详情 404 空格口径——无 started 子句**尾空格**（`…build number: 99 `）、有子句**逗号前空格**（`…99 , build started: <ts>`）（a/10a/10c/10d 逐字）。
              7) **D7（低·文案）**：malformed started 400 = 参照 DateTimeFormatter 形 `Invalid format: "<input>" is malformed at "<rest>"`——rest 取规范布局前缀行走的首个断点（skeleton `0000-00-00T00:00:00`+分数段+偏移首字符判定；a/10b 样例 `+`→空格逐字复刻）；详情面 ?started= 预检先行。
              8) **D10（低）**：批删 deleteAll 文案的 `<repo>` 内插解析后 buildRepo——**复验结论：2C 实现已正确**（res.Repo=Coordinate.Resolve() 产物；自定义 buildRepo 走 body 面传递）——新增 TestBatchDeleteAllInterpolatesResolvedRepo 钉死（PUT ?buildRepo=l0232f-bi-build-info → POST body buildRepo 同键 → 文案 `under 'l0232f-bi-build-info'`）。报告 B 侧观察疑为路径面无参请求（缺省仓即解析仓）或 2C 前版本；以本测试为回归锚。
              9) **D8/D9 未动**（INTENTIONAL 候选，归裁定通道）；D11（AQL property REST 面）未动（T-511 提权裁决中）——D3 的属性通道走 metadata 面，不触 AQL REST。
Files:        新增：internal/httpapi/build_promote_collection_semantics_test.go（manifest 通道真搬迁+回显对/属性通道 jf 形搬迁/D10 内插三用例）；internal/metadata/migrations/{sqlite,postgres}/027_build_artifact_wire.sql。
              修改：internal/build/promote.go（D1 refuse 硬化/D2 汇总行删除/D3 双通道+实时直查+migrateTagged/PropertyFinder seam/签名重排）；internal/build/upload.go（Artifact.OriginalDeploymentRepo wire 键+resolveArtifactNode originalRepo 优先+存储贯通）；internal/httpapi/build.go（D4/D5 echo 族/D6 空格/D7 invalidFormatMessage+malformedAt 前缀行走+详情预检）；internal/metadata/api.go（BuildArtifact 两字段+NodePropStore.FindByProps 接口）；internal/metadata/substores_nodeprops.go（FindByProps 实现）；internal/metadata/substores_builds.go（wire 列存取）。
              测试对位：internal/build/promote_test.go（dangling 臂改 errors[] 断言+lenient 单行断言）；internal/httpapi/build_append_promote_semantics_test.go（E12 双臂改信封/单行断言）；build_promote_test.go（成功空 messages）；build_query_semantics_test.go（slim 键省略/D6 空格/D7 逐字）；build_rest_test.go（echo 结构体扩六键+path 原样断言）。
              删除：无。
Tests:        新增三用例：manifest 通道（originalDeploymentRepo+path 直查→200 空 messages→源 404 目标 200→echo 对保真）；属性通道（jf 形：node 标注 build.name/number+裸路径行→真搬迁）；D10 内插。存量六处对位后全绿（见 Changes-测试对位）。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；golangci-lint run（0 issues）；go test ./internal/build/ ./internal/metadata/ -count=1（metadata 一次中途 FAIL 为编辑中编译产物，复跑 ok 53.1s）；go test ./internal/httpapi/ -count=1 -timeout=20m 全量（**ok 1052.9s**——本机多套件并跑自扰时单套件超 600s 默认超时会产生「无名 FAIL」假象〔panic: test timed out〕，单跑加 timeout 后全绿；ticket A/B/C 时代的 186-266s 基线同码无回归，慢为负载面）；go test -race ./internal/build/ -count=1（ok 381.0s）；go test -race ./internal/httpapi/ -count=1 -timeout=20m（build 族）+ go test -race ./internal/metadata/ -count=1 -timeout=20m（顺序单跑）；冒烟：隔离实例 127.0.0.1:18096（独立 data-dir，零 docker 零 172.16.58.130，探针后全清）F1-F10 逐臂 + jf 2.122.0 真腿三连。
Outputs:      冒烟逐臂（对齐差分报告 wire）：F1(=case 18) 400 `{"errors":[{status:400,message:"Unable to find artifacts of build 'l0232f-app3' #1 from artifactory-build-info repo: aborting promotion."}]}` 信封逐字；F2(=19) 200 单 WARNING 行 `Unable to find the following artifacts of build 'l0232f-app3' #1: a.jar`（无汇总行）；F3b(=20 报告原请求形 target+空制品) 200 单行 `Skipping promotion status update: no status received.`；F4(=21) **400+messages** 双面性维持（INFO relocation-skip + ERROR `invalid\unparsable timestamp not-a-time.` 字面反斜杠）；F5(=22a) manifest 直查 → **200 `{"messages":[]}` + 真搬迁**（dev 404/rel 200）；F6(=22b) statuses 六元组 `{repository:"l0232f-rel",status,timestamp .SSSZ,timestampDate,user}` + artifact echo 六键（空串 checksum 在）+path `x/y/a22.jar`+originalDeploymentRepo 保真；F7(=26c) buildRetention 回显 `{"buildNumbersNotToBeDiscarded":[],"count":1,"deleteBuildArtifacts":false}` 默认三字段补齐；F8(=09) slim modules [] 且 **properties 键不存在**；F9(=10a/10d) `'…build number: 99 '`（尾空格）与 `'…6 , build started: …'`（逗号前空格）repr 级逐字；F10(=10b) `Invalid format: "2026-09-15T09:00:00.000 0000" is malformed at " 0000"` 逐字。**jf 真腿三连**：`jf rt upload --build-name/--build-number` exit ok（标注落地）；`jf rt build-publish` PUT 落库（尾部 version='dev' 既有债+AQL property D11 面外维持，登记未动）；**`jf rt build-promote l0232f-jfapp 7 l0232f-rel --status=smoked` exit ok「Promoted build …」+ 属性通道真搬迁（dev 404/rel 200）+ statuses 落地全 wire 形**——差分报告 §4 该腿 DIVERGENT(D1+D3) 的双端修复实证。
Compatibility: 权威对照（差分报告优先于 docs/reverse/build-info.md）：D1 两面性（abort=errors[] 信封/流程完成=400+messages）——报告 §3-D1「L023-1 §11.5-9『body 同形』为误读」的落地修正，**spec 勘误输入①已随并行规格回写票处理**；D2 空 messages（自造汇总行证伪删除）；D3 双通道（manifest originalDeploymentRepo+path 实时直查→属性通道回退）+originalDeploymentRepo 升格 wire 第六键；D4/D5 echo 保真五处；D6/D7 文案逐字；D10 复验已正确+回归锚。**登记**：① D3 属性通道匹配规则（path 等值/后缀匹配无地址行）为最小实现——报告未载 A 的精确对账规则，jf 形实测通过；② D7 的 rest 计算为规范布局前缀行走的近似（Java 位置语义的多布局面未全仿），样例逐字、任意输入逐字节等价未证——差分复跑若出新样本再校；③ 载体拒绝臂（copy/move 权限缺）仍走 messages 面（报告未证其归属——abort 证据仅 E12 面）；④ dependencies=true 的 BinFlow 自有 warning 行保留（报告未点名）；⑤ dry-run 面 messages 现为空（汇总行删除的连带推定——报告无 dry-run 样本）。
Security:     FindByProps 参数化查询（零拼接）；属性通道读取经既有 ACL 之后的 promote 门内（d(buildRepo)+w(target)+逐项 carrier 阶梯不变）；D7 文案内插输入值经 writeError JSON 转义；冒烟口令仅 shell 变量，实例/临时文件用后全清（build deleteAll+repo 删除+进程杀除+文件清除，final-list 404 复验）。
Performance:  无新测量面。属性通道一次 FindByProps（idx_node_props_name 索引，limit 1000）+ 内存 path 匹配 O(n·m)（build 制品量级 KB 级）；manifest 直查每行一次 nodes.Get 主键读；027 两列 ALTER 仅增列零重写。jf 真腿全链时延无回归观察。
Risks:        ① D3 的 A 侧精确对账规则 UNKNOWN（登记①）——契约化前建议差分复跑加「文档先于制品发布」「多制品部分命中」两探针；② D7 近似面（登记②）；③ 027 迁移对在途部署=两列默认值增列（零数据搬运，风险低于 026 重建）；④ metadata 一次中途 FAIL 为并行编辑期编译产物（复跑绿）——无真实回归。
Blockers:     无。
Next:         ① 差分复跑（buildinfo_diff.py 同 case 集）验证 D1-D7/D10 全 SAME——本票冒烟已按 wire 逐臂自证，双端复跑归差分通道；② D8（media-type label 族）/D9（projects 写门）候裁；③ D11 T-511 提权；④ spec 回写票并行（报告 §7 七条勘误输入）；⑤ matrix D07-R07（promote）翻态候选——D1/D2/D3 修复后差分复跑确认。
