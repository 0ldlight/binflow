# L008-2 — dev-registry-adapter（docker 域）双小票：D3 blob 豁免 + virtual 冷 404 注记

Ticket:       L008-2 双小票（conductor LOOP 008 轨道 L008-2；P 级随轨道——两票均终裁 BUG 对齐收实现面）
Role:         dev-registry-adapter（领域实例：dev-package-docker；helmoci 经 docker /v2 plane 单码径自动覆盖）
Area:         internal/adapter/docker（helmoci 零改动——delegation 单码径，仅跑其既有测试回归）
Input:        reports/compatibility/L007-d3-virtual-probe.md §1（D3 四臂铁证）/§2.4（virtual 冷负 miss 首答注记分歧）；docs/compatibility/known-divergence.yaml docker/remote-blob-expired-revalidation（conductor LOOP 007 终裁 2026-09-12：BUG 对齐收——blob 豁免检索窗）与台账 docker/virtual-remote-cold-negative-body-annotation；ADR-0012 决策 1（制品不可变原则）+ 勘误一（expired-but-serving）；internal/repo/api.go RemoteProbe* 状态机（STALE=过期副本+Node 非空）；internal/remote/cachestate.go（HIT/MISS/STALE/REVALIDATED 词表，超集头契约 tolerated——docs/compatibility/contracts/docker-remote.yaml superset_note）
Changes:      小票 1（D3 豁免）：① remote.go serveRemoteBlob 在 Negative 臂后加「已验证 standing 副本豁免臂」——probe.Node 非空且 node.Sha256==请求 hex（LandRemoteBlob 落地即 digest 强校验，standing 即校验和已验）→ 本地应答（CacheHit 标记、零上游、缓存行不重写不滑窗）；负缓存行（RemoteProbeNegative）先判早退语义不变；manifest 面到期条件重验臂一字未动。② virtual.go serveVirtualBlob 成员 walk 同款豁免（成员域 standing 判定，serve 与 HIT 臂同形 origin=""）。小票 2（virtual 冷 404 注记）：③ virtual.go 两处 walk 终态（manifest+blob 臂）unfound.write(w, summary) → 纯 body（unfound.write(w, "")），summary 非空时移 WARN 日志行（last_member_summary 字段）；④ 文件头 walk 降级注释同步（plain 终态）。直读面终态本就 plain（remote.go 冷 miss unfound.write(w, "")），未动
Files:        修改：internal/adapter/docker/remote.go、internal/adapter/docker/virtual.go、internal/adapter/docker/remote_unfound_shapes_test.go（翻面：message 前缀断言→逐字节相等断言）；新增：internal/adapter/docker/remote_blob_expiry_exemption_test.go；reports/agents/L008-2.md
Tests:        新增 table-driven：TestRemoteBlobExpiryServesStandingCopy（直读面五臂：过期 GET×3=standing HIT/INM=本地 304 全 face/HEAD=standing + 上游计数冻结断言 + manifest 控制臂仍回源）、TestVirtualBlobExpiryServesStandingCopy（walk 面：过期 GET×2/INM=304 + Resolved-From + 上游冻结）；翻面：TestVirtualUnfoundBodyCarriesRemoteShape（virtual 终态 404 message 逐字节=E6 静态串，无 upstream-summary 后缀）；回归（未改期望全绿）：TestManifestRevalidationUpstream304（manifest 窗语义不动）、TestT392RemoteDegradationMatrix（STALE 降级臂）、TestVirtualColdMissNegativeCache（负缓存窗）、TestClientConditionalBlobMatrix、TestRemoteUnfoundBodiesMatchArtifactory、helmoci 全包（TestRemoteDegradationMatrix/TestVirtualDegradationMatrix 等）
Commands:     go build ./... && go vet ./internal/adapter/docker/... ./internal/adapter/helmoci/... && gofmt -l internal/adapter（空）&& $(go env GOPATH)/bin/golangci-lint run ./internal/adapter/...（0 issues）；go test ./internal/adapter/...（全 ok，docker 20.6s/helmoci 14.8s/14 包全绿）；定向 -run 'TestRemoteBlobExpiry|TestVirtualBlobExpiry|TestVirtualUnfoundBody|TestRemoteUnfoundBodies|TestManifestRevalidation|TestT392RemoteDegradation|TestVirtualColdMiss|TestClientConditionalBlobMatrix' 8/8 PASS。差分：make release VER=uat-l0082-d3 → 离线镜像（tools/difftest/l0072/build-image-offline.sh 复放，ALPINE_DGST 钉基）→ binflow-ga 换容器（同卷 binflow-uat_binflow-data）；上游 registry:3 @127.0.0.1:5591 种 l0082/busybox:t1（config blob c6348fa8/459B 与探针同指纹）+ l0082/hello:cold；双端三仓（l0082-b-remote/l0082-v-remote TTL60 + l0082-virt）回读确认
Outputs:      小票 1 差分（探针 §1.2/1.3/1.4/1.5 四臂重跑，上游计数按 path×UA）：warm 后 sleep 70 过窗——§1.2 过期 GET×3：BF 200/459B×3 全 X-Binflow-Cache: HIT（5-17ms 本地）上游 Δ=0，ART 本地上游 Δ=0（原 BF GET1 必 +1 DIVERGENT→今 0 vs 0 SAME）；§1.3 过期窗+quoted INM：BF **本地 304**/5.7ms/full artifact face（Etag 105e5808…）上游 Δ=0，ART 304 本地 Δ=0（原 BF +1 重取后再 304）；§1.4 >600s=10.4×TTL：BF 200/459B/HIT 6ms 上游 Δ=0，ART 本地 Δ=0；§1.5 真实 docker CLI 27.5.1（dind）双端 pull exit 0 且镜像可运行——manifest BF=条件 GET→上游 304（窗语义不动）ART=HEAD 探查（X1 形）；config blob **BF Δ=0（过期豁免本地）ART Δ=0**（原按窗必 +1）；layer 冷取 BF +1（ART 0 系其持久 filestore 跨会话去重环境伪差，N1/N2 形状族既有登记内，非本票面）；行证：remote_cache 行 fetched 05:23:57→expires 05:24:57 全程未重写（blob 文件 mtime 亦未动）=参照「既不回源也不重写」语义对齐。小票 2 差分（一臂）：virtual 冷负 miss 首答——BF 138B **plain**（"The named manifest is not known to the registry."，无 "(upstream answered 404 404 Not Found)" 后缀；原 176B 注记版）vs ART 137B plain，唯一差=尾换行（既有归一内）；neg1 body ≡ neg2 body；BF 首问上游 GET 404 +1、二问起本地零回源（负缓存机制不变）；WARN 行取证：`level=WARN msg="docker virtual: no member answered (terminal unfound)" repo=l0082-virt image=l0082/hello reference=nope last_member_summary="upstream answered 404 404 Not Found"`（诊断信息离线入日志）；直读面冷 404 复核仍 plain 138B（无回归）
Compatibility: 小票 1 = D3 账（docker/remote-blob-expired-revalidation）review_gate 兑现：四臂上游计数归零双端同，分类可按终裁由 BUG→resolved（evidence=本日志 Outputs + known-divergence resolved 段待 compatibility-engineer 回写）；小票 2 = 台账 docker/virtual-remote-cold-negative-body-annotation 实现面清偿（首答对齐 plain，尾换行既有归一内），登记条目可销。可差分 surface（供 differential-qa-engineer L5）：/v2/<remote>/<img>/blobs/<digest> GET|HEAD ×{过期窗 ×3、INM、10×TTL} → {200 standing-HIT 全 face/304 全 face}+上游计数 0；/v2/<remote>/<img>/manifests/<tag> 过期窗 → 仍回源（控制臂）；/v2/<virtual>/<img>/manifests|blobs 冷负 → plain 404 体（137B/138B 归一内）。漂移点：豁免臂 X-Binflow-Cache 取 HIT（超集头契约 tolerated；STALE 词义=故障降级故不取）；ART layer 0-接触系其持久 filestore 去重环境伪差
Security:     无新增面：路径校验/SSRF 链未动；豁免臂零上游接触（攻击面收窄）；凭据不落日志（WARN 行仅 summary 文本）
Performance:  正向：过期 blob 每窗省一次上游往返+带宽（2.1MB layer 量级 ×N 窗）；流式路径未动；负缓存行/降级矩阵不变
Risks:        ① 豁免后 standing blob 的缓存行永呈 expired（行不滑窗）——负缓存行键与内容行同路径但先判早退，语义边界已在测试锁（负 miss 后 standing 副本在 missedTTL 内不可见=既有行为维持）；② node.Sha256≠请求 hex 的脏行角落回落旧回源路径（未消除既有 stale-serve 行为，超票据范围）；③ 客户端覆盖面：curl 8.7.1 + docker CLI 27.5.1（真实 pull/run）；crane/skypeak/ORAS 未跑（blob GET 语义与 UA 无关，风险低）
Blockers:     无
Next:         ① compatibility-engineer：known-divergence.yaml D3 条目按 resolved 回写（date 2026-09-12、evidence=本报告）+ virtual-remote-cold-negative-body-annotation 销账；② ADR-0012 勘误四建议文本已随本票回报（architect 域，未自写 DECISIONS.md）；③ 后续差分消费 UAT :8083=uat-l0082-d3（healthy，含本双票）；金样候选：virtual 冷负 plain 404 体（137B/138B 归一对）
