# T-435 工作日志 — FR-141.4 规格增量段三份（P0，B0 前置锚）

- role: reverse-engineer ｜ area: docs/reverse/（aql.md 增量段 + remote-browsing.md 新建 + cron-scheduling.md 新建）+ 本日志
- 日期：2026-09-03 ｜ 分支 develop ｜ dep: —（与 T-436 ADR-0044 软协作：cron 锚 §6 对齐缝，Accepted 前对齐）
- 输入：docs/M16-SPLIT.md §1.2 T-435 票块、docs/prd/milestone-16.md §4.1/§4.8/§4.10 + K67~K72 + LC-91/93/94/95、docs/reverse/{aql.md,repo-semantics.md,inv-1-core.md §E,inv-2-surface.md §1.C,auth-integration.md}、reports/agents/{T-425.md §1/§2,T-381.md §0/INC-1,T-407.md}、reports/agents/t407-evidence/（v8k/l/m/n/o + v16）、reverse-src/artifactory/（只读）、JFrog 官方文档 + Quartz 官方教程（2026-09-03 实取）

## 产出（三份 + 索引）

1. **docs/reverse/aql.md §14 增量段**（+头部标题行更新，M15 冻结面 §0~§13 零改动）：
   - §14.1 statistics 域字段集：十字段表（类型/域默认输出/语义/置信度）+ 域默认集 = {downloaded, downloads, downloaded_by} + 零值 null 语义 + **remote_* 族语义澄清**（= 下游 smart remote 代理回拉本 local 制品，与 K69 三分口径正交——T-438/T-440 映射关键防错条）+ usage 端点 = 统计域固定模板查询（一鱼两吃先例）。
   - §14.2 `/api/search/usage` wire 全表：`notUsedSince`（**PRD 文本 `usageSince` 与 wire 实参名不符——三源钉死 notUsedSince，警示交 T-440**）+ createdBefore 缺省回退 + 命中布尔式 + 五字段行（活体 v8m 逐字节 vs 官方两字段瘦行——差异登记）+ 404 空集/缺参怪癖 + 排序键。
   - §14.3 dates/creation 双端点：from 必填 400 逐字、区间开闭（>from, <=to）、**to 缺省两源冲突登记**（官方 now() vs 反编译无上界→V-j）、dateFields 四合法值（含 remote_last_downloaded）+ 未知 400 逐字、creation 实按 created OR lastModified 匹配 + created 字段回显 fallback 怪癖、maven-metadata.xml 排除、上限键族。
   - §14.4 QRL 全量锚（K72）：REST 三操作 wire（admin 门 + 纯文本成功文案 + disabled 400 文案）+ 三态语义表（触发键/出厂默认=disabled）+ 两型配置模型 + HA permits 均分 + 指标 job 60s/jfrt_qrl + **与 AQL 429 并发闸的正交性澄清** + BinFlow 映射注记（非裁定）。
   - §14.5 UI 搜索族四端点操作清单（artifactsearch 9 操作/stashResults 10 操作含出厂 false 总开关与 404 文案/packagesSearch 2/syntax-search 开关与 400）+ 挂载前缀待验证注记 + searchResults 外延登记。
   - §14.6 对 §8.2 四条校正（零静默升格）+ §14.7 待验证 V-i~V-o 七条 + §14.8 取证锚点。
2. **docs/reverse/remote-browsing.md 新建**：T-425 §1/§2 直接成稿——可选档语义（默认 false/metadata TTL 缓存）+ 官方支持面（5 型，Opkg 缺位登记）+ 机制正反例 + 13 型枚举矩阵全表 + 上游故障降级六条（不整树塌/assumed-offline/display-only 零落库/缓存行优先/SSRF 无新面）+ §8.5 virtual 扩面（T-412 张力收口指向）+ 取舍基线登记 + 待验证 R-a~R-d（t226 Pro 建仓面不可活体→官方单源 + T-425 §1.4 留痕链）。
3. **docs/reverse/cron-scheduling.md 新建**（K70 归位）：Quartz 6/7 域语法表 + 特殊字符语义 + 出厂模板实例五形态（含裸 `/N` 形态）+ 出厂调度默认值表（backup×2/GC 4h/cleanup/virtualCleanup/CRON_NEVER/随机错峰）+ 校验拒绝形态表（cronExp is required/Invalid cronExp/emptyCron/invalidCron/shortCron 5min 闸/pastCron）+ next-run 语义（严格未来/now 重算不补跑/crontime 预览端点 Date.toString 怪癖）+ 待验证 C-a~C-d + §6 BinFlow 子集建议输入（**明示非裁定**，ADR-0044 软协作缝 + 待 T-436 对齐回填占位行）。
4. docs/reverse/README.md 索引：aql.md 行补 M16 增量段注记 + 新增两文件行。

## 活体腿状态（如实登记——降级，票据 R1 内置路径）

- t226（VM 172.16.58.129）**2026-09-03 会话不可达**：本机到 VM 路由经 TUN 代理（utun8/198.18.0.1），SSH TCP 假通但 kex 即断（`kex_exchange_identification: Connection closed`，重试 4 次）、API 8181/UI 8182 零响应——T-381 §0 `docker start` 恢复链无法执行（需 SSH 进 VM）。
- 降级处置：官方文档 + 反编译双/单源推进；**既有活体证据（T-407 会话 2026-09-01，同实例同版本）覆盖条目直接引用**（statistics 输出形态 v16、usage 五字段行 v8m、404 空集族 v8l/n/o、creation 命中行 v8k）；本段新增锚未复现项进待验证（aql §14.7 / remote-browsing §7 / cron §5），**零静默升格**。
- t226 保留态：未建立任何会话、零接触零残留（INC-1 差集法纪律下连第一次写探针都未发出）；环境恢复后按 T-381 §0 起栈、只读 GET 探针 + 前后清单差集复验。

## 自测与命令记录

- 反编译走读（只读）：`aql/model/AqlPhysicalFieldEnum.java`（statistics 十字段 + defaultResultField）、`rest/resource/search/types/{UsageSince,CreatedInRange,AnyDateInRange,GenericSearchResource}`、`api/rest/search/{LastDownloadRestResult,RestDateFieldName}`、`search/{SearchServiceImpl,stats/LastDownloadedItemsSearcherUtils}`（usage 布尔式/区间开闭/maven-metadata 排除/上限键）、`rest/resource/system/{QueryRateLimiter,RateLimiterResource}`、`throttling/qrl/**` 全族（三态/工厂/HAS/指标 job/指标 provider）、`throtlling/common/model/{RateLimiterConfig,RateLimiterSettings}`、`common/ConstantValues.java`（qrl*/search*/limit.search.results.in.dates.range/ui.search.stashResults*）、`ui/rest/resource/artifacts/search/` 四资源 + `addon/search/packages/rest/PackagesSearchResource`、`cron/CronUtils`（CRON_NEVER/5min 闸/getNextExecution）、`ui/rest/{resource,service,model}/utils/cron/`（crontime 端点/错误码族/Date.toString）、`rest/resource/replication/ReplicationConfigRequestHelper`（400 文案两臂）、`schedule/QuartzCommand`（随机错峰）、`config-templates/artifactory.config.xml`（出厂 cron 五表达式）。
- 官方实取（WebFetch/WebSearch 2026-09-03）：aql-entities-fields-reference（statistics 字段表逐条对齐）、reference/searchusage（notUsedSince/createdBefore 回退语义）、reference/searchcreation（now() 声明——与反编译冲突登记 V-j）、Quartz CronTrigger tutorial（域表/特殊字符/? 规则/大小写）、JFrog Regular Maintenance Operations（GC 4h 默认/Next Run Time 列）；QRL REST 官方 reference 无页（llms.txt 核对）→ §14.4 整节反编译单源标注。
- 环境探测（全部只读）：ping/nc 22·8181·8182、`curl ping`×2、SSH ×4（BatchMode）——结论见上「活体腿状态」。
- `make docs` 绿（docs-site build success，reverse/ 不入站但流程无回归）；docs/reverse/ 三文件 + README 落盘后 grep 复核结构（§14.1~§14.8 / §1~§8 / §1~§6 编号完整）。
- 零 git 写操作、BOARD 零写、reverse-src 零改动、t226 零接触。

## 解锁声明（AC3 tech-lead 就绪度）

- **T-440**（AQL statistics/usage）：aql.md §14.1/§14.2 字段集与 wire 已定案可动工；注意 §14.2 命名警示（notUsedSince 非 usageSince）与 §14.1 remote_* 防错条。
- **T-452**（QRL+UI 族+dates）：§14.3/§14.4/§14.5 锚面完整；V-j（to 缺省语义）票内定案留痕、V-k/V-l/V-m 活体恢复后补验不阻塞实现。
- **T-442/T-448/T-461**（远端浏览）：remote-browsing.md §1~§5 语义 + 矩阵直接可引；R-a~R-d 登记在案。
- **T-446/T-450/T-462**（cron 域）：cron-scheduling.md §1~§4 行为锚就绪；§6 子集建议待 ADR-0044（T-436）对齐——差异缝已留痕（〔待 T-436 对齐回填〕占位行），不构成硬阻塞（软协作先例 M15 T-407/T-408）。

## 遗留与归位

1. **t226 环境修复**：VPN/TUN 路由断致 SSH kex 失败——conductor/用户侧修复后，待验证清单三处（aql V-i~V-o / remote R-a~R-d / cron C-a~C-d）可一次只读会话补齐。
2. **T-436 对齐回填**：cron-scheduling.md §6 占位行待 ADR-0044 Accepted 后回写一行定论（子集边界/间隔闸范围/裸 /N 触发集 C-d）。
3. **PRD 措辞项**：FR-148.1/T-440 AC 的 `usageSince` 参数名与 Artifactory wire `notUsedSince` 不符——建议 PM 下版统一措辞（M16-SPLIT 歧义口径机制）。
4. `searchResults` UI 资源（第五资源，inv-2 未列）已登记外延项——T-452 不承载则缺位登记，无动作项。
