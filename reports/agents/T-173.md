# QA 报告 T-173 — S3 后端（MinIO）M1~M5 全部 P0 序列复跑 + 兼容性验证

- role: qa-engineer
- 日期: 2026-08-22
- 票据: T-173 [P0]（BOARD 只读引用；AC=H68/H69 口径 + H07~H21 专项）
- 被测物: **独立 git worktree @ 00f73e7（HEAD，与 T-172 基线同 commit）**，`make console` 后构建 `/tmp/t173/binflow-server`（23MB，CGO_ENABLED=0，minio-go v7.3.0 已入二进制）。不含主工作树在途 T-171/T-175/T-186/T-187 改动
- S3 服务商: **MinIO RELEASE.2025-04-22T22-12-26Z**（docker 容器，S3 API 127.0.0.1:19000 / console 19001，`use_path_style=true`、`region=us-east-1`、http 端点）
- 桶: binflow-qa（主序列）/ binflow-h10（H10）/ binflow-mig（迁移）/ binflow-cold（冷启动）
- 实例: A=S3 主实例 127.0.0.1:18080（data=/tmp/t173/data-s3，元数据 SQLite 在本地、blob 全在 MinIO）；H08/H09 负路径 18081（未起服）；H10 18082；迁移腿 18083；disk 对照 18084；冷启动 18085/18086
- 客户端: curl 8.7.1 / jq / docker CLI 29.7.2（**docker:27-dind 容器内 daemon**，`--insecure-registry host.docker.internal:18080`，T-44/T-172 手法）/ mvn 3.9.9 + Temurin JDK 21.0.12.1（本票重新置备 /tmp/t173/tools，x86_64）/ npm 10.9.8 / pip 26.1.2 + twine 7.0.0（venv）/ oras 1.2.2 / crane 0.20.3 / Playwright Chromium（worktree web/ 既有规格）
- mock 上游: python3 http.server 9099（M 系）+ 9098（M44b 专用）；真上游腿 maven-central / pypi.org 可达
- 口径: 直接复用 T-172 剧本与断言（C01~C30、D 系、M 系、W 系、G 系），差异仅存储后端为 S3；本票新增 S3 侧证据（mc 对象计数 / 对象键布局 / 元数据 / RSS）

## 总结论: **FAIL（有保留）**

- 五序列 P0 **行为面全部通过**（C 60/60、D 全可跑项、M 全可跑项、W REST 69 项 + W08/W37a、G 系）——S3 后端下协议与治理行为与 disk 等价
- **4 条实质缺陷**（1×P0 + 3×P1）+ 2 条 P2 观察，全部集中在 **S3 专属路径**（迁移触发、stats/export 的 disk 假设、上传内存缓冲、blob-created-at 元数据）：
  - **D-3（P0）**: REST 触发的迁移被请求 context 取消，`migration/start` 返回后后台迁移立即死亡（"list disk blobs: context canceled"）→ **H12/H14 无法达成**
  - **D-1（P1）**: `/api/v1/storage/stats` 在 S3 后端下恒 500 → H16 的 stats 断言载体不可用 + 浏览器 G32a-1 挂
  - **D-2（P1）**: `export` 在 S3 后端下必失败（dangling reference）→ H04/W28~W30 备份恢复面不可用
  - **D-4（P1）**: S3 会话 Append 全量内存缓冲 → 1GB 上传 RSS 增量 **~1.96GB**（<256MB 门）→ H06/NFR-P3 不达
- 零「无因」5xx：主实例 5xx 共 33 条 = 27 条 D-1 + 3 条故意 501（迁移未配置）+ 1 条故意 502（hardFail 仓）+ 2 条客户端断连（C30 中断上传、crane 匿名半途断开，与 disk 基线同类的 client-disconnect 5xx）；迁移腿另有 14 条 500 为**本票自伤**（mc 拷贝布局误写 blobs/blobs/ 造成 dangling 引用，修复布局后 120/120 全 200；dangling→500 与 disk 语义一致）
- Q8（AWS 真云验收）为条件腿：**未执行**（待用户定案；本地 MinIO 为主腿，如上）

---

## 1. AC 逐条结论

| # | 验收标准 | 结果 | 证据摘要 |
|---|---|---|---|
| 1 | H69/H01~H05 五序列 P0 复跑全绿（T-172 口径） | ✅（行为面）/❌（S3 专属面见 #6~#8） | C 60/60（§2.1）；D 序列全可跑项过（§2.2，D17 podman/D19 skopeo 客户端不可得、D08b buildx 形态未复跑=环境限制同 T-172）；M 序列全可跑项过（§2.3，M59 P2 未实现同基线）；W REST 69/69 + W08/W37a 过（§2.4）；G 系过（§2.5）。**例外**：W28~W32（备份恢复）因 D-2 失败、浏览器 3 挂（1 既有断言漂移 + 1 D-1 + 1 数据量超时） |
| 2 | H07~H11 S3 配置与健康检查 | ✅ | H07 起服+`/health` storage:ok+`/readyz` 200；H08 假桶 exit 1+明文报错+无监听；H09 InvalidAccessKeyId/SignatureDoesNotMatch 标准 S3 文案双证；H10 env-only 装配 S3 生效+roundtrip+桶可见；H11 http+path-style+us-east-1 上传下载 sha256 一致（§3.1） |
| 3 | H12~H15 本地→S3 迁移 | ❌（D-3 阻塞 H12/H14；H13 写路径 ✅；H15 ✅ 有保留） | §3.2：POST /start 后迁移即死（context canceled）；双写上传 201 且盘+桶双侧有 blob；纯 S3 模式删本地 blobs 后 120/120 制品 GET 200（blob 迁移为运维等价 mc 拷贝——因 D-3 自动迁移不可用，completed 装配语义为实证对象） |
| 4 | H16~H18 S3 下 GC 与去重 | ✅ / ❌（H18） | H16 去重：同内容双路径 S3 对象数不变（stats 载体 500=D-1，以 mc 计数为证）；H17 GC dry-run 28 候选→apply 28 删→孤儿对象从桶中消失、被引用幸存、路径 404；H18 ❌：`blob-created-at` 元数据未落到任何对象 + GC grace 实际用 S3 LastModified（§3.3，D-5） |
| 5 | H19~H21 S3 性能基线（对比 disk 同剧本） | ✅（H19/H20）/ ❌（H21 大文件 RSS） | H19：匿名 1000×200/0 失败/13s、认证 1000×200/0 失败/40s、docker pull×20 全过；H20 冷启动 0.63s、空载 footprint 138.2MB vs disk 同机 138.3MB（**后端持平**；两腿均高于 100MB 绝对线——本机本轮测量语境，见 §3.5 注）；H21：1MB×100 上 10.8s/下 6.6s、1GB 上 41.1s/下 6.1s（disk：15.6s/1.9s）、GET P50 220ms/P95 3044ms（disk 259/785ms）；**1GB 上传 RSS 增量 1.96GB（D-4）** |
| 6 | Q8 AWS 真云（条件腿） | ⛔ 未执行 | 待用户定案；本地 MinIO 为主腿。NFR-S38（https 强制）未涉（本地 http 腿） |
| 7 | QA 报告产出、零意外 5xx | ✅（有保留） | 本报告；5xx 全数归因（§4），意外 5xx 仅 D-1 一类（27 条，已开缺陷） |

## 2. 五序列明细（T-172 口径复跑）

### 2.1 M1 C 序列（curl；60/60）

C01~C30 全过：ping/401/建仓 200/非法 key 400/列仓/查仓/10MB 上传服务端 sha256 对账（**+1 S3 对象，键 `blobs/fa/<sha256>`**）/下载逐位/HEAD X-Checksum-Sha256/item info/C12 去重（**同内容二路径 S3 对象数 2 不变**；stats 载体 500 见 D-1）/C13 一致 201/C14 不一致 409/C15 秒传 201+未命中 404/mkdir 201+children=3/子目录 deep list/根级 list 400（T-18 定案）/删除 204-404-404/删仓非空 400+deleteContent 200/改密双路由+旧口令即失效+错旧口令 400/Token 发放（Bearer 64 字符）-充当口令-吊销-幂等-互斥 400/用户+ACL（403→授权 201→无 delete 403→admin 204）/匿名读开+匿名写拒/`/v2/` 401+Bearer realm+api-version+认证 200 `{}`+匿名 pull token+双入口 token+offline_token+POST 表单/`/artifactory` 404/npm api 404/无根镜像 404/拒绝解包 400/remote+docker 400/健康 ok+版本/中断上传（--max-time 3 退出 28、路径 404、S3 无可见残留对象）/NFR-S1 db 无明文口令/NFR-S5 无 tmp 残留/**C27 匿名关（401+`WWW-Authenticate: Basic realm="BinFlow Realm"`+admin 200+ci-bot 有读 200/无读 403）**。

### 2.2 M2 D 序列（curl + docker(dind) + oras + crane）

- D01 建仓 200×2；D04 探针族全过（匿名 401+挑战头、认证 200 `{}`、匿名 pull token 换发、手工 token、ci-bot `/v2/token` 换发 vs 管理面 403、offline_token、POST 表单凭据）
- D06 monolithic：Location 相对路径、PUT?digest 201、单段 name 404、逐位一致、Docker-Content-Digest 回显；D07 单请求 201
- D08（dind 真客户端）：login Succeeded/错误口令被拒/tag→push（digest sha256:6c2a97…）→rmi→**pull（删缓存重拉）**→`docker run echo ok`
- D09：catalog 含全名、tags `["v1"]`、`?n=1` Link rel=next（2 tags 时）、last 游标互斥（rest=null）、n=abc 400、未登记 name 404
- D10 chunked 10 段 201+cmp 一致；D10b offset `Range: 0-1048575`
- D11 去重继承：**同镜像再 push S3 对象数 6 不变**
- D12 伪 digest 400 DIGEST_INVALID；D13 跨 name mount 201+GET 200；D13b 引用缺失 MANIFEST_BLOB_UNKNOWN；D13c by-digest DELETE 202→404、**tag 级联清（tags=null）**、by-tag 405 UNSUPPORTED；D13d blob DELETE 405
- D14 kill -9：chunked 进行中 kill -9 →重启→ healthz 200、历史 blob 200、未终结会话 PUT 404、桶内无可见残留对象（**但遗留 incomplete MPU 6 条，见 D-6**）
- D15 Helm OCI（oras 1.2.2 --plain-http）：push Digest 回显、pull 逐位一致、repo tags 含 1.0.0；D18 crane：digest/manifest（schema2 mediaType）/实例内 copy（catalog 可见 copy/app）
- D22 ci-bot 仅 read：push denied（exit 1）、（授权 docker-local read 后）pull 正常；D23 token 作 docker 口令 pull ok→吊销后拒
- NFR-S11 `--path-as-is` 下 `/v2/../../etc/passwd/blobs/uploads/` 400、数据目录外无文件
- ⛔ D17 podman / D19 skopeo（P1）：客户端不可得（同 T-172 环境限制）；D08b buildx 多架构（P1）：未复跑（同 T-172 DNS/buildkit 约束；协议层构造腿已在同 HEAD 由 T-172 验过，blob/manifest 数据面本轮已被全部 docker 腿覆盖）

### 2.3 M3 M 序列（mvn 3.9.9 / npm 10.9.8 / pip+twine 真客户端）

- M01~M05 仓库模型全过（三协议 local、remote 缺 url/file:// 400、virtual 未知成员 400、type/packageType 过滤、remote+docker 400）
- mvn：M11 deploy DEPLOY_OK；M12 jar 逐位+pom sha1 40hex+旁车裸 hex 40；M13 全新 repo resolve；M14 版本组 XML+旁车 SHA1_MATCH；M15 合并 versions=2/latest=1.1.0；M16 SNAPSHOT×2（timestamped 直连 200——文件名从 `<value>` 拼，T-172 E-3 口径、snapshotVersions=2、buildNumber=2、`-U dependency:get` SNAP_OK）；M16c/M17 宽容策略两态；M18 正确旁车 201+回读一致/deadbeef 409；M19 快照关 409；M20 layout 两例 400；M21 Range 206+ETag 304
- npm：M22 publish `+ demo-pkg@1.0.0`/view/whoami=admin；M22b dist-tags.latest/tarball URL（含 /binflow/ 前缀路径，E-4 口径）/packument shasum==tarball sha1；M23 install+清缓存重装；M24 dist-tag add/ls/install@beta/rm；M25 scoped `@acme/util` publish→install；M26 同版本重复 403；M27 -rev 占位恒成功+unpublish 后无 1.0.1；M28 匿名读 200/匿名 PUT 401
- pypi：M30 wheel+twine upload；M31 simple index `#sha256=`/PEP629 api-version=2/三形态归一 200/无尾斜杠 302/ETag 304/`:action=submit` 400；M32 pip install 0.1.0+落盘 sha256==index fragment；M33 重复 twine 400；M34 依赖链 demo-pkg==0.2.0 自动装齐 demo-lib；M35b PEP691 JSON files≥1
- remote：M41 pull-through FETCH_OK/CACHE_HIT/上游 GET 计数稳定（锚定 `GET /dir/up.bin HTTP` 计数，E-5 口径）；M42 SSRF 400+WARN；M43 负缓存两连发上游仅 1 GET；M44 短 TTL 仓 stale 200+`X-Binflow-Upstream-Error` 头/未缓存默认 404/hardFail 502/health 恒 ok；M45 **真 maven-central** dependency:get junit:4.13.2 PROXY_MAVEN_OK+缓存 200+`.sha1` 不回源 404；M46 npm mock 腿 install mockpkg（布局修正后）；M47 **真 pypi.org** pip install six；M48 remote PUT 405+Allow: GET+删缓存 204→新仓回源 200（避 300s 静默期，T-172 口径）
- virtual：M50 local 成员 resolve+remote 成员透传（junit 200）；M51 **成员顺序交换实证**（remote-first→mock 字节 B）；M52 virtual PUT 405+Allow: GET
- M57 npm ping `{}`；M58 四端点 404；M60b 50 并发 pip download 0 失败；NFR-S18 `../`/`%2e%2e`/`@scope%2f..` 全 400（path-as-is）
- 注：M59（P2 remote/stats）同 T-172 O-5 未实现，不在本轮口径

### 2.4 M4 W 序列（curl REST 69 项 + Playwright）

- W01/W01b/W02：`/binflow/` 301→ui/、SPA 挂载点、深链 200、保留字 ui 400、资产 immutable/shell no-cache
- W03~W07：未认证 401、登录 `{username:admin,admin:true}`、Set-Cookie `Path=/binflow; HttpOnly; SameSite=Lax`、错口令 401、whoami、logout 204→401
- **W08 TTL=5s：t0=200 → 6s 后 401**；**W37a：优雅重启后 session 存活 200**
- W12a includes/excludes 双 409+excluded GET 404；W14 搜索命中/缺 name 400；W15 checksum 搜索 ≥2；W16 repo 过滤空集+ci-bot 仅见授权面（其 ACL 下 0 条，符合授权面——T-172 同判）
- W17~W20：组 CRUD/成员 email/未知组 400/组授权继承（写 201/删 403/越 pattern 403）/移组即时 403/被引用组删 409→解除后 200
- W22 系：actor/action 过滤+limit>1000 400+非 admin 403+匿名 401+分页 2+cursor；W23 词表含 deploy/delete 等+**审计零明文口令**；W39 audit 无修改面 404×2
- W24/W25（=H17）：dry-run candidates=28 且 S3 对象数不变→apply deleted=28→**孤儿 blob 对象从 MinIO 删除、被引用幸存 200、删除路径 404**
- W26/W27：800B 配额 1024 首传 201/二传 413（used/quota）/被拒原子 404/usage=800/读删不受限/删后回落再传 201
- W38 同源写 201/跨 Origin 写 403；W36 四边界 404
- **W28~W32 ❌（D-2）**：`binflow-server export` 在 S3 后端下 exit 1——`snapshot references blob … the filestore under /tmp/t173/data-s3 does not carry it (dangling reference)`；import 面随之不可测（无产物可导）
- 浏览器面（Playwright Chromium，既有规格）：login/console-smoke/repositories/artifacts **17 过 1 挂**（挂项 repositories.spec.ts:52 = T-172 D-3 既有断言漂移，行为正确）；governance/security/auth-shell **12 过**；t134-g32 **G32a-1 挂（根因 D-1：UI 探测 /api/v1/storage/stats 得 500×3）+ 5 项级联未跑**；governance 审计分页用例 1 挂（30s 超时于 audit-more 翻页——本实例审计量 >900 条的数据量敏感超时，非 S3 行为；REST 同面 W22 已过）

### 2.5 M5 G 序列（票据口径：GC/治理/本地腿 + 性能）

G19 docs 匿名 200；G27（=H19）1000 并发拉取零错误（匿名 1001×200/13s、认证 1001×200/40s、docker pull×20）；G28（=H20）冷启动 0.63s；G30a 深层上传/隐式父级/删深叶剪空链；G30d 未复跑（export 不可用=D-2）；G31 token.issue/revoke 审计在案+token 明文零命中+docker login 不发管理 token；G32 t134 规格见 §2.4；G33 uri 形态（repo key 恰一次、/binflow 前缀）；NFR-P3（=H21 大文件 RSS）**❌ 见 D-4**；G01~G17 发布产物面属 release-engineer 域未复跑（同 T-172 理由）

## 3. S3 专项明细

### 3.1 H07~H11 配置与健康

| 项 | 结果 | 证据 |
|---|---|---|
| H07 | ✅ | `s3 storage backend active`（bucket/endpoint/prefix 日志）；`/healthz` 200、`/readyz` 200、`/binflow/api/v1/health` `{"status":"ok","storage":{"status":"ok"}}` |
| H08 | ✅（勘误级注） | 假桶：exit 1，`opening s3 engine: bucket no-such-bucket-t173 does not exist at http://… (create the bucket…)`，18081 无监听。**注**：日志不含字面 `NoSuchBucket`/`403` 码（BucketExists 探测将 404 归并为人类可读文案）——语义等价，建议 PRD 勘误或补码字 |
| H09 | ✅（同上注） | 错 AccessKey：`The Access Key Id you provided does not exist in our records.`（=InvalidAccessKeyId 标准 S3 文案）；错 Secret：`The request signature we calculated does not match…`（=SignatureDoesNotMatch）；均 exit 1 |
| H10 | ✅（实现名） | env-only 装配（`BINFLOW_STORAGE__BACKEND/…__S3__BUCKET/REGION/ENDPOINT/ACCESS_KEY_ID/USE_PATH_STYLE` + `BINFLOW_STORAGE_S3_SECRET_ACCESS_KEY`）起服 S3 生效、roundtrip 201/200、blob 落桶、本地无 blobs/。**PRD 勘误**：PRD §4.1 的 `BINFLOW_STORAGE_BACKEND`/`BINFLOW_S3_BUCKET` 单下划线拼写被严格解码器拒绝（`unknown BINFLOW_ environment variable(s)… keys map as BINFLOW_<SECTION>__<KEY>`）——实现用全库统一的双下划线约定（T-170 已按实现落笔），二选一需 product-manager 定 |
| H11 | ✅ | `endpoint=http://127.0.0.1:19000` + `use_path_style=true` + `region=us-east-1`：1MiB 上传下载 sha256 一致、对象键 `blobs/72/<sha256>` |

### 3.2 H12~H15 迁移

前置：disk 起服灌 120 个 blob（layout `blobs/<xx>/<sha256>` 落盘验证）。

| 项 | 结果 | 证据 |
|---|---|---|
| H12 | ❌（D-3） | 切 `backend=s3 + migration.enabled=true` 起服：`storage migration dual-write mode` 日志、读路径回退正常（disk blob GET 200）；`POST /api/v1/storage/migration/start` → 202 后迁移**立即死于** `"error":"list disk blobs: storage: gc: context canceled"`（`total=0 migrated=0`，S3 侧 0 对象）。根因 `internal/httpapi/migration.go:48` 把 `r.Context()` 传给 StartMigration——ServeHTTP 返回即取消（Go 契约），任何真实客户端触发皆死 |
| H13 | ✅（写路径）/⚠（完成面受阻） | dual-write 下上传 new1.bin → 201 → **disk `blobs/xx/<sha>` 与 MinIO 对象双侧均有**，读回一致。「迁移完成后 done=true+新制品 S3 可达」后半被 D-3 阻塞 |
| H14 | ⛔ BLOCKED | 迁移无法持续运行，断点续传不可端到端验证（T-164 引擎层单测绿；REST 集成路径死） |
| H15 | ✅（有保留） | blob 以**运维等价手段**（mc 按分片拷贝，非产品迁移——D-3 所致）入桶后切纯 S3（`migration.enabled=false`）：`s3 is the source of truth` 日志；**删除本地 blobs 目录后 120/120 制品 GET 200**；纯 S3 下迁移端点 501（T-164 契约）。保留声明：completed/pure-S3 装配与 S3-only 读语义为实证对象；blob 拷贝过程本身未走产品代码 |

附带：`migration.enabled=true` + `backend=disk` 拒起（T-178 语义，测试钉死）。

### 3.3 H16~H18 去重与 GC

| 项 | 结果 | 证据 |
|---|---|---|
| H16 | ✅（载体替换） | 同内容两条路径 PUT 后 **MinIO 对象计数不变**（跨仓 mount 亦然——D13 同 blob 单对象）。`/api/v1/storage/stats` 在 S3 下恒 500（**D-1**），stats 载体不可用 |
| H17 | ✅ | dry-run candidateCount=28（对象数不变）→ `apply:true,graceHours:0` deleted=28 → 孤儿 blob 的 S3 对象**实际消失**（mc stat 404）、被引用 keep.jar 幸存、已删路径 GET 404 |
| H18 | ❌（D-5） | ① `blob-created-at` 元数据**未出现在任何对象上**（minio-go StatObject 直读：21B 小对象与 1MiB 对象均只有 Content-Type）——根因：Commit 第 3 步 CopyObject 设了 `UserMetadata` 但未设 `ReplaceMetadata:true`，minio-go 明文注释「UserMetadata is only set to destination if ReplaceMetadata is true」（api-compose-object.go:56），COPY 语义静默丢弃；migration copyBlob 同路径（backend.Put 直传路径代码里有设、但运行时上传走 session 路径）。② GC grace 实际用 `obj.LastModified`（s3.go:294），从不读该元数据。影响方向：GC 比规格**更保守**（迁移 blob 的宽限期从迁移时刻起算），无数据丢失，P2 定级但 AC 判 ❌ |

### 3.4 H19~H21 性能（同机同剧本 disk 对照）

| 指标 | S3(MinIO 本机) | disk（同剧本） | 备注 |
|---|---|---|---|
| 1000 并发拉取（匿名，P50×20 波） | 1000×200 / 0 失败 / 13s | 1000×200 / 0 失败 / 13s | H19 ✅；认证腿 1000×200/0 失败/40s；docker pull×20 全过 |
| 冷启动（空库空桶） | 0.63s | 0.36s | H20 ✅（<2s，含 BucketExists 探测） |
| 空载 footprint（60s） | vmmap 138.2MB（ps 145MB） | vmmap 138.3MB（ps 145MB） | **后端持平**。注：两腿均高于 100MB 绝对线；T-172 在同 commit 记 disk 为 73.3MB——本轮两腿同机同时测量偏高（多实例共存/页账目语境），以后端差值为准：S3 无额外空载开销 |
| 1MB×100 上传 | 10.8s | 28.8s（本轮异常慢，机器负载） | 顺序 curl，非并发 |
| 1MB×100 下载 | 6.6s | 7.3s | 持平 |
| 1GB 上传 | **41.1s** | 15.6s | S3 2.6×慢 |
| 1GB 下载 | 6.1s | 1.9s | S3 3.2×慢（本机 MinIO+multipart GET） |
| 1GB 上传 RSS 增量 | **1,962,416 KB（~1.9GB）** | 4 KB | **H06/NFR-P3 ❌（D-4）**；下载侧未见 comparable 增长 |
| 1MB GET 服务端延迟 | P50 220.5ms / P95 3044ms / mean 866ms（n=2100） | P50 259ms / P95 785ms / mean 412ms（n=1100） | P50 持平略优，P95 尾部 3.9×（本机 MinIO 多进程争用），如实记录 |

### 3.5 Q8 条件腿

AWS S3 真云验收未执行（BOARD 标注待用户定案）。本地 MinIO 主腿结论如上；对真云的外推风险点：virtual-hosted style（本轮只测 path-style）、https/TLS 握手（本轮 http）、真云延迟剖面。NFR-S38 未涉。

## 4. 5xx 终扫

```
主实例 s3-a.log:  total 5xx = 33
  27  500 GET /binflow/api/v1/storage/stats   —— 缺陷 D-1（C12/W24 探测 + 控制台 UI 探测）
   3  501 GET /binflow/api/v1/storage/migration —— 故意（纯 S3 未配置迁移，T-164 契约）
   1  502 generic-remote-hardfail              —— 故意（M44 hardFail:true）
   1  500 PUT  acme/big.bin                    —— C30 客户端 --max-time 3 自杀（client-disconnect 类，与 disk 基线同类）
   1  500 POST /v2/…/blobs/uploads/            —— crane 匿名半途断开（同上类；错误行实证 context canceled）
迁移腿 mig-pure.log: 14×500 —— 本票自伤（mc 拷贝布局误写 blobs/blobs/ 造成 dangling 引用；修复后 120/120=200；
                     dangling 元数据→存储缺失返回 500 与 disk 语义一致）+ 1×501 故意
mig-dual/s3-cold/disk-perf: 0 条 5xx
```

## 5. 自动化基线

| 命令 | 对象 | 结果 |
|---|---|---|
| `go test -count=1 ./internal/storage/ ./internal/config/`（worktree@HEAD） | storage 223.7s ok / config 14.9s ok | ✅（S3/迁移引擎单测全绿——D-3 属 cmd/httpapi 装配缝，单测未覆盖真实请求生命周期） |
| `go test -race -count=1 -run "TestOpenStackS3\|TestSecureFromEndpoint\|TestReadiness" ./cmd/binflow-server/ ./internal/httpapi/` | 17.6s ok / 20.2s ok | ✅ |
| 全量 `make test` @00f73e7 | 见 **T-172 §5**（同 commit 同 worktree 基线：20/21 包 ok + 1 已知负载敏感 flaky） | 引用不重跑 |

## 6. 缺陷清单（不修码，开票建议）

| # | 级别 | 摘要 | 复现 / 期望 vs 实际 |
|---|---|---|---|
| **D-3** | **P0** | **REST 触发的迁移被请求 context 杀死**：`internal/httpapi/migration.go:48` `StartMigration(r.Context())`——handler 返回即取消（net/http 契约），后台迁移在盘存阶段即死 | MinIO + `backend=s3,migration.enabled=true` 起服（disk 侧有 blob）→ `curl -X POST …/api/v1/storage/migration/start` → `GET …/migration` 得 `"error":"list disk blobs: storage: gc: context canceled"`，`total=0 migrated=0`，桶空。期望：迁移脱离请求生命周期（`context.Background()`/detached ctx + 优雅停机取消），H12~H14 可达成。**阻塞 H12/H14，H15 只能运维等价验证** |
| **D-1** | **P1** | **`GET /api/v1/storage/stats` 在 S3 后端下恒 500**：`internal/httpapi/system.go:293` 无条件 `dirSize(data_dir/blobs)` | S3 实例上 `curl -u admin … /binflow/api/v1/storage/stats` → 500 `size of blobs dir: lstat …/blobs: no such file or directory`。期望：S3 下按 ListObjectsV2 聚合（或显式 N/A）。连带：控制台 UI/G32a-1 浏览器规格 3×500 挂；GC 响应 sizing 走盘路径打 WARN（`httpapi: gc candidate sizing incomplete`，候选数本身正确）；`/metrics` 存储容量同族盲区 |
| **D-2** | **P1** | **`export` 在 S3 后端下必失败**：`cmd/binflow-server/backup.go:178` `CopyBlobsTree(dataDir,…)` 只认盘 | S3 实例（有制品）`binflow-server export -c s3.yaml -output bk` → exit 1 `snapshot references blob … the filestore under … does not carry it (dangling reference)`。期望（PRD H04）：S3 下 export 只导元数据（blob 已在 S3），import 后 blob 经 S3 可达。**W28~W32/H04 整面不可用** |
| **D-4** | **P1** | **S3 会话 Append 全量内存缓冲**：`internal/storage/s3.go` s3Session.Append 以 `bytes.Buffer` 缓冲整个 reader（注释自认「Buffer the part…」），单体 PUT 全 body 一次 Append → 全文件驻留内存（≈2× 涨幅） | S3 实例 `curl -T gig.bin`（1GB）→ 进程 RSS 221MB→2,183MB（**增量 ~1.96GB**，门 <256MB）；disk 同剧本增量 4KB。期望：按 `upload_part_size`（默认 5MiB）分片流式。**H06/FR-48-AC6/NFR-P3 FAIL；并发大文件上传为 OOM 向量** |
| **D-5** | P2（AC=P1） | **`blob-created-at` 元数据双失**：① Commit 的 CopyObject 设 UserMetadata 但缺 `ReplaceMetadata:true` → minio-go 静默丢弃（api-compose-object.go:56 明文注释），实测所有对象（含 migration copyBlob 路径）无该元数据；② GC grace 用 `obj.LastModified`（s3.go:294）从不读该元数据 | `mc stat --json`/minio-go StatObject 直读桶对象：metadata 仅 Content-Type。期望（H18/ADR-0019/K2）：上传携带该元数据且 GC grace 基于之（无则 fallback LastModified）。影响：GC 更保守（无数据丢失方向） |
| **D-6** | P2（观察） | **孤儿 multipart upload 不回收**：kill -9/半途放弃的会话在 MinIO 留 incomplete MPU（`mc ls --incomplete` 见 `sessions/<uuid>/data` ×6），无启动清扫（disk 有 startup session sweep 对应物） | D14 kill -9 后 `mc ls --incomplete --recursive local/binflow-qa/` 计数增长。期望：启动时 ListMultipartUploads+Abort 清扫或生命周期策略文档化 |
| （观察 O-1） | P2（勘误） | PRD §4.1 环境变量名（`BINFLOW_STORAGE_BACKEND`/`BINFLOW_S3_BUCKET` 单下划线族）与实现（全库 `BINFLOW_<SECTION>__<KEY>` 双下划线）不一致；PRD 拼写会被严格解码器 fail-fast | 见 §3.1 H10。建议 PRD 回写实现名（或别名支持立项） |
| （观察 O-2） | P2（勘误） | H08/H09 要求日志含 S3 错误码字面量（NoSuchBucket/InvalidAccessKeyId）；实现输出人类可读等价文案（标准 S3 message 文本），无字面码 | 见 §3.1。建议 PRD 勘误为「含 S3 错误的确定性诊断文案或错误码」 |

**沿用基线的已知问题（非本票新证，同 HEAD）**：T-172 D-1（Argon2 认证风暴）、D-2（TestIdleSessionEviction flaky）、D-3/D-4（e2e 断言漂移/501 放行）。

## 7. 未复跑项及理由

| 项 | 理由 |
|---|---|
| D17 podman / D19 skopeo（P1） | 客户端本机不可得（同 T-172）；docker/crane/oras 已覆盖同协议面 |
| D08b buildx 多架构（P1） | 本机 crane→docker.io DNS 污染 + buildkit plain-HTTP 需 builder 配置（T-105/T-172 已知）；协议层构造腿同 HEAD 已由 T-172 验证，本轮 blob/manifest 数据面全过 |
| W30b remote 凭据随行、M35 wheel+sdist、M53~M55（P1） | P1 面 + W28~W32 本轮被 D-2 阻塞（export 本体不可用），另行开票后复验 |
| G01~G18、G22~G26、G29b | 发布产物/安全审计面：release-engineer/security-auditor 域（同 T-172 理由） |
| AWS S3 真云（Q8） | 条件腿待用户定案（BOARD 标注） |

## 8. 环境清理记录

- 容器：t173-minio / t173-dind 已停并 rm -f；dind 内 admin/ci-bot 凭据已 logout；minio、mc、docker:27-dind 镜像已 rmi
- 进程：18080/18081/18082/18083/18084/18085/18086/9099/9098 全部停止，无监听残留；未触碰他人实例（arca 容器、bf-t175 两实例、kind 集群、buildx builder）
- 工作区：`git worktree remove --force /tmp/t173-work`；/tmp/t173 数据/venv/工具/工作文件已删；**/tmp/t173/logs 保留**（本报告证据日志，约 6MB，随 /tmp 生命周期）
- 主仓工作树零改动（本票只读 + 本报告落盘）；测试凭据（t173qa/mig-pw/n3w!pw2 等）仅存于已删临时文件与保留日志，不进任何提交文件
