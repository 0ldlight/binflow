# T-517 工作日志 — HA/联邦证据包（reverse-engineer）

- **ticket_id**: T-517（Phase A / 计划票 T7「HA/联邦证据包」）
- **role**: reverse-engineer（本票实例域：HA 集群 + Federated 仓库）
- **date**: 2026-09-24
- **area**: docs/reverse/enterprise/ha-cluster.md、docs/reverse/enterprise/federation.md（enterprise/ 目录新增两文件；与既有 feature-catalog / feature-gates / license-behavior / runtime-behavior 无条目冲突，replication.md §7 概念短注已显式互链不重复）
- **scope_files**:
  - 新建 `docs/reverse/enterprise/ha-cluster.md`（9 节：总模型/节点与加入/通信/配置同步/binarystore/故障面/常量表/官方差异/UNKNOWN）
  - 新建 `docs/reverse/enterprise/federation.md`（8 节：总模型/成员模型/同步机制/边界/删除重命名/常量表/官方差异/UNKNOWN）
- **commands_run**（反编译根 = /Users/lzw/workspace/artifactory-decompiled，全程只读；关键命中摘录）:
  - `ls backend` ×3（569 模块定位：artifactory-addon-ha / artifactory-addon-federated / artifactory-addon-mirror / artifactory-addon-grid / artifactory-federation-dto / rtfs-service / platformfederation-client-* / topology-service）
  - `find backend/artifactory-addon-ha -type f`（63 文件清单）；`find backend/artifactory-addon-federated -type f`（120+ 文件清单）
  - `grep -rl "ha-node.properties" backend` → 唯一命中 artifactory-config/.../ArtifactoryHome.java:130
  - `grep -rln "heartbeat" artifactory-core artifactory-common artifactory-config` → 7 命中（ArtifactoryHeartbeatServiceImpl / ServerModel / ServersServiceImpl / ConstantValues / JobName 等）
  - `grep -n "ha[A-Z]" artifactory-config/org/artifactory/common/ConstantValues.java` → 25 行 HA 常量（5s/30s/180min 等默认值）
  - `grep -n "hasHeartbeat|isRunning|isOther|qualifiesForDeletion" artifactory-common/.../ArtifactoryServersCommonService.java` → 8 谓词定义（25-32 行）
  - `grep -n "membershipPort" -r backend` → 7 文件（确认 membership.port 仅持久化：ArtifactoryStateManagerUtil 写 0）
  - 全文 Read：HaNodeProperties / HaRoleManagerServiceImpl / PrimaryBasedHaRoleManagerServiceImpl / HaRoleManagerServiceFactory / HaPropagationServiceImpl / HaRestAuthenticationFilter / ClusterOperationsServiceImpl / HaAdminResource / ArtifactoryHeartbeatServiceImpl / ServerModel / ServersServiceImpl / ArtifactoryServer / RemoveServerService / GracefulShutdownServiceImpl(节选) / HaInitLockFactory(节选) / PropagationResource / FederatedMemberOrchestrator / FederatedEventsServiceImpl / MirrorResource(节选) / MirrorImpl(节选) / GridClient(节选) / GridFederatedRepoServiceImpl / UnidirectionalFullReplicationVisitor / FederatedStatisticsServiceImpl / FederationHealthCheckJob
  - `grep -n "@Path|@POST|public Response" RestHaResource.java / MirrorResource.java / HighAvailabilityResource.java / FederatedMonitorUIResource.java` → 端点表来源
  - `grep -rn "clusterTopologyChanged" backend` → 4 命中（确认监听器链、无 binary provider 实现消费点在源内）
  - `grep -rln "BinaryProvider" backend` → 仅 4 个 UI/retention 间接引用 → **U-1 结论依据：binarystore 提供者链不在反编译源**
  - `grep -n "deniedEventTypes|allowedEventTypes" FederatedRepoUtils.java` → 130-131 行防环 hint 白黑名单
  - `grep -n "shouldActOnEvent" -r backend` → 4 命中定位 FederatedRepoMirrorCallBack.java:1899
  - `grep -n "UPDATE |INSERT INTO" ArtifactoryServersDao.java / EventsDao.java` → 表名 artifactory_servers / node_events / node_event_cursor / node_events_tmp
  - `find . -name pom.properties -exec grep -h "^version=" {} \; | sort | uniq -c` → 82×7.161.14（Artifactory 自有模块）；版本口径核验（见 decisions_made #1）
  - `grep -n "federation|federated" RestHaResource.java`（零命中）+ `sed -n '564,640p' HaAddonImpl.java` → 修正 HA 联邦传播端点为 /api/federation/**（queue/deregister、queue/reset、migration、metadata/invalidate）
- **key_outputs**:
  - ha-cluster.md：端点/操作面 20+ 行（§3.1 同步面表、§5.5 REST 表）；语义流程 5 组（加入时序、心跳循环、传播前置校验、优雅停机、角色选择）；布局/数据结构 3 张表（artifactory_servers 列、ha-node.properties 键面、常量表）；UNKNOWN 7 条（U-1 filestore 链为最大缺口）
  - federation.md：端点表 5+4 行（/api/mirror/* 五端点 + /api/federation/* HA 传播）；语义流程 6 组（校验链、事件防环、队列状态机、全同步仲裁、删除防护、URL 替换）；数据结构 3 张（队列状态映射、同步/不同步清单、常量表）；UNKNOWN 8 条（F-2 RTFS gRPC 线协议未解）
- **test_evidence**: 无——本票纯规格（参照实例 :8082 为单机 Pro，无 HA/联邦面可活体探测；已在两文件 UNKNOWN 表中给出活体验证方案供后续动态验证票取用）
- **compat_evidence**:
  - 双源（高置信）条目主要落在：心跳 5s/30s/180min 阈值、HA license 类型集、Task Affinity vs primary、版本闸门、集群 license 文件名、镜像模式枚举与默认 bidirectional、事件防环 hint 规则、节点间认证 401 行为——反编译 + 官方 HA/Federated 文档表述互证
  - 单源（中/低）条目：传播 HTTP 参数、重试矩阵、队列状态机细节、单向全同步仲裁（后者虽单源但 visitor 全文可见故标高/中已在文中注明）
  - 版本口径：**两文件头部已注明派发口径 7.161.24（部分 7.161.20）与实测 pom 戳 7.161.14 的出入**，行为结论按 7.161.x 系列理解；与 docs/reverse/replication.md（7.161.16 基线）无冲突条目
- **open_questions**（对 ha-engineer 最要紧的三件）:
  1. binarystore HA 提供者链（cluster-default/eventual-consistency、NFS/S3 一致性）完全不可证（U-1）——BinFlow 侧需设计决策而非逆向填充
  2. RTFS gRPC 联邦服务线协议不可证（F-2）——receiver 模式实现依赖它，建议先按 UNKNOWN 挂起
  3. 脑裂：源码无 quorum（§5.4 中置信度否定结论），BinFlow 若做 HA 需自行决定仲裁策略并记录为有意分歧
- **decisions_made**:
  1. 版本标注采用「派发口径 + 实测 pom 戳」双写（不冒认 7.161.24/7.161.20；证据是 82 个模块 pom 均 7.161.14）
  2. 修正初稿错误：HA 内联邦队列传播端点实为 /api/federation/queue/*（非 /api/ha/federation/**），已按 HaAddonImpl:596-622 实锚改正并联动修正 ha-cluster.md 交叉引用
  3. 「未见端点/未见仲裁」类否定性结论一律标中（而非低），理由是否定证据来自定向 grep + 模块通读，但反编译源本身有已知空洞（U-1）
  4. Grid（federation V2）与 RTFS 作为独立小节收录但整体置信度压低（服务端/topology-service 侧不在源内）
- **deferred**:
  - BidirectionalFullReplicationVisitor 逐条仲裁规则（F-1，活体双成员并发场景更划算）
  - MirrorResponseStatus 全枚举与 config version 协商阈值（F-4）
  - ConfigMismatchResolver 策略（F-5）、federated remote/virtual 仓开闸行为（F-6）
  - haMembersIntroduction.* 现役性（U-3，疑似 Hazelcast 遗留）
- **risks**:
  1. 反编译源缺失 binarystore 提供者链与 rtfs-service 主体——相关条目只有间接证据，若后续补齐反编译需回填 §4/§2.5
  2. 版本戳 7.161.14 vs 派发口径 7.161.24：若 conductor 确认另有 7.161.24 源目录，本包结论需按 diff 复核（HA/联邦面 7.161.14→24 间小版本变动风险低但非零）
  3. 否定性结论（无脑裂仲裁、membership.port 无监听）存在「空洞导致误判不存在」风险，已在文中显式标注证据边界
  4. federation.md §2.2 端点表「挂 /api/mirror」的挂载前缀为推断（@Path("mirror") + Artifactory JAX-RS 惯例），发送端 URL 字符串 `{remoteUrl}/api/mirror/statuses/message` 直接可见可证 statuses 一条，其余四条同构推断——已在表中按端点分别标高/中
- **handoff_to**: ha-engineer（域 U，docs/design/ha-federation.md）——两份规格即其唯一行为输入；建议下一步（按依赖序）：conductor 派动态验证票覆盖 U-1/U-4/F-1（活体）；compatibility-engineer 将 §3.1/§5.5/§2.2 三张端点表契约化
- **status**: done
