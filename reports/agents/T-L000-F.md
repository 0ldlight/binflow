Ticket:       L000-F · 修复 docker remote 上游 wire path 缺 /v2/ 前缀（P0 BUG，LOOP 000 第一张代码修复票）· P0
Role:         dev-registry-adapter（领域实例：dev-package-docker）
Area:         internal/adapter/docker
Input:        reports/compatibility/L000-docker-remote-diff.md（DIVERGENT#1 P0；C04/C09 灭因；重放协议引证据报告 §10）；reports/compatibility/L000-docker-remote-evidence.md（E4：X-Artifactory-Origin-Remote-Path 直证 `<root>/v2/<name>/manifests/<ref>`；E0-3 dind 方法学）；docs/reverse/docker-registry.md §路由（官方 DIST-API 根=/v2/）；docs/compatibility/protocol/ 为空、contracts 无 docker 契约 → 按票面规则以官方规范 + docs/reverse 为准；docs/design/architecture.md 适配器 SPI（走读既有 remote.go/remoteSession 链）
Changes:      ① remote.go v2WireManifestPath/v2WireBlobPath 补 `/v2/` 前缀（上游 URL 语义定为 registry 根，注释引 L000-F 与 E4 证据）② 新测试 remote_wire_path_test.go（table-driven：扁平/嵌套 image × tag/digest ref × manifest/blob）③ 既有测试对齐新语义：remote_session_test.go 两处 fixture URL 去 `/v2` 后缀、删除旧 TestV2WirePaths；remote_pull_test.go 自引用拓扑改为「URL=registry 根 + 镜像名带上游 repoKey 命名空间（up-local/myapp，即 docker.io library/hello-world 同构）」——原 fixture 的 `URL+"/v2/up-local"` 正是缺失前缀的补偿形态 ④ P3 service 回显：经核查扩散（handler.go:103 + token.go:284 两处挑战构造 + catalog_test/token_gate_test/token_test 多处字面断言）不满足票面「一行且不扩散」→ 不动，留 LOOP 001 ⑤ Review A B1 返工：同一 seam 的 helmoci 包 fixture 迁移（review 指出 6 例红，上游日志实收 `/v2/helmoci-local/v2/mychart/...` 双前缀）——helmoci/remote_test.go 与 virtual_test.go 的 seed URL 改 registry 根、image 名带 `helmoci-local/` 命名空间、dead 上游 seed 去 `/v2/nothing` 后缀（test-only，不碰实现码）
Files:        修改 internal/adapter/docker/remote.go（2 函数 + 注释）、remote_session_test.go、remote_pull_test.go；internal/adapter/helmoci/remote_test.go、virtual_test.go（均 test-only 迁移）；新增 internal/adapter/docker/remote_wire_path_test.go；追加 reports/compatibility/L000-docker-remote-diff.md「复验」段（仅追加）；本日志。部署面联动：deploy/compose/.env.uat 的 BINFLOW_VER → uat-l000f-59f33ab5（gitignored 本地文件，UAT 重建流程一部分）
Tests:        单测：TestV2WirePathPrefix（新，wire path 前缀/形态）；docker 包受影响回归 TestSessionBearerDance/TestSessionDanceRefusedCredential/TestSessionPlainUpstream/TestT392RemotePullThroughChain/TestT392RemoteDegradationMatrix/TestT392RemoteBearerChain/…（全包）；helmoci 包（B1 返工后）TestRemotePullThroughChain/TestRemoteUpstreamDeletedCacheServes/TestRemoteDegradationMatrix/TestVirtualDualDomainChain/TestVirtualFirstSeenShadowing/TestVirtualDegradationMatrix 全绿。差分重放（真实客户端）：C04 docker pull 成功+digest 一致；C09 Range 206；C05/C06/C07/C08/C10 五阻断案逐案闭合（详见 diff 报告复验段）
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter/docker internal/adapter/helmoci（空）&& ~/go/bin/golangci-lint run internal/adapter/docker/... internal/adapter/helmoci/...（0 issues）&& go test ./internal/adapter/docker/ -count=1（ok 33.0s）&& go test ./internal/adapter/helmoci/ -count=1（ok 108.2s）&& go test ./internal/repo/ -run 'Docker|Remote|V2'（ok 49.8s）；UAT 重建：make console && make docs && make release VER=uat-l000f-59f33ab5 && REGISTRY= PUSH=0 SMOKE=0 ARCHES=amd64 VARIANTS=alpine ./deploy/release/build-release.sh uat-l000f-59f33ab5 && compose up -d --force-recreate；重放：registry:3 上游 5588 + docker:27-dind（--insecure-registry=host.docker.internal:8083）+ dind docker login/pull
Outputs:      上游日志（灭因直证）`"GET /v2/hello-world/manifests/latest HTTP/1.1" 200` + 两 blob GET 200；pull `Digest: sha256:d1a8d0a4eeb63aff09f5f34d4d80505e0ba81905f36158cc3970d8e07179e59e`（与上游参照逐字一致）；Range `206 + Content-Range: bytes 0-63/577`；二次 pull 上游计数 26→26 冻结 + X-Binflow-Cache MISS→HIT；INM 恒 200；随机 digest 上游 404 一次后本地 15-42ms 负缓存
Compatibility: 契约缺失（protocol/ 空）→ 按 Registry v2 规范 + E4 证据实现。复验判定：SAME 2→6（+C04/C07/C08/C09）、DIVERGENT 11→12（−C04/C09、+C05/C06/C10）、UNKNOWN 5→0；残留 DIVERGENT（C01/C01b/C02/C03/C11/C12/C13/C14/C15/C16/D21）逐项复测未扩张（C11 405、C12 detail.reference、C13 detail.digest、C15 无聚合（上游加 t2 后仍只回 t1）、C16 repo 域 404 UNSUPPORTED——形态均原样）
Security:     wire path 为纯 egress 路径构造，输入 image/ref 均经既有 digest/tag 校验后进入；无新增入参面；SSRF 防护链（allowPrivateUpstream）未触碰；重放用容器已销毁、凭据未落盘
Performance:  blob 流式落库路径未改（仅前缀字符串变化）；首拉 1.94s/二次 1.42s（本地上游），上游计数冻结证明零重复回源
Risks:        ① 上游 URL 语义变化：存量若配了带 `/v2` 后缀的 URL（旧补偿形态，如 repo 层历史测试种子）会得到 `/v2/v2/...`——未做 URL 归一化（Artifactory 是否裁剪无 clean-room 证据，不猜测补齐）；需在 LOOP 001 由 compatibility-engineer 裁定是否按 Artifactory 剥尾。树内已触发同 seam 的 helmoci fixtures（Review A B1，双 `/v2` 实证 6 例红，已一并迁移修复；repo 层 unreachable 种子不受路径影响，复跑绿）② registry:3 对 Accept 严格（`*/*` 不收 OCI）——真实 daemon 无碍，curl 探测须显式带 OCI media type ③ 本地 registry 上游 RTT 小，C07 耗时比弱（靠标记+计数器补强）
Blockers:     无
Next:         LOOP 001 票不变：P1 tags/list 远端聚合 + _catalog 兼容；P2 错误形态（C02/C11/C12/C13）；P3 service=host 回显（本次核查确认非一行改动，含测试断言更新）；待裁 C01b/C03/C14；部署面 D20/D21。可差分 surface：`/v2/<repo>/<image>/manifests/<ref>`（GET/HEAD）、`/v2/<repo>/<image>/blobs/<digest>`（GET/HEAD/Range）、token 端点、`/v2/<repo>/<image>/tags/list`
