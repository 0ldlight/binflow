# L024-9 — web 侧双债：twMerge 字号修票 + spec 内 PUT-as-replace 清偿

Ticket:        L024-9 · web 双债（① twMerge 吞字号类修票——UI 批 5 登记；② spec 内自有 repo PUT-as-replace 清偿——T-SEED50 登记）· P1
Role:          dev-frontend（area：web/src/lib/utils + web/src/components/ui/badge（①）；web/e2e spec 面（②））
Area:          web/src/lib/utils/cn.ts · web/src/components/ui/badge.tsx · web/e2e/p2/core-flow.spec.ts + 三处过时注释
Input:         派发单（两件独立可分 + 验收四门/单测/grep 归零口径）；T-SEED50 报告（reports/agents/T-SEED50.md，Risks②「spec 文件内自有 repo PUT-as-replace ~20 文件，fresh+uniq 不受影响」+ Next①）；ADR-0050 语义在 internal/httpapi/repositories.go 的实现现状走读（handleRepoPut：PUT=create-only，对已存在 key 回 400 字面「Repository key already exists」；PUT-create 回 200；POST=merge-on-omit）；web/scripts/seed-m8.mjs 的 ensureRepo/makeClient/adminCredential 助手与 seed-m8.d.mts 类型面；design-system/tailwind.css @theme 五个自定义字号槽（--text-{dense,aux,form,h3,h2}）；tokens/typography.css（--bf-fs-xs:12px / --bf-lh-xs:1.4 = text-aux 及其行高伴生档，等值迁移前提）

## 分节一：twMerge 吞字号类修票（UI 批 5）

Changes:
               1. web/src/lib/utils/cn.ts：twMerge 换 extendTailwindMerge——`extend.classGroups['font-size']` 注册五个 @theme 自定义密度字号名（dense/aux/form/h3/h2）。根因：默认组表只认内置字号阶（text-xs…9xl），`text-aux` 被误判进 text-color 组，与变体色类同串时被当冲突消解；注册后字号/色彩两族互不消解，同组内仍后者胜
               2. web/src/components/ui/badge.tsx：批 5 tint 七变体退役「任意值 token 自持」规避法——`text-[length:var(--bf-fs-xs)] [line-height:var(--bf-lh-xs)]` 删除，字号/行高继承基线 text-aux（12px/1.4 值等值：--text-aux=var(--bf-fs-xs)、--text-aux--line-height=1.4=--bf-lh-xs）；头部批 5 注释的规避段改写为修票后语义
               3. 行为修复面（非等值部分，如实登记）：软底状态变体（success/warning/info/destructive/destructive-soft）此前基线 text-aux 被变体色类吞掉——状态徽章实际丢失 12px 字号（继承父级字号）；修后按设计回 12px/1.4。tint 族完全等值
               4. 新增 web/src/lib/utils/cn.test.ts（node:test，7 用例）：五字号名×双向色彩共存、同组后者胜（自定义/自定义、自定义/内置、色彩/色彩）、badge 配方回归（基线字号+变体色+追加类）、退役任意值形与语义类同组消解
               5. web/package.json 增 `test:unit` script（node --test，Node ≥22.6 类型剥离；本机 v26.8.1）
Files:         修改 web/src/lib/utils/cn.ts · web/src/components/ui/badge.tsx · web/package.json；新增 web/src/lib/utils/cn.test.ts
Tests:         单测 7/7 绿（见 Commands）。e2e 消费面：e2e/m8/permissions.spec.ts（T-266 shared-badge verdict=tint-success/tint-danger + 角色徽章 axe 双主题）5 passed。grep 口径：`grep -rn "任意值 token" web/src` = 0（规避注释零残留）
Commands:
               ```
               cd web
               node --test src/lib/utils/cn.test.ts        # tests 7 pass 7 fail 0
               npm run test:unit                           # pass 7 / fail 0
               npx eslint src/lib/utils/cn.ts src/lib/utils/cn.test.ts src/components/ui/badge.tsx   # exit 0 零输出
               BASE=http://127.0.0.1:18085 npx playwright test e2e/m8/permissions.spec.ts   # 5 passed (14.3s)
               ```
Outputs:       node --test：`tests 7 / pass 7 / fail 0`；permissions.spec：`5 passed (14.3s)`；eslint 改动文件零告警

## 分节二：spec 内自有 repo PUT-as-replace 清偿（T-SEED50）

Changes:
               1. **全量盘点先行**（fresh+uniq 保留口径的执行依据）：web/e2e 全域 repo-config PUT 调用点共 115 处（`request('PUT'`/`api(page,'PUT'`/`sessionApi`/`rest('PUT'`/`admin.put` 五种形态 + waitForRequest 观测点排除），逐文件核 key 溯源——113 处 uniq()/Date.now() 每 test 现取（fresh 首建=create 语义，保留，含 rbac:210 与 repositories-admin:389 两处负向腿）；1 处字面 key 但 spec 自托管 throwaway 实例（m13/t366-consumer.spec.ts:240 't366-local'——beforeAll 起自带 server+tmp data-dir、afterAll rmSync，key 每轮必新，保留）；**真实「同 key 重复 PUT」面 = 1 处**：e2e/p2/core-flow.spec.ts:65 固定 key 'p2-local'（本文件三个 describe 的 beforeAll 各 seed 一份 + 共用/二次实例 key 已存在——旧 converge 六轮吞 400 硬过，种子语义已死）
               2. p2/core-flow.spec.ts：converge-PUT 改 ensureRepo（复用/导入 web/scripts/seed-m8.mjs 的 ensureRepo+makeClient+adminCredential；Basic 凭据 env-first 与 seed 族同链）：404→PUT 首建、200→POST 同 body merge；头部环境协议注释同步
               3. 三处仍声称 PUT-replace 语义的过时注释清偿（grep「PUT 更新自有仓」模式的文档面残留）：m8/auxiliary.spec.ts:26（「重复跑幂等——PUT 覆盖」→ 如实改为 uniq 累积+marker 圈定）、m9/usage-fanout.spec.ts:27（seedM9 已骑 ensureRepo）、m9/permissions-mholder.spec.ts:49（同）
               4. 盘点结论与登记面差异（如实上报）：派发单「~20 处」承 T-SEED50 Risks② 的粗估（~20 **文件**）；按「同 key 重复 PUT」口径精查后实改 1 处 + 注释 3 处——其余全为 fresh+uniq 首建（派发单自身 carve-out）或已骑 T-SEED50 的 ensureRepo/seedRepos（m8/m9/m10 种子族、roles.ts provisionRoles、shell.spec REPO）
Files:         修改 web/e2e/p2/core-flow.spec.ts · web/e2e/m8/auxiliary.spec.ts · web/e2e/m9/usage-fanout.spec.ts · web/e2e/m9/permissions-mholder.spec.ts
Tests:         隔离实例双轮（fresh create 路径 + exists-merge 路径——后者即清偿的病面）：core-flow run1 16 passed / run2 同实例 16 passed、serve 侧零 retry 噪音、repo 配置收敛（description='p2 core-flow'）。grep 口径：spec 内字面 key repo-PUT 仅剩 t366（自托管 fresh，保留理由如上）；converge 包 repo-PUT 模式归零（唯一命中为测试名文案）
Commands:
               ```
               mkdir -p /tmp/bf-l0249 /tmp/bf-l0249-data
               go build -o /tmp/bf-l0249/bf ./cmd/binflow-server
               printf 'server:\n  listen: 127.0.0.1:18085\nstorage:\n  data_dir: /tmp/bf-l0249-data\n' > /tmp/bf-l0249/binflow.yaml
               (cd /tmp/bf-l0249 && ./bf serve &)          # ping 200
               cd web
               BASE=http://127.0.0.1:18085 npx playwright test e2e/p2/core-flow.spec.ts   # run1: 16 passed (29.5s)
               BASE=http://127.0.0.1:18085 npx playwright test e2e/p2/core-flow.spec.ts   # run2 同实例: 16 passed (32.1s)
               curl -s -u admin:password .../api/repositories/p2-local                    # 配置收敛回显
               grep -c "seed retry" → 0
               pkill -f '/tmp/bf-l0249/bf serve'; rm -rf /tmp/bf-l0249 /tmp/bf-l0249-data  # throwaway 已清
               ```
Outputs:       run1 `16 passed (29.5s)`；run2 `16 passed (32.1s)`；p2-local GET 回 `{key:p2-local, rclass:local, packageType:generic, description:p2 core-flow}`；隔离实例/数据目录已删、无残留进程

## 公共门（两节共用）

Commands:      `cd web && npm run build && npm run typecheck && npm run lint && npm run test:unit`
Outputs:       build：assert-tokens 通过 + assert-i18n「零硬编码中文；3120 调用点 / 2347 键，en 包与 zh 清单同构」+ vite `✓ built in 7.85s` + relink-assets `verified — no ui-segment asset reference survives` + wire-brand-assets 7 文件指纹；typecheck exit 0；lint `0 errors, 45 warnings`（全部为存量 react-hooks set-state-in-effect 族，改动文件 scoped eslint exit 0）+ assert-i18n OK
Compatibility: 无契约面改动（分节一纯前端类名合成；分节二纯 e2e 种子形态）。ADR-0050 对齐正向：spec 侧最后一处固定 key PUT-as-replace 清偿，与 T-SEED50 种子基建同一语义（PUT=create-only / POST=merge）。契约漂移：无
Security:      无新增面：不触 secret、不外发；隔离实例凭据为默认 admin/password（throwaway，已删）；未触碰用户实例 ./data 与 172.16.58.130
Performance:   分节二：p2 种子从「二次实例 6×300ms 吞错重试 ×3 describe」变 GET+POST 两次轻调用；其余无影响
Risks:         ① 软底状态徽章（success/warning/info/destructive 族）字号回归 12px 是可见变化（修 bug，非等值）——design-baseline golden 归 conductor，翻新时状态徽章像素可能移位（tint 族等值不受影响）；② src 内仍有未标记的任意值字号消费点（DeployDialog/SetMeUpDialog/RepositoriesPage:525 静态串，不经 cn 或无批 5 注记）——不在本票「注释定位规避点」口径内，未动；③ 过程事故如实记：中途一次误发 `git stash push` 波及全工作树（含并行 dev-go-core 的 internal/ 未提交改动），当即 `git stash pop` 字节级还原并 diff --stat 复核，零丢失（会话开始时的 L023-3/package-lock.json 改动经查已由 conductor 提交为 dbbcb78f/8f7ee0d3）
Blockers:      无
Next:          ① 状态徽章字号回归请 conductor 在 design-baseline 翻新轮知悉（非 tint 面）；② RepositoriesPage:525 手搓 tint-neutral span 建议收敛到 Badge 组件（单独小票）；③ 未标记任意值字号消费点若要全量迁语义类，建议随 styleguide 票走；④ T-SEED50 登记的「~20 文件」粗估可按本票盘点结论（fresh+uniq 保留面）回写销项
