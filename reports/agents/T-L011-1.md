# T-L011-1 — dev-go-storage：元数据面计数污染修复（含 Review A 返工）

Ticket:       T-L011-1 · 元数据面下载计数污染修复（rest/metadata-plane-download-count-pollution，BUG）· LOOP 011 轨道 L011-1 · P1；Review A REQUEST_CHANGES 两条 blocking（B1 治理门脱落=安全级、B2 matchStart 臂丢失）已返工
Role:         dev-go-storage
Area:         internal/httpapi（本票明示授权域）+ internal/repo/service.go（Review A 修法明示授权：coordinator 指示「repo.Service 增一个非计数解算」——落地为 *service 上的 ResolveMeta 方法，不扩 Service 接口，见 Changes③）；storage/remote 包零改动
Input:        台账 docs/compatibility/known-divergence.yaml#rest/metadata-plane-download-count-pollution（review_gate = 本票）；证据 reports/compatibility/L010-list-p2-diff.md §6-a（隔离探针 0→1→2→3）；ADR-0044 K69（internal/repo/downloadstats.go「De-probing the metadata faces is an audit-face ticket」= 本票）；先例 statsNode（T-438）；T-95/W12a 双值裁决（拒绝始终不可见）；internal/auth/pathmatch.go matchStart/isFolder 规则；随票 permissions.go:547 反编译名改写；Review A 退回意见（B1：service.go:432-434 治理 pattern 门未进新路径，被拒节点 GET 404→200 泄漏 size/checksums/属性、PUT/DELETE 404→204；B2：旧 Get 门以原始拼写跑 allow()，新 List 以 TrimSuffix 前缀跑——裸目录 pattern 的 matchStart 臂永不触发 + 403 文案 path 拼写漂移）
Changes:      第一轮（计数修复）：① 前置探针（双端活体，修复前）：BinFlow virtual 面 item-info 使成员计数 0→1、props PUT→2、GET→3——virtual 面确认污染；参照全 0（且参照 ?properties/?stats 打在 virtual 均 404——既有分歧，见 Compatibility）② storageNode 从 Get-based 改 List-based 非计数解算 ③ search_ui leadFile 直连 Get 改走 storageNode ④ permissions.go:548 注释出处改 live-wire ⑤ 三件测试 + UAT 重建差分三组复验 + mdTimestamps 递归臂取证。第二轮（Review A B1/B2 返工，同根=新 List 路径未跑旧 Get 路径谓词）：⑥ internal/repo/service.go 新增 ResolveMeta（*service 方法）：Get 的谓词链原序复刻——validateNodePath（空/根/退化拼写 400 前置，statsNode 同款前置姿态）→ loadRepoRow → allow() 以**原始拼写**（matchStart 文件夹臂）→ local 臂 parseGovernance.allowsPath 拒绝→ErrNodeNotFound（T-95 双值）→ listRows 行走（virtual 成员联合/remote 缓存行，不回源）精确匹配 → 非本地文件夹无行时 display 标记（T-406/getVirtualFolder 先例，display-only 不落行；本地 miss 保持 miss——materializeAncestors 保证行存在）；零 markDownload/audit/blob-open/upstream fetch ⑦ httpapi storageNode 改经 metaNodeResolver 消费者侧接口断言（remoteBrowseViewer 先例——不扩 repo.Service 接口：internal/adapter/docker/blob_test.go 的 32 方法手写 fake 在禁改域，接口加方法即断其编译）；断言失败=装配错误，诚实 500 fail-closed，无静默回退 ⑧ storageNode 原内联 List 行走+合成臂删除（职责移入 ResolveMeta）；statsNode 保持 List exact-only 语义不动（T-438 冻结面）
Files:        修改 internal/httpapi/storage.go（storageNode 重写+metaNodeResolver 断言）、internal/httpapi/search_ui.go（leadFile）、internal/httpapi/permissions.go（注释出处）、internal/repo/service.go（ResolveMeta +73 行）；新增 internal/httpapi/metadata_face_download_count_test.go（五测试）；UAT 面：镜像 binflow:uat-l0111-293a754c-wip2-alpine（worktree @293a754c + 本票全量 diff，离线组装）、.env.uat 版本键、容器 binflow-ga 换装（数据卷沿用）、双端夹具删净。无删除文件；worktree /tmp/binflow-l0111 已移除
Tests:        metadata_face_download_count_test.go 五件：TestMetadataFacesDoNotCountDownloads（八面计数恒 0 + 真 GET 1→2）、TestVirtualMetadataReadsDoNotCountOnMember（virtual 面不污染成员 + 经 virtual 内容 GET 仍 K69 arm 2 计数 + virtual ?stats 读成员行）、TestMetadataFaceResolutionArms（拼写回退/root 400/dot-segment/**N1 两臂**：退化斜杠 /// = validateNodePath 前置 400「the repository root is not a node」、中段 // = 400「empty path segment」——路由 splitStoragePath 实测 // 前导折叠、/// 传递 relPath="/"）、TestMetadataFaceGovernanceRefusal（**B1**：宽 pattern 上传→收紧→被拒路径 item-info/props GET/PUT/DELETE/?permissions 五面全 404 missing-item 文案 + 允许路径 200 对照——补齐 t95 只钉内容面的历史债）、TestMetadataFaceACLMatchStartArm（**B2**：裸目录 pattern「acme」下显式斜杠 acme/team/ props GET=200 matchStart 臂、无斜杠同址=403、403 文案携带原始拼写含尾斜杠；properties 家族为演示面——item-info 的 children 列举走 List 自身 trimmed 门系**既有姿态**（旧 Get 代码同 403），非本票回归面）。**红检**：注释掉治理块+门改 trimmed 拼写 → B1 五臂+B2 两臂全红 → 还原全绿（测试确实咬住谓词）
Commands:     第一轮：go build ./... && go vet ./...；gofmt -l internal/httpapi（空）；~/go/bin/golangci-lint run ./internal/httpapi/...（0 issues）；go test ./internal/httpapi/ ./internal/storage/... -count=1；go test -race ./internal/httpapi/ -run 'TestMetadataFace|TestVirtualMetadataReads|TestStorage|TestRemoteDegraded' -count=1 solo；go test -race ./internal/storage/... -count=1；UAT worktree 离线全链（DaoCloud 源 EOF，l0072 配方）+ 换装 + 复验三组。第二轮（Review A 返工后全量串行重跑）：go build ./... && go vet ./...；gofmt -l internal/httpapi internal/repo（空）；~/go/bin/golangci-lint run ./internal/httpapi/... ./internal/repo/...（0 issues）；go test ./internal/repo/ -count=1（ok 65.1s）；go test ./internal/httpapi/ -count=1（ok 155.4s）；go test ./internal/storage/... -count=1（ok 72.8s）；go test -race ./internal/httpapi/ -run 'TestMetadataFace|TestVirtualMetadataReads|TestStorage|TestRemoteDegraded|TestT95' -count=1 solo（ok 52.3s）；go test -race ./internal/repo/ -run 'TestGovernance|TestGet|TestVirtual|TestList' -count=1 solo（ok 27.3s）；UAT wip2 重建换装 + B1/B2/N1 活体复验
Outputs:      第一轮（保留）：隔离探针 0→0→0×九面→真 GET=1；remote 未缓存 item-info 404 不回源（参照同臂 404）；A30 脏实例收敛（干净缺席/真下载在/污染行历史保留）；mdTimestamps 递归臂取证（参照 E1-E4 子项 mtime 随递归写移动 vs BinFlow 只记 addressed 节点——升级票素材）。第二轮（Review A 活体，UAT wip2）：B1 被拒路径（收紧 excludesPattern secret/** 后）item-info/props PUT/GET/DELETE 四面全 [404]，内容面对照同 [404]，文案「Unable to find item: node l0111-gov/secret/leak.bin/: node not found」（与 missing 不可区分，T-95 双值恢复）；B2（裸目录 pattern acme + scoped 用户）props GET 显式斜杠 acme/team/=[200]（matchStart 臂恢复）、无斜杠同址=[403]「read l0111-acl/acme/team: permission denied」（原始拼写）、文件夹拼写拒绝文案保留尾斜杠「read l0111-acl/other/box/: permission denied」；N1 退化 ///=[400]；复验后隔离仍 0（inner.bin downloadCount=0）
Compatibility: 与 docs/reverse/rest-api.md §3 及 T-95/W12a、auth-model §4（matchStart/isFolder）对照：元数据面计数修复成立（参照 0→0 对齐）且两条 blocking 谓词回归修复——治理门与内容面同 404 不可区分、matchStart 文件夹臂与旧 Get 门逐臂一致（含 403 文案拼写）。翻绿建议归 compatibility-engineer：rest/metadata-plane-download-count-pollution → resolved（evidence = 本日志两轮 Outputs + L010-list-p2-diff §6-a）。行为备注：item-info/properties 未缓存 remote=404 不回源（参照对齐方向，顺带修复）；无行文件夹 display 标记时间戳 epoch（getVirtualFolder/根 FolderInfo 既有姿态）；item-info 文件夹 children 列举的 trimmed 门为 List 既有姿态（非 Get 回归，B2 演示面故用 properties 家族）。新观察（不立案，供 conductor）：参照 ?properties/?stats on virtual=404 vs BinFlow 200（T-438/FR-89.2 既有裁量）；参照 ?stats 体 lastDownloaded 数值 0 恒在场 vs BinFlow omitempty
Security:     B1 即安全级修复本体（泄漏面关闭：被拒路径元数据四面 404 与内容面同形）；红检证明测试咬合；admin 口令仅经 .env.uat（gitignored）运行时 source 未落日志/报告；UAT 绑 127.0.0.1:8083；离线组装仅本地缓存镜像无外发；scoped 测试用户/权限目标/仓夹具用后删净
Performance:  ResolveMeta = 谓词（内存/单行读）+ 单次 listRows 前缀行走 + 内存精确匹配；替代旧 Get 的 blob Open+计数 UPDATE+audit 行——元数据面净减一次文件系统打开与两笔写；未缓存 remote 免整制品拉取+落地； spelling 回退臂最坏两次 ResolveMeta（与旧两次 Get 同形）
Risks:        ① 元数据面自此不写 download audit 行（K69 对偶同拆——de-probe 即两者同去；依赖 item-info 审计行的下游需知悉）② metaNodeResolver 断言失败=500 fail-closed：现网装配（cmd+harness 三处）均注入 *service 恒成立；未来若注入 fake ReposSvc 需实现 ResolveMeta（注释已锚）③ 无行文件夹 display 标记 epoch 时间戳（角 case，见 Compatibility）④ item-info 文件夹 children 列举仍走 List trimmed 门（既有姿态，B2 范围外——若参照对拍发现 folder-listing ACL 分歧另立票）⑤ 递归属性写子项 mtime 分歧（已取证，实现归下票）⑥ listRemoteFolderItems=true 的 remote 元数据解算可见上游 derived display 行（与 ?list/根列举同通道同暴露，T-448 既有裁量）
Blockers:     无
Next:         ① 翻绿 known-divergence.yaml#rest/metadata-plane-download-count-pollution → resolved（归 compatibility-engineer）② 递归属性写 mtime 升级票素材已齐（一轮 Outputs E1-E4）③ 可选新账候选：参照 ?properties/?stats on virtual=404 vs BinFlow 200 ④ UAT 现载 uat-l0111-293a754c-wip2（293a754c + 本票全量 diff 含 Review A 返工）；复位基线复用 T-L011-4 Commands（binflow:uat-l0114-293a754c-alpine 本地仍在）⑤ DaoCloud 源再 EOF（本日二遇）——离线配方可固化为 build-release.sh OFFLINE 开关（归 devops）
