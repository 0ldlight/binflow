# T-184: [P2] replication CRUD 面 docs 回写

- **Role**: architect
- **Area**: `docs/design`（architecture.md + console-ux.md，仅此两文件）
- **Status**: done（自测全绿，见下）
- **Dep**: T-180（复制面桥接，done——本票即其 docs 遗留项）；款式基准 T-176/T-182（「回写记录」题头口径）

## 做了什么（AC 对应）

### AC① architecture.md §7.1 路由表——replications CRUD + status 按实现定稿

事实源（先读代码后写字）：`internal/httpapi/replication.go`（467 行全量）+
`internal/httpapi/router.go:343-362`（四条路由注册）+ `internal/metadata/migrations/
{sqlite,postgres}/009_replication.sql`（FK 级联）+ `reports/agents/T-180.md` 裁定表。

改动：**整块替换**原 ADR-0021 草案 7 端点（POST/GET 集合 + GET/PUT/DELETE {id} +
POST {id}/trigger + GET {id}/tasks）为实际落地 4 端点 + 聚合 status：

| 新行 | 关键契约点（逐项自实现核对） |
|---|---|
| `GET /api/v1/replications` | bare JSON array（沿 permissions 惯例）；行形 = 配置行减密码字段 11 键；target_password 只写不读永不回显；target_username 在 CRUD 面保留 |
| `POST /api/v1/replications` | 201 + 配置行；name 1..64 字符 `[A-Za-z0-9._-]` 首字符字母数字；source_repo 存在性预检查（**不校验 local 类型**，记入回写差异）；target_url 绝对 http/https；数值非负；enabled 缺省 true（*bool）；max_items_per_push 缺省 1000；重复名 409；带密码无 master key → 400 点名 BINFLOW_REMOTE_CREDENTIALS_KEY；store busy 503 |
| `DELETE /api/v1/replications/{name}` | **按 name 寻址非 id**（store 按 id 删、name 经列表解析）；204；任务行随 009 FK 级联；未知名 404 |
| `GET /api/v1/replication/status` | T-159 面板载荷（T-180 桥接）：targets[]（配置×ConfigStatus 拍平，凭据全不下发——**连 target_username 也不出**，与 CRUD 面差异写明）+ events[]（跨配置合并 newest-first，?limit= 1..500 缺省 50）；两数组恒 [] 非 null；*_at RFC3339、'' 哨兵 |

共用语义收进行尾「回写记录」段：未落地的 4 个草案端点（GET/{id}、PUT/{id}、
trigger、按配置 tasks）**不承诺**并记原因（PUT 待 enable/disable 语义裁定——
router.go 注释挂号；trigger/tasks 无消费方，事件经 status 聚合面可见）；{id}→
{name} 寻址变更；source_repo「local 类型」原行未实现；四端点 admin 门（路由 +
create/delete handler 双查）；未接线实例统一 501 非 404；errors[] 信封；审计
动作 `replication.config.create/.delete`；契约以 replication.go 为准。

### AC② console-ux.md 复制页补 CRUD 面说明

- **§4.11 线框**：空态注「M6 桥接后可用」→「T-180 已落地」；只读句改「配置
  CRUD 走 REST 面（T-180 已落地）；**CRUD UI 另票（T-180 遗留 4）**，本页暂不
  消费」；回写记录段补 T-180 后续：status 面零差异（「待桥接」收口）+ CRUD 面
  形状九要点（bare array/201/204/409/密码只写不读/username 两面差异/enabled
  true/max_items 1000/limit 50）+ 契约定稿指向 architecture.md §7.1 + 组件零
  改动、repl-* 锚不变。
- **§3.2 路由行**：数据源单元格「前端假设契约——端点未桥接」已过时 → 「T-180
  桥接完成、零差异」+ CRUD 面同批落地但本页不消费（不改 §3.1 导航树——无过时
  内容）。
- **§10.3 锚清单块头**：「端点为前端假设契约待 T-180 桥接对齐」→「已由 T-180
  桥接、零差异……锚随组件不变」（锚本身 11 个不变——组件零改动）。
- **刻意不动**：§4.11 收敛行「404（端点未桥接）」保留——组件 ReplicationPage
  的 404 防御分支与提示文案仍在（L113/L328 核对），§4.11 是组件实现形态的如实
  记录；§10 锚总量、§0 修订表、版本号沿 T-176/T-182 惯例不动（补录非改版）。

### AC③ 同款「回写记录」题头

architecture.md 行尾「——回写记录：实现先于契约，T-180 核验发现（T-184 回写，
T-180 遗留之 docs 项）」+ console-ux.md §4.11「（回写记录：实现先于契约，
T-180 核验发现）」——与 T-176（T-160）/T-182（T-159）同款口径。
（编号注记：BOARD 票面引「T-180 遗留④」，T-180.md 遗留清单里 docs 回写在⑤、
④是 CRUD UI 票——两处编号不一致，题头用「遗留之 docs 项」措辞规避以讹传讹。）

## 自测（实际执行）

```
# 1) 逐项 grep 实现核对（契约断言 ↔ replication.go，19 项 + 2 项缺失断言 + FK）
bare-array-list          L219 out := make([]replicationConfigResponse, 0, …)  hits=1
POST-201                 writeJSONBody(w, http.StatusCreated                 hits=1
DELETE-204               w.WriteHeader(http.StatusNoContent)                 hits=1
dup-409                  http.StatusConflict                                hits=1
no-pwd-in-read-shape     json:"target_password" 仅在 body 结构体             hits=1
crud-face-username       TargetUsername … json:"target_username"（响应结构体）hits=2
enabled-default-true     enabled := true                                    hits=1
maxitems-default-1000    maxItems = 1000                                    hits=1
limit-default-50         defaultReplicationEventLimit = 50                  hits=1
limit-cap-500            maxReplicationEventLimit     = 500                 hits=1
unwired-501              http.StatusNotImplemented                          hits=1
busy-503                 http.StatusServiceUnavailable                      hits=2
unknown-404              http.StatusNotFound                                hits=1
masterkey-400            BINFLOW_REMOTE_CREDENTIALS_KEY                     hits=2
audit-create/delete      replication.config.create / .delete 字面量          各1
name-charset             func validReplicationName                          hits=1
src-exists-precheck      s.deps.Repos.Get(r.Context(), body.SourceRepo)     hits=1
handler-admin-recheck    !p.Admin（create/delete 双查）                      hits=2
status-face-no-username  replicationTargetStatus 结构体内 target_username    hits=0（缺失即正确）
FK 级联                  009_replication.sql replication_id … ON DELETE CASCADE（sqlite+postgres 双栈）

# 2) 路由门禁核对：router.go L350-362 四条路由均 routeAuth{required:true, admin:true}

# 3) 结构完整
§4.11 复制框（L597-626）逐行显示宽 80-82（CJK×2 计），outliers NONE
architecture.md 续行列位全部 col 50、首行 [M6]@col 55——与邻行一致，outliers NONE
围栏配对：architecture.md 40 / console-ux.md 46，均成对

# 4) 陈旧表述清零
grep "M6 桥接后可用|待 T-180" console-ux.md → 0 hits（§10.3 已修；
L611「404（端点未桥接）」刻意保留——组件防御分支仍在，实现形态如实）

# 5) git diff 仅两文档（本会话前后对比：改前 docs/design 工作区干净）
 docs/design/architecture.md | 53 ++++++++-----
 docs/design/console-ux.md   | 25 +++----
 2 files changed（4 hunks：arch §7.1 块替换 ×1；console §3.2/§4.11×2/§10.3）
```

## 偏差与说明

- AC①「四端点」票面口径：按 T-180 日志裁定表落地为 GET/POST/DELETE
  replications + GET replication/status 共 4 条路由（router.go 同款四条），
  任何读法下本票均已全覆盖。
- 未动 §8 replication 配置段（那是 engine 调度器配置非 REST body；原 POST 行
  「body 见 §8 配置段」系错误引用，新行已改为内联描述 body 形状，无需 §8 改动）。
- 未动 docs/prd、docs-site：PRD 面（milestone-6.md 如涉复制 REST 拟形）勘误归
  product-manager；docs-site build 产物由主会话流水线再生成（沿 T-176 遗留④口径）。

## 遗留

（无新增）既有维持：T-180 遗留 1（audit 词汇落 internal/audit）、遗留 3
（metadata DSN 收编）、遗留 4（CRUD UI 票——消费本票定稿的 §7.1 契约形状）。
