# L025-7 工作日志 — G1 渲染收敛后控制台读位断裂修复

```
Ticket:       L025-7 G1 渲染收敛后控制台读位断裂修复（L025-6 登记项②，急）+ P0
Role:         dev-frontend（area = web/src 仓库域读位 + web/e2e 仓编辑/配额 spec 组）
Area:         web/src/lib/repos.ts（读位）+ web/e2e/{repositories,repo-policy-keys,m8/repositories-admin,t104-supplements,m16/t443-list-entry-dirty-test}.spec.ts + web/scripts/seed-m8.mjs
Input:        L025-6 报告登记项②（commit 989ad5ed：BinFlow 专键离开 v1 详读面，控制台读位在 repos.ts，quota-edit priorityResolution 写隐患）；
              internal/httpapi/repo_config_render.go（v1 键表 local 61/remote 102/virtual 50 + renderConfigSeats「blob wins」）；
              internal/httpapi/repositories.go（repoListItemOf：列表面 configuration = 存储 blob 原样回显 = listConfigurationOf 先例）；
              internal/repo/service.go UpdateRepo（remote=omit-merge，local/virtual=caller-owned replace——全量回传是 local/virtual 的正确姿态）；
              本票指令（择小 diff；不动 internal/；禁 docker/禁 172.16.58.130；isolated 实例验证）
Changes:      ① 读位合并（根因修复）：getRepoDetail 从「单读 v1 面、期望旧 configuration 子对象」改为「v1 面（平铺键）∪ 列表面 configuration blob 覆盖」——身份四键剥离，其余平铺键（含 remote 上游 url）即配置面；blob 覆盖与渲染器自身优先级同向（存储值优先）。列表面 403（m-holder 形态：持仓级 manage 过详情读臂、CapRepoRead 拒清单）降级为面上键，其余错误照常抛（读不完整宁可响亮失败，不静默喂缺省给全量替换保存）。一处改动收拢全部消费方（RepoDetailPage/QuotasPage 行内配额/RepositoryFormPage 预填/DeployDialog/SetMeUpDialog/artifacts hooks/buildLocalQuotaBody）。
              ② 写路径保险（非增量改写）：local/virtual 是 caller-owned replace 语义，改成增量提交会丢未建模键（比原隐患更糟）——保险落在「读位完整性」（①）+ 既有 dirty-gating：预填带真值后未触碰字段以真值随行回写，priorityResolution/quotaBytes/byHash 族不再翻转。
              ③ e2e 对账腿真实对齐 L025-6 后 wire：详读面键平铺（configuration.X → 顶层 X）；BinFlow 专键（quotaBytes/defaultDeploymentRepo）对账改读列表面 blob（新增 listConfigOf 助手）；未知 key 断言 404 → 400 "Bad Request"（L025-5/G5 参照 quirk）。
              ④ 新增回归腿 repositories.spec「L025-7: BinFlow-only keys survive an untouched-fields edit」= 本票点名冒烟路径的 playwright 化：列表 → 详情（专键回显）→ 编辑器零触碰开关只改 description 保存 → 列表面 blob 对账 priorityResolution=true / quotaBytes / includesPattern 不翻转。
              ⑤ 顺车归位四处与本票验证路径相撞的既有 spec 漂移（straggler，断言语义不弱化）：repo-policy-keys 节题三处（T-463 i18n 外化后改拼写「索引引擎策略键（仅 <pkg> 仓…」）+ mock 体平铺化；m8 .empty-state 类选择器 → data-testid（batch-5 退役旧类）；m8 repo-tab-config(s) → repo-tab-configuration/replication（P2 八 Tab 化改名）；seed-m8 ensureRepo 把 400 "Bad Request" miss quirk 按 absent 处理（原样抛错令全部 m8/m16 种子腿崩溃）。
Files:        修改：web/src/lib/repos.ts；web/e2e/repositories.spec.ts；web/e2e/repo-policy-keys.spec.ts；web/e2e/m8/repositories-admin.spec.ts；web/e2e/t104-supplements.spec.ts；web/e2e/m16/t443-list-entry-dirty-test.spec.ts；web/scripts/seed-m8.mjs。新增：reports/agents/L025-7.md。删除：无。
              （internal/console/dist 为 make console 同款生成物刷新，仅 placeholder.html 受 git 跟踪，tracked 树零变化）
Tests:        新增腿：repositories.spec L025-7（列表→详情→编辑→未触碰专键不翻转——本票 hazard 的直接回归钉）。真实对齐腿：同 spec 建仓/编辑/remote/virtual 四处 API 对账 + m8 quota 对账（改走 admin 客户端读列表面——m-holder 403 于清单）+ t443 remote url 对账。四态：读位失败路径维持既有 ErrorCard/表单错误面（getRepoDetail 401/5xx 照抛）；403 清单降级有 m-holder 腿覆盖。
Commands:     cd web && npm run build（含 assert-tokens + assert-i18n + vite build + 资产接线）；npm run typecheck；npm run lint；
              go build -trimpath -o /tmp/l0257/binflow-server ./cmd/binflow-server（BINFLOW_STORAGE__DATA_DIR=/tmp/l0257/data 独立实例，127.0.0.1:8080，未 docker、未触 172.16.58.130、未碰 repo ./data）；
              npx playwright test repositories.spec.ts repo-policy-keys.spec.ts governance.spec.ts（16 passed）；
              npx playwright test t104-supplements.spec.ts m16/t443-list-entry-dirty-test.spec.ts m8/repositories-admin.spec.ts（24 passed / 5 failed，均为 HEAD 对照复现的存量视觉/布局债，见 Risks）；
              curl 对账：PUT 专键仓 → GET v1 面（quotaBytes/byHash 缺席、priorityResolution=true 在）→ GET 列表面 blob（quotaBytes=1048576/byHash=ALL 全在）
Outputs:      build: ✓ built in 7.16s + relink-assets verified + assert-i18n OK（3120 调用点/2347 键，en/zh 同构）；typecheck: tsc --noEmit 零输出；lint: 0 errors（45 条存量 warning，非本票文件）；
              e2e: 票面集 16/16 绿（含新 L025-7 腿 2.5s；lifecycle 编辑往返 quota 1048576→2097152 预填恢复）；扩展集 24 绿 + 5 存量红（下述）；
              HEAD 对照（stash 后重建二进制）：grid-wizard 腿在 HEAD 更早死于 remote 卡 URL 空（「上游URL ⧉http」——正是本票修的读位断裂）；.page-note color-contrast 与 three-tab contrast 在 HEAD 逐字节复现
Compatibility: 锚册对照：不涉锚册行（页面树/导航零变化）。契约漂移：本票零新增（消费既有两面，未加任何 query/键，v1 兼容面未污染）；沿 L025-6 既有漂移两处在 spec 内对齐——① GET /api/repositories/{key} 键顶层平铺无 configuration 子对象；② 未知 key 答 400 "Bad Request"（非 404）。**新发现服务端座位缺口**：m-holder（持仓级 manage、无 CapRepoRead）对 BinFlow 专键全无读位（v1 面离表 + 列表面 403）——前端已按 403 降级为面上键，quota 行内编辑仍可用但存量 quotaBytes 值不可见，需后端裁决。
Security:     无新增用户内容渲染面（值经既有 cfg* 收窄进既有组件，React 转义不变）；password 仍只写不读（blob 无 password，NFR-S14 维持）；探测/日志零凭据。
Performance:  getRepoDetail 每次 2 请求且列表面为全量 blob（用户实例 260+ 仓 ≈ 数百 KB）——管理台详情/编辑/配额场景可接受；根解 = 下述 /api/v1 投影面（Next）。无虚拟滚动/分页影响。
Risks:        ① m-holder 专键读位缺口（上 Compatibility 项）；② 五条存量红（HEAD 对照二进制逐条复现，均非本票引入、不在本票读写域）：m8 three-tab 与 readonly_admin 与 t443 axe 三腿 = color-contrast serious（列表页 + 详情 .page-note——batch-5/MUI 视觉迁移债）；m8 grid-wizard 与 t443 entry 两腿 = pkg-grid-cancel 按钮在 1280x720 视口外不可点（包类型网格 Dialog 布局）；③ local/virtual 全量替换编辑会丢表单未建模的 blob 键（blackedOut/environments/repoLayoutRef…）——L025-6 之前即有的姿态，与本票正交；④ 编辑器读 socketTimeoutSecs 不识别 socketTimeoutMillis 别名存储（15000ms=15s 恰好同值，显示层无感）。
Blockers:     无（后端投影面诉求走 Next 提票，不阻塞本票：m-holder 场景已降级可用，读位断裂已修复）。
Next:         ① 提票（后端/ architect）：GET /api/v1/repositories/{key} 的 BinFlow 配置投影面（自有 /api/v1 族，照 /api/v1/storage/usage 先例）——一次性收拢：m-holder 专键读位 + 消灭 getRepoDetail 的全量列表面扇出；② 提票（dev-frontend 视觉债）：.page-note / 仓库列表 color-contrast 三腿 + pkg-grid Dialog 视口裁剪两腿（存量红，本票 HEAD 对照已取证）；③ 登记：编辑器未建模键随全量替换丢失（可评估编辑提交透传未建模 transport 座位键）；④ 契约回写诉求：rest-api.md 需明确「列表面 configuration blob = BinFlow 扩展回显位（Artifactory 无此字段）」的控制台消费地位。
```

## 附：两清单（盘点产出）

### 参照键清单（console 经 v1 详读面读、L025-6 后仍在键表上——读位完好）

| rclass | 键 |
|---|---|
| local | priorityResolution、includesPattern、excludesPattern、handleReleases、handleSnapshots、snapshotVersionBehavior、checksumPolicyType、forceConanAuthentication（conan）、calculateYumMetadata、yumRootDepth、enableFileListsIndexing（rpm）、forceMetadataNameVersion、forceNonDuplicateChart（helm） |
| remote | url、username、hardFail、listRemoteFolderItems、priorityResolution、retrievalCachePeriodSecs、missedRetrievalCachePeriodSecs、assumedOfflinePeriodSecs |
| virtual | repositories |

（实证注：priorityResolution 在 v1 面上仍为 blob-backed seat——L025-6 登记的「priorityResolution 写隐患」在 seat 意义上未实际断读；真正断读的是下表专键。合并读位对两者都成立。）

### BinFlow 专键清单（L025-6 后离开 v1 详读面——读位断裂，全部经列表面 blob 恢复）

| rclass | 键 | console 消费位 |
|---|---|---|
| local | quotaBytes | 详情配额行内编辑 / QuotasPage / 表单 |
| local | byHash、optionalIndexCompressionFormats、debianDefaultArchitectures、historyCycles、origin、label（debian） | 表单策略节（policyFields 册） |
| local | yumGroupFileNames（rpm） | 表单策略节 |
| remote | allowPrivateUpstream | SSRF 警示横幅 / 表单（静默翻 false = 安防线意外放宽） |
| remote | socketTimeoutSecs（面键拼写 = socketTimeoutMillis，名与值形双漂移） | 详情参数行 / 表单 |
| remote | metadataRetrievalCachePeriodSecs（今日无 console 读者，同族登记） | — |
| virtual | defaultDeploymentRepo | 详情 / 表单 |

## 修法与验证摘要

- 修法（择小 diff）：未新增后端面（web 域内闭环）——getRepoDetail 读位合并（v1 面 ∪ 列表面 blob，403 容错降级），一处改动覆盖全部消费方；写侧不改增量（local/virtual replace 语义下增量=丢字段，保险=读完整 + dirty-gating）。
- 验证：四门绿；隔离实例（独立 data-dir，同 989ad5ed 代码栈）票面 spec 集 16/16 绿含新回归腿；扩展集 24 绿，5 存量红均以 HEAD 对照二进制取证为先前视觉/布局债，未删测未加 ignore。
