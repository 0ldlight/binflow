# QA 报告 T-172 — M6 回归基线（本地 filestore，M1~M5 全 P0 序列复跑）

- role: qa-engineer
- 日期: 2026-08-22
- 票据: T-172 [P0] M6 回归基线 — 本地 filestore 下 M1~M5 全部 P0 序列复跑（BOARD 只读引用）
- 验收口径: docs/prd/milestone-{1..5}.md §5.4 验收命令区（C/D/M/W/G 序列），断言按 M3/M4/M5 §5.6 回归反转表更新
- 被测物: **独立 git worktree @ 00f73e7（HEAD）构建** `/tmp/t172/binflow-server`（22MB，CGO_ENABLED=0；`make console` 后重建以嵌入真控制台）——不含主工作树在途 T-190 改动
- 实例: A=`127.0.0.1:18080` 匿名开（默认配置语义，data=/tmp/t172/data-a）；anonymous-off 腿以 `security.anonymous_access:false` 同数据目录重启承载（C27）；TTL 腿以 `console.session_ttl_seconds:5` 重启承载（W08）；import 往返另起 18082/18084 新数据目录
- 端口说明: PRD 命令面 `localhost:8080` 在本机被无关项目容器（arca-frontend）占用 → 全程改 **127.0.0.1:18080**，命令本体不变
- 客户端: curl 8.7.1 / jq 1.7.1 / docker CLI 29.7.2（**docker:27-dind 容器内 daemon**，`--insecure-registry host.docker.internal:18080`，T-44 手法——宿主 daemon 无 insecure-registries 且不动用户配置）/ mvn 3.9.9 + Temurin JDK 21.0.12（/tmp/t67-tools） / npm 10.9.8 / pip 26.1.2 + twine 7.0.0（/tmp/t172/venv） / oras 1.2.2 + crane 0.20.3（本票下载） / Playwright Chromium（web/e2e 既有规格）
- mock 上游: python3 http.server 9099（M02/M03/M41~M48/M51）；真上游腿走公网（maven-central / registry.npmjs.org / pypi.org 均可达）
- 收尾清理: 见 §8（容器/进程/worktree/临时目录/凭据全清，未触碰他人实例）

## 总结论: **PASS（有保留）**

- 五序列 P0 **全部通过**：C 序列 30/30、D 序列 25/25 可跑项（2 项客户端不可得如实标注）、M 序列 61/62 可跑项（M59 为 P2 未实现端点，见观察项）、W 序列 48/48、G 序列（票据口径：GC/备份恢复/治理 + 本地腿）12/12
- **零意外 5xx**：全程 access 日志 5xx 共 656 条，逐条归因——652 条 client_disconnect（C30 中断上传、D05 daemon 取消、G27 风暴断连）+ 3 条故意 501（migration 未配置，T-164 语义）+ 1 条故意 502（M44 hardFail 仓）；**无一为有效请求的服务端失败**
- 自动化: `make test` @HEAD worktree **20/21 包 ok**；`internal/adapter/docker` 的 `TestIdleSessionEviction` 在全量套件并发负载下失败一次（隔离复跑 3/3 过 + 整包过 → 负载敏感 flaky，缺陷 D-2）
- 浏览器面: Playwright 既有规格 **38 过 / 2 挂**，两挂均为 e2e 断言漂移而非产品行为错误（缺陷 D-3/D-4）
- 缺陷 4 条（1×P1 健壮性、1×P2 flaky test、2×P2 e2e 断言漂移）+ 6 条 PRD 勘误建议；另 1 条在途代码警示（T-190 主树 make test 红，见 §5）

---

## 1. AC 逐条结论

| # | 验收标准 | 结果 | 证据摘要 |
|---|---|---|---|
| 1 | M1 C 序列 P0 全绿 | ✅ | C01~C30 全过（§2.1）；C17 根级 400 为 T-18 定案行为、子目录载体过；C24 `/v2/` 401 与 C26 remote 200 为 M2/M3 反转表口径，均按现口径验证通过 |
| 2 | M2 D 序列 P0 全绿 | ✅ | D01~D24 + NFR-S11 全过（§2.2）；docker login/push/pull/run 真客户端链（dind 承载）全 0 退出；D17 podman / D19 skopeo 客户端不可得 → 环境限制标注（D19 为 P1） |
| 3 | M3 M 序列 P0 全绿 | ✅ | M01~M58 全过（§2.3）；mvn 3.9.9 deploy/resolve/snapshot、npm publish/install/dist-tag/unpublish、twine+pip 全真客户端；M45 走真 maven-central、M47 走真 pypi.org；M59（P2）端点未实现 → 观察项 O-5 |
| 4 | M4 W 序列 P0 全绿 | ✅ | W01~W39 全过（§2.4）；浏览器面 login/console-smoke/repositories/artifacts/governance/security/auth-shell/t134 规格 38 过 2 挂（挂项为断言漂移，行为正确，缺陷 D-3/D-4） |
| 5 | M5 G 序列 P0 全绿（GC/备份恢复/治理） | ✅ | G19/G27/G28/G30a/b/d/G31/G31c/G32/G33 全过（§2.5）；发布产物腿 G01~G17 属 release-engineer 烟测域且非本地 filestore 面，本轮不复跑（无相关改动） |
| 6 | QA 报告产出，零失败零 5xx | ✅（有保留） | 本报告；5xx 656 条全数归因（§4），零意外；「零失败」在 4 条缺陷口径下成立（均非 P0 功能失败） |

---

## 2. 场景明细

### 2.1 M1 C 序列（curl；A 实例）

| 项 | 结果 | 证据 |
|---|---|---|
| C01 ping | ✅ | `OK`（匿名） |
| C02 未认证管理 API | ✅ | 401 |
| C03/C04 建仓/非法 key | ✅ | 200 `Successfully created repository 'generic-local'` / 400 |
| C05/C06 列仓/查仓 | ✅ | key 在列；`local`+`generic` |
| C07 上传对账 | ✅ | 10MB 服务端 sha256 == 本地；item size==10485760 |
| C08/C09/C10 | ✅ | 下载 sha 一致；HEAD `X-Checksum-Sha256` 一致；item info sha256 一致 |
| C12 去重 | ✅ | 同内容二路径 blobs 2→2 不变 |
| C13/C14/C15 | ✅ | 一致 201；不一致 409（msg 含 received/actual）；秒传命中 201/未命中 404 |
| C16/C17 | ✅ | mkdir 尾斜杠 201、children=3；子目录 `?list&deep=1` 列出 artifact.bin/v2.bin（根级 400 为 T-18 O3 定案） |
| C18 删除幂等 | ✅ | 204 / 404 / 404 |
| C19 删仓 | ✅ | 非空 400；`?deleteContent=true` 200 |
| C20 改密双路由 | ✅ | 自有 200；changePassword 别名 200；旧口令即失效 401；错误旧口令 **400** `Incorrect username/password` |
| C21 Token | ✅ | form 发放（Bearer/api:*/64 字符）；充当口令 200；revoke 200 `Token revoked`→401；幂等 `Token not found`；token⊕token_id 400 |
| C22 用户/ACL | ✅ | PUT 建用户 201；列表 `{name,uri,realm}`；缺 email 400；无权写 403；授权写 201；无 delete 403；admin 删 204 |
| C23 匿名默认开 | ✅ | 匿名 GET 200 + sha 一致；匿名 PUT 401 |
| C24 边界 | ✅ | `/v2/` 401（M2 已挂载+挑战，反转表口径）；`/artifactory/**` 404+前缀提示；未实现 api 404 E-01；无根镜像 404 |
| C25 拒绝解包 | ✅ | 400 |
| C26（反转） | ✅ | remote generic 建仓 200（M3 §5.6）；remote+docker 400 `not supported in M3`（M05 定案） |
| C27 匿名关 | ✅ | 匿名 GET 401+`WWW-Authenticate: Basic realm="BinFlow Realm"`；admin 200；ci-bot 有 read 200/无 read 403 |
| C28 健康版本 | ✅ | `{"status":"ok"}`；version/product 如实（dev/`BinFlow`） |
| C29 重启持久化 | ✅ | SIGTERM 优雅停；制品/新口令均存活 |
| C30 慢上传中断 | ✅ | darwin 形态 `--max-time 3 --limit-rate 64k` 退出码 28；路径 404 |
| NFR-S1/S5 | ✅ | binflow.db grep 无明文口令；data 目录零 .tmp/.part 残留 |

### 2.2 M2 D 序列（curl + docker(dind) + oras + crane）

| 项 | 结果 | 证据 |
|---|---|---|
| D01 | ✅ | docker-local/charts 建仓 200×2 |
| D04 探针族 | ✅ | 匿名 401+`Bearer realm=.../v2/token,service="binflow"`；认证 200 `{}`；匿名 pull token 可签发；offline_token 接受并忽略；POST 表单凭据换 admin 主体 token（Bearer catalog 200） |
| D04b/D04c | ✅ | 手工协商 token（64 字符）；ci-bot 经 `/v2/token` 换 pull token；同一用户管理面发 token 403 |
| D05 login | ✅（dind） | `Login Succeeded`；错误口令被拒（daemon 报 unauthorized，服务端 401 不泄露存在性） |
| D06 monolithic | ✅ | POST→Location 相对路径；PUT?digest 201；单段 name 404；GET 逐位一致；`Docker-Content-Digest` 回显正确 |
| D07 单请求 | ✅ | POST?digest+body 201 |
| D08 全链路 | ✅（dind） | tag→push（digest sha256:6c2a97…）→rmi→pull→`docker run echo ok`→`ok` |
| D09 | ✅ | catalog 列 `docker-local/acme/app`；tags `[v1,v1-copy]`；`?n=1` Link `rel="next"`；last 游标互斥可枚举完；`n=abc` 400（P2）；未登记 name 404 |
| D10/D10b | ✅ | 10 段 PATCH 终结 201，cmp 一致；offset 查询 `Range: 0-1048575`（P1） |
| D11 去重继承 | ✅ | 同镜像再 push blobs 6→6 |
| D12 | ✅ | 伪 digest 400 `DIGEST_INVALID` |
| D13 mount | ✅ | 跨 name mount 201 + GET 200 |
| D13b | ✅ | 引用缺失 blob 400 `MANIFEST_BLOB_UNKNOWN`（**注**: PRD 命令 URL 缺 `/manifests/` 段，勘误 E-2） |
| FR-9-AC4 | ✅ | manifest 错 digest 400 `DIGEST_INVALID` |
| D13c | ✅ | by-digest DELETE 202→404；tag 级联清（tags:null）；by-tag DELETE 405 `UNSUPPORTED` |
| D13d | ✅ | blob DELETE 405 `UNSUPPORTED` |
| D14 kill -9 | ✅ | chunked 进行中 kill -9 → 重启 → 历史 blob 200、health ok、未终结 blob 不可见 |
| D15 Helm OCI | ✅（oras 1.2.2） | push Digest 回显；pull `IDENTICAL`；`oras repo tags` 含 1.0.0；crane 可 inspect（helm config mediaType，FR-12-AC3 P1） |
| D16 docker | ✅ | 即 D08 |
| D17 podman | ⛔ 环境限制 | podman 未安装且 darwin 需 VM——**无法验证**，非产品缺陷 |
| D18 crane | ✅（crane 0.20.3） | digest/manifest（schema2 mediaType）/实例内 copy（catalog 可见 copy/app） |
| D19 skopeo | ⛔ 环境限制（P1） | skopeo 未安装 |
| D20 冷启动 | ✅ | start→listening 0.65s（空库计时另见 G28：0.78s） |
| D22 | ✅ | ci-bot 仅 read：push `denied` exit 1；pull 正常 |
| D23 | ✅ | Bearer token revoke 后 401 |
| D24 | ✅ | `/v2/<name>/referrers/…` 404+spec 体（UNSUPPORTED）；深路径 404 `errors[].code` |
| D08b 多架构（P1） | ✅（协议层） | curl 构造 OCI index（mount blob→子 manifest→index 201）；crane 读 `application/vnd.oci.image.index.v1+json`、双子 manifest by-digest 200。buildx 形态受阻：本机 crane→docker.io DNS 被污染（IPv6 超时），且 buildkit 对 plain-HTTP 上游需 builder 级配置（T-105 已知） |
| NFR-S11 | ✅ | `/v2/../../etc/passwd/blobs/uploads/` 400；数据目录外无文件 |

### 2.3 M3 M 序列（mvn 3.9.9 / npm 10.9.8 / pip 26.1.2 + twine 7.0.0 真客户端）

| 项 | 结果 | 证据 |
|---|---|---|
| M01~M05 仓库模型 | ✅ | 三协议 local 200；remote 缺 url/file:// scheme 400×2；virtual 未知成员 400；`?type=`/`?packageType=` 过滤正确；remote+docker 400 |
| M10/M11 | ✅ | 隔离 settings（`-s`，未触碰 ~/.m2）+ `DEPLOY_OK` exit 0（pom/jar/sha1/md5/metadata 全 wire 201） |
| M12 | ✅ | jar 与构建产物逐位同；pom item sha1 40hex；`.sha1` 旁车裸 hex 40 |
| M13 | ✅ | 全新 `-Dmaven.repo.local` resolve `RESOLVE_OK`（sha1 校验随下载通过） |
| M14/M15 | ✅ | 版本组 XML 含 1.0.0；旁车 SHA1_MATCH；1.1.0 合并后 versions=2、latest=1.1.0 |
| M16 系 | ✅ | 两次 unique deploy → timestamped `demo-app-1.2.0-20260821.234728-2.jar` 直连 200；snapshotVersions=2、buildNumber=2；`-U dependency:get` SNAP_OK 解析到 buildNumber 2（**注**: 文件名需从 `<value>` 拼 `demo-app-<value>.jar`，勘误 E-3） |
| M16c/M17 | ✅ | client-checksums 伪 sha1 409；server-generated 仓 201 |
| M18 | ✅ | 正确旁车 201+回读一致；`deadbeef` 旁车 409 |
| M19/M20/M21 | ✅ | snapshot 关闭 409；layout 两例 400；Range 206 + ETag 304 |
| M22 系 | ✅ | `npm publish` `+ demo-pkg@1.0.0`；packument dist-tags/tarball（绝对 URL 含 /binflow/，与 T-74 基线同形，勘误 E-4）；whoami=admin |
| M23/M24/M25 | ✅ | install（含 cache clean 重装）；dist-tag add/ls/rm + install@beta；scoped `@acme/util` publish→install |
| M26/M27 | ✅ | 同版本重复 **403** `Cannot modify pre-existing version`；`-rev` 占位 200 恒成功且内容无变化；unpublish 后 versions 无 1.0.1 |
| M28 | ✅ | 匿名读 packument 200 / 匿名 PUT 401 |
| M30~M33 | ✅ | twine upload（真客户端）；simple index `#sha256=`、PEP629 api-version=2、三形态归一 200、无尾斜杠 302+`Location: demo-pkg/`、ETag 304、`:action=submit` 400；pip install 0.1.0 + 落盘 sha256 == index fragment；重复上传 twine E400 |
| M34/M35b | ✅（P1/P2） | 依赖链 demo-pkg==0.2.0 自动装齐 demo-lib；PEP 691 JSON files=2 |
| M41 | ✅ | pull-through FETCH_OK；二次 CACHE_HIT；上游 GET 计数 miss 仅 +1、第三次 0 |
| M42 | ✅ | 私网上游（无 allowPrivate）400；WARN 日志 `remote: outbound target rejected (ssrf-guard) ssrf-remote 127.0.0.1:9099` |
| M43 | ✅ | 负缓存：两连发上游仅 1 次 GET（**注**: python http.server 每 404 打两行，计数须按 GET 行——T-75 脚本口径复核，勘误 E-5） |
| M44 | ✅ | stale 服务带 `X-Binflow-Upstream-Error` 头（短 TTL 仓复现）；未缓存默认 404；hardFail 仓 502；health 恒 ok；上游恢复 |
| M45 | ✅（真上游） | `dependency:get junit:4.13.2` 经 BinFlow 代理 PROXY_MAVEN_OK；缓存后直连 200；`.sha1` 404 `Checksums are not downloadable.` |
| M46 | ✅（mock 腿） | 真实 npmjs 不兼容为 T-74 v1.3/E4 已证既有边界（回源请求 `<name>/packument.json` 后缀）；mock 布局下 `npm install mockpkg` + `require()` 执行 42 |
| M47 | ✅（真上游） | `pip install six` 经 pypi.org 代理成功；二次 install 命中缓存 |
| M48 | ✅ | remote PUT 405+`Allow: GET`；删缓存 204→回源 200（新仓承载，避开 300s 静默期） |
| M50/M51/M52 | ✅ | virtual 解析 local 成员 + 透传 remote 成员（junit 200）；**成员顺序交换实证**：remote-first → mock 字节 B、local-first → 本地 jar（PK 头）；virtual PUT 405+Allow |
| M57/M58 | ✅ | npm ping `{}`；nexus index/npm search/pypi json/pypi-ui 404×4 |
| M60b | ✅ | 50 并发 `pip download`（--no-cache-dir）0 失败、101×200、0 5xx |
| NFR-S18 | ✅ | `../`、`%2e%2e`、`@scope%2f..` 等变体 400；数据目录外无文件 |

### 2.4 M4 W 序列（curl REST + Playwright Chromium）

| 项 | 结果 | 证据 |
|---|---|---|
| W01/W01b | ✅ | `/binflow/` 301→`/binflow/ui/`；SPA 挂载点；深链 200；保留字 `ui` 400 |
| W02 | ✅ | 资产 `Cache-Control: public, max-age=31536000, immutable`；shell `no-cache`（**注**: 需 `make console` 后构建——纯 HEAD 源码构建内嵌占位 shell，属既定构建策略 T-89，非缺陷） |
| W03~W07 | ✅ | 未认证 whoami 401；登录 `{username:admin,admin:true}`；Set-Cookie `Path=/binflow; HttpOnly; SameSite=Lax`；错口令 401；logout 204→401 |
| W08/W37a | ✅（P1） | TTL=5s 覆盖重启后 6s 即 401；SIGTERM 重启 session 存活 200 |
| W09~W14 浏览器面 | ✅（38 过/2 挂，挂项见 D-3/D-4） | login/console-smoke/repositories/artifacts：17 过 1 挂；governance/security/auth-shell：12 过 1 挂 + grep-invert 后余 3 过；t134-g32（docker 视图/文档入口）6 过 |
| W12a | ✅ | includes/excludes 双向 409；GET 排除路径 404 |
| W14~W16 | ✅ | 搜索 `name=artifact` 命中 `/acme/artifact.bin`；缺 name 400；checksum 搜索 ≥2（实测 3）；repo 过滤空集；ci-bot 仅见有读仓库（其 ACL 下 0 条，符合授权面） |
| W17~W20 | ✅ | 组 CRUD/成员 email/未知组 400；组授权继承（写 201/删 403/越 pattern 403）；移组即时生效 403；被引用组删 409→解除后 200 且成员组联动清空 |
| W22/W22b/W23/W39 | ✅ | actor/action/since 过滤 + limit>1000 400 + 非 admin 403 + 匿名 401；分页 2+2+906=total 910；词表含 deploy/delete/repo.create/group.create/login.failed 等 20+；口令与 token 明文零命中；audit 无修改面 404×2 |
| W24/W25 | ✅ | dry-run candidateCount=109 且 blobs 383 不变；apply deleted=108==candidates；被引用 keep.jar 幸存 200 |
| W26/W27 | ✅ | 800B 配额 1024：首传 201、二传 **413**（msg 含 used/quota）；被拒文件原子（404）；usage=800；GET/DELETE 不受限、删后回落可再传 |
| W28~W32 | ✅ | 在线 export exit 0（manifest formatVersion/blobCount=277/totalBytes/metadata.sha256 对账）；import 至新目录后四协议+docker manifest parity、jane 登录、审计历史、35 仓全恢复；缺 blob → exit 1 `referenced blob … missing`；篡改 manifest → exit 1；mtime 逐位保留（**注**: PRD W30 的 `BINFLOW_DATA_DIR` env 形态有效，但须确认 env 确已设置——本轮首跑漏设导致误报，env 覆盖实测有效） |
| W36 | ✅ | search/props、storage/prune/start、v2/security/permissions、api/export 四边界 404 |
| W37 | ✅ | 100 并发 session 检查全 0；SPA 首屏由 Playwright 规格覆盖 |
| W38 | ✅ | 跨 Origin 写 403；无 Origin/同源 201 |

### 2.5 M5 G 序列（票据口径：GC/备份恢复/治理 + 本地 filestore 腿）

| 项 | 结果 | 证据 |
|---|---|---|
| G19 | ✅ | `/binflow/docs/` 匿名 200 |
| G27 | ✅ | T-147 承载口径 **P50×20 波 = 1000 请求**：匿名 1001×200/0 失败/38.8s；认证 1001×200/0 失败/44.1s；两腿窗口 5xx=0。瞬时 -P1000 形态见缺陷 D-1（Argon2 饱和） |
| G28 | ✅ | 空库冷启动 **0.78s**（<2s）；空载 60s：ps RSS 145MB（macOS 虚高，与 T-147 的 149MB 同源）、vmmap Physical footprint **73.3MB**（<100MB） |
| G30a/G30b | ✅ | 深层上传 a/b/c/d.bin 201→隐式父级 info 200→直接父目录 `?list` files=1；删深叶后空链剪除（a/ 404）；有兄弟目录不剪 |
| G30d | ✅ | mkdir 实例 export→manifest 零占位→import→folder info 200 |
| G31/G31c | ✅ | token.issue/revoke 审计在案；token 明文审计零命中；docker login+pull 后无新 token.issue（1→1） |
| G32 | ✅ | t134-g32 规格 6 过（docker 树特化视图 + help→docs 入口） |
| G33 | ✅ | `?list`/item info/permissions 同基：uri 中 repo key 恰一次、/binflow 前缀、可解析 200 |
| NFR-P3 | ✅ | 1GB PUT 201/7.7s，全程 RSS 增量 **0MB**（<256MB） |
| G01~G17 | — 未复跑 | 发布产物矩阵（goreleaser/镜像/compose/Helm/K8s/systemd/离线包）属 release-engineer 烟测域、非本地 filestore 面、M5 后无相关改动；G18 docs 离线构建面同理由不复跑（G19 挂载面已验） |

---

## 3. 客户端矩阵

| 客户端 | 版本 | 操作 | 结果 |
|---|---|---|---|
| curl | 8.7.1 | C/D/M/W/G 全 REST 面（含 docker 协议裸 curl 腿） | ✅ 全过 |
| docker | CLI 29.7.2 + dind daemon 27.5.1 | login（对/错口令）/tag/push/rmi/pull/run（删缓存重拉） | ✅ 全 0 退出（dind 承载，宿主 daemon 不配 insecure-registries——T-44 先例） |
| mvn | 3.9.9（JDK 21.0.12） | deploy 1.0.0/1.1.0/SNAPSHOT×2、resolve（全新 repo、-U）、dependency:get（代理 junit） | ✅ 全 BUILD SUCCESS |
| npm | 10.9.8 | publish/view/whoami/install（清缓存重装）/dist-tag/scoped/unpublish | ✅ 全 exit 0 |
| pip / twine | 26.1.2 / 7.0.0 | wheel 构建、twine upload（含重复拒）、pip install/download（hash 对账、依赖链、代理 six、50 并发） | ✅ 全过 |
| oras | 1.2.2（本票置备） | Helm OCI push/pull/tags | ✅ 逐位一致 |
| crane | 0.20.3（本票置备） | digest/manifest/copy（实例内） | ✅（→docker.io 因本机 DNS 污染不可达，D08b 改 curl 构造 index） |
| podman / skopeo | — | D17 / D19 | ⛔ 客户端不可得，如实标注（D19 为 P1；不猜测） |
| Playwright Chromium | 既有 web/e2e | login/console/repos/artifacts/governance/security/auth-shell/t134 | 38 过 2 挂（断言漂移 D-3/D-4） |
| 浏览器（docker 视图） | 同上 | G32 树/标签视图 | ✅ 6 过 |

---

## 4. 5xx 终扫（A/off/import/g28 全日志）

```
total 5xx: 656
  652  client-disconnect（C30 中断上传 1、D05 daemon 取消 1、G27 风暴断连 650）——均 WARN + client_disconnect:true + context_canceled
    3  501 /api/v1/storage/migration（T-164「未配置迁移」既定语义，governance 规格探测）
    1  502 generic-remote-hardfail（M44 hardFail:true 既定行为）
    0  意外
```

---

## 5. 自动化基线

| 命令 | 对象 | 结果 |
|---|---|---|
| `make test`（= `go test -race -count=1 ./...` 除 web） | **HEAD worktree（00f73e7）** | 20/21 包 ok（httpapi 330s ok、binflow-server 193s ok…）；`internal/adapter/docker` **FAIL: TestIdleSessionEviction**（review_fixes_test.go:189 `evicted = []`）——同一时段本机并行承载 QA 实例/dind/Playwright。**复跑**: `-run TestIdleSessionEviction -count=3` → 3/3 ok；整包 `-count=1` → ok 32.5s。判定负载敏感 flaky（D-2），非确定性产品缺陷 |
| `make test` | 主工作树（含在途 T-190：internal/auth/token.go、httpapi/security.go、router.go 等） | **FAIL `internal/httpapi`**（并发跑时只见包级 FAIL；具体用例名未留存——首轮管道截断）。**警示 conductor**: T-190 合入前须在全量套件下自证全绿，勿以本票基线背书 |

---

## 6. 缺陷清单（不修码，开票建议）

| # | 级别 | 摘要 | 复现 / 期望 vs 实际 |
|---|---|---|---|
| D-1 | **P1** | **Basic 认证 Argon2 风暴拖垮可用性**：每请求全量 argon2id（m=64MB,t=1,p=4，M1 以来未变），无并发闸、不随 ctx 取消、无凭据缓存。瞬时大并发认证 + 客户端批量断连后：服务端 CPU 100~337%、RSS 飙至 **3.9~4.8GB**、~800 个 CLOSED 套接字各挂不取消的推导，**新连接（含匿名 ping）饿死 10 分钟以上**；且风暴后 RSS 长期不回落（Go 堆不还页，无 GOMEMLIMIT） | 复现（本机）：`seq 1000 | xargs -P 1000 -I{} sh -c 'curl -sf -u admin:<pw> --max-time 30 -o /dev/null http://127.0.0.1:18080/binflow/generic-local/perf/$(({}%50+1)).bin'` 后 `curl ping` 超时、ps 观察 RSS/CPU。期望：认证开销有界（信号量/限速/凭据短 TTL 缓存/ctx 感知验证），拒绝或排队但不饿死匿名面。注意：T-147 的 P50×20 波口径在安静系统通过（本轮亦复证 1001×200/零 5xx），故不阻塞 GA 基线，但属真实健壮性缺口 |
| D-2 | P2 | `TestIdleSessionEviction`（internal/adapter/docker）负载敏感 flaky：全量套件 + 并行负载下必挂，隔离跑稳定过 | `make test`（同时跑 QA 实例/dind）vs `go test -race -count=3 -run TestIdleSessionEviction ./internal/adapter/docker/`。期望：时序断言容忍调度抖动（fake clock 或放宽窗口） |
| D-3 | P2 | `web/e2e/repositories.spec.ts:52`（删非空仓 reason 断言 `holds 1 node(s)`）在 HEAD 红：ADR-0016 目录实体化后 `release/app.bin` = 2 nodes | `cd web && npx playwright test e2e/repositories.spec.ts`。行为本身正确（reason 呈现 + deleteContent 预勾选）。期望：断言改 `holds [12] node(s)` 或精确 2 |
| D-4 | P2 | `web/e2e/governance.spec.ts:191`（GC 用例）在 HEAD 红：UI 探测 `/api/v1/storage/migration` 得 501（T-164「未配置」既定语义），规格未放行该码 | `npx playwright test e2e/governance.spec.ts`。期望：规格将 501-not-configured 列为预期态（或端点改为 200+状态体——需 product-manager 定口径） |
| （观察 O-5） | P2 | `GET /binflow/api/v1/remote/stats`（RE-11/M59，P2）404 `not implemented` | 优雅降级符合 E-26；若 M6+ 需要该运维面再立票 |

**在途警示（非本票缺陷）**：主工作树 T-190 改动使 `internal/httpapi` 全量红——T-190 收口前不得引用本基线作绿背书。

## 7. PRD 勘误建议（不改行为，改剧本）

1. **E-1**（M1 C17）：`?list` 载体应写子目录（根级 `Cannot list files of root.` 为 v1.2 定案，T-18 O3）。
2. **E-2**（M2 D13b）：命令 URL 缺 `/manifests/` 段——`$REG/v2/docker-local/acme/broken:m1` 应为 `.../broken/manifests/m1`。
3. **E-3**（M3 M16）：timestamped 文件名断言应从 version 级 metadata `<value>` 拼 `demo-app-<value>.jar`（XML 不含完整文件名）。
4. **E-4**（M3 M22b）：`dist.tarball` 实为绝对 URL（含 /binflow/，BaseURL 重写），断言「以 /binflow/ 开头」应放宽为「含 /binflow/ 前缀路径」。
5. **E-5**（M3 M43）：python `http.server` 每 404 请求打两行日志（code 404 行 + GET 行），上游计数断言必须数 GET 行。
6. **E-6**（M4 W30）：`BINFLOW_DATA_DIR` env 覆盖有效，但剧本应提醒确认 env 已 export（漏设时 import 打到 config 的 data_dir 且以「目录非空」退出）。

## 8. 环境清理记录

- 容器：t172-dind 已 `docker logout`（admin/ci-bot 两凭据）后 rm -f；`docker:27-dind` 镜像已 rmi；宿主 crane/oras 凭据已 logout（`~/.docker/config.json` 中 18080 引用 0 条）
- 进程：实例 A / import 实例（18082）/ mock 上游 9099 全部停止；18080/18082/18085/9099 无监听残留
- 工作区：`git worktree remove --force /tmp/t172-work`（make console 产物随之清除）；`/tmp/t172`（数据/日志/venv/二进制）已删
- 开工前清理：删除遗留孤儿 go-build 测试进程（PID 18572，占 18080）；未触碰他人实例（arca 容器、PID 56638/56678 的 binflow 实例、kind 集群）
- 主仓工作树零改动（本票只读 + 报告落盘）

## 9. 未复跑项及理由（防口径争议）

| 项 | 理由 |
|---|---|
| D17 podman / D19 skopeo（P1） | 客户端在本机不可得；docker/crane/oras 已覆盖同协议面。**BLOCKED-环境，非产品问题** |
| D08b buildx 形态（P1） | 本机 crane→docker.io DNS 污染 + buildkit plain-HTTP 需 builder 配置（T-105 已知）；已用 curl 构造 OCI index 在协议层验证（§2.2） |
| M35 wheel+sdist 并存（P1）、M53~M55（P1）、W30b remote 凭据密文随行（P1） | P1 面，M3/M4 已证且 HEAD 无相关改动；本轮 P0 优先 |
| G01~G18、G22~G26、G29b | 发布产物/文档构建/安全审计面：release-engineer 与 security-auditor 域，非本地 filestore 回归范围，M5 后无改动 |
| NFR-S2（token 只存哈希） | C21 token 在 DB 中 grep 无明文已附带验证（§2.1 NFR-S1 同法） |
