# T-142: FR-41 内容（下）：API 参考、FAQ 扩写、管理页增强

- **Ticket**: T-142 [P0]
- **角色**: tech-writer
- **状态**: done
- **工作目录**: `/Users/lzw/dev-center`

## 完成内容

### 1. API 参考文档（新建）
- **文件**: `/Users/lzw/dev-center/docs/user/api-reference.md`
- **侧边栏**: `/Users/lzw/dev-center/docs-site/sidebars.js` — 已取消 `api-reference` 标注的注释

覆盖内容：
- 兼容端点总表（E/DE/ME/NE/PE/SE/SR 七个域 + 仓库管理 + 系统 + 会话）
- `/api/v1` 自有端点表
- 三种认证方式（Basic / Token Bearer / 会话 Cookie）各带 curl 示例
- 三种错误信封格式（errors[] JSON、纯文本、OAuth）各带至少一个真实示例
- 请求/响应头参考表（通用头 + 各协议专用头）
- 审计日志查询参数与响应格式
- GC 触发参数与响应格式

### 2. FAQ 扩写
- **文件**: `/Users/lzw/dev-center/docs/user/faq.md`
- 追加内容：
  - Docker 快速通道（push/pull/auth/tag 管理/HTTPS 问题）
  - 通用制品上传下载（curl 单行 + checksum 声明 + 校验和验证）
  - Maven settings.xml 最小配置 + deploy/resolve/mirror 三形态
  - npm .npmrc 配置 + publish/install
  - PyPI pip.conf + .pypirc + upload/install
  - 部署方式选择指南（7 种场景表格 + 选择关键）
  - 常见报错速查表（401/403/404/409/413/网络基础设施分类）
  - 性能与资源（高 QPS Token 建议、存储空间、备份）
  - 日志与诊断（三件套命令）
  - 从 Artifactory 迁移三步走
  - 原有「M4 有意不兼容清单」与「迁移对照表」已存在且完整，未重复追加

### 3. 治理指南增强
- **文件**: `/Users/lzw/dev-center/docs/user/admin/governance.md`
- 追加/增强内容：
  - 用户管理 CRUD（create/replace/query/list/update — DELETE 未实现已备注）
  - 组管理（create/update/delete 含 409 保护、组名规则）
  - 权限模型（principals/patterns/actions 并集、即时生效、组不可提权到 admin）
  - 有效权限视图（`?permissions` 参数）
  - Token 审计（签发/吊销命令、已知 M4 缺口、docker token 同表说明）

### 4. 备份恢复 DR 流程增强
- **文件**: `/Users/lzw/dev-center/docs/user/admin/backup-restore.md`
- 原页面已有基础备份/恢复内容（在线 export/import、BINFLOW_REMOTE_CREDENTIALS_KEY、常见报错表）
- 追加内容：
  - 灾难恢复标准流程（6 步骤）
  - 恢复后验证清单（8 项表格）
  - 恢复前排查（密钥/schema/空目录/完整性四检）
  - 备份策略建议（每日/每周/每版/异地频率表）
  - 恢复演练脚本（每季度完整演练）
  - 一致性窗口含义说明

### 5. 控制台指南
- **文件**: `/Users/lzw/dev-center/docs/user/console.md`
- 评估：原页面已覆盖完整走查（六组页面、角色可见性、浏览器兼容、有意不兼容清单）——树视图、搜索、上传对话框等细节在 M4 已经完备，无需额外增强。

### 6. 构建验证
- `make docs` 构建成功，产出 `internal/docs/dist/` 1.83MB
- 预存警告全部来自 T-141 区域的未创建页面（install/binary.md 等），与 T-142 内容无关

## 构建验证

```bash
cd docs-site && npm ci && npm run build
# [webpackbar] Server/Client: Compiled successfully
# [SUCCESS] Generated static files in "build".
rm -rf internal/docs/dist/assets internal/docs/dist/search
cp -R docs-site/build/. internal/docs/dist/
# docs site size (raw total): 1.83 MB (1917480 bytes)
```

无新增告警或链接错误（所有警告均为 T-141 区未创建文件或已存在的跨仓库链接）。

## 文件变更清单

| 操作 | 文件 |
|---|---|
| 新增 | `/Users/lzw/dev-center/docs/user/api-reference.md` |
| 编辑 | `/Users/lzw/dev-center/docs-site/sidebars.js` |
| 编辑 | `/Users/lzw/dev-center/docs/user/faq.md` |
| 编辑 | `/Users/lzw/dev-center/docs/user/admin/governance.md` |
| 编辑 | `/Users/lzw/dev-center/docs/user/admin/backup-restore.md` |
| 已评估（无需改） | `/Users/lzw/dev-center/docs/user/console.md` |

## 备注

- `docs-site/docs/` 目录不存在，Docusaurus 配置 `path: '../docs/user'` 引用 `docs/user/` 作为内容源
- `make docs` 构建流程：Docusaurus build → 拷贝到 `internal/docs/dist/`（go:embed）
- 所有新增/修改内容均未触及 T-141 区域（install.md 等）
- 构建警告列表中 `getting-started/binary.md` 等为 T-141 待创建文件，属预期