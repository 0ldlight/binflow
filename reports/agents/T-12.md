# T-12 工作日志 — repo.Service：local 用例编排与仓库 CRUD

- role: dev-go-core
- 日期: 2026-08-18
- area: `internal/repo`（严格未越界；依赖包只读）
- 票据: BOARD.md T-12（AC 全文见 reports/agents/T-6.md T-12 节）
- 结论: **done**（自测全绿，证据见下）

---

## 一、交付物

| 文件 | 内容 |
|---|---|
| `internal/repo/api.go` | Service 接口（Get/Put/Delete/List + CreateRepo/GetRepo/ListRepos/UpdateRepo/DeleteRepo）、sentinel 错误全集、`Principal = auth.Principal` / `AuditEvent = audit.Event` 别名、Authorizer/AuditLogger 消费侧接口、`New` / `NewWithClock` 构造 |
| `internal/repo/validate.go` | repo key / rclass / packageType / node path 校验、folder 判定、parentPrefix 等纯函数 |
| `internal/repo/service.go` | 全部用例实现：Put 会话编排与 blob-first 元数据写入、幂等重传/覆盖/目录/递归删/prune/删仓 |
| `internal/repo/repo_test.go` | 28 个测试函数（含子测试共 136 用例；review 修复后新增 4 函数，见第七节），全部 table-driven 或分支驱动 |
| `internal/repo/fakes_test.go` | 测试基座：真 storage + 真 SQLite metadata（高保真）；policyAuthz/auditLog 记录器；hookStore 错误注入 + 写序 journal 装饰器 |

## 二、关键实现决策（对照 AC）

### AC1 Put 事务边界（正确性核心）

严格按架构 §3.3 + §3.2（T-25 a 案裁定）实现：

```
BeginSession → Append(reader) → Commit(expect)     ← 物理 blob 先就位
  ↓ (成功后)
blobs 行（ON CONFLICT DO NOTHING）                   ← blob-first 第二步
  ↓
nodes 行（upsert by (repo_key, path)）               ← 最后
```

- **反向被禁止且被 FK 兜底**：nodes.sha256 → blobs.sha256 外键使「先 node 后 blob」在数据库层不可能成功。
- **崩溃窗口**：Commit 后、元数据前崩溃 → 残留无引用 blob，GC 宽限期回收（ADR-0006 设计路径）；绝不产生指向不存在 blob 的 node。
- 两条**顺序敏感断言**固化该不变量（见测试节）。
- 存储层 sentinel（ErrChecksumMismatch/ErrSessionPoisoned/…）经 `%w` 原样透传，httpapi 可判。

### AC2 校验矩阵

- repo key：`[a-z][a-z0-9-]{1,62}`（PRD FR-3-AC4；T-22 回写后的 {1,62} 口径，总长上限 63）。
- 保留字：`api`、`v2` 精确匹配拒绝（ADR-0008）——`api-local` 合法（只是路由段前缀撞车才保留）。
- rclass：local 唯一可服务；remote/virtual → `ErrRepoTypeNotSupported`，message 含 "supported from M3"（400 形态归 httpapi 翻译，语义留在服务层）。
- packageType：generic 唯一（docker/maven/npm/pypi → 同上 M3 语义）；未知值 → `ErrInvalidRepoType`。
- UpdateRepo：description/config 可改，**type 与 packageType 不可变**（变更会静默改变全部 adapter 路由决策）。

### AC3 local 语义（repo-semantics.md 吸收）

| 规格（§3/§4，高置信度） | 实现 |
|---|---|
| 同路径同 checksum 幂等重传 → **免覆盖检查也免 deploy 权限检查**，保留 created/createdBy | `Put` 在权限门**之前**先做 `isIdempotentRedeploy` 判定；命中则跳过全部检查直接走 metadata 刷新路径。测试固化：**零权限用户** alice 重传同内容成功（spec 原文「无 deploy 权限也可完成」） |
| 同路径不同 checksum（或未带 checksum）= 覆盖，需对旧 node 有 DELETE 权限 | 覆盖检查先于 write 门；拒绝 message 含 `needs DELETE permission`（对齐规格文案形态） |
| 覆盖保留 created/createdBy、更新 modified 系 | putNode 的 overwrite 分支；测试用注入时钟断言 UpdatedAt 移动、CreatedAt/CreatedBy 不动 |
| Delete 只删 node 引用（blob 从不删，交 GC） | Delete/DeleteByPrefix 只动 nodes；测试断言 blob 文件在 node 删除后仍可 Stat |
| 路径不存在 → 404 幂等 | `ErrNodeNotFound`（服务层 sentinel；metadata 层的等价物不外漏） |
| 目录递归删 + prune 空父目录 | 尾斜杠路径 → DeleteByPrefix 子树删 + `pruneEmptyParents` 沿父链上溯；有存活子节点立即停；folder 行最后删（崩溃只可能多留不可能多删） |
| 删仓：非空不带参数 → 报错含 `deleteContent` 提示；带 true → 全删 | `ErrRepoNotEmpty` message 原文含 `deleteContent=true`（FR-3-AC5） |

**目录 node 表示**（M1 骨架自定，规格未约束存储形态）：nodes.sha256 列 NOT NULL + FK，目录行共享一个固定 marker sha256（64 个 '0'）+ 对应 blobs 行（ON CONFLICT DO NOTHING，全局一条）。目录的 Get 返回 `(nil, node, ErrIsFolder)`——adapter 以此渲染 FolderInfo。marker 从不可经 storage.Open 读到（无物理文件）。

### AC4 测试（全分支）

**基座选型**：按票据建议用**真引擎 + 临时目录**（storage.OpenEngine + metadata.Open on t.TempDir），集成度最高；仅错误注入与写序断言用装饰器包装真 Store（hookStore：journal 记录每次子库写操作的顺序 + 按操作名注入失败）。

覆盖清单（24 函数）：
- key 校验矩阵 16 例（非法字符/超长/保留字/边界长度）；类型矩阵 11 例（remote/virtual/docker/maven/npm/pypi → M3；未知值 → invalid）
- CRUD 生命周期（重复建/未知 key/不可变字段/列仓排序/删空仓）
- 管理面权限矩阵（匿名/非 admin × create/delete/get/list/update）
- Put/Get 往返、校验失败透传（storage.ErrChecksumMismatch）、路径校验 12 例
- **幂等重传免权限**（零权限用户成功）、**覆盖需 DELETE**（缺权限 403 + message 断言；授权后成功且 UpdatedAt 移动、CreatedAt 保留、旧 blob 行留存待 GC）
- 目录 node、非空 body 拒绝、marker 不可读、Get(folder) → ErrIsFolder
- 文件删/幂等删、目录递归删（含前缀兄弟 `d` vs `dx` 不误删）、prune 空父（全部剪除 vs 有存活子即停）
- 删仓两分支（拒绝时仓与 node 均存活；deleteContent=true 后全部消失；空仓直删）
- **事务边界三件套**：
  1. `TestPutMetadataFailureRollsBackNode`——注入 nodes.put 或 blobs.put 失败 → node 不存在；重传成功（一次性注入模拟瞬态故障）
  2. `TestPutBlobFirstOrder`——journal 断言 blobs.put 先于 nodes.put（T-25 a 案）
  3. `TestPutPhysicalBlobBeforeMetadata`——blobs.put 触发时物理 blob 文件必须已在磁盘（§3.3 反向禁止）
- 读权限矩阵、nil Authorizer fail-closed、审计尽力而为（失败不阻断）+ 事件序列

## 三、依赖与并发事件记录

1. **T-11 在本票执行中途合流**（auth 与 audit 两包）。已全部对齐为真类型别名：
   - `type Principal = auth.Principal`；action 常量改引 `auth.ActionRead/Write/Delete`——与架构 §3.3/§3.4 签名完全一致，`auth.Service` 结构化满足 `repo.Authorizer`，T-16 装配可直接 `repo.New(eng, md, authSvc, auditSvc)`。
   - `type AuditEvent = audit.Event`；audit action 常量改引 `audit.Action*` 词汇表（audit.Event 比 §3.5 多一个 RemoteAddr 字段，服务层不填、由 Logger 侧补）。
2. **发现并绕过 T-11 的临时 import cycle**：T-11 落地中期 `internal/metadata/password.go` 直接 `import internal/auth`，而 auth 的 token.go 又 import metadata → `go build ./...` 循环报错。T-11 侧随后改为 injectable hook 方案（`RegisterHasher`/`RegisterVerifier`），循环解除，模块级构建与测试最终全绿。**本票未改动任何依赖包文件**。

## 四、自测证据

```
$ go test -race -count=1 ./internal/repo/
ok  github.com/lzwzzy/binflow/internal/repo  16.5s        （当时 24 函数 / ~125 用例；review 修复后 28 函数 / 136 用例，见第七节）

$ go test -race -count=1 ./internal/repo/ ./internal/storage/ ./internal/metadata/
ok  internal/repo       23.6s
ok  internal/storage   106.2s
ok  internal/metadata   19.6s                              （依赖包无回归）

$ golangci-lint run ./internal/repo/...
0 issues.

$ gofmt -l internal/repo
（空）

$ CGO_ENABLED=0 go build ./... && CGO_ENABLED=1 go vet ./...
（通过；vet 无输出）

$ go test -race -count=1 ./internal/...
ok  internal/config     4.6s
ok  internal/metadata  25.6s
ok  internal/repo      31.1s
ok  internal/storage  118.6s
（console/httpapi 无测试文件；全模块零失败）
```

## 五、规格依据（docs/reverse/）

- repo-semantics.md §3（覆盖/禁止覆盖语义，高置信度）：同 checksum 幂等重传免覆盖权限检查、不同 checksum 需 DELETE、覆盖保留 created/createdBy 更新 modified 系——**直接实现并测试固化**。
- repo-semantics.md §4（删除 9 场景，高置信度）：只删引用、404 幂等、目录递归 + prune 空父、仓库根与删仓区分——直接实现。低置信度项（blackedOut 404、`atomic` 参数中间态）不属本票 AC，未实现未猜测。
- repo-semantics.md §6：includesPattern/excludesPattern 的 M1 行为按「BinFlow 自有语义」经 Authorizer 注入实现（本包不做 pattern 匹配，归 auth 包）。
- 架构 §3.3（Put 事务边界）、§3.2 a 案（blob-first 两语句 + FK 兜底）、ADR-0006（GC 宽限回收路径）、ADR-0008（保留字 api/v2）、PRD FR-3-AC4/AC5。

**低置信度/自定行为标注**（规格缺位，按 BinFlow 自有语义实现）：
- 目录行的存储形态（marker sha256）与 `ErrIsFolder` 信号——规格只约束 HTTP 行为（mkdir 201/FolderInfo），存储形态为 BinFlow 自有设计。
- UpdateRepo 的不可变字段集合（type/packageType 不可变、description/config 可改）——规格未明说，按「不可静默改变路由决策」原则取最保守面。

## 六、遗留

1. **T-11 import cycle 余波（已自解，建议 review 复核）**：本票执行中 metadata↔auth 一度循环（password.go 直 import auth ↔ token.go import metadata）；T-11 侧改为 injectable hook（RegisterHasher）后 `CGO_ENABLED=0 go build ./...` 与全模块测试均通过。建议 conductor 在 T-11 review 时确认 hook 注册路径（`RegisterHasher` 的调用方）确实闭环。
2. DeleteRepo 用 `ListByPrefix("").length` 判空（全表载入）。M1 规模无碍；大仓场景建议 metadata 层补 `CountByPrefix`（接口变更走 architect）。
3. 规格低置信度项（blackedOut 上传拒绝、`?atomic=true`）未实现——M1 无对应配置面，留待 M3 remote 票。
4. **（review 转达，T-14/15 范围）`%2e` 等百分号编码变体必须在 httpapi 层先 decode 再进 Service**——否则 `validateNodePath` 的 dot-segment 防御可被 `%2e%2e` 绕过。conductor 已记入 T-14 派单要点，此处留档。
5. 覆盖判定 TOCTOU（review M1，非阻塞）：t0 不存在（只查 WRITE）→ 写入时已存在的窗口下，无 DELETE 权限者可覆盖他人刚上传内容。单实例 SQLite 串行化下窗口极窄，M1 接受；M2 条件 upsert（`WHERE sha256 = 旧值`）时一并解决。
6. 幂等重传刷新 Mime（review nit，非阻塞）：零权限幂等重传会更新 node 的 Mime 字段。保留现行为（"modified 系"解读），如需保留原值属一行改动。
7. UpdateRepo 无法区分「未提供 description」与「清空 description」（review nit）：只想改 config 的调用会清掉描述。M1 调用方（httpapi PUT 全量语义）不受影响，如需字段级更新在 T-15 层做 merge。

## 七、review 修复记录（2026-08-18，correctness review REQUEST_CHANGES → 已修复）

review 报告：`reports/agents/T-12-review.md`。事务边界/并发/校验矩阵/写序断言被证实过硬；两个 blocking 同根因——**带尾斜杠前缀进 metadata 的 likePrefix 会生成 `"d//%"` 子树臂，因双斜杠被校验禁止而永不命中任何路径**。

### 修复前先复现（reviewer 探针等价复跑，均 CONFIRMED）

- B1：`d/`（显式目录行）+ 删 `d/one.bin` + `d/two.bin` 存活 → `d/` 行消失而 `d/two.bin` 仍在（数据丢失级）。
- B2：`List("d")`=2 行 vs `List("d/")`=1 行（契约违反）。

### B1（blocker）pruneEmptyParents 检查失效 — 已修复

- 根因：`parentPrefix` 返回带尾斜杠前缀 `"d/"`，`ListByPrefix("d/")` 的子树臂 `"d//%"` 永不命中 → 循环里只剩目录行自身（被 `c.Path == folder` 跳过）→ 目录被误判为空 → 删除并沿父链一路误删。
- 修复：查询前 `strings.TrimSuffix(folder, "/")` 归一化（service.go pruneEmptyParents；对照同函数族 DeleteByPrefix 调用处既有的显式 TrimSuffix，确系疏漏）。exact 臂若命中同名**文件** `"d"` 视为存活子节点停下（安全方向）。
- 回归测试 `TestPruneKeepsFoldersWithLiveChildren`（3 子测试）：叶子删后显式目录行 + 存活子节点俱在、子树删后共享父目录行 + 兄弟文件俱在、真空链仍剪到根。

### B2（major）List 双形态不一致 — 已修复 + 日志勘误

- 修复：`Service.List` 入口对 prefix 做 `TrimSuffix("/")` 归一化；`"/"` 归一后为空 → `ErrInvalidPath`（根不可作为列表前缀，`""` 才是全仓）。api.go 契约文本同步更新。
- 回归测试 `TestListPrefixFormsEquivalent`：`"d"` 与 `"d/"` 各 3 行且集合相同、深层 `"d/sub"` 等价、`"/"` 拒绝。
- **勘误本日志原遗留 #2**：~~「"a" 与 "a/" 等价——metadata 层 likePrefix 的既有语义」~~ 与事实相反——修复前两形态**不等价**（`"a/"` 只返回目录行自身）。该声明已从遗留清单删除；T-13 adapter 票可放心依赖「两形态等价」契约。

### 顺手修复（review 建议，不阻塞但同源或零成本）

- **M2 目录删误伤同名文件**：`Delete("d/")` 的子树删除改为「先 ListByPrefix（剥斜杠形式）取候选，进程内过滤 `v.Path == "d/"` 或 `d/` 前缀，逐行精确删」——同名文件 `"d"`（与目录合法共存）被豁免。测试 `TestDeleteFolderSparesSameNamedFile`。
- **M3 folder deploy 先耗 body 再判权**：写权限门提前到 `countReader` 之前；`countReader` 读错误不再吞掉（"0 字节即出错"不再被当作空 body 放行）。测试 `TestFolderDeployChecksWriteBeforeBody`（含 errReader 证明 body 未被消费）。
- **M4 递归删权限粒度**：service.go 目录删处补注释，明示 M1 简化（仅对目录路径判一次 DELETE，不逐子节点复核；仅授权 d/ 未授权 d/sub/ 者可删 d/sub/ 内容），留 M2+ 选项。
- **nit CreateRepo 判重竞态**：并发同名建仓时主键约束失败兜底映射为 `ErrRepoExists`（`metadata.ErrDuplicate` 或 UNIQUE constraint 消息），httpapi 可稳定映射 409。

### 修复后全量验证

```
$ go test -race -count=1 ./internal/repo/
ok  github.com/lzwzzy/binflow/internal/repo  32.6s     （28 测试函数 / 136 用例全绿）

$ go test -race -count=1 ./internal/...
ok  internal/auth      34.2s
ok  internal/config     3.2s
ok  internal/metadata  25.1s
ok  internal/repo      32.6s
ok  internal/storage  111.7s
（全模块零失败；console/httpapi 无测试文件）

$ golangci-lint run ./...
0 issues.

$ gofmt -l internal/repo
（空）

$ CGO_ENABLED=0 go build ./... && go vet ./...
（通过）
```

新增测试名汇总：`TestPruneKeepsFoldersWithLiveChildren`（leaf delete keeps folder with live sibling / subtree delete keeps shared parent folder / fully empty chain still prunes to root）、`TestListPrefixFormsEquivalent`、`TestDeleteFolderSparesSameNamedFile`、`TestFolderDeployChecksWriteBeforeBody`。

未修（记入遗留 5–7）：M1 覆盖判定 TOCTOU、nit 幂等重传刷新 Mime、nit UpdateRepo description 清空歧义——均为 review 明示 non-blocking，M1 接受并在上表留档。
