# T-347 工作日志 — FR-112.1 architecture.md §15.4/§23 回写（M12 回写批·architect 腿）

- 票：BOARD L1119 `T-347 [P1] FR-112.1 architecture.md §15.4/§23 回写`（docs-only）｜dep：T-346 ✓（已合入，PR#38/commit 322483c）
- 角色：architect；area：docs/design/architecture.md + 本日志
- 日期：2026-08-29 ｜ 状态：done（自检见 §4）
- 纪律：共享工作树**零 git 写操作**（只读 `git log/show/diff` 用于历史取证）；未触碰 T-347A（tech-writer，docs/user/ 域）与 T-348（reverse-engineer，docs/reverse/ 域）任何文件。

## 1. 回写清单（AC1——按文件位置列 diff 面）

文件：`docs/design/architecture.md`（2470 行 → 2734 行，净增 264 行；四处原位重写〔表行 2/4/5/6〕+ 其余增补，git diff 以 conductor 收口时为准）。

| # | 位置 | 动作 | 内容 |
|---|---|---|---|
| 1 | 文档标题行（L1） | 增补 | 加注「M11 增量未新增章节号——票据级 as-built + ADR 承载；M12 操作域/MPU/remote 字段/可观测回写见 §15.4 与 §23 [M12]」 |
| 2 | §7.1 路由表 MPU 行（原 L1316-1319） | **原位重写** | T-289 旧八臂行（JSON 体 create/config、GET urlPart/status 路径 id、complete 201+sha256、routeAuth+501 八臂）→ **ADR-0039 Artifactory 形六端点 + part**（QueryParam create / GET /config 探测 / POST urlPart?partNumber / POST status / POST complete?sha1=202 / POST abort 204 / PUT part?token=200）；认证双车道与 filestore config=200{false} 边界入行 |
| 3 | §7.1 路由表 | 新增一行 | [M12] 制品操作域 + 回收站路由族（copy/move/archive-download/trash 三动词；槽门 + trash 的 manage=system:write 路由门 + 浏览骑 storage 面注记）→ 指针 §15.4.2/§15.4.3 |
| 4 | §7.1 门模型 bullet（原「内容面族新增：八臂 routeAuth」） | **原位重写** | 旧「八臂 routeAuth + 会话 id capability + bare status 列表（K29/ADR-0032 定案段）」**退役**；新双车道模型（create/config 常规门；urlPart/status/complete/abort+part = `isMPUCapabilityRoute` 豁免 + handler 自裁；capability 语义改由 `<32B hex>.<sid>` token + caller blob v2 承载） |
| 5 | §15.4 整节 | **整体重写** | 标题「快赢包挂点」→「操作域与 MPU 面 as-built」；两行规划挂点文字移除（历史指向 ADR-0039 + M10 commit 3fcd74f）；新增 MPU Artifactory 形 as-built 块（端点表/token 模型/异步 complete 任务模型/create 范围/乱序分片重排暂存 64 片·128MiB/后端边界/已知残余） |
| 6 | §15.4.1 | **终态重写**（PRD §1.3 时序条款兑现——T-346 已合入） | remote 字段终态：**canonical = `socketTimeoutMillis`**（xsd 拼写，默认 15000；T-346 翻转），`socketTimeoutMs` 降输入别名（只进不出）；**行列名 `socket_timeout_ms` 不变**（014 列 = fetcher 权威源）；解析序 014 列 > canonical JSON > legacy secs > 默认；存量行 append-only（下次 PUT 翻新，无迁移）；missedRetrieval 拼写条款、unused cleanup 引擎已生效指针（§23.1）；MPU as-built 块自本节上移至 §15.4 本体 |
| 7 | §15.4.2（新增） | 新增 | 制品操作域：copy/move 端点/参数/响应（vendor CT + messages[] + 聚合状态码规则）、**五段管线**（parse→precheck→validate 六链→transfer 零拷贝→assemble + copy-only CopyMoveObserver）、仓型语义、repo-operations 槽（pro，整族一槽）；归档族三面（archive/download 六默认+执行序+流式打包、`archive!/` 首切分+嵌套 256MiB+checksum 后缀+骑 Get、explode 白名单闭集+router 层拦截 E-25 反转+201 空体+Atomic 补偿回滚） |
| 8 | §15.4.3（新增） | 新增 | Trash can：auto-trashcan 懒落库 + 布局 + **属性五元组**；**捕获语义 fail-closed**（Delete local 臂权限门后、行删除前；失败即中止）+ 跳过集闭集 + 不捕获面；**`_system_` 身份契约**（SystemPrincipal 身份即豁免 + SystemIdentity/ExactPath 双内部旗仅进程内）+ 观察者豁免；REST 三动词 + 浏览骑 storage 面 + 门序（manage=CapSystemWrite 路由门 + RequireAddon 处理器首行，RBAC 先于 license）；恢复三级目的地 + 剥标记；保留期引擎（1h tick + updated_at 回退）+ **GC/cleanup 双免疫论证**；系统仓守卫；trashcan 槽 pro 暂行（Q3 建议 community，翻转点注明）+ 零值=M11 硬删 |
| 9 | §15.4.4（新增） | 新增 | checksum-deploy token 窄域（T-349/FR-113.3，ADR-0039 残余①闭账）：migration 017 `deploy_scope` 列 + 自描述 scopeDoc{kind,repos,path}；IssueChecksumDeployScoped 铸造域（唯一落地 PUT）；**校验 fail-closed**（畸形/未知 kind/空坐标一律 ERROR 永不静默 nil）；执法点 middleware（scoped 凭据唯一 PUT 放行其余 403；窄域是减法面非旁路）；与 §3.4 Authorizer 词表零交集 |
| 10 | §11.42 / §11.43 | 追加状态注 | 42：cleanup 引擎已生效 + canonical 终态指针；43：kill -9 续传**已兑现**（caller blob v2——M11 descope 注记闭账）、中继 URL 姿态沿用、presigned 直传债维持 |
| 11 | §11.44 / §11.45（新增债两条） | 新增 | 44：协议索引联动观察者未接线（SystemPrincipal 契约已备、适配器导出缝缺——T-339 AC2 归属）；45：folderDownloadConfig/trashcan 配置旋钮未进 internal/config（生产恒默认关/恒 14d） |
| 12 | §23（新增整节） | 新增 | 见 §2 |

## 2. §23 内容摘要（监控/可观测 as-built）

- **编号承接说明**（节首注）：T-332 转交记录写「§15.4/§23」但 M11 未新增章节号（票据级 as-built + ADR 承载，§16~§22 空号保留）——本段固定编号 §23 = PRD FR-112.1/BOARD T-347 的回写锚点。
- **§23.1 cleanup 引擎与指标**（M11 T-324）：三腿单锁（SessionSweeper / policy 删除——在用 oracle = 审计 trails ∪ virtual 成员下载 / GCSweep apply + grace 递延）；零孤儿保证 + 惰性残留自愈；REST `POST/GET /api/v1/system/cleanup`（dry-run 默认，system:write/read 门）+ 每小时 cron；指标 `binflow_cleanup_objects`/`binflow_cleanup_bytes`（gauge 型 scrape 快照、无 `_total`、未接线族不暴露）。
- **§23.2 replay 七指标族**（M12 T-338/ADR-0040 + 装配 wiring）：**七枚实名表**（`binflow_replay_queue_depth` / `_window_open` / `_drained_total` / `_failed_retry_total` / `_failed_permanent_total` / `_source_gone_total` / `_read_fallback_total`——counter 语义以 gauge scrape 快照发布，dual-write 独有）；**注明 ADR-0040 决策 8 草案名 `binflow_storage_replay_*` 与三 outcome 合一 counter 未采用**（as-built 权威 = 本表 + metrics.go）；审计两事件（storage.replay.window/drained，装配层 facet 发射）；启动水位 INFO + completed+队列非空拒启 gate；migration status 形状冻结与「队列不是引用不是 hold」重申。
- **§23.3 e2e 专用 CI job**（T-350/K46）：`.github/workflows/ci.yml` `e2e` job 形态（专用不共享 runner、真二进制默认参数启动〔FR-6-AC5〕、真 REST 面种子、三 project 全量、workers=2 升档证据条款、main-only）；**权威信号口径**——负载 flake 家族权威 = 本 job，本地「串行重跑绿 = 过」维持，**分歧时 CI 胜**（本地红 = 隔离性发现）；与 Go 级 race gate（§14.2-6）互补不可互替的永久耦合注记。

## 3. 交叉引用核对（纪律③——ADR-0034/0039/0040）

| 核对项 | 结论 |
|---|---|
| ADR-0039 六端点 ↔ router.go as-built（L478-491 + 豁免谓词 L250-256） | **一致**。六端点动词/QueryParam/sha1/202/204 与路由表逐点核对通过；§15.4/§7.1 回写按 as-built 落笔。 |
| ADR-0039 决策表 part 行「202 会话回显」 | ADR **内部陈旧**：其「后果」节真客户端修形②已翻 **200**（202 被客户端当失败重试）——ADR 只追加不删改，本票以后果节为准回写 200，此处留痕供 conductor 知悉（无需改 ADR）。 |
| ADR-0040 决策 8 指标名 ↔ metrics.go as-built | **名实分歧**：草案 `binflow_storage_replay_*` 六枚（三 outcome 合一带 label）→ as-built `binflow_replay_*` 七枚（outcome 拆三独立族）。T-347A 在 docs/user 已登记同款；本票在 §23.2 落 as-built 权威表 + ADR 文字修正注。建议 conductor 在下次 ADR 汇编时补一行后记（不翻原文）。 |
| ADR-0034 决策 2（对外绝对 URL 统一 `server.base_url`，空值回退请求推导） ↔ uploads.go L1131 `requestBase(r)` | **适用面差**：MPU urlPart 答案（对外绝对 URL）纯请求推导，**未接 `server.base_url`**。ADR-0034 字面管辖「新协议 adapter 的对外 URL」，MPU 非协议 adapter——不违约但违「同一 codebase 一种习惯」精神。已在 §15.4 已知残余登记待裁（反向代理后 request Host 形态不可信的场景与 npm/nuget 同理）。 |
| §11.43 ↔ ADR-0039 as-built（kill -9 续传） | **旧债闭账**：§11.43 原文「AC2 续传 descope M11」已被 caller blob v2 token 持久化兑现（探针腿 4 + 单测钉死）——本票追加状态注，不改历史原文。 |
| §11.42 ↔ T-324/T-346 as-built | 同上追加状态注（cleanup 引擎已生效；canonical 终态指针 §15.4.1）。 |
| BOARD T-347 票面「§23 MPU 新 wire 回写」措辞 | 与 conductor 派单口径（MPU 面 = §15.4、§23 = 监控/可观测段）已按后者执行；两处锚点均在本次回写中落盘，票面 AC1 两半（MPU 新 wire + remote 字段终态）全覆盖。 |
| 文档外引用（docs/user/admin/license.md「§15」、docs/user/properties.md「§15.3」） | 纯文本引用非链接，且 §15/§15.3 编号未动——零断链风险。 |

## 4. 自检（实际执行）

| 检查 | 命令/方法 | 结果 |
|---|---|---|
| 结构完整性 | `grep -n "^## \|^### 15\|^#### 15"` 标题序 | §1~§15.6 既有编号零变动；新增 §15.4.2/.3/.4 与 §23；§16~§22 空号已在 §23 节首注承接说明 |
| 旧 wire 残留 | `grep "八臂\|urlPart/{id}\|clamp 至 5MiB\|awaiting-complete"` | 仅退役说明处保留「旧八臂」字样（历史指称），无行为性旧描述残留 |
| `/api/v1/uploads` 一致性 | 全文 grep | 三处（§7.1 路由行 / §7.1 bullet / §15.4 块）均为新 wire 口径 |
| 交叉引用自洽 | `grep "§15.4\|§23\|§11.4"` | 新增引用（§15.4.1→§23.1、§15.4.4→ADR-0039、§11.44/45→§15.4.2/3、§23.2→§14.2-6）目标节全部存在 |
| `make docs`（AC2/L27 组成部分） | `make docs`（后台全量跑，实际执行） | **SUCCESS**：`[SUCCESS] Generated static files`、EXIT=0；输出 grep broken/error 零命中；站点 4.20 MB（预算 ≤15MB，与 T-347A 基线持平）。architecture.md 非 docs-site 源文件，本票对构建输入面本就为空——全量绿是双重确认。注：生成物（docs-site/build、internal/docs/dist）随之刷新，属 make 正常产物，git 面由 conductor 收口。 |

## 5. 遗留 / 移交

1. **§16~§22 空号**：编号承接已在 §23 节首注说明；若后续里程碑要补 M11 域的独立章节（如复制硬化/MUI 契约），编号可回收或续用 §24+，无需改本注。
2. **base_url 适用面差**（§3 表第 4 行）：建议随下一张 internal/httpapi 或 adapter 票裁定——MPU urlPart/未来协议面统一走 `server.base_url` 优先、请求推导兜底（ADR-0034 决策 2 字面化）。
3. **Q3 终裁落点**：§15.4.3 已按「暂行 pro + 建议 community + 翻转点」留痕；终裁后仅需翻转 slots.go 一行 + 本节档位措辞（FR-86-AC2 单源，架构册无独立断言面）。
4. **观察者接线票**（§11.44）与 **config 旋钮票**（§11.45）：已在技术债台账立项级登记，等 conductor 排程。
5. ADR-0039 决策表 part 行 202/ADR-0040 指标草案名的**文字层陈旧**：本票不动 ADR（只追加纪律），建议 conductor 在下次 DECISIONS.md 维护窗口补后记。
