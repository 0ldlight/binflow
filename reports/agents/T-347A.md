# T-347A 工作日志 — M12 文档积压清偿（README 双语 + 文档站双更新）

- 状态：done（两新篇 + 七文件增补 + 双 README 刷新 + 侧栏挂页；`make docs` SUCCESS、两新路由生成、断链/断锚 0）
- 角色：tech-writer；日期：2026-08-29
- 纪律遵守：共享工作树**零 git 操作**；只动 `README.md` / `README.zh-CN.md` / `docs/user/**` / `docs-site/sidebars.js`（conductor 派单授权面）+ `make docs` 生成物（docs-site/build、internal/docs/dist）。命令与行为全取 T-337/T-338/T-339/T-341/T-343/T-345 票日志的真实输出；关键面（路由/门/槽位/指标名/装配 wiring）对照 HEAD 代码复核（router.go / trash.go / slots.go / metrics.go / main.go）。

## 一、积压面与去向

| 用户可见变更（T-328 后落地） | 去向 |
|---|---|
| 制品操作族 copy/move/zip/`archive!`/explode（pro 槽 repo-operations） | **新篇** `docs/user/admin/artifact-operations.md` + README M12 节 + api-reference M12 速览 + FAQ |
| Trash can（恢复/清空/保留期；pro 暂行） | **新篇** `docs/user/admin/trash-can.md` + README M12 节 + api-reference + FAQ + license.md 槽位 |
| NuGet v2 全路由 + v3 上游代理/动态解析 | `docs/user/integrations/nuget.md` 重写 v2/v3 两节（v2 最小面 → 全 18 端点表 + 重复臂 + remote 代理 + virtual 九步；v3 search 分仓型语义 + `registration-semver2` 族） |
| dual-write S3 停机 fail-open | `docs/user/admin/storage-config.md` 新节（行为表 + 边界 + 指标七枚 + 实测锚）+ 报错表三行 + FAQ |
| MPU Artifactory 形（jfrog-cli 兼容，T-332 M11 期落地） | README 叙述段一句话（FAQ/上传细节 M11 期已在位，未重复） |
| UI 视觉全面 MUI 原生化（T-344 五波） | README 叙述段一句话（base.css 983→443） |

## 二、变更清单

1. **README.md / README.zh-CN.md**（双语逐节对齐）：
   - 叙述段新增 M12 段（六面：操作族/Trash/NuGet/fail-open/MPU/MUI）。
   - pro 档能力表补「制品操作族 + 回收站功能槽（M12；trash 档位暂行）」；enterprise 行 M12+ → M13+（对齐 ROADMAP 滚程）。
   - 新增 `### M12` 小节：community 403 门真实 curl（T-339 §7 逐字）、pro copy messages[] 形、explode 201 + 计数头、trash 五元组/restore/empty 真机输出（T-345 §3）、NuGet/fail-open 指南链接。
   - 文档索引表：里程碑行 → 「M1 内核 → M12 生命周期程；M1~M11 done」；PRD 行换 milestone-12.md。
   - 许可/状态节：M1~M11 done and tagged（`m11-done`）+ M12 进行中（修正了英文版残留的「M11 in final verification」陈旧态）。
2. **docs/user/admin/artifact-operations.md**（新，~190 行）：五动作表 → 前置（pro 槽 + 门序）→ copy/move（端点形状/参数表/校验链七序/数据语义/仓型语义）→ 打包下载（执行序 + 六默认 + **总开关默认关、配置旋钮未落的已知边界**）→ `archive!/`（切分/嵌套/checksum 后缀/可读集/404 逐字）→ explode（双头/白名单/201 空体 + `X-Binflow-Exploded-Files`/排除项/zip-slip/Atomic）→ 门控姿态（真 curl 403 + 与 Artifactory 400 text/plain 的差异登记）→ 报错对照 → 下一步。
3. **docs/user/admin/trash-can.md**（新，~150 行）：语义（捕获点 fail-closed/五元组/跳过集/不捕获面）→ 内置仓守卫 → 浏览（骑 storage 面）→ restore（目的地三级/标记剥除/真机 sha256 对账）→ empty/clean（JSON 摘要）→ 保留期（14d/小时 cron/updated_at 回退）→ GC/cleanup 免疫 → 已知边界表（**config 旋钮未落、Q3 档位暂行、docker manifest 不入站**）→ 报错对照。
4. **docs/user/integrations/nuget.md**：头部改 M10+M12；v3 速查表 SearchQueryService 行改「分仓型」（remote = 上游实时代理——旧「先 restore 才可搜」退役）；新增 `registration-semver2` 行与「remote search 的上游代理」小节（层阶/400 重试/空集贡献/**搜索出站不带上游凭据**如实登记）；v2 节整体重写为全路由面（12 行端点表 + 重复推送臂四形态表 + semVerLevel 活体语义 + remote 代理 + virtual 九步）；报错表换四行新条目、删两行过时行；符号服务器边界措辞更新。
5. **docs/user/admin/storage-config.md**：头部 +M12；新节「dual-write 停机窗 fail-open（M12，ADR-0040）」——写/读/治理/开窗/恢复/重启六行行为表 + 滞后量/无死信/completed 拒启/bypass 零路径/status 形状不变 + **落地的七枚指标实名**（`binflow_replay_*`——代码核对，ADR 草案名 `binflow_storage_replay_*` 未采用）+ T-338 实测锚；报错表 +3 行。
6. **docs/user/faq.md**：头部版本 M1~M11 → M1~M12；新增「M12 增补三问」（trash 保留期与救回 / 操作族门控与 community 报错形态 / fail-open 三边界）。
7. **docs/user/api-reference.md**：头部 +M12；新增「M12 增补速览（T-347A）」——制品操作域五行表（copy/move/archive-download/`archive!`/explode，含门与 vendor CT）+ trash 域三行表（restore/empty/clean + 浏览骑 storage 面注记）+ NuGet 面指引行。
8. **docs/user/admin/license.md**：槽位矩阵 15 → **17**（+`repo-operations`〔pro〕/`trashcan`〔pro 暂行，Q3 终裁建议 community〕两行）；解锁数速查 6→15→17；三档语义与 nuget 行描述同步；enterprise 本体 M11+/M12+ → M13+。
9. **docs/user/README.md**：管理指南组 +2 新篇行；nuget/license/storage-config/api-reference/faq 五行描述同步 M12 面。
10. **docs-site/sidebars.js**：管理指南组追加 `admin/artifact-operations`、`admin/trash-can`（frontmatter sidebar_position 50/51）。

## 三、构建输出

| 命令 | 结果 |
|---|---|
| `make docs` | **SUCCESS**；`docs site size (raw total): 4.20 MB`（预算 ≤15MB；M11 期 3.93MB） |
| 路由核对 | `build/admin/{artifact-operations,trash-can}/index.html` 均生成；`.docusaurus/registry.js` 含两新 id |
| 断链/断锚 | **0**（build 输出 grep broken/anchor/error 零命中，onBrokenLinks=warn 下无告警） |
| 锚点抽查（生成 HTML 实证） | storage-config 生成 `id="dual-write-停机窗-fail-openm12adr-0040"` ⇄ faq 链接命中；api-reference 生成 `id="m12-增补速览t-347a"` ⇄ 篇内自链与 artifact-operations 下一步命中 |

## 四、命令证据来源（未在 scratch 复跑的部分如实登记）

- copy/move community 403 + 头 + 信封：T-339 §7 真二进制 curl 逐字；pro 形态消息链为 httptest 真服务端栈（真 license REST 安装）断言。
- trash 全链（五元组/restore sha256 对账/clean/empty 摘要）：T-345 §3 真二进制 + 真签名 pro license（port 18345）。
- fail-open 实测锚：T-338 §1 真 MinIO docker stop/start（PUT 201/GET 200/1 秒排空/mc 零缺/skipped=12）。
- NuGet v2/v3：T-337/T-341 live curl 矩阵（真 curl × 真回环上游 × 异构 @id 夹具）+ nuget.org 活体闭合。
- 代码复核：router.go（copy/move/archive/trash 路由与门序注释）、trash.go（RequireAddon）、slots.go（RepoOperations/Trashcan）、metrics.go（`binflow_replay_*` 实名）、main.go（三处 fail-open wiring 已合入：Replay 面板装配/SetReplayEvents/CheckCompletedReplayQueue 拒启）。

## 五、遗留 / 待确认

1. **folderDownloadConfig 配置旋钮未落**（T-343 遗留①）：打包下载端点生产实例默认关（pro 也 403）——两新篇与 FAQ 已如实登记「已知边界」；旋钮票落地后 artifact-operations.md 对应段需刷新。
2. **trashcan 配置旋钮未落**（T-345 遗留②）：保留期生产恒 14d——trash-can.md 已登记；config 域小票落地后同步。
3. **Q3 档位终裁**：trash-can 暂行 pro；若终裁 community，需刷 trash-can.md / license.md / 双 README pro 表行 / FAQ 四处表述（翻转点已在 T-345 §2 列明）。
4. **copy/move 后的协议索引联动观察者**（maven/deb/conan reindex 接线，T-339/T-343/T-345 共同遗留）：接线票落地后 artifact-operations.md「数据语义」段可补「索引随行重算」一句。
5. **NuGet 真机 dotnet 矩阵**：M12 增量以 curl 矩阵验证（环境无 dotnet SDK）；有 SDK 渠道时跑 `BINFLOW_T287_CLIENT_E2E=1` 复验后 nuget.md 头部验证注记可升级（T-341 遗留②同款）。
6. trash FE（T-352 未落）：trash-can.md 目前只写 REST/存储面；控制台 Trash Can 节点交互落地后补 UI 段。
