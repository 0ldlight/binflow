# T-51 工作日志 — R3 正式消歧回写（manifest CT 策略 + 双 node 布局追认）

- 角色: architect
- 日期: 2026-08-19
- 输入: reports/agents/T-39-review-arch.md（第五节 R3 正式消歧意见 + non-blocking #2/#3/#5）、修复合入现状（manifest.go 专形/透传双轨已实证）
- 状态: done（一致性自检通过）

## 落点清单

### 1. §5.3 校验链① 重写（R3 终审口径）

旧文「mediaType ∈ {四种}（M2 四种，其余 415/400）」废除，改为：
- **Content-Type 透传存储（非白名单）+ 结构性验证**：四种已知类型走专形解析；未知 CT 不拒收，按 body 形状判读（`manifests[]`→index、`config`→image、皆无→400 MANIFEST_INVALID）；
- **唯一显式拒收 CT 家族 = schema1 两种** → 400；缺 CT 拒收；
- 透传对象 = 裸 media type（剥 `;` 参数——non-blocking #4 的字面歧义一并补注）；
- 不裁回白名单的理由精简入注：cosign/oras 生态 CT 今天就在推；结构判读已接管白名单的防假 manifest 职责（T-39 review §五）。

### 2. 判读优先序入文（non-blocking #5）

「未知 CT 且 body 同时含 `manifests[]` 与 `config` 时**按 index 判读**」写进校验链①（此前只存在于 manifest.go:544 代码里；标注 OCI 1.1 无此合法形态、属 edge case 防御）。链②③ 同步补一句「未知 CT 透传类型同样走②③」——校验链对透传类型全部生效。

### 3. §6 DDL 注释同步

`docker_manifests.media_type` 注释从「§5.3 四种之一」改为「客户端透传的裸 media type（剥 ; 参数，非白名单——§5.3 校验链①，T-51 消歧）」。

### 4. 双 node 布局追认（non-blocking #2）

§6 002_docker.sql 布局注释扩写为**正式契约**（标注 T-39 裁定、T-51 追认）：
- 双 node 布局语义：manifest digest 落 manifests 行 + 引用链 blobs 行族；index 子 manifest 的 descriptor mediaType 未知/拼错也能在 blobs 路径探测成功（mediaType 选路失误被双行兜住）；
- 「DELETE manifest 后 blob node 存活、manifest GET 404 而 blob GET 仍 200」为既定语义（blob 物理回收归 GC）；
- 已是探测锚点与 FR-7-AC4 测试钉死面。

### 5. PRD DE-15 注记（授权一行）

`docs/prd/milestone-2.md` DE-15 行补 OCI-Subject 注记：无条件返回（subject digest 良构即回）vs OCI spec「repo 开 referrers API 才回」的条件——有意偏差，oras 凭头探测、404 后安全回退 tag 模式，不伤推送体验；头信号与端点能力不一致显式归档；referrers 端点本体维持 M2 不做。兼容矩阵「有意不兼容」列补「（OCI-Subject 头为有意偏差，见注记）」。

## 一致性自检

grep「四种」：仅命中新文本身（「四种已知类型走专形解析」）；白名单旧措辞（「M2 四种，其余 415/400」「§5.3 四种之一」）零残留。T-51/T-39 来源标注 6 处。

## 边界自律

architecture.md 两节（§5.3 校验链、§6 DDL 注释与布局段）+ PRD 一行（DE-15）；未改 DECISIONS.md（无新决策，R3 消歧是既有 T-32 暂行口径的终审落地）；未改代码；未动 BOARD.md。

## 遗留问题

1. T-39 review non-blocking #6（repo 导出 shutdown 哨兵 `repo.ErrUnavailable`，让 adapter 错误映射脱离 storage 类型名）是独立 dev-go-core 小票，归 conductor 派发——非本票范围。
2. 同 digest 换 CT 的两源 mime 分歧（T-39 §六.1）维持「记录即可」，若 T-43 conformance 暴露再议。
