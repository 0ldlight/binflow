# T-465 [P1] release 烟测 + UAT 随里程碑 PR（M16 收官笔）

- 角色：release-engineer ｜ 状态：done（35 票收官面全绿；m16-done 收口清单 §5 备妥）
- 基线：起步 HEAD `ca416424`，收笔时 HEAD `84c1f4e4`（conductor 在途提交，工作树活性——release 烟测取当时 HEAD `1eaa9034` 打戳）。零 git 操作，未 commit。
- 证据目录：`/tmp/t465/`（matrix-run.log / goreleaser.log / build-release.log / k8s-render.yaml / chart-render.yaml）
- 净窗披露：全程与他 lane 并行（go test -race 全树 + bf-ga kind 集群 + buildkit builder 在跑；一次 PreToolUse lint 钩子与他人 golangci-lint 撞车重试通过）。goreleaser 限 `-p 6`（16 核机共租）。Docker Desktop 保持拉起态（他 lane 容器在载，未关停——T-466 §8-7 知悉位延续）。

## 1. 两枚 P2 快修（T-466 §7 移交）

### D-T466-2 — setup_client 追加缺尾随空格（单工具 dockerize 时 client_base 失灵）

`ci/protocol-matrix.sh` `setup_client`：

```diff
-    DOCKERIZED_TOOLS="$DOCKERIZED_TOOLS $tool"
+    DOCKERIZED_TOOLS="$DOCKERIZED_TOOLS $tool "
```

（附六行注释留痕。`client_base` 的 case 模式是 `*" $1 "*`——两侧都要分隔符；初始值 `" "` 单空格，无尾随空格追加后单工具形态为 `"  mvn"`，尾侧无空格永不匹配 → 容器客户端拿 BINFLOW_BASE（容器自身 127.0.0.1 回环）。gradle 腿手工追加行（`" java "`）本就带尾——两处现已同形。）

模式演示（脚本语义复刻，pre/post）：

```
PRE  [  mvn]      → client_base mvn = BINFLOW_BASE   （受损）
PRE  [  mvn  npm] → npm = BINFLOW_BASE（最后追加者同样受损）/ mvn = CBASE
POST [  mvn ]     → CBASE；POST [  mvn  npm ] → 两者 CBASE
```

（比 T-466 记载更精确一处：多工具时**最后一个**追加者同样受损，不止单工具形态。）

### D-T466-1 — npm 腿 remote 段 ssrf SKIP 后 virtual 段引用未建成员仓

同文件 npm 腿 virtual 段两处：

```diff
-    ensure_virtual uat-matrix-npm-virtual npm \
-      "uat-matrix-npm-local,uat-matrix-npm-remote" || return 1
+    if [ "$NPM_UP_OK" = 1 ]; then
+      ensure_virtual uat-matrix-npm-virtual npm \
+        "uat-matrix-npm-local,uat-matrix-npm-remote" || return 1
+    else
+      ensure_virtual uat-matrix-npm-virtual npm \
+        "uat-matrix-npm-local" || return 1
+    fi
```

record 行同步分叉：降级态记 `npm/virtual PASS "local-member aggregation only — remote-member assertions skipped (npm/remote SKIP: ssrf-guarded self-upstream)"`——按 strict-tools 语义留痕（远端成员断言本就被 NPM_UP_OK 门控跳过；降级记录行如实命名形态，不冒领双成员）。产品侧行为不变（virtual 引用不存在成员仓 400 拒配维持——T-466 判定正确）；strict-tools CI 面（公网 UAT）恒走双成员支路。

### 修后验证

- `shellcheck ci/protocol-matrix.sh` 零告警；`bash -n` 通过。
- **实跑一脚（两修同场验证）**：净实例 `127.0.0.1:18170`（HEAD 二进制、临时 data 目录、community tier）+ `--docker-clients` 姿态，`BINFLOW_BASE=http://127.0.0.1:18170`、`BINFLOW_CLIENT_BASE=http://host.docker.internal:18170`：

```
BINFLOW... ./ci/protocol-matrix.sh --docker-clients --leg npm --leg maven   → EXIT 0
preflight      INFO  tier=community base=http://127.0.0.1:18170
npm/remote     SKIP  self-upstream 127.0.0.1 is private/loopback (ssrf-guarded by design)
npm/remote-npmjs PASS tarball pull-through byte-identical + packument 404 boundary tripwire
npm/virtual    PASS  local-member aggregation only — ... (ssrf-guarded self-upstream)   ← D-T466-1 修复位（T-466 同姿态红）
npm            PASS  ok
maven          （client 'mvn' not on host — docker fallback: maven:3.9-eclipse-temurin-17）← dockerized
maven/remote   PASS  central pull-through + mvn copy junit:junit:4.13.2 (MISS -> HIT)   ← 容器内 mvn 经 CBASE 达宿主实例 = D-T466-2 修复位
maven/virtual  PASS  dependency:resolve both members + §8.3 metadata union
maven          PASS  ok
protocol matrix GREEN
```

D-T466-2 证明链：mvn 仅经 `client_base mvn` 取基址；容器内 `127.0.0.1:18170` 无监听（连接必拒），deploy/resolve 双绿 ⇒ 基址必为 `host.docker.internal:18170`（CBASE 生效）。前置探针：alpine 容器 `wget host.docker.internal:18170/healthz` = OK。

## 2. goreleaser 六平台快照烟测

命令（Makefile 同形直调，`-p 6` 共租限流）：`BINFLOW_VER=v1.0.0-snapshot.1eaa9034 goreleaser release --snapshot --clean` → **EXIT 0，3m8s**（18 二进制 = 3 命令 × 6 平台，零 CGO）。`make release-verify RELVER=v1.0.0-snapshot.1eaa9034` 全过（6/6 档案在场 + shasum -c 全 OK + check-size 门）：

| 产物（dist/） | SHA256 |
|---|---|
| binflow_v1.0.0-snapshot.1eaa9034_darwin_amd64.tar.gz | 02846d62ac17fadef29f8a2757b96f2bf84fe7331971d46d300e33b88eee69c2 |
| binflow_v1.0.0-snapshot.1eaa9034_darwin_arm64.tar.gz | 84d9a27adbb398ac46c0ec0af726c5f7d13d0814564535afdf67fcf7d3206efe |
| binflow_v1.0.0-snapshot.1eaa9034_linux_amd64.tar.gz | ab5e5a93526e9be18362cbe8dae24c0affe96e5a27ef4b2eb7b42f0ffd176373 |
| binflow_v1.0.0-snapshot.1eaa9034_linux_arm64.tar.gz | e87b327a2f6c5b31c0ea6625e851271295fc851caa2c1fbe5350206d87ee181f |
| binflow_v1.0.0-snapshot.1eaa9034_windows_amd64.zip | c7f06ba3cd0900dab9362df64b9bd8c67d0568ce660405a6f8f90b961578d244 |
| binflow_v1.0.0-snapshot.1eaa9034_windows_arm64.zip | d1fe5b1b65a7367394a6bbca849856e65fac3ee85099e284ae2d2dd4fe543ccd |

- 版本注入实证（darwin/amd64 原生解包三件全过）：`binflow-server/bf/bf-migrate --version` → `v1.0.0-snapshot.1eaa9034 (1eaa9034)`（-X main.version/revision 落位）。
- **F1 门：112.72MB ≤ 120MB GREEN**（M15 104.52 → +8.2MB；裸 server 裸二进制 40MB 原始预算线维持 WARN-only——M15 已登记观察项延续）。**余量收窄至 7.3MB**：本机 staged internal/console/dist（09-06 08:36，真 SPA）+ docs 站随版本增长；M17 收口窗建议复查是否再基线（登记遗留①）。
- CI 面引证（免重跑）：GH `ci` 工作流 main 连续 4 绿（`gh run list` 实查最新 `f43d67bb` success 2026-09-05T20:16:48Z——release-dryrun 含其中；T-466 §5 同口径）。本机快照与 CI 面**同命令同配置**（.goreleaser.yaml 自 M15 零变更）。
- 注：archives 阶段 `no files matched glob=LICENSE*` 六行——repo 无 LICENSE 文件既知形态（T-127 起如此），goreleaser info 级，不影响产物/校验和。
- 不对外发布遵守：snapshot 面 + `release.disable: true`，零上传。

## 3. Helm Chart 联动裁定：**维持 1.5.0，不升**

依据：`git log 73d45569..HEAD -- charts/` = 空（Chart 最后动于 M15 T-429，1.5.0）；M16 服务端零新配置面（`git diff 73d45569..HEAD -- internal/config`：零新增 env/yaml 键——cmd 面 backup/schedule wiring 均内部常量承载，T-466 DoD#4「16 张纯 FE 票服务端 diff=0」同源）；抽查复验 `helm lint charts/binflow` = 1 chart linted 0 failed、`helm template` 默认值渲染 6 资源（ConfigMap/Deployment/PVC/Secret/Service/ServiceAccount）无误。appVersion 维持 1.0.0（产品版本基线未动）。

## 4. 部署矩阵三面抽检

| 面 | 形态 | 结果 |
|---|---|---|
| **Docker 镜像**（矩阵 #2，随腿） | `REGISTRY= PUSH=0 build-release.sh v1.0.0-snapshot.1eaa9034`（dist 档案零-RUN 注入） | **EXIT 0**：4 镜像（alpine/distroless × amd64/arm64），arch 字段四验全对；smoke amd64 双变体 readyz 200 + version 注入对上；arm64 qemu 腿 readyz/version 同绿。环境披露：DaoCloud 镜像源 R2 blob CDN 本机 EOF 不可达 → `ALPINE_SRC=alpine:3.24`（Docker Hub）+ `DISTROLESS_SRC=gcr.io/distroless/static-debian13:nonroot` 覆写（两 manifest inspect 预验通过）；PUSH=0 零推送（红线） |
| **compose**（矩阵 #3） | 真部署：prebuilt 镜像 `up -d`（G10 链的 prebuilt 形态；:18171） | Docker HEALTHCHECK healthy + /readyz 200 + 容器内探针 OK；建仓 200 → PUT 201 → GET **逐字节一致**；console SPA `/binflow/ui/` 200 + 哈希资产 200 + index 与裸二进制实例**逐字节一致**（同一嵌入 dist）；`down -v` 清场（卷删）。注：多架构 manifest 装配属 push 路径（PUSH=0 不走），compose 用本地 `-amd64` 别名 tag——单架构本地烟测口径，arch 证明由镜像腿四验承载 |
| **k8s 清单**（矩阵 #5） | lint 级 + 不变性留痕 | `kubectl kustomize deploy/k8s` 渲染 4 资源 OK；`kubeconform` **4/4 Valid**；`kubectl apply --dry-run=client -k` 4 created 干跑通过；`git log 73d45569..HEAD -- deploy/k8s/` = 空——**与 M15 T-429 kind 真部署全绿时逐字节未变**（distroless rollout/PVC Bound/roundtrip 当时已证）。不另起第二 kind 集群：bf-ga 集群为 GA lane 在用（不动他 lane），共租窗内不加重载（口径：lint 级留痕成立） |
| **systemd**（矩阵 #6） | 容器腿（unit 语义，release alpine 镜像） | ① `ExecStart` 形（`serve -c /etc/binflow/binflow.yaml`）boot + readyz 200；② `User=binflow` → 进程 uid=10001(binflow)（image User 指令同源）；③ `EnvironmentFile` 语义：root 读入注入 → `BINFLOW_TRASHCAN__RETENTION_DAYS=21` 于 `GET /api/v1/system/settings` 回显 21；④ 0600 root 属主 env 文件：root 可读 / uid10001 `Permission denied`（容器内 chown/chmod 实测——宿主 bind mount 因 Docker Desktop 共享层不显权限，改容器内落盘验证）；⑤ `KillSignal`/`TimeoutStopSec` 语义（SIGTERM + 45s 窗）：`docker stop -t 45` → 日志 `shutting down gracefully` + replication/webhook drained；⑥ version/revision 注入对上。测毕容器删 |

烟测后清理：compose 栈 + 卷已 down -v；systemd 腿容器已删；净实例 :18170 已停；我方 matrix mktemp 工作目录已删（12:45 新生的三个 binflow-matrix.* 为他 lane 在用，未动）；本地镜像 tag（binflow:v1.0.0-snapshot.1eaa9034-* 四枚 + 别名）留存本地未推任何 registry。

## 5. m16-done 收口清单（conductor——T-466 §8 移交项按序落位）

**前置**：T-466 终验 PASS with notes（§0）；本票 release 面全绿；T-460 §6-6 后半（tech-lead B1 就绪度签收）仍空位——收口窗顺带。

1. **tag m16-done**（先例 m15-done = 轻量 tag 打在里程碑 merge commit 上）：
   ```
   git tag m16-done <m16 milestone merge commit>
   git push origin m16-done        # 常态授权内；仅 origin（0ldlight/binflow）
   ```
2. **UAT 随里程碑 PR**（链路：main → CircleCI `uat` workflow = build → deploy_uat（TLS 层 + 二进制换装 + smoke）→ protocol_leg ×10 + e2e，deploy/ci/README.md）：
   - conductor 自建自合 develop→main PR（PR self-serve 常态；近期先例 PR #67/#68）；merge 即触发 UAT 换装 + 十腿矩阵 + e2e。
   - **CircleCI 平台事故期**：merge 前先探平台恢复（Project → Pipelines 页可开新 run 即恢复）。未恢复也可先合（main 的 GH `ci` 工作流面照常绿），UAT 换装补跑：
   - **恢复后补证**（对 T-466 §5 事故期口径的收口）：CircleCI Pipelines 对该 main push **re-run**（或任一 main 提交触发新 run）→ 确认 build/deploy_uat/protocol_leg×10/e2e 四段全绿；十腿 report（`ci/protocol-matrix.sh --report` 产物）留档收口笔。GH `protocol-matrix` 工作流（workflow_dispatch，可选 legs 入参）为即时可用的替代面。
   - UAT 换装后快验：`https://uat.binflow.org/binflow/api/system/version` → `uat.<sha>`（随 PR 的 merge sha）；本票 D-T466-1/2 修复将随该部署后的下一次矩阵复跑首次在 CI 面承载（strict-tools 双成员支路，与本地降级支路互补）。
3. **ROADMAP 备稿启用三行增补**（T-466 §6 建议——「M16 未纳入项」契约漂移族 bullet〔现行 290 行〕尾部加，登记处 BOARD T-457/T-459 + console-ux §9-R6/R11 + parity v1.12 已在册，本增补使备稿自声明「按 as-built 修订勾稽」闭环）：
   - `Profile SSH 公钥卡 ssh_keys 端点 BE 全域缺位（诚实缺位——BE 建议票候选；BOARD T-457 + console-ux §9-R11）`
   - `Access Tokens 令牌清单端点缺位（A7 联动；BOARD T-457 + console-ux §9-R6）`
   - `Service Status Uptime 无端点（如实缺位不伪造；BOARD T-459 + parity v1.12 注）`
4. **t381 处置（Q12——随本票决定项）**：维持登记（ROADMAP 未纳入项条件票段已在册；VM 取证快照 15MB + 空仓，OSS license 门挡删）——除非 conductor 本窗裁删（需 license 侧动作，超出本票）。
5. **Docker Desktop 知悉位**（T-466 §8-7 延续）：拉起态、他 lane 容器在载，收口笔后由末 lane 决定关停。

## 6. 遗留登记

1. **F1 门余量 7.3MB**（112.72/120，M15 后 +8.2MB）——M17 收口窗复查再基线（或裁 console dist 资产累积——staged dist 的 assets/ 有多轮构建哈希残留嫌疑，`make console` 重制可回收）。
2. DaoCloud 镜像源本机不可达（R2 blob CDN EOF）——本票以官方源覆写通过；CI 链（DaoCloud 直连网络）不受影响；若本机复现需镜像源恢复或常备覆写（登记备查，非缺陷）。
3. `no files matched glob=LICENSE*`——repo 无 LICENSE 文件（T-127 起既知）；GA 发布前若需随包 LICENSE，另开小票。
4. D-T466-2 附带发现：多工具 dockerize 时**最后一个**追加者同样受损（T-466 仅记单工具形态）——同一行修复覆盖，无需另票（此处留痕勘误）。

**状态：done——两 P2 修讫实证、六平台快照全绿（sha256 表在档）、Chart 维持 1.5.0 有据、三面抽检全过、收口清单备妥；零 git 操作，待收编。**
