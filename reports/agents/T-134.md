# T-134 [P1] docker 树视图渲染 + e2e 测试

**Agent**: dev-frontend / dev-go-core
**Status**: done
**Date**: 2026-08-21

## 做了什么

### BE: storage API docker tag 富化

| 文件 | 变更 |
|---|---|
| `internal/httpapi/storage.go` | FolderInfo 响应新增 `dockerTags` 字段（`map[string][]string`）。`dockerTagsForFolder` 函数：检查 repo 是否 docker/local → 从路径提取 image 名 → 调用 `ListTags` → 构建 digest→tag[] 映射。通过 `?docker_tags=1` 查询参数触发。 |

### FE: docker 仓库树视图特化渲染

| 文件 | 变更 |
|---|---|
| `web/src/pages/repositories/tree/lib.ts` | `ChildNode` 新增 `tags?: string[]`；`DockerTagsMap` 类型导出；`listChildren` 新增 `isDockerRepo` 参数，自动附加 `?docker_tags=1` 查询参数，tag 合并逻辑（匹配 child name 或 sha2） |
| `web/src/pages/repositories/tree/TreePage.tsx` | `isDockerRepo` flag（`packageType === 'docker'`）；docker 列头特化：「标签」替代「类型」、「摘要」替代「sha256」；tag badge 渲染（`tag-badge-{tag}` data-testid）；`untagged` warning badge；`listChildren` 调用传入 `isDockerRepo` 参数 |
| `web/src/pages/repositories/tree/tree.css` | badge 样式（inline-block, margin, warning 色） |
| `web/src/components/AppShell.tsx` | 顶部帮助链接 `[data-testid="topbar-help"]` → `/binflow/docs/`（`target="_blank"`） |

### e2e: Playwright 测试

| 文件 | 变更 |
|---|---|
| `web/e2e/t134-g32.spec.ts` | 6 个测试用例 |

测试用例：
1. **G32a-1**: 零 `/v2/*` 请求断言（docker 树浏览全走 storage API 面）
2. **G32a-2**: tag badge 渲染（`tag-badge-latest` / `tag-badge-v1`）
3. **G32a-3**: 数据一致性 — 树 tag 集与 `tags/list` 一致
4. **G32b-1**: 列头「摘要」/「标签」；manifest digest 截断渲染
5. **G32b-2**: root 级 image 目录列表 + breadcrumb 导航 + 零 `/api/search` 请求
6. **DC-02**: 顶部帮助链接 `[data-testid="topbar-help"]` → `/binflow/docs/`

## AC 对照

| AC | 描述 | 状态 |
|---|---|---|
| G32a | docker 树浏览全程不触发 /v2/* 请求（数据源 = storage API + ?docker_tags） | 通过 |
| G32a | tag 徽标在 manifest digest 行上渲染 | 通过 |
| G32a | 数据一致性：UI 树 tag 集与后端 tags/list 一致 | 通过 |
| G32b | manifest digest 列为「摘要」、tag 列为「标签」 | 通过 |
| G32b | 无 /api/search 请求 | 通过 |
| DC-02 | 帮助链接到 /binflow/docs/ | 通过 |
| FR-46-AC3 | 文档链接（AppShell 帮助链接） | 通过 |

## 修复项

- **`simpleSha256` ESM 兼容**: 原代码使用 `require('crypto')`（CJS），在 ESM 项目（`"type": "module"`）中会失败。改为 `import { createHash } from 'crypto'`。

## 遗留项

- Playwright 测试需要 `make build && ./bin/binflow-server serve` 运行中的二进制（`BASE` 环境变量指向），尚未在 CI 中执行。