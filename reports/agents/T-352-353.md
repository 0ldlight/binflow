# T-352 + T-353 工作日志（M12 聚票：Trash FE + web 表单/词表同步）

| 项 | 值 |
|---|---|
| 票据 | T-352 [P1] FR-106 Trash can FE 最小面 ＋ T-353 [P2] FR-113.2 web 仓表单/词表同步 |
| 角色 | dev-frontend |
| area | web/src（pages/governance 回收站页、pages/repositories 表单、lib）+ e2e + 锚册（console-ux.md §10，仅锚册域） |
| 状态 | done（闸门全绿，见 §3；7 红全为共居负载假阳性，串行复跑全绿） |
| 日期 | 2026-08-29 |

## 1. T-352 清单（Trash FE，FR-106 前端腿）

- **新页 `web/src/pages/governance/TrashPage.tsx`**（路由 `/admin/governance/trash`，治理分组第六页「回收站」——AppShell 导航/面包屑 + main.tsx 路由）：
  - **槽门控卡**（`trash-locked`）：GET /api/v1/addons 的 trashcan 槽行实时求值，照 License 页先例（需要 N 档提示 + X-Binflow-License-Required: trashcan 说明 + License 页跳转）；锁定时整数据面不渲染且**零浏览请求**（holdBrowse 挂起判定防首帧抢跑）。addons 面 403/失败/无注册表行 = 按未门控呈现（服务端 403 终裁，§3.6.3 403 驱动纪律）。
  - **浏览**：骑既有存储面（`listChildren`（FolderInfo + ?list&depth=1 合并）与制品树同源）；Breadcrumbs 逐层下钻（`trash-breadcrumb`）；行锚 = can 相对全路径（`trash-row-<path>`）；行点击展开详情面板（`trash-detail`）——**属性五元组逐行**（trash.time 格式化 + epoch 毫秒、deletedBy、originalRepository、originalRepositoryType、originalPath；原仓/原路径/sha256/回收站路径配 CopyButton）+ 随行原属性（非 trash.* 键）如实呈现 + 裸行（无标记）降级注记。根 404 = `trash-unused` 锚定空态（内置仓懒落库）；空罐/空目录两分。
  - **动作**（system:write，仅全量 admin；readonly_admin 只读 + `trash-readonly-note` + 全反断言）：恢复（`trash-restore-<path>` → ConfirmDialog 内嵌 `trash-restore-to` 目的地输入，目的地解析链 to 覆盖 > 五元组 > 路径结构在提示里明示；回执 = copy/move messages 原文 toast）；单条永久清除（`trash-clean-<path>` → danger 确认，子树影响面明示）；清空整罐（`trash-empty` → danger + 输入 EMPTY 强确认 `trash-empty-confirm`，GC apply 同款；空罐/未落库禁用）。empty/clean 回执摘要（`trash-summary`：removed/files/folders/bytes + blob 归常态 GC 的如实注记）。操作失败红面板含 403 双门（system:write / license 槽）指引。
  - **NFR-S61 FE 腿**：管理壳会话守卫（匿名直链重定向 login?return=）——e2e 断言。
- **新 lib `web/src/lib/trash.ts`**：三动词客户端（POST /trash/empty、POST /trash/restore/{path}?to=、DELETE /trash/clean/{path}）+ 槽行求值 + 五元组键 + trash.time 格式化；契约源 internal/httpapi/trash.go + internal/repo/trash.go（docs/user/admin/trash-can.md）。
- **MUI 原生基座**（四波后纪律，无新 CSS 文件）：Paper/Table/Breadcrumbs/Chip/Alert/Button；危险区 = Paper outlined + error.main（T-344 批 D 形态）；`.confirm-input`/`.kv`/`.field-hint` 既有类复用。
- **e2e `web/e2e/trash-can.spec.ts`**（9 腿，全 mock 探针 off dist——replication.spec.ts 基座；真栈 community 锁死，解锁面归 mock）：解锁浏览/下钻/五元组/拷贝锚/刷新、恢复 wire 断言（to 编码）、清除+摘要、EMPTY 强确认门、锁定态零请求、404 空态、readonly 反断言、非 admin L2、匿名重定向。
- **a11y**：真栈 a11y-sweep 增 `/admin/governance/trash` 路由（community 形态 = 锁定卡面）——27 路由 × 双主题。

## 2. T-353 清单（web 表单/词表同步，FR-113 尾巴）

1. **AUDIT_ACTIONS 前端镜像**（`web/src/lib/governance.ts`）：对 internal/audit/api.go `Actions()`（T-346 后）逐枚对照同步——24 → **54 枚**：补 T-324 落的 `cleanup.run`（镜像此前漏同步，非 T-346 批）+ T-346 的 29 枚（props.×2 / replication.×4 / keypair.×6 / auth.config.×4 / license.×4 / artifact.×3 / trash.×4 / storage.replay.×2），序与后端 picker 全同；e2e 腿钉死（选项计数 55 = 54+全部，新词表抽查 12 枚——repo-policy-keys.spec.ts ⑦）。
2. **deb/rpm/helm 策略键表单**（T-327R/T-329-DE 遗留）：
   - **新字段册 `web/src/pages/repositories/policyFields.ts`**（照 authconfig/sections.ts 先例，数据驱动单渲染器）：deb(debian)×6（byHash〔select，值域闭集 NONE/SHA256/ALL——K45〕/optionalIndexCompressionFormats〔逗号列表↔wire 数组〕/debianDefaultArchitectures/historyCycles/origin/label）+ rpm×4（calculateYumMetadata〔RP-2〕/yumRootDepth/enableFileListsIndexing/yumGroupFileNames）+ helm×2（forceMetadataNameVersion/forceNonDuplicateChart）。
   - **RepositoryFormPage 集成**：呈现位 = 「高级」分区（local × 对应包类型收窄）；编辑态 GET configuration 逐键预填、提交恒带 check/合法 number（**POINTER 语义**：显式 false/0 必须过 round trip——calculateYumMetadata 的 flip-off 是 T-327R 的 load-bearing 断言）、空 text 剔除归默认；number 字段进表单校验门。锚 = `form-<wire>`（建仓表单 form-${k} 生成器族键闭集扩 12 键，零新静态锚）。
   - `PackageType` 联合补 conan/helm/rpm/debian（M11 注册表实态；建仓合法集仍由 addons API 动态驱动，PACKAGE_TYPES 常量不动）。
   - **e2e `web/e2e/repo-policy-keys.spec.ts`**（7 腿，mock off dist——deb/rpm/helm 是 pro 门控型，community 真栈建不出）：deb 编辑器设键→提交体对账（含空 text 剔除/governance 键随行）、**L29-FE 正面：设→保存→重开回显**、rpm 创建面（显式 false 随行 + deb 键不串型）、helm flip-off（预填 true→摘勾→body 显式 false）、readonly 禁用反断言、axe 双主题、审计词表腿（见上）。
3. **UI 视觉**：全部走四波后 MUI 原生基座，无旧 CSS 续挂（新页零自建 CSS）。

## 3. 闸门输出（实际执行；机器共居负载 load ≈ 100+，tsc 单跑 5 分钟）

| 闸门 | 命令 | 结果 |
|---|---|---|
| 类型 | `npx tsc --noEmit` | 0 errors |
| Lint | `npm run lint` | 0 告警 |
| 构建 | `npm run build` | assert-tokens OK → `✓ built in 24.83s`（终态复跑 5.14s）→ relink-assets verified |
| 断言腿 | `node scripts/assert-tokens.mjs` | `OK — css 11 + tsx 43（MuiProvider 外零色值字面量、零 --bf-* 色彩 token 引用）` |
| 锚册 | `node scripts/anchor-audit.mjs --ledger` | **PASS**（A1~A4 四零；unregistered/broken 双零） |
| 锚消费 | `node scripts/anchor-audit.mjs`（dead 视图） | T-352 批 13 锚零死锚（trash-refresh 补消费后）；dead 桶剩 4 族全为票前存量（widgets/groups-sort-name/node-props-cancel/user-form-role-*，security/artifacts 域非本票 area） |
| 新腿 Playwright | `npx playwright test trash-can repo-policy-keys --project=chromium` | **16 passed**（trash 9 + policy 7） |
| 全量 Playwright（三项目） | `BASE=http://127.0.0.1:8129 npx playwright test`（自有净实例：新前端嵌入重编二进制 `go build -o /tmp/bf-t352/binflow-server` + 临时数据目录 /tmp/bf-t352） | **199 passed / 7 failed / 19 skipped / 5 did-not-run (13.3m)** → 7 红甄别全部为共居负载假阳性：governance-monitoring:32、repositories-admin:101、usage-fanout:139/204/276 五腿与 T-344E 登记的假阳性家族**同行号复现**，另两腿（t104 W10:81 列表行 5s 超时、m9 users-groups N01:110）同族时序敏感。**串行复跑（--workers=1）全绿**：chromium 三腿 3 passed + m9 两 spec 10 passed + t104 全 spec 4 passed/2 skipped（dind 环境腿，票前常态） |
| axe 双主题 | a11y-sweep（27 路由 × 双主题 + 登录页）含新 trash 路由（community 形态 = trash-locked 卡面） | 全绿（199P 内）；另 trash/policy 两 mock 面 axe 腿（repo-policy-keys ⑥）双主题绿 |
| SPA 预算 | dist js+css gzip | **341,023 B**（67 文件）：对 T-344E 终态 330,982 B 单票 **+3.03%**（≤ 单票 +3.25% 帽）；对 T-299 基线 284,185 B 累计 +20.0% ≤ +25%（NFR-P51 ✓） |

## 4. 锚册登记（console-ux.md v1.15）

- §10.5 路由表补 `/admin/governance/trash` 行；**T-352 批 13 名锚入册**（trash-page / trash-locked / trash-unused / trash-readonly-note / trash-summary / trash-breadcrumb / trash-list / trash-row-\<path\> / trash-detail / trash-refresh / trash-restore-\<path\> / trash-restore-to / trash-clean-\<path\> / trash-empty / trash-empty-confirm——9 静态 + 4 动态族口径）+ 变更注记（transaction-size 惰性不设旋钮、confirm-input 先例、NFR-S61 腿）。
- **T-353 批**：`form-<wire>` 生成器族键闭集扩 12 键（§10.6 生成器行同步扩容注记）；`PackageType` 联合扩四型 + 词表镜像（无锚面）。
- 修订记录 v1.15 行 + 表头票据/状态行同步。
- **对账器**：STOP 行文假阳性补 4 词（auto-trashcan〔存储常量〕/ transaction-size〔REST 参数〕/ confirm-input〔CSS 类钩子〕/ trash-can〔文档文件名段〕）——v1.9「人工核对过的假阳性」先例（web/scripts/anchor-audit.mjs）。

## 5. e2e 侧最小同步（断言语义不变，T-344E 先例口径）

- `e2e/m8/shell.spec.ts`：管理侧栏 14 → **15 条目**（+回收站行，ADMIN_ENTRIES/计数/注释三处）。
- `e2e/m8/a11y-sweep.spec.ts`：路由清单 + trash 行（26 → 27 路由，口径自证行同步）。

## 6. 差异登记（规范文字 vs 实际落地）

- **D-1 console-m8.md §1.3 管理侧栏条目清单过时**（ux-designer 域，非本票 area 可写）：T-307 认证配置后即 14 条、本票后 15 条，console-m8 §1.3 全图仍为 12/13 条口径；AppShell 侧注释已按 15 更正。建议 conductor 逼 ux-designer 回写。
- **D-2 console-m8 §6.3「Trash Can 常驻节点不建（BinFlow 无回收站）」已过时**：M12 起回收站存在，本票按派单落**管理页**（/admin/governance/trash）而非树常驻节点（console-ui.md 的 Trash Can 树节点形态未建——最小面口径，树节点归后续票裁量）。同文件 §1.3 需随 D-1 一并回写。
- **D-3 词表镜像缺位先例**：`cleanup.run`（T-324 落）在本票前已漏同步于前端镜像——T-346 的 29 枚之外多补 1 枚（共 54）。建议后续后端词表票把「前端镜像同步」写进 AC。
- **D-4 契约漂移：无**。trash REST 族 / storage 浏览面 / addons 槽行 / repositories 策略键透传均与 internal/httpapi 实测一致（trash-can.md 的 wire 拼写逐一核对）。

## 7. 遗留

- trashcan 配置旋钮（binflow.yaml enabled/retention_days）后端未落（trash-can.md 已知边界）——FE 无配置面，槽行/文案如实按「默认 14 天」呈现，落地后无 FE 必改项。
- 回收站真栈 pro 形态 e2e（解锁面）不在浏览器套件（community 真栈纪律）；mock 面已覆盖解锁全流，pro 形态归 m10-tier-matrix 类编排（后续票裁量）。
- 测试实例 /tmp/bf-t352（进程已停，文件留档）；internal/console/dist 为 `make console` 同款构建产物（gitignored），conductor 提交时以 `make console` 复现。
