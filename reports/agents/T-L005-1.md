Ticket:       L005-1 — C14+D1 合票：manifest 冷 miss 负缓存写入（M-a/M-b）+ unquoted INM 按值匹配 + 三处证伪注释连修 + 修后全 304-matrix 重跑（LOOP 005 轨道 1；P 级未具名，台账 D1 review_gate 硬门=修后全矩阵重跑）
Role:         dev-registry-adapter（领域实例：dev-package-docker）
Area:         internal/adapter/docker
Input:        reports/compatibility/L004-negative-cache-probe.md（C14 定谳：T-363 起 manifest 冷 miss 负缓存从未存在、ADR-0048 Errata 已触发）；reports/compatibility/L004-304-ping-diff.md §1/§2/§5.1（D1 定谳：M2/M2c/M2d 三臂 unquoted INM 反转）；docs/reverse/helm.md §8.3（原「tag 404 不写负缓存」口径——本票推翻，文本权归 reverse-engineer）；ADR-0047（链门先例）/ADR-0048 Errata；internal/repo RemoteV2Plane seam（ProbeRemoteCache/CacheRemoteMiss 既有口径）；tools/difftest/l0041/{304,expired}-matrix.sh（重跑载体，零改动）
Changes:      ① D1：client_conditional.go 重写——manifestClientNotModified/blobClientNotModified/quotedEntityTags 三函数并为一个 clientConditionalNotModified（按值不区分引号匹配、任意 INM 在场封死日期臂、etag 空时仅日期臂可答），两读面共用（差异只剩 304 响应形状，分别在 serveRemoteManifestCopy/serveRemoteBlobCopy）；② C14：remote.go serveRemoteManifest 冷 miss 臂（isMissError）前置引用键负缓存探针（NEGATIVE → 本地 unfound 零回源），404 臂 standing==nil 落点补 CacheRemoteMiss 引用键写入；③ 新键法 manifestMissNodePath（digest→既有 image/manifests/<hex>；tag→image/tags/<tag> 命名空间，无 node 冲突、tags/list wire 路径不受影响）；④ virtual.go 404 臂冷 miss 补 V2CacheMemberMiss 成员作用域写入（walk 自身读探针待 repo 面 seam 扩展，注释注明）；⑤ 三处证伪注释连修：client_conditional.go 文件头+函数组、client_conditional_test.go 文件头+blob 矩阵注释、manifest 矩阵 4 个用例名与期望（unquoted→304、M2c→200、M6 机理改名）
Files:        修改 internal/adapter/docker/{client_conditional.go,client_conditional_test.go,remote.go,virtual.go}；新增 internal/adapter/docker/manifest_cold_miss_negative_cache_test.go；新增 reports/compatibility/L005-c14-d1-diff.md + 本日志。未动 internal/repo（seam 零变更）、未动 docs/BOARD/契约（建议回报）
Tests:        manifest_cold_miss_negative_cache_test.go 五子测：M-a tag miss×3 上游恰 1+行落地+60s 到期回源+再冻结 / digest miss 键 manifest 节点路径 / M-b 未知名 repeats 本地（首问 1 次） / HEAD+GET CLI 形状 / 既有 tag 拉通不受 miss 记忆影响（pull-through 保全）；client_conditional_test.go：D1 翻面后矩阵（unquoted 匹配 304、unquoted 非匹配+IMS 200、star 封日期臂、M6 改机理名）
Commands:     go build ./... && go vet ./...（皆零输出）；gofmt -l internal/adapter（空）；~/go/bin/golangci-lint run（0 issues）；go test ./internal/adapter/docker/ -count=1（ok 18.051s）；go test ./internal/adapter/helmoci/ -count=1（ok 34.991s）；go test ./internal/httpapi/ -run TestV2 -count=1（ok 13.839s）；go test ./internal/adapter/docker/ -run 'TestClientConditional|TestManifestColdMissNegativeCache|TestManifestRevalidation|TestRemoteUnfound|TestVirtualUnfound|TestT392' -race -count=1（ok 14.641s）；差分腿=304-matrix.sh/expired-matrix.sh 双端 + C14 三臂 curl + dind CLI pull（原文见 L005-c14-d1-diff.md §5）
Outputs:      差分（uat-l0051-792c347c vs :8082）：304 矩阵 20/20 臂 SAME（M2 304/304、M2c 200/200、M2d 304/304——三 DIVERGENT 全收敛；M6/M2b 未翻面）；过期矩阵 6/6 客户端可见 SAME（X1 上游行 `GET …/manifests/t1 304 0 binflow-remote/1.0` 透传直证保留；X3/X4=D3 既有）；C14：M-a 双端 1/3（BinFlow try1 GET / 参照 try1 HEAD，try2/3 本地 22/7ms）；到期复核双端回源+1+行重写（negative 19:08:53Z→19:09:53Z）；M-b 双端 1/3 同形——**L004-2 参照侧 0/3 未复现**（干净仓+catalog 聚合两态均首问一次）；CLI 拉缺失 tag 上游恰 1 往返、客户端渲染 manifest unknown: The named manifest is not known to the registry.；sqlite 三 negative 行直证
Compatibility: 契约对照结论（L005-c14-d1-diff.md §4，供翻绿）：docker/remote-manifest-conditional-get DIVERGENT→VERIFIED；known-divergence C14 BUG→FIXED；条目 #16 expect 按「首问一次→窗口内本地→到期回源」重写（M-b 勿沿用零回源强读法）。可差分 surface：/v2/<repo>/<image>/manifests/<ref> GET×{quoted/unquoted/star/IMS/组合}（11 臂）、同 by-digest 2 臂、blobs 6 臂、过期窗 4 臂、冷 miss×{tag,digest,未知 image}×{首问/窗口内/到期后}。漂移点：M-b 口径修正（L004-2 参照侧观察未复现，建议 conductor 复核其观察条件）；helm.md §8.3「tag 404 不写负缓存」句已过时（文本权归 reverse-engineer）；internal/repo CacheRemoteMiss doc 注释「TAG miss has no storage path to key」已过时（repo 面一行 doc 票）
Security:     manifestMissNodePath 输入（image/reference/wantHex）均经 parseV2Name/validateManifestTag/parseDigestParam 边缘校验后拼接——tag 字符集不可能构成 dot 段，validateNodePath 二道防线在 CacheRemoteMiss/ProbeRemoteCache 内恒跑；负缓存行仅写入本 repo 键空间（无跨仓污染）；上游会话仍走既有 SSRF 守卫链（本票零上游路径改动）
Performance:  冷 miss 臂新增一次本地 ProbeRemoteCache（sqlite 读，零网络）；404 落点新增一次行写（best-effort，失败仅影响记忆不影响应答）；矩阵 20 臂上游 delta 全 0 证实热路径零回归
Risks:        ① M-b 设计张力：纯本地 oracle 无法既「未知名零回源」又不破坏 pull-through（信息论上限），实现取引用键负缓存（与今日参照活体同形 1/3）；若 conductor 复核 L004-2 后坚持零回源口径，需 repo 面账本（如 docker_refs 式 image 账本）另立票；② virtual 面冷 miss 读探针缺口（V2MemberManifest 只在 rows 存在时探针）——walk 对 tag-keyed 行写而不读，成员直读面可读；repo 面 seam 扩展建议转 conductor；③ 负缓存行令「上游先 404 后补 tag」场景有 ≤missedTTL 窗口的本地 404（与参照同窗口语义，B2 面先例）；④ 计数方法教训：grep 不锚访问行会误计 registry:3 debug/trace 行（本轮曾把 2 计成 3）
Blockers:     无
Next:         ① 契约翻绿三件（conditional-get VERIFIED / C14 FIXED / #16 重写）转 compatibility-engineer；② M-b 口径复核（L004-2 §2.1 子臂观察条件）转 conductor；③ helm.md §8.3 + repo 面 CacheRemoteMiss doc 两处过时文本各一行票；④ virtual 成员 facts seam 冷 miss 读探针（repo 面）候选；⑤ D3（过期 blob 回源）裁定仍悬
