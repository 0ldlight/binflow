# T-171 工作日志 — 文档 5 类（OIDC/LDAP/S3/bf CLI/迁移指南）+ Prometheus 参考

- 票据：T-171 [P2] 文档 5 类（OIDC/LDAP/S3/bf CLI/迁移指南）`role:tech-writer` `area:docs-site/docs/`
- 状态：done（AC 三条全过；见「验证」节）
- 日期：2026-08-22
- area 纪律：内容全部落在文档内容树 `docs/user/`（= docusaurus.config.js 的 docs preset `path: '../docs/user'`；**`docs-site/docs/` 目录不存在**，AC 路径按「或同级目录结构按现状」条款映射至此——tech-writer 的唯一内容写入面即 docs/user，docs-site/ 仅动 sidebars.js 一处注册）。另有两处最小支撑编辑：`docs/user/README.md`（导航补 6 条目）、`docs/user/faq.md`（1 行过时行修正，见「发现」）。

## 交付（6 新文件 + 3 支撑编辑）

| 文件 | 内容要点 |
|---|---|
| `docs/user/guides/oidc-config.md` | auth.oidc 全键表（含缺省/必填）、`BINFLOW_AUTH_OIDC_CLIENT_SECRET` env-only、启动发现 fail-fast、PKCE S256 登录流拆解、`/api/v1/auth/methods` 探测、首登自动建号 + 组 REPLACE 同步 + admin 每次刷新（T-185 后行为）、Keycloak 26.2.5 实测参考配置、Helm `config.oidc` + existingSecret、7 行报错对照 |
| `docs/user/guides/ldap-config.md` | auth.ldap 全键表（含 T-186 的 `start_tls`/`skip_tls_verify`/`group_base_dn`）、bind password env-only、先本地后 LDAP 回退、TLS 三路线一表（ldaps 压过 start_tls 打 WARN；StartTLS 失败拆连接绝不回落明文；无 CA 键走系统信任库 + macOS 陷阱）、osixia ACL / olcTLSVerifyClient 排障实录（T-174 §1/§2）、Chart 新键未渲染的如实注记 |
| `docs/user/guides/s3-config.md` | storage.backend=s3 + storage.s3 全键表、secret env-only、必填校验、`/healthz`/`/readyz` 三连探测（HeadBucket→Put→Delete；endpoint scheme 决定 TLS）、compose `--profile s3` 全流程（.env 键、MINIO_ROOT_PASSWORD=secret、建桶门控）、Helm 段、在线迁移三步（双写→面板/API 启动→completed 手工收口）、status 字段真名（total/running/done/migrated/skipped/failed）、约束与 6 行报错对照 |
| `docs/user/guides/bf-cli.md` | 四子命令旗标表 + 示例（本机实跑）、~/.bf/config.yaml 多 profile（密钥 env 引用制：password_env/token_env）、地址与凭据覆盖优先级、零配置默认、roundtrip 验证步骤、已知边界（create-only、无 config set、virtual 字段走 REST） |
| `docs/user/guides/migrate-artifactory.md` | 三阶段（repos→users→tokens）语义与保序、`--dry-run`/`--resume`/进度文件（端点绑定+原子落盘）、口令与 token 值不可导出的两条 Artifactory 硬约束及策略、迁移后手工收尾清单、**已知缺口如实段**（client 编码面 members/includes/excludes vs 服务端 repositories/includesPattern/excludesPattern——T-191 对齐前 virtual 成员/patterns 丢失，附 curl 补齐 workaround）、制品不在范围、5 行报错对照 |
| `docs/user/metrics/prometheus-reference.md` | `/metrics` 根级端点表（GET/HEAD、0.0.4、匿名默认 + `metrics.require_auth` 401→200）、四类 7 族指标名逐字对齐 `internal/httpapi/metrics.go`、path 归一化折叠表、零流量预播语义（counter/gauge 预置 0；histogram 首观测前无序列——实测修正）、prometheus.yml + PromQL 示例 |
| `docs-site/sidebars.js` | 新增「专题指南」类目（slug `/guides`）注册 6 页 |
| `docs/user/README.md` | 导航补专题指南 6 条目；顺带删掉「API Token · 监控（随里程碑补齐）」占位行 |
| `docs/user/faq.md` | 「LDAP/SAML/OIDC 永不」行 → SAML/报表/扫描永不 + OIDC/LDAP M6 已交付并链指南（M4 时代的 Non-goal 行与 M6 现实矛盾，最小修正） |

## 验证（全部实际执行）

1. **`make docs` 构建通过**：`[SUCCESS] Generated static files in "build"`；docs-size 2.60MB（预算 15MB 内）。broken links/anchors 仅存量 3 处（docker-registry→deploy/dev/README、README→getting-started×2、governance 锚点）——与本票基线一致，**新页零新增**。
2. **AC③ 200 可访问（超票面要求的强证明）**：`make docs` 刷新 `internal/docs/dist` 后重编二进制真机起服，逐路由 curl：
   `200 /binflow/docs/guides/oidc-config`（另 5 页同 200）。sitemap 含全部 6 页；globalData 确认 6 页 sidebar 注册（path 前缀 `/binflow/docs/`）。
3. **配置键名/旗标/指标名与代码交叉核对（grep 证明）**：脚本比对 120 个 token（auth.oidc 8 键、auth.ldap 13 键、storage(.s3/.migration) 12 键、metrics.require_auth、3 个 env-only 密钥名、env 覆盖路径、7 个指标族名、7 个端点、bf 8 旗标+10 配置/env 名、bf-migrate 10 旗标、migration status 8 字段、chart 6 值）对 `internal/config/{load,config}.go`、`internal/httpapi/{metrics,router,server}.go`、`cmd/bf{,-migrate}`、`internal/storage/migration.go`、`charts/binflow/values.yaml`——全数命中（唯二"miss"是检查模式 `json:"started_at"` 未含 `,omitempty` 后缀的假阴性，字段名本身正确）。
4. **真二进制行为实测**（HEAD 构建，临时 data_dir，完毕已清理）：
   - `GET /api/v1/auth/methods` → `{"password":true,"oidc":false,"ldap":false}`（默认关）✓
   - `GET /metrics` 匿名 200 + 7 条 `# TYPE` + 预播 0 序列；`metrics.require_auth: true` 实例匿名 401 / Basic 200 ✓；零流量 histogram 无序列（修正了一处草稿表述）✓
   - `bf` 四子命令全链：建仓→上传（sha256 打印）→curl 回读内容一致→建用户→发 Token（首行值 + token_id）✓
   - 配置拒启三例实录：oidc enabled 缺 issuer_url / `client_secret` 进 YAML（报错点名 env 变量）/ bf-migrate 缺 `--artifactory-url` ✓
5. **文档行为引用来源**：登录/TLS/组同步细节锚定 T-174（真 Keycloak+OpenLDAP 验收）、T-185（组同步修复）、T-186（TLS 修复）、T-167（bf-migrate 细节）；S3 迁移链锚定 T-164/T-160/T-177/T-178。

## 发现（转交 conductor）

1. **`internal/client` 编码面缺口仍在**（T-191 todo 未收）：`RepoCreateRequest` 仍发 `members`/`includes`/`excludes`，服务端读 `repositories`/`includesPattern`/`excludesPattern`——迁移指南已如实标注为已知缺口并给 workaround。T-191 落地后应删该节。
2. **faq.md「永不」行已修**（本票内最小修正）；同文件若有其他 M4 时代口径与 M6 冲突处未逐行清（非本票面）。
3. **install/* 八页至今未注册进 sidebar**（T-141 seam 注释仍在 sidebars.js）——页面可访问但导航不可见，建议随下次文档票收口。
4. T-187 票面仍在 todo，但 `internal/audit` 的 method/reason 字段代码已在（ldap 指南排障节已引用其现状行为）。

## 遗留（待确认项）

- Helm Chart 未渲染 `group_base_dn`/`skip_tls_verify`（T-186 遗留小票）——ldap 指南已如实注记；该小票落地后删注记。
- bf-migrate 对真实 Artifactory 的整体验收仍是 Q9 条件腿——指南头部已声明，建议验收后补「实测」注记。
- 制品本体迁移（H62~H67 面）后续票；落地后迁移指南「制品搬运」节应替换为正式流程。
