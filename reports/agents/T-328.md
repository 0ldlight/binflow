# T-328 工作日志 — 文档五类（认证/存储/四包型接入/api 增量含 L25 反转/FAQ）+ README 刷新

- 状态：done（六新篇 + 六文件增补〔含 license.md 槽位表 15 槽刷新〕 + 双 README 刷新 + 侧栏挂页；`make docs` SUCCESS、路由全生成、断链告警 0）
- 角色：tech-writer；日期：2026-08-28
- 纪律遵守：共享工作树**零 git 操作**；与 T-325（deploy/charts/.circleci）零交集——本票只动 `docs/user/`、`README*.md`、`docs-site/sidebars.js`（conductor 派单授权的侧栏面）及 `make docs` 生成物。

## 一、五类清单

### 1. 认证配置指南（admin/auth-config.md，新，~200 行）

- 控制台三 Tab（/admin/security/auth：ldap|oauth|saml）+ REST 九端点表（门 = CapSecurityRead / CapSecurityWrite，test 归写门）。
- **哨兵语义 write-only 三态表**（缺席=保持 / ""=清除 / 新明文=替换 / 回传 20 星 → 400）——**scratch 实例实测**往返（含 400 文案逐字 `refusing the masked placeholder…`）。
- enc:v1 密封与主密钥前置（无钥拒写、存量行 fail-fast）；保存即生效（PUT 禁用 LDAP → `/api/v1/auth/methods` 位翻转，实测）；测试连接 TestReport 实测（dial/unreachable）；首启种子双源 + 「带 secret 文件段种子需 BINFLOW_REMOTE_CREDENTIALS_KEY」升级注意（T-305 遗留 5 的部署侧承接）。
- 已知边界如实登记：SAML 运行时 SP 断言消费不在 M11、证书动作端点缺口、userDnPattern 未消费、LDAP 单段模型。

### 2. 存储配置指南（admin/storage-config.md，新，~150 行）

- binstore.yaml schema（version/chain/migration；三链表；s3 参数族=内嵌平移；secret 恒 env）。
- **Q5 三分支并存裁决表** + 分歧拒启边界注记（显式默认值也算显式）。
- **fail-fast 四形态报错原文全部实测落文**（语法错带行号 / 明文 secret 带 env 逃生口 / cache-fs 保留名 / 双源分歧点名两文件）；启动 INFO `storage chain resolved`（providers/mode/source）与兼容窗 WARN、权限 WARN 实测；升级与在线迁移路径表；s3/dual-write 链引 T-306 MinIO roundtrip 证据。

### 3. 四包型接入指南（integrations/，新四篇）

| 篇 | 要点 | 命令证据源 |
|---|---|---|
| conan.md（sidebar 27） | v2/v1 双客户端、修订链 latest、remote/virtual（search 不代理、v1 仅 local、写路由）、reindex 双端点 | T-308（conan 2.31.2 + 1.66.0 客户端 e2e）/T-312 |
| helm-charts.md（28，**与 install/helm.md〔Chart 部署〕命名区分**，篇内双向辨析） | index.yaml 重算、relative urls、.prov clearsign 配方（detach-sign 会被 helm 拒——T-309 D-1 实测发现）、enforce 403 逐字、virtual S8 改写/_external/_transitive、helm·helmoci 不混仓 | T-309/T-313（helm 4.2.4 + gpg 2.5.21 + kind install deployed） |
| rpm.md（29） | debPUT 同构的 .rpm 上传 + **RP-2 默认不自动重算**注记、reindex 七分支、dnf 配置、gpgcheck 三步（rpm --import served repomd.xml.key）、keypair 关联、SHA-256 唯一例外 | T-311/T-315/T-322（Rocky 9 dnf 全链 + repo_gpgcheck=1 三腿） |
| debian.md（30） | debPUT 坐标矩阵参数（三坐标必带 400）、索引族/byHash、apt trusted=yes 与 signed-by dearmor（armored 直用会挂——T-321 容器腿真问题）、InRelease/Release.gpg 签名、remote archive-root、virtual 现算聚合/签名族 404 回退 | T-310/T-314/T-321（bookworm 容器 + dpkg-deb 真包，262s gpg 链） |

每篇含：用途 → 前置条件（pro 档门控三缝）→ local/remote/virtual 三段 → 边界表 → **常见报错对照表** → 下一步交叉链。

### 4. api-reference.md 增量（M11 增补速览节，+~110 行）

- **L25 反转（T-317）**：`enableTokenAuthentication`（Bearer 头切换）+ `contentSynchronisation` 四子字段表（propertiesEnabled 拉取侧附着生效；statisticsEnabled/sourceOrigin 接受回显无行为如实标注）；**scratch 实测** PUT+GET canonical 回显。M10 速览节的 smart remote 注意行同步退役按名 400 表述（remote-virtual.md 字段表同步：两字段行 + unusedArtifactsCleanupPeriodHours 改「M11 起生效」+ cleanup 状态面指引）。
- **keypair 族（T-319）**：九端点表（Artifactory 兼容 7 + BinFlow 原生 generate + v2 关联双形态）；Summary additive 字段；**scratch 实测** generate 201/list（algorithm=RSA-2048 回显）/verify（`Key was verified.`）/delete（`OK`）；X-GPG-PASSPHRASE 不收（D-8）；删除 in-use 护栏。
- **cleanup 端点（T-324）**：POST（dry-run 默认）+ GET 状态面；三腿语义、在用 oracle（审计 trails ∪ virtual 成员）、audit.enabled=false 时 policy 腿拒绝；**scratch 实测** dry-run 报告与 status 全字段。`/api/v1` 自有端点表补 cleanup 行。
- **/api/yum 虚仓分支（T-315，conductor 已接线）**：四包型 reindex 管理族一表（conan 双拼写/helm 全仓+部分/deb async/yum 含虚仓 200/202 + path 自动补 /repodata + auto-async 409）。
- 头部适用版本 M1~M9 → M1~M11；下一步链接补六新篇。

### 5. faq.md 增补（M11 增补四问）

四包型 tier 矩阵问（十二包型 × 三档表）、license 失效数据安全问（四包型逐条 D1/D2）、四包型 remote/virtual 差异速览表（search 不代理/v1 仅 local/聚合不签名/RP-2 默认/binstore+auth 新面升级零动作问）。M4 不兼容清单的「SAML 登录」行更新为「配置面已交付、运行时不在 M11」现状。头部版本行同步。

## 二、README 变更摘要（README.md + README.zh-CN.md 同步）

- 「五大包型」→ **十二包型矩阵表**（community 五核心 / pro 七包型含 M10·M11 分批 / enterprise 功能槽位）+「读永不劫持」一句话档位语义 + `/api/v1/addons` 实时判定。
- 里程碑叙述补 M10/M11 段（license 门控、四包型、认证配置面、binstore.yaml、keypair 签名、cleanup）。
- 文档索引表：M9 行 → M11 行（PRD 链接改 milestone-11.md）；「M1→M9 all done」→ 「M1~M10 done and tagged；M11 in final verification」。
- 新增 M10/M11 小节：license 安装/查询命令、四包型三行示例（conan remote add / helm repo add / dnf）、认证配置面（控制台+REST 路径）、binstore.yaml、keypair 与 cleanup 一段。
- 配置节：storage 注释指向 binstore.yaml；auth 段注「首启种子进运行态配置面」；两段式新面说明（binstore.yaml 语义 + 运行态认证权威源）。
- 许可/状态节：九里程碑 → M1~M10 tagged + M11 终验中；移除过时的 213 条目 M9 时点统计。

## 三、docs-site 与构建

- `docs-site/sidebars.js`：客户端接入组追加 conan/helm-charts/rpm/debian（27~30 位），管理指南组追加 auth-config/storage-config（48/49 位）——conductor 派单授权的侧栏面；**未动其它装配**。
- `make docs`：**SUCCESS**，`docs site size (raw total): 3.93 MB`（预算 ≤15MB）。
- 路由核对：`build/{integrations/{conan,helm-charts,rpm,debian},admin/{auth-config,storage-config}}/index.html` 全生成；**断链/断锚告警 0**（onBrokenLinks=warn 下无输出）；搜索索引（hashed assets）含新篇关键词（binstore 抽查命中）。

## 四、自测命令与证据（实际执行）

| 命令 | 结果 |
|---|---|
| `go build ./...` / `make build` | OK（bin/ 三二进制产出——scratch 实测前置） |
| scratch 实例（:8328，BINFLOW_REMOTE_CREDENTIALS_KEY=32B，HEAD 构建） | auth 三段 GET 默认形 / PUT ldap 200 + GET 哨兵 `********************` / 原样回传 400 逐字 / 缺 secret 键 PUT=保持 / ldap/test 400+TestReport / methods 位随 enabled 翻转（两次） |
| keypair 面实测 | generate 201（RSA-2048 公钥回显）/ list（algorithm/createdAt/repositories additive 字段）/ verify `Key was verified.` 200 / delete `OK` 200 |
| L25 实测 | PUT remote generic + 两字段 → 200；GET canonical 回显 `enableTokenAuthentication:true` + contentSynchronisation 四子字段 |
| cleanup 实测 | POST `{}` dry-run 200（repos[0] periodHours=168/cutoff=7d 前/全零计数）；GET status（tickEvery=1h0m0s） |
| binstore 实测（:8329 独立实例五次 boot） | [filestore] 链 INFO + 0644 权限 WARN / 明文 secret 拒启（line 6 + env 提示）/ cache-fs 拒启（保留槽文案）/ 双源分歧拒启（两文件点名）/ 兼容窗 WARN（embedded source） |
| `make docs` | SUCCESS（3.93MB）；路由 6 新页全生成；断链 0 |

四包型接入篇的命令**未在 scratch 复跑**（pro 档门控，scratch 无 license）——按派单口径全取各票真实客户端日志（T-308/309/310/311/312/313/314/315/321/322 的实测 transcript），行为描述对照 ADR-0036/0038 与代码复核。

## 五、遗留 / 待确认

1. **四包型命令的 QA 复跑**：conan/helm/rpm/debian 各篇命令取自票日志，建议 T-329 终验时在 pro 实例上抽样复跑（尤其 debian signed-by 与 rpm gpgkey 两段的最长链）。
2. **SAML 运行时**：配置面已写明「SP 断言消费不在 M11」；SAML 登录腿落地后需更新 auth-config.md 已知边界表与 FAQ M4 清单行。
3. helmoci（T-320 未触发）：落地后 helm-charts.md 的「helmoci 另有槽位」行与虚仓混仓表述需复核。
4. cargo remote/virtual（T-316/T-318 卡用户裁决）：cargo.md「remote/virtual 归 M11」表述与其落地时同步刷新。
5. bz2 索引压缩档（T-310/314 降级登记）：网络恢复补 writer 后 debian.md 压缩集行需一行更新。
6. ~~license.md 槽位表漂移风险~~ **已顺手收口**：`admin/license.md` 槽位矩阵刷到 15 槽（+conan/helm/rpm/debian 四行、档位解锁数 6→13→15、三档语义行同步「七个包型」）——与 FAQ/README 的十二包型表述单一口径。
