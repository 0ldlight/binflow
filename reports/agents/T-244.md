# T-244 工作日志 — 键盘可达与焦点管理补齐（FR-75）+ 前端共享层债收口

| 项 | 值 |
|---|---|
| 票据 | T-244 [P1]（BOARD B5 尾票，area：`web/src/components/` 共享层 + `styles/base.css`/`pages.css` 债点 + 页面键盘小补丁 + console-ux §10 回写 + e2e） |
| 角色 | dev-frontend |
| 日期 | 2026-08-24 |
| 状态 | done（六项债全收口；四绿 + 键盘 spec 6/6 + axe 双主题 8 扫描 serious=0 + 全量 --workers=1 绿 + gzip 192,565 B / 350KB 预算 55.0% 维持） |
| 过程事故 | 03:37 API 配额上限击落，06:02 配额重置后恢复续作（conductor 备忘）——击落点在第⑥项锚册对账脚本编写中，前五项改动经 git status 逐文件核对完整在树后续写，无返工 |

## 0. 交付总览（六项债收口清单）

| # | 债 | 落地 | 证据 |
|---|---|---|---|
| ① | base.css 语义 badge 亮主题对比度家族（warning 4.26 / success 4.14 / danger 4.12） | 四语义族文字色改 `color-mix(语义色 78%, --bf-text)`（15%/18% tint 底不动） | WCAG 核算脚本：双主题 × {surface-1, surface-2（悬停行）, bg} 三承载面全 ≥4.59:1（修复前最差 3.55）；axe 双主题 8 扫描 color-contrast serious=0 |
| ② | pages.css 死掉的 .stepper 族 | 删 `.stepper`/`.dot`/`.step-label`/`.sep` 5 块（约 37 行，T-240 单页分区式后零引用）；`.form-layout` 仍在役保留 | grep src/tsx 零引用；build 绿 |
| ③ | AppShell quick-set-me-up 占位接线 | 菜单项 Link→button，开全局 SetMeUpDialog（无仓上下文 → 步骤 0 网格）；关闭回焦菜单钮；会话菜单加 ↑↓ 循环 | keyboard.spec 腿 5：Enter 开 smu-dialog + Esc 关 + 回焦 session-toggle 断言 |
| ④ | quota 重组 helper 双份 | `buildLocalQuotaBody` 抽 `lib/repos.ts`；QuotasPage 与 RepoDetailPage 两份同源副本合一（行为零变化） | tsc/eslint 绿；governance + repositories 域 spec 复跑绿 |
| ⑤ | 树页双 Deploy 入口 | 面包屑 `tree-upload`（UploadDialog）退役；页头 `tree-deploy`（DeployDialog）唯一入口（console-m8 §6.3[1] 裁定）；UploadDialog 删除、`mavenTarget` 归位 `lib/maven.ts`（T-242 seam：components→pages 反向依赖消除）；空目录 CTA + 目录上下文（preselectedDir）带入；readonly_admin 禁用平移到 tree-deploy；补双 403 降级臂（普通 user 无 manage 时按 Generic 直传——W12d 语义保全） | 9 个 spec 文件上传腿全量迁移 deploy-* 锚全绿；W12a（409/413）、maven GAV 预检零写请求、B1 关闭落闸（含哈希相）、403 行内指引四组语义腿在新对话框下复证 |
| ⑥ | console-ux §10 锚册回写 | v1.7：T-238 存储批 12 锚（D-1）+ T-242 对话框批 48 锚 + D-2 散锚（含审计新溯的 T-158/T-160/T-177/T-218/T-241 历史散锚）+ D-3 显式退役条目（settings-password + 本票 tree-upload/upload-* 15 枚）+ §10.6 死锚登记（D-4，112 家族）+ 三方对账器 `web/scripts/anchor-audit.mjs` | 对账终态：src 423 家族/493 落点，册 482，**unregistered 0 / broken 0**，retired 表恰等于已登记退役集 |

## 1. 键盘全链（FR-75 / console-m8 §3.4 + §9）

**共享件**（新 `lib/keys.ts`）：

- `onTablistKeys`：role=tablist 容器 ←/→ 切换（选中 + rAF 落焦）——接入 `node-tabs`（NodeDetail）、`repo-tabs`（RepoDetailPage）、`repos-tabs`（RepositoriesPage，nav 形态 Tab 只加方向键不改语义）；smu-tabs 自有实现维持。
- `onTableRowKeys`：数据表行 Enter 激活 + ↑/↓ 同 tbody 行间移动——接入 repos/users/perms 三列表行（行补 `tabIndex={0}`）；树页 tree-list 行原有实现维持。

**对话框复核**：ConfirmDialog / SetMeUpDialog / DeployDialog 三处焦点陷阱的 FOCUSABLE 选择器加 `:not([disabled])`——禁用钮（强确认前置未满足 / 空队列的部署钮 / 空口令的提交钮）此前会让陷阱首尾落在不可聚焦节点上破口。SetMeUpDialog 包类型网格（radiogroup）补 ←↑→↓ 焦点移动。

**批量选择 Space**：无承载面——console-m8 §3.5 定案「无批量选择」（无复选框列、无批量端点），不造批量 UI；`lib/keys.ts` 头注钉死口径。

**spec**（新 `e2e/m8/keyboard.spec.ts`，6 腿）：① 登录全键盘 + 侧栏 Tab 落点 + nav-mode-switch 双向 Enter（aria-current 同步断言）；② 树方向键全语义（↓ sibling → 展开加载 → 子节点 Enter 进路径 → 切仓后 ← 收起）+ 表格行 ↑↓/Enter（目录进路径、文件进 ?focus=）+ Shift+F10 菜单 + Esc；③ 管理表行（repos + users 双页实证共享件）；④ tablist 方向键（repo 三 Tab + node 两 Tab）；⑤ 对话框陷阱（Tab 首尾循环跳过禁用钮、连打 10 次 Tab 不漏、Esc、ConfirmDialog 初始聚焦取消）+ quick-set-me-up 接线；⑥ axe 双主题（明示 localStorage 落主题）× 4 个 badge 密集页（树/仓库列表/用户/仪表盘）= 8 扫描 serious/critical=0。

**坑与修法**（spec 编写期）：

1. 跨仓根（repoKey=''）时树的懒加载效果早退——手动展开只翻视觉态不拉子层；展开断言须在任一仓内路由做。
2. `page.goto` 是整页装载（组件重挂载、expanded 归零）——「收起」断言前须重新展开。
3. 选中仓钉住展开态（`isOpen = expanded.has || selectedRepo===key`）——收起断言须先切到别的仓。
4. 相邻行断言不可假设唯一（实例上累积多轮测试的仓）——改为「焦点移到同族另一行」的序无关断言。
5. nav-mode-switch 连击 Enter 会用旧 mode 闭包重导航同址——第二次 Enter 前断言 aria-current（等 React 提交）。

## 2. 共享层债细节

- **① 对比度**：修法与 T-237 `role-warning` / T-241 `perm-verdict` 的页面组自持修法同源上收；核算发现 default badge（info 亮 4.27）与暗主题 danger（4.20）也在线下，一并修（家族收口而非三枚点名）。78% 配比在保证双主题 × 三承载面 ≥4.59 的前提下保留最多语义色相（82% 在亮 surface-2 悬停行上 4.35~4.49 不过门）。页面组自持类（role-warning/perm-verdict，82%/15%）在其承载面上仍合规，保留不撤——登记为其属主页面的可选清理项。
- **⑤ 收敛**：UploadDialog 删除后 tree.css 的 `.upload-*`/`.drop-zone`/`.gav-grid`/`.preview-path` 死样式族一并清除（DeployDialog 用 dialogs.css 自持同形类）。t104-supplements 的 docker P6 腿语义改写：断言 DeployDialog 候选下拉不含 docker 仓（原断言 tree-upload 不渲染——全局入口收敛后按仓不可达的语义由候选集承载）。

## 3. 锚册对账器与 v1.7 回写

`web/scripts/anchor-audit.mjs`（node 直跑零依赖）：src 侧收 `data-testid` / `testid` prop / prop 缺省值（`?? 'empty-state'`）/ `itemTestid` 回调 / 三元双臂（perm-matrix/-groups 显名）五种形态；册侧 `{a|b}` 备选展开（内容类含数字/连字符——`password2`、`copy-repo-path`）+ `<动态段>` 家族化；spec 侧三形态引用。四表输出，broken=0 为硬门。

终态：**src 423 家族 / 493 落点；spec 372 家族（1,356 具体引用）；册 482；unregistered 0；broken 0；retired 恰为已登记退役集；死锚 112 家族（26%，§10.6 全量清单 + 守卫规矩：新票锚消费率 ≥80%、broken 必须为空、死锚两大版本零消费走退役流程）**。

册侧顺手规范化：v1.2~v1.6 行文的逗号短写族（`user-form-{name,...}` 等 9 处）改竖线备选/显名列出——对账器此后可精确展开，不再依赖人工对照。

## 4. 自测证据

```sh
cd web
npx tsc --noEmit          # 零输出
npx eslint .              # 零输出（0 error 0 warning）
npm run build             # assert-tokens OK；vite ✓；SPA payload gzip = 192,565 B
node scripts/anchor-audit.mjs
  # src 423 家族/493 落点；unregistered=0；broken=0；死锚 112
make console && make build  # embed 27.4 MB（预算 40MB）；gzip 192,565 B = 55.0%
```

真栈（scratch 数据目录 /tmp/t244-data + 新鲜二进制 127.0.0.1:18092，seed 10,291 节点）：

```sh
BASE=…:18092 npx playwright test e2e/m8/keyboard.spec.ts        # 6/6 passed（含 axe 8 扫描）
BASE=…:18092 npx playwright test e2e/artifacts.spec.ts e2e/m8/artifacts-tree.spec.ts  # 15/15
BASE=…:18092 npx playwright test e2e/t104-review-leftovers.spec.ts                     # 3/3（403 降级臂复证）
BASE=…:18092 npx playwright test --workers=1
  # → 147 passed / 1 failed / 4 skipped（6.3m）
```

- 4 skipped = 3 条既有 dind/docker-CLI 降级腿（T-232 基线口径）+ 1 条 armed
  step-up 探针腿（默认实例自动 skip）——与 T-242 全量形态一致。
- 1 failed = `t146-docs.spec.ts G19b-1`——**例门口径非回归**（沿 T-236/T-240/
  T-242 同款登记）：该 spec 锁 `ADMIN_PW=docsqa-test-pw-146`，本实例口令是
  `password`。用匹配口令的 fresh 实例（:18093，`BINFLOW_ADMIN_PASSWORD=
  docsqa-test-pw-146`）复跑：

```sh
BASE=…:18093 ADMIN_PW=docsqa-test-pw-146 npx playwright test e2e/t146-docs.spec.ts
  # → 8 passed（含 G19b-1 帮助链接腿——本票 topbar-help 锚的消费者）
```

收尾清理：本票实例（18092/18093 + scratch 数据目录）与 T-242 遗留实例
（18088 + /tmp/bf-t242*）全部停止并删除，端口已释放。

## 5. 契约漂移

1. **DeployDialog 候选集语义**（本票补臂暴露）：`GET /api/repositories`（CapRepoRead）与 `GET /api/repositories/{key}`（CanManageRepo）对无 manage 的普通 user 双 403——旧 UploadDialog 不查仓清单（树页喂 repoKey+mode），DeployDialog 查。收敛后补「双 403 → 入口仓按 Generic 直传降级」臂保 W12d 语义（写权限由内容面 ACL 终裁，403 行内指引不变）。这是 UI 编排差异，非契约变更；登记备查。
2. **迁移面板 §4.11 框注过时**：console-ux §4.11 写「控制台无触发/写入口」，但 T-177 已为迁移加控制台启动入口（migration-start 等四锚实存）。v1.7 入册时以注记指明「以 src 为准」——§4.11 框图的正式改写归 ux-designer 后续版本。
3. 其余（键盘语义/焦点陷阱/锚退役流程）与 console-m8 §3.4/§9、console-ux §10 纪律一致，无漂移。

## 6. 遗留与移交

- `repos-deploy-<key>` / `repo-deploy` 入口对 readonly_admin 未禁用（PUT 403 行内呈现兜底）——与 tree-deploy 的预收敛禁用不完全一致；统一归仓库域后续微调（跨 T-240 页面组 area，本票不越界）。
- T-237/T-241 页面组自持对比度类（role-warning / perm-verdict-*）在新 base 下冗余，可回退到语义 badge——归其属主页面清理（非必须，现值合规）。
- §10.6 守卫规矩（锚消费下限 AC / broken 硬门 / 死锚两大版本退役流程）建议 conductor 纳入后续票 AC 模板。
- 锚册 v1.7 的 423 家族口径与旧「落点」口径（293/242 等）并存——§10.6 已注明口径定义；后续版本可统一只按家族口径报数。

## 7. 变更清单

```
web/src/lib/keys.ts                    新（tablist/表格行键盘共享件）
web/src/lib/maven.ts                   新（mavenTarget + GavForm 归位，UploadDialog 删除后共享层承载）
web/src/lib/repos.ts                   +buildLocalQuotaBody（④）
web/src/components/AppShell.tsx        quick-set-me-up 接线 + 全局 SetMeUpDialog 位 + 菜单 ↑↓（③）
web/src/components/ConfirmDialog.tsx   陷阱选择器排除 disabled（复核）
web/src/components/SetMeUpDialog.tsx   同上 + 网格 radiogroup 方向键
web/src/components/DeployDialog.tsx    同上 + preselectedDir + 双 403 降级臂 + mavenTarget 改引 lib/maven（⑤）
web/src/pages/artifacts/UploadDialog.tsx  删除（⑤）
web/src/pages/artifacts/ArtifactsBrowser.tsx  tree-upload 退役/空目录 CTA→Deploy/tree-deploy readonly 门（⑤）
web/src/pages/artifacts/NodeDetail.tsx     node-tabs 方向键
web/src/pages/repositories/RepoDetailPage.tsx  repo-tabs 方向键 + quota helper 改引 lib（④）
web/src/pages/repositories/RepositoriesPage.tsx  行键盘 + repos-tabs 方向键
web/src/pages/repositories/tree/tree.css   死 upload 样式族清除
web/src/pages/security/UsersPage.tsx       行键盘（+useNavigate）
web/src/pages/security/PermissionsPage.tsx 行键盘
web/src/pages/governance/QuotasPage.tsx    quota helper 改引 lib（④）
web/src/styles/base.css               badge 对比度家族（①）
web/src/styles/pages.css              .stepper 族清除（②）
web/scripts/anchor-audit.mjs          新（三方对账器，⑥）
web/e2e/m8/keyboard.spec.ts           新（键盘全链 6 腿 + axe 双主题）
web/e2e/{artifacts,t104-perf,t131-g30e,t104-crossbrowser,t104-review-leftovers,t104-supplements}.spec.ts
web/e2e/m8/artifacts-tree.spec.ts     上传腿迁移 deploy-*（⑤）
docs/design/console-ux.md             §10 v1.7 回写（⑥）
```
