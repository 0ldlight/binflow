# T-L002-1 — C10 修复：remote blob 代取的 marker 门控语义（ADR-0047 DigestChainGate）

```
Ticket:       L002-1（LOOP 002 轨道 1）C10 修复——remote blob 代取的 marker 门控语义 / ADR-0047 DigestChainGate / P0
Role:         dev-registry-adapter（领域实例：dev-package-docker + dev-package-helmoci，docker+helmoci 域唯一实例）
Area:         internal/adapter/docker + internal/adapter/helmoci（+票面明示的 repo/metadata 最小缝：refs 账本经接口）
Input:        DECISIONS.md ADR-0047/ADR-0048；docs/design/remote-cache-v2.md §2/§3/§4（D1-D4）；reports/compatibility/
              L000-docker-remote-diff.md C10/C10b + L000-docker-remote-evidence.md §10（E4-1/E4-2/E6-2）；
              docs/compatibility/contracts/docker-remote.yaml 条目 docker/remote-blob-unknown-digest-404
Changes:      ① internal/repo/api.go 新增 DigestChainGate optional facet（type-assertion discovery，先例
              RemoteV2Plane/V2VirtualPlane）：BlobInChain（direct 臂，read-gated）+ V2MemberBlobInChain（virtual
              walk 成员作用域，membership-guarded）。② internal/repo/dockerremote.go 实现 BlobInChain
              （validateDockerImage/validateDigest + loadRemoteV2Repo 门 + blobInChainCore 账本查询）；dockervirtual.go
              实现 V2MemberBlobInChain（v2VirtualGuard 成员守卫 + 同 core）。③ internal/metadata DockerStore 增
              BlobInImageChain（(repo_key,image,blob_digest) 精确作用域 EXISTS 查询；EXPLAIN QUERY PLAN 实测走
              PK covering (repo_key,image) 前缀 seek，零新索引、零迁移——嵌面 hookDockerStore 借 embed 自动升面）。
              ④ internal/adapter/docker remote.go serveRemoteBlob direct 臂接门（probe miss 且无 standing 副本 ⇒
              查 gate ⇒ false → 既有 blobUnfound 臂本地 404 且不写负缓存行；standing 副本 = node 存在性短路，
              永不问 gate）；virtual.go serveVirtualRemoteBlob 成员臂同构（拒绝 = virtualMemberUnfound 无 summary，
              walk 终态保持 E6-2 plain body）。⑤ **伴生根因修复**：remoteManifestRefs 此前以空 Content-Type 调
              parseManifest（恒失败——refs 账本在 remote manifest 落地时从未写入，潜伏缺口，门控是其首个消费者），
              改传协商 mediaType——refs 写入时机自此才真正与 Artifactory createManifestMarkers 同构。⑥ 可观测性：
              门控 oracle 故障 WARN（cache_result=chain-gate-error，故障放行=降级到 pre-ADR 姿态不 wedge pull）；
              refs record 失败 WARN 升级（cache_result=chain-unrecorded + 耦合说明——ADR-0047 边缘①，不改
              best-effort 契约）。闭集纪律：仅 digest-addressed {docker, helmoci}（helmoci 经同一 docker /v2 栈
              天然覆盖，helmoci 包补钉腿）；maven/npm 等路径寻址协议不引入；blob streaming 直代面不做（UNSUPPORTED）。
Files:        修改: internal/repo/api.go、internal/repo/dockerremote.go、internal/repo/dockervirtual.go、
              internal/metadata/api.go、internal/metadata/substores_docker.go、internal/adapter/docker/remote.go、
              internal/adapter/docker/virtual.go、docs/compatibility/contracts/docker-remote.yaml（仅条目
              docker/remote-blob-unknown-digest-404 的 binflow_state/status/evidence 注记——票面明示允许）、
              reports/compatibility/L000-docker-remote-diff.md（追加复验 L002-1 段——票面指令）
              新增: internal/adapter/docker/remote_chain_gate_test.go、internal/adapter/helmoci/remote_chain_gate_test.go、
              internal/repo/digest_chain_gate_test.go、internal/metadata/docker_chain_query_test.go
              删除: 无（调试用 gate_debug_test.go 已删净）
Tests:        单测（全部真实栈，行为命名文件）——docker 包 TestChainGateColdDigestPosture（链内双 digest 代取 200/
              链外本地拒 + 上游 journal 零 blob 往返 + 拒绝不写负缓存行）、TestChainGateShutUntilManifestLands
              （C10 原案：门关零往返→manifest 落地→同 digest 代取 200 MISS）、TestChainGateUnrecordedRefsRefuseBlobs
              （refs 缺行=record 失败耦合，ADR-0047 边缘①：blob 拒但 manifest 续服务）、TestChainGateEvictedNodeRefetches
              （node 逐出 + refs 记忆→重取 200 MISS）、TestChainGateInChainUpstream404KeepsNegativeCache（ADR-0048
              交界：链内上游 404 付一次往返且负缓存行在、重复 miss 零往返）、TestChainGateVirtualWalkPerMember
              （virtual walk 成员作用域 + 拒绝终态 plain body + Resolved-From）；helmoci 包
              TestRemoteChainGateHelmOCI（家族覆盖：随机 digest 本地 404 上游计数冻结 + 链内 chart blob 续代取）；
              repo 包 TestDigestChainGateLedger/TestDigestChainGateVirtualMember（facet 契约：账本应答/非 remote 类拒/
              成员守卫）；metadata 包 TestDockerBlobInImageChainScoping（image 作用域精确匹配——他 image 同仓 digest
              链外、异 manifest 同 image 链内、异仓恒 false）。既有套件零回归（含 T-392 全链/helmoci 链/virtual 双域/
              unfound 形态族）。
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter internal/repo internal/metadata（空）&&
              ~/go/bin/golangci-lint run ./internal/adapter/docker/... ./internal/adapter/helmoci/... ./internal/repo/... ./internal/metadata/...（0 issues）
              go test ./internal/adapter/docker/ ./internal/adapter/helmoci/ ./internal/repo/ -count=1（全绿，另附 metadata 包）
              go test ./internal/adapter/docker/ ./internal/adapter/helmoci/ -run 'TestChainGate|TestRemoteChainGate|TestT392RemotePullThroughChain|TestRemotePullThroughChain|TestVirtual' -race -count=1
              go test ./internal/adapter/... ./internal/remote/ ./internal/httpapi/ -count=1（17 包全绿，根因修复波及面核验）
              UAT: docker compose -p binflow-uat --env-file deploy/compose/.env.uat -f deploy/compose/docker-compose.yml up -d --build
              重放: curl（token+三步 blob/manifest）+ docker exec difftest-dind docker pull/rmi/pull（真实 docker CLI 27.x）
Outputs:      门未开 blob GET → 404 {"errors":[{"code":"BLOB_UNKNOWN","message":"blob unknown to registry",
              "detail":{"blobSum":"sha256:c6348fa8…"}}]} in 4.99ms；manifest 拉取 200 MISS（上游 1 往返）→ 同 digest
              200 459B MISS（上游 1 往返）；随机 digest ×3 → 404 in 4.5/12.2/15.9ms，上游日志该 digest 请求数=0；
              dind pull 成功 + rmi 重拉 0.157s 零上游；UAT 服务日志零 "chain gate" WARN。详见 diff 报告 L002-1 段五步表。
Compatibility: 契约条目 docker/remote-blob-unknown-digest-404：DIVERGENT→VERIFIED（binflow_state verdict SAME，
              timing 断言 upstream_rtt:false/max_ms:100 由新行为直接通过——4.5-15.9ms 实测；evidence 增 L002-1 锚）。
              可差分 surface（供 differential-qa-engineer L5）：GET/HEAD /v2/<remote>/<image>/blobs/sha256:<digest> ×
              {链外随机 digest（本地 404 零往返）| 链内未缓存 digest（代取 200 MISS）| manifest 前后时序（门开）|
              重复链外（恒零往返）} + virtual 面同臂（终态 plain body）。漂移点：无（对拍形态与 E4-1/E4-2/E6-2 逐案对齐）。
              known-divergence.yaml#docker/remote-blob-marker-semantics 的 BUG→FIXED 翻转归 conductor/
              compatibility-engineer（本票不写台账）。C14 维持 ADR-0048 INTENTIONAL（本轮零触碰）。
Security:     facet 全链 read-gate（loadRemoteV2Repo 复用既有授权对）；virtual 臂 membership 守卫（不放大到成员
              权限对）；门控拒绝路径零上游接触——盲代取面（任意 digest 打上游）收窄为链内闭集，SSRF/扫描面直接收敛；
              输入校验（validateDockerImage/validateDigest/精确参数化 SQL）无新增注入面。
Performance:  门控查询 = 每冷 miss blob GET 一次 metadata EXISTS（EXPLAIN QUERY PLAN：SEARCH docker_refs USING
              COVERING INDEX sqlite_autoindex (repo_key=? AND image=?)——无需补复合索引）；链外 digest 省去一次
              上游 RTT（docker hub 限流场景收益）；大制品流式路径零改动。
Risks:        ① 存量缓存迁移面：修复前落地的 remote manifest 无 refs 行（remoteManifestRefs 空类型 bug）——其未缓存
              blob 会被门控拒绝，直到 manifest 重拉（TTL 再验证或客户端重取 tag）补记 refs；UAT 卷 busybox 全 blob
              已缓存不受影响，生产升级窗口需知悉（缓解=触发 manifest 重验）。② oracle 故障放行姿态=降级 pre-ADR
              盲代取（WARN 可见），未配置熔断——上游 metadata 故障时退化为旧行为（可接受方向，已注释声明）。
              ③ 上游日志旁路流量（LOOP 002 并行轨共享 difftest-upstream）已按路径核对隔离，纯全局计数不可用。
Blockers:     无
Next:         ① known-divergence docker/remote-blob-marker-semantics BUG→FIXED 翻转（conductor）。② 设计稿 §8 票 B
              （包型检索窗 21600s）/票 C（上游条件 GET+304 透传，C06 头集票先行）/票 D（Invalidate 连带删 refs——
              metadata 需 DeleteRefsByImage 面）。③ 差分 L5 重放纳管本票 surface 清单（C10 已灭，C14 INTENTIONAL
              复测附腿仍开放）。④ 金样采集候选：链外 blob 404 body（E6-2 逐字已在契约冻结，可直接采）。
```

## 断点快照（如被中断）

已完成：全部代码 + 四门 + 四包测试 + race 子集 + 17 包波及核验 + UAT 重建（uat-l0021-5f9c48d0）+ C10 五步重放 +
diff 报告 L002-1 段 + 契约条目更新 + 本日志。未完成：无。断点位置：无。
