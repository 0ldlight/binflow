```
Ticket:       L007-2 · D3 定向探针（docker/remote-blob-expired-revalidation LOOP 007 裁决门）+ virtual 面冷 miss 活体差分（L006-2 挂账）· P1
Role:         differential-qa-engineer
Area:         tools/difftest/l0072/ + reports/compatibility/（docker remote/virtual U-PROTO 面）
Input:        conductor 派发（双件套、串行禁后台、凭据不落盘、资产删净）；known-divergence.yaml D3 条（authority=pending「LOOP 007 定向探针后裁」）；L004-304-ping-diff.md §3 X3/X4（D3 原证）；T-L006-2.md Risks「virtual 面活体差分未跑」；L003-3.md §2 重建指南；develop HEAD=c1193f5f
Changes:      ① UAT 重建至 HEAD（镜像源断网 → 离线变体构建脚本 tools/difftest/l0072/build-image-offline.sh：本地 alpine:3.24 RepoDigest 钉基 + 复用缓存 assets 镜像 + classic builder 复放 build-release.sh phase 1/2/4）② D3 定向探针五臂矩阵（warm 对齐 / 过期 ×3 / 过期+INM / 10×TTL / 真实 docker pull）③ virtual 面三臂活体（既有 tag 冷 miss ×3 / 到期回源 / 负缓存冷 miss ×3 + 直读面对照）
Files:        新增 tools/difftest/l0072/build-image-offline.sh、reports/compatibility/L007-d3-virtual-probe.md、本日志；修改 deploy/compose/.env.uat 版本键（uat-l0072-c1193f5f/c1193f5f，gitignored）；无产品码/契约/金样改动
Tests:        case 11（D3 五臂 + virtual 三臂 + 对照臂），一致 8 / 差异 3（D3 上游计数臂 ×2 + virtual 注记臂）；skipped 0。复跑稳定性：×3 臂内部即重复采样（GET2/3 全同）；矩阵未整轮重跑（TTL 等待总长 ~15 min×2 代价，判据为日志差分非时序敏感）
Commands:     批跑=报告 §4 复现骨架（臂命令逐条内嵌会话）；真实客户端腿：dind 27.5.1 pull 双端 exit 0；判据核心：docker logs l0072-upstream | grep -cE '<path>.*<UA>' 前后差分；行证：docker cp binflow.db{,-wal} → sqlite3 remote_cache
Outputs:      reports/compatibility/L007-d3-virtual-probe.md；D3 三选一建议=INTENTIONAL 候选+对齐路径注记（对齐收被 10×TTL+真实 pull 零回源证据排除）；新 BUG 候选 docker/virtual-remote-cold-negative-body-annotation（virtual 面冷负 miss 首答 body 注记，直读面无）；提金候选：本报告 §1.2/1.3/1.4/2.1-2.3 八臂（上游计数+行证双锚，golden-capture 候选）
Compatibility: 回归对照：X3/X4 复现无回归；N1/N2 形状族维持既有登记；L006-2 walk 机制活体化；新增 1 候选账（§2.4）。契约建议（归 compatibility-engineer）：docker-remote.yaml 过期 blob 重验证相关条目 evidence 追加本报告；D3 条目 authority pending→differential
Security:     auth 面未触碰（Basic 直连既有凭据，无新 auth 面）；凭据仅 shell 变量；参照实例仅 API 读写 + filestore ls（只读）；无密钥落盘
Performance:  矩阵全程 ~25 min（含 600s 跨窗等待）；超时 case 0；异常时延 1 例（容器重建后首笔远端请求 29s/48s，后续正常 0.4-4s——记 Risks）
Risks:        ① 10×TTL 只测到 ~14 min 窗（更长效窗理论存在但参照 manifest/blob 二分语义已由四类臂交叉锁定）；② ART pull 臂 manifest 回源动词未逐字留痕（+1 计数在案，动词按 X1 既有 HEAD 形归档）；③ 首笔慢请求归因未做（不涉票面）；④ 本轮 UAT 镜像=离线变体链构建（binary vcs.revision 纯净性已验，但构建链与 CI 链非逐字节同源）
Blockers:     无（参照容器 04:23Z 自行 Exited 一次，docker start 后 5 min 收敛，未损会话）
Next:         ① conductor/compatibility-engineer：D3 三选一裁定（素材=报告 §1.7）；② compatibility-engineer：§2.4 新账登记 + 契约 evidence 追加；③ dev 域：virtual 面注记修复票候选（virtual handler 上游 404 回显 message 拼接）
```

## 断点快照（会话被限额中断一次后续跑，最终态完整）

- 已完成：全部 11 case（D3 五臂 + virtual 三臂 + 直读对照 + 复跑采样）；环境清理完毕（双端仓删净 grep=0、dind/upstream 容器删净、/tmp 中间件删净、宿主种子 tag 删净）。
- 未跑 case：无。
- 双端实例处置：UAT :8083 保留 uat-l0072-c1193f5f 运行（后续差分直接消费，.env.uat 已指向）；参照 :8082 运行中 ping 200（未动配置）。
