# L001-5 — D21 remote repo GET 顶层 url 回显真实上游

```
Ticket:       L001-5 — D21：remote 仓 GET 回显顶层 url 为自指派生值，应回真实上游（configuration.url）（P1，LOOP 001 尾票）
Role:         dev-go-core（area 实例：internal/httpapi）
Area:         internal/httpapi
Input:        reports/compatibility/L000-docker-remote-diff.md D21 条目（E4：Artifactory repo GET 顶层 url = 真实上游）+ §转票记录（位点 internal/httpapi/repositories.go 的 repoConfigOf/repoListItemOf）；docs/reverse/rest-api.md section 2（列表 url=<contextUrl>/<key> 通则，remote 例外由差分实证覆盖）；docs/reverse/repo-semantics.md remote 配置 url 必填/规范形
Changes:      ① 新增 remoteUpstreamURL(row, cfg) 助手——仅当 row.Type==remote 且已回显的 canonical configuration 里 url 为非空 string 时返回该上游，否则 false；只读 "url" 一个键，不触碰 username/password（NFR-S14）② repoListItemOf 与 repoConfigOf 两面在 contextURL 自指值之后叠加该覆盖：remote 行顶层 url=上游，local/virtual 维持 <contextUrl>/<key>（T-493 FR-157① 不变）③ 同步更新两函数与 struct 注释中「remote 上游 url 只活在 configuration 内」的旧裁决（被 D21 实证推翻）④ compat_test.go C05 循环改为按 type 分期望：remote 条目断言 http://127.0.0.1:9099，其余断言 context URL ⑤ 新增行为命名测试文件 repo_url_echo_test.go（票号在文件头注释）
Files:        新增 internal/httpapi/repo_url_echo_test.go；修改 internal/httpapi/repositories.go、internal/httpapi/compat_test.go；删除 无
Tests:        TestRepoURLEchoListDetailBothFaces（表驱动 5 行 × 两面）：canonical remote（REST 创建→repo.Service 脱敏链路）顶层 url=上游；raw-seeded remote（带 url）同举；urlless remote 回落 context URL（防御臂）；local/virtual 控制组维持 context URL；附断言：顶层无 username/password、list body 不含密码标记、configuration.url 与顶层 url 不矛盾。TestRepositoriesCRUD C05（更新后）+ TestCurlCompatReposAndStorage 全绿（既有面回归）
Commands:     go build ./... && go vet ./internal/httpapi/ && gofmt -l internal/httpapi/；$(go env GOPATH)/bin/golangci-lint run；go test ./internal/httpapi/ -count=1 -run 'TestRepoURLEchoListDetailBothFaces|TestRepositoriesCRUD|TestCurlCompatReposAndStorage' -v；go test ./internal/httpapi/ -count=1
Outputs:      build/vet/fmt → BUILD-VET-FMT-OK；golangci-lint（全仓）→ 0 issues；目标子集 → PASS: TestRepoURLEchoListDetailBothFaces / TestRepositoriesCRUD（C05 含 remote url=http://127.0.0.1:9099 断言 PASS）/ TestCurlCompatReposAndStorage；全包 → ok github.com/lzwzzy/binflow/internal/httpapi 232.429s；运行日志佐证脱敏链路：WARN repo: remote repository password dropped — no credentials master key configured repo=echo-remote
Compatibility: 契约对照：docs/compatibility/contracts/docker-remote.yaml 未收 repo GET 列表/单仓面（该契约管 docker 代理协议面），本面无契约可漂移；行为依据 = L000 报告 D21/E4 差分实证（高置信）+ rest-api.md section 2 通则的 remote 例外。与 docs/reverse/rest-api.md 列表条目「url=<contextUrl>/<key>」的表述存在通则-例外张力：以差分实证为准，建议 compatibility-engineer 将 remote 例外回写规格/契约（见 Next）
Security:     只从已脱敏 configuration 提升单一 "url" 键；不读/不回显 username/password；测试断言顶层无凭据字段且 list body 无密码标记；无新增输入面
Performance:  无（每次 GET 多做一次 map 断言与字符串判空，零分配路径未变——configuration 本就已被 unmarshal 复用）
Risks:        ① 前端/其它消费方若依赖旧的自指 url 字符串拼下载地址，行为会变（Artifactory 对齐即目标；FE 未查，超出本票边界）② urlless remote 回落 context URL 属防御臂，无 Artifactory 实证（其服务层必填 url，不可达态）——已在测试注释声明 ③ docs/reverse/rest-api.md 通则句未改（reverse 域归 reverse-engineer）
Blockers:     无
Next:         建议 conductor：① 转 compatibility-engineer 把「remote 行列表/单仓顶层 url=上游」落进契约与 rest-api.md 规格（消除通则-例外张力）② UAT 侧复跑 L000 §10 拓扑的 repo GET curl 取证闭 D21 ③ L000 报告 D21 状态由 DIVERGENT 翻 SAME 由差分侧更新
```

## 备注

- 工作区同时存在另一 agent 的在途改动（internal/httpapi/docker_token_test.go、docker_v2_test.go——C02 坏凭据面），非本票触碰，未动；全包测试绿含该在途态。
- 未 commit、未写 BOARD.md（票面边界）。
