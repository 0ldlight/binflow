Ticket:       T-L005-4 LOOP 005 轨道 L005-4 — Review A/B 微票批（LOOP 003 双审遗留 6 条 non-blocking 一次清偿：N1-N6）；P2
Role:         dev-go-storage
Area:         internal/storage + internal/httpapi/system_storage_admin*（ticket 明示允许）
Input:        LOOP 003 双审 N1-N6 清单（conductor 派发票面）；reports/agents/T-L003-1.md Risks ③（gc×prune 双删败者 NotFound 记 firstErr 的保守方向）与 Tests 段（walk 11 用例的既证形态）；docs/reverse/storage/prune-gc-admin.md E1-E4/§2.2/§3.2（既有实现的行为依据，本票未改语义仅补测试与注记）
Changes:      ① N1：prune_walk_test.go TestPruneConcurrentWithPublishNeverDeletesLiveBlob 补种子——live blob 两行（e.holds.acquire(live) + mark 集含 live，即发布者时间线 parked-at-committed 态），加存活断言与 post-race 盘点断言（processed=磁盘真相）；pruneOf 改为主线程一次性解析后传值入 racer goroutine（t.Fatal 不再可能从非测试线程调用）
              ② N2：pruneManager.observe 的 Progress 改 max(current, stats.Index) 钳制——startFrom 续跑时跳过目录的 observe（index 1..startFrom-1）不再把分子拖回低位
              ③ N3：pruneShard 的 e.Delete 失败臂分类——errors.Is(err, ErrBlobNotFound) 为良性跳过（双清扫器竞逐的败者：删除已由对方得手），不再记 shard firstErr 落 REST error 终态；败者不计 cleaned/Deleted；godoc 同步注记
              ④ N5：新增 TestStoragePruneStopIdleWithHistoryAccepted——有历史报告+不在跑 → 202 "stop request submitted"，且空闲 marker 不毒化下一次运行（begin 清 stopReq，次轮 finished 非 stopped）
              ⑤ N6：handleStorageGCStream 补 out==nil（Mark 期失败）臂的 gc.run 审计行（error:true 零计数 detail）+ ErrorContext 日志；新增 TestStorageGCStreamMarkFailureAudited（markFailingPruner 注入，断言 200 钉码 + 流内 500 行 + gc.run 审计行落地）
              ⑥ N4：newPruneManager 注释声明单进程假设——报告/单飞旗标/停止标记为进程内状态，双进程共数据目录在既有形态边界之外（datalock 仍串行化其维护趟，但互相看不见对方 marker）
              附加（测试有效性证明）：TestPruneLostDeleteRaceIsBenignSkip（deleteOnLiveMarker 在 Live 探针时删文件 = ReadDir 与 Delete 之间败者形态，确定性触发 N3 路径）；TestPruneManagerProgressMonotonicAcrossResume（internal 包直测 manager 钳制）
Files:        新增：internal/httpapi/prune_manager_internal_test.go、reports/agents/T-L005-4.md
              修改：internal/storage/prune.go（N3 分类+godoc）、internal/storage/prune_walk_test.go（N1 重写+N3 用例）、internal/httpapi/system_storage_admin.go（N2 钳制+N4 注记+N6 审计臂）、internal/httpapi/system_storage_admin_test.go（N5+N6 用例+import）
              删除：无
Tests:        N1：TestPruneConcurrentWithPublishNeverDeletesLiveBlob（live=hold+mark 双护下 4 racer 存活；-race 验证）；N2：TestPruneManagerProgressMonotonicAcrossResume（129 起步，跳过目录 observe 1/128 不回退，0xc9 前进）+ TestStoragePruneStartFromDirectoryMessageAndProgress（既有，终态 256 of 256）；N3：TestPruneLostDeleteRaceIsBenignSkip（败者 NotFound 无 pass error、不认领删除、processed=1）；N5：TestStoragePruneStopIdleWithHistoryAccepted；N6：TestStorageGCStreamMarkFailureAudited。测试有效性双向证明：禁用 N3 guard → 该用例 FAIL（"must be a benign skip, got the pass's error: ... blob not found"）；去掉 N2 max 钳制 → 该用例 FAIL（"progress regressed to 128 ... want 129"）；恢复后均绿
Commands:     go build ./...；go vet ./internal/storage/... ./internal/httpapi/；gofmt -l internal/storage internal/httpapi（空）；golangci-lint run internal/storage/... internal/httpapi/...（$HOME/go/bin，PATH 无）；go test ./internal/storage/... -count=1；go test ./internal/httpapi/ -count=1 -run 'TestStorage'；solo race：go test -race ./internal/storage/ -count=1；go test -race ./internal/httpapi/ -count=1 -run 'TestStoragePrune|TestStorageGC|TestPruneManager'（全串行，无后台）
Outputs:      四门：build/vet/gofmt 空输出、lint "0 issues."；storage 全包 ok 75.061s；httpapi TestStorage 子集 ok 13.895s（含两个新用例 PASS：TestStoragePruneStopIdleWithHistoryAccepted 0.49s、TestStorageGCStreamMarkFailureAudited 0.09s）；race solo：storage 全包 ok 111.862s、httpapi 子集 ok 9.383s；TestPrune* 12 用例 -race 全 PASS（含重写后的并发用例 0.07s）
Compatibility: 无行为规格变更：E1-E4 端点语义、26 字段 schema、状态码臂全部不动；N2 是纯展示修复（progress 分子单调化，renderPruneReport 输出形状不变）；N3 是错误分类修复（REST 终态从误 error 回到真实 finished/stopped——对齐 spec §2.2 error 态仅在真故障时出现）；N6 补齐审计可见性（gc.run 行）；N4 为声明性注记。docs/reverse/storage/prune-gc-admin.md 无需修订
Security:     无新错误面：N6 审计 detail 不含路径/凭证（仅布尔与计数）；N3 错误信息只含 sha 与 sentinel 错误；测试文件不含凭证
Performance:  无热路径变化：N2 为 O(1) 钳制；N3 为 Delete 错误分支内 errors.Is 一次；N6 仅 out==nil 异常路径；storage 全包时长与改前同量级（75s vs 既有 ~80s）
Risks:        ① N1 用例对 hold 门的隔离检测：mark 集含 live 时 mark 门同样保护 live——单门隔离仍由 TestPruneHoldGateProtectsInFlightUpload 承担（按票面两行种子执行；并发用例的价值=竞态下双护状态不破+race 检测器）；② GCSweep（gc 侧）的同类败者 NotFound 仍记 firstErr（L003-1 Risks ③ 原状）——票面只 scope pruneShard，gc 为 datalock 串行域且其错误报告形态既有，未擅动；③ N6 审计行为未对照活体（Artifactory gc 失败是否落审计 UNKNOWN，spec 未覆盖）——为 BinFlow 侧可观测性纪律
Blockers:     无
Next:         ① 若 gc 侧败者 NotFound 需同样分类，开后续微票（一处同构 guard）；② E4 空闲 stop 的 marker 持久性：进程重启后 stopReq 丢失（内存态）——活体行为 UNKNOWN，可入规格挂账；③ iteration-004 Next-4 的其余微票（E5 compress 测试等）不在本票六条内
