# T-88 工作日志 — M4 工程 ticket 拆解

- ticket: T-88 [P0] M4 工程 ticket 拆解 `role:tech-lead`
- 状态: done（待主会话审核录入 BOARD.md）
- 日期: 2026-08-19
- 输入: docs/prd/milestone-4.md（v1.0：FR-23~FR-33 共 60+ AC、29 端点矩阵 CE/SE/SR/GE、W01~W40、§5.5 K1~K3 暂行、§7 Q1~Q6 暂行）、docs/design/architecture.md（M4 增量：ADR-0014 console/session、ADR-0015 治理、§6 004 四表、§7.5/§7.6）、docs/design/console-ux.md（v1.0：18 路由表/11 线框/四态矩阵/R1~R10 API 需求清单）、reports/agents/T-6.md、T-32.md、T-61.md（M1/M2/M3 拆票范式）、BOARD.md（只读）
- git 序核实: T-86（ADR-0014/0015，d0ff1fb）先落 → T-87（console-ux，a6f0d77）→ T-85（PRD v1.0，3d1cfbf，未引 ADR-0014）——R1 裁决依据；代码侧核实 `permission_principals.principal_type` 列与 001~003 迁移实存（internal/metadata/substores_auth.go）、`internal/console` 仅占位、`Principal.Groups` 未落（架构 §3.4 声明位）。

## 结论

19 张票（T-89~T-107），P0 x16 / P1 x3 / P2 x0；首波可并行 3 张（T-89/T-90/T-92），最大波宽 3；关键路径 T-89→T-91→T-98→T-99→T-100→T-104→T-105→T-106（前端链 8 票 9 批）。

---

## 〇、口径总则（裁决 / Q1~Q6 写死 / 复用清单 / ux-R1~R10 映射）

**R1 裁决（session/console 命名面，详见风险 R1）**：PRD v1.0（commit 3d1cfbf，**后出**）与 ADR-0014/architecture §7.1/§7.5（d0ff1fb，先出）在挂载路径、端点族、cookie 名、TTL、CSRF 形态上正面冲突，且 PRD 未引 ADR-0014。**全部票按「PRD 面 + ADR 内核」**：挂载 `/binflow/ui/**`、301、`/api/v1/session`（POST/GET/DELETE，含 whoami）、cookie `binflow_session`、Origin 同源校验 CSRF、保留字并集 `{api,v2,docs,console,ui}`——理由：W01~W40 是验收锚；ADR 端点族缺 whoami（ux 导航收敛必需）；ADR 第三层「无 X-BinFlow-Console 头即拒」与其自身决策 3「CLI/curl/前端同一面」及 W38 第二腿（cookie 无 Origin 写 → 201）/W39（cookie DELETE → 404 先过 CSRF）行为自冲突。内核采 ADR-0014：web_sessions 表（id_hash=sha256 存储同 tokens 规）、登出吊销、last_used 滑动受绝对 TTL 封顶、无 console 专属 API 树、前端依赖政策（lockfile/零 CDN/npm audit）。**architect 勘误 ADR-0014 + 增补 ADR-0008（ui）须赶在批 1 派发前**（M2 R1 同款流程）。

**Q1~Q6 暂行口径写死**：Q1 server-side session → T-91【标注】；Q2 仅 repo 级 quotaBytes（0=不限）超限原子 413 → T-95；Q3 搜索 P0=name 子串+checksum 精确、gavc P2、props M5+ → T-92；Q4 在线 export（锁互斥+快照先行+manifest 边界）→ T-96；Q5 docker remote 不进 M4 维持 400 → 无票（T-105 回归断言维持）；Q6 GC 同步 REST → T-94。K1~K3（§5.5 暂行）→ T-97/T-92 标注，逆向扩编票（conductor 并行派发）落地后按 M1/M3 先例回写。

**M1~M3 能力复用清单（零重做，票内不再实现）**：

| 复用面 | 来源 | M4 消费方式 |
|---|---|---|
| `permission_principals.principal_type` 列 + 子接口 | M1 T-10/T-11（substores_auth.go 已证） | T-97 groups 行**零 DDL 变更**（架构 §6 004 注记原文） |
| auth 三臂扩展点（Principal.Groups 字段位、Cookie 臂注释、TokenRegistry/argon2id） | M1 T-11 + 架构 §3.4 | T-91 只加第三臂；T-97 认证时填充 Groups |
| audit_events 表 + Append/Query 先例 | M1 T-10/T-11 | T-90/T-93 只扩 Filter 与索引；词表新动作是数据非代码 |
| metadata 迁移器模式（001/002/003 实存） | T-10/T-34/T-62 | T-90 的 004 照模板（零 BEGIN/COMMIT、幂等、老库升级） |
| repo.Service Put 全族（Put/PutFromBlob/PutLandedBlob/PutOpts） | T-12/T-13/T-64 + 架构 §11.17 配额挂点预留 | T-95 配额与 pattern 门**单点挂链，五协议 adapter 零改动** |
| storage.GC mark-sweep + grace=mtime | T-9/T-42 | T-94 REST 包壳零语义改动；T-96 拷贝保 mtime（同约束） |
| httpapi middleware 链 / E-01 信封 / 错误三分层 | T-14/T-15 | session/search/audit/gc/usage 新端点直接复用 |
| E-04~E-15、RE-01~09 既有 REST | T-15/T-80 | FE 仓库/树/上传**零新增后端**（PRD：UI 消费端点 90% 已存在） |
| `/api/v1/permissions` CRUD（E-24） | M1 T-15 | T-101 编辑器直接消费；T-97 仅扩 principals.groups 接受与回显 |
| `enc:v1:` 凭据链 + env 密钥 fail-fast | M3 T-66（ADR-0012） | T-96 原样搬运（恢复链 W30b） |
| config 模式 / Makefile+CI / smoke.sh / QA 基线脚本 | T-8 / T-7 / T-16 / T-18/19/43/44/74/75/76 | T-91 console 键 / T-89 前端构建步 / T-106 烟测 / T-105 复跑 |

**console-ux R1~R10 逐条映射**：R1 目录分页 → **P2 债务**（前端按 ux §6.5 客户端分页 + >2000 提示兜底）；R2 搜索 → T-92；R3 审计分页/时间窗 → T-93（CSV P2 债务）；R4 GC → T-94 仅 POST dry-run/apply（状态端点不做，上次运行走 audit `gc.run`——P2 债务）；R5 备份 UI → **不做**（CLI only + 文档引导，ux 兜底成立，T-107）；R6 token 列表 → P2 债务；R7 配额 → T-95/T-102；R8 pattern 测试器同源 → T-101（Go 用例导出 fixtures 方案，无后端端点）；R9 remote stats → P2 债务（兜底 `—`）；R10 session 对协议路径可用 → T-91 AC② 断言项。

---

## 一、票据列表（T-89 ~ T-107，共 19 张）

### 脚手架（最前，批 1）

**T-89** [P0] web 前端工程脚手架：vite+React+TS + go:embed 接线 + CI 前端构建 + Playwright 基建 `role:devops-engineer` `area:web/（工程骨架、vite 配置、e2e harness）、internal/console（embed 与静态服务）、Makefile、.github/workflows、.gitignore` `dep:—（M3 基线）`
review：单 reviewer（架构边界：ADR-0005 Go 白名单与 ADR-0014 决策 4 前端依赖政策）。

AC:
① ADR-0014 决策 1/4：`web/` 工程（vite + React + TS，**base=/binflow/ui/**——R1 裁决值），路由懒加载缝预留；`make console` = `npm ci`（lockfile 锁定）→ vite build → dist 复制进 `internal/console/dist`；`console.Handler` go:embed（SPA shell `no-cache`、`/binflow/assets/<hash>.js|css` 指纹资产 `immutable`、**段内任意路径 history fallback 不越段吞 `/binflow/<repo>/<path>`**）；零运行时 CDN、直接依赖白名单（react/react-dom/react-router-dom 等）变更过 architect；CI 增 `npm audit` 步；构建输出记录 SPA gzip 体积（≤5MB 目标，W37 前置）
② CI：workflow 增 node≥20 构建 step（make console → make build → go test ./...），`make build` 在无 node 环境仍可编译（dist 占位策略二选一并注明）；Playwright 基建 `web/e2e/` + playwright.config（Chromium、baseURL=`$BASE`）+ 冒烟 spec（`/binflow/ui/` 见挂载点，`id="root"`）；`tsc --noEmit` + eslint 进 CI
③ `make build` 单二进制自带完整控制台（ADR-0002）；空库冷启动 <2s 自测（正式归 T-105）；internal/console 占位测试同步更新；`data/`、`web/node_modules`、`web/dist` 入 .gitignore

W 映射：W01/W02 工程前提、W37 embed 面。

### 后端基座与 /api/v1 扩展（批 1~3）

**T-90** [P0] metadata：004_console_governance 迁移 + Groups/WebSessions 子接口 + 审计查询扩展 `role:dev-go-core` `area:internal/metadata` `dep:—（M3 基线）`

AC:
① 004 迁移（sqlite 实装 + postgres 占位，001~003 模板）：`groups`/`group_members`/`web_sessions`（id_hash=sha256(plaintext) 主键、expires_at、last_used_at、revoked_at）/`repo_usage` 四表严格按架构 §6 定稿 DDL；**增量两处按 PRD（R2 登记）**：`users` 加 `email` 列（FR-27-AC8）+ `audit_events` 查询索引 `(actor,time)`/`(action,time)`（GE-01/NFR-P17）；文件体内零 BEGIN/COMMIT（T-10 M9 红线）；迁移幂等 + 001~003 老库升级路径单测
② Store 增 `Groups()`（CRUD + 成员 upsert/按用户解除 + 按用户解析组列表）与 `WebSessions()`（create/get-by-hash/touch/revoke/过期清扫候选——复用启动清扫模式，架构 §11.18）子接口；`AuditStore.Query` 扩全参数（actor/action/since/until + keyset cursor 倒序），消费 ① 索引，EXPLAIN QUERY PLAN 无全表扫描（1 万行单测抽验；10 万行归 T-105）
③ 建表零行为变化：M1~M3 metadata 套件零回归；`-race` 覆盖成员级联/会话并发 touch

W 映射：W22 基座、W37a 落库前提。

**T-91** [P0] session 三臂认证 + console 挂载路由 + CSRF 防线（FR-23，CE-01~07） `role:dev-go-core` `area:internal/auth（Cookie 第三臂）、internal/httpapi（console 挂载 + session 端点 + 保留字）、internal/config（console 段）` `dep:T-89,T-90`
review：**双 reviewer（安全 + 架构）——硬性要求（session/CSRF 面）**。
【Q1 写死：server-side session＝PRD 暂行与 ADR-0014 机制一致；命名面按 R1 裁决】

AC:
① CE-01/02/07（W01/W01b/W02）：`GET /binflow/` → **301** `/binflow/ui/`（M1 占位 JSON 终结）；`/binflow/ui/**` SPA shell + 深链 fallback（不越段）；指纹资产 `Cache-Control: public, max-age=31536000, immutable`、shell `no-cache`；repo key 保留字扩并集 `{ui(PRD), docs,console(ADR-0014)}` → 建仓 400（api/v2 维持）；W01b 断言四字全拒
② CE-03~05（W03~W08/W37a）：`POST /api/v1/session`（JSON/form 双形态）→ 200 `{username,admin}` + `Set-Cookie: binflow_session=<高熵 ≥128bit>; HttpOnly; Path=/binflow; SameSite=Lax`（`Secure` 依 TLS/`server.base_url` scheme 下发，明文 HTTP 不加——文档警示，R1 附注）；错凭据 401 E-01 文案不泄露存在性 + `login.failed` 落审计；`GET` = whoami（200/401）；`DELETE` = 204 服务端吊销（重放 401）；session 落库**重启不掉线**（W37a）；TTL `console.session_ttl_hours`（默认 24）+ `console.session_ttl_seconds` 覆盖键（W08 秒级测试粒度，R4）+ last_used 滑动受绝对 TTL 封顶（ADR-0014）；**cookie 与 Basic/Token 等价认证**——内容路径、协议 adapter 路径（npm packument/docker tags 等，ux R10）、管理面同用；session 值不进日志（NFR-S19），签发/吊销 info 级结构化日志
③ CE-06（W38）CSRF：SameSite=Lax + **Origin 同源校验**——cookie 认证的非 GET/HEAD 写请求，`Origin` 存在且非同源 → 403；无 Origin/同源放行（curl+cookie 与 CI 零感知：W38 双腿 403/201、W39 的 DELETE audit 先过 CSRF 再 404）；SPA 统一附 `X-BinFlow-Console: 1`（ADR-0014 第二层，服务端记录不强制）；table-driven（三臂 × 凭据正误 × TTL 过期 × 重启 × Origin 矩阵）+ curl cookie jar 全周期自测（W01~W08 等价）

W 映射：W01~W08、W37a、W38、W01b。

**T-92** [P0] 搜索域：/api/search/artifact + checksum（FR-26，SR-01/02/04） `role:dev-go-core` `area:internal/httpapi（api/search.go）、internal/repo（Search 用例）、internal/metadata（LIKE 与 blob 反查）` `dep:—（M3 基线；排批 1，避开与 T-91 同波 httpapi）`
【Q3/K2 写死：P0 = name 子串（SQL LIKE）+ checksum 精确；`*` 通配 P2；gavc P2 债务；props M5+；匹配语义待逆向校准回写】

AC:
① SR-01（W14/W16）：`GET /api/search/artifact?name=&repos=` → 200 `{"results":[FileInfo...]}`（E-09 字段集复用）；name 缺 → 400 E-01；repos csv 限定；**ACL 过滤零泄漏**（NFR-S24：无 read 的 repo 不出现、admin 全见、匿名按匿名通道语义——谓词 SQL 侧收窄；`anonymous_access=false` 实例 ci-bot 负断言）
② SR-02（W15）：`GET /api/search/checksum?sha256=&sha1=&md5=&repos=`：至少一值（全缺/格式非法 → 400）；精确命中返回全部引用 node（跨仓去重可见，C07 同 blob 双路径场景 results≥2）；SR-04（W36）：`/api/search/props|users|artifactory|pattern|badge...` → 404 E-01（M1 E-26 断言**部分反转**，routes 测试同批更新注明来源——R5）
③ table-driven（子串/过滤/ACL 矩阵/非法 checksum/空结果）+ NFR-P17 10 万 node 造数 P95<1s 抽验（正式归 T-105）+ W14/W15/W16/W36 curl 自测

**T-93** [P0] 审计查询面 + 词表（FR-29，GE-01/02） `role:dev-go-core` `area:internal/audit（Filter 全参数/cursor）、internal/httpapi（api/v1/audit.go）` `dep:T-90`

AC:
① GE-01（W22/W22b）：`GET /api/v1/audit?repo=&actor=&action=&since=&until=&limit=&cursor=` → 200 `{events:[{id,time,actor,action,repo,path,detail}],nextCursor}`（detail 对象、time RFC3339 UTC、倒序、cursor 不透明 keyset）；limit 默认 100/上限 1000（超限 400）；since/until 闭开区间；admin only（非 admin 403/未认证 401）；`limit=2`+追随 cursor 两次三页合计 == 全量（≥5 条构造）
② GE-02（W23/W23b）：M4 词表全集可查（`group.*` 源在 T-97、`gc.run` 在 T-94、`quota.exceeded` 在 T-95、`export.run/import.run` 在 T-96——本票供查询面与联调桩）；Redact 链对新端点生效（全量导出 grep：口令字面量/token 明文/Authorization 头零命中）；append-only（W39）：PUT/DELETE `/api/v1/audit*` → 404（路由层无写面）；metadata 层无 UPDATE/DELETE audit 代码路径（NFR-S21 评审项）
③ table-driven（过滤矩阵/时间边界/cursor 追随/limit 边界）+ W22/W22b/W23/W23b/W39 curl 自测

**T-94** [P0] GC 管理化 + data 目录互斥锁（FR-30，GE-03） `role:dev-go-core` `area:internal/httpapi（system/gc.go）、internal/storage（跨进程文件锁）` `dep:T-90`
【Q6 写死：同步执行；异步 job 框架 M6+；超长库 CLI `gc` 兜底】

AC:
① GE-03（W24/W25）：`POST /api/v1/system/gc` body `{"apply":bool,"graceHours":int?}`（缺省 `storage.gc_grace_hours`；grace 基准 = blob mtime，ADR-0006 语义零变更）；dry-run 200 `{candidateCount,candidateBytes,deletedCount:0}` 且 stats blob 数不变；apply `deletedCount==candidateCount`、被引用 blob 幂等幸存（W25 keep.jar 手法）、再 dry-run 0 候选；admin only、E-01 信封；`gc.run` 落审计（REST 与 CLI 两路径 actor=admin）；storage.GC 既有实现复用零改动
② 互斥锁（一致性关键，ADR-0015 四安全边界）：data 目录级**跨进程文件锁**（flock，`data_dir` 下锁文件；形态 architect review 把关，R9）：export 运行中 POST gc → **409**（message 含 `export in progress`）；GC 运行中 export CLI 退出码非 0（export 侧消费在 T-96，本票锁原语 + GC 侧持有/释放）；CLI `gc -c/--apply/--grace-hours` 零回归（M2 O3 断言复跑）
③ GET gc 状态端点（architecture §7.1 列有、PRD 矩阵未列）**不做**——上次运行经 audit `gc.run` 查询（P2 债务，R2）；table-driven（dry-run/apply/互斥 409 时序/grace 缺省）+ W24/W25/W25b（GC 侧）curl 自测

**T-95** [P0] repo 治理字段与配额：includes/excludes 双值码 + quotaBytes enforcement + usage（FR-24 字段面 + FR-31，GE-05/06） `role:dev-go-core` `area:internal/repo（pattern 门 + 配额预检）、internal/metadata（repo_usage 同事务）、internal/httpapi（repositories 透传 + usage 端点）` `dep:T-90`
【Q2 写死：仅 repo 级 quotaBytes（0=不限默认）；全局/文件数/用户级不做；remote pull-through 落盘不计量】

AC:
① W12a（M1 勘误②收编）：repo 配置 `includesPattern`（默认 `**/*`）/`excludesPattern`（默认空），**excludes 优先**；不匹配上传 → **409**（message 含 pattern）、下载 → **404**；enforcement 挂 repo.Service.Put/Get 门（generic 与 maven/npm/pypi 内容面生效；docker 域 P2 观察不设断言）；默认仓（未配）行为与 M1~M3 逐字节一致；`quotaBytes` 进配置校验（正整数，0=不限）与 GET 回显（W26 前置）；remote `password` 不回显维持
② GE-05（W26/W26b/W27）：配额预检挂 **repo.Service.Put 全族**（Put/PutFromBlob/PutLandedBlob/PutOpts——架构 §4.6/§11.17 挂点，五协议 adapter 零改动防旁路）：计量 = `repo_usage.logical_bytes` 与 node 增删**同一事务**；expect.Size 已知（秒传/mount/docker finalize）Commit 前直判、流式未知落盘后判（超限回滚 node 登记、blob 留 GC）；超限 → **413** E-01（message 含 `quota exceeded` + used/quota 双值）+ `quota.exceeded` 落审计（detail: actor/repo/path/used/quota）+ WARN 结构化日志；**原子拒绝**（被拒路径 404、stats 不变、docker 上传会话可弃零残留）；读/删不受限、删后 used 回落再传 201（W27）；秒传与 mount 同受限（NFR-S23）；`quotaBytes:0` 零回归
③ GE-06：`GET /api/v1/storage/usage/{repo}` → `{repo,usedBytes,quotaBytes}`（admin 或有 read 权限用户）；generic+docker 自测（五协议全负断言归 T-103）；table-driven（pattern 矩阵/配额三态/Put 四族/事务回滚）+ W12a/W26/W26b/W27 curl 自测

review：单 reviewer（正确性关注点：413 原子性与同事务计量）。

### 备份恢复（批 3）

**T-96** [P0] 备份/恢复 CLI：export / import（FR-32，GE-07/08） `role:dev-go-core` `area:cmd/binflow-server（export/import 子命令）、internal/storage（blobs 拷贝/manifest helpers）、internal/metadata（SQLite 快照）` `dep:T-94（锁原语消费）`
review：**双 reviewer（正确性 + 架构）——硬性要求（备份恢复面）**。
【Q4 写死：在线 export（锁互斥 + 快照先行 + manifest 边界，窗口内多余 blob 允许）；停机 export 为文档可选；import 一律停机 + 空 data dir】

AC:
① GE-07 export（W28/W28b）：`binflow-server export -c <cfg> --output <dir> [--tar P2]` 在线执行：获取 data 目录锁（T-94；与 GC 双向互斥 W25b）→ SQLite online backup API 快照（不阻塞服务写入）→ 拷贝 `blobs/<2hex>/`（**mtime 保留**——ADR-0006 勘误②硬约束）→ `manifest.json` `{formatVersion,createdAt,binflowVersion,blobCount,totalBytes,metadata:{file,sha256},graceNote}`；**顺序不可换**（先 DB 后 blobs；反向悬空引用）；产物目录 0700（NFR-S22：含口令哈希与 `enc:v1:` 密文，manifest 零明文秘密）；manifest 引用 blob 全存在、export 后新上传 sha256 ∉ manifest（W28b）；`export.run` 落审计 + 结构化日志
② GE-08 import（W30/W30b/W31/W32）：`binflow-server import -c <cfg> --input <dir> [--verify full]`：目标 data dir 非空 → fail-fast 退出码非 0；manifest 校验（formatVersion 兼容、metadata.sha256 实测一致）；完整性 = size 全验 + sha256 抽验前 100（`--verify full` 全量重哈希，P1）；缺失被引用 blob / 篡改 blobCount → fail-fast 且目标清回空（**无半恢复**）；恢复 blobs 保 mtime（W32 stat 对账，grace 时钟不重置）+ metadata 就位；恢复后 serve 正常启动：四协议制品 sha256 一致 + users/groups/permissions/tokens/audit 全保真 + jane 组授权矩阵复验（W19 手法）+ token Bearer 可用 + remote 凭据密文原样（无 `BINFLOW_REMOTE_CREDENTIALS_KEY` 启动 fail-fast——ADR-0012 恢复链，W30b）；`web_sessions` 不入备份（架构 §11.19，恢复后重登）；`import.run` 落审计
③ GE-09（W36 边界）：`/api/export/**`、`/api/import/**` → 404 E-01（路由本就无此面）；NFR-P19 吞吐记录（≥100MB/s 记录不设门）；`--tar` P2 债务；table-driven 损坏矩阵（删引用 blob/篡改 manifest/非空目标/跨版本 schema_migrations）+ W28~W32 全序列真机自测

### groups 打通（批 4，后端收官）

**T-97** [P0] groups 域与权限继承（FR-27，SE-01~08） `role:dev-go-core` `area:internal/auth（Principal.Groups 填充 + Authorizer 并集/即时生效）、internal/httpapi（api/security/groups + users 扩展 + storage ?permissions + /api/v1/permissions principals.groups）` `dep:T-90,T-91（auth 链稳定后动，错波）`
review：**双 reviewer（安全 + 架构）——硬性要求（权限 groups 链）**。
【K1/K3 暂行（§5.5）：端点族形态（PUT 201 无 body 等）与组删除 409 按 PRD 暂行写死；逆向扩编票落地回写后出入为码值/文案窄改动；SE-07 组无 admin 位 = 有意不兼容】

AC:
① SE-01~04（W17/W20）：`/api/security/groups`（admin only，错误体走用户管理纯文本层）：GET → `[{name,description,uri?}]`；PUT `{name,description}` → **201 无 body**（body name ≠ 路径 400；已存在 → 200 更新）；POST → 200 部分更新（仅 description，404 不存在）；DELETE → 200 纯文本，**被 permission target 引用 → 409（message 列 target 名，K3 从严）**、成功删除联动解除成员（users/jane groups → []）；组名规则（非空/≤64/`[a-z][a-z0-9._-]*`/保留字 `anonymous`、`_system_` → 400）；`group.create/update/delete` 落审计
② SE-05/06（W18/W40）：`GET /api/security/users/{name}` → `{name,email,admin,groups[],realm,lastLoggedIn?}`（无口令字段）；PUT/POST users `groups:["devs"]` 维护成员（组不存在 → 400 定案文案 `Unable to find group by name '<g>'.`，auth-model §1.3-⑧ 高置信度）；email 落盘回显（T-90 列）；列表简形态 `{name,uri,realm}` **不变**（双端点分工）
③ SE-07/08（W19/W19b/W19c/W21）：`Principal.Groups` 认证时填充（group_members JOIN）；`Authorizer.Can` = 命中 targets 的 **user principals ∪ 所属 groups principals** 动作并集（M1 folder 尾斜杠/excludes 优先/同 checksum 幂等等语义零变更；C22/C27 回归归 T-105）；**即时生效**（逐请求现算或变更失效——移出组下一次请求即 403，无重启/无窗口，NFR-S25）；`POST /api/v1/permissions` principals.groups 接受与回显（W19 前置）；`GET /api/storage/{repo}/{path}?permissions` → `{uri,principals:{users,groups}}` r/w/d 位（virtual/remote → 400 P2 注记）；**组无 admin 位**（admin 组成员的非 admin 用户对管理 API 403——有意不兼容断言，FR-27-AC9）；table-driven（并集/即时性/409 保护/名规则全分支）+ W17~W21/W40 curl 全序列自测

### 前端页面组（批 4~6，按 console-ux 路由表分组）

**T-98** [P0] FE 基座：登录页 + 框架壳 + 仪表盘 + 设置页 `role:dev-frontend` `area:web/src（app 壳、路由守卫、login、dashboard、settings、公共组件/api client/设计 token/四态基座）` `dep:T-89,T-91`

AC:
① 登录页（W09，console-ux §4.1）：用户名/口令、行内 401 错误（不泄露存在性）、提交 loading、成功跳 `return`；未登录守卫（重定向 `/login?return=`）；登出（底栏）；会话过期 401 → toast + 重登（ux §5.1）
② 框架壳：左导航（§3.1 十入口；**admin/非 admin 收敛以「API 403 即隐藏」兜底**，首登 version + 仓库列表探测）、顶栏（版本号 `/api/system/version`、全局搜索 ⌘K/`/`、主题切换）、toast/危险确认对话框（焦点陷阱+Esc）/404 页；`--bf-*` 设计 token 暗色优先双主题零分叉；可达性基线（§8：Tab 序/焦点环/aria-label）；`data-testid` 按 console-ux 规范命名（QA 锚）
③ 仪表盘 + 设置：健康/storage stats/仓库计数/remote 状态（非健康项）/最近审计 8 条——卡片独立骨架独立到达（§5.2）；设置页实例信息 + 改密（复用 `/api/security/password`）；空实例「创建第一个仓库」CTA；SPA 首屏 <3s 自测（正式归 T-105）

W 映射：W09。

**T-99** [P0] FE 仓库组：列表/新建/详情/设置/危险区（FR-24 UI） `role:dev-frontend` `area:web/src/pages/repositories（list/new/detail/settings；tree 子目录归 T-100）` `dep:T-98,T-95（quotaBytes/patterns 字段）`

AC:
① W10/W10b：列表（type/packageType 过滤、badge、remote assumed-offline 标记、virtual 成员浮层）；分步建仓表单（§4.4：三步 + 摘要侧栏、key `[a-z][a-z0-9-]{1,62}` 与 url 前端预检 + 服务端 400 行内回显、Remote×Docker 等非法组合置灰、remote password **永不回显**）；编辑复用（rclass/packageType 锁定）；建/改后 curl 单查对账（packageType/url 回显）
② W11：删除走详情危险区（P5：红边框、非空显示将删 N 制品、`□ deleteContent` 勾选、**输入 key 确认**——不勾时 UI 呈现 400 原因而非让用户撞墙）；其后内容路径 404 断言
③ `quotaBytes`（正整数/0=不限）与 includes/excludes（mono chip 编辑）表单字段 + 详情 used/quota 进度条（usage 端点）；表单校验与服务端一致（缺 url 的 remote 前端即拦、网络面板零写请求，FR-24-AC5）；接入命令块（P3：五协议按 docs/user 同源，复制按钮）；Playwright 手测建/改/删三链（正式 W10/W10b/W11 归 T-104）

**T-100** [P0] FE 制品树 + 上传/下载/删除 + 搜索页（FR-25/FR-26 UI） `role:dev-frontend` `area:web/src/pages/repositories/tree、web/src/pages/search` `dep:T-99（同目录串行）,T-92`

AC:
① W12/W12b/W12c(P1)：树浏览（左树懒加载一层 + 右表当前层 children 按名排序、目录/文件图标、size/lastModified/createdBy、面包屑、骨架屏 §5.2）；五协议特化视图 P1（docker 两级 catalog→tags + manifest 详情面板；maven GAV + metadata XML 只读；npm/pypi 包目录——§3.4 矩阵）；virtual 来源成员列（`X-BinFlow-Resolved-From` 可得时）；权限过滤自然生效（无 read 不在树上，NFR-S26）；下载落盘 sha256 与 item info `checksums.sha256` 对账徽标（P2 原则）
② W13/W12d：上传对话框（§4.7：拖拽多文件、行级进度/速度、**浏览器端流式 sha256 计算 + 服务端比对**、409 双值/403 权限指引/413 quota 文案原样呈现、generic+maven 表单生成路径且前端 layout 预检；docker/npm/pypi 不出上传入口以命令块替代——P6）；删除（E-14 幂等呈现、read-only 403 原因呈现且 curl 侧仍 200）；大目录「加载更多」客户端分页 + >2000 提示（ux §6.5 兜底）；1GB UI 上传流式（RSS 归 T-105）
③ W14b：搜索页（防抖 300ms、结果表 repo/path/size + 协议语义副行、行点击跳树定位、空关键词引导态）；四态矩阵齐备（§5.3）；Playwright 手测 W12/W12b/W12c/W13/W12d/W14b（正式归 T-104）

**T-101** [P0] FE 安全组：用户/组/权限 target 编辑器（FR-28 UI） `role:dev-frontend` `area:web/src/pages/security` `dep:T-98,T-97`

AC:
① W33 三步流：建组（qa-team）→ 建用户（bob）入组 → 建 target（repo/pattern/principals **users+groups 双栏矩阵** r/w/d，§4.9 线框）→ curl 侧三实体字段对账；W33c 删被引用组 → 409 + 引用 target 名呈现，解除后可删
② 编辑器灵魂件：**模式测试器**（include/exclude 逐条命中明细 + 「exclude 优先」最终判定）——**判定向量与 auth.pathmatch 同源**：从 internal/auth table-driven 用例导出 fixtures JSON（生成脚本进 CI，漂移即红——ux R8 方案，无后端端点）；保存前变更摘要 diff 确认（§4.9[4]）
③ W33b 即时生效：UI 移出组后第二浏览器上下文同路径 PUT 403 呈现；用户页（建/禁用/改密入口/删）；token 页 **P2 不做**（ux R6 兜底：签发引导 + 既有 revoke，债务登记）；错误呈现统一解析服务端原文（三分层不暴露给用户，ux §5.1）

**T-102** [P1] FE 治理组：审计页 + GC 页 + 配额页（FR-29/30/31 UI，P1 面） `role:dev-frontend` `area:web/src/pages/governance、web/src/pages/audit` `dep:T-98,T-93,T-94,T-95`

AC:
① W34 审计页：过滤器（actor/action/repo/时间窗）+ 表格 + cursor「加载更多」；与 REST 同过滤结果集抽样对账；动作值原样 mono 显示；CSV 导出 P2 隐藏
② W35 GC 页：stats 概况 + dry-run 结果面板（候选数/字节 + grace 提示）+ apply **二次确认**（输入实例名，P5 危险区）+ deleted 结果与 curl stats 对账；无候选绿色空态
③ 配额页：每仓 used/quota 水位条（80% 黄/100% 红）+ 行内编辑上限；备份页**不做**（ux R5 兜底：CLI 引导块 + 文档链接）；非 admin 403 无权限卡

### 验收与收尾（批 5~9）

**T-103** [P0] QA：curl/CLI 全量矩阵（PRD §8 剧本 2/4/5/6/7 非浏览器面） `role:qa-engineer` `area:验收（session/search/groups/audit/GC/quota + CLI export/import）` `dep:T-91~T-97`

AC:
① 基座全周期：W01→W01b→W02→W03~W08→W37a→W38（cookie jar、TTL、CSRF 双腿、保留字四拒）；搜索 W14/W15/W16 + W36 边界抽查（search 未实现族/prune/v2 permissions/api export 四族 404）
② 权限与治理：W17→W18/W40→W19/W19b/W19c→W20→W21 + 组名/组无 admin 位边界（FR-27-AC9）；W22/W22b→W23/W23b→W39→W24/W25/W25b→W12a→W26/W26b/**W26c 五协议负断言**（docker push/npm publish/mvn deploy/twine upload 超限客户端报错 + 服务端 413 + 零残留）→W27；每条附原始输出，任何 5xx/非预期码打回附票号
③ 备份 W28/W28b→W30/W30b→W31→W32 真机往返（四协议 + jane/devs/权限/token/quota 仓全保真 + mtime stat 对账）；W 序列按 PRD 执行，**R1 裁决若改面以回写后 PRD 为终断言**（派单注明版本）；报告 `reports/agents/T-103-qa.md`

**T-104** [P0] QA：Playwright 浏览器矩阵（FR-33-AC1，剧本 3） `role:qa-engineer` `area:验收（Chromium E2E 全链）` `dep:T-98~T-102,T-103（QA 实例串行）`

AC:
① 全链 exit 0 且服务端零 5xx：W09→W10/W10b/W11→W12/W12b/W12c/W12d/W13→W14b→W33/W33b/W33c→W34→W35；选择器 testid 按 console-ux 终稿替换 PRD 占位；实例环境与其他 QA 票串行防污染
② FR-25-AC6：UI 上传 1GB 服务进程 RSS 增量 <256MB；50 并发 Playwright 登录无 session 串号抽查（NFR-P20 P1）
③ WebKit/Firefox 三链（登录/树/上传）P2 观察（成败记录不阻塞，FR-33-AC4）；报告归档

**T-105** [P0] QA：回归基线（M1~M3 全序列）+ 性能收尾（FR-33-AC2/AC3） `role:qa-engineer` `area:验收（回归/性能）` `dep:T-104`

AC:
① 回归基线：M1 C 序列 P0 + M2 D 序列 P0 + M3 M 序列 P0 复跑全绿（断言按 §5.6 反转表更新：search 404→分派、`/binflow/` 占位→301、groups 404→分派、audit 404→200、repo key ui/docs/console 400）；docker/mvn/npm/pip 真实客户端各一条链零回归；C22/C27 权限语义复验（FR-27-AC10）
② 性能 W37：空库冷启动 <2s（含 embed，ping 计时）；SPA 首屏 <3s（Chromium trace，P1 记录）；二进制大小 + SPA gzip 体积记录（40MB 预算记录不阻塞）；100 并发 REST 混合（session/search/usage/children）退出码全 0 零 5xx；NFR-P17/P18 造数（10 万 node 搜索 P95<1s、10 万 audit `actor+since` P95<500ms、children 1000 子项 P95<300ms）
③ M4 QA 总报告（DoD §9 第 1/2 条结论：P0/P1 AC 清单 + P2 处置建议）；报告 `reports/agents/T-105-qa.md`

**T-106** [P1] 部署烟测：compose 实例控制台链（FR-33-AC5，DoD 第 5 条） `role:release-engineer` `area:deploy/dev（compose/镜像多阶段：node build → go build embed）、烟测脚本` `dep:T-105`

AC:
① compose 实例（非本机 bare 进程）控制台可登录、建仓、上传、树浏览全链 exit 0；镜像构建含 console 资产（CGO_ENABLED=0 维持）；restart 后会话与制品持久化
② 烟测报告 `reports/agents/T-106-smoke.md` 归档；对外发布镜像/Chart/二进制**先经用户确认**（安全底线）
③ 部署产物与 M1~M3「同一二进制同一 YAML」原则零漂移

**T-107** [P1] M4 用户文档四篇（FR-33-AC6，DoD 第 4 条） `role:tech-writer` `area:docs/user` `dep:T-103（起草可自批 5 起，终验在 T-105 后）`

AC:
① 控制台使用指南（登录/导航/浏览器要求/四态语义）+ 权限与组管理（三步流/并集/即时生效/模式测试器）+ 治理（审计词表与查询/GC dry-run→apply 与互斥/quota 413 语义）+ 备份恢复手册（export/import 命令、**mtime 与产物 0700 保管告警 NFR-S22**、`BINFLOW_REMOTE_CREDENTIALS_KEY` 恢复链、停机强一致可选操作）
② 有意不兼容与差异清单（组无 admin 位/SE-09 v2 permissions/SR-04 搜索族/GE-04 prune/GE-09 REST export/Q6 同步 GC）+ 常见码对照（409 pattern/413 quota/409 组引用/401 session/403 CSRF）
③ 全部命令在 T-103/T-105 验收产物上抽样复跑退出码 0；中文文档、命令与标识符英文；frontmatter Docusaurus 兼容子集（ADR-0011）

---

## 二、分批派发表（宽度 ≤4，批内 area 不重叠）

| 批次 | 票 | 宽度 | 说明 |
|---|---|---|---|
| 1 | T-89 / T-90 / T-92 | 3 | **前端脚手架最前**（硬性）；004 迁移与搜索域并行（web+console / metadata / httpapi+repo 搜索子域互不重叠）；**R1 收口为批 1 派发门槛**（T-89 的 vite base 路径取决于裁决） |
| 2 | T-91 / T-94 | 2 | session（auth+httpapi session/console/config）与 GC（httpapi system/gc + storage lock）httpapi 子域不同、文件级边界注明；主会话从严判冲突则 T-94 顺延批 3（M2 批 2 先例） |
| 3 | T-93 / T-95 / T-96 | 3 | audit（audit+httpapi/audit.go）/ 治理字段配额（repo+metadata usage+httpapi usage/repositories）/ export CLI（cmd+storage+metadata）——httpapi 与 metadata 均子域不同，文件级边界派单注明 |
| 4 | T-97 / T-98 | 2 | groups 后端收官（T-91 后 auth 链空闲）+ FE 基座起步（登录可联调） |
| 5 | T-99 / T-103 | 2 | FE 仓库组（字段面 T-95 已 done）+ QA curl 全量（后端冻结于批 4；QA 用独立实例与 FE dev 分离） |
| 6 | T-100 / T-101 / T-102 | 3 | FE 三页面组并行（tree+search / security / governance 互不重叠）；共享组件限 T-98 已有面（R6） |
| 7 | T-104 | 1 | Playwright 矩阵（FE 全冻结；QA 实例串行） |
| 8 | T-105 | 1 | 回归基线 + 性能收尾 |
| 9 | T-106 / T-107 | 2 | compose 烟测 + 文档终验 |

review 强度汇总：**双 reviewer（硬性）= T-91（安全+架构，session/CSRF）、T-96（正确性+架构，备份恢复）、T-97（安全+架构，权限 groups 链）**；单 reviewer = T-89、T-90、T-92、T-93、T-94、T-95、T-98~T-102、T-107；QA/烟测票（T-103~T-106）按 M1~M3 惯例无 code review，报告 + conductor 复现核验。

**关键路径**：T-89 → T-91 → T-98 → T-99 → T-100 → T-104 → T-105 → T-106（8 票 9 批，前端链为最长链）。次链：T-90 → T-97 → T-101 汇入 T-104；后端支链 T-90 → T-94 → T-96 汇入 T-103。QA 三段串行（T-103→T-104→T-105 共用实例防污染）为尾部固定成本。

---

## 三、风险清单（R1~R10，供 conductor 转交）

- **R1（最急——批 1 派发门槛）PRD vs ADR-0014 session/console 命名面冲突**：git 序核实 ADR-0014（d0ff1fb）先落、PRD v1.0（3d1cfbf）后出且未引 ADR-0014。分歧全表：挂载 `/binflow/ui` vs `/binflow/console`；301 vs 302；`/api/v1/session`（GET/POST/DELETE 含 whoami）vs `/api/v1/login`+`/logout`（无 whoami）；cookie `binflow_session` vs `bf_session`；TTL `console.session_ttl_hours` 默认 24 vs 绝对 12h+滑动；CSRF Origin 校验 vs `X-BinFlow-Console` 头强制；保留字 `ui` vs `docs/console`。**票按 PRD 面 + ADR 内核**（依据：W 序列验收锚；whoami 为 ux 导航收敛必需；ADR 第三层头强制与其决策 3「CLI/curl/前端同一面」及 W38 第二腿/W39 行为自冲突）；保留字取并集防 M5 碰撞。需 architect 勘误 ADR-0014 + 增补 ADR-0008（ui 保留字）、PM 在 PRD 标注对齐——**赶在批 1（T-89 vite base）派发前**；若用户裁 ADR 全胜，改动面 = T-89 base 路径 + T-91 路由/端点常量 + QA W 断言，结构不变。
- **R2（architect 勘误清单）architecture §6/§7 与 PRD 端点矩阵其余分歧**：`/api/v1/groups`（架构路由表）vs `/api/security/groups`（PRD SE 域，票按 PRD）；GET `/api/v1/system/gc` 状态端点（架构列有、PRD 无→不做，P2 债务）；`/repositories/{key}/quota` 专用端点（架构列有、PRD 走 repo 配置字段→不做）；004 缺 `users.email` 列与 audit 查询索引（PRD FR-27/GE-01 明示→T-90 落）；login/logout vs session 族（并入 R1）。请 architect 一页 M4 勘误回写 §6/§7，批 2/批 3 前完成，防「迁移/端点越权」review 争议。
- **R3（K1~K3 暂行 + 逆向票时序）**：groups 端点族形态、artifact name 匹配语义、组删除 409 均按 §5.5 暂行写死进 T-97/T-92；逆向扩编票（auth-model 扩编，conductor 并行派发）落地回写后若推翻，改动面 = 码值/文案常量 + LIKE 谓词（窄）；T-92/T-97/T-103 派单注明「以回写后 PRD 为终断言」。
- **R4（PRD 内部自冲突：TTL 键单位）**：FR-23/Q1 写 `console.session_ttl_hours`（默认 24），W08 命令用 `console.session_ttl_seconds=5`。T-91 双键实现（hours 主键 + seconds 覆盖键供测试粒度）；PM +0.1 收口。
- **R5（断言反转时序，M2 R10 同款）**：`/binflow/` 占位 JSON→301、`/api/search/**` 404→分派（未实现族仍 404）、`/api/security/groups/**` 404→分派、`/api/v1/audit` 404→200、repo key `ui/docs/console` 可建→400——既有 routes 测试硬编码旧期望处由对应票**同批更新并注明来源**，防 CI 批 1~3 中途红；T-105 终判。
- **R6（FE 并行边界）**：T-99/T-100 同 `pages/repositories` 目录已排串行（批 5/6）；T-100/T-101/T-102 并行但共享 api client/toast/confirm/token（T-98 沉淀）——派单注明「公共组件改动限 T-98 已有面，新需求登记不互改」，冲突则顺延。
- **R7（体积/性能预算）**：SPA gzip ≤5MB（T-89 构建输出记录项）+ embed 后冷启动 <2s + 100 并发零 5xx——React 全量引与路由不懒加载会超；T-89 锁 vendor 预算 + 懒加载缝；超限 fallback = code splitting 小票（不占本 19 票额）。
- **R8（Playwright 环境）**：node≥20 + Chromium 下载依赖网络/系统库；离线降级（预装 Chromium / 本地缓存）；E2E 基建归 T-89、矩阵归 T-104、`baseURL=$BASE` 与 FE dev server 口径写死。
- **R9（互斥锁跨进程形态）**：GC（serve 进程）↔ export（CLI 进程）互斥必须 flock 级跨进程；T-94 落 `data_dir` 文件锁，ADR-0015 四安全边界为依据、architect review 把关；双进程首启竞态（M1 T-10 债，PRD §6.4 登记）同面评估、不扩 scope。
- **R10（补登与 P2 债务台账，无擅自加戏）**：补登项来源——T-90 email 列/audit 索引（PRD FR-27/GE-01）、T-89 lockfile/npm audit（ADR-0014 决策 4）、T-94 锁原语（PRD 明示归 architect）、T-103 造数脚本（NFR-P17）。**P2 债务（conductor 录 BOARD P2 区，不占票额）**：gavc（SR-03）、token 管理面端点+UI（ux R6）、审计 CSV（ux R3）、`--tar`（GE-07）、GET gc 状态、remote stats UI（ux R9）、WebKit/Firefox 门槛化（AC4）、存量 `ui` 仓启动 WARN、目录分页后端（ux R1/NFR-P18）、`?permissions` virtual/remote 分支、SPA 首屏 P1 记录项。

---

## 输出契约

- 状态: done
- 产出: /Users/lzw/dev-center/reports/agents/T-88.md（本文件，票单唯一全文）；BOARD.md 录入由主会话执行
- 统计: 19 张票（P0 x16 / P1 x3 / P2 x0），首波可并行 3 张（T-89/T-90/T-92），最大波宽 3
- 依赖链: 关键路径 T-89→T-91→T-98→T-99→T-100→T-104→T-105→T-106（前端链）；次链 T-90→T-97→T-101 汇入 T-104；后端支链 T-90→T-94→T-96 汇入 T-103
- 风险/待澄清: R1~R10；**R1（PRD vs ADR-0014 命名面）与 R2（architecture §6/§7 勘误）为派发门槛，须在批 1/批 2 前由 conductor 转 architect+PM 收口**；R4 需 PM 回写键名；P2 债务十条已列 R10 供录板
