# T-264 — web 工具链 chores：t104/t146 e2e 缺省自洽 + assert-tokens 扩面 css（FR-82-AC3/AC4）

状态：done（2026-08-25）｜role: dev-frontend｜area: `web/scripts/assert-tokens.mjs` + `web/e2e/{t104-perf,t146-docs}.spec.ts`（package.json 无需改动——build 门已接线）

## 0. 击落-恢复说明（conductor 备案）

首轮作业在 00:39 因 API 配额上限中断；02:05 后配额恢复续作。中断前仅完成只读勘察（定位 security.css:80 命中在注释、t146 专用口令 env 语义），**未落任何盘上改动**——恢复后 `git status` 核对为零半成品，全部变更由恢复后的会话完成。

## 1. AC3（QA-5）：两 spec 环境变量缺省自洽

T-263/T-265 两轮全量踩中的同一对失败（裸跑必 2 failed）：

| spec/腿 | 原因（T-265 报告第 3 节第 1 轮实证） | 修法 |
|---|---|---|
| t104-perf `NFR-P20`（50 并发登录） | `process.env.BASE` 未设时在 `page.goto` **之前**读 `new URL(page.url()).origin`——此时 page 尚在 `about:blank`，origin 不可用 → `pwRequest.newContext({baseURL})` Invalid URL | base 取值移到 `goto('/binflow/ui/')` 之后（真实 origin，与 config 缺省 `http://127.0.0.1:8080` 天然一致）；env 覆盖仍优先 |
| t146-docs `G19b-1`（控制台帮助入口） | 缺省口令 `docsqa-test-pw-146`（docs QA 专用实例口令）≠ 标准 scratch `password` → 登录失败 | `ADMIN_PW ?? 'password'`——专用口令语义保留为 **env 覆盖优先**（docs QA 脚本 `ADMIN_PW=docsqa-test-pw-146` 显式设置时仍用专用口令） |

t104 的 `ADMIN_PW ?? 'password'` 缺省本就正确（未动）；1GB RSS 腿维持 `QA_SERVER_PID` 未设即 skip 的既有约定（5 skipped 的组成部分，与 T-265 基线口径一致）。

## 2. AC4：assert-tokens 扩面至页面级 css

原版只扫 `src/styles/*.css`（3 文件）。T-264 扩面：

- **递归收集 `src/` 全量 css**（styles 共享层 + `components/dialogs.css` + `pages/**` 页面级分片 browser/repositories/tree/search/security），共 9 文件；`tokens.css`（任意层级）仍为唯一色值字面量豁免层。
- **注释感知**：断言前抹掉 `/* ... */` 但保留换行结构（失败信息行号不漂移）——只断言真实声明。动机：扩面首扫唯一命中 `security.css:80` 是 T-237 的对比度实测**注释注记**（`#946200 on 18% tint`），其真实声明用 `color-mix(var(--bf-warning)...)` 完全合规；css 注释中的说明文字不应误伤。
- **门内生效**：`npm run build` 前置既有接线（package.json 零改动），扩面自动进 build 门。
- **负例验证（有牙）**：向 search.css 追加 `.t264-probe { color:#ff0000; box-shadow:...rgba(...) }` + 注释 `#1234ab` → 精确 FAIL 2 处（`pages/search/search.css:116` 双命中，注释值不计），还原后复绿。
- T-239/T-242 人工核对过的页面级 css 扩面后**零真实硬编码声明**（唯一命中即上述注释）；与 T-266 并行的 security.css/repositories 改动以「零硬编码声明」语义天然兼容，未发生冲突。

## 3. 自测证据（scratch 实例 :8080，BINFLOW_HOME=/tmp/bf-t264，binary 00:01 构建 + seed-m8 10291 节点 + seed-m9 verify green）

```
$ env -u BASE -u ADMIN_PW -u ADMIN_USER -u QA_SERVER_PID npx playwright test --workers=1
  → 174 passed / 5 skipped / 1 failed (9.7m)   # /tmp/t264-full-run.log
$ 同上 + e2e/t104-perf.spec.ts e2e/t146-docs.spec.ts
  → 10 passed / 1 skipped (11.0s)              # 两修复腿定点：t104 NFR-P20 ✓、t146 G19b-1 ✓
$ env ADMIN_PW=docsqa-test-pw-146 npx playwright test e2e/t146-docs.spec.ts --grep G19b-1
  → 1 failed（登录即败）                        # 专用口令 env 覆盖优先——缺省不再吞掉 env
$ npm run typecheck → exit 0
$ npm run lint      → exit 0
$ npm run build     → assert-tokens: OK — 9 个 css（src 全量，注释感知）… ✓ built in 2.00s
$ node scripts/assert-tokens.mjs（负例注入 search.css）→ FAIL 2 处 file:line；还原复绿
```

### 唯一 failed 的归因（非本票回归，非 flake）

`e2e/m8/repositories-admin.spec.ts:214` readonly_admin 腿：**T-266 并行在途的未提交 spec 改动**（`git diff` vs HEAD +19 行——失败的 `toBeDisabled`/tooltip 断言正是 T-266 注释自标的「浏览器部署入口预收敛」新增段），而本次全量跑的 binary（00:01 构建）内嵌 console 早于 T-266 前端半改（RepoDetailPage/RepositoriesPage 亦为其未提交改动）。单独重跑同败（27.8s，4 passed/1 failed）——排除负载/并发 flake。**口径**：180 腿总数与 T-265 基线（175+5）一致；174 = 175 − 1，该 1 的断言集在 T-265 基线测量时尚不存在。本票两目标腿（原 2 failed）裸跑全绿即 AC3 达成；全量单命令 0 failed 的终验建议排在 T-266 落地（或其 spec/前端两半合流重建 binary）之后由 conductor 复跑。

并行实例备忘：T-266 侧进程占 :8090（PID 19829，02:19 起，非本票）；本票 scratch 留在 :8080（PID 18474，`kill 18474` 可停，数据 /tmp/bf-t264）供复验。

## 4. 契约漂移

无（零后端请求面变化；两 spec 仅改缺省回退与取值时机）。

## 5. 变更清单

- `web/scripts/assert-tokens.mjs`——递归 src 全量 css + 注释感知 + 相对路径输出
- `web/e2e/t104-perf.spec.ts`——NFR-P20 base 取值移至 goto 后（缺省自洽，env 优先）
- `web/e2e/t146-docs.spec.ts`——ADMIN_PW 缺省回退 `password`（env 覆盖优先）

## 6. 遗留

- 全量单命令 0 failed 终验依赖 T-266 合流后复跑（见第 3 节归因；`repositories-admin.spec.ts` 新断言 vs 旧 console 的窗口期错配）。
- t104 1GB RSS 腿仍需 `QA_SERVER_PID`（设计内 env-skip，与 5 skipped 基线口径一致）；e2e 默认并发恢复归 T-268。
