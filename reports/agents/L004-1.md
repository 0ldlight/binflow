# L004-1 — differential-qa：§8-C 304 透传 + ping 三消息臂 差分复验（E3-2 勘误出证）

Ticket:       L004-1 · §8-C 304 透传 + ping 消息分型 差分复验腿（实现码=conductor 收编工作树，本票零改码）
Role:         differential-qa-engineer
Area:         tools/difftest/l0041/ + reports/compatibility/L004-304-ping-diff.md
Input:        conductor 派发（304 矩阵 + ping 三臂 + E3-2 勘误双证并排 + revoked 模型级差异登记 + 契约建议归 L004-3）；自读 L000-docker-remote-evidence/diff（E3-2/E3-3/C08）、L003-3.md §2（UAT 重建指南）、L004-2（registry:3+dind 方法学）、internal/adapter/docker/{client_conditional,handler,remote,remote_face}.go + internal/auth/token.go（收编实现面）
Changes:      无产品码/契约/台账改动。新增三个可复跑差分脚本（tools/difftest/l0041/{304-matrix,expired-matrix,ping-arms}.sh）；UAT 重建 :8083→uat-l0041-worktree（.env.uat 版本键随改，gitignored）
Files:        reports/compatibility/L004-304-ping-diff.md（交付物）、reports/agents/L004-1.md（本文件）、tools/difftest/l0041/*.sh ×3；镜像 binflow:uat-l0041-worktree-alpine{-amd64}（本地留存）
Tests:        新窗口 304 矩阵 18 臂 ×2 轮（SAME 15 / DIVERGENT 3，双轮 STABLE）；过期窗矩阵 4+2 臂（客户端可见 SAME 4/4，上游计数 DIVERGENT 2）；ping 8 臂（三消息臂逐字节 SAME，revoked 第四臂 DIVERGENT=模型级）；真实 docker CLI login+pull 双端 Succeeded；skipped 0
Commands:     make release VER=uat-l0041-worktree + build-release.sh（ALPINE_SRC=alpine:3.24 直连 Hub——daocloud mirror CDN 当窗 EOF，零改动绕行）+ compose up -d --force-recreate；curl 矩阵/ping 臂（Bearer=各端 /v2/token 自签）；docker logs l0041-upstream access 行计数；dind docker pull ×2；DELETE 仓 ×4
Outputs:      reports/compatibility/L004-304-ping-diff.md；提金候选：blob 面条件矩阵（docker/remote-blob-conditional-get，§2 全 SAME）与过期 tag 重验证服务（docker/remote-manifest-revalidation-serve，§3 客户端可见 SAME）——B1 完整 artifact 面 304 头集与 X1 条件 GET→304→200 全量链可作 golden 捕获，交 compatibility-engineer 评审
Compatibility: 差异分类建议：BUG 候选 1（D1 manifest-unquoted-INM）/ INTENTIONAL 候选 1（D2 revoked 臂模型级）/ UNKNOWN 待裁 1（D3 过期 blob 回源 vs 本地）；回归对照：E3-2/E3-3 推翻、L000-F C08 复验账实缺口（只测 BinFlow 腿）→ 契约 remote-manifest-conditional-get 需重写翻 DIVERGENT（细节 §5.2）；前轮 C02/L003 ping/L004-2 各族无回归
Security:     auth 面双端对照即本票主体：ping 三臂消息串逐字节一致（Bad Credentials/Props…not found/…expired），挑战头 Basic realm="Artifactory Realm" 一致；revoked 臂差异=D2 模型级（非漏洞面）；凭据未落盘（前 agent /tmp token 材料已销毁，本会话 token 仅 shell 变量，dind 随容器销毁）
Performance:  全臂本地/单往返量级；新窗口 18 臂双端上游 0 往返；过期 tag 重验证双端 1 往返 0 字节（BinFlow 条件 GET→304 / 参照 HEAD）；无超时 case；矩阵双轮连跑稳定
Risks:        ① 参照 ping 200 有效臂 0 字节裸应答 vs BinFlow {} + CT（N4 超集，未裁）；② 参照吊销生效观察到一次 +0s 竞态（200），手工四点复核均稳定 401 revoked，报告以手工链为准；③ M6/M2b BinFlow 经「丢弃→日期臂」路径与参照「quote 不敏感匹配」结果偶合——D1 修复时这两臂可能翻面，复验须重跑全矩阵；④ E3-2 原始命令未存档，勘误的「当时为何没看到」只能给排序假设（校验值错配最可能），无法回放确证
Blockers:     无（参照全程在线；daocloud mirror 失败以直连 Hub 零改动绕行）
Next:         ① D1（manifest-unquoted）裁 BUG 后开实现票：quotedEntityTags 改 quote 不敏感+在场封日期臂（与 blob 面同构），修复后重跑 304-matrix 复验；② D2 revoked 臂模型级裁定（对齐参照需吊销态可验模型，改动面大，建议 conductor 先裁）；③ D3 过期 blob 回源方向裁定；④ L004-3 消费 §5.2 五项契约建议（含 E3-2/E3-3 errata 注记）；⑤ 提金候选两案评审

## 断点快照（会话结束时）

- 已完成 case：新窗口矩阵 18 臂×2 轮双端、过期窗 X1–X4 双端、ping P_anon/P0d/P0s/P1/P2/P3/P4a/P4b 双端、真实 pull 种缓存双端、复跑门 STABLE×2
- 未跑 case：token 端点（/v2/token）同三凭据臂（票面范围为 ping，未扩张；前 agent a_tok_*.h 为空文件无先验）
- 断点 case id：无
- 双端实例处置：difftest 测试仓 4 个已删净（含前 agent audit-l0041-docker-remote 遗留）；difftest-dind/l0041-upstream 容器已 rm；UAT :8083 = uat-l0041-worktree 保留运行；参照 :8082 原配置未动
