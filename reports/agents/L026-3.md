# L026-3 — main P0 回归：L025-6 六面键表渲染把 adapter 策略键逐出 configuration 面（返工）

Ticket:       L026-3（main P0 回归，PR #123 CI 三包红：adapter/deb ×2、adapter/rpm ×1）P0
Role:         dev-go-core（area = internal/httpapi 渲染层；附 adapter 两测试文件读位对齐，见 Files/Blockers 注）
Area:         internal/httpapi（根修）；internal/adapter/{deb,rpm} 仅 t327r_policy_test.go 的 restGetRepoConfig 读位（测试专用改动，见 Risks①）
Input:        conductor 定性事实（t327r_policy_test.go:90/:152 三红复现命令）；989ad5ed（L025-6）键表化渲染 + reports/agents/L025-6.md（Risks① 已登记漂移清单、「configuration」回显删除实测依据）；repo_config_render.go 键位表；docs/reverse/debian.md §40/§84/§118/§140（byHash/optionalIndexCompressionFormats/historyCycles/debianDefaultArchitectures 规格锚）；差分臂 fixture=tools/difftest/l0254/d02_config_diff.py（PUT 体仅 {rclass,packageType[,repositories,url]}）
Changes:      ① 根修 repo_config_render.go：新增 appendUnmodeledBlobKeys——键表未认领的存储键按「未建模键 blob 回显」口径逐字随面回显（deb/rpm 策略族 byHash/origin/label/historyCycles/optionalIndexCompressionFormats/debianDefaultArchitectures/yumGroupFileNames、helm enforce 对、conan 开关、quotaBytes 等 PUT→GET 顶层往返）；仅当 blob 实存该键才回显，参照探针仓（只带已建模键）键面零变化。② 回显排除集 blobEchoDenyKeys：stages 别名（值走 environments 座）、password（NFR-S14 恒不越读面）、repo.Service 合成进每个 remote blob 的五个 BinFlow 存储形键（allowPrivateUpstream/socketTimeoutSecs/metadataRetrievalCachePeriodSecs/enableTokenAuthentication/maxUniqueSnapshots——v1 面有对应 wire 拼法座、46 键 v2/configurations 面没有，回显会把合成形加进实测键集破 L025-8 零差；其不在窄面=L025-6 已登记漂移，FE 读 list 面不变）。③ optionalIndexCompressionFormats 未入键表：本机参照 :8082 不通、172.16.58.130 禁碰，无法 A 侧复核；且入 local 共享表会给 generic 仓也渲染默认值（A 按包型出键，61 键 generic 实测面无此键）必破零差——按回显走，包型条件座+debian 默认值探针登记候裁（见 Next）。④ deb/rpm 两 t327r_policy_test.go 的 restGetRepoConfig 读位对齐：详读面是 L025-6 实测定谳的平铺键体（A 无「configuration」包裹——旧读位是 BinFlow M3 形，L025-6 已删包裹并迁 11 个 httpapi 测试文件读位），helper 改整Body解码，断言键与期望值零改动。⑤ repo_config_projection_test.go 按语义补 TestRepoConfigUnmodeledKeyEcho：回显契约钉死（v1 面=实测 61 键+回显额外键精确集、v2/configurations 共渲染器同回显、stages 不以本拼写漏出、非 admin 4 键片面不受回显加宽）。
Files:        修改：internal/httpapi/repo_config_render.go（appendUnmodeledBlobKeys+blobEchoDenyKeys）；internal/httpapi/repo_config_projection_test.go（+TestRepoConfigUnmodeledKeyEcho）；internal/adapter/deb/t327r_policy_test.go（restGetRepoConfig 平铺读位）；internal/adapter/rpm/t327r_policy_test.go（同）。新增/删除：无。
Tests:        TestRepoConfigUnmodeledKeyEcho（新）：回显逐键值形、v1/v2/configurations 三面键集=实测集+额外键、stages 不漏出、非 admin 4 键守窄。回归门：deb 全包（含 TestRestByHashConfigDrivesIndexEngine、TestRestCompressionAndArchConfigDrivesIndexEngine）、rpm 全包（TestRestPolicyKeysDriveReindexBranches）、httpapi 全包；11 个键集精确断言文件未改未破（TestRepoConfigKeyFacesAdmin/NonAdmin/FaceValueShapes 原样绿）。
Commands:     go test -count=1 -run "TestRestByHashConfigDrivesIndexEngine|TestRestCompressionAndArchConfigDrivesIndexEngine" ./internal/adapter/deb/（复现→修后）；go test -count=1 -run "TestRestPolicyKeysDriveReindexBranches" ./internal/adapter/rpm/；go build ./... && go vet ./... && gofmt -l internal/httpapi/ internal/adapter/deb/ internal/adapter/rpm/；~/go/bin/golangci-lint run internal/httpapi/... internal/adapter/deb/... internal/adapter/rpm/...；go test -count=1 ./internal/adapter/deb/ ./internal/adapter/rpm/；go test -count=1 -timeout=25m ./internal/httpapi/；go test -race -count=1 -run "TestRepoConfigUnmodeledKeyEcho|TestRepoConfigKeyFaces|TestRepoConfigFaceValueShapes|TestRepoConfigurations|TestRepoBatchRead|TestRepositoriesV2Config" ./internal/httpapi/
Outputs:      复现（修前）：deb `--- FAIL: TestRestCompressionAndArchConfigDrivesIndexEngine (0.72s) configuration[historyCycles]=<nil> want 5 …`；rpm `configuration[calculateYumMetadata]=<nil> want true …` 四键全 nil。修后：复现命令 → `ok github.com/lzwzzy/binflow/internal/adapter/deb 1.792s`；rpm 定向 → `ok 1.105s`；BUILD+VET+FMT OK（gofmt 空）；lint `0 issues.`；`ok …/adapter/deb 42.705s`；`ok …/adapter/rpm 38.839s`；`ok …/httpapi 490.912s`（负载下偏慢，全绿）；race 定向 `ok 35.802s`。
Compatibility: 键集零差维持：差分臂 fixture（d02_config_diff.py）PUT 体仅已建模键，回显不触发；projection 键集断言原样绿=四层实测面不变。回显只在 blob 实存键表外键时加键（BinFlow 原生配置键），A 无对应键无对照面，属 conductor 口径内的「未建模键 blob 回显」。排除集五键=服务合成存储形，与 L025-6 Risks① 已登记漂移一致（未翻案）。漂移登记（新）：optionalIndexCompressionFormats 系 Artifactory deb wire 键，本应按 wire 形入表+A 侧复核——参照不可达（:8082 不通、172.16.58.130 禁碰）且共享表必破 generic 61 键面，暂走回显（调用方设值精确往返，A 对设值仓同样回显）；包型条件座+debian 仓默认值（debian.md §40 称默认 ["bz2"]，待 A 实测定谳）候 compatibility-engineer 探针。
Security:     非 admin partial 片面不经渲染器加回显路径，恒 4/5 键（新测试断言守窄）；password 恒排除（NFR-S14）；stages 别名不漏出。无新增输入面。
Performance:  渲染纯内存多一轮 blob 遍历（≤数十键/仓），无 IO；套件用时与历次同量级（httpapi 490s 为机器负载，非本改动）。
Risks:        ① adapter 两测试文件属他角 owns，本票读位对齐为测试专用最小改动（断言/期望零改），根修全在 httpapi——请 conductor 知悉备案。② 回显排除集为枚举：repo.Service 未来往 remote blob 合成新存储形键时会再破键集断言（键集测试会立刻红，非静默漂移）——合成键与 wire 拼法对齐的治本归 repo.Service 层后续票。③ deb 默认值形（未设 optionalIndexCompressionFormats 的 debian 仓）A 侧不可探，未实现（不猜）。
Blockers:     无（A 侧参照不可达不阻塞本票——回显路径无需 A 对照；入表候裁已登记）。
Next:         ① compatibility-engineer：A 侧可达时探 debian 仓 GET 键面（optionalIndexCompressionFormats 默认值形+是否按包型出键）定谳包型条件座；② conductor：FE 读位票（L025-6 Risks①）现在详读面顶层已回显 quotaBytes/byHash 等存值，L026-2 fe-rewrite 可直接读顶层；③ 治本候选票：repo.Service remote 合成键改 wire 拼法存储（排除集可退役）；④ 本票不 commit、不 push。
