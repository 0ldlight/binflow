# T-441 工作日志 — FE②-b：包类型弹窗 924px 居中 + 8 包型开禁（FR-143.3 / B-2.6 翻正，形态翻转③）

- role：dev-frontend ｜ 日期：2026-09-03 ｜ 状态：done（自测四门全绿 + 双实例 e2e + 八型真实客户端矩阵全绿）
- area：`web/src/pages/repositories`（modal 族）+ 联动 lib/依赖注释 + e2e 三 spec 翻新 + 一新 spec；parity 册 M1 行 v1.6 留痕 + 锚册 v1.34。
- 票面：M16-SPLIT §1.2 AC1~AC3（按 7.161 勘误执行：880px 票面值 → **924px 实测值**）。

## 1. 变更清单

| 文件 | 变更 |
|---|---|
| `web/src/pages/repositories/RepositoryFormPage.tsx` | ① PackageTypeGrid：Dialog 宽 `min(440px,…)` → **`min(924px, calc(100vw - 48px))`** + **`maxWidth={false}`**（MUI 'sm'=600px 钳制必须显式解除——924 档首位宽 Dialog）；② `pkgChoiceBlock` 整函数退役（前端 license 槽位门开禁）：磁贴去掉 disabled/title/mono 变体，恒 brand + desc 回归协议描述；③ 表单 radio 行（`form-package-*`）同门禁用退役（两承载面一致）；④ 头注/步注释 T-441 定案记录 |
| `web/src/pages/repositories/repositories.css` | ① `.pkg-grid-item:disabled` 规则退役（注释留痕——门控三件套随 T-441 消失，`:hover:not(:disabled)` 防御性保留）；② **新增磁贴内档位徽章文字色收敛**：`.pkg-grid-item .MuiChip-root.tier-{pro,enterprise} .MuiChip-label` 沿 base.css 语义徽章家族 `color-mix(语义色 78%, 正文色)` 配方（开禁后磁贴进 WCAG 义务面，raw info `#0b6bcb` 在 surface-2 `#e9edf1` 上 = 4.48:1 差 0.02；收敛后 light ≈5.4:1 / dark 更高——axe 双主题实证） |
| `web/src/lib/addons.ts` | `lockedHint` + `PkgTypeOption.disabledByConfig` 随唯一消费者退役（死代码清账；`isDisabledByConfig` 保留——LicenseAddonsPage 仍在用）；头注记 T-441 口径 |
| `web/src/lib/repos.ts` | 注释更新：前端槽位门（原 pkgChoiceBlock）退役 → 后端 D3 唯一裁决点 |
| `web/e2e/m16/t441-pkg-modal-open.spec.ts` | **新 spec（5 测）**：① 924px 居中（boundingBox 宽 + 视口居中腿）+ **13 型计数断言**（不伪造 41 型）+ 八型 enabled/brand/opacity 1/pro 徽章/title 退役；② 纯前端门开禁·后端终裁双态分支（community → form-error 含 `package type not available` + 停留表单页；licensed → 成链 + API 对账）；③ **八型建仓矩阵条件腿**（槽位探测 skip / 全跑）；④ axe 双主题 |
| `web/e2e/m14/t383-newrepo-parity.spec.ts` | 宽度断言 440→924 + 居中腿 + 注释翻转③留痕（测试名同步） |
| `web/e2e/m14/t390-pkg-icons.spec.ts` | 门控三件套断言翻新（disabled/mono/0.4 → enabled/brand/1；徽章保留）；440→924；dark 注释更新 |
| `web/e2e/m10/console-license-addons.spec.ts` | L27d 腿：门控磁贴/radio 由 disabled+hint → **enabled+徽章**（D5 不变）；头注 T-441 口径 |
| `web/e2e/repo-policy-keys.spec.ts` | **T-439 straggler 顺车归位**（先例 = T-439 顺车修 m8/repositories-admin 的 T-431 漏翻新腿）：六腿补 `gotoAdvanced`（策略键节自 T-439 起驻 Advanced 步——HEAD 上即红，非本票引入；断言语义不弱化只补步进导航） |
| `web/scripts/anchor-audit.mjs` | STOP 表 +2 行文假阳性（v1.34：surface-2 / t441-pkg-modal-open——T-439 先例同款带版注） |
| `docs/design/console-artifactory-parity.md` | **v1.6**：§0 版本行 + 状态头 + M1 行四格改写（Artifactory 行为格升 7.161 勘误口径〔924px 居中 + Create a Repository 文案 + enterprise 零禁用〕；MUI 映射格 924px；载体格 as-built；差距格**翻转③留痕**——v1.4 440px 定案被勘误翻平，翻案依据 M16-SPLIT §1.2 AC1 非默认推翻）+ §12 B-2.6 行 → **翻正 · 已落（T-441）** |
| `docs/design/console-ux.md` | **v1.34**：§0 版本行（零锚变更——尺寸断言走既有 pkg-grid 锚）+ 票据行 + 状态头（**顺车勘误 v1.33 头部滞后**）+ §10.5 T-288 批后新增 T-441 变更注记 + §10 锚清单 pkg-grid-item 行改动态全集口径 |
| 服务端 | **diff=0（纯 FE 票）**——`git status` cmd/ internal/ 零本票条目 |

## 2. 自测（实际命令与关键输出）

四门（web/，最终树状态）：

- `npx tsc --noEmit` → exit 0。
- `npm run lint` → **0 errors** / 36 warnings（HEAD 基线同数）。
- `npm run build` → `✓ built in 3.14s`（RepositoryFormPage chunk 50.35 kB / gzip 15.02 kB）+ relink/wire 正常；console 重嵌（`rm -rf internal/console/dist` 后整目录重拷，placeholder.html 保留）+ `bin/binflow-server` 重build。
- `node scripts/anchor-audit.mjs --ledger` → `PASS（registered 与册一致：unregistered/broken 双零，retired 全部在表）`。

SPA 预算（gzip js+css 合计）：T-439 基线 392,201B → **390,152B = −2,049B ≤ 10KB/票** ✓（门控门死代码清账净减）。

e2e（scratch 净实例 `/tmp/t441-data`，listen 127.0.0.1:18081，console 重建 + go:embed 重嵌 + 种子 seed-m8）：

- **community 轮**（无 license）：新 spec `t441-pkg-modal-open` → **3 passed + 1 skipped**（八型矩阵条件腿自 skip；D3 拒绝链腿走 community 分支绿——form-error 原文 `package type not available`）；表单族合跑（t383 + t390 + repositories + m8/repositories-admin + m8/a11y-sweep + t439 + repo-policy-keys）→ **34 passed**（repo-policy-keys 六腿为 **T-439 straggler**，归位后 7/7 绿——见 §5 遗留）；`m10/console-license-addons`（--project=m10）→ 5 passed；t104-supplements + console-smoke + m8/shell + governance → 15 passed + 2 skipped（docker/remote 凭据环境条件腿，T-439 同款）。
- **licensed 轮**（`POST /api/system/license` 装 `/tmp/binflow-enterprise.lic` → tier=enterprise）：新 spec → **4 passed**（八型矩阵全跑：逐型磁贴点选 → key → Create → toast + 详情 + API 对账 packageType 落库 + 删仓收尾）；t439 → **6 passed**（**T-439 的 conan 条件腿在此窗口全跑**——其「FE 腿待 pro 档 e2e 窗口」遗留清偿）；t383 + t390 → 9 passed。
- 实例留存：`BASE=http://127.0.0.1:18081`（admin/password，enterprise）留验，八型 roundtrip 夹具仓 `t441-rt-*` 在场可活体核对。

## 3. AC2 八型真实客户端 roundtrip 矩阵（licensed 实例 + 控制台建仓仓）

八仓全部**经控制台 UI 建仓**（Playwright 驱动 924px 弹窗磁贴点选 → 表单 → Create；脚本实测 modal box **924×656 @ x=178 = (1280−924)/2 精确居中**）。客户端全绿：

| 包型 | 真实客户端（版本） | roundtrip 证据（命令 → 输出） |
|---|---|---|
| go | go 1.26.6 | PUT .zip/.mod/.info 三件 201 → `GOPROXY=…/t441-rt-go GOSUMDB=off`（.netrc 凭据——go 拒绝 insecure URL 内嵌凭据）`go mod download` + `go build` + run → stdout `t441mod@v1.0.2`（包体常量证明） |
| nuget | dotnet 10.0.400（/tmp tarball 装） | `dotnet pack`（真 csproj）→ `dotnet nuget push --source binflow-rt` → `Created …/flatcontainer/ 208 毫秒` → `dotnet add package T441.Lib --version 1.0.0`（restore 走 BinFlow）→ `dotnet run` → `hello from T441.Lib via BinFlow`；另 curl v3 面对账：push 201 + index.json `{"versions":["1.0.0"]}` + nupkg 下载**逐字节一致** + search `t441.probe 1.0.0` |
| cargo | cargo 1.98.0 | API token（POST /api/security/token）→ `cargo publish --registry binflow`（`Published t441crate v0.1.0`）→ 净 CARGO_HOME 消费：`cargo add` + `build` + `run` → `t441crate@0.1.0` + search API `total 1` 对账 |
| conan | conan 2.32.0（venv 装） | `remote add/login` + `create` + `upload`（revision `4f06123d…` Uploaded）→ 净 CONAN_HOME `install --requires=hello/1.0@myuser/stable -r binflow-rt` → `Install finished successfully` + 缓存内 `hello.txt` 内容 roundtrip |
| helm | helm 4.2.4 | `helm package` + chart PUT 201 → `repo add/update/search`（`binflow-rt/mychart 0.1.0`）+ `pull` **逐字节一致** + `show chart` |
| helmoci | helm 4.2.4（OCI 面） | 凭据文件方案（明文 HTTP 替代 registry login，docs/user/docker-registry.md 同款）→ `helm push --plain-http …oci://127.0.0.1:18081/t441-rt-helmoci`（digest `sha256:7f98d47…`）→ `pull --plain-http`（同 digest + 逐字节一致）+ `show chart` |
| rpm | dnf（rockylinux:9 容器） | 容器内 rpmbuild 真 .rpm → curl PUT 201 + `POST /api/yum/<repo>?async=0` REINDEX 200 + repomd 在场 → 容器内 dnf `makecache` + `repoquery binflow-t441-0:1.0-1.el9.noarch` + `install` → `/opt/binflow-t441/hello.txt` = `binflow t441 rpm roundtrip` |
| debian | apt-get/dpkg（debian:bookworm 容器） | 容器内 dpkg-deb 真 .deb → debPUT（matrix 三坐标）201 → Release/Packages 自动重算（`Package: binflow-t441`）→ 容器内 `apt-get update/install` + `dpkg -s`（`Status: install ok installed` / `Version: 1.0-1`）+ 装包二进制 run → `binflow t441 deb roundtrip` |

**零条件 skip**（本机 enterprise.lic 在场走满 FAT 路径——票面「或 spec 条件 skip + 留痕」的降级分支未启用；spec 内条件腿在 licensed 实例已实跑）。

## 4. 过程发现与处置

1. **开禁引出对比度新义务（已修）**：原门控磁贴 disabled 子树被 WCAG 豁免（axe 跳过）——开禁后 `pro` Chip 标签 raw info 色在 surface-2 上 4.48:1（差 0.02）首扫即红。修法 = 磁贴语境下徽章文字沿语义徽章家族「78% 语义色 + 22% 正文色」收敛配方（base.css `.badge.tier-*` 同款——彼配方的 `:not(.MuiChip-root)` 排除当时假设 MUI 皮肤自足，surface-2 上不成立）。surface-1（表单 radio 行徽章）raw 主色 5.2:1 本就达标不动。
2. **T-439 straggler（顺车归位）**：`repo-policy-keys.spec.ts` 六腿在 HEAD 即红（T-439 把策略键节迁 Advanced 步时该 spec 不在其翻新 ×4 清单内；其报告「合跑 31 passed」的算术不含本 spec 7 测）。按 T-439 顺车先例补 `gotoAdvanced`，断言不弱化，7/7 绿。
3. **MUI maxWidth 钳制**：924 > Dialog 缺省 maxWidth='sm'（600px），不显式 `maxWidth={false}` 会被静默钳到 600——首只宽 Dialog 的坑，已在锚册/代码注释留痕。

## 5. 契约漂移 / 登记

1. **dotnet 9+ 的 nuget 客户端对 http 源要求 `allowInsecureConnections="true"`**（nuget.config packageSource 行）——T-287 时代的 nuget.config 形态在 dotnet 10 直接拒推（`NuGet 需要 HTTPS 源`）。**建议 tech-writer**：nuget 接入文档（docs/reverse/nuget.md 或 docs/user 对应篇）补该属性（明文 HTTP 内网档）；`dotnet new console` 在纯 tarball SDK 上还有模板退出码坑（--force/手工 csproj 绕过，runbook 细节）。
2. **观察（转 conductor，非漂移）**：T-439 报告「表单族合跑 31 passed + 2 skipped」未实际覆盖 repo-policy-keys（见 §4-2——HEAD 上 6 红）；本轮已归位，建议 QA 终验把「T-439 straggler sweep」补进收口清单（T-439 自己提过的同款建议）。
3. **锚册状态头滞后**（v1.33 行在档但「状态」格停 v1.32）——v1.34 顺车勘误留痕。
4. 无 API 契约漂移（本票零服务端接触；`/api/v1/addons`、D3 拒绝文案与 addons.ts 既有模型逐字一致）。

## 6. 遗留

- **e2e README §1 的全量口径**未在本票重跑（全量 132+ 测需纯净 community 宿主 + ~20min；本票跑的是票面关联面 + 表单族 + 邻面回归，含 a11y-sweep 全路由双主题）。conductor 如需全量收口证据可指示 QA 终验腿。
- licensed scratch 实例（:18081）与 `/tmp/t441-dotnet`、`/tmp/t441-conan-venv` 留验在场；**不受保护的 /tmp 产物随机器重启消失**，复跑口径见本日志 §2/§3 命令。
- 磁贴列数在 924px 档为 auto-fill 自适应（~4 列）；Artifactory 7.161 的 6 列 90×90 方磁贴形态**未跟**（BinFlow 190×44 高密度卡是 v1.4 既有口径且翻转③票面只钉宽度档——若 ux 要追平磁贴形态另立票）。
