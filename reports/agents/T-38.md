# T-38 工作日志 — blob 域全链路：upload 三式 + mount + 状态查询 + 读路径（FR-8）

- ticket: T-38 [P0] blob 域全链路：upload 三式 + mount + 状态查询 + 读路径（FR-8） `role:dev-registry-adapter`
- 状态: done
- 日期: 2026-08-18
- area: `internal/adapter/docker/`（blob 子域新增 blob.go / uploads.go / range.go / digest.go + 两处最小增量接线：api.go 的 WithStorage 缝、handler.go 的路由分派）+ 对应测试 + 集成测试 `internal/httpapi/docker_blob_test.go` + 装配点（cmd/binflow-server/main.go、httpapi/harness_test.go 各一行）
- 输入: T-32.md T-38 节 AC（DE-02~07/14 细节）、docs/prd/milestone-2.md v1.1（FR-8/DE-02~07/DE-14/D06/D07/D10/D10b/D12/D13/D13d/D14）、docs/design/architecture.md §5.3（会话对接三裁定 + §11.11 跨进程续传 M3+）、docs/reverse/docker-registry.md §2（高置信度行）、T-37 交接（authorizeRoute 权限门 + 挑战矩阵）、T-35（PutManifest/PutFromBlob 用例）、storage.Session（T-9：BeginSession/Append 流式/Commit(expect)/Abort/ErrSessionPoisoned）

## 实现

### ① upload 三式（DE-02/04/05，`uploads.go`）

- **POST initiating** → 202 + `Location: /v2/<name>/blobs/uploads/<uuid>`（**相对路径**，R6）+ `Docker-Upload-UUID`（纯 UUID，无 `.patch` 后缀）+ `Range: 0-0` + `Content-Length: 0` + api-version 头。官方形，不采纳 Artifactory 的省略 Range。
- **POST `?digest=` 单请求**（规格照抄 7）→ 201 + `Location` blob URL；digest 失配 → 400 `DIGEST_INVALID`（官方码；Artifactory 的 `BLOB_UPLOAD_INVALID` 文案不采纳）+ 会话 Abort 零残留。
- **PATCH**：无 `Content-Range` = 全量流式（官方 Stream upload）；给了则**起点必须 == 服务端 received**，失配 416 + `Range: 0-<received-1>` 空 body（§2.2#3 高置信度行）。`Range` 头随每块返回累计值。
- **PUT `?digest=` 终结**：body 可空（流已推完）或带末段（`Content-Length != 0` 时先 Append 再 Commit）；`Commit(expect)` 失配 → 400 `DIGEST_INVALID`，会话已被 storage 消费（Commit 失败即 finalize 的 storage 契约）+ 注册表移除。
- **GET `<upload-url>`** → 204 + `Range` + `Docker-Upload-UUID`（官方端点，规格建议栏 5——Artifactory 的非标 `.patch` GET 不做）；**DELETE** → 204 取消。
- **会话注册表**（`sessionRegistry`）：进程内 UUID→Session+received 映射，重启后旧会话 404 `BLOB_UPLOAD_UNKNOWN`（架构裁定 1：跨进程续传 M3+；磁盘残渣由 storage 既有 TTL sweep 收）。
- **毒化对接**（ErrSessionPoisoned）：Append 失败 → 注册表标记 `poisoned` 但保留条目（客户端能观察到失败而非静默归零）；毒化会话的后续 PATCH → 416 + 权威 offset（重启线索）；毒化 finalize → 400 `BLOB_UPLOAD_INVALID` + 消费（绝不 Commit）。

### ② mount + 读路径（DE-03/06/07/14，`blob.go`）

- **mount**：`?mount=&from=` → 源 repo read（NFR-S12，经 `Authorizer.Can`）且源 layout 有该 blob（`repo.Service.Get` 探测）→ 201 走 `PutFromBlob` 零拷贝（sha1/md5 取 ledger 行，不信任客户端声明）；任一不满足 → **降级 202 普通会话**（spec 的 fall-back，永不报错）。目的地权限形拒绝（403 族）仍如实渲染。
- **GET blob**：200 流式 + `Docker-Content-Digest` + X-Checksum 全套 + ETag(=sha1) + `Accept-Ranges: bytes`；Range 206/416 复用 M1 条件请求基建语义（单 range 切片 / 越界 416 `bytes */<total>` / 多 range 与非 bytes 单位忽略走 200）；404 `BLOB_UNKNOWN` spec 体（detail 带 digest）。
- **HEAD blob**：200 + Content-Length + Docker-Content-Digest + X-Checksum-Sha256/Sha1/Md5（规格照抄 6）。
- **DELETE blob** → 405 `UNSUPPORTED` + `Allow: GET, HEAD`（DE-14 有意不兼容：物理回收唯一入口是 GC）。
- **node 路径** `<image>/blobs/<hex>`（架构 §6 布局约定）。
- **空层特例**（规格照抄 4）：digest `sha256:a3ed95ca...` 的 GET/HEAD **直接合成 32B 固定响应**，不查库（docker push 空层高频路径——客户端根本不上传该 blob）；sha1/md5 由常量 bytes init 时计算（非手抄），测试逐位校验常量自洽。manifest 校验豁免归 T-39（引用链查询时同样短路）。

### ③ 关键设计裁定：blob 的 metadata 挂账走 `repo.Service.Put`（非 PutFromBlob）

上传 finalize 后的 ledger+node 挂账不能走 `PutFromBlob`：它要求 ledger 行**已存在**（孤儿 blob 拒绝），而 ledger 行只由 `Put` 的 `putNode` 路径创建——全新 docker push 没有先前的 generic deploy 可依托。裁定：adapter 在 `Commit` 后将刚落盘的 blob 经 `store.Open` 回读成流，喂给 `svc.Put(path, body, expect)`——与 generic 上传**同一条落账路径**（blobs 台账行 + node 行、既定顺序、幂等重传规则全继承），`Put` 内部的 session Commit 对已存在 blob 是 singleflight 幂等命中（同 FS 零拷贝、无第二份物理副本）。挂账失败（Commit 已成功）→ 按架构语义记 ERROR 日志、blob 成 GC 候选，201 仍然成立（blob 已持久）。此裁定不越 area（未改 repo 包，复用既有 SPI）。

### ④ 装配（最小增量）

- `docker.Handler.WithStorage(store storage.Engine, ledger BlobLedger)` setter：T-37 的 `New` 签名不动（旧装配点零改动编译通过），生产装配两处（`cmd/binflow-server/main.go`、`httpapi/harness_test.go`）各加一行 `.WithStorage(st, md.Blobs())`。
- storage seam 为接口注入（架构 §5.3 会话对接裁定的落点：adapter 持协议状态 received+UUID 配对，storage 不感知协议头）；`BlobLedger` 与 generic.BlobLedger 同构（consumer-side interface）。
- `handler.go`：repo 门 + `authorizeRoute` 之后按 tail 分派 `blobs/uploads*` 与 `blobs/*`，manifests/tags/catalog 仍落 T-39/T-40 的基座 404——**不动 token/scope/既有 handler 逻辑**（仅删掉 T-33 留的空 sessionRegistry 桩，实现移至 uploads.go）。

## 自测（命令 + 结果）

```
$ go build ./...                              → 退出码 0
$ CGO_ENABLED=0 go build -trimpath -o /tmp/binflow-t38 ./cmd/binflow-server  → 退出码 0
$ go vet ./...                                → 零输出
$ gofmt -l cmd internal                       → 空
$ golangci-lint run ./...                     → 0 issues
$ go test -count=1 ./...                      → 12 包全 ok
$ go test -race -count=1 ./...                → 见注
```

race 注记：单次全量并发跑 storage 包 605s 超时（挂点 `TestSingleflightExecutesOnce`，M1 T-9 遗留的 concurrency_test.go 时序用例，非本票文件；该用例的 `started` 通道 announce/收流机制在重负载下有理论死锁窗）。以当前代码树分组复跑：**12 包 `go test -race` 全 ok**（storage 48.2s / adapter/docker 9.9s / httpapi 39.0s / repo 26.2s / generic 14.3s 等；`TestSingleflight*` 单独 -race 亦 ok）。该 flake 建议转 dev-go-core 复核（超出本票 area）。

## 客户端验证（curl 黑盒 · 裸进程真栈）

binary `/tmp/binflow-t38`（零 CGO），127.0.0.1:18238，独立数据目录。矩阵脚本 `/tmp/t38-curl.sh`：**38 断言全 PASS、0 FAIL（exit 0）**。关键行：

| 序列 | 断言 | 结果 |
|---|---|---|
| D06 | POST → 202，Location **相对路径**，UUID==Location 尾，`Range: 0-0`，CL 0 | PASS ×5 |
| D06 | PUT finalize → 201；GET 10MB → 200，`cmp` 逐位一致；HEAD `Docker-Content-Digest`/`Content-Length: 10485760`/`X-Checksum-Sha256` 全套 | PASS ×5 |
| D07 | `POST ?digest=` 单请求 → 201 | PASS |
| D10 | 3 段 PATCH（4MB×2+2MB）全 202，Range 递增 `0-4194303`→`0-8388607`→`0-10485759`；空 body PUT → 201；`cmp` 一致 | PASS ×7 |
| D10b | 2 段后 GET upload-url → **204 + `Range: 0-4194303`**；续传第 3 段 + finalize → 201 | PASS ×3 |
| D10 变体 | Content-Range 起点失配（0-4194303 当 received=4194304）→ **416 + 权威 `Range: 0-4194303` + 空 body** | PASS ×3 |
| D12 | digest 失配 → 400 `"code":"DIGEST_INVALID"`；错误 digest 处 GET → 404（无幽灵 blob） | PASS ×3 |
| D13 | mount（源读 + 在场）→ 201；目的地 GET → 200 | PASS ×2 |
| D13d | DELETE blob → 405 `"code":"UNSUPPORTED"`；blob 存活 | PASS ×3 |
| 空层 | GET 全新 repo → 200，32 字节，sha256 == `a3ed95ca...`；HEAD CL=32 | PASS ×3 |
| FR-8-AC7 | generic `curl -T` 后 docker 推同内容：stats blob 计数 1 → 2（generic 一个 + docker **去重零新增**） | PASS |

补充真栈探针：

- 匿名 POST uploads → 401 + `Www-Authenticate: Bearer realm=".../v2/token",service="binflow",scope="repository:docker-local/acme/app:pull,push"`（T-37 门在新端点上继承生效）。
- `digest=sha512:<128hex>` → 400 `DIGEST_INVALID`（M2 仅 sha256，架构裁定 2）。
- `Range: bytes=0-3` → 206 `Content-Range: bytes 0-3/42`；越界 → 416 `bytes */<total>`。

**D14（kill -9）**：5MB 唯一内容推 2.5MB 后 `kill -9` → 重启：

| 断言 | 结果 |
|---|---|
| 未终结 blob GET | **404**（不可见） |
| 历史（kill 前已终结）blob GET | **200**，`cmp` 一致 |
| health | **200** |
| 旧 session URL | **404 `BLOB_UPLOAD_UNKNOWN`** |
| 重启后重推（新会话从头）PATCH+finalize+GET | 202/201/200，`cmp` 一致 |
| sessions/ 残渣 | 1 目录（TTL grace 内，既有 sweep 语义——PRD D14 原文「残渣由既有 ttl 清理」即此行为） |

**docker 客户端级（D05/D08）不在本票**（票据注明归 T-44；本机 daemon 的 insecure-registries 环境项见 T-37 日志，同样适用于此处）。

## 测试清单

新增 `internal/adapter/docker/blob_test.go`（包内单测，真 storage 引擎 + fake service）：

- `TestUploadThreeStyles`（5 行表：monolithic PUT / 单请求 POST / chunked×3+空 PUT / 无 Content-Range 流式 / PUT 带末段——每式推同一内容后 GET 逐位一致 + HEAD 头 + layout node 在场 + 会话清零）
- `TestUploadStartResponseShape`（202 精确形状：相对 Location/UUID 配对/Range 0-0；GET 204；DELETE 204 后 404 BLOB_UPLOAD_UNKNOWN）
- `TestContentRangeMismatchMatrix`（5 行：起点落后/超前/远超前/无 dash/非数字 → 416 空 body + 权威 Range；**会话存活**，正确锚点续传恢复）
- `TestDigestMismatchAbortsSession`（D12 双式：finalize 与单请求；会话与 blob 双零残留）
- `TestPoisonedSessionRefusesOperations`（取消 context 中断 Append → 毒化标记；毒化 PATCH 416；毒化 finalize 400 BLOB_UPLOAD_INVALID 且消费）
- `TestConcurrentSessionsConverge`（8 worker × 3 repo 并发，全 201/202，会话收敛 0）
- `TestBlobUnknownAndDelete`（404 BLOB_UNKNOWN 体；DELETE 405+Allow）
- `TestBlobRangeRequests`（9 行：slice/open/suffix/clamp/越界/倒置/空后缀/多 range 忽略/其它单位忽略）
- `TestEmptyLayerSynthesis`（常量自洽 sha256 校验 + GET 32B + HEAD 头族）
- `TestCrossRepoMount`（4 子例：零拷贝 201 且 puts 断言恰一次挂账 / 源缺 blob 降级 202 / **无源读降级**（NFR-S12）/ 坏 digest 降级）
- `TestUploadMethodMatrix`、`TestDigestParamShapes`（405+Allow；8 行 digest 文法）

新增 `internal/httpapi/docker_blob_test.go`（真栈集成，15 顶层）：

- D06/D07/D10/D10b/D12/D13/D13d/空层/Range/404 等价全链（`TestV2BlobUploadMonolithic` 等 10 个）
- `TestV2BlobSessionDiesWithProcess`（D14 核心：同 dataDir 二次装配 = 进程重启；半推内容前后都不可见、历史 blob 存活、旧会话 404）
- `TestV2BlobSessionSweepResidue`（sessions/ 残渣由启动 sweep 清——grace 语义下当前测试用 TTL 到期路径验证目录清空）

（注：sweep 残渣测试以「无 state 的孤儿目录」走 TTL 臂验证；grace 内活残渣保留是 M1 既有行为，非本票引入。）

- `TestV2BlobCrossProtocolDedup`（FR-8-AC7：generic PUT 后 docker 推同内容，`blobs.Count` 不变 + 两 node 共享同一 sha256）
- `TestV2BlobUploadAuthGate`（匿名写挑战带 scope / 无授权 403 DENIED / 授权后 202）
- `TestV2BlobConcurrentUploads`（6 并发全收敛 + ledger 计数 6）

修改：`internal/adapter/docker/api.go`（WithStorage + BlobLedger + store 字段）、`handler.go`（分派 + 桩移除）、`errors.go`（新错误码 BLOB_UNKNOWN/BLOB_UPLOAD_INVALID/BLOB_UPLOAD_UNKNOWN/DIGEST_INVALID/UNAVAILABLE + writeAPIVersionHdr）、`cmd/binflow-server/main.go` 与 `httpapi/harness_test.go`（装配一行）。

M1/M2 既有测试零回归（12 包全 ok 即证；T-33/T-37 的 name/token 矩阵原样通过）。

## 规格依据

- 202 头形/Range 0-0/相对 Location/纯 UUID：docker-registry.md §2.1 + §10 建议栏 6/7（官方形，不采纳 Artifactory 的省略 Range 与 `<uuid>.patch`）。
- Content-Range 错位 416 空 body：§2.2#3 高置信度。
- digest 失配 400 DIGEST_INVALID：§2.3 + T-32 裁定（Artifactory 的 BLOB_UPLOAD_INVALID 文案不复刻）。
- POST 单请求 201：§10 照抄栏 7。
- GET 上传状态 204 + Range：官方端点，§10 建议栏 5（`.patch` 变体不做）。
- mount 降级 202：spec fall-back 语义（架构 §5.3 裁定 3）。
- 空层 32B 合成：§2.3 + §10 照抄栏 4；常量 bytes 取自逆向规格确认的固定 artifact（hex 直写 + 测试逐位断言）。
- blob DELETE 405：DE-14/D13d（ADR-0006 安全底线）。
- Range/条件请求：M1 generic/conditional.go 的语义契约在 docker 面复述（generic 实现是包私有；跨包导出会耦合两 adapter 的 HTTP 面，故同规则本包实现，契约注释指明同源）。

## 遗留 / 交接

1. **T-39（manifest 链）**：blob 读路径与 node 布局已就位——manifest body 走本票的上传端点落 blob 后，`PutManifest` 直接可用；空层豁免在 manifest 引用链查询处短路（`emptyLayerDigestHex` 常量已导出于包内）；`blobNodePath(image, hex)` 是共享布局函数。
2. **T-40（catalog/tags）**：路由分派点已留（tail 非 blobs* 落基座 404），接上即走。
3. **`TestSingleflightExecutesOnce` flake（转 dev-go-core）**：M1 concurrency_test.go 的 announce/收流机制在 12 包全量 -race 并发跑的重负载下 605s 超时一次；当前树分组 -race 与单用例 -race 均 ok。非本票文件，建议复核其 `started` 通道的握手时序。
4. **PRD 勘误建议（转 conductor/PM）**：PRD §8 的 D06/D13 示例 URL 用 `/v2/docker-local/blobs/uploads/`（**单段 name**），按 ADR-0010 第 3 条（name = repoKey/image）这是 404 形。T-43 复跑时应写 `/v2/docker-local/<image>/blobs/uploads/`（本票 curl 与测试均已用全名形）。
5. **`Docker-Upload-UUID` 响应头大小写**：Go 的 http.Header 规范化为 `Docker-Upload-Uuid`；HTTP 头域大小写不敏感，docker/oras/crane 均按规范化名匹配（distribution 参考实现同样经 Go 栈发出 `Docker-Upload-Uuid`），非兼容性风险，记录备查。
6. **挂账失败的 201**（裁定 ③ 的边界）：**已被修复轮 B4 推翻改 5xx**，见下「修复轮」。本条仅存档。
7. **docker 客户端级验证**（D05/D08）：环境项（daemon insecure-registries）归 T-44/T-45 compose 路径（T-37 已留档）。

---

## 修复轮（双 review REQUEST_CHANGES → 已全部修复，2026-08-18）

输入：T-38-review-correctness.md（B1~B4 + 8 条 non-blocking）、T-38-review-arch.md（B3 同根 + N1~N6）、conductor 修复单。产出文件：`internal/adapter/docker/review_fixes_test.go`（新增）+ `uploads.go`/`blob.go`/`blob_test.go` 修改。

### B1（必修）：liveUpload 无锁并发读写（race 实证）→ 已修

- `liveUpload` 增 `mu sync.Mutex`（uploads.go:39）；**临界区覆盖整个会话操作**：PATCH 的「Content-Range 判定 + Append + received 赋值」一体、PUT finalize 全程、GET 偏移快照读、DELETE 的 Abort——共 5 个锁点（uploads.go:279/302/321/…）。
- reviewer 实证的 race 告警面（旧 :232/:265/:270 裸读写）全部进入锁内；注册表 `lookup` 仍只护 map（指针交接），条目状态由 `up.mu` 管——两层锁职责分离写在 `liveUpload` godoc。
- **测试**：`TestConcurrentSameUUIDPatchSerialized`（6 并发 PATCH 同 UUID 锚 0：恰 1 个 202、5 个 416，被接受者 Range 精确 `0-4095`，恢复块续传收敛）；`TestConcurrentMixedVerbsSerialize`（GET×2/PATCH/PUT×2 同 UUID 混合竞态：无 5xx、注册表终态为空）。两测试设计为 -race 下跑——修复前代码在此处触发 detector（reviewer 探针已实证），修复后绿。
- **真栈行为验证**：6 个并发 `curl -X PATCH -H 'Content-Range: 0-99'` 同会话 → `202 416 416 416 416 416`（恰一个锚点赢家，权威 Range 一致）。
- 修复此问题时竞态测试暴露一个**连带缺陷并一并修复**：PUT 赢得锁后，迟到的 PATCH 落在 storage 已 finalize 的会话上，旧代码把它渲染成 500（"session already finalized"）。新增 `liveUpload.done` 标志：会话经任一终结路径离场后，迟到者观察 `BLOB_UPLOAD_UNKNOWN`（协议视角会话已不存在），绝非 500。`finalizeUpload` 六个出口、DELETE、驱逐路径全部置位。

### B2（必修）：会话注册表 + fd 无限泄漏 → 已修（idle TTL 驱逐）

- `sessionRegistry` 增 TTL 驱逐：常量 `idleSessionTTL = storage.DefaultSessionTTL`（24h，与 storage 侧同一数字——注释说明两处 TTL 的对齐理由）+ `idleSweepInterval = TTL/48`（默认每小时两次）；`startSweep()` 在首个会话建立时 `sync.Once` 启动后台 goroutine。
- `evictIdle(now)`：锁内快照过期集合并**先从注册表移除**（竞态中的 PATCH 观察 404 而非加入正被拆的会话），锁外 `sess.Abort`（磁盘工作不占锁）并置 poisoned；Abort 幂等 + 与合法 finalize 双向幂等remove，故 sweep 不可能破坏活状态。死字段 `started` 转为驱逐锚（`lastActivityLocked`）——N5 一并消化。
- **测试**：`TestIdleSessionEviction`（TTL 50ms 注入：过期会话被驱逐且 `sessions/<id>` 目录消失；四动词全部 404 `BLOB_UPLOAD_UNKNOWN`；TTL 内新会话同轮 sweep 存活且偏移查询 204）。
- **真栈验证**：5 次未终结 POST grant → 磁盘 5 个会话目录由该机制在 TTL 后收（storage 启动 sweep 只兜进程重启，运行期归本驱逐——修复前这两个目录会永驻）。

### B3（必修，双 review 同根）：blobPresent 丢 reader → fd 泄漏 → 已修

- `blob.go:330` 按两 reviewer 给的两行修法：`rc, _, err := Get(...); if err != nil { return false }; _ = rc.Close(); return true`，附 nolint 理由（只读 fd；Close 结果不影响在场判定）与泄漏成因注释（真栈 Get 返回 `*os.File`，单测 fake 内存 reader 掩盖）。
- **测试**：`TestMountProbeClosesReader`（countingGetService 包装，断言成功 mount 的 probe reader opened==closed，非零即 wired）；**真栈臂** `TestRealStackMountNoFdAccumulation`——真 storage+真 metadata+真 repo.Service 上 50 次连续 mount，进程 `/dev/fd` 计数不增（before/after 差 ≤5 容差；`Readdirnames` 而非 `ReadDir`，绕开 macOS /dev/fd 符号链接 fstatat 失败）。

### B4（必修）：Commit 成功但挂账失败回 201（假成功）→ 已改 5xx

- `writeBlobCreated`（uploads.go）：`registerBlobNode` 失败 → **500 `UNKNOWN`**，body 明示 "blob stored but its repository record could not be written; the push is safe to retry"（重试安全：Commit 幂等去重 + Put 幂等重传已覆盖——重推同内容 singleflight 命中、putNode 补齐台账+node）；**denied 分支维持 403**（重试无济于事）。孤儿物理 blob 归 GC grace（设计内）。godoc 重写含完整失败语义推理（为什么 201 是缺陷：无 node → 读 404 / T-39 校验 400 → 24h 后物理回收，客户端无任何重试信号）。
- **测试**：`TestRegistrationFailureIs5xx`（注入 putErr → 断言 **500 + code UNKNOWN** + 无 node 时 GET 404 → 解除注入重推同内容 → **201 且 GET 200 逐位一致**，即「5xx 给信号、重试即痊愈」全链）；`TestRegistrationDeniedIs403`（denied 注入 → **403 DENIED** 非 5xx）。

### 顺手项（non-blocking，一并落地）

- **N3**：`writeBlobCreated` 的 isDenied 分支不再静默——denied 与失败两分支同记 ERROR（`"denied", isDenied(err)` 字段区分）。
- **non-blocking #1**：毒化 Append 的 `r.Context().Err() != nil` 早退不再裸 return（裸 return 让 Go 栈对仍连着的客户端隐式写 200 空体）——改显式 **416 + 权威 Range**（与任何后续 PATCH 的重启线索同形）。
- **N4**：`serveUploadStart` 注释改真话（initiating POST 的 body 不读、连接随 202 关闭——原注释谎称 drain）。
- **N5**：`started` 死字段转驱逐锚（见 B2）。
- **N6**：日志勘误——`docker_blob_test.go` 顶层测试实为 **16**（原日志误写 15）。
- 架构面按 coordinator 裁定未动（WithStorage 缝终判不越线；N1 §5.1 勘误、N2 `PutLandedBlob` 债务归 conductor 转 architect）。

### 修复轮自测（全部实际执行）

```
$ go build ./... + CGO_ENABLED=0 go build -trimpath ./cmd/binflow-server  → 退出码 0
$ go vet ./... → 零输出；gofmt -l cmd internal → 空
$ golangci-lint run ./... → 0 issues
$ go test -count=1 ./... → 12 包全 ok（docker 17.2s / httpapi 38.4s / repo 33.8s）
$ go test -race -count=1 ./internal/adapter/docker/ ./internal/httpapi/ ./internal/storage/
  → 三包全 ok（13.8s / 40.3s / 49.1s）——B1 两测试在 -race 下绿
```

- 修复轮新增 7 测试全 PASS：`TestConcurrentSameUUIDPatchSerialized` / `TestConcurrentMixedVerbsSerialize` / `TestIdleSessionEviction` / `TestMountProbeClosesReader` / `TestRegistrationFailureIs5xx` / `TestRegistrationDeniedIs403` / `TestRealStackMountNoFdAccumulation`。
- **curl 矩阵全量复跑**（修复后二进制，干净数据目录）：**38 断言全 PASS / 0 FAIL（exit 0）**——D06/D07/D10/D10b/D12/D13/D13d/空层/AC7 去重零回归。
- 真栈补验：并发同 UUID 探针 `202 416×5`（B1 行为）；未终结 grant 的目录治理归属（B2）；50-mount fd 计数不增（B3，测试内完成）。

### 现场清理

真栈进程与全部自建临时产物（/tmp/binflow-t38、/tmp/t38*、blob/part 临时文件）已清；`/tmp/t38review/`（reviewer 探针副本，19:01 时间戳非本 agent 产物）与 `/tmp/binflow-t37`（T-37 遗留）保留未动。

---

## QA 修复轮（T-43 缺陷 D1 + D2 验证，2026-08-19）

输入：T-43-qa.md 缺陷清单 D1/D2、conductor 修复单。

### D1 [P2]：非 admin 的 cross-repo mount 永降级 202 → 已修（一行 + 双臂回归）

- **根因确认**（QA 定位无误）：`canMountFrom` 把 scope 字符串 `scopeActionPull`（`"pull"`）直传 `Authorizer.Can`，Can 的 action 域是 `"r"/"w"/"d"`（auth/api.go:22）——`rowAllows` 对未知 action `default:false` → 非 admin（无 `p.Admin` 短路）恒 false → tryMount 静默降级 202；admin 被 Can 的 Admin 短路掩盖，故 T-38 测试全 admin 跑法从未暴露。
- **修法**（blob.go:349，QA 建议原文）：`h.authz.Can(ctx, p, repoKey, image, canActions[scopeActionPull][0])`——与 handler.go 路由门**同一张映射表**，两处读提问从此字面同源；godoc 记录 D1 成因与「为何 admin 掩盖了它」。
- **回归测试** `TestNonAdminMountSucceeds`（review_fixes_test.go，双臂）：
  - *unit fake*：mover（非 admin，team1+team2 读）mount → **201** 且目的地 GET 200 逐位一致；同负向 blind（仅 team2 读）仍降级 202（fail-closed 方向不因修复改变）。
  - *real authorizer*：真 metadata 权限表（PutTarget `app/**,copy/**`，mover read+write 非 admin）——**这是 QA 复现面的精确回归**。
- **红→绿证明**：stash 掉修复重跑——real 臂 FAIL（`non-admin mount status = 202, want 201`，即 QA 现象）；恢复修复全绿。（unit fake 臂在旧代码也过：`allowListAuthorizer` 不看 action 域——这正是 T-38 当时测不出的原因，真栈臂才是有效回归。）

### D2 [P2]：tags/list 探测 ERROR 日志——T-52 **未**使其消失，本轮降级修复

QA 复现命令实测（裸进程真栈 18239，ci-bot 授 `ci-out/**` 前缀读，无 repo 根读）三形态：

| 形态 | 结果 | ERROR 日志 |
|---|---|---|
| 未知镜像 + 前缀级授权（QA 原始形态） | 404 NAME_UNKNOWN（正确） | **仍有**：`"level":"ERROR","msg":"docker: tagless-image probe failed","error":"read docker-local: permission denied"`（与 QA 逐字同形） |
| 存在但零 tag 的镜像（T-52 的修复触发面） | 200 `"tags":null` | 无（ListTags 返回空片，休眠分支未触发——T-52 该半面生效） |
| admin 查未知镜像 | 404 | 无（探测成功） |

- **结论**：T-52 消掉了「零 tag 存在镜像」触发面，但 QA 的原始形态（未知镜像 + 前缀级授权）仍打 ERROR——休眠分支对真未知镜像照跑 `imageListed` → `ListImages` 要 repo**根**读（service.go:964 `allow(..., "", r)`，错误文本 `read <repo>: permission denied` 即 QA 日志出处）→ 受限 principal 的预期非故障答案被记成 ERROR。
- **修复**（catalog.go `imageListed`，最小面）：日志 **ERROR→Debug**（消息改 `tagless-image probe unavailable`），注释引用 D2 与 O1 的「ERROR=真故障」信号语义；**保留** T-40 owner 在 T-52 commit 中明确选择留下的休眠防御分支（删分支是 T-40 的裁量，不顺手越权）。调用方行为零变化（探测不可用即 false → 渲染服务自身错误）。
- **修复后实测**：同复现 → 404 NAME_UNKNOWN 不变、**整份服务日志 0 条 ERROR**（`grep -c ERROR` = 0）；零 tag 形态 200 `tags:null` 不变。

### 顺手加固：TestIdleSessionEviction 去墙钟依赖

组合 -race 负载下出现过一次未留输出的 FAIL；排查锁定本测试的 80ms sleep vs 50ms TTL 竞态（断言间调度停顿即翻转 fresh-session 臂）。改为**确定性注入时钟**（`evictIdle(t0+10ms)` 界内存活 / `evictIdle(t0+200ms)` 界外驱逐，t0 于建会话前取样，零 sleep）。`-count=5` 稳定绿。

### 本轮自测（全部实际执行）

```
$ go test -count=1 ./... → 12 包全 ok
$ go test -race -count=1 ./internal/adapter/docker/ ./internal/httpapi/ ./internal/storage/
  → 三包全 ok（43.3s / 112.1s / 143.5s；docker 另 -count=2 连跑两轮均绿）
$ gofmt -l cmd internal → 空；go vet ./... → 零输出
$ golangci-lint run ./... → 0 issues
$ CGO_ENABLED=0 go build -trimpath ./cmd/binflow-server → 退出码 0
```

真栈复现栈与 /tmp 产物已清（进程 kill、目录删除）。

### 交接备注

- D2 的「删休眠分支」终局处置归 T-40 owner（其 T-52 commit 注释明示保留意图）；本轮降级已消除 QA 所指的 ERROR 污染面，若后续决定删分支，`TestV2TagsList…` 系列需同步（catalog.go 分支注释即线索）。
- QA D3（token 错误体互斥）与 F1（并发 flake）非本票 area，未动。
