# T-83 工作日志 — T-66/T-67 review 裁决落地

- 角色: architect
- 日期: 2026-08-20
- 输入: reports/agents/T-66-review-correctness.md（§决策点裁决 4/5 + 跨 area 申报）、T-66-review-arch.md（§5 措辞 3/4 + blocking-1）、T-67.md（遗留①=SPI 缝请求）、T-71.md（遗留②维持原建议）
- 状态: done（一致性自检通过：`Warning: 111`/「无副本 → 502」/独立 `local-first` 表述零残留）

## 落点清单（architecture.md 七处编辑；DECISIONS.md 无需动——均为既有裁定的文档同步，无新决策）

### 1. §4.5 两处（T-66 裁决 4 + T-79 勘误一欠账）

- **original checksum「登记不拒」**：按 review 草案措辞回写——M3 只 WARN 登记、不拒收（PRD 四值策略 M4 优先于架构草案「强校验不符即弃」）；带两条 M4 落地注记：repo-semantics §7.5 默认 `generate-if-absent` 是拒收语义（四值策略票必须实现拒收分支，WARN 分支即挂接点）；M3 WARN 未入库 original 值，M4 登记无历史数据（可接受，M4 票补）。
- **故障降级旧口径替换**：`Warning: 111`/「无副本 → 502」→ T-79 勘误一定案口径（assumed-offline 300s 静默 + 含过期服务缓存附 `X-Binflow-Upstream-Error` + 无缓存 404 + hardFail 502 + 负缓存 expired-but-serving）。

### 2. §5.4 两缝补记（T-66 review §5 措辞 3）

新增「服务层渲染缝」段：`repo.StatusError`（Code/Message/Header + Unwrap 哨兵，adapter 原样渲染——不改签名、不在 httpapi 拦截）+ `ExtraHeaders() http.Header`（reader 结构化探测，X-BinFlow-Cache / X-Binflow-Upstream-Error / X-BinFlow-Resolved-From 同缝）；标注 T-67/69/70/71/72 复用勿另开缝；附两条执行注记——`adapter.CopyExtraHeaders` 基座助手建议（防四协议复制漂移）与 httpapi `writeServiceError/writeStorageError` 缺 StatusError 分支的归票（T-80 或小票）。

### 3. §5.1 RepoTypes 修订（T-48 勘误的执行注记）

Handler 注释更新：generic `{"local","remote"}`（T-66 blocking-1 代码修正配套）、virtual 待 T-71；docker `{"local"}`、maven/npm/pypi M3 按实际服务面声明；写入「声明与行为必须一致，否则元数据说谎」原则。

### 4. SPI 豁免入口最终契约（T-67 遗留②，T-68 已授权方向）

定稿措辞（对 review 建议的 `PutOptions{SkipOverwriteCheck}` 做**收窄调整**）：
- 落点：`PutOpts(ctx, p, repoKey, path, body, expect, mime, opts PutOptions)` 变体方法——**既有 `Put` 冻结签名不变**（等价零值 opts），比「三个 Put\* 都加参」更保守；
- 语义收窄：只跳过「覆盖写需对旧 node 有 delete 权限」检查；读门、路径校验、checksum 链、同字节幂等豁免全部不变；
- 调用方限定：**仅服务端自有写入**（T-68 的 maven-metadata 计算器 + 旁车落盘，repo-semantics §3 高置信度）；godoc 明示「client puts must not set」；
- 安全边界：写门（w 权限）不豁免——服务端计算 metadata 是主制品写操作的伴生产物，可写性由触发写权限担保。

### 顺带：T-79 遗留的 architecture.md 同步债收口（三处）

§5.4 接线段改两桶序后 §6/§10/§12 三处残留 local-first 形成新内部矛盾，一并修正：§6 003 注释（position=桶内声明序）、§10 对齐表行（两桶序 + stale/写路由语义）、§12 #8（priorityResolution 项划掉已定案部分，余影子仓/合并算法挂 §7.3）。

## 遗留问题

1. httpapi `writeServiceError`/`writeStorageError` 的 StatusError 分支（T-66 review 范围外缺口）归 T-80 或小票——文档已注明，待 conductor 排期。
2. `PutOpts` 落地属 dev-go-core 面的实现票（约 10 行 + godoc），T-68 消费方开工前须在——建议随 T-68 派单指令附本日志第 4 节契约。
