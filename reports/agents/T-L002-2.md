Ticket:       L002-2 —— 契约 21 条目全量重放 + D21 闭账 + 金样活体重采（LOOP 002）
Role:         differential-qa-engineer
Area:         reports/compatibility/ + docs/compatibility/golden/docker-remote/（金样=本轨道资产）+ known-divergence 回填（票面明示）
Input:        contracts/docker-remote.yaml 21 条目 + fixtures/normalize.yaml#docker-remote + 模式 dual；UAT :8083（uat-l0011-f80c46aa → 因 D21 修复不在该容器内，按 L000-F/L001-1 先例同卷重建为 uat-l0022-5f9c48d0）；参照 :8082 Artifactory-pro 7.161.20；上游 registry:3（自建 difftest-upstream :5588）
Changes:      无产品码改动。产出：契约重放报告（含 D21 双面闭账、分页第一手取证、上报项 3 条）；金样 2 件 derived-from-report → live-captured（metadata+response 重写，request 未动）；known-divergence 两处回填（D21 复验证据、pagination 第一手证据——票面明示授权）；`.env.uat` BINFLOW_VER/REVISION 随重建指向 uat-l0022-5f9c48d0
Files:        reports/compatibility/L002-contract-replay.md（新）；docs/compatibility/golden/docker-remote/{empty-blob-synthetic,pull-sequence}/{metadata,response}.yaml（重写）；docs/compatibility/known-divergence.yaml（两处回填）；deploy/compose/.env.uat（VER 两行）
Tests:        21/21 条目零漏跑：SAME 12 / DIVERGENT 8 / in-flight 1（C10 归 L002-1）。新增显性 DIVERGENT 3（#1 ping Content-Type charset、#2 ping 坏凭据臂 Basic-realm+pretty 形态、#11 blob GET 缺 Last-Modified+Origin-Remote-Path）；维持待裁 3（#3/#5/#16）；C06 族维持 2（#6/#7）；#16 运行时翻转趋同（双端均无负缓存，miss 3/3 上游往返，归因疑 L002-1 WIP）
Commands:     双端仓创建（PUT /api/repositories 同构 remote/docker，上游 host.docker.internal:5588）；dind 真客户端 `docker pull host.docker.internal:808{2,3}/audit-probe-docker-remote/{hello-world:latest,busybox:t3}`（exit 0；8082 腿 digest c766679d…645c0a、8083 腿 92b1d1ca…f46e3 均与上游逐字）；curl 全端点族（ping/token ×5、manifest GET/HEAD/digest/INM、blob GET/Range/empty/unknown、404 族、tags/catalog/pagination n/last/abc、push 三端点）；`docker logs difftest-upstream` 为上游 RTT 唯一判据（本地上游 timing 不可分）
Outputs:      reports/compatibility/L002-contract-replay.md；金样 live-captured ×2（confidence medium→high，含 digest 全值与 32B raw hex）；known-divergence D21 闭账回填 + pagination 实锤回填；提金候选：无（本轮即完成重采，两件已落 live-captured）
Compatibility: 差异分类建议：BUG 0 新立（#11 blob 头集建议并入既有 docker/remote-manifest-headers 实现票族）；UNKNOWN 待裁新增 2（#1 charset 归一提案、#2 ping 臂改锚/对齐——均提 compatibility-engineer）；INTENTIONAL 候选 1（#16 若 L002-1 落定后双端维持"无负缓存"可按 resolved(parity) 先例闭环）。回归对照：fixed 0 / 仍在 5（#3/#5/#6/#7/#16）/ 新增显性 3 / 转在途 1（#14）
Security:     auth 面 5 条目双端对照完结：ping-anon service 回显双侧一致（#1 charset 为非安全面小分歧）；坏凭据 token 端点逐字一致；匿名策略差异（#5）维持产品待裁——凭据全脱敏，dind 登录凭据随容器销毁
Performance:  上游本地上游腿 RTT 亚毫秒，timing 断言一律以对拍上游日志判定（0 命中=无往返）：empty blob 双端 0 往返、unknown digest 双端 0 往返、miss 双端 3/3 往返；无超时 case
Risks:        ① 共享宿主并发污染——L002-1 同场探测（上游见 curl UA 请求与 l0021-probe 镜像），_catalog 以背靠背同刻取样对冲，其余判定全用唯一路径/digest 归因；② 种子字节差——本轮 docker save 注种与 L001-1 轮不同（docker vs OCI manifest media type），BinFlow 卷内旧缓存 HIT-stale 属检索窗语义非分歧，但意味着"tag 缓存不回源"行为仅观察未对拍 Artifactory 同窗行为；③ #11 无法区分前轮漏判 vs 本轮回归（f80c46aa 轮无该两头在档观察）——已按回归上报；④ UAT 容器含 L002-1 未提交 WIP，#14/#16 观察值随其落定可能再变
Blockers:     无（参照实例中途自重启一次 ~8min，等恢复后完成 Artifactory 腿）
Next:         待 compatibility-engineer：#1 charset normalize 提案或对齐、#2 条目改锚 token 臂或立票、#7 契约对参照 HEAD 头集高估复核、#16 翻态（待 L002-1 落定 + authority）、pagination 补契约条目；待 conductor：#11 归并实现票、L002-1 WIP 落定后契约 C10/C14 条目账面刷新
