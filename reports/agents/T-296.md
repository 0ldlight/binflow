# T-296 — 文档五项：M10 用户文档面收口

状态: done（五项产出 + FAQ 三目 + cargo.md R-1 修订 + 侧栏挂页 + docs-site 构建绿；未 git 提交——按规约主会话统一提交）
角色: tech-writer ｜ 日期: 2026-08-26（两次中断恢复：API 配额 + 网络瞬断，均经 `git status` 核实后从断点续作，最终产物完整）

## 1. 五项产出

| # | 文件 | 行数 | 内容 |
|---|---|---|---|
| 1 | `docs/user/admin/license.md`（新） | 232 | 三档语义与 D1~D7 降级闭集；安装（控制台粘贴 + REST——as-built 为 **POST**，见 §4-1）；GET 状态字段表；DELETE 卸载（控制台 typed UNINSTALL）；**11 槽位矩阵**（`internal/addons/slots.go` 单源：五核心 community 地板 + properties〔community，Artifactory 归 pro 的有意分歧〕+ go/nuget/cargo〔pro〕+ ha/xray-integration〔enterprise 占位〕）；`addons.disabled` 熔断（YAML CSV + `BINFLOW_ADDONS__DISABLED`、重启生效、写平面断路器非数据墓碑、无 `X-Binflow-License-Required` 头）；`bf license keygen/issue/inspect` 用法 + 首发换钥三步（引 ADR-0032，不含任何钥料）；审计四事件；速查表 |
| 2 | `docs/user/properties.md`（新） | 158 | 值域闭集（键 `[A-Za-z][A-Za-z0-9_.-]{0,63}`、值 ≤1KiB 非空无控制位、单键 ≤32 值、节点 ≤64 键）；矩阵参数（成对 `;k=v` 整段剥离 / 非成对字面保留、同键累积多值、尾斜杠 folder）；`?properties` 三动词（GET 过滤+尾 `*` 通配+atomic 门禁+空集 200；PUT 合并律+逗号文法+%2C 存活+recursive；DELETE 幂等+`*` 全删+前缀通配）；控制台 Properties 页签操作；CI 打标场景（PRD 场景 E 四步，Bearer token 认证形态） |
| 3 | `docs/user/integrations/golang.md`（新） | 171 | GOPROXY 地址形态（无额外 goproxy 段）；建仓（含 remote/virtual）；curl PUT 三件套 + 校验头；**环境变量矩阵**（GOPROXY/GOSUMDB/GONOSUMDB/GOPRIVATE/GOINSECURE）；**GOPRIVATE='*' 高频卡点**（T-285 §2.1 go1.26.6 实测：会使模块绕过 GOPROXY 走 VCS；正确形态 = GOPROXY+GOSUMDB=off）；`!lower` 转义；go 1.26 构建 roundtrip + remote 回源计数=1 + virtual 聚合；校验和链（h1 客户端域）；报错对照（D2/D3 门控、400 版本文法/major 一致性/.info/.mod 内容、409 checksum、405 remote PUT）；M10 不做（sumdb 代理等） |
| 4 | `docs/user/integrations/nuget.md`（新） | 160 | v3 index / v2 URL 形态；**nuget.config 源映射**（`<clear/>` 收口 + packageSourceCredentials Basic）；dotnet 8 push（multipart 单 part、直推 publish base、sidecar 服务端生成、512MiB）→ add package（无版本走 search / 带版本走 flatcontainer）→ restore → run 全链；remote 代理（上游接触恰 1 次断言 + Newtonsoft.Json 13.0.2 LIVE 腿）；flatcontainer/归一化/search 存储事实/DELETE 硬删 + relist 405 速查；v2 最小面；**符号服务器边界（M11+）**；报错对照 |
| 5 | `docs/user/integrations/cargo.md`（新） | 168 | `.cargo/config.toml` alternate registry（sparse+ 前缀）；token 三形态（login / `CARGO_REGISTRIES_BINFLOW_TOKEN` / 凭据文件）+ **裸 `Authorization: <token>` wire 形态**（T-294 探针实证，BinFlow 认证面 M10 起接受）；索引路径四档；publish（409 重复版本 CG-3）→ add/build → yank/`yank --undo`（1.98 无 unyank 子命令）→ search 全链；**cksum 三方对账**（索引行 = 存储实测 = 下载体，附 shasum 手工对账命令）；边界（local-only M11、git 索引/owners 不做、`index/**`/`.cargo/**` bare PUT 403）；报错对照 |

客户端版本标注：go 1.26（1.26.6 实测）/ dotnet SDK 8（8.0.412 实测）/ cargo 1.98（1.98.0 实测）——全部取自 T-285/T-287/T-294 日志的实测命令与输出。

## 2. 其余改动

- `docs/user/faq.md`：头部适用版本补 M10 句；新增「**M10 增补三问**」——① 无 license 时 pro 功能的行为（community 地板 / D3 400 / D2 403+头 / D1 读恒 200 / 到期即降级 / 与 addons.disabled 熔断的区分）；② 属性与矩阵参数的区别（同存储两入口 + 分工）；③ S3 与 filestore 的 MPU 差异（S3 全族中继 / filestore 恒 501 非 404 / 会话进程态重启 404 / 普通上传两后端无差异 / 与 docker /v2 面区分）。
- `docs/reverse/cargo.md`：**R-1 点状修订 ×4**——§2 publish 行成功体去 `"errors":[]` 并注明「errors 键存在即判失败（即使空数组），cargo 1.98 真机实证，T-294 D-1」；§2 信封行补实测收窄；§5.2-6 改为 CG-2 as-built（4xx/5xx+信封、禁 200+errors，D-2 拆 400/500）；§12 裁决三点中 ②③ 标已裁（CG-2/CG-3），① 留 remote/virtual 票。
- `docs-site/sidebars.js`：客户端接入 +`integrations/{golang,nuget,cargo}`；管理指南 +`admin/license`；顶层 +`properties`（faq 前）。
- `docs/user/README.md`：导航同步（三接入篇 + 属性系统 + license 篇 + FAQ 行 M10 注）。
- `docs/user/api-reference.md`：头部 M10 注收口（「归 T-296 补齐」→「已随 T-296 补齐」）+ 新增「**M10 新增端点速览（T-296）**」节（license ×3 + 复数 404 / addons ×1 / uploads ×7 + S3-only 501 说明 / go·nuget·cargo 三接入面挂载表）+「下一步」链接补全。——此节为 T-293 在 api-reference 头注点名欠 T-296 的债，虽在五项清单外仍属本票 docs/user/** 名下，一并补齐。
- `docs-site/build/**` 与 `docs-site/.docusaurus/**`：随构建再生成（非手写产物，未逐文件清点）。

## 3. 验证

| 命令 | 结果 |
|---|---|
| `cd docs-site && npm run build` | **SUCCESS**（Server compiled 2.59s / Client 6.07s / Generated static files）；**零 broken-link / broken-markdown-link 告警** |
| 首轮构建 | 失败：license.md 行 44 「（<档位> 档）」被 MDX 解析为 JSX 标签 → 改反引号代码_span_后复绿（如实记录） |
| 产物核对 | `build/admin/license/index.html`、`build/properties/index.html`、`build/integrations/{golang,nuget,cargo}/index.html` 全部生成 |
| 事实核对源 | license/addons/门控：`internal/httpapi/{license,addons,license_gate}.go`、`internal/license/tier.go`、`internal/addons/{slots,view}.go`、`internal/config/api.go`、`cmd/bf/license.go`、web `LicenseAddonsPage.tsx`；属性：`internal/httpapi/properties.go`、`internal/adapter/layout.go`（SplitMatrixParams/ParseMatrixProps）、`internal/metadata/props.go`、web `PropertiesTab.tsx`；三包型：T-285/T-287/T-294 日志实测命令 + `docs/reverse/{goproxy,cargo}.md`；MPU：`internal/httpapi/uploads.go` 头注 + T-293 as-built |

## 4. 派单口径修正与遗留（待确认）

1. **装 license 的 REST 动词**：派单写「REST PUT」——as-built 为 **POST** `/api/system/license`（ADR-0032 决策 4；router.go 实注册；无 PUT 路由）。文档按 as-built 写 POST 并明示「无 PUT 动词」。
2. cargo.md（用户篇）索引示例按官方四档规则推导 `index/my/cr/mycrate`；`docs/reverse/cargo.md` §9 示例 `index/3/m/mycrate` 与四档规则不符（3 档仅适用 3 字符名）——非 R-1 范围未动规格，此处登记供 reverse-engineer 复核。
3. nuget.md「CI 上 ClearTextPassword 环境变量扩展（`value="%NUGET_PW%"` 视 CI 而定）」为通用建议措辞，未经 BinFlow 链路实测——按纪律标注为建议性表述。
4. `docs/reverse/goproxy.md` §7.1 的 GOPRIVATE='*' 勘误是 T-285 登记给 reverse-engineer 的（非本票 area，未动）；用户文档侧已按正确形态书写。
5. 未跑 `make docs`（其含 `npm ci` + 拷贝进 `internal/docs/dist`——会触碰 internal/ 面与 node_modules 重装；本票约束不碰 internal/，故验证止于 `npm run build`，与 CD 链同目标）。
6. 未 git 提交；BOARD.md 未写（主会话唯一写入者）。**提交时注意 add 六个 untracked/新文件**：`docs/user/admin/license.md`、`docs/user/properties.md`、`docs/user/integrations/{golang,nuget,cargo}.md`、本日志（防 T-288/T-294 式漏 add）。
