# T-375 文档增量（webhook 指南 + HelmOCI remote/virtual + 旋钮 + api-reference + FAQ）— 工作日志

- 票：T-375 [P1] M13 文档波（BOARD L1247；dep T-362~T-368 已合入）
- 角色：tech-writer；area：docs/user/ + docs-site/sidebars.js + 双语 README
- 日期：2026-08-30/31；零 git 操作（conductor 收口）
- 状态：done（AC1/AC2 全绿；`make docs` SUCCESS 零断链）

## 1. 变更清单

| 文件 | 性质 | 内容 |
|---|---|---|
| docs/user/admin/webhooks.md | **新** | Webhook 使用指南：9 型触发表 / wire schema 与 criteria / 七端点 curl / 七字段信封 / HMAC 双态 + openssl 验签 + **Python 接收端验签示例（实测）** + 接收端幂等义务 / 投递语义表（5 首试计入·10s 固定·30s 预算·4xx 终态·限流 1000+10000·并发 50000）/ 死信与排障环 / metrics 五族 / SSRF 边界与 `webhook.allow_private_target` / 常见报错对照 |
| docs/user/integrations/helm-charts.md | 改 | 标题与辨析更新（+HelmOCI）；chartsBaseUrl 专节（M13）；virtual 增补 `_external`/`_transitive` 落盘缓存与识别基址；**新增「HelmOCI 仓型」节**（remote：url 含 /v2 约定/认证链/降级/tags 只见缓存；virtual：首见路由/并集/写拒绝 405/成员同型校验）；边界表翻转 chartsBaseUrl 行 + 新增 helmoci 行；报错表 +3 行 |
| docs/user/integrations/conan.md | 改 | **断言反转两处**（L70 命令注释 + L150 差异表行：「删 latest 修订链」→整树删，T-369）；§5 删除节重写（实测证据）；**布局对齐一处标注**（D-F2：v1 files 通道规格布局 + M12 升级启动 sweep 幂等迁移/失败拒启） |
| docs/user/admin/trash-can.md | 改 | 旋钮行翻转（`trashcan.retention_days` 落地；`trashcan.enabled` 维持非配置键——strict schema 拒绝）；保留期节 + YAML/env/哨兵 0/重启生效/回显核对 |
| docs/user/admin/artifact-operations.md | 改 | 「已知边界」块翻转 → folder_download 六字段旋钮说明（YAML 块 + env 形态 + 实测语义要点：403/401 逐字文案、camelCase 拒绝）；报错表行更新 |
| docs/user/api-reference.md | 改 | 头部 M13 增补速览链接；API 面清单 + 事件面（第 4 面）；**M13 增补速览新节**（webhook 七端点族 + system/settings + chartsBaseUrl 字段表）；/api/v1 自有端点表 + settings 行；archive/download 行旋钮措辞翻转；trash 域保留期措辞更新；下一步 +webhooks |
| docs/user/faq.md | 改 | **M13 增补三问**（webhook 事件丢失排查七步 / 死信重放——如实登记无 REST 面 / remote 缓存命中观测四件套）；trash 保留期答案与 folderDownload 两处措辞翻转 |
| docs/user/console.md | 改 | 导航树治理组 +Webhooks/**+回收站（M12 漂移顺手修）**；治理节 +Webhooks/+回收站 bullet；边界表 Webhooks 行移出「不建」、Move/Copy·TrashCan 行按 M12 现状改写、树内「无回收站」表述修正 |
| docs/user/admin/remote-virtual.md | 改 | remote 字段表 +chartsBaseUrl 行；packageType 段 +helmoci remote/virtual（M13） |
| docs/user/admin/license.md | 改 | 槽位矩阵 18→19（+webhook 行；**顺手补 M12 漏登的 helmoci 行**）；三档语义与解锁面两处同步；enterprise 本体 M13+→M14+ |
| docs/user/admin/governance.md | 改 | 审计词表 +webhook 族五词 + 后续里程碑词族指引注 |
| docs/user/metrics/prometheus-reference.md | 改 | +webhook 五指标族节 + 告警示例两行（queue_depth/enqueue_failures） |
| docs/user/README.md | 改 | 导航：admin +webhooks 行；conan/helm/api/faq 行 M13 增量描述 |
| docs-site/sidebars.js | 改 | 管理指南分类挂 `admin/webhooks`（trash-can 之后） |
| README.md / README.zh-CN.md | 改 | **双语收口**：M12 段改完成态；新增 M13 段（webhook/HelmOCI/chartsBaseUrl/conan 整树删/旋钮，双语对译）；矩阵行 +webhook；里程碑链接行更新（+milestone-13.md）；**英文版 M11 句 helmoci 误植移除**（中文版本就正确——双语漂移点）；enterprise 槽位 M13+→M14+ |
| reports/agents/T-375.md | 新 | 本日志 |

## 2. 实测取证清单（L24：全部命令真跑）

环境：HEAD（`make build` 产物 bin/binflow-server，含 T-362~T-371 全部能力 + conductor 接线）；双实例 A:18411（默认配置）+ B:18412（`webhook.allow_private_target: true` + folder_download 自定义 + trashcan.retention_days=7，`BINFLOW_REMOTE_CREDENTIALS_KEY` 32B）；pro license = `bf license issue`（仓内开发钥）30 天双实例各装一份；接收器 = python 脚本（/ok /flaky /fail500 /notfound 分路径）；helm 4.2.4；conan 1.66.0/2.31.2 venv（复用 T-369 环境）。

### 2.1 webhook 域（B 实例）

| # | 命令腿 | 结果 |
|---|---|---|
| 1 | `GET /binflow/event/api/v1/subscriptions` | 200 `[]` |
| 2 | `POST …/subscriptions`（criteria anyLocal+include `**/*.jar`、secret+签名态） | **201**；回显 criteria 规范化全维度、`secret:"********"` |
| 3 | `GET …/subscriptions/{key}` / miss | 200 / 404 `Subscription not found` |
| 4 | `PUT …/subscriptions/{key}`（全量体） | **204 无体** |
| 5 | `POST …/subscriptions/test`（明文 secret） | 200 `{"message":"Test successful","ok":true,"attempt":{"status_code":200,"elapsed_millis":1}}`；接收端 `X-JFrog-Event-Auth: plain-secret`（**明文直传态实证**） |
| 6 | test → 404 接收器 | 200 + `ok:false` + `attempt.error:"receiver answered 404"`（4xx 终态预告） |
| 7 | **SSRF 默认拒**（A 实例，未开旋钮，test → 127.0.0.1） | `ok:false` + `target rejected: upstream target 127.0.0.1:18413 rejected: loopback address 127.0.0.1` |
| 8 | 触发：PUT 制品 → 投递 | 接收端收到**七字段信封**（domain/event_type/data{repo_key,path,name,sha256,size}/subscription_key/jpd_origin/source=`binflow/binflow@<ULID>`/userContext{admin,false,internal}） |
| 9 | **HMAC 验签** | `printf '%s' "$PAYLOAD" \| openssl dgst -sha256 -hmac "s3cr3t-hmac-key"` = `82248438b9b3054a…` 与线上 `X-JFrog-Event-Auth` **逐字一致** |
| 10 | **文档接收端代码段端到端**（python hmac.compare_digest 版，:18416） | 投递后输出 `VERIFIED artifact/deployed -> verify-demo` |
| 11 | 重试腿（/flaky：500,500→200） | 送达；到达时刻 t/t+10s（**固定 10s 间隔实证**）；环记录 retries_attempted 0 起 |
| 12 | **死信腿**（/fail500 恒 500，双事件） | **每事件恰 5 次尝试**（环 10 条记录，retries 0..4；到达 t/t+10/t+20/t+30/t+40）；审计 `webhook.dead_letter` ×2（actor=webhook；detail 含 attempts:5/error/status_code/subscription/url） |
| 13 | `GET …/troubleshooting?subscription=` | 按订阅过滤命中；失败必录、成功不录（debug=false）/ debug=true 订阅成功也录（#8 腿同订阅） |
| 14 | `DELETE …/subscriptions/{key}` / 再删 | 204 / 404 |
| 15 | `/metrics` | 五族齐：`binflow_webhook_{deliveries,retries,dead_letter,enqueue_failures}_total` + `queue_depth`（dead_letter=2 与 #12 对账） |
| 16 | **community 门**（A 卸载 license 后 POST 订阅） | **403 + `X-Binflow-License-Required: webhook`**；读面 200（D1）；重装 201 |

### 2.2 旋钮域（T-368）

| # | 腿 | 结果 |
|---|---|---|
| 17 | `GET /api/v1/system/settings`（B：自定义 512/1000/7d） | 回显与配置逐字段一致 |
| 18 | 同（A：缺省） | `enabled:false` + **1024/5000/10** + `retention_days:14`（规格默认实证） |
| 19 | env 形态 + 重启生效（A 以 `BINFLOW_FOLDER_DOWNLOAD__ENABLED=true BINFLOW_TRASHCAN__RETENTION_DAYS=3` 重启） | 回显 enabled:true / retention_days:3 |
| 20 | 目录打包：B 开 → A 缺省 | B 200 + zip 条目与树一致；A **403 `Download Folder functionality is disabled.`**（逐字） |
| 21 | 匿名腿（B enabled_for_anonymous=false） | **401 `You must be logged in to download a folder or repository.`**（逐字） |
| 22 | strict schema：camelCase `maxDownloadSizeMb` / `trashcan.enabled` | **拒启**（yaml unmarshal `field … not found`，两形态原文取证） |

### 2.3 HelmOCI / chartsBaseUrl / _external（helm 4.2.4）

| # | 腿 | 结果 |
|---|---|---|
| 23 | A 建 helmoci local + `helm push` | Pushed；Digest `sha256:ff9c0f45…` |
| 24 | B 建 helmoci remote（url=A `/v2/up-local`）+ `helm pull` 经 remote | Digest 与 push 一致；tgz sha256 与本地打包逐字节一致 |
| 25 | 二次 pull + curl manifest | `X-Binflow-Cache: HIT`；**上游内容 GET 计数冻结**（A access 日志：4 个内容 GET 各恰 1 次） |
| 26 | B 建 local+remote 成员 virtual `hv`；双域 pull | remote 域 Digest 一致 + `X-Binflow-Cache: HIT` + `X-BinFlow-Resolved-From: r1`；local 域 Digest 一致 + `Resolved-From: virt-local`（无 Cache 头） |
| 27 | tags/list 经 hv | `{"name":"hv/t375chart","tags":["0.1.0"]}`（并集）；`_catalog` 含 hv/ 前缀行 |
| 28 | `helm push` → hv | **405** + `No local repository was configured as local deployment repository for the (hv) virtual repository.`（逐字） |
| 29 | helmoci virtual 配 docker 成员 | **400** `virtual repository cannot mix the helmoci and docker package types …; member "dock-local" is a docker repository`（逐字） |
| 30 | chartsBaseUrl：REST 配置/回显 | PUT 200；GET 回显 `"chartsBaseUrl": "http://127.0.0.1:18414"` |
| 31 | per-protocol 门 | maven remote 携带 → **400 点名字段**；`ftp://` → **400 scheme must be http or https**（两文案原文取证） |
| 32 | 异构基址回源 | `helm repo add/update`（index 走 A）+ `helm pull` → tgz sha256 与打包一致；**A 的 tgz GET 计数 = 0**（实体全走 charts base） |
| 33 | 二次拉取 | `X-Binflow-Cache: HIT` + `X-Checksum-Sha256` 正确；文件服务计数冻结 |
| 34 | `_external` 折叠与落盘 | 手制 index（条目 URL 指向仓 URL 之外）→ 虚仓 index 改写为 `_external/http/127.0.0.1:18414/deps/depchart-0.1.0.tgz`；GET 折叠路径 200 MISS + `X-Binflow-Upstream` + `X-BinFlow-Resolved-From: dep2`；二拉 HIT 且上游计数冻结；storage REST `GET /api/storage/dep2/_external/http/…` 200（落盘可见） |

（注：#32 首跑踩 assumed-offline 窗——文件服务器随宿主 shell 超时被杀，回源 3 连败触发 300s 静默窗；以新 repo key + 独立 detach 的文件服务重跑通过。文档 FAQ「remote 缓存命中」问的 STALE/负缓存口径即取此实测链。）

### 2.4 conan（venv 真客户端）

| # | 腿 | 结果 |
|---|---|---|
| 35 | conan 1.66 upload（坐标 hello/1.0@myuser/stable，修订 `0`） | Uploaded；服务端 revisions 见 `0` |
| 36 | conan 2.31.2 同坐标再 upload（内容变更） | 新修订 `6a757039…`；`conan list '<ref>#*'` 见**双修订** |
| 37 | **conan 1.66 `remove -r -f`（D8 整树删）** | v2 revisions 404（`Couldn't find revisions`）、修订 `0` 的 files 404、`conan list` 报 miss |
| 38 | 再删 | 404（`Path not found` miss 文案，conan 1 客户端呈现 `ERROR: 404 Not found`） |

### 2.5 文档站

| 命令 | 结果 |
|---|---|
| `make docs` | **SUCCESS**（`Generated static files in "build"`）+ 嵌入 internal/docs/dist（4.44MB） |
| 全量 build 日志 grep `broken` | **0 命中**（onBrokenLinks=warn 下零告警 = 零断链，含锚点） |
| build/admin/webhooks/index.html | 在场（sidebars 挂载生效；正文含 X-JFrog-Event-Auth ×4） |

### 2.6 README 双语收口核查（AC2，结论）

- **双语对齐**：README.md 与 README.zh-CN.md 的矩阵/里程碑/段落结构逐段对译一致；发现并修复**一处双语漂移**——英文版 M11 句把 `helmoci` 误计入 M11 十二包型（中文版正确）；已改。
- **里程碑状态漂移**：两版均停在「M12（进行中）/M1~M11 done」——M12 已 m12-done；本次更新为完成态并新增 M13 段（双语对译）+ milestone-13.md 链接行；enterprise 槽位「本体 M13+」→ M14+（M13 未建 HA/Xray）。
- **档位速查**：license.md 槽位矩阵表漏登 M12 `helmoci` 行（速查行有、表格无）——顺手补齐；18→19 槽。

## 3. 顺手修复的既有漂移（票面外，最小面）

1. console.md：治理组缺**回收站**条目/页面 bullet（M12 漏登）；「无回收站——删除即永久」「跨路径 Move/Copy 不做（服务端无端点）」两处 M12 起即失真——按现状改写（REST 有 copy/move；trash 捕获 + 治理页 UI）。
2. license.md：槽位矩阵表 helmoci 行漏登（速查行与表格不一致）。
3. governance.md：审计词表止于 M4/M7——补 M13 webhook 族 + 「后续里程碑词族见各自指南」指引注（未整表重写，归档在各特性页）。
4. README 英文版 M11 句 helmoci 误植（§2.6）。

## 4. 遗留 / 待确认

1. **webhook 死信重放无 REST 面**（T-364 §5-3 登记）：文档如实标注「当前恢复 = 重新触发源事件」；Replay 机制挂 REST/控制台后 webhooks.md 该段与 FAQ 同步刷新。
2. **dormant 57 型**：REST 回显无 wired/dormant 标注字段（官方 wire 无此字段）；控制台表单以静态镜像标注（T-366）。文档以触发表 + 控制台口径呈现，无需动作。
3. **排障环进程内存储**（重启失史）：已在 webhooks.md/FAQ 如实标注；log/redis 持久化归后续票（T-362 §5-5）。
4. **pypi twine 上传/explode 不触发 artifact/deployed**（T-362 §5-2 已登记）：webhooks.md 触发表按 9 型 as-built 呈现，未单列该缺口——建议后续在触发源补齐时回刷。
5. **architecture.md §7.1/§15/§11.45**（system/settings 端点行 + 旋钮条目翻转）归 architect（T-368 遗留②）；Helm chart values.schema 旋钮键族归 release-engineer（T-368 遗留③）——本文档面已就绪。
6. **PRD §5.4 断言反转 QA 核实**（conan D8 腿）：本文以真客户端双修订整树删实测复证（§2.4 #35~38），供 QA 回写对账。
7. 测试环境已清理（双实例 kill、接收器/文件服务 kill、helm repo 清理本地条目、/tmp/t375 保留取证产物）。
