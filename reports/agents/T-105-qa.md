# QA 报告 T-105 — M1~M3 回归基线（全序列）+ 性能收尾（FR-33-AC2/AC3）

- role: qa-engineer
- 日期: 2026-08-21
- 票据: T-105 [P0] QA 回归基线 + 性能收尾（AC 全文见 reports/agents/T-88.md T-105 节；FR-33-AC2/AC3 + W36/W37 + NFR-P16~P18/P20）
- 被测基线: commit **62d92f1**（独立 git worktree `/tmp/t105/wt` 检出、`make console && make build` 真二进制；SPA gzip **151,255 B**、二进制 **19.57 MB**——与 T-104 记录逐字节一致）。**T-120（D-104-1 FE 修复）于本票执行中合入主仓（170cf38）**：回归面全部为后端，与该 FE 修复正交，按派单口径**不重跑**；跨引擎 overlay 见 §4.3
- 环境: darwin/**amd64**（Intel i9）；实例 A=`127.0.0.1:18140`（匿名默认开，主回归）/ B=`18141`（匿名关，C27）/ 性能实例 `18142`（空库冷启动 → 造数 → 并发/吞吐）；mock 上游 `127.0.0.1:19199`；全部 fresh data dir、唯一端口段 18140+
- 客户端: curl（主驱动）/ docker 27.5.1（dind 容器，`--insecure-registry host.docker.internal:18140`）/ podman 5.x + skopeo（quay 容器形态，T-44 先例）/ crane 0.20.3 / oras 1.2.2（`--plain-http`）/ mvn 3.9.9 + Temurin JDK 21.0.12（独立 settings + repo.local）/ npm 10.9.8（node 22，HOME 隔离）/ twine 7.0.0 + pip（隔离 venv）/ Playwright 1.62.1（chromium-1234 / webkit-2336 / firefox-1538）
- 断言账本: 全部命令与逐条 PASS/FAIL 落 `/tmp/t105/results/regression.log`（清理前已留档副本），本报告引用其结论

## 总结论: **PASS**

- **回归基线全绿**：M1 C 序列（C01~C30，子断言 63 条）+ M4 反转面（8 条）+ M2 D 序列（D01~D24，子断言 68 条）+ M3 M 序列（M01~M59，子断言 140 条）——**distinct PASS 279 条，FAIL 0 条**；2 项按基线 P2 延后记录 N/A（M55b 聚合浏览 / M59 RE-11 stats，行为与 T-75 基线一致，非回归）
- 首试 79 条 FAIL **全部为 QA 侧构造/口径问题**（详见 §2.修正账），逐条复验后全绿；**零产品缺陷**
- **性能全部门槛达标**：冷启动 120~151ms（<2s）、SPA 首屏 393~477ms（<3s）、NFR-P17a 搜索 P95 72ms（<1s）/ P17b 审计 P95 16ms（<500ms）、NFR-P18 children P95 56ms（<300ms）、100 并发 REST 混合 + 50 并发内容读写 + NFR-P2 100 并发 GET 全 0 退出零 5xx、1GB PUT 194MB/s（RSS 增量 52KB）
- 5xx 零容忍达成：三实例 + 内存对照实例合计 11 条 5xx **全部为有意负例或已定性 client_disconnect 类**（§5 归因表）；panic 0、ERROR 级 1 条（即 M44d 502 负例自身的访问日志）

---

## 1. AC 逐条结论（T-88 T-105 节）

| # | AC | 结果 | 证据摘要 |
|---|---|---|---|
| ① | M1 C 序列 P0 + M2 D 序列 P0 + M3 M 序列 P0 复跑全绿（断言按 §5.6 反转表更新：search 404→分派、`/binflow/` 占位→301、groups 404→分派、audit 404→200、repo key ui/docs/console 400）；docker/mvn/npm/pip 真实客户端各一条链零回归；C22/C27 权限语义复验（FR-27-AC10） | ✅ | §2.1~§2.4：C01~C30 全绿（C19 删仓按依赖重排至块尾，见修正账）；反转面 8/8（301+Location /binflow/ui/、search artifact 200 + props 404、groups 分派、audit 200、ui/docs/console 建仓全 400）；docker 全链（login/push/pull/rm、mount、chunked+中断恢复、多架构 buildx、catalog/tags 分页）+ mvn deploy/resolve/snapshot ×3 版本 + npm publish/install/dist-tag/scoped/unpublish + twine upload + pip install 哈希对账全绿；C22（ci-bot 路径 ACL 三态）+ C27（匿名关四态）复验全绿 |
| ② | 性能 W37：空库冷启动 <2s（含 embed）；SPA 首屏 <3s（P1 记录）；二进制 + SPA gzip 体积记录；100 并发 REST 混合退出码全 0 零 5xx；NFR-P17/P18 造数（10 万 node 搜索 P95<1s、10 万 audit actor+since P95<500ms、children 1000 P95<300ms） | ✅ | §3：150/151/120ms；477/398/393ms；19.57MB + 151,255B；xargs -P100 rc=0 零失败；72ms / 16ms / 56ms（造数 101,001 node + 100,002 audit + 1,000 children，SQLite 直插后 API 全可见） |
| ③ | M4 QA 总报告（DoD §9 第 1/2 条结论：P0/P1 AC 清单 + P2 处置建议）；报告归档 | ✅ | §6 DoD 终判素材 + 本文件即归档 |

## 2. 回归矩阵明细

### 2.1 M1 C 序列（C01~C30）— 63/63

| 块 | 覆盖 | 结果 |
|---|---|---|
| C01~C06 | ping / 未认证 401 / 建仓 / 非法 key 400 / 列仓 / 查仓 | ✅ 全过 |
| C07~C10 | 10MB 上传 + 服务端 checksum/size 对账、下载 sha256 一致、HEAD X-Checksum-Sha256、item info | ✅ 全过 |
| C12~C15 | 同内容去重（blob 计数不变）、客户端 checksum 一致 201 / 不一致 409（received/actual）、checksum-deploy 命中 201 / 未命中 404 | ✅ 全过 |
| C16~C18 | mkdir 201、children>0、deep list、删除 204 → 幂等 404 → GET 404 | ✅ 全过（C17 载体按 T-18 O3 口径为子目录 list；首轮根 400 为 v1.2 定案行为） |
| C20 | 改密双路由（PUT password + POST changePassword）、旧口令即时 401、回改恢复 | ✅ 全过 |
| C21 | token 发放（form）/Bearer 使用/吊销 401/幂等 "Token not found"/token XOR token_id 400 | ✅ 全过 |
| C22/C23 | ci-bot 建用户（email 必填负例 400）、未授权写 403、target 授权后写 201、无 delete 403、admin 全权；匿名读开（GET 200 bytes 一致、PUT 401） | ✅ 全过 |
| C24~C26 | `/v2/` 401（M2 反转）/ artifactory 404 / 根镜像 404 / npm 未实现子路径 404；X-Explode-Archive 400；docker remote 400 + generic remote 200（M3 反转，临时仓即删） | ✅ 全过 |
| C19/C30 | 删仓双态（非空 400 → deleteContent 2xx）；慢上传中断（--limit-rate 64k 客户端 3s 中断 exit≠0）→ big.bin 404 | ✅ 全过（重排说明：C19 移至 C23 后，因 C20~C23 依赖 generic-local 存续） |
| C27/C29（实例 B） | 匿名关：匿名 GET 401 + WWW-Authenticate、admin 200、有 read 200 / 无 read 403；kill + 重启后 y.bin 200 | ✅ 全过 |
| INV（M4 §5.6 反转面） | 301 / search 分派+未实现族 404 / groups 分派 / audit 200 / ui·docs·console 三保留字 400 | ✅ 8/8 |

### 2.2 M2 D 序列（D01~D24）— 68/68

| 块 | 覆盖 | 结果 |
|---|---|---|
| D01/D04 系 | 建 docker 仓 ×2；/v2/ 探针（401 + Bearer realm + Docker-Distribution-API-Version、认证 200 `{}`、匿名 pull token）；手工 token / ci-bot 双入口对照（管理面 403）/ offline_token / POST 表单 | ✅ 全过 |
| D05/D06/D07 | docker login（dind）；monolithic blob 上传 201 + 回取 sha256 一致 + HEAD DCD；单段 name 404；单请求上传 201 | ✅ 全过（Location 为相对路径，T-43 3.1 基线一致，客户端拼 BASE） |
| D08 系 | push → rmi → pull → run（echo 输出对账）；D08b 多架构 buildx（linux/amd64+arm64）push → index mediaType `application/vnd.oci.image.index.v1+json` | ✅ 全过（buildx 需 docker-container driver + buildkitd http=true 配置，构建侧手法） |
| D09/D10 系 | catalog/tags、`?n=1` Link rel=next、last 游标 exclusive、`n=abc` 400、未登记 name 404；chunked 3+7 段续传 201 + cmp 逐位一致、中断恢复 Range 探测 | ✅ 全过（分页断言前置第二 tag） |
| D11~D13d | 同内容再 push blob 计数不变；DIGEST_INVALID 400；cross-repo mount 201 + GET 200；manifest 引用缺失 blob 400（MANIFEST_* 族）；by-digest 删 202 → GET 404、by-tag 删 405 UNSUPPORTED、blob DELETE 405 UNSUPPORTED | ✅ 全过 |
| D15/D18 | oras Helm OCI（config+chart tgz 双 media type）push/pull cmp 逐位一致/repo tags；crane digest/manifest/copy（digest 一致） | ✅ 全过（`--plain-http` + `crane auth login --insecure`，T-44 口径） |
| D17/D19 | podman（容器形态）login/pull/push（服务端 tag 可见）；skopeo inspect（digest 与 crane 同值）/ copy docker-archive 双向 | ✅ 全过（`--tls-verify=false`） |
| D14/D21/D22~D24 | 慢速 chunked 中途 kill -9 → 重启 health ok / 残会话 404 / 历史 manifest 200；重启后 dind 缓存重拉 exit 0；ci-bot push denied；token 吊销后 Bearer 401；referrers 与 4 段路径 404 | ✅ 全过 |

### 2.3 M3 M 序列（M01~M59）— 140/140 + 2 N/A

| 块 | 覆盖 | 结果 |
|---|---|---|
| M01~M05 | maven/npm/pypi 三 local 仓；remote 校验（缺 url 400 / file:// 400）；virtual（成员校验 400）；type/packageType 过滤；docker remote 400（Q4 维持） | ✅ 全过 |
| M10~M21 | mvn deploy 1.0.0/1.1.0/1.2.0-SNAPSHOT×2（buildNumber=2）全 BUILD SUCCESS；jar 逐位对账、pom sha1/旁车裸 hex；consumer 全新 repo resolve；metadata 版本合并 + latest；timestamped 直连 GET + snapshotVersion≥2 + `-U` resolve；checksum 两态（client-checksums 409 / server-generated 201）；旁车两态（good 201 echo 一致 / bad 409）；snapshot 关闭 409；layout 400 ×2；Range 206 / ETag 304；sha1-only 秒传 201（M3 收编 P2） | ✅ 全过（M17 文件名需 layout 合规前缀——PRD 字面 `bad.jar` 先撞 layout 400，见勘误 E3；M16 断言载体按 T-74 E2 为 metadata+直连 GET） |
| M22~M28 | npm publish/view/packument（dist-tags.latest、tarball /binflow/ 前缀、shasum 与 tarball sha1 一致）/whoami；install + 清缓存重装；dist-tag add/install@beta/rm；scoped publish+install；重复 publish 403；`-rev` 假成功 + unpublish 1.0.1 移除；匿名 packument 200 / 匿名写 401 | ✅ 全过 |
| M30~M35b | twine upload；simple index（#sha256=、PEP 629 api-version=2、三名归一化 200×3、无尾斜杠 302、ETag 304、unknown action 400 文案逐字）；pip install 0.1.0 + whl sha256 == index 哈希；重复上传服务端 400（body 含 already exists，twine 7 只渲染 Bad Request，退出码非 0）；PEP 691 JSON files≥1 | ✅ 全过 |
| M41~M48 | MISS→HIT 上游计数冻结；SSRF（无 allowPrivateUpstream 请求 400 + WARN 取证）；负缓存（二次 miss 零上游）；上游故障五段（stale 服务 + `X-Binflow-Upstream-Error` 头、未缓存默认 404、hardFail 502、静默期零上游流量、health ok、恢复）；maven 代理真实 Central（junit 4.13.2 resolve + 缓存 200 + `.sha1` 不回源逐字文案）；npm/pypi 代理（见下）；remote 写 405 + Allow GET、删缓存 204 → 回源 200 | ✅ 全过（M44b 以 `retrievalCachePeriodSecs:5` 仓构造过期态后验证） |
| M46/M47 专项 | **M46 mock 腿全绿**（packument MISS 200 → npm install/view 经代理）；真实 npmjs 边界复核成立（`<name>/packument.json` 布局 404，T-75 已证的 M4 边界非回归）；**M47 真实 pypi.org 可用**（url=`https://pypi.org`，six 1.17.0 simple+whl 双 MISS 下载成功，T-75 O3 同形） | ✅（M46 按 PRD §5.4「公网依赖可 mock」替代口径） |
| M50~M55 | virtual 混合解析（local 成员 mvn resolve + remote 成员透传）；解析顺序（local-first→A、swap→remote B，重建即生效）；写 405+Allow；defaultDeploymentRepo 写路由（mvn deploy 经 virtual → virtual/local 双 200）；npm/pypi virtual（本地+上游成员一次装齐）；maven metadata versions 并集（含 remote 1.1.0） | ✅ 全过 |
| M57~M59 | npm `-/ping` `{}`；`.index`/npm search/pypi json/pypi-ui 四族 404；RE-11 stats | ✅ + M59 **N/A**（404 为 T-66 AC③ 批准的 P2 延后，基线一致） |
| M55b | 聚合浏览 children | **N/A**（T-61/T-71 批准 P2 延后，实测单 item 形态与 T-75 记录一致，非回归） |

### 2.4 修正账（首试 FAIL → 终局全绿的 QA 侧原因，零产品缺陷）

| 类别 | 项 | 原因与处置 |
|---|---|---|
| 断言口径 | C22e（201≠200）、C19c（计数方向）、M33（twine 7 不透出 body 文案） | PRD 文本即 2xx / 计数 0=已删 / 以服务端 400 + 退出码非 0 为断言 |
| 环境前提 | C17（隐式目录无 folder node → list 载体）、C30a（仓已删时 PUT 立即 404）、M16c/M16c2（同 + `<value>` 是 version 段需拼 artifactId） | mkdir 前置 / 仓库存态下复验（curl exit 28 + 404）/ 按 T-74 E2 载体 |
| 客户端旗标 | D06/D10（Location 相对路径）、D15（--plain-http）、D17（--tls-verify=false）、D18（crane auth）、D19（容器 entrypoint）、D08b（buildx driver） | T-43/T-44 基线口径；修正轮全绿 |
| 脚本缺陷 | D09c/d（tag 数不足）、D15 grep、D18 grep 模式、M11~M21 首块（mvn 未读到 settings → `-s` 显式）、M31f 引号、M33 CWD、M41c（UP1 计时点）、M44b（缓存未过期无头）、M46/M47（mock 布局 + pypi 仓 URL）、M48d/M54c（连带） | 逐条复验全绿（详见 regression.log 更正记录块） |

## 3. 性能收尾（W37 / NFR-P16~P18 / P20 + 基线对照）

| 项 | 实测（62d92f1） | 门 | 对照基线 | 结果 |
|---|---|---|---|---|
| NFR-P16 冷启动（空库、含 embed，ping 计时）×3 | **150 / 151 / 120 ms** | <2s | T-103 记录值同量级；M1 T-19 亚秒级 | ✅ |
| SPA 首屏（Chromium fresh context ×3，t104-perf spec 原样） | tVisible **477/398/393 ms**（DCL 47/37/32） | <3s（P1 记录） | T-104 388/387/389 ms——一致 | ✅ |
| 体积 | 二进制 **19.57 MB**；SPA gzip **151,255 B** | 40MB 预算 / ≤5MB（记录） | T-104 同值 | ✅ |
| NFR-P17a 搜索（10 万 node，命中 111~1111 条） | P95 **72ms**（max 92ms） | <1s | — | ✅ |
| NFR-P17b 审计 actor+since（10 万行） | P95 **16ms** | <500ms | — | ✅ |
| NFR-P18 children（1000 子项） | P95 **56ms** | <300ms | — | ✅ |
| FR-33-AC3 100 并发 REST 混合（session/search/usage/children，cookie 会话） | xargs -P100 **rc=0、100/100 成功、零 5xx** | 全 0 零 5xx | — | ✅ |
| 50 并发内容读写（PUT+GET 各 50） | rc=0 零 5xx | 零 5xx | — | ✅ |
| M1 NFR-P2 100 并发 GET 10MB | xargs -P100 rc=0、零 5xx | 全 0 零 5xx | T-19 8.1 同 | ✅（附 O-MEM1 观察） |
| 1GB blob 传输 | PUT **194 MB/s**（服务进程 RSS 增量 **52KB**）；GET **760 MB/s**；roundtrip sha256 一致 | 记录 | T-19 1GB PUT 206MB/s + RSS 增量 56KB——同量级 | ✅ |
| M60c 代理开销（10MB） | 直连 3-4ms / MISS **183~228ms**（服务端 upstream_duration 165-201ms）/ HIT **20ms** | remote 专属增量 ≤100ms（T-76 O1 口径） | 本地 10MB PUT **176ms**、1B PUT **117ms** → MISS 成本≈落盘 fsync 固定成本；**remote 专属增量 ≈7ms 达标**；HIT 与本地下载同量级 | ✅（同 T-76 O1 形态） |
| 50 并发登录无串号（NFR-P20 P1） | 未重跑（浏览器面归 T-104：50/50 身份回读正确 + session 值全互异） | 无串号 | T-104 §4 | ✅（引用） |

造数口径：SQLite 直插 `nodes` 100,001（1000 显式目录行 + 10 万文件行）+ `audit_events` 100,002（RFC3339 时间）+ `bigdir/` 1000 children，重启后 search/children/audit API 全可见且结果正确（抽验 1111/1000/200）。

## 4. 缺陷与观察

**无 P0/P1 缺陷。**

| # | 级 | 摘要 | 证据 | 处置建议 |
|---|---|---|---|---|
| O-MEM1 | P2（观察） | 每请求 Basic 认证的 argon2id 验证具 64MB 瞬态工作集（memory-hard）：100 并发认证 GET（10MB）后 macOS 上 RSS 3.8~4.9GB 且不回落；**同负载匿名 GET RSS 增量仅 0.5MB**；gctrace 显示并发窗口活堆 1.8~2.4GB、事后回落（非泄漏）；**m1-done 基线二进制同形**（100 并发→1.37GB）→ M1 起既有、非回归；流式传输本体干净（匿名 10MB×100 零增长、1GB PUT 增量 52KB） | §3 各行 + mem 实验三轮（含 GODEBUG 对照）；T-19 8.2 已注明 argon2 m=64MB 驻留 | 文档引导高 QPS/CI 客户端用 Bearer token（免 argon2 路径，C21 已验）；若需收紧再评估认证结果短 TTL 缓存（M5+） |
| O-104-1 复核 | 记录 | D-104-1 症状在 62d92f1 仍现：`/binflow/ui/assets/*.css` → `text/html`（SPA fallback）vs `/binflow/assets/*` → `text/css`；跨引擎 overlay 在该基线 webkit/firefox 树/上传链 4 败（登录链双过） | §4.3 overlay + curl content-type 双探针 | **T-120 已合入（170cf38）修复**，主会话验证 cross-engine 9/9；本票按派单口径不重跑，overlay 以 62d92f1 记录归档 |

## 5. 5xx 归因表（零容忍判定依据）

| 实例 | 条数 | 逐条归因 |
|---|---|---|
| A（18140） | 2 | ①C30 慢上传客户端中断 → PUT 500（`client_disconnect:true`/`context_canceled`——T-19 已定性同款）；②M44d hardFail:true 仓上游故障 → **502 为票面期望断言**（其访问日志即唯一 ERROR 级行） |
| 性能（18142） | 9 | 全部 `GET /api/v1/storage/stats` 500 且 `client_disconnect:true`——Playwright 跨引擎矩阵关页中断（T-100/T-104 已定性类，非服务端故障） |
| B / 内存对照 / m1 对照 | 0 | — |
| 合计 | 11 | **非预期 5xx = 0**；panic = 0 |

## 6. DoD §9 第 1/2 条终判素材

- **第 2 条（§8 剧本全绿，§5.3 矩阵 P0 成员全过）**：本票覆盖剧本 #1（回归基线）与 #8 性能腿全绿；#2~#7 已由 T-103/T-104 全绿归档；#9（T-106）/ #10（T-107）在批 9。五客户端矩阵（本票回归面）：docker ✅ / podman ✅ / crane ✅ / oras ✅ / skopeo ✅ / mvn ✅ / npm ✅ / pip+twine ✅ / curl ✅
- **第 1 条（P0/P1 AC 全绿）**：M1~M3 基线 AC 复跑全绿（本票）；M4 P0/P1 AC 由 T-103（217+8 项）、T-104（47/47）归档全绿——**回归 + 性能收尾后无新增阻塞项**，M4 DoD 前四条素材齐备（待 T-106 烟测 / T-107 文档终验收尾）

## 7. 勘误建议（E 编号，转 PM）

| # | 位置 | 现文 | 建议 |
|---|---|---|---|
| E1 | M2 §5.4 D06/D10 | Location 直接当 URL 用 | 注明「Location 为相对路径（T-43 3.1 定案），客户端需拼 scheme://host」 |
| E2 | M3 §5.4 M17 | `PUT .../1.1.0/bad.jar` 期望 409 | 文件名违反 maven layout 校验先命中 400——改 layout 合规名（如 `demo-app-1.1.0-badchk.jar`） |
| E3 | M3 §5.4 M16c | `?list&deep=1` 于 SNAPSHOT 目录 | T-74 E2 已勘误载体，PRD 命令宜同步为「metadata snapshotVersions + timestamped 直连 GET」 |
| E4 | M3 §5.4 M47 | `url=.../pypi/simple` | pypi remote 仓 URL 应为 `https://pypi.org`（服务端拼 `simple/<pkg>/`）；M46 注明 mock 布局为 `<name>/packument.json`（真实 npmjs 布局不兼容为 T-75 已证 M4 边界） |
| E5 | M1 §5.3 C30 | `timeout -s KILL` | darwin 无 timeout；QA 以 `curl --max-time 3 --limit-rate 64k` 等价替代（exit 28），PRD 可注记可替代形态 |

## 8. 环境与清理

- 实例 A/B/性能/内存对照 ×3、mock 上游 19199 全部停止；端口 18140~18144/19199 零监听
- 容器 t105-dind / t105-podman / t105-skopeo 已删；为本票拉取的 5 个镜像（docker:27-dind、alpine:3.20、moby/buildkit、podman/skopeo stable）已 rmi——宿主 Docker Desktop / daemon.json 零改动，用户既有镜像未动
- git worktree（/tmp/t105/wt @62d92f1、/tmp/t105/wt-m1 @m1-done）已移除；`/tmp/t105` 全目录（含口令、jar、settings、venv）已删；断言账本副本留 `/tmp/regression.log`、`/tmp/perf.log` 至 conductor 复核
- npm 全程 HOME 隔离 + 独立 cache；mvn 独立 settings + `-Dmaven.repo.local`；pip/twine 隔离 venv；用户 ~/.m2 / ~/.npm / ~/.cache 零触碰；凭据零入提交文件；本票零代码改动（主仓工作树仅 BOARD.md 为 conductor 侧改动）
