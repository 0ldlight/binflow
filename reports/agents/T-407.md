# T-407 工作日志 — FR-132 aql.md 规格票 + t226 活体核验 + 口径归一

- role: reverse-engineer；日期：2026-09-01（BOARD 只读；零 git 操作）
- 产出：`docs/reverse/aql.md`（新建）+ 勘误回写三处（inv-1 §E / inv-2 §1.C / 主矩阵 L283·L284·L338）+ `docs/reverse/README.md` 补 aql.md 行
- 证据：`reports/agents/t407-evidence/`（36 份 t226 API 响应体逐字节实录 + README）

## 状态：done（自评）；就绪度判定材料见 §4

## 1. 交付明细（对 AC）

| AC | 交付 | 状态 |
|---|---|---|
| 1 aql.md 落盘（webhook.md 体例：官方文档逐条锚 + inv 补白 + 置信度列） | aql.md §1~§13：REST wire / 域与入口 / 字段全集 / 操作符 / 尾缀链 / envelope 逐字（QUERY_PREFIX + range）/ 错误文案逐字表（E1~E10）/ K63 校准表 / 权限语义 / virtual 语义 / 老搜索族 / 子集边界表 / 基座映射表 / inv 补白清单 / 待验证清单 / 取证锚点（8 官方页 URL + updatedAt） | 完成 |
| 2 t226 活体核验腿（oss 档；差集法只读 INC-1） | 36 探针（§2）；语言边角（$not/链序/distinct 引号/type 默认/stat 字段）/ 错误文案逐字（parse error 格式串、空查询、匿名 401、OSS sort 门、Pro 门文案含尾 \n）/ 上限行为（12k 超长未触发、offset/limit range 回显、8 路并发无 429）/ virtual 语义（实例无 virtual 仓 + 只读纪律禁建仓 → 官方+反编译双源定案，如实标注不可拍——零静默升格） | 完成（virtual 活体腿如实施记不可执行） |
| 3 口径归一 | ①14-vs-13：**14 定案**（SearchResource 注册 14 子资源铁证，inv-1 漏 license；官方另有 archive/latestVersionByProperties 2 枚外挂全量 16——远期登记）已回写三处；②子集边界表（aql.md §2.1：M15 item+property / M16 stats+QRL+UI 族 / 远期 build 系+releases）；③K64：**维持 LIKE 子串（无断言反转）**+两个校准点（大小写不敏感对齐、`*` 维持字面）交 T-417；④基座映射表（§10：name/path/type/depth 派生、modified=updated_at 单列双义、sha1/md5 走 blobs join、无双值语义——差异留痕 5 处待 ADR-0043） | 完成 |
| 4 inv-2 §1.C + 主矩阵 AQL 行勘误回写（就绪度翻转留痕） | inv-2 §1.C 两行（14 计数定案 + AQL 行「缺失→M15 实现中（规格就绪）」）；主矩阵 L283（就绪度翻转留痕）/L284（14 勘误+M15 收编注记）/L338（13→14）；inv-1 §E 96 行（13→14 补 license） | 完成 |

## 2. t226 活体核验（OSS 7.84.10 rev 78410900，2026-09-01 会话）

- 恢复沿 T-381 §0：容器栈抵达时即 Up（T-381 保留态），`docker start` 未需执行；用毕维持 Up、零配置改动（INC-1：全程仅 GET + POST /search/aql 只读查询；零 delete/update/dryRun、零建仓/建属性）。
- admin 口令取 VM `~/t228-admin-pw`（0600）仅会话内使用，零仓库/报告落盘。
- 关键发现（全部回写 aql.md，逐条带证据文件名）：
  1. **`$not` 不支持**（400 parse error）——PRD FR-133.1 前提校准为 $and/$or（官方文档亦无 $not）。
  2. **OSS 档 `.sort()` 被许可门挡**（400 逐字文案）——BinFlow 无门按官方全集实现（A 层）。
  3. **未支持域/未知字段 = 通用 parse error 400**（无专门 domain-not-supported 文案）——BinFlow 400 码位一致、增强文案为 C 层（Q3 校准：无 parity 义务约束文案）。
  4. **stats 字段在 OSS 可用**（嵌套 stats[] 输出）——BinFlow 排 M16 的理由保持自有基建缺失，非档位。
  5. **老搜索空集族分化**：artifact/gavc/prop 未命中 200 空数组；usage/creation/dates 未命中 404 "No results found."——PRD FR-134「空集 200」按族校准（M15 三端点均 200 族）。
  6. **pattern/checksum/license/badChecksum/dependency/versions/latestVersion/buildArtifacts 在 OSS 为 Pro 门 400**（文案逐字含尾 \n）——BinFlow 对齐源 = 官方文档全集（非 OSS 降级行为），LC-70 注记。
  7. artifact name = 大小写不敏感子串（`name=T228-LIB` 命中 t228-lib）；envelope uri-only（BinFlow E-09 为超集，留痕不追改）。
  8. `?query=` 参数回退、`?compact=true` 行体压缩、`.distinct(true)` 无引号、链序敏感（limit 后 sort = 语法错）、匿名 401 "Authentication is required"。
  - 未复现（以核验为准，aql.md §12 V-a~V-h）：429 触发（语料过小）、6,000 长度门（12k 仍 200——版本差异，BinFlow 按官方值实现）、property 数据腿（实例零属性）、非 admin `unknown` 脱敏（无低权凭据）、virtual 活体对拍（无 virtual 仓）。

## 3. clean-room 合规自查

- 反编译只读零修改；aql.md 全文为行为描述，零 Java 代码粘贴（唯一字面量 `\n{\n"results" : [ ` 为 wire 字节序列=数据契约，非代码）；官方文档优先（8 页锚点在册，updatedAt 2026-03-22）；反编译补白逐条标注（§11）。

## 4. tech-lead 就绪度判定材料（L20——解锁 B1 的输入）

- **可拆性：就绪**。语言/内核/门/端点四票（T-409/411/413/415）+ 老搜索（T-417）的规格输入齐备：字段全集（§2.2）、操作符集（§2.4）、尾缀链与顺序（§2.5）、envelope 逐字（§3）、错误文案底本（§4 E1~E10）、K63 校准表（§5）、基座映射表（§10）。
- **裁决点清单（进 ADR-0043/实现票）**：①`$msp` 是否入 M15 子集（建议入——一行组内判定）；②`$eqic/$eqvic/$matchic` 反编译变体不收（官方未文档化）；③non-admin `unknown` 脱敏是否对齐（BinFlow 无 PRD 断言，ADR 裁）；④`modified=updated_at` 单列双义与 sha1/md5 blobs join（§10 差异 5 处）；⑤K63 冲突项按 Q2 走 C 层留痕（§5 表）；⑥dates/creation 判 **trivial 可顺车**（epoch 毫秒直查 created_at + 404 空集语义 + 行加 created 字段——K65 余量条款材料在案，T-417 票内裁量）。
- **缺项是否阻塞**：不阻塞。全部低置信项为边角（§12 V-a~V-h），各有明确归位路径（BinFlow 实现断言/后续活体）；L20 底线以文档锚为基准已达标。

## 5. 遗留与移交

- virtual 仓 AQL 活体对拍 → T-412/T-415 e2e virtual 夹具腿（V-g）。
- K64 两校准点的断言现值核对 → T-417（K64 落笔臂）；如 BinFlow artifact 搜索现区分大小写，改大小写不敏感（非断言反转，票内留痕）。
- Q4 材料（远端浏览）不在本票；Q1 分阶段边界材料 = aql.md §2.1/§9（PM 收口用）。
