# T-27 工作日志 — 三处累积裁决回写

- 角色: architect
- 日期: 2026-08-18
- 输入: coordinator 指令（T-13 修复 PutFromBlob 契约 / T-14 review RepoLookup 终判 / C28a QA 剧本勘误）；核对 internal/repo/api.go 实际签名与 ErrOrphanBlob sentinel 后落笔
- 状态: done

## 三处落点

### 1. §3.3 补 PutFromBlob 契约（architecture.md）

在 `repo.Service` 接口 List 之后、仓库 CRUD 注释之前补入方法与四点契约注释：
- 签名 `PutFromBlob(ctx, principal, repoKey, path, ref storage.BlobRef, mime) (*metadata.Node, error)`——与 internal/repo/api.go:162 实际签名逐字一致；
- ① 判权先于 blob 打开；② storage.Open 物理在 + blobs 台账行在双维校验；③ 孤儿 blob（物理在、台账缺行）→ `ErrOrphanBlob`，理由写明：秒传把无主 blob 实体化会使该制品永久失去 sha1/md5 附属摘要（sha256-only 降级不可逆）；④ sha1/md5 只从台账行取，不重算。
- 来源标注 T-13 修复。

### 2. §7.1 + §3.4 路由解析位置注记 + ADR-0009 补句

- §7.1 认证分层段后新增「路由解析位置」段：repo key → repo 行查询位于**授权门之后**；RepoLookup 用 metadata.Get 是有意为之的匿名读前置缝，安全面成立（无权者在查询前即被拦，repo 行不泄漏给未授权请求）；PackageTypeOf(ctx, key) 收回服务层列 T-15/M2 非阻断重构项。
- §3.4 Authorizer 注释块加两行同义交叉引用（指向 §7.1）。
- ADR-0009 后果条目追加 T-14 review 终判括号句（同义，一处）。

### 3. PRD C28a 勘误（docs/prd/milestone-1.md，两处最小改动）

- C28a 命令 `curl -sf $BASE/binflow/api/v1/health` → `curl -sfu admin:$ADMIN_PW ...`，行尾注释标明 v1.3.1 勘误及原因（该端点属管理面需认证，`curl -f` 遇 401 非零退出会误判失败）。
- §0 修订记录追加 v1.3.1 行：定性为 QA 剧本缺陷非实现缺陷（T-14 review 范围外发现 + T-27 回写），注明 FR-6-AC2 原命令不带 `-f` 且未声明免认证、不受影响。

## 边界自律

- architecture.md 两处（§3.3 一段、§7.1+§3.4 注记）、DECISIONS.md 一句（ADR-0009 后果条目括号追加）、PRD 一行命令 + 修订记录一行，共五处编辑，全部在授权范围内。
- 修订记录行插入用 python 定位行号完成（v1.3 行超长导致 Edit 字符串匹配失败，改用行号插入并已验证位置正确）。
- 未改代码、未动 BOARD.md。

## 遗留问题

无新增。PackageTypeOf 收回服务层的重构项已按裁决记入 §7.1 注记（T-15/M2 非阻断），无需 architect 后续动作。
