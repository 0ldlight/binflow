# T-456 [P1] QA 中期回归（逐批 V 式复核）— 中期报告（终稿）

> qa-engineer ｜ 2026-09-04 ｜ 基线 HEAD = `2832114`（git archive 净树导出跑 race——工作树当时含 T-444 在途手笔，见 §5）
> 复现环境：scratch 三实例 :18095（主腿→licensed）/ :18096（双实例腿上游）/ :18097（e2e 净实例 community→licensed），二进制 = HEAD `make build`（F1 门顺带过：94.13MB/120MB）。参照实例 127.0.0.1:8082（7.161.20，只读 INC-1）。
> 清场已完成：三实例停、临时目录删、rockylinux:9 镜像移除；证据日志留存 /tmp/t456-{race,repo-solo2,httpapi-solo,httpapi-solo2,bigtree}.log + /tmp/t456-t226-out2.json。

## 0. 结论

**中期 PASS（带 1 项 P1 缺陷登记 + 2 项在途标注）**。已落面（T-434/T-439~T-454，20/35）逐批复核全绿；race 三红全定谳为负载噪音（净机 solo 全绿、零 DATA RACE）；L41 ON 态活体腿被 REST 传输层缺线阻断（D-T456-1，T-461 依赖面）；L39 按 T-444 在途现状核实（HEAD 四动词闭集）。

## 1. AC1 逐批复核矩阵

| # | 腿 | 结果 | 证据（命令 + 关键输出） |
|---|---|---|---|
| L36 三段+字段域 | ✅ | t439 spec licensed 6P（步进/复制第三步/八域表驱动/重置钮退役/净度/tripwire）；**字段域 tripwire 活体复核**：PUT `blackedOut:true`+`repoLayoutRef`+`maxUniqueSnapshots`+`archiveBrowsingEnabled` → 200 但 GET 回显四域全缺（sqlite 存量 config 同证）＝T-439 契约漂移① as-built（BE 承接票待落，FE spec 钉住不破） |
| L36 八型 roundtrip 矩阵 | ✅ 8/8 | 独立复验（licensed :18095，REST 建仓＝UI 建仓语义等价，UI 建仓腿由 t441 spec 覆盖）：**go**（GOPROXY 三件 PUT 201 → 净 HOME 消费 `go run` 出包常量 `t456mod@v1.0.2-via-binflow`）／**nuget**（dotnet 10 pack→push `Created …flatcontainer/`→restore+run `hello from T456.Lib via BinFlow`，flatcontainer index `['1.0.0','1.0.1']`）／**cargo**（token→publish `Published t456crate v0.1.0`→净消费者 `cargo add/build/run` 出 `42`）／**conan** 2.32（create→upload revision Uploaded→净 CONAN_HOME `Install finished successfully`+内容逐字）／**helm classic**（PUT 201→repo add/update/search 命中→pull 逐字节一致）／**helmoci**（plain-http push digest `sha256:f150e74…`→pull 同 digest 逐字节一致）／**rpm**（rockylinux:9 容器 rpmbuild 真 .rpm→PUT 201+REINDEX 200→容器内 dnf makecache/repoquery `binflow-t456-0:1.0-1.el9.x86_64`/install→装后文件内容在场）／**deb**（debian:bookworm dpkg-deb 真 .deb→debPUT 矩阵坐标 201→Packages/Release 重算→容器内 apt update/install→`Status: install ok installed`+内容在场） |
| L36 入口/列表/dirty/Test | ✅ | t443 spec（community+licensed 轮均在 m16 全目录跑内绿）：三预选分路由+/new 兼容映射+Replications 列+dirty-gating+Test 三臂 |
| L37 页签序+字段族 | ✅ | t445 spec 5P（三级序 常规→有效权限→属性、URL slug 零变化、File URL 三形态、Downloads 族=？stats 面端到端） |
| L37 属性编辑+下载形态 | ✅ | t447 spec 4P（常显输入+Add+网格搜索、✎/🗑 退役、单 24px 图标钮、E1 危险确认） |
| L37 搜索栈 | ✅ | t449 spec 6P（列集归一断言反转②、行体 inert、顶栏驻留+快滤、空历史占位、日期含时区、?focus= 发射端翻新）+ t451 spec 4P（分页 ×9、档位 [20/50/100/200/1000] 冻结） |
| L38 统计基建单源 | ✅ | 独立 curl：下载直连 2+经 virtual 1 → **？stats 面 `downloadCount:3`/`lastDownloadedBy:admin`** ＝ **usage 搜索行 `downloadCount:3`** ＝ **AQL stats 块 `downloads:3, downloaded_by:admin`** 三面同值（K69 三分口径兑现）；?stats GET 自身零自增；usage `notUsedSince` 严格小于两臂语义正确（未来窗命中/过去窗 404）；行五字段含 uri 逐字（aql.md §14.2） |
| L38 ACL | ✅ | 三面越权探针：无权限用户 usage→404 空集零泄漏；？stats→403；AQL→200 空行（allow() 过滤） |
| L39 Annotate 迁移 | ⏸ **在途** | HEAD 现状核实：internal/auth 动词闭集仍四动词（r/w/d/m，M7 as-built）；ActionAnnotate 仅在工作树（T-444 未合入）。M7 RBAC 全量 spec（rbac/security 27P）HEAD 零回归。断言反转⑤未落＝符合在途预期 |
| L41 BE 可选档 off 态 | ✅ | 双实例夹具（B 为 helm 上游）：remote helm 仓默认（absent）→树仅缓存行（拉取缓存 1 文件后 children 恰该行，文件级字节核对）；pull-through 回源 200+逐字节一致 |
| L41 BE 可选档 on 态 | ❌→**D-T456-1** | PUT `listRemoteFolderItems:true` → 200 但 GET echo + sqlite 存量均 false——**httpapi 传输层无该字段（internal/httpapi 全 grep 0 处）**，repo 服务层接受位孤悬；服务/引擎层测试绿（`TestT448|TestT442` repo 11.3s ok + remote 7.3s ok——独立复跑）；降级腿活体同被阻断（服务层 TestT448UpstreamFault… 绿） |
| L42 statistics·usage 腿 | ✅ | 见 L38（单源）；QRL 三态：工厂 disabled 400 逐字 `Query rate limiter is disabled` → POST mode=enabled 200 → GET 默认 DEFAULT/LOW_PRIORITY=(4,10000ms)（K63 口径）→ merge-write LOW_PRIORITY=5 生效 → DELETE 复位 400；非法 setting 400 点名；非 admin 三面 403 |
| L42 UI 搜索族+dates | ✅ | artifactsearch/quick 200 命中+空参 400 点名；stashResults 404 逐字 `Stash search results endpoint is disabled`；packagesSearch leadFile/artifacts 200（uri/repoKey/path/name/size）+缺参 400；syntax-search POST 裸 AQL 200；creation/dates 瘦行 `{uri,created}`、空集 404 `No results found.` 逐字、缺 from 400 `'from' parameter cannot be empty!` 单引号逐字；匿名 401；受限用户 ACL 过滤零泄漏；panorama 测试（HEAD 导出）绿 |
| L48 引擎腿 next-run 对拍 | ✅ | 复制配置带 `0 * * * * ?` 建 → echo `next=05:20:00Z`（创建时刻下一分钟界，独立计算核对一致）；台账行 next_run_at 每次触发后推进（05:21:00Z→05:22:00Z）；GC 槽 `0 0 /4 * * ?` → nextRun `08:00:00Z`（05:2x UTC 后首个 4h 倍数，对拍正确） |
| L48 零重复投递双实例腿 | ✅ | 3 制品先落 A → 调度 fire（log `scheduled full sync seeded … scheduled=3`）→ B 逐路径 sha256 三路一致；窗内第 4 制品 → 事件轨即达（B=4）；下次 fire 全量重种 → **B node 计数仍 4**（幂等收敛零重复）；POST /{id}/run Replicate Now 并存 → 200 + B 仍 4；audit `replication.schedule.set`(admin)+`replication.schedule.run`(actor=scheduler) 双行在场；维护面过去时间表达式 400 `no future trigger`+畸形表达式 400 引擎点名 |
| t226 活体对照关键页 | ✅ | 只读复核：参照树头工具带（Filter By facet 复选组+Local/Remote/Cache/Virtual/Release Bundle/Federated+Select All/Clear All+My Favorites+Sort By 三键+Tree View）与锚册记录一致；BinFlow 侧 as-built：建仓表单两段（基础/高级——建仓态无复制载体，t439 冻结口径）/详情页签序 常规→有效权限→属性/用户页列选器+分页档位 [20/50/100/200/1000]（T-451 冻结）——Release Bundle/Federated 缺位＝M17 stay-out 不伪造，形态族对齐 |

## 2. AC2 回归窗口 + 断言反转 + E1/E6

- **M1~M15 P0 双形态增量窗口**：community 轮（:18097 无 license）m16 目录 **41P/2S** + 回归族 artifacts/repositories/repo-policy-keys/rbac/security **27P** + m8 shell/repositories-admin/a11y-sweep + login/auth-shell/console-smoke **25P** + m10 properties-matrix **7P**/console-license-addons **5P** + m15 t419 **7P**/t414 **5P** + t416-virtual-tree/governance/pathmatch-parity **12P**；licensed 轮（装 enterprise.lic → tier=enterprise）t441+t439 条件腿 **10P**（八型控制台建仓矩阵全跑）。累计 **139 passed / 0 failed**，axe 双主题腿全绿（a11y-sweep/t449/t451/t441/t453 等 spec 内嵌）。
- **断言反转①~⑤现值**：①树栈四项（t434 spec，m16 轮绿）；②搜索列集归一（t449 绿）；③重置钮退役+modal 924px（t439/t441 绿——924px 非票面 880px，T-441 已按 7.161 实测定案留痕）；④用户/组路由表单化+内联卡删尽（t453 绿）；⑤annotate/write 拆分＝**未落（T-444 在途，HEAD 四动词）**——①~④现值全兑现，⑤按在途标注。
- **E1 零倒退（Q2 新口径）**：浏览器面删除件收紧断言在 t434（children 表收窄+确认）/t447（属性行危险确认）内绿；BE 删除守卫经 rbac/security 面绿。
- **E6 零倒退（Q3 出口后）**：UI 中文+英文术语保真维持（全量中文标签断言绿）；双语翻案本体＝FR-149 B15/B16 波次未开——**双 locale 抽样腿按票面预留不跑**。
- 锚册门：`node scripts/anchor-audit.mjs --ledger` → **PASS（unregistered/broken 双零，retired 全在表）**。

## 3. race 矩阵与红面定谳

全树 `go test -race -count=1 ./...`（HEAD 净树导出，25m 包窗）：**34 包 ok + 3 包红，零 DATA RACE**。三红逐包串行 solo 定谳（净机复核）：

| 包 | 首跑红面 | solo 结果 | 定性 |
|---|---|---|---|
| internal/metadata | `TestRBACMigration011HundredUsersUnderOneSecond`（1.97s vs <1s 性能门） | **ok 141.7s**（全包含该测试） | 负载噪音（首跑与我的八型客户端矩阵并行+Docker VM 尖峰） |
| internal/repo | `TestBigTreeCopyNo5xx`（dry run 10k 树 58.1s vs 45s 预算） | **ok 816.8s**（全包含该测试；污染窗复跑曾 100s——彼时他 agent 测试进程 httpapi.test 146%/repo.test 76% 在跑+VM 582% CPU） | 负载噪音 |
| internal/httpapi | 包级 25m 超时（panic 于 TestLicenseLogRedaction 1s 处＝非死锁纯时长） | 第一次 solo（25m 窗）仍超时 1508s——**彼时 Docker VM 582% CPU（postgres/artifactory/kind/buildkit 常驻群尖峰）**；第二次 solo（40m 窗）**ok 733.7s 零失败** | 负载噪音（VM CPU 窃取）；净机真实墙钟 ~12m |

**观察项 O-1（非缺陷）**：httpapi 包 race 墙钟净机 ~733s，默认 25m 包窗在 VM 满载窗内会被击穿（本次实证 1508s）——建议后续全量 race 跑在 Docker VM 静窗或提高 TEST_TIMEOUT 敏感度留档。

## 4. 缺陷登记

- **D-T456-1 [P1]** `listRemoteFolderItems` REST 传输层未接线：`internal/httpapi/repositories.go` 的 repoConfig 解码/configJSON 序列化均无该字段（全 httpapi grep 0 处），PUT 带 `listRemoteFolderItems:true` 被静默丢弃（GET 回显/存量 config 双 false 佐证）；连带 **类型门 400 不可达**（generic remote 带 true → 实测 200 而非服务层设计的按名拒绝）。影响：L41 on 态/降级活体不可达、T-461 FE 开关与树消费无 REST 通路。repo 服务层接受位已在（T-448 config.go:187），修复面 = httpapi 传输 + FolderInfo 降级 note 渲染（T-448 §5-2 缝）。建议：并入 T-461 BE 支撑或 30min spot 票。
- 过程假警报销案（记录备考）：usage 行「缺 uri」与树行「uri 消失」两次均为 shell 输出压缩对原始 JSON 显示的改写——已改用落盘+python 字段级断言复核，均实为在场。**教训入册：wire 校验禁用管道裸读，一律落盘解析。**

## 5. 在途与诚实跳过

- **T-444（Annotate）在途标注**：工作树实测含 internal/auth `ActionAnnotate` + httpapi 四文件改动 + 新测试 `permission_wire_verbs_test.go`（编译中间态——工作树 httpapi 测试包暂不可构建，非 HEAD 缺陷；我的 race 在 HEAD 净树导出跑故不受扰）。L39 面按 HEAD 现状核实并标注在途；M7 全量 spec HEAD 零回归。
- **未跑面（票面外/未合入）**：双 locale 抽样腿（i18n 后置预留）；L40a/b 批次④余面（T-455/T-457/T-459 未合入——其中 T-455 与本票同波 B10）；FE 远端浏览树消费（T-461 未开）；B 47 项收口审计表＝T-466 终验硬 AC（本票不在范围）；t226 对照深度＝关键页结构级（树/表单/详情/安全），逐项 DOM 级对账归终验。
- 环境披露：参照实例与 BinFlow 对照期间全程只读（GET 导航）；rockylinux:9 镜像为本次拉取并已移除；QA 过程新建 token/用户/仓全部随 scratch 实例销毁。

## 6. 回归证据归档

- race：/tmp/t456-race.log（34ok+3红）、/tmp/t456-repo-solo2.log（ok 816.8s）、/tmp/t456-httpapi-solo2.log（ok 733.7s）、/tmp/t456-bigtree.log（污染窗单跑 100s 定谳用）
- t226 对照：/tmp/t456-t226-out2.json（双端结构捕获）
- 八型矩阵与 L38/L41/L42/L48 全部 curl/客户端命令与输出已摘入 §1（实例销毁前取证）

**状态：qa 完毕，中期 PASS——待收编。**
