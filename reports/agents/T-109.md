# T-109 — M4 PRD v1.1：R1 对齐标注 + R4 TTL 双键收口

- 角色：product-manager
- 状态：done（2026-08-20）
- 来源：tech-lead T-88 转交（R1/R4）+ 顺手项
- 边界：只改 docs/prd/milestone-4.md；BOARD 未触碰

## 落点（docs/prd/milestone-4.md，v1.0 → v1.1，共 7 处编辑）

1. **R1 命名面对齐**（裁决「PRD 面 + ADR 内核」，正文命名零改动）：
   - §0 修订记录追加 v1.1 行（完整记录裁决内容与 T-108 勘误归属）；
   - FR-23 用户故事后插入「对齐注记」：命名面以本 PRD 为准（`/binflow/ui/**`、保留字 `ui`、`/binflow/` 301、cookie `binflow_session`），机制内核以 ADR-0014 为准（server-side session 落库、CSRF 分层、Authenticator Session 第三臂、无 console 专属 API 树、vite+React+TS 构建链）；冲突以注记分界为准；
   - §5.2 矩阵引言后插入同源注记（覆盖 CE-01~07）；
   - 头部状态行升 v1.1（沿用 M2/M3 每版同步状态行范式）。
   - 依据核实：DECISIONS.md ADR-0014 原文命名面为 `/binflow/console/**`（保留段 {api,v2,docs,console}、cookie `bf_session`），与本 PRD `/binflow/ui/**` 冲突属实；裁决后 ADR 命名面归 T-108 勘误对齐，本票不改 PRD 正文命名。
2. **R4 TTL 双键收口**：
   - FR-23 登录会话规格：`console.session_ttl_hours`（**主键**，默认 24）+ **覆盖键 `console.session_ttl_seconds`**（同给时以 seconds 为准，测试/短期会话粒度，W08 依赖）；
   - §7 Q1 暂行 cell 同步双键措辞；
   - W08 命令注释补「覆盖键」标注（命令本体维持 seconds=5 形态，未动）。
3. **顺手**：§5.5 表后追加注记——K1~K3 暂行值已写死进 T-97/T-92 派单口径，逆向扩编票落地后按表回写，冲突以校准值为准并同步勘误两票断言。

## 未动项（有意）

- CE-03~06 行为要点、W04b 断言（cookie 三属性）、CSRF 的 Origin 校验暂行口径：属机制内核/待 T-108 勘误与 ADR-0014 决策 2（CSRF 三层）收敛的范畴，本票 mandate 仅「补引用与版本行」，不越界改语义——若 T-108 后 ADR 内核与 PRD 行为面仍有出入（如 cookie 属性集、CSRF 机制形态），由 conductor 另派勘误票处理。
- ROADMAP.md：v1.1 为 PRD 内部收口，基线引用仍指「PRD v1.0，T-85」→ 建议主会话顺手把需求基线行升为 v1.1（未越界代改）。

## 自检

- 7 处编辑全部成功（§0 两行、状态行、FR-23 注记 + TTL 句、Q1 cell、W08 注释、§5.2 注记、§5.5 注记——其中 §0 与状态行各 1 处，共 8 处落点，见上）；grep 无占位符残留；`session_ttl_hours` 出现处（FR-23/Q1）均已带主键/覆盖键成对措辞，`session_ttl_seconds` 出现处（FR-23/W08）均已带覆盖键标注。
