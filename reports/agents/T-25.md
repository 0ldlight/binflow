# T-25 工作日志 — 架构文档回写：三基础包 review 裁决落地

- 角色: architect
- 日期: 2026-08-17
- 输入（只读）: reports/agents/T-9-review-arch.md（§7 十条回写清单）、T-10-review-arch.md（B1/B2/M1/M9/M10）、T-8 裁决（BINFLOW_DATA_DIR 保留）、T-9 修复新增契约（ErrSessionPoisoned、state.json version）；核对实现代码 internal/storage/api.go、engine.go、session.go、internal/metadata/api.go、store.go、internal/config/doc.go 以文档对齐**事实**而非仅对齐报告
- 状态: done（一致性自检通过，见下）

## 回写点清单（architecture.md 12 处 + DECISIONS.md 勘误 2 处 = 14 处）

### A. storage 契约（T-9 review §7 十条，来源标注在文内）

| # | 位置 | 内容 |
|---|---|---|
| A | §3.1 GC | 签名改集合形 `referenced func() (map[string]struct{}, error)`；mark=一次 DISTINCT 查询、sweep=磁盘扫描（更彻底，能发现 blobs 缺行/DB 回退孤儿）；grace<=0=默认 24h；引用集驻内存 M6+ 流式变体注记 |
| B | §3.1 Engine | 补 `Close() error`（排空在途会话、拒绝变更操作、Open/Stat 继续服务、幂等）；§7.4 停机序列补「关 storage.Engine」步骤 |
| C | §3.1 错误约定 | 六 sentinel 全集：原三个 + `ErrBlobCorrupt`/`ErrEngineClosed` + **`ErrSessionPoisoned`（T-9 修复新增契约：Append 失败毒化会话，双 %w 错误链，含 Session.Append 注释）** |
| D | §3.1 Open 注释 | 返回 BlobRef 只填 Sha256+Size；sha1/md5 事实源是 metadata blobs 表（无 sidecar） |
| E | §3.1 Stat 注释 | 「轻量探测」→ 完整性校验语义（全量读重算三摘要、O(size)、ErrBlobCorrupt；存在性探测走 metadata） |
| F | §4.1 state.json | 契约定稿 `{"version":1,"id","created_at","received","sha256":null}`；**version 演进规则由本票定稿**：字段新增必须 bump version、未知 version 视为不可恢复按过期清扫、字段不得删改；received M1 每次 Append 后更新（文件不撒谎）、M2 chunked offset 基准 |
| G | §4.4 + ADR-0006 勘误 | grace 基准明确为 **blob 文件 mtime**（非 blobs.created_at）；**备份/恢复必须保留 mtime 硬约束**（tar/rsync -a 默认保留）写入 ADR-0006 勘误条目 |
| H | §3.1 | `ErrNoSuchSession` → `ErrSessionNotFound`（错误约定行注明「早期文档误写」） |
| I | §3.2 + §4.4 | `FilterUnreferenced` 定位改「对账/一致性检查用」，GC mark 自集合形后不走它；§4.4 mark 段同步改写 |
| J | §4.4 + §8 | session_ttl/grace 零值=默认 24h 语义明确（零值≠立即回收） |

### B. metadata 机制（T-10 review，ADR-0007 勘误 + §6 前言）

| # | 位置 | 内容 |
|---|---|---|
| K | §6 前言 + ADR-0007 勘误 | **per-connection PRAGMA 必须走 DSN `_pragma=...`**（foreign_keys/busy_timeout/case_sensitive_like 每连接生效；`db.ExecContext` 只配第一条连接，扩池后 FK 静默失效——机制原因写明）；WAL 库级同走 DSN；池 NumCPU；ADR-0007 加勘误条目（决策方向不变、机制表述升级） |
| L | §6 前言 + §3.2 注 | `case_sensitive_like=ON` 全局生效：前缀查询（ListByPrefix/DeleteByPrefix）大小写敏感，与 repo key/path 语义一致；§3.2 DeleteByPrefix 注释同步 |
| M | §3.2 | 「Txn 只读透传」残缺注释行作废；T-10 review M1 裁决（a 案）落文档：blob-first 两语句 + FK 兜底为 M1 事务边界；§1.1 与 §3.3 的「一个事务」措辞同步修正 |
| N | §6 前言 + DDL | 布尔统一 INTEGER（unreachable_mask 的 BOOLEAN 声明勘误）；迁移文件禁自带 BEGIN/COMMIT（M9）；schema_migrations 由迁移器自建、不在 001 内（M10）；§3.2 Store 补 `Ping`（T-10 扩展合规注记） |

### C. config 例外名（T-8 裁决）

| # | 位置 | 内容 |
|---|---|---|
| O | §8 | 四个例外名清单（ADMIN_PASSWORD / SECURITY_ANONYMOUS_ACCESS / **BINFLOW_DATA_DIR（docker -e 高频，保留）** / BINFLOW_HOME）；HOME 与 DATA_DIR 边界：HOME=目录解析根（cmd/T-16 用，config 忽略）、DATA_DIR=storage.data_dir 直配且优先 |

## 一致性自检（grep 逐条核对，全部通过）

旧措辞零残留：`referenced func(ctx`（逐条形）、三 sentinel 短清单、「轻量探测」独立表述（仅存于勘误说明句）、`ErrNoSuchSession`（仅存于「早期文档误写」注记）、`FilterUnreferenced 供 GC`、state.json 旧单行形状、`Txn 只读透传`、DDL 内 `BOOLEAN` 声明（仅存于勘误注释）、`CREATE TABLE schema_migrations`（已移出 DDL 块）、「三个例外」、§1.1/§3.3「一个事务」——全部 0 命中或仅以勘误说明形式存在。
新契约就位：集合形 GC 签名、Close()、六 sentinel、ErrSessionPoisoned 双 %w、case_sensitive_like、四例外名、T-25/T-9/T-10 来源标注 18 处。

## 边界自律

只改 docs/design/architecture.md 与 DECISIONS.md（ADR-0006/0007 各追加勘误条目，决策本体未动）；未新开 ADR（均为实现合理、文档滞后的对齐）；未改代码；未动 BOARD.md。

## 遗留问题

1. ADR-0006 勘误②的 mtime 硬约束需 M4 备份票与 tech-writer ops 文档显式继承（否则约束只活在 ADR 里）。
2. state.json version 演进规则由本票定稿为「bump + 未知版本按不可恢复处理」——M2 chunked 实现时若需要更细粒度（如部分字段前向兼容），届时再议。
3. §3.2 Store 的 `Ping` 系 T-10 代码先行、本票补录；后续接口扩展仍应走「api.go 报备 + architect 回写」流程，避免文档二次滞后。
