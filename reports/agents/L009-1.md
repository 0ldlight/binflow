# L009-1 — 四条 UNKNOWN 到期 gate 首件（改期 LOOP 010 + 候裁建议）

```
Ticket:       L009-1 四条 UNKNOWN 到期 gate 首件（invalid-value 族 R-10 + readers R-13 + anonymous R-14 + security-read-family R-12）
Role:         compatibility-engineer（rest 域台账实例）
Area:         docs/compatibility/
Input:        known-divergence.yaml 四条 LOOP 008 限期条目 + docs/prd/pending-rulings.md v2.0（L008-4a 呈批包）+ loop-state.yaml L009-1
Changes:      ① rest/repo-config-invalid-value-family：review_gate 改 LOOP 010（R-10）+ rationale 补改期行（呈批包指针+R-10a~c 席位+触发条件）；
              ② rest/security-read-family-readonly-admin-gate：同上（R-12；ADR-0026 决策 1 补引路径与收窄分支随改期行保留）；
              ③ rest/user-create-default-group-readers：同上（R-13）；④ rest/anonymous-user-row：同上（R-14；写面未取证臂注记）。
              classification 四条均维持 UNKNOWN 未动（候裁不越权）。
Files:        修改 docs/compatibility/known-divergence.yaml（4 条目 ×2 处 = 8 edit）；新建本日志。
Tests:        不适用（无 probe 本票）——校验以 YAML 门与 grep 引用门代替（见 Commands）。
Commands:     ruby -ryaml parse 门 → "parse OK, entries=25"；四条目 review_gate dump 全数 LOOP 010 限期（见上）；
              grep "LOOP 008 限期升级" → 0 命中（旧 gate 清零）；grep "LOOP 010 限期" → 4；grep "LOOP 009 L009-1" → 4；
              grep "docs/prd/pending-rulings.md §1" → 4 行（L253/306/342/356，各含席位号 R-10a~c/R-12/R-13/R-14）。
Outputs:      契约 0 / probes 0 / matrix 行更新 0 / known-divergence 新增 0（改期 4 条，非新账）。
Compatibility: 状态机零变动（4 条 UNKNOWN 维持；无 DISCOVERED→SPECIFIED 等迁移）。置信度不变：4 条证据链均维持
              原立账置信（invalid-value 族 E4+E5 活体、readers/anonymous E5 双端活体、security-read-family E1+E1 规格）。
Security:     无新断言（R-12 涉 readonly_admin 门面，候裁建议含安全面注记——见下）。
Performance:  无。
Risks:        ① R-14 写面（PUT/DELETE anonymous 行）参照行为未取证——实现票红线已在改期行注记（先取证或登记未取证臂）；
              ② 若用户整包批复拖延过 LOOP 010，四条将二次到期——升级建议见 Next。
Blockers:     无（authority 阻塞已按处置改期登记，非本票 blocker）。
Next:         候裁建议呈 conductor（不越权直改）：
              [1] R-12 security-read-family——最强候裁：证据链完备（router.go 实测 + rbac-model.md #2 高置信 + 参照存在主体面零差），
                  authority 锚已存在（ADR-0026 决策 1，2026-08-23 Accepted：readonly_admin 管理面={system:read, security:read, repo:read}），
                  PM 建议已备（维持超集→INTENTIONAL）。缺的仅 pending-rulings 表 authority 列的「+用户确认」一腿——建议 conductor
                  将 R-12 单独提请用户确认（或随整包批复），批复即由本角色翻 INTENTIONAL（authority={type: adr, ref: DECISIONS.md#adr-0026 决策 1}+R-12 终裁）。
              [2] R-10a/b——次强候裁：authority 类型为「产品（+ADR 登记）」（PM 域内可裁类），但 INTENTIONAL 需 ADR 入册为锚
                  （ADR 尚未写——裁定后登记），故现在不可自裁；建议 conductor 评估是否让 PM 先域内裁 R-10a/b 并开 ADR 登记票，
                  不必等整包。
              [3] R-13/R-14——建议立场均「对齐」，裁定即开实现/建模票（台账翻 BUG 修复路径），无 INTENTIONAL 捷径，
                  维持随整包批复；R-14 实现票须带写面取证前置。
```

## 候裁建议明细（呈 conductor，未改动任何 classification）

| 条目 | 席位 | 证据是否足裁 | 缺什么 | 可走的快路径 |
|---|---|---|---|---|
| rest/security-read-family-readonly-admin-gate | R-12 | 足（E1 实测 + E1 规格高置信 + 参照主体面零差） | 仅「用户确认」一腿（ADR 锚已在册） | 单项请用户确认 → 即翻 INTENTIONAL（authority=ADR-0026 决策 1） |
| rest/repo-config-invalid-value-family | R-10a/b | 足（E4 活体逐字） | ADR 未登记（产品类裁定需 ADR 入册为 authority 锚） | PM 域内裁 + ADR 登记票，可不等整包 |
| rest/repo-config-invalid-value-family | R-10c | 足 | 裁定本身（对齐则开实现票） | 随整包 |
| rest/user-create-default-group-readers | R-13 | 读面足 | 裁定本身（对齐=建模票+翻 BUG） | 随整包 |
| rest/anonymous-user-row | R-14 | 读面足 / **写面未取证** | 裁定本身 + 写面取证（实现票前置） | 随整包；实现票红线=写面先取证 |

## 改期后四条目终态（ruby dump 实录）

```
rest/repo-config-invalid-value-family      | UNKNOWN | LOOP 010 限期（随 R-10 批复升级——用户 authority 待决）
rest/security-read-family-readonly-admin-gate | UNKNOWN | LOOP 010 限期（随 R-12 批复升级——用户 authority 待决）
rest/user-create-default-group-readers     | UNKNOWN | LOOP 010 限期（随 R-13 批复升级——用户 authority 待决）
rest/anonymous-user-row                    | UNKNOWN | LOOP 010 限期（随 R-14 批复升级——用户 authority 待决）
```
