# T-271 — M9 用户文档四项：SSO 铸 Token / 用户增删闭环 / 旧书签公告 / npm 权限口径

- 状态: done（四项全落 + api-reference 六端点增补 + T-273 运营提醒 + `make docs` 内容级零告警；E1~E6 与删除链全部自起栈活体复验）
- 角色: tech-writer ｜ 批次: M9 B11 ｜ 日期: 2026-08-25
- area: `docs/user/`（9 篇改动 + README 导航）+ `make docs` 产物同步（docs-site/build 与 internal/docs/dist 刷新）。**BOARD 零改动、零 git 提交**。

## 1. 四项逐条

### ① SSO 铸 Token（T-260 OIDC 控制台腿）

| 文件 | 改动 |
|---|---|
| `admin/token-step-up.md` | 「SSO 用户 CLI 铸 Token 的路径」**过渡形态转正**：路径 1 改为「控制台直接铸（M9 起推荐）」——whoami `source=oidc` 分腿、401 不出口令框改出重认证引导、`purpose=step_up` 跳转、fragment 应用引导期消费即抹除、自动续铸恰一次、失败内联 `error_description` + 绝不以旧 grant 重试、TTL 倒计时提示性质；旧浏览器+curl 过渡形态降为历史注记。卷首适用版本补 M9 增补与复验 provenance |
| `console.md` | Set Me Up「生成令牌」节**补 OIDC 臂**：非 admin 会话按 `source` 三分腿（admin 豁免 / local+ldap 口令框〔M8〕/ oidc 重认证〔M9〕），含 pending「等待重认证完成」提示语义 |
| `guides/oidc-config.md` | ① **YAML-only 精确化**：例外从仅 `CLIENT_SECRET` 修为「CLIENT_SECRET + `AUTH_OIDC_READONLY_GROUP`（M7 角色映射键）」——原句与 `config.go` splitEnvKey 白名单不符（复核对码发现，顺手修正）；明示 `BINFLOW_AUTH__OIDC__ENABLED` 等拒启 → 不能用 env 把 OIDC armed 起来。② 新增「**与 token step-up 联合部署（armed 形态）**」节：armed.yaml 实测样例（token_step_up env 可达 / oidc 子键必须进 `-c` 文件）+ 启动自检 + 本地 mock IdP 演练法。③ 登录流节补 `purpose=step_up` 一段（prompt=login / 不建新 session / fragment 回传 / 未知 purpose 400） |

### ② 用户增删闭环（T-251/T-257/T-252/T-253/T-254）

| 文件 | 改动 |
|---|---|
| `api-reference.md` | 六端点全进表：SE 域 E2（列表加宽）/E3（enabled）/E4（**新 DELETE 行**）/E5（includeUsers）/E6（filter=manage）；系统端点表 + `/api/v1` 自有端点表补 E1 usage 批量；`DELETE permissions` 补 **204** wire 事实；纯文本错误节加 DELETE users 家族指针。新增「**M9 增补速览**」节：E1 参数四臂（bare array / `?repos=` 静默缺失 / `?include=counts`〔nodeCount 只计文件、updatedAt=配置时刻〕/ 未知值 400 信封——curl 逐条为实测输出）、E4 四护栏逐字文案表 + 检查序 + 级联清单 + **重复删除确定性 404（有意非幂等）** + 404 文本体 wire 偏差注记、E5 字面 true 语义、E6 三臂（admin 逐字节一致 / 覆盖集子集部分覆盖隐藏 / 空集 403 同形同字节） |
| `admin/governance.md` | 用户 CRUD 表「删除 M4 未提供」**纠偏为 M9 起提供**；新增「**删除用户（M9 起）**」节（curl 双例 + 护栏表 + 级联不可恢复 + 组 409 vs 用户级联的有意不对称 + 非幂等裁定 + 控制台强确认面对照）；组管理节补 E5 花名册 curl；审计词表补 `user.delete` |
| `admin/governance.md`（T-273 顺带） | 新增「**用户管理风险：last-admin 竞窗（运营提醒）**」节：census 在事务外、双 admin 并发互删可致零 admin（T-273 候选背景）；**重种无效**的机制解释（seedAdmin 只在 admin 行不存在时插入——对码 `metadata/store.go` 核实）；恢复双路径 = 停机 sqlite 手术（SQL 内联，列名核实自 seedAdmin INSERT）或备份 import；事前串行化建议 |
| `admin/groups-permissions.md` | 用户维护表四行 M9 化（含删除行从「未提供」→ 端点 + 语义链接） |
| `admin/rbac-roles.md` | 能力表补 E6 读臂行 + usage 批量行；「过滤列表 M8+ 评估」→「**延后 M10+**」（architecture §14.1 E7 定案对齐）；头注补 M9 |
| `artifactory-path-map.md` | 新增**删用户对照行**：Artifactory 对话框确认 vs BinFlow 输入用户名强确认；差异面如实——护栏（Artifactory REST 面低置信/不可见，未夸大为「自删可致锁死」）、重复删除 404 vs 幂等成功 |
| `console.md` | 用户与权限节 M9 消费面：列表单请求（21→1 扇出退役）+ Status 真值 + knownEnabled 兜底退役注记；删除双入口强确认；组穿梭 E5 按需 + 计数 E2 投影；m-holder 编辑器可达（filter=manage）链接 |

### ③ 旧书签公告（T-263）

- `console.md`/`faq.md` 旧路径终态（T-263 已落）**核对通过**：faq L140 已是「M9 起重定向已移除 → 404 页 + 更新书签」终态，锚 `#旧路径--新路径m9-起不再重定向` 构建产物 id 验证存在；`artifactory-path-map.md` grep 零旧路径引用（T-263 结论复核成立）。本票补强：console.md 卷首适用版本挂 M9 增补清单；README 导航措辞同步。
- **活体复验**：HEAD 构建 scratch 实例跑 `e2e/m8/shell.spec.ts` → 5/5，其中「legacy routes: all console-m8 §1.4 paths land NotFound」腿绿（19 条旧路径 404 + 查询串不复活 + 回主页）。
- 顺带核对：18080 用户实例为 M8 期构建（E1 GET 实测 404、老控制台仍处兼容窗口）——文档 M9 语义以升级后为准，已在日志登记非文档缺口。

### ④ npm 权限口径（T-249/T-262）

- `integrations/npm.md`：「发布权限语义（M8 起）」节（标题未动，保 faq 双锚）增补「**复制引擎同口径（M9 钉死）**」段——引擎只发单版本发布文档与单 tag PUT、从不整包覆写；目标凭据最短答案同 CI：`read`+`write`（T-262 全栈实证）；头注挂 M9 复核。
- `faq.md`：新增「M9 增补两问」——重复 DELETE 404 读作终态 + npm 复制凭据不授 delete；迁移对照表 users/groups 行补删除语义对照。403 表三处 npm 口径复核一致（M8 语义未动）。
- T-262 报告建议的「复制节最小权限注记」落位说明：user docs 尚无复制管理专篇（复制 REST 面未入 api-reference，M6 遗留面），故凭据口径落在 npm.md 发布语义节（读者配复制 principal 的自然入口）——专篇归后续。

## 2. 自测证据（全部实跑，2026-08-25）

### curl：E1~E6 + 删除全链（HEAD 构建 scratch :18097，种子 g-local/m-local/devs/jane/mholder/t-mh）

```
E1  GET usage                 → 200 bare array（2 行，行形 {repo,usedBytes,quotaBytes}）
    ?include=counts           → nodeCount=1（folder 排除）/ updatedAt；缺省形态两字段零出现
    ?repos=g-local,no-such    → 恰 [g-local]（未知名静默缺失）
    ?include=bogus            → 400 errors[]（message 逐字入文档）
    匿名                       → 401；jane（仅 m-local read）→ 恰 [m-local]
E2  GET users                 → 条目 {name,uri,realm,source,email,adminRole,enabled,groups} 全渲染；空组 []
E3  GET users/jane            → enabled:true 恒渲染；POST {"enabled":false} → 登录 401 → {"enabled":true} 复启
E4  DELETE victim（带 token/组员）→ 200 文本体逐字；token 即刻 401；重复删 404 `User not found` 文本体；
    内置 admin 400 / 自删 400 / last-admin 400 逐字（demote 内置后 sole-admin 自删触发）；
    非 admin 403；user.delete 审计 detail {user}；devs 组 E5 视图成员联动清空
E5  groups/devs?includeUsers=true → userNames:["jane"]；bogus → 无键 200；未知组带参 404 `Group not found`；列表不加宽（三字段）
E6  mholder 无 filter → 403（冻结）；?filter=manage → 恰 [t-mh] 全字段；admin 双形态 diff BYTE-IDENTICAL；
    bogus → 400 逐字；jane（空覆盖）→ 403 与无 filter 同形
    DELETE permissions/t-del → 204
```

### e2e（`make console` + go build 嵌入 HEAD SPA；`--project` 定向）

```
e2e/m8/shell.spec.ts        --project=chromium → 5 passed（含 19 条旧路径 404 腿）
e2e/m9/oidc-stepup.spec.ts  --project=m9 BASE=:18098（armed: token_step_up+oidc→mock-idp:18099）→ 4 passed
                            （armed 腿：真 purpose=step_up 跳转 + prompt=login + 真 grant 单次消费 +
                              Bearer 200 + 审计 oidc_reauth + 重放 401 逐字；服务端日志
                              「oidc step-up grant issued user=ssouser ttl=5m0s」在案）
e2e/m9/users-groups.spec.ts --project=m9 → 6 passed（N03 强确认删除三段/E5 级联视图/404 逐字/护栏全过）
```

首轮各 spec 有 3/2/1 例冷加载 flake（首轮 30s 内 asset 冷载），**立即复跑全绿**——与 T-263/T-260 期登记的同类现象，非本票回归（证据：失败腿与内容零相关、第二轮起零失败）。

### 18080（用户实例，M8 期构建，只读 GET）

`GET /api/v1/storage/usage` → **404**（E1 为 M9 端点，里程碑标注与实例代际一致）；ping/version 200 实例健康。未写任何配置。

### make docs（×3，终轮）

`make docs` → SUCCESS，3.09 MB（预算 15MB 内）；**内容级零告警**——onBrokenLinks/onBrokenMarkdownLinks 零输出；仅存两条**既有管线告警**（`docsDir`/`blogDir` doesn't exist——脚手架期即有的 plugin 默认路径提示，与内容无关，docs-site 属 build plumbing〔ADR-0011：tech-writer 不触碰 docs-site/〕）与 npm 依赖树 deprecation（既有）。internal/docs/dist 已随刷新（go:embed 重建归发布步）。

### 交叉链接三向核对（构建产物 id 直验，非目测）

python 抽取 build/**/*.html 的 `<hN id>`：`m9-增补速览` / `删除用户m9-起` / `用户管理风险last-admin-竞窗运营提醒` / `manage-能做什么--不能做什么` / `用户与权限adminsecurity` / `与-token-step-up-联合部署armed-形态` / `旧路径--新路径m9-起不再重定向` / `发布权限语义m8-起` / `set-me-up客户端接入向导` 全部存在——faq↔console↔api-reference↔governance↔npm↔token-step-up↔oidc-config 引用闭环。

## 3. 事实核查对码（写前核对，防编造）

- `auth.oidc` env 白名单：`config.go` splitEnvKey——oidc 子键 env 可达者仅 `AUTH_OIDC_CLIENT_SECRET`（secret）与 `AUTH_OIDC_READONLY_GROUP`；oidc-config.md 原句「其余键只能 YAML」例外清单不全，已修正。
- `seedAdmin`（`metadata/store.go`）：`admin` 行存在即 return，**永不覆盖**——「零 admin 后重种无效」成立（内置 admin 行受护栏 2 保护不可删，互删场景下行恒存在）。
- sqlite 手术 SQL 列名（username/is_admin/role）核实自 seedAdmin INSERT 语句。
- users 404 文本体 vs Artifactory 无 body 404：gap-endpoints §2.1/§2.3 + 活体 Content-Type `text/plain` 双证。
- 「Artifactory 自删可致锁死」**未采信**（reverse 规格 REST 面低置信）——path-map 对照行按「低置信/不可见」如实表述。
- E7 repos 过滤「延后 M10+」：architecture §14.1 E7 定案（rbac-roles 原文「M8+ 评估」已过期）。

## 4. 遗留 / 待确认

1. **18080 用户实例仍是 M8 构建**：旧路径重定向窗口在其上仍生效、E1~E6 不在——文档 M9 语义需实例升级后对其生效（非文档缺口；建议随 M9 收官升级该实例）。
2. **复制管理专篇缺失**（M6 复制 REST 面从未入 user docs/api-reference）：npm 凭据口径暂落 npm.md；若 T-272 终验 F 池对账点名，建议 M10 开「复制管理」篇承载。
3. mock-idp.mjs 在 oidc-config.md 作为本地演练工具引用——它是仓库内 e2e 资产（不随二进制分发），文档已措辞为「仓库自带」；若发布物不含源码仓库，此提示对外部用户不可达（低风险，保留给评估者）。
4. T-257 契约漂移①②（console-m8 设计规格 §4.6/§6.9 回写）归 architect 触点——文档面（console.md）已按落地形态描述，与规格的措辞差由规格侧收敛。
5. docs-site/build 为 tracked 产物随本次重建变更（体积债 T-269 已登记，非本票裁量）。

## 5. 环境清理

scratch 双实例（:18097/:18098）与 mock IdP（:18099）已停（pgrep 零残留）；证据保留 `/tmp/t271/`（armed.yaml、armed.log 含 grant issued 行、server.log、二进制）；18080 未做任何写操作。
