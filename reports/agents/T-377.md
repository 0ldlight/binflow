# T-377 — M13 QA 终验（L01~L24 全量 + DoD 八条 + T-371 行为面 + 遗留处置核对）

- 票：T-377 [P0] QA 终验 `role:qa-engineer` area:全量验收矩阵 dep:全部票 + T-376
- 被测：HEAD `e8aec33`（develop；`make build` 自建——check-size **94.13MB / 100MB** 过门）
- 日期：2026-08-31 ｜ 纪律：**零 git 写操作**（审计只读 log/diff/show/archive；`git archive 7bb3039~1` 出 /tmp 旧源码建 pre-T-371 二进制——只读对象库 + /tmp 写入）；零产品/测试代码改动；测试凭据全驻 /tmp/t377；共享工作树仅新增本报告
- 结论：**PASS**（DoD 八条逐条达标；L01~L24 全绿——一处已知 race 按「串行绿=通过」协议处置；两枚新登记缺陷均非门内项，详见 §8）

## 0. 编排（复用 T-373 夹具形态）

| 实例 | 端口 | 形态 | 用途 |
|---|---|---|---|
| c | 18601 | 无 license | 门控负形态（后期被环境超时误杀，剩余负臂转 e2e 实例完成） |
| p | 18602 | pro + `BINFLOW_REMOTE_CREDENTIALS_KEY` | webhook 主面 / NFR-P58 / nuget D-10 / maven/npm/pypi/docker 客户端 |
| k | 18603 | pro + `allow_private_target` + folder_download + retention=1 | L09 旋钮臂 / L14 / L15（trash cron 每小时 tick） |
| hA / hA2 / hB | 18604/06/05 | pro（hA2 匿名关） | HelmOCI 上游/认证上游/下游；hB 后补凭据钥 |
| conan | 18607 | **preT371 旧二进制 → HEAD 新二进制（同数据目录）** | L17 真升级链（git archive 63eeb8b 构建） |
| e2e | 18610 | 社区档（默认口令） | Playwright 全量宿主（§5 环境教训） |

接收器：recv.mjs（node 双栈——`127.0.0.1:18690` + GUA `[240e:341:…:eef1]:18691`；latin1 原字节 + 头日志 + `/ok /deliv /flaky /fail500 /fourxx /mode/`）。文件服务 ×3（18692 charts base / 18693 手制 index / 18694 Artifactory 形上游）。dind 29.7.2（经典 overlay2 snapshotter——见 §9 环境注记）。真客户端：helm 4.2.4、conan 1.66.0 + 2.31.2（复用仓内 venv）、maven 3.9.16（容器 + file:// 离线插件镜像）、npm 10.x、twine 7.0.0、curl 8.x、Go 1.26（自写 10k blob 压测）。

**承证判定**：T-373 测于 `56b0185`；`56b0185..e8aec33` 间 **internal/ cmd/ 生产代码零变化**（b3dfcf5=纯测试、e8aec33=纯部署面、余为报告/文档）——T-373 的 L02~L13 全量证据按承证口径平移，本票在其面上做 HEAD 二进制复跑压缩验证 + 全部增量面亲测。

## 1. DoD 八条逐条（实测数字归档）

| # | DoD 条款 | 判定 | 证据（实测） |
|---|---|---|---|
| 1 | P0/P1/P2 AC 全绿 + 条件票留痕 | ✅ | 实现票 20/20 done（BOARD）；条件票 T-378 触发兑现（live 四臂 §3.3）；T-379（Q2 symbol server）未触发——BOARD 留痕在案；T-380（Q5）未触发——BOARD 留痕在案；deb bz2（Q7）维持不立项——PRD/BOARD 在案 |
| 2 | §8 剧本全绿 + §1.2 门槛表逐行数字 | ✅ | §2 L01~L24 全绿；门槛表数字归档于 §2/§3/§4（订阅面/事件面/投递/真实消费者/HelmOCI/chartsBaseUrl/_external/旋钮/conan/文面/测试债/运维尾巴） |
| 3 | 回归硬门槛（P0 双形态 + 契约面归属 + 三处反转 PRD 回写） | ✅ | M1~M12 P0 双形态 §6；归属审计 §7（88 文件 100% + 45e73c4 已记账）；PRD §5.4 反转表三行全在（D8 整树删/folderDownload 开关化/D-F2 布局对齐）+ L18 flat/AC2 加注落笔核实 |
| 4 | 前置产物齐备（webhook.md + ADR-0041 回填 + ADR-0042 + Q 终裁归位） | ✅ | webhook.md 298 行在档（官方锚点 + 13 域 66 型）；ADR-0041 决策 4 已按官方值回填（启动行实测 `retry_count:5 retry_wait:10s attempt_timeout:30s`）；ADR-0042 Accepted；Q3/Q4/Q6 归位见 §3.4 |
| 5 | 文档增量交付 + 客户端命令可复跑 | ✅ | T-375 全量复核（§3.5）：`make docs` SUCCESS 零断链（0 broken）；webhooks 页 75.6KB 已挂侧栏；本票真客户端命令全部实测可复跑（§0/§6） |
| 6 | NFR-P58~P60 + race 全树一次绿×2 + lint/gofmt + e2e + footprint/check-size | ✅* | NFR 三条 §4（P59 一枚 24 路极端并发边角缺陷登记，门内 8 路口径零 5xx）；race：T-361 Round C/D 承证 + 本票干净机复跑一轮 **exit=0 35 包零 FAIL**（§4.4——首轮随舰负载红 1 例复跑转绿，如实留痕）；lint **0 issues**；gofmt 空；Playwright 全量 236 passed + 1 已知 race 串行绿（§5）；footprint 11.9MB(vmmap@ready)/100MB + check-size 94.13/100MiB |
| 7 | Non-goals 对账 + ROADMAP「M13 未纳入项」段 + 出处义务 | ✅* | 出处义务全票可审计（webhook.md 锚点逐条）；「M13 未纳入项」段为收口时动作（ROADMAP L166 未勾——conductor/PM 收口笔，非 DoD 缺口，本报告留痕） |
| 8 | 总裁定 PASS → tag m13-done | ✅ | 本报告总裁定 PASS；tag/UAT 随里程碑 PR 归档按票面归 conductor（本票后置动作） |

\* 标注项的执行细节与限定见对应小节；无门内缺口。

## 2. L01~L24 全量判定

**承证面（T-373 在案 + 本票 HEAD 复跑压缩验证）**：L01（规格票走查——T-358 tech-lead 确认在案）；L02~L09 webhook 域、L10/L11 HelmOCI、L12/L13 配置缝——生产代码自 T-373 后零变化，本票在 HEAD 二进制上复跑核心臂全部一致（下表实测）。

| L | 判定 | 本票 HEAD 实测（承证面见 T-373 对应节） |
|---|---|---|
| L02 订阅 CRUD+test | **PASS** | create 201（官方 wire `event_filter{domain,event_types,criteria}`+`handlers[{handler_type,url,secret,use_secret_for_signing}]`；criteria 全维回显 + secret 哨兵 `********`）→ get 200/list 200/PUT **204**（须带 key；event_types 更新生效 [deleted,deployed]）→ test 200 `{"message":"Test successful","ok":true,"attempt":{"status_code":200,"elapsed_millis":27}}` → delete 204 → get **404 "Subscription not found"** 逐字；负臂：未知 event_type 400（`event type "nope_event" is not registered for domain "artifact"`）/ftp url 400/未知 criteria 键 400/**`enabled` 缺省 false=官方规格钉死**（webhook.md L49——建后默认禁用，非缺陷） |
| L03 事件触发 | **PASS** | PUT→七字段信封逐字段（domain/event_type/data{name,path,repo_key,sha256,size=17}/subscription_key/jpd_origin/source=`binflow/binflow@…`/userContext{admin,false,internal}）；sha256 与本地 `shasum` 一致 |
| L04 过滤器 | **PASS** | include `**/*.bin` 三文件全发；**exclude `**/skip-*` 双臂**：hit/f3.bin 发（/deliv+1）、skip-this.bin 不发（count 不增） |
| L05 权限与门控 | **PASS** | plain 403/403；readonly_admin GET 200/POST 403；匿名 401；**community 三缝**：POST 403 + `X-Binflow-License-Required: webhook`（头逐字）+ 读面开放；19 槽清单 `webhook enabled:false` |
| L06 outbox 幸存 | **PASS** | mode=500→两 PUT 201（0.13-0.14s 零阻塞）→首试 500→**kill -9**→ping refused→同数据目录重启→水位行 `outbox_pending:2`→mode=ok→**补投 2/2 零丢**（`binflow_webhook_deliveries_total 2`） |
| L07 重试/死信 | **PASS** | flaky(500,500→200)：3 次到达间隔 **10051ms/10043ms**（固定 10s）；fail500：恰 **5 次**（10058/10041/10056/10058ms）→dead + WARN `attempts:5`；fourxx(404)：**恰 1 次到达零重试**终态 dead + WARN `attempts:1 status_code:404`；死信可查（troubleshooting 环按订阅过滤） |
| L08 签名 | **PASS** | HMAC 态：线上字节（latin1 往返）`hmac_sha256(t377-hmac-secret, body)` == `X-JFrog-Event-Auth` **逐字 MATCH**（7367284c…） |
| L09 SSRF | **PASS** | 默认姿态：loopback `target rejected: … loopback address 127.0.0.1` / 10.9.9.9 + 192.168.1.1 `private_rfc1918`（status_code=0）；旋钮（allow_private_target=true）：test ok:true **且 dispatcher 投递亦达**（/kdeliv+1——双臂闭环） |
| L10 remote 全链 | **PASS** | push digest `sha256:1cbc9057…` → 经 r10 pull 同 digest + tgz sha256 `ee998a7f…` 逐字节一致；上游计数冻结（二/三拉 + by-digest + manifest 探头后 27→27→27）+ `X-Binflow-Cache: HIT`；**认证腿**：上游匿名关（直连 401）+ 仓凭据（hB 补 `BINFLOW_REMOTE_CREDENTIALS_KEY` 后重存）→ pull 同 digest、上游日志 `user=admin` 全链、tgz 一致（⚠️ 凭据在无钥期写入会被丢弃——需带钥重启后重 PUT，WARN 文案在案） |
| L11 virtual 与边界 | **PASS** | remote 域 pull `X-Binflow-Resolved-From: r10` + HIT；local 域 `Resolved-From: v11loc`（无 Cache 头）；tags/list 并集 `["0.1.0","0.3.0"]`（未缓存 0.2.0 不可见——D-2 维持）；push→virtual **405 "No local repository was configured…"** 逐字；混仓 400 逐字（helmoci+docker）；**停上游**：已缓存 manifest+blob 200 HIT、未缓存 404 MANIFEST_UNKNOWN"(upstream unreachable)"、B 零 5xx |
| L12 chartsBaseUrl | **PASS** | 相对 urls index（A/idxup）+ chartsBaseUrl=18692/charts：pull sha256 `b6fa4207…` 与源一致；**18692 计数恰 +1 后冻结**；A 的实体 GET=0；二拉 `X-Binflow-Cache: HIT`；REST 回显 `chartsBaseUrl:"http://127.0.0.1:18692/charts"` |
| L13 `_external`/`_transitive` | **PASS** | 见 §3.2 拼写规则专节 |
| L14 folderDownload | **PASS** | 四态：默认 403 `Download Folder functionality is disabled.` 逐字；开启 200 zip 解包 sha256 逐位一致（ae62babb…/254da98a…）；匿名 401 `You must be logged in…` 逐字；max_files=1 → 400 `Number of files under the path 't377fd/fd' (2) exceeds the max allowed file count for folder download (1).` 逐字；settings 回显两实例（默认 1024/5000/10/false + 14；旋钮 enabled=true + 1） |
| L15 retention | **PASS** | 旋钮实例 trashcan.retention_days=1：回拨 3 天条目被小时 cron 清（trash 树仅剩 fresh.txt；审计 `trash.retention:{"bytes":38,"files":1}` actor=system-trash）；**期内条目幸存**（fresh.txt 在场）；默认 14 回归（inst-p settings 回显 `retention_days:14`） |
| L16 D8 整树删 | **PASS** | conan 2.31.2 双修订上传（rrev 37983e11 + 3a4f2ce5 同坐标）→ **conan 1.66 `remove -f`** → v1 坐标 404 + v2 revisions 404 + **两修订 files 均 404** + DB 残留行=0 + 二次 remove 404 miss 面 |
| L17 D-F2 迁移 | **PASS** | 见 §3.1（T-371 行为面四 AC 全绿） |
| L18 文面核查 | **PASS** | M12 PRD v1.1 flat 八处=「折叠进 copy 主参数族」口径（v1.1 ③ 在案）；artifact-operations.md L66=折叠口径；FR-107 AC2 v1.1 加注在场（boot 探针 fail-closed 解释 + 「加注不改行为、ADR-0040 零修改」） |
| L19 D-10 | **PASS** | live 四臂（§3.3）：④ 新包 201 `Successfully published NuPkg to: t377.pkga/1.0.0/…`；**②b 同字节+w-only → 409 `Package already exist: …` 逐字**；②a 异字节+w-only → 409（byte-blind 构造证明）；③ admin 覆盖 201 |
| L20 race 全树 | **PASS** | T-361 Round C/D 承证（隔离快照背靠背双 exit=0）；本票**干净机复跑一轮 exit=0**（35 包全 ok、零 FAIL——§4.4）；escape 台账 grep 19 命中/5 文件全注释（FR-121.3 可审计） |
| L21 CI 三连+运维尾巴 | **PASS** | CI 承证 T-361（R6/R7/R8 本地逐字复跑 222 passed ×3 + .circleci e2e job 在盘 + Jenkins #2~#10——**e2e job 首跑待 main 合并**，BOARD 在案）；运维尾巴：Playwright 全量 236 passed（§5）；`make docs` SUCCESS 零断链；T-374 侧栏清单/尾斜杠注记复核在案 |
| L22 全量回归 | **PASS** | §6 双形态客户端矩阵 + §7 归属审计 |
| L23 NFR | **PASS**（1 缺陷登记） | §4：P58 零丢+投递 p95=2ms+发布路径不阻塞；P59 万 blob 树 8 路零 5xx（24 路边角 §8-D1）；P60 footprint/cold-start/check-size |
| L24 文档复跑 | **PASS** | T-375 承证 + 本票复跑：docs SUCCESS、五类新文档在场、命令可复跑（本票全程用文档口径命令） |

## 3. 增量面详录

### 3.1 T-371 行为面四 AC（L17——升级链全绿）

**真升级复现**：preT371 二进制（63eeb8b 构建）起服 → conan 1.66 upload（recipe+package）→ 旧树自然落双拼形（DB 实证 `…/0/package/<pid>/0/package/<pid>/{conan_package.tgz,conaninfo.txt,conanmanifest.txt}`）→ 停旧 → HEAD 二进制同数据目录启动。

| AC | 判定 | 实测 |
|---|---|---|
| 启动 sweep 幂等 | **PASS** | 首启 INFO `conan v1 layout sweep: repo=conan-local moved=3 dedup=1 conflicts=0`（钉死文案 + conflicts=0 对账门）；**二启 `moved=0 dedup=0 conflicts=0`** + 节点树逐行 diff 空（TREE-IDENTICAL）；零 conan 仓实例零 sweep 行（冷启动无感，inst-p 日志 0 命中） |
| sha256 三口径对账 | **PASS** | ①折叠映射全等：旧树按 D-F2 映射折叠后与迁移后树逐行（path+size+sha256）一致——全部 FILE 行精确相等；两处规格内例外=脚手架文件夹行 `…/0/package/` 删除（ADR-0042 决策 2 随迁删除）+ 孪生文件夹行 dedup（=INFO 行 dedup=1）；②FILE 节点数 10=10；③blobs/ 目录清单迁移前后不变（10 文件） |
| ref-search 恢复 | **PASS** | 旧二进制上 `settings:{},options:{},requires:{}`（D-F2 饥饿面）；迁移后 `settings:{"arch":"armv8","os":"Macos"}`；`-q` 过滤腿（客户端侧）：`os=Macos` 命中列包 / `os=Linux` "There are no packages … matching the query"；快照键 前缀键 `package/<pid>/…` → **裸文件名**；通道 GET 规格地址 404→200 |
| install roundtrip | **PASS** | 旧服务器上 install 无法解析二进制（D-F2 客户端面——settings 匹配腿饿死，升级动机实证）；迁移后全新缓存 install 成功取二进制：`dl/pkg/…/conan_package.tgz` `ec1c172e…`、conaninfo `0ed215e0…`、payload `0ab4f1de…`、conanfile.py `b0ea503a…` **全部与上传参考字节 MATCH** |

### 3.2 `_external`/`_transitive` 拼写规则复核（T-373 观察② 处置）

同拓扑 + 更精细三分支 live 实测（helm 4.2.4 + curl）：

| 拓扑 | 上游 index 条目形态 | 下游虚仓折叠拼写 | GET |
|---|---|---|---|
| A：上游=手制**绝对外部 URL** index（18693） | `http://…18692/charts/…`（仓外） | **`_external/http/127.0.0.1:18692/charts/…`** | 200 MISS（`X-Binflow-Upstream`+`X-BinFlow-Resolved-From: vdep`）→ 二拉 HIT；storage REST 节点可寻址（checksums.sha256=b6fa4207…）；**helm dependency update 经虚仓成功**（charts/ 落地 sha 一致） |
| B：上游=BinFlow 虚仓（HL-2 **相对 urls**） | 相对 `_external/http/…` | **`_external/http/…`（原样透传）** | 200（T-373 所见形态） |
| C：上游=自引绝对 `_external` 路径（Artifactory 形态，手制 18694） | `<上游基>/_external/http/…`（基内） | **`_transitive/http/127.0.0.1:18692/charts/…`** | **200 MISS→HIT**、字节一致、上游计数冻结 |

**处置结论：无行为缺口，规则与 helm.md §2/§3 逐字一致**——判别式=「成员 charts 基内绝对 URL 且余部以 `_external/` 起 → `_transitive`；基外允许清单命中 → `_external`；相对条目（BinFlow 上游 HL-2 缺省）原样透传」。T-367 与 T-373 所见不同源于上游 index 条目**绝对（Artifactory 形态）vs 相对（BinFlow HL-2）**，非折叠深度；两种拼写均 200 MISS→HIT、冻结行为一致。local 仓两拼写 **400 "external dependency downloads (_external/_transitive) are served by remote and virtual helm repositories only"** 逐字。（`_transitive` 取数=path-joined 引擎链——C 形实测经上游 `_external` 面取到 18692 实体。）

### 3.3 断言反转三处终验复跑（全部独立复证）

| 反转 | 判定 | 实测 |
|---|---|---|
| conan D8 latest 链→**整树删**（T-369） | **PASS** | §2 L16（多修订夹具 + conan 1.66 真客户端 + 零 DB 残留） |
| folderDownload 恒关→**旋钮化**（T-368） | **PASS** | §2 L14 四态（关态 403/匿名 401/超限 400 文案逐字维持——关态文案与 M12 基线逐字一致） |
| D-F2 双拼→**规格布局**（T-371） | **PASS** | §3.1 四 AC |
| （附）nuget D-10 翻转（T-378） | **PASS** | §2 L19 四臂 live（M12 L03 断言反转归属豁免票在 v2full_test 注明） |

### 3.4 Q3/Q4/Q6 终裁归位（AC2）

| Q | 判定 | 证据 |
|---|---|---|
| Q3（D-10→对齐 409） | **实质归位 ✅** | 终裁在 BOARD（2026-08-30 裁决窗）；T-378 done + live 四臂 409 逐字（§3.3）；nuget.md §5.1 D-10 关闭留痕 + §11 对照新行在档。**残留**：PRD LC-56 单元格仍写「待裁」——PM 一行回写未落（BOARD T-378 行已注明「LC-56 回写归 PM」），登记 §8-R3 |
| Q4（webhook 槽=pro+） | **归位 ✅** | slots.go `Kind: KindFeature, MinTier: TierPro`（代码位）+ BOARD 终裁定案行 + live 三缝（community 403+头/pro 201/19 槽 enabled:false） |
| Q6（事件覆盖界） | **归位 ✅** | 注册表 grep 实数 **66 型 = 9 wired + 57 dormant**（13 域；与 webhook.md/ADR-0041 决策 7 单一事实源一致）；订阅校验对未注册型 400（live） |

### 3.5 收口双项复核（AC2 前半）

- **README 双语**：两版 M13 段逐段对译在场（webhook 描述/矩阵 +webhook 行/里程碑行 milestone-13.md 链接）；T-375 修复的英文版 M11 helmoci 误植确认已清。两版现为「M13（进行中）」——**收口 tag 后须翻完成态**（收口笔动作，M12 同式）。
- **文档站随新能力核查**：`make docs` SUCCESS + build 日志 **0 broken**；`build/admin/webhooks/index.html` 在场（75.6KB，侧栏挂载）；五类增量（webhooks.md 298 行 / helm-charts.md +HelmOCI 节+chartsBaseUrl 专节 / conan.md 反转两处+D-F2 标注 / trash-can.md+artifact-operations.md 旋钮翻转 / api-reference+FAQ 增补）全部在档；metrics 参考页 webhook 五族节在场。

## 4. NFR 三条（L23）

| NFR | 判定 | 实测 |
|---|---|---|
| **P58** 投递吞吐/时延/不阻塞 | **PASS** | 100 并发发布：**100/100 入箱零丢**、零错误；投递逐事件滞后 **p50=1ms p95=2ms max=2ms**（20 事件逐对测：PUT 完成→接收器到达，同机钟）；发布路径不阻塞：A（订阅活）p95=6.26s vs 无订阅对照 B1/B2/B2' = 6.08/6.45/6.39s——**A 落在对照噪声带内（偏差≈0）**；~5-6s p95 地板=100 路并发新建文件 PUT 的串行化持久层（fsync）地板，**无 webhook 时同样存在**（对照列），非投递链阻塞。burst 期投递与发布同跑（100 事件在 4.64s 窗内全部到达） |
| **P59** HelmOCI 回源 | **PASS**（1 边角登记） | 首拉口径：helm 客户端单流首拉与经典 remote（chartsBaseUrl 腿）同数量级（亚秒级）；**万 blob 缓存树**：上游种 10000 manifest（唯一 blob 配置+层）→ 经 remote 全量首拉：**8 路并发：10000 拉取 5xx=0、2xx=9999、4xx=1（=种树侧自身 1 次 400 miss，非 remote 面）、p50=184ms p95=837ms**；24 路极端并发：5xx=27/10000（0.27%，SQLITE_BUSY——§8-D1 登记，均可重试） |
| **P60** 资源门 | **PASS** | `make footprint`：vmmap physical footprint **11.9MB@ready / 14.1MB@idle60s**（预算 100MB）+ 探针自带冷启动 722ms（<2s）；冷启动三连 **0.618/0.201/0.197s**；check-size **94.13MB/100MiB**（webhook 引擎未破 M12 门） |

### 4.4 race 全树（L20 承证+复跑）

- **承证**：T-361 Round C/D（隔离快照背靠背，双 exit=0、35 包 ×2 零 FAIL、无隔离复跑辩护）。
- **本票复跑**：首轮在满编排（7 实例+接收器+压测）下跑——`internal/repo` 包 `TestBigTreeCopyNo5xx` 预算臂超时（**64.76s > 45s race 档预算**，机器负载放大 wall-clock；33 包 ok 仅此一臂红）。**干净机（全部停清后）复跑一轮：exit=0、35 包全 ok、零 FAIL**（/tmp/t377-maketest-race2.log）。结论：机制面维持 T-361 定标，负载敏 rumor 如实留痕——重负载期跑全树须让机。

## 5. Playwright 全量（L21）

- 宿主：scratch 社区档实例（默认口令）。**首轮误用 pro license 宿主 → 132 红**（登录/授权面 + trash-locked 卡/L27 community 地板/a11y 路由面——**suite 的成文前提=纯净 community 实例**，T-374 L3 登记的同一条，环境教训非产品面）。
- **正确宿主全量：236 passed / 24 skipped（自门控腿）/ 1 failed / 11.6m**。唯一红=`repositories-admin.spec.ts:101`（three-tab + axe）——**串行复跑 ✓ 绿（30.0s）**：T-374 已根因的 hover 对比度 4.41:1 光标几何 race（登记缺陷 L2，修复票建议在案）——按「串行绿=通过」协议处置，非新缺陷。
- m13 面随全量绿（t366-webhooks/t372-trash-node 消费者/树节点腿在 236 内）。

## 6. M1~M12 P0 双形态复跑（L22 前半）

| 面 | 正形态（pro） | 负形态（community） |
|---|---|---|
| docker（dind 29.7.2 经典 snapshotter） | push digest `sha256:4648a150…` → **全新 dind 拉回 RepoDigest 同值** → `docker run` 执行（binflow-server --help 输出=真运行） | —（helmoci/docker 建仓门见负形态行） |
| maven（3.9.16 容器） | deploy-file 上传 jar+pom+metadata（storage 节点六件套含 .md5/.sha1）→ **fresh .m2 消费者工程解析 + 编译 + 执行 "hello from binflow t377 maven"** | — |
| npm（host 10.x） | `npm publish + @t377/qa-pkg@1.0.1` → install 388ms + `require()→t377` → **清 _cacache 重装** 414ms 同绿（registry 行带尾斜杠——npm.md 口径） | — |
| pypi（twine 7.0.0） | 上传 whl+tgz → `pip install --index-url BinFlow` → `import` 恒等 | — |
| conan | D8/D-F2 全链（§2 L16/§3.1） | — |
| nuget | D-10 四臂（§3.3） | — |
| curl generic | 全程 roundtrip + sha256 对账；trash restore 面（T-373 承证+本票 L15 cron 面） | — |
| license 门 | 19 槽/webhook/helmoci/repo-operations 门（pro 侧全通） | **helmoci 建仓 400 "package type 'helmoci' is not available (license tier…"** / webhook 订阅 403+`X-Binflow-License-Required: webhook` / folderDownload 403 `license required: addon 'repo-operations'…` / `webhook enabled:false` |
| 自动化 | `make test`（race）35 包 exit=0；lint 0 issues；gofmt 空；footprint/check-size 绿 | |

## 7. 契约变更面归属审计（L22 后半——T-373 审计更新复跑）

`git diff m12-done..HEAD -- internal/ cmd/`：**14 提交 / 88 文件**。逐提交→票号映射（脚本核对）：
- T-373 审计的 13 提交 81 文件原样 + **增量 1 提交 b3dfcf5（T-361）6 文件**（deb/repo 测试 + race_*_test.go 构建标签对 + .circleci——全部测试面）。
- **唯一无票号提交仍是 45e73c4**（style: De Morgan，internal/repo/operations.go 一处布尔式）——conductor 已记账（M12 seam lint 追补，P2）；该文件同时被 T-362 触及，文件级不孤儿，无纯孤儿文件（脚本验证 owned-ONLY-by-no-ticket = ∅）。
- **结论：100% 归属 M13 票号 + 1 已记账 style 提交；无新孤儿。**

## 8. 缺陷与登记（本票新发现）

| # | 级 | 缺陷/登记 | 复现与建议 |
|---|---|---|---|
| D1 | **P2** | **remote 缓存树 24 路并发首拉 SQLITE_BUSY 500**：万 blob 首拉 24 workers → 27/10000（0.27%）`upload-sessions create: metadata: store busy: database is locked (5) (SQLITE_BUSY)` → 500（每例先经历 ~17s busy 重试窗；客户端可重试恢复）。8 路并发同树 **5xx=0** | 复现：`/tmp/t377/bench/nfr59`（WORKERS=24 + 独立 remote key）。建议：metadata busy 超时/重试预算随并发档提升，或 cache-fill 写路径降级排队；登记 M14 候选（单写者矩阵内、门内口径 8 路已零 5xx） |
| D2 | P3（环境注记） | dind **containerd snapshotter** 形态下 docker pull 对 plain-HTTP registry 的 blob 取数走 https 回退（--insecure-registry 不被该 resolver 遵守）→ 拉取超时；经典 overlay2 snapshotter 同实例全链绿；BinFlow 全程未收到该 https 请求（访问日志零到达） | 客户端侧已知 quirk；文档可选注记（dind 调试建议 `--feature containerd-snapshotter=false`） |

## 9. 遗留处置态核对表（登记池逐项）

| 来源 | 事项 | 处置态（本票核验） |
|---|---|---|
| T-364 §5-① | audit `webhook.dead_letter` 入词表 | **已清**（governance.md L45 webhook M13 族五行含 dead_letter，detail 字段齐） |
| T-364 §5-② | metrics 注册位平移 | **已裁维持现位**（conductor，BOARD 收口登记行）；/metrics 实见五族（本票 L07 对账） |
| T-364 §5-③ + T-366 §4-2 | Replay REST / outbox 行级 REST | **开放（候裁池）**——机制/排障环在档；webhooks.md/FAQ 如实标注「当前恢复=重新触发源事件」；不阻塞（死信可查+重发面归后续小票） |
| T-364 §5-④ | disable 快照契约钉死 | 开放（登记维持） |
| T-364 §5-⑤ | 3xx 终态 V4 | 开放（登记维持） |
| T-366 | Jenkins dogfood 条件腿 | 未触发（用户环境）——容器/脚本接收器腿已交付并本票复验；BOARD 留痕非 DoD 缺口 |
| T-373 观察① | 45e73c4 无票号 style | **已记账**（conductor；本票审计复核维持） |
| T-373 观察② | `_transitive`/`_external` 拼写 | **本票关闭**（§3.2 规则钉死：绝对自引 `_external` → `_transitive`；基外 → `_external`；相对透传；两拼写行为一致、无缺口） |
| T-373 观察③ | /v2 remote 不织入 artifact/cached | **开放待 architect**（seam 边界与 T-362 §1-4 织入位点一致；无人在 BOARD 裁定——移交 conductor 转 architect 确认覆盖界） |
| T-373 §3-5 | 环境限制（docker.io/central 出网、nuget.exe 缺席） | 维持同状（本票同姿势：dind save|load / maven file-mirror / nuget curl 四臂等价） |
| T-374 L1 | npm 交互式 login 被内容面匿名 PUT 门拦 | 开放（服务端小票建议在案；npm.md 已如实标注） |
| T-374 L2 | 仓库表 row-link hover 对比度 4.41:1 | 开放（FE 小票建议在案；本票全量再现一次 + 串行绿——协议处置 §5） |
| T-374 L3 | playwright 纯净实例假设未成文 | **本票再次实证**（pro 宿主 132 红→community 宿主 236 绿）；随 L3 建议归 CI/README 一行注明 |
| T-376 | helm uninstall 连删 PVC | **已裁 M14 候选池**（conductor） |
| T-376 | 启动日志 "defaults (no binflow.yaml found…)" 措辞失真 | 开放（cmd 一行级顺手票建议） |
| T-376 | e2e job 首跑 + UAT 首跑 | **程序性待里程碑 PR**（T-376 §4.3 清单在案；本票 PASS 后 conductor 开 PR 触发） |
| T-376 | buildkit VM goproxy 不可达 | 环境注记（CI 两链不受影响） |
| 本票 R3 | PRD LC-56 单元格「待裁」未随终裁回写 | PM 一行（终裁实质已三面归位：BOARD+spec+live） |
| 本票 R4 | README 双语 M13 段「进行中」待收口翻完成态 | conductor 收口笔（tag 后） |
| 本票 R5 | ROADMAP「M13 未纳入项」段收口时建（DoD#7 字面） | conductor/PM 收口笔（L166 未勾在案） |

## 10. 清理

t377 全部实例/接收器/文件服务/dind 已停（13 端口复核全 DOWN；pgrep 复核零残留——仅他票既有 port-forward）；helm 三 registry 凭据与 repo 条目、conan 两 remote（CONAN_HOME/USER_HOME 全在 /tmp/t377）、npm .npmrc/venv、maven scratch 均驻 /tmp/t377（**保留供复验**——含 nfr59 压测器与三份 race/全量日志；如需释放整目录可删）；共享工作树仅本报告一个新文件、零 git 操作、零代码改动。

## 11. 总裁定

**PASS**——DoD 八条达标（§1）；L01~L24 全绿（§2，含三处反转+T-371 四 AC 独立复证）；Q3/Q4/Q6 实质归位；遗留池无门内项。两枚新登记（D1 极端并发边角、D2 环境注记）与五项收口笔（R3/R4/R5 + e2e/UAT 随 PR）移交 conductor。建议：conductor 打 `m13-done`。
