Ticket:       L015-3 — internal/client 存量失败票（失败用例 T-231/T-233）+ P1
Role:         dev-go-core（area 实例：internal/client，根因修复落 internal/metadata）
Area:         internal/client（票面）；根因位 internal/metadata/substores.go（越界前已按「修在根因位」授权：LIKE 转义 bug 在 metadata 层）
Input:        L014-2 终轮全包清查（两用例 b4de1ef5 前置即红，非当票回归）；internal/client/realstack_test.go（T-189/T-231 断言）；internal/httpapi/storage.go listFile 注释（L008-3 §1 items 4/9 证据）；commit 53522a6c（L009 list 形状五包）与 b4c73a56（L011-1 ResolveMeta）提交说明
Changes:      1) 根因定性（worktree bisect）：53522a6c 起 ?list 条目 uri 带前导斜杠（32 臂双系统矩阵证据的行为变更）→ 测试断言过期；b4c73a56 起 item-info 改走 ResolveMeta→ListByPrefix，暴露 likePrefix 的 exact 臂拿 LIKE 转义后文本参与字面 `=` 比较（`%`→`\%` 不等）→ 含 % 全路径前缀 0 行 → 404。
              2) internal/metadata/substores.go：likePrefix 的 exact 返回改为 RAW（trim 前导 / 后原样），转义只保留给 LIKE 子树臂——ListByPrefix 与 DeleteByPrefix 两个调用点同修（共享 seam 一处修）。
              3) internal/client/realstack_test.go：T-233 与 T-231 五腿的 files[].uri 断言翻面为前导斜杠拼写（"/hello.txt"、"/"+base），失败文案同步。
              4) internal/client/artifact.go：ArtifactListEntry 文档注释同步 wire 契约（uri 前导斜杠，L009 five-pack）。
Files:        修改：internal/metadata/substores.go、internal/metadata/store_test.go、internal/client/realstack_test.go、internal/client/artifact.go、reports/agents/T-L015-3.md。新增/删除：无。
Tests:        internal/metadata/store_test.go：TestListByPrefix 新增 4 腿（percent 全路径 exact=T-231 D-1 复现、percent 目录、underscore 文件 exact、underscore 目录不野扩进 underXscore）+ 种子 3 行（% 文件、_ 文件、underXscore 兄弟+keep.txt 子节点）；TestDeleteByPrefix 新增 percent 全路径删除臂（pre-fix 会漏删）。internal/client/realstack_test.go：断言翻面（非新增）。
Commands:     go test ./internal/client/ -count=1
              go test ./internal/metadata/ -count=1
              go test ./internal/metadata/ -count=1 -run 'TestListByPrefix|TestDeleteByPrefix' -v
              go build ./... && go vet ./internal/metadata/ ./internal/client/ && gofmt -l internal/metadata internal/client
              $HOME/go/bin/golangci-lint run ./internal/metadata/... ./internal/client/...
              go test ./... -count=1
Outputs:      复现：TestClientAgainstRealServerStack FAIL（uri "/hello.txt" want "hello.txt"）+ TestArtifactSpecialPathsRealStack 5 腿 FAIL（4 腿 listing 前导斜杠、percent 腿 GetArtifactInfo 404 "node .../percent%.txt/: node not found"）。
              bisect：53522a6c^ (03396aa1) 两测 ok；53522a6c 全部仅 listing 断言红（percent 腿 listing 正确显示 percent%.txt）；6cb887ba 同；b4c73a56 起 percent 获 404 于 414 行 → 双断点定性成立。
              独立复现（HEAD 上 metadata 探针）：ListByPrefix("r", "t231/percent/sym'bols$/percent%.txt") 对同路径行返回 0 行。
              修后：internal/client ok 10.3s；internal/metadata ok 102.9s；TestListByPrefix/TestDeleteByPrefix PASS；build/vet/gofmt 空；golangci-lint "0 issues."；全树干净串行复验 go test ./... -count=1 → exit=0、ok×39、FAIL×0（41 包中 2 包无测试文件；首次全树跑有两例负载敏感门红——aqlperf P95 646ms>500ms 与 couch storm 并行判定——系与并发 golangci-lint 抢 CPU，单独复跑两包均 PASS：P95 8/32/48ms 三档全绿、storm ok 1.3s，与本票改动无关，AQL 亦不经过 ListByPrefix）。
Compatibility: 契约面：?list 条目 uri 前导斜杠与 docs/reverse/rest-compat L008-3 §1 items 4/9（listFile 注释内引）一致——实现未动，仅测试翻面对齐既有行为；likePrefix 修复让 ListByPrefix/DeleteByPrefix 对含 %/_/\ 路径的字面前缀语义与「精确+子树」文档契约一致（此前 exact 臂对含通配符前缀静默漏配）。无契约漂移。
Security:      无新增输入面。修复属查询正确性：此前含 % 的精确前缀查询静默 404（信息面无泄漏）；DeleteByPrefix 同 bug 可致按前缀递归删除漏删精确节点（修复后删除语义完整）。无凭证/日志面改动。
Performance:   无（同一 SQL 形状，参数侧少一次 Replacer 拷贝；无测量需求）。
Risks:        likePrefix 行为变更影响所有 ListByPrefix 调用方（repo.List、ResolveMeta、GC、复制、归档等）：exact 臂从「转义后必然 miss（含通配符时）」变为「字面命中」——全树测试绿为证；理论上存在依赖旧漏配行为的调用方（未发现）。
Blockers:     无
Next:         无强制后续。可选：reverse 文档 rest-api.md section 3 的 ?list 条目形状若仍写无斜杠拼写，属 reverse-engineer 文档刷新项（L009 提交已述 listFile 注释为证据源）。
