# T-327F — License 页 addons 表 spec 滞后修正（L27a 裁定执行）

- 状态：done（待 conductor 收口）
- area：`web/e2e/m10`（单一 spec 文件）；锚册零改动
- 裁定依据：reports/agents/T-327.md §7-1——产品正确、spec 滞后；`GET /api/v1/addons` = 15 槽

## 1. 事实核对（改动前）

- 后端装配序实态 `internal/addons/slots.go`（15 槽）：
  generic/docker/maven/npm/pypi（core）→ **go/nuget/cargo/conan/helm/rpm/debian（7 个
  Kind=package-type + MinTier=pro 门控）** → properties（community）→ ha/xray-integration
  （enterprise）。与裁定书 15 槽矩阵逐 id 一致。
- 前端为单源数据驱动（`web/src/lib/addons.ts` FR-86-AC2）：License 矩阵表与建仓对话框
  包型网格均实时吃 `GET /api/v1/addons`，无前端槽位硬编码（grep conan/debian 在
  web/src 零命中）——故 spec 修正后无需任何 src 改动。
- 滞后点唯一：`web/e2e/m10/console-license-addons.spec.ts` L60 `toHaveCount(11)`（含
  L13/L57 两处注释）。e2e 全目录 grep 11 槽引用无第二处；m10 README 无计数文字。

## 2. diff（web/e2e/m10/console-license-addons.spec.ts，唯一变更文件）

1. 头注 posture：`GET /api/v1/addons = 11 slots (T-282 manifest)` → `15 slots
   (T-327F ruling; … grown by the M11 package-type tickets)`，门控包型描述自
   go/nuget/cargo 扩为七个。
2. 常量：`PRO_PKG` 自 `['go','nuget','cargo']` 扩至 `+ ['conan','helm','rpm','debian']`；
   新增 `NEW_PRO_PKG = ['conan','helm','rpm','debian']`（新四型逐行断言腿用）。
3. 行数断言：`toHaveCount(11)` → `toHaveCount(15)`；注释改「T-327F 裁定后 = 15」。
4. **四新包型行断言**：L27a 新增循环——`addons-row-{conan,helm,rpm,debian}` 内
   `.badge` hasText `package-type`（kind 徽章 wire 值）。minTier=pro 标注由既有
   `PRO_PKG` 扩容自动覆盖：`addons-tier-<id>` toHaveText('pro') +
   `addons-state-<id>` toContainText('需要 pro')。
5. 扩容连带：L27d（建仓面对话框/表单）与 L27c（readonly 反断言）经 `PRO_PKG` 常量
   同步覆盖新四型——语义正确（同 wire 注册表驱动），实测通过。

## 3. 锚册（console-ux §10）核对

- §10 T-288 批次在册锚为动态族形：`addons-row-<addonId>` / `addons-tier-<addonId>` /
  `addons-state-<addonId>`——动态段天然覆盖新四型 id，**零册改**（裁定预判成立）。
- anchor-audit 闸门输出（`node scripts/anchor-audit.mjs --ledger`，web/ 下）：

```
== ledger 断言（A1 unregistered=0 / A2 broken=0 / A3 retired⊆退役表∪白名单 / A4 退役表∩src=∅）==
  PASS（registered 与册一致：unregistered/broken 双零，retired 全部在表）
```

（dead 桶 4 家族为处置队列信息视图、非门，且均非本票锚族，未触碰。）

## 4. 闸门证据（全绿）

| 闸门 | 命令 | 结果 |
|---|---|---|
| tsc | `cd web && npm run typecheck` | 0 错误（静默通过） |
| lint | `cd web && npm run lint` | 0 告警（静默通过） |
| 锚册对账 | `node scripts/anchor-audit.mjs --ledger` | PASS（见 §3） |
| e2e 相关组 | fresh 实例 + `npx playwright test --project=m10 m10/console-license-addons.spec.ts` | **5 passed (7.5s)** |
| 邻接回归 | 同实例 + license-lifecycle / gating-matrix 两 spec | 2 skipped（占位骨架，设计态） |

fresh 实例纪律：`find cmd internal go.mod go.sum tools.go -type f -newer bin/binflow-server`
初查非空（internal/docs/dist 滞后）→ `make build` 重建 → 复查 0 滞后；`./bin/binflow-server
serve`（127.0.0.1:8080，console probe 200）→ 跑组 → pkill 收尾（端口确认释放）。

## 5. 契约漂移

- architecture §15.2.5 / license.md 的 15 槽矩阵与实态一致（裁定已确认）；本票为 spec
  侧滞后修正，**无新漂移**。
- 附注（非本票 area）：spec 头注原引「T-282 manifest = 11」为当时实态，M11 T-308~T-311
  扩容后未同步——根因是 spec 注释引用了具名 manifest 计数而非闭集槽位族，已在新注记中
  改为引裁定（T-327F）。

## 6. 遗留

- 无（git 操作按纪律留给 conductor：共享工作树含他票 in-flight 改动）。
