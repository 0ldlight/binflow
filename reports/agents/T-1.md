# T-1 工作日志 — M1 里程碑 PRD

- **票据**: T-1 「M1 里程碑 PRD」 `role:product-manager` `area:docs/prd`
- **执行者**: product-manager
- **日期**: 2026-08-17
- **状态**: done（v1.1 已回写用户对 8 项开放问题的定案；等待逆向规格落地后按 §5.5 增量校准）

## 做了什么

1. 通读 PRODUCT.md（愿景 / Non-goals / 成功标准 / 技术约束）、ROADMAP.md（M1 全部条目）、BOARD.md（只读，当前 todo 为空）、DECISIONS.md（ADR-0001~0004）、docs/reverse/README.md（规格清单排期：M1 应产出 4 份规格，尚未产出）。
2. 撰写 `docs/prd/milestone-1.md`（Draft v1.0），逐条覆盖 ROADMAP M1 的 7 个交付条目（脚手架 / 存储引擎 / 仓库模型 / Generic 本地仓库 / 认证权限骨架 / 开发环境 / QA+README）。

## 产出文件

- `/Users/lzw/dev-center/docs/prd/milestone-1.md`（本票唯一产出；ROADMAP.md 本次未动，待本票 qa 通过后由主会话决定是否更新里程碑条目）

章节清单：背景与目标（含量化门槛表）→ 范围（In scope 映射表 + 两级 Non-goals）→ 用户与场景 → 功能需求 FR-1~FR-6（每条用户故事 + 可执行 AC，标注 P0/P1/P2）→ 兼容性矩阵（层级定义 / E-01~E-26 端点表 / C01~C30 验收命令 / 后续协议边界 / 待校准项）→ 非功能需求（性能/安全/可观测性）→ M1 验收剧本（10 步）→ DoD → 开放问题 Q1~Q8。

## 关键决策（PRD 内部口径，均已在文中标注理由）

1. **M1 端点兼容 26 条分四层**：兼容/兼容子集 18、`/api/v1` 3（health / storage/stats / permissions）、有意不兼容 5（remote|virtual 建仓 400、X-Explode-Archive 拒绝、未实现端点 404+标准错误体等）。所有非 2xx 统一 E-01 错误体 `{"errors":[{status,message}]}`，消除模糊地带。
2. **权限对象走 `/api/v1/permissions`**：Artifactory 对应端点在 `/api/v2/security/permissions/*`，M1 只做骨架，不承诺原路径；M4 权限完整版再评估是否补 v2 路径。
3. **验收命令先行**：C01~C30 直接给到命令行与期望输出（状态码 / sha256 对账 / jq 断言），qa-engineer 可原样转测试脚本；macOS 与 Linux 的 `stat` 差异已标注。
4. **逆向规格并行风险的处置**：Artifactory 行为描述暂以公开文档为据并逐条标置信度；DELETE 成功码、checksum-deploy 未命中码等 6 项列入 §5.5「待校准项」，规格落地后回写 PRD，不阻塞开发。
5. **防蔓延边界**：Non-goals 分「产品级（继承 PRODUCT.md）」与「M1 里程碑级」两层，每个不做项都给了归属里程碑与 M1 的隔离边界（返回码级），例如 rclass 仅接受 local、`/v2/**` 404。
6. **M2+ 前向兼容需求显式化**：路径命名空间预留、packageType 分发点、认证中间件按前缀挂载三条作为对 architect 的需求输入（非实现指令）。

## 开放问题（需用户决策，共 8 项，详见 PRD §9）

- Q1 兼容端点是否仅挂 `/artifactory` 前缀（不做根路径镜像）
- Q2 匿名读默认关 vs Artifactory 传统默认开
- Q3 admin 初始口令引导方式（环境变量 / 随机打印 / 强制改密）
- Q4 `/api/system/version` 如实返回 BinFlow 版本 vs 伪装 Artifactory 版本
- Q5 Postgres 可选支持未排里程碑（ADR-0003 说「可选」，ROADMAP 未落）
- Q6 Range/断点续传 P2 是否提前（M2 docker blob 依赖）
- Q7 Go module 路径 `<org>` 未定
- Q8 根 README.md 唯一写入者未在文件地图指定

## 自测与边界声明

- 工作范围严格遵守 `docs/prd/`：未改 BOARD.md（只读）、未改 ROADMAP.md（按票据指示本次不动）、未改 PRODUCT.md。
- 本票为文档票，无可执行自测命令；文档内部一致性（AC ↔ C 命令 ↔ E 端点交叉引用）已人工核对。

---

## 修订 v1.1（2026-08-17，T-1 续：8 项开放问题定案回写）

主会话转达用户对 v1.0 §9 全部 8 项开放问题的定案，整体重写 `docs/prd/milestone-1.md` 为 v1.1（新增 §0 修订记录）。变更明细：

1. **Q1 统一 `/binflow` 前缀**（推翻 v1.0 暂行假设「仅 `/artifactory`」，影响最大）：
   - 新增 §4「路径前缀总则」：兼容端点 `/binflow/api/...`、内容路径 `/binflow/<repo>/<path>`、自有端点 `/binflow/api/v1/...`；不用 `/artifactory`，不做根镜像。
   - §5.1 兼容层级定义改为「前缀重写后语义兼容」（判定标准 = 脚本仅改 base path 即可通过）；E-01 错误契约补充 `/artifactory/**` 返回 404 且 message 提示新前缀。
   - §5.2 端点表 E-02~E-26 全部路径替换为最终路径；§5.3 C01~C30 全部命令 URL 替换。
   - §3 场景 D 迁移表述修正：成本 = base path 一次替换（`/artifactory` → `/binflow`），不再宣称「只改域名」。
   - §2.2 Non-goals 新增「`/artifactory` 前缀仿真与根路径镜像：不排期」；C24 新增 `/artifactory/api/system/ping` 与根路径 `/api/system/info` 的 404 断言。
   - §5.4 记录协议例外：Docker `/v2/` 为客户端固定路径，不挂 `/binflow`，M2 由部署层反代/端口路由解决。
2. **Q2 匿名读默认开**（推翻 v1.0 暂行假设「默认关」）：
   - FR-5 模型改写：内容 GET/HEAD 匿名可读；写与管理 API 恒需认证；`security.anonymous_access` 默认 true。
   - 新增 FR-5-AC12（匿名读默认开：匿名 GET 200 / 匿名 PUT 401 / 匿名管理 API 401）与 FR-5-AC13（关闭后 401+WWW-Authenticate，并在该模式下验证 read 权限差异化）。
   - 新增命令 C23（匿名读三断言）与 C27（关闭匿名读四断言，含 ci-bot read 200 / 越权 403）。
   - FR-5-AC7/AC8 表述调整：匿名读开启时不能用「GET 403」区分未授权用户，read 验证移到关匿名模式；E-04/E-09/E-11~E-14/E-20 标注认证边界。
   - 新增 NFR-S8（匿名读暴露面：README 必须明示默认值 + 关闭方法 + 非可信网络建议关闭）；§6.3 日志 user 字段匿名记 `anonymous`。
   - §7 剧本第 6 步扩为 C02→C20→C21→C22→C23→C27。
3. **Q3 admin 口令**（定案=暂行假设）：环境变量 `BINFLOW_ADMIN_PASSWORD` 优先，缺省 `password` 仅限评估；FR-5-AC1 落定。
4. **Q4 version 端点**（定案=暂行假设）：如实返回 BinFlow 版本 + `product:"BinFlow"`；E-03 落定，C28 断言 `.version,.product`。
5. **Q5 纯 Go SQLite 零 CGO**（新信息：Derby/H2 讨论已否决）：新增 FR-1-AC7（`CGO_ENABLED=0 go build ./...` 退出码 0）、FR-3 技术注记（`modernc.org/sqlite`）；FR-3-AC10（Postgres 检测报错退出）维持；§7 剧本第 1 步纳入 AC7。
6. **Q6 Range 维持 P2**（定案）：FR-4-AC14 标注定案 + M2 前置提醒。
7. **Q7 module 路径**（定值）：FR-1-AC5 落定 `github.com/lzwzzy/binflow`。
8. **Q8 README 归属**（定案）：devops-engineer 起草、tech-writer M5 收编；写入 FR-6-AC4。
9. §9 由「开放问题（暂行假设列）」改为「已决决策」表（问题 + 决策两列），8 项全部关闭；文头状态改为 v1.1。

一致性核对：C 命令 ↔ AC ↔ E 端点引用关系随路径替换同步更新；未发现残留 `/artifactory` 用作 BinFlow 自身路径（仅存在于「Artifactory 对应路径」说明与 C24 的 404 断言中，属有意保留）。

v1.1 边界声明：仅改 `docs/prd/milestone-1.md` 与本日志；BOARD.md / ROADMAP.md / PRODUCT.md 未动。
