Ticket:        L001-4 [P0] remote-cache-v2 + storage-v2 双设计文档（Phase 1 架构族，LOOP 001）
Role:          architect
Area:          docs/design/remote-cache-v2.md（新）+ docs/design/storage-v2.md（新）
Input:         派发指令（L001-4）；实读 reports/compatibility/L000-docker-remote-evidence.md（E1/E4/E5 全文）+ L000-docker-remote-diff.md（含复验段）；docs/reverse/storage/binary-provider-chain.md 全文；docs/reverse/protocols/registry.yaml docker/helmoci/npm 条目；docs/compatibility/known-divergence.yaml 首 6 条目；docs/ai-engineering/artifactory-binflow-gap.yaml storage-filestore/db-persistence 条目；DECISIONS.md ADR-0006/0007/0012/0013/0019/0031/0036/0039/0040（节选精读）；as-built 只读：internal/remote/{fetcher,cachestate}.go、internal/repo/{api,dockerremote,trash}.go、internal/adapter/{api,metadata}.go、internal/adapter/docker/remote.go、internal/storage/api.go、internal/metadata/migrations/sqlite/002_docker.sql、internal/httpapi/router.go、internal/config/api.go
Changes:       文档 1（remote-cache-v2）：四决策点 D1 门控状态存哪（A 文件翻译/B docker_refs 复用/C 新表 → B）+ D2 SPI 位置与跨协议面（repo.DigestChainGate optional facet；digest-addressed 闭集 {docker,helmoci}）+ D3 负缓存（保留+INTENTIONAL，附 Artifactory 跨 missedTTL 复测义务）+ D4 检索窗 21600s 包型默认/INM 恒 200 契约锚/上游条件 GET 304 透传；含 ADR 建议稿（§7）、实现票拆分 5 张（§8）、验证锚（§9）。文档 2（storage-v2）：25 行裁定总表（KEEP 14/REFACTOR 2/INTENTIONAL 5 项/UNSUPPORTED 6/UNKNOWN 挂账 3/技术债 1）+ 数据完整性 P0 八项 as-built 锚 + 接口级改造清单（storage.Engine/Backend/Session/GCMarker 零改动；仅 prune/gc 管理面 P2 + 包型检索窗 P1 跨域）+ ADR 建议稿（§6）
Files:         docs/design/remote-cache-v2.md 新增（~9.5KB）；docs/design/storage-v2.md 新增（~11KB）；本日志
Tests:         结构门：python 表格列数解析器两文档零不一致（贴「表格OK」×2）；引用门：21 个被引文件存在性全 OK（internal/repo/dockerremote.go 等）+ 行号锚漂移检出（remote.go 并行李改动 795→1051）→ 三处行号引用改函数名锚（抗漂移修正后 go 行号引用残留 = 0）；接口门：DigestChainGate 建议稿独立 module gofmt -l 零输出 + go vet 通过（/tmp/l0014）；出处门：每项行为断言带 E 等级+版本（E1/E4@7.161.20 rev 86120900、E5@BinFlow rev 59f33ab5）或 binary-provider-chain 节-条目+证据基+置信度
Commands:      grep ADR 标题/条目（DECISIONS.md 382.8KB 超 256KB 读限 → 分节读）；grep -n func serveRemoteBlob/remoteManifestRefs（漂移定位 1051/1112/1207）；python heredoc ×4（表格校验/文件存在/锚修正/重校验）；gofmt -l + go vet -C /tmp/l0014（零输出通过）；hook 侧记：两次 bash 被 PreToolUse make lint 撞并行 golangci-lint 锁终止，改 python/无 cd 形态重跑成功
Outputs:       ADR 建议稿 2 份（remote-cache-v2 §7 门控四决策 / storage-v2 §6 存储链终案——均未写 DECISIONS.md，归正式流程）；接口契约 1 个（repo.DigestChainGate.BlobInChain，语法+vet 验证）；实现票建议 7 张（remote 族 5 + storage 族 2）
Compatibility: docs/compatibility/contracts/ 为空（无 VERIFIED 契约可冲突）；与 known-divergence.yaml 对账：C10（BUG）本设计即其 review_gate 所指设计票（修复路径闭环）；C14 给 INTENTIONAL 转 ADR 前置义务；C05 拆解为 library/ 归一（行为，随错误形态票）与缓存树布局（单独立裁，倾向 INTENTIONAL）——不绕开、显式拆解留痕；C08/C17 建议契约化
Security:      文档 1 §3 门控 oracle 走 read-gate（Principal 复用 plane 面纪律）；盲代理收敛本身是上游扫描面收窄（少打未知 digest 到上游）；smart remote 凭据/事件复制面显式划出域外（ADR-0021 边界维持）
Performance:   门控查询 = docker_refs 单列索引 idx_docker_refs_blob 回行过滤（同 digest 跨 image 稀少），实现票 EXPLAIN QUERY PLAN 实测 + 必要时复合索引（§2.1-3）；GCSweep 零改动（现有 hold-set/Live 模型维持）
Risks:         ① refs 记录 best-effort ⇒ 门控耦合（record 失败 → blob 本地 404，与 Artifactory marker 写失败耦合等价，WARN 可观测性升级列入实现票）；② 并行实现票在动 internal/adapter/docker/remote.go（行号漂移实证），设计引用已函数名锚化但实现票需防冲突；③ C14 复测若翻转（Artifactory 有负缓存）则 INTENTIONAL 自动作废按对齐收；④ prune/gc 响应字面量未差分复核（G3 活体已取，冻结归规格票）
Blockers:      无
Next:          ① conductor 将两份 ADR 建议稿走 DECISIONS.md 流程（编号分配 + C14/C10/C05 拆解定谳）；② 派实现票：P0 门控接线（repo+adapter/docker）、P1 包型检索窗、P1 304 透传、P1 契约票（C08/C10/C17）、P2 Invalidate-refs 联动、P2 prune/gc 管理面（规格票先行）；③ 产品裁定上交：HA 链（gap 总账 ha 候选）、GCS/Azure、云重定向 UNKNOWN 取证票（MinIO）；④ architecture.md §4/§11 回写建议三行（§6 后果段）待派票

## 追加记录（2026-09-11，conductor 验收后追加指令执行）

- 指令: 三份不依赖用户裁定的 ADR 建议稿正式化进 DECISIONS.md（只追加+Errata 纪律）；HA/GCS/Azure/云重定向/C05 布局/token TTL/匿名默认七项用户待裁不入 ADR。
- 执行: ① ADR-0012 追加**勘误三**（docker/helmoci remote 内容 TTL 默认 21600s 包型默认、显式值优先、存量不回改——正文与勘误二原行保留，机制轴零翻动）；② **ADR-0047**（DigestChainGate 门控：docker_refs 账本 + node 短路，拒绝 marker 文件逐行翻译的裁定理由入记录；边缘语义两条随 ADR 定谳 INTENTIONAL：refs best-effort 耦合等价、image 精确匹配严于 AQL 前缀）；③ **ADR-0048**（C14 负缓存保留 INTENTIONAL，authority=本 ADR；Errata 触发条件显式登记——Artifactory 跨 missedTTL 复测翻转则本 ADR 作废按对齐收）。
- 验证: python 复核 ADR 标题 48 个、编号无重复、尾部序列 0046→0047→0048；勘误三落位 ADR-0012 节内（锚 = 勘误二尾行唯一匹配）。
- Files: DECISIONS.md（+2 ADR、+1 Errata 条目）。
