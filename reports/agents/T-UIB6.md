# T-UIB6 — UI Phase 1 批 6：域件与打磨（七批收官）

Ticket:       T-UIB6 · UI Phase 1 批 6：域件与打磨——七批收官 · P0（design-system-plan §6 批 6 行 / §4.2 状态矩阵 / §7 门）
Role:         dev-frontend（area = web/src 域件 + ui 原语运动面 + aggrid 主题 + styleguide；web/e2e styleguide 扩腿）
Area:         web/src/{pages/explorer,pages/artifacts,pages/dev,pages/security,features/aggrid,components,design-system} + web/e2e/styleguide.spec.ts
Input:        docs/design/design-system-plan.md §6 批 6 行、§4.1 P1/P2 清单、§4.2 状态矩阵（ArtifactTree★/ChecksumBlock★/Breadcrumb☆/Modal★/Toast★ 行）、§3.6 motion 三档、§7 门；docs/reverse/frontend/binflow-baseline/axe-current-{light,dark}.json（axe 现值底片）；批 0-5 产出（token 六族、tailwind 桥、ui 原语、tint/soft 双族、golden dev.96b20cbe）
Changes:      按件分节（域件逐件）：
              ① ArtifactTree 重皮（pages/explorer/TreePanel.tsx）：行态 token 化——hover=surface-2 / on-chain=surface-2 / selected=accent 左缘 2px 条（absolute w-0.5 bg-primary）+ bg-primary/10 软底 + semibold（对齐 §4.2「accent 左缘 2px+surface 软底」，替换旧 bg-accent 即 surface-2 的同值混用）；缩进线=每祖先层级一枚 absolute w-px bg-border 竖线（行堆叠成贯穿导轨）；行加 transition-colors duration-fast ease-standard + focus-visible outline（键盘腿补环，旧实现无可见焦点）；交互逻辑/testid/role/键盘语义零改
              ② ChecksumBlock 重皮（pages/explorer/DetailInspector.tsx）：checksum 行 mono+tabular-nums；长哈希（>24 字符）截断呈现 + 点击 reveal 全值（ChecksumValue 按钮：aria-expanded/stopPropagation/dotted underline，decoration 走 border-strong→primary hover）；复制恒全值（CopyButton 纪律不动）；缺失算法改渲染「—」占位（EMPTY_VALUE，§4.2「不伪造」——旧实现直接跳行）；mimeType/上传比对徽章零改
              ③ PathBreadcrumb 重皮（pages/explorer/ExplorerPage.tsx）：接入批 4 Breadcrumb 件（nav/ol 语义面）+ 路径分隔形态（BreadcrumbSeparator 传「/」——路径语义区别通用件 chevron）；段全 mono、末段 BreadcrumbPage 实色、中间段 BreadcrumbLink hover accent 文字；整径 CopyButton 与刷新/目录动作钮原位；.crumb/.cur 类 = e2e 锚（artifacts-tree/t134-g32/console-fr82/artifacts 四 spec 的 button.crumb:first-child）原样保留
              ④ PropertiesTable 重皮（pages/artifacts/PropertiesTab.tsx）：行 hover=surface-2 + transition token；空态迁统一件 EmptyState（testid=node-props-empty 原样，illustration 槽）；表单/搜索/删除语义零改
              ⑤ AG Grid 主题双谱（features/aggrid/theme.ts）：四根供给外扩五参——rowHoverColor=surface-2、selectedRowBackgroundColor=accent 10% 软底（color-mix，§4.2 DataTable selected 同口径）、cellFontFamily=--bf-font-sans、cellFontSize=13（=--bf-fs-sm 密度主档）、rowHeight=32（对齐轻量 DataTable 密度）；参数仍全 var()/派生式，双谱随 [data-theme] 活动解析（对齐 token 非逐像素复刻，§8 妥协登记位）
              ⑥ motion token 消费（design-system/tailwind.css + ui 七件）：tailwind.css 增 6 keyframes（bf-fade/pop/slide-in/out——只动 opacity/独立 scale/独立 translate，不碰 transform 不与 Radix 定位互踩）+ 6 @utility（anim-fade=fast120ms/anim-pop=base160ms/anim-slide=slow240ms，§3.6 三档映射）+ 两段特异度接管规则（sonner [data-sonner-toast].bf-toast、vaul [data-vaul-drawer].anim-slide-in——两库运行时注入 .4s/.5s 隐式时长，attr+class 双选器压回 token 档）；ui/dialog（overlay+content fade-fast）、ui/sheet（side 变体注入 --bf-slide-from 方向变量 + slide-slow）、ui/drawer（vaul 自有 keyframes 不夺名，仅挂接管钩类）、ui/popover、ui/dropdown-menu、ui/context-menu（pop-base）、ui/sonner（bf-toast 类）全部接入；prefers-reduced-motion 零特判随 token 层 1ms 降级
              ⑦ P2 件最小实现：ui/command.tsx CommandItem 补 transition-colors duration-fast（palette 重皮最小位）；components/ConfirmDialog.tsx 升格——直挂 primitive 的 Overlay/Content 换 ui/dialog 统一件（继承 fade 动画与卸载回焦），danger 标记 data-state=danger→data-variant=danger（data-state 归 Radix 开合态，动画变体依赖 open/closed 双值——仓内零消费者 grep 证实）；components/layout/transfer-box.tsx 重皮——security.css .transfer-* 十规则迁 Tailwind 语义类（等值：token 引用→语义类、720px 断点→max-[720px]），.transfer-item 类留 DOM 作 e2e 钩（m9 users-groups）；Card/Separator 已是 token 基座零改；ScrollArea Viewport 补 tabIndex=0（axe scrollable-region-focusable，首消费面随本批 styleguide 引入）；列选器（SearchPageV2 ColumnsMenu）走 Popover 原语随 ⑥ 继承运动，零改点
              ⑧ 徽章两族收敛呈裁（pages/dev/StyleguidePage.tsx 新增 section#badge-convergence）：tint 七变体（旧配方复刻族）与 soft 五变体（新语义底族）同屏并列，各带语义标签（tint=legacy recipe batch 5 / soft=new semantic surface batch 4）+ 同状态两族对照格；未删任何一族（终裁归 conductor）
              ⑨ styleguide 矩阵行补齐（StyleguidePage.tsx）：新增六区——domain-tree（行态四联：repo/chain/selected/leaf + 缩进线，点击迁移选中）、checksum（sha256 截断+reveal + sha1 全值 + md5 缺失「—」）、path-breadcrumb、properties（kv 行 + EmptyState）、motion（dialog/sheet/drawer/popover/toast 五触发器带 sg-motion-* testid）、p2（Card/Separator/ScrollArea/TransferBox）；AG Grid 未拉进 styleguide（嵌入面皮肤非交互原语，§8 妥协位——矩阵归 Explorer/Search e2e）
              ⑩ e2e 扩腿（web/e2e/styleguide.spec.ts）：section 清单 +7；badge 两族存在性（data-variant 锚）；域件交互腿（树选中迁移 + 左缘条计数、reveal 两态往返、缺失占位）；motion 时长 computed 断言（dialog 0.12s/popover 0.16s/sheet 0.24s/vaul drawer 0.24s/toast transition 0.24s——逐面开→量→Esc 关，模态不叠开）；prefers-reduced-motion 腿（emulateMedia reduce → 四面 0.001s + toast ≤1ms 数值断言——sonner 对 reduce 自身整关 transition 得 0s，同为降级值）；旧 breadcrumb 断言收窄到 section#breadcrumb（第二 nav 引入后的 strict mode 修正）
              ⑪ i18n 新键：「点击展开/收起完整校验值」（artifacts 域 en+zh 双侧，3120 调用点/2347 键同构过闸）
Files:        新增：reports/agents/T-UIB6.md（本件）。
              修改（24 文件）：web/src/design-system/tailwind.css（motion 消费层：keyframes/@utility/两段接管）、components/ui/{dialog,sheet,drawer,popover,dropdown-menu,context-menu,sonner,command,scroll-area}.tsx（九件运动/可达性接入）、components/ConfirmDialog.tsx（升格）、components/layout/transfer-box.tsx（重皮）、features/aggrid/theme.ts（五参扩）、pages/explorer/{TreePanel,DetailInspector,ExplorerPage}.tsx（域件三件重皮）、pages/artifacts/PropertiesTab.tsx（重皮）、pages/security/security.css（-66 行 .transfer 族退役）、pages/dev/StyleguidePage.tsx（+7 区）、i18n/locales/en/artifacts.ts、i18n/manifests/zh/artifacts.json（各 +1 键）、web/e2e/styleguide.spec.ts（+4 用例 +3 处既有用例适配）；reports/agents/styleguide/{styleguide-light,dark}.png（证据截图翻新）。
              删除：无。
Tests:        styleguide.spec.ts 13 用例（原 9 + 新 4：badge 两族/域件交互/motion 时长/reduced-motion）；四态覆盖：域件矩阵行陈列 hover/selected/空/缺失占位各态，styleguide axe 双主题 0 serious/critical；Explorer 消费面四态由 artifacts/m10/m16 族复核（上传/树/深链/props/checksum 全绿）；prefers-reduced-motion 腿 computed style 断言四面 0.001s + toast ≤1ms
Commands:     cd web && npm run build && npm run typecheck && npm run lint && npm run build:styleguide；实例（禁 docker、未碰 172.16.58.130 与 repo ./data）：make console 等价管道手工执行（rm internal/console/dist/assets + cp web/dist）→ go build -o /tmp/bf-uib6/binflow-server-{sg,prod,head} ./cmd/binflow-server → BINFLOW_HOME=/tmp/bf-uib6/{sg,prod,head}-home BINFLOW_ADMIN_PASSWORD=password BINFLOW_SERVER__LISTEN=127.0.0.1:{8199,8198,8197} serve（8199=styleguide 档、8198=生产档、8197=HEAD worktree 对照档）；BASE=:8199 npx playwright test e2e/styleguide.spec.ts（STYLEGUIDE_SHOT_DIR=reports/agents/styleguide）；BASE=:8198 同 spec（负证）；BASE=:8198 npx playwright test e2e/{artifacts,trash-can,pathmatch-parity}.spec.ts e2e/m10/properties-matrix.spec.ts e2e/m15/t416-virtual-tree.spec.ts e2e/m16/{t449-search-stack,remote-browse-tree,t445-detail-fields,t447-props-download,t434-tree-stack}.spec.ts；BASE=:8198 npx playwright test e2e/m8/a11y-sweep.spec.ts；BASE=:8198 npx playwright test e2e/m8/users-groups.spec.ts e2e/m9/users-groups.spec.ts；BASE=:8198 npx playwright test e2e/{console-smoke,login,shell-layout}.spec.ts e2e/m8/{setmeup-deploy,keyboard}.spec.ts e2e/m13/t366-webhooks.spec.ts；BASE=:8197 同族失败集对照；design-baseline.spec.ts 按票面未跑（golden 归 conductor 封票）
Outputs:      四门：assert-tokens OK（css 11 + tsx 104 零色值字面量/零色彩 token 直引 + token 定义层 7 豁免）；assert-i18n OK（3120 调用点/2347 键同构）；vite ✓ built in 7.45s（生产）/ styleguide 档 built in 9.02s；typecheck 0 错；lint 0 error / 45 warning（存量 react-hooks 族，与批 5 同数）。styleguide e2e（:8199）：12 passed 1 skipped（负证用例按探针 skip）+ axe 双主题 0 serious/critical + 截图两张翻新落 reports/agents/styleguide/；负证（:8198 生产档）：1 passed（路由未注册→#root 空白）12 skipped。Explorer 族 clean 轮（:8198）：50 passed / 8 failed——8 例全部归因：4 spec 6 用例 HEAD 对照同败（t434:84/230 批 5 已登记存量、t445:202 Module ID 标签快照竞态、remote-browse:355、t449×4 fresh 实例无种子数据面）；artifacts:61/269 与 t447:289 与 keyboard:210 为并行负载 flake（solo 复跑分别 7/7、4/4、6/6 全绿——t447/keyboard 失败发生在与 a11y-sweep 并行打同一实例时，T-459 已登记该族敏感性）。axe 全量：m8/a11y-sweep 3 passed（31 路由 × 双主题 62 面 + 登录页，serious/critical=0）；对照 axe-current-{light,dark}.json 底片 serious/critical=0 → 零新增。motion/reduce 腿全绿（见 Tests）。消费面：users-groups 9 passed（m9 N03:196 一例 HEAD 同败——服务端 404 文案断言，非前端面）；smoke/login/shell/setmeup-deploy/webhooks 27 passed（含 setmeup drawer + deploy dialog 双主题 axe）；m8/keyboard 6 passed
Compatibility: 锚册对照：无涉（不动 IA/导航/URL 深链/交互形态；弹窗/抽屉结构零变化仅加过渡与等值皮肤）。契约漂移：无 API 面触碰；唯一 DOM 契约微移 = ConfirmDialog danger 标记 data-state=danger→data-variant=danger（原借位 Radix 开合态属性——grep 仓内/e2e 零消费者，语义更正非漂移）；AG Grid 主题密度参数（rowHeight 32/cellFontSize 13）为皮肤面，树锚/行锚 testid 不受影响（artifacts/t416 族绿）
Security:     无新增用户内容渲染面（checksum reveal 为纯展示态切换，值本就已在 DOM）；XSS 面零变化；无敏感信息进前端日志
Performance:  无新依赖（产物零新增包）；motion 全 CSS animation/transition（无 JS 动画）；AG Grid rowHeight 收紧为纯视觉密度，虚拟滚动/无限行模型行为零变化；树/表格大数据面（分页/虚拟化）逻辑零改
Risks:        ① sonner/vaul 运动接管走「特异度压制」缝——上游库改注入选择器强度会静默失效，styleguide motion 断言腿已立 computed 护栏；② toast 在 reduce 下走 sonner 自关路径得 0s（非 token 1ms）——断言按 ≤1ms 数值收口，两路径都算降级；③ 域件重皮致 golden 翻新未做（票面归 conductor）——design-baseline 15 页在本批后必翻新；④ t434/t445/t449/remote-browse/N03 存量红在 fresh 实例复现（HEAD 同败）——与批 5 登记的存量腐烂族合流，建议单独修复票；⑤ 并行 axe 与交互 spec 同实例共跑会放大 5s expect 超时 flake——复跑建议错峰
Blockers:     无
Next:         1) 徽章两族终裁（conductor 封票：tint 旧配方族 vs soft 语义底族去留——styleguide#badge-convergence 并列面已备，裁决后删族 + golden 翻新一晚）；2) design-baseline golden 翻新（本批域件重皮 + AG Grid 密度合法视觉变更，--update-snapshots 归 conductor）；3) 存量 spec 腐烂修复票（t434:84/230、t445:202、t449×4、remote-browse:355、m9 N03:196——fresh 实例面）；4) 若后续要把 AG Grid 拉进 styleguide，先评估 ag-grid chunk 对 styleguide 构建体积的影响；5) 锚册无更新诉求（无涉）
