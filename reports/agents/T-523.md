# T-523 工作日志 — difftest v2 Maven 用例首批

① **Ticket**：T-523 [P0] difftest v2 Maven 用例首批（≥3 case）`role:differential-qa-engineer` `area:tools/difftest/`（BOARD.md 已冻结，状态只写本文件；Linear 回填 pending）

② **Role**：differential-qa-engineer

③ **起止时间**：2026-09-25（单会话完成；A 参照凭据未注入，双发实跑待凭据）

④ **Area**：`tools/difftest/v2/`（cases + README）＋本日志；未触碰 `reports/compatibility/`（无双发结果，不出差分报告）

⑤ **AC 清单**（票面）：
1. ≥3 个 Maven 用例遵循 demo_ping.py 结构与注册约定 → 交付 4 个（deploy / resolve-remote-cache / metadata-merge / delete-passthrough）
2. deploy 用例：mvn deploy 到 virtual（defaultDeploymentRepo）SNAPSHOT+RELEASE 各一；断言状态码、checksum、落仓路径、maven-metadata.xml 存在性 → `maven-virtual-deploy` 全覆盖
3. resolve-remote-cache：远端制品首拉 + `<remote>-cache` 投影断言 + 二次拉取 → `maven-resolve-remote-cache`
4. metadata-merge：双 local 成员版本并集 + latest/release 重算 → `maven-virtual-metadata-merge`
5. （可选第 4 个）delete→404 透传对拍 → `maven-virtual-delete-passthrough`
6. 本地可验证部分实跑，无 A 凭据如实 BLOCKED，不为跑通放宽断言 → 见⑨

⑥ **用例清单与断言摘要**（断言值全部由 `docs/reverse/` 规格推导为 `EXPECTED` 字典，双端各自对照规格 + A/B 互比；PASS=双端全中）：

| case id | 层 | 断言（期望值） | 规格锚 |
|---|---|---|---|
| `maven-virtual-deploy` | L5 | mvn 两条腿 exit=0；release pom 经 virtual GET=200+sha256 精确（fixture 无 `<repositories>`，PomInterceptor 默认策略为 no-op）；release jar 落 local 精确路径 200+sha256；`.sha1` 端点=服务端计算值；SNAPSHOT 版本目录 metadata 200 且 groupId/artifactId/version/buildNumber≥1；`<snapshotVersions>` 含 pom+jar（mvnMetadataVersion3Enabled 默认开）；落盘形态=timestamped（repo 显式 `snapshotVersionBehavior=unique`）；jar 内容 sha256 精确；组级 metadata versions=[1.0.0, 1.0.1-SNAPSHOT] / latest=1.0.1-SNAPSHOT / release=1.0.0 | virtual-resolution §5.2、repo-semantics §8.2 L217、maven-npm-pypi §1.1/§1.3/§1.4/§1.5 |
| `maven-resolve-remote-cache` | L6 | 经 virtual 首拉 Central `javax.annotation-api-1.3.2.pom` 200+sha256=上游常量；`GET /<remote>-cache/<path>`=200（投影仓持有）；二拉 200+同 sha256（TTL 7200s 内缓存服务；wire 级 HIT 证明需实例日志，证据面=投影持有+字节精确，README 已声明局限） | remote-cache-projection §2.1 L56/§3/§4、virtual-resolution §2 |
| `maven-virtual-metadata-merge` | L7 | 成员各自 {1.0.0,1.2.0}/{1.1.0,2.0.0,3.0.0-SNAPSHOT}（PUT 直发+客户端 checksum 头）→ virtual 组级 metadata versions 按 Maven 比较器序=[1.0.0,1.1.0,1.2.0,2.0.0,3.0.0-SNAPSHOT]；latest=3.0.0-SNAPSHOT；release=2.0.0；`<virt>-cache` 同路径恒 404（合并结果不落 virtual-cache，区别于 npm） | virtual-resolution §5.1（逐行） |
| `maven-virtual-delete-passthrough` | L7 | PUT pom=201；virtual 解析 200+sha256；**DELETE 经 virtual=404 且成员制品存活（GET local=200）**；成员直删=204；随后 virtual 解析=404 | repo-semantics §8.2 L220 + virtual-resolution §7.5（勘误级，高置信） |

实现要点：真实客户端腿=`mvn deploy:deploy-file`（沿 l0134 已验证旗标：`-Dmaven.resolver.transport=wagon` + retryHandler=1）；settings.xml 用 `${env.DIFFTEST_MVN_USER/PASS}` 插值——密码只经 subprocess env，零落盘零 argv。异步 metadata 计算用 poll（15s 预算，永不自动放宽）。临时仓 key 全部 `difftest-mvn-` 前缀，finally 双端清理。

⑦ **产出文件清单**：
- 新增 `tools/difftest/v2/cases/_mavenlib.py`（共享助手：repo 供给/清理、poll、maven-metadata.xml 解析（拒 DOCTYPE 防 entity expansion）、mvn 腿、四态判定 `judge()`）
- 新增 `tools/difftest/v2/cases/maven_virtual_deploy.py`、`maven_resolve_remote_cache.py`、`maven_virtual_metadata_merge.py`、`maven_virtual_delete_passthrough.py`
- 新增 `tools/difftest/v2/_smoke_mock.py`（无凭据接线自检哑端点，`_` 前缀不参与发现）
- 修订 `tools/difftest/v2/README.md`（Maven 批次章节；并纠错 `ctx.sides` 访问形态文档）
- 运行产物 `tools/difftest/v2/run/t523-*`（gitignored；含逐 case evidence/<id>/{a,b}-leg.json + summary.json）
- 本日志

⑧ **实际执行命令与关键输出**（均在 worktree 根）：
```
python3 -m py_compile tools/difftest/v2/cases/_mavenlib.py tools/difftest/v2/cases/maven_*.py tools/difftest/v2/_smoke_mock.py   → PY_COMPILE_OK
python3 tools/difftest/v2/runner.py --list
  → demo-ping / maven-resolve-remote-cache / maven-virtual-delete-passthrough / maven-virtual-deploy / maven-virtual-metadata-merge（5 例全发现）
python3 tools/difftest/v2/runner.py --dry-run --out tools/difftest/v2/run/t523-dryrun
  → PASS=0 FAIL=0 BLOCKED=0 NOT_RUN=5；score.sh → {"pass":0,...,"not_run":5,"total":5,"coverage":null}
python3 tools/difftest/v2/runner.py --out tools/difftest/v2/run/t523-noenv        # 本 shell 六 env 全缺
  → BLOCKED=5，逐例 reason="missing required env: A_BASE, B_BASE, A_USER, A_PASSWORD, B_USER, B_PASSWORD"；env 段只记 set/MISSING
python3 tools/difftest/v2/_smoke_mock.py 18082 / 18083（本机哑端点，占位凭据仅进进程 env）
env A_BASE=http://127.0.0.1:18082 B_BASE=http://127.0.0.1:18083 A_USER=mocka A_PASSWORD=mocka B_USER=mockb B_PASSWORD=mockb \
  python3 tools/difftest/v2/runner.py --out tools/difftest/v2/run/t523-mock2   → PASS=1 FAIL=4 BLOCKED=0
  …同命令 --out …/t523-mock3                                                              → PASS=1 FAIL=4 BLOCKED=0（连续两轮）
mvn 腿证据（t523-mock-deploy evidence/a-leg.json）：deploy_release_mvn_exit="0"、deploy_snapshot_mvn_exit="0"
  （4 次真实 mvn deploy:deploy-file 对哑端点全部上传成功；HTTP 断言如实记 404 → FAIL，符合设计）
grep -rniE "password\s*[:=]|192\.168\.120\.38|JFrog@|apikey|secret" … → 仅命中纪律注释文字，无凭据值、无参照地址
```

⑨ **测试四态（逐 case）**：
| case | 真双发 | mock 接线自检 |
|---|---|---|
| demo-ping（存量） | **BLOCKED**（缺六 env；A 凭据 pending） | PASS |
| maven-virtual-deploy | **BLOCKED**（同上；含 A 侧——本票禁止对参照实例写入，且无凭据） | FAIL（断言值=status=404 等，管线全通含 mvn 子进程；首轮曾 BLOCKED=AttributeError，见⑬，修复后 FAIL） |
| maven-resolve-remote-cache | **BLOCKED** | FAIL（404 族） |
| maven-virtual-metadata-merge | **BLOCKED** | FAIL（404 族，poll 15s×3×2 端如期超预算退出） |
| maven-virtual-delete-passthrough | **BLOCKED** | FAIL（哑端点 DELETE=200 → 记录为规格违例） |
- 无凭据实跑 = 全批 BLOCKED（reason 逐例列缺失变量）＝真实差分结论数为 0；mock FAIL 是接线证明，**不是差分结果**，报告与 results.json 分目录隔离（t523-mock*）。
- 稳定性门：t523-mock2 与 t523-mock3 逐 case (status, reason) 完全一致（stable=True × 5）。

⑩ **lint**：`python3 -m py_compile` 全部通过（含共享库与 mock）；stdlib-only 契约未破（新依赖=0）；results.json 逐份 `json.load` 通过（5/5 JSON_OK）。

⑪ **证据锚（规格行引用）**：`docs/reverse/virtual-resolution.md` §5.1（L82-94 metadata 合并逐规则；L93 不缓存）、§7.5（L165-167 virtual DELETE 归属勘误）、§5.2（L96-109 pom 清洗）；`docs/reverse/repo-semantics.md` §8.2 L217-220（defaultDeploymentRepo 路由/405/DELETE）；`docs/reverse/maven-npm-pypi.md` §1.1-§1.5（L28-102：端点表、unique 快照改写、metadata 计算时机与内容、checksum 行为）；`docs/reverse/remote-cache-projection.md` §2.1 L56（cache 直 GET 可用）、§3 L82（回源先写 cache 再回流）、§4 L94（TTL/新鲜期）。框架契约：`tools/difftest/v2/README.md`（env 契约/四态/normalize 注册制）。

⑫ **风险**：
1. BinFlow 侧四桶解析/metadata 合并若未实现 → case 3/1 预期 FAIL=合法 DIFFERENT（票面已预告，非用例 bug）；差异分类届时无 authority → UNKNOWN 上报。
2. repo 创建 JSON 带 `snapshotVersionBehavior=unique` 等字段：若 BinFlow 静默忽略未知字段，case 1 落盘形态断言将 FAIL——真实信号（配置面透传缺口），非误报。
3. normalize.yaml 无 maven 域（全 raw）——本批用「派生断言」（解析后比语义字段）绕开归一需求，未新增任何归一规则/提案。
4. case 2 依赖双实例对 repo1.maven.org 出网；上游不可达 → 断言记 status 而非 BLOCKED（404 与不可达的边界在响应形态，复跑可辨）。
5. metadata 异步窗口：poll 预算 15s 固定；慢实例可能记 FAIL(status=404)——按「复跑绿=通过」协议复核，不放宽预算。
6. mvn 首跑需解析 deploy 插件（本机 ~/.m2 已热）；mvn 不在 PATH 的环境 → 用例 BLOCKED（SetupError，已处理）。
7. wire 级缓存 HIT 不可外证（需实例日志）——case 2 判据已降为投影持有+字节精确并在 README/证据 note 声明。

⑬ **偏差**：① v2 README 的 `ctx.sides` 文档写 `.base` 属性访问而 runner 实际传 dict——mock 首轮暴露（maven-virtual-deploy BLOCKED: AttributeError），已改 dict 访问并纠 README（双处）；② 案例文件曾含未用 import（json），lint 阶段清除。无规格偏差、无断言放宽。

⑭ **后续（凭据到位后的双发补跑步骤）**：
```
export A_BASE=http://192.168.120.38:8082/artifactory B_BASE=<UAT 实际 base 含上下文根>
export A_USER=… A_PASSWORD=… B_USER=… B_PASSWORD=…        # 只经 env，不落任何文件
python3 tools/difftest/v2/runner.py --out tools/difftest/v2/run/maven-batch1
bash tools/difftest/v2/score.sh tools/difftest/v2/run/maven-batch1/results.json
# 连跑两轮对照 (status, reason) 稳定；差分结果抄送 reports/compatibility/<date>-maven.yaml
```
- 届时 A 侧将发生 difftest 命名空间写入（建 `difftest-mvn-*` 临时仓 + mvn 部署，finally 清理）——属差分运行 sanctioned 操作（role §6）；本票阶段未对参照实例做任何写入。case 2 在 A 侧会建指向 Maven Central 的 remote 仓（同样 difftest 前缀、跑后删）。

⑮ **结论**：**用例就绪，双发 blocked-on-credentials**。4 个 Maven 用例（超出 ≥3 要求）落地 v2 框架，全部遵循既有注册约定与凭据纪律；发现/预检/dry-run/接线自检（含 4 次真实 mvn 腿、连续两轮稳定）全绿，真实差分 0 结论（六 env 未注入，逐例 BLOCKED 有据）。修复了 v2 README 一处会误导后续用例作者的文档错误。状态：**DIFFERENTIAL 前置就绪；建议保持 READY，待 A/B 凭据注入后补跑并出 `reports/compatibility/` 差分报告。**
