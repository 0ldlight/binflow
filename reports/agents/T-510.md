# T-510 — FR-152.3/FR-159.1 webhook build 域 dormant→wired（断言反转④）

状态: done（停工待收编；零 git 操作）
agent: dev-go-core · area: internal/webhook + internal/build（+ 装配层 httpapi 的 Emit facet 接线——ADR-0045 决策 12 明文划归本票；build 服务装配点在 httpapi/server.go〔T-508 先例〕，故接线落在彼处 cmd 零改）
日期: 2026-09-07 · deps: T-508/T-509（done）

## 一、三事件矩阵（发射点 × payload × 签名验证）

| 事件 | 发射点（与 audit 同址，ADR-0045 决策 7） | data 四字段（webhook.md §3.4 冻结） | 验证 |
|---|---|---|---|
| build/uploaded | `Upload` 成功尾（首次 + 覆写臂均发）；`Append` 成功尾（说 **resolved parent** 四元，非调用者松地址） | build_name / build_number / build_started（canonical `…T…+0000` 字面）/ build_repo | emit_test.go 三臂 + build_webhook_test.go 全栈腿 |
| build/deleted | `RetentionPlan.Execute` 每个 discarded run（DeleteBuild + audit 行同址，每 run 一发；BinFlow 无独立 DELETE 路由——retention 即删除面，票线索裁定） | 同上（该 run 自己的四元） | emit_test.go（2 run 丢弃 = 2 发，kept run 静默）+ 全栈腿 |
| build/promoted | `Promote` 成功尾（AppendPromotion + audit 行同址；**dryRun 早退零事件**；payload 的 build_repo = run 自身仓，永非迁移 target） | 同上 | emit_test.go（dryRun 静默臂）+ 全栈腿 |

**签名验证（openssl 逐字）**：`TestBuildDeliverySignedToReceiver`（internal/webhook/build_events_test.go）——httptest 真实接收器 + dispatcher 真投递，X-JFrog-Event-Auth 双臂验证：① `webhook.Signature`（包内 HMAC-SHA256 hex over 精确收到的 wire bytes）；② **openssl 二进制本体** `openssl dgst -sha256 -hmac <secret>` 过同一字节流，digest 与收到的 header 逐字相等（本机执行、未跳过——`exec.LookPath` 守卫，无 openssl 环境才降级日志跳过）。

## 二、注册表翻转前后对照（eventtypes.go，零 schema 变化）

| 项 | 前 | 后 |
|---|---|---|
| build/uploaded、deleted、promoted | SourceDormant（3 行字面量） | SourceWired（3 常量 TypeBuild* 行） |
| wired 总数 | 9（M13 裁定） | **12**（+build 3；TestT362EventClosedSet 同步改钉） |
| 其余 10 域 57 型 | dormant | **维持 dormant 不动**（零伪造——TestBuildDomainRegistryAudit 逐域 wired 计数断言：artifact 5 / artifact_property 2 / docker 2 / build 3 / 其余全 0；总数 66 不变） |
| envelope / 订阅 schema | — | **零变化**（ADR-0041 决策 7：翻 Source = 加发射行，非 schema 变更；`len(env)==7`、`len(data)==4` 双断言钉死；dataFor 的 build 分支自成四字段集，artifact 族基础字段不搭车） |

## 三、criteria 面 build scope（webhook.md §2.2 build 行）

`CriteriaFilter.MatchesBuild(name)`（criteria.go 新增）+ `criteriaAdmit` 的 DomainBuild 分支：**anyBuild**（全收）/ **selectedBuilds**（build 名精确命中——选中名即匹配该名全部 run，全局 scope 语义）/ **include-excludePatterns**（Ant 对 build 名求值，exclude 先赢）；空选择不收（家族法则）；**repoKeys/anyLocal 族不被 build 域消费**（规格表如实——测试有反断言臂）。9 腿 table-driven（TestBuildCriteriaScope）+ 全栈 selectedBuilds miss / includePatterns miss / anyBuild 命中三臂（build_webhook_test.go）。

## 四、织入面（import 禁令保持）

- `internal/build/emit.go` 新文件：`WebhookEvent`（四字段 + *Principal）+ `WebhookEmitter` **func 类型**（ADR「emit.go 定义 func 类型」逐字落地）+ `WithEmitter` + `emitWebhook`（recover 护体——adapter 是装配层代码，panic 不染主路径；bus 自身 shield 之上再加一层）。boundary_test.go 白名单零改动——build 仍不 import webhook（禁令的编译期证明随测试全绿）。
- `internal/httpapi/build.go`：`buildWebhookEmitter` 适配器（build.WebhookEvent → webhook.Event；`buildHookActorOf` 投影 userContext 三元——repo 域 hookActorOf 同型复制，彼助手段未导出）。
- `internal/httpapi/server.go` build 装配块：`deps.Webhooks != nil` 时 `build.WithEmitter(...)`（unit 栈零织入，行为字节不变）。

## 五、自测（实际执行）

| 门 | 命令 | 结果 |
|---|---|---|
| build | `go build ./...`（快照树） | OK |
| vet | `go vet ./...`（快照树） | OK |
| lint | `golangci-lint run ./internal/webhook/... ./internal/build/... ./internal/httpapi/...` | **0 issues**（全树 `golangci-lint run` 的 4 条红全在并行 T-513 WIP 文件〔internal/bundle/model.go ×3 unused + internal/metadata/api.go gofmt〕——非本票面，留 T-513 收口） |
| test | 快照树 `make test`（race 全量） | **37 ok / 1 FAIL**：`internal/adapter/rpm TestRemoteWritesRefused`——与本票面零交集，**solo 复验绿**（211.5s，T-491「负载伪影 solo 复验」先例协议）；本票三包全量内全绿：build 202.8s / httpapi 916.8s / webhook 118.2s；追加 TestBuildDomainRegistryAudit 后三包终态 race 复跑（快照树）**全绿**：webhook 52.7s / build 68.5s / httpapi 940.9s（exit 0） |
| e2e M13 | `npx playwright test e2e/m13/t366-webhooks.spec.ts`（真二进制 + console） | **11/11 passed**（50.7s） |
| e2e M13 consumer | `npx playwright test e2e/m13/t366-consumer.spec.ts` | **8/8 passed**（1.9m；PIPE 腿 = 真 PUT → 接收器七字段签名 envelope + HMAC 逐字节验证、RETRY/DEAD/HANG/SSRF 全链） |

**隔离说明**：并行 T-513 正写 internal/bundle + metadata——live 树两度不可编译（substores_bundles.go defer 拼写）。全量门在 **rsync 快照树** `/tmp/t510-tree` 跑（13 个本票文件 diff 验证与 live 字节一致；快照内 T-513 文件的 defer 拼写误植打了补丁仅为可编译，未动 live）。`bin/binflow-server` 于 13:2x（live 尚绿时）`make build` 重出——e2e 跑的是**含本票改动的真二进制**。

单测新证据：webhook 包 `go test -race -run 'TestBuildEventsFireSubscriptions|TestBuildCriteriaScope|TestBuildDeliverySignedToReceiver|TestT362EventClosedSet|TestT362EmitGateAndDormantDrops|TestBuildDomainRegistryAudit'` 全 PASS（openssl 二进制臂确认执行）；build 包 `…-run 'TestUploadEmits|TestPromoteEmits|TestRetentionEmits|TestEmitterPanic|TestEmitFacetTypes'` 全 PASS。

## 六、规格依据（docs/reverse/）

- **webhook.md §3.4**（高置信）：build 三事件 data = build_name/build_number/build_started（`1970-01-01T00:00:00.000+0000` 格式）/ build_repo（示例 `artifactory-build-info` = metadata.DefaultBuildRepo）——四字段逐字，零发明。
- **webhook.md §2.2** criteria-by-domain build 行：anyBuild/selectedBuilds/include-excludePatterns；「全局 scope 匹配系统内全部同名 build」→ selectedBuilds 按名匹配全部 run。中置信细节（pattern 求值对象 = build 名）：以测试固化（MatchesBuild 行为钉死），无低置信项需降档登记。
- **webhook.md §4**：envelope 七字段（domain/event_type/data/subscription_key/jpd_origin/source/userContext）——AC1 断言锚。
- **webhook.md §6**：HMAC-SHA256 hex over 精确 payload bytes，`X-JFrog-Event-Auth`——openssl 验签双臂锚。
- **ADR-0045 决策 7/12**（DECISIONS.md）：翻转①②③三步、emit.go func 类型、发射点与 audit 同址、criteria = Bus 既有过滤器。

## 七、遗留 / 需上游确认

1. **FE 镜像陈旧（本票 area 外，未动 web/）**：`web/src/lib/webhooks.ts` 的 EVENT_TYPES 静态镜像仍标 build 三型 dormant、头注「9 型 wired」——与 Go 注册表不一致（AC2 的呈现面）。3 行翻转 + 注释归 FE 票（T-512 或顺腿小票）；console 订阅表单的 build 域 criteria 仍无 anyBuild/selectedBuilds 输入（CRITERIA_MANAGED_DOMAINS 不含 build——FE 走 {} criteria 会一封不发，REST 面完全可用）。建议 conductor 派 3 行镜像同步。
2. `docker/promoted` 维持 dormant（PRD M14+，规格原注）——本票不动。
3. DELETE 批删族端点未路由（T-509 遗留重申）：build/deleted 目前唯一发射臂是 retention；批删路由落地时应在其成功尾同址补发。
4. 快照树 `/tmp/t510-tree` 留存至收编（复验用）；8080 临时社区实例已停。
