# T-200 — 审计词表 web 波及修复（T-193 N-1）工作日志

- 状态: **done**（AC①②③④ 全达成；全量 e2e 82 过 / 0 败 / 3 skip，优于 T-193 基线）
- 日期: 2026-08-22
- role: dev-frontend / area: `web/src/lib`、`web/e2e`
- 输入: BOARD.md T-200 条目（只读）；reports/agents/T-187.md（§4 越界清单第 5 项）、reports/agents/T-193.md（§5 N-1、§3 基线 80/2/3/0）

## 1. 根因与修法

T-187 把审计认证失败动作全局更名为 `auth.failed`（`internal/audit/api.go:119`
`ActionAuthFail = "auth.failed"`；`login.success` 不在更名范围），两处 web 消费方未跟随
（T-193 N-1）：控制台动作选择器选不到新词、按旧词过滤零行；W23b 断言旧词 → 全量两轮红。

### AC① web/src/lib/governance.ts（:39）

`AUDIT_ACTIONS`（`internal/audit` `Actions()` 的前端镜像，AuditPage 选择器与 W23b
selectOption 的取值源）：

```diff
   'login.success',
-  'login.failed',
+  'auth.failed', // M6 PRD FR-56 拼写：认证失败动作（T-187 更名后的全局单一词汇）
```

`login.success` 保留（T-187 明确不在更名范围，e2e :250 断言仍有效）。method/reason 是
detail JSON 字段（`{"method":"local","reason":"bad_credentials",...}`），不进选择器
词表，本票无需触碰——governance.ts 无引用，核对过。

### AC② web/e2e/t104-supplements.spec.ts（:306,308 + 同步注释 :283,:304-305）

W23b 的 selectOption 取值与 toContainText 断言 `login.failed` → `auth.failed`；
两处注释同步（注释若留旧词会破「grep 清零」验收）。

### 附带一行：web/e2e/login.spec.ts（:108）

mock 错误文案 `'oidc login failed'` → `'oidc sign-in failed'`。非审计词表（是 OIDC
mock 错误体的人类可读英文句子，带空格非 token），但裸 `grep login.failed`（正则点号
匹配空格）会命中它——改写使 AC 的 grep 验收字面成立，语义不变（login.spec 5 用例含
断言该文案渲染的两条全绿）。

## 2. 自测（实际命令与输出摘要）

服务端词表实证（自起真后端 127.0.0.1:18200，`go build` 二进制内嵌本次 console 产物）：

```
$ curl POST /binflow/api/v1/session（admin/错口令）      → 401
$ GET /api/v1/audit?action=auth.failed → events: [{action:"auth.failed",
    detail:{method:"local",reason:"bad_credentials",remote_addr:...}}]
$ GET /api/v1/audit?action=login.failed → events: []（旧词只命中存量旧行，本实例零行）
```

构建 / 静态检查（cd web）：

```
$ npm run build      → ✓ built in 4.87s；relink-assets verified（2/36 rewritten）
$ npx eslint .       → 0 problems（exit 0）
$ npm run typecheck  → exit 0
```

grep 清零（两种口径均零命中，含构建产物 dist）：

```
$ grep -rn 'login\.failed' web/src web/e2e web/dist        → 零命中（exit 1）
$ grep -rn 'login.failed'  web/{src,e2e,dist,scripts,*.ts,*.json,*.js,*.html} → 零命中（exit 1）
```

（`login.success` 有意保留：governance.ts:38 与 t104-supplements:250 各一处。）

e2e（真后端：`CGO_ENABLED=0 go build -trimpath ./cmd/binflow-server`，临时 data 目录
/tmp/t200/data，端口 18200；console 构建产物按 make console 同款复制进
internal/console/dist 后编译嵌入）：

```
$ BASE=http://127.0.0.1:18200 npx playwright test e2e/t104-supplements.spec.ts
  → 4 passed / 2 skipped（dind 双腿设计内降级）——W23b ✓ 3.5s（T-193 时全量两轮红）
$ BASE=… npx playwright test e2e/governance.spec.ts e2e/auth-shell.spec.ts e2e/login.spec.ts
  → 14 passed（audit 用例 ✓ 28.0s：过滤/keyset/path 客户端过滤/REST 对账全过）
$ BASE=… ADMIN_PW=password npx playwright test（全量）
  → 82 passed / 0 failed / 3 skipped（1.4m）
```

**AC④ 基线对照**（T-193 run2 = 80/2/3/0，2 败为 W23b + G18-4）：W23b 复绿（82 = 80 +
W23b + G18-4 本轮未复发），**无新败**。3 skipped 同基线：t104-perf RSS 腿（QA_SERVER_PID
未设）+ t104-supplements dind ×2。

### 过程记录：两轮环境噪声（均与本票改动无关，已定位排除）

1. **ADMIN_PW 缝**：t146-docs.spec 默认口令是其专属 QA 实例的 `docsqa-test-pw-146`
   （:16），对本实例必 401 → G19b-1 败。按 T-193 先例 `ADMIN_PW=password` env 覆盖后
   即绿（既定缝，非缺陷）。
2. **机器负载尖峰**：一轮全量期间本机 load 32（VMware 213% CPU + Virtualization VM
   162% + 并行 agent 的 storage.test 127%，均非本票进程，未触碰）。超时族用例
   （W10 卡「登录中…」、G30e-1、G32a-1、governance audit 60s 预算）在尖峰轮败、负载
   回落（load 10）后全绿；governance audit 单独跑 18.0s/28.0s 稳绿（T-193 §8 已登记
   该用例负载敏感，60s 余量是 T-193 所加）。所有轮次中 W23b/W34 恒绿——词表改动面
   零回归。

## 3. 变更清单

| 文件 | 改动 |
|---|---|
| web/src/lib/governance.ts | :39 `'login.failed'` → `'auth.failed'`（+词源注释） |
| web/e2e/t104-supplements.spec.ts | :306,308 W23b 选择器取值与断言同步；:283,:304 注释同步 |
| web/e2e/login.spec.ts | :108 mock 文案 `oidc login failed` → `oidc sign-in failed`（grep 清零附带项） |

越界检查：`git status` web/ 内仅上述三文件 + T-193 在途两 spec（governance/repositories，
本票未触碰）；internal/console/dist 构建产物 gitignored 未入库；二进制与临时目录均在
/tmp/t200（已清理）。

## 4. 环境清理

- 自起实例（18200）已停（pkill + lsof 复核端口空闲）；并行 agent 的 storage.test 与
  VMware/VM 全程未触碰。
- /tmp/t200（二进制/data/日志/cookie jar）与 web/test-results 已删。

## 5. 遗留 / 需上游知悉

1. **T-187 §4 越界清单第 5 项（本票）闭环**；其余越界项（docs/user 措辞、architecture
   DDL 注释、M4 PRD 历史词、存量 audit_events 旧行）不在 web 面，归各自票。
2. `login.success` 维持 M4 词汇（T-187 遗留①：PRD §6.4 成功词表 `auth.login.{local,
   oidc,ldap}` 未实施）——若后续收口，本票两处（governance.ts 镜像 + W23b 同族断言）
   是同款波及面。
3. governance audit 用例负载敏感（60s 预算、尖峰轮会破）与 t146-docs login helper 5s
   断言偏紧——T-193 §8/N-3 已登记，本票未扩面，归 conductor 统筹。
