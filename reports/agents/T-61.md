# T-61 工作日志 — M3 工程 ticket 拆解

- ticket: T-61 [P0] M3 工程 ticket 拆解 `role:tech-lead`
- 状态: done（待主会话审核录入 BOARD.md）
- 日期: 2026-08-19
- 输入: docs/prd/milestone-3.md（**v1.1**：FR-15~FR-22 共 66 AC、35 端点矩阵 RE/ME/NE/PE、M01~M61 验收命令、§5.5 C1~C8 定案、Q1~Q8——Q4 docker remote 已定不做）、docs/design/architecture.md（M3 增量：ADR-0012 remote 基线/ADR-0013 virtual/§4.5 缓存面/§5.4 三协议 adapter/internal/remote 包/MetadataProvider 注册表/003 迁移）、docs/reverse/maven-npm-pypi.md + repo-semantics.md §7/§8（T-59 规格，置信度标注）、docs/reverse/oss-structure.md（§6 结构启示：per-protocol 三件套/接口两粒度/拦截器链）、reports/agents/T-6.md 与 T-32.md（M1/M2 拆票范式）、BOARD.md（只读）、M1/M2 代码现状（repo.Service 签名与 validate 类型矩阵 / adapter SPI / metadata 子接口先例）
- 口径总则: Q1 凭据存储按 **ADR-0012 定案**（AES-256-GCM + env `BINFLOW_REMOTE_CREDENTIALS_KEY`）写死——PRD §7 的「明文暂行」与 ADR/任务硬性要求冲突，回写风险 R1（赶在 T-66 派发前）；Q2 写路由按 **PRD 暂行口径**（默认 405 + 显式 defaultDeploymentRepo 后路由写入）写入 T-71 并标注【暂行】；M1/M2 能力全部复用零重做（见下清单）；三协议行为依据 = 官方规范优先 + T-59 规格定案值，票内已按 v1.1 勘误口径写死（npm 重复 publish 403、snapshot policy 409）。

## 结论

16 张票（T-62~T-77），P0 x13 / P1 x2 / P2 x1；首波可并行 2 张（T-62/T-63），最大波宽 4（批 3：T-66/T-67/T-69/T-70）；关键路径 T-62→T-64→T-66→T-71→T-72→T-74→T-75→T-76。

---

## 〇、口径总则（复用清单 / 遗留收编 / Q1·Q2 处置）

### M1/M2 能力复用清单（零重做，票内不再重复实现）

| 复用面 | M1/M2 来源 | M3 消费方式 |
|---|---|---|
| storage.Session（流式三摘要/singleflight/毒化） | T-9 | remote 回源落盘走同一 BeginSession→Commit（架构 §4.5）；三协议上传全走它 |
| metadata 迁移器 + 子接口模式 | T-10/T-34 | 003 迁移照 002 模板；Remote()/Virtual() 子接口与 Docker() 同构；NodeStore ListByPrefix 是 simple index/versions 聚合的查询基座 |
| repo.Service 统一入口 + 覆盖检查链 | T-12/T-13 | Get/Put 签名 M1 已预留三型分流注释；同 checksum 幂等/不同 checksum 需 delete 权直接被三协议与 remote 缓存写入复用；PutFromBlob 供 sha1-only 秒传；**唯一新增用例 = PutLandedBlob**（§11.13 债务，收编进 T-64） |
| adapter SPI（packageType 分发键/RepoTypes） | T-13/T-33/T-48 | maven 挂内容路径**零 httpapi 改动**；npm/pypi 走 `/binflow/api/{npm,pypi}` 分发缝（M1 §5.4 预留位兑现）；RepoTypes 扩三型即通 |
| 权限面 + 匿名分层 + TokenRegistry | T-11/T-28/T-37 | 三协议与 remote/virtual 原样适用（NFR-S17）；npm login 复用 TokenRegistry（/v2/token 双入口先例） |
| httpapi：middleware 链/E-01 信封/EscapedPath 分发树/断连定界/Range+条件请求 | T-14/T-41/T-20/T-15 | 三协议共用 `errors[]` 信封（规格 §0 定案）；npm scoped `%2f/%2F` 等价、packument/simple 304 全靠既有链 |
| GC 与删仓级联 | T-9/T-42/T-12 | remote 缓存 node 即普通 node，GC 零特判（§4.5）；ON DELETE CASCADE 已有 |
| QA 基线脚本 | T-18/T-19/T-43/T-44 | M1 C 序列 + M2 D 序列直接复跑（FR-22-AC5），仅按 §5.6 反转表改断言 |

### PRD §6.4 遗留 5 项处置

| 遗留项 | 处置 |
|---|---|
| ① sha1-only checksum deploy | **T-73 工程票**（P2） |
| ② RepoTypes class 键（§5.1 勘误） | 代码侧清理并入 **T-63 AC②**（架构侧 T-48 已落） |
| ③ remote-cache 仓命名（`<key>-cache`） | **无票**——T-59 已收口：不模拟影子仓投影（缓存 node 直落 remote repo 命名空间），决策归档 §6.4/架构 §10 |
| ④ O4 匿名读开时已认证零权限用户 403 | **无票**——语义维持，QA T-74/T-75 覆盖 npm/pypi/remote 域变体（M28/M35） |
| ⑤ Email 不落盘 | 不收编（PRD 明示归 M4） |

另：M1 勘误 ×2（T-59）——snapshot policy **409** 已吸收进 T-67 AC（M19 口径）；includes/excludes 双值码 M3 未暴露字段、无 AC，仅归档。

### Q1/Q2 处置

- **Q1【暂行→ADR 定案冲突，票按 ADR-0012 写死】**：PRD §7 暂行「明文 SQLite / `BINFLOW_SECRET_KEY` M4 候选」与 ADR-0012 定案「AES-256-GCM / env `BINFLOW_REMOTE_CREDENTIALS_KEY` / 003 迁移加密 / 有行无钥 fail-fast」直接冲突（任务硬性要求「凭据加密」与 ADR 同向）。T-62/T-66 按 ADR-0012 实现，PM 回写见 R1（**赶在 T-66 派发前**）。
- **Q2【暂行，按 PRD 口径】**：virtual 默认 405 不可写；仅显式配置 `defaultDeploymentRepo`（成员中 local 仓）后路由写入（P1 面）。写入 T-71 并标注。ADR-0013「M3 只读」的 PM 联动条款已被 PRD FR-21 触发，建议 architect 补记（R2）。

---

## 一、票据列表（T-62 ~ T-77，共 16 张）

### 基座（波 1~2）

**T-62** [P0] metadata：003_remote_virtual 迁移 + Remote/Virtual 子接口 `role:dev-go-core` `area:internal/metadata` `dep:—（M2 基线）`

AC:
① 003_remote_virtual.sql（sqlite 实装 + postgres 占位，同 002 策略）：`remote_cache` 表（etag/last_modified/fetched_at/expires_at/kind + `idx_remote_cache_expiry`）+ remote_configs 扩列（content_ttl_seconds 86400 / metadata_ttl_seconds 600 / allow_private_upstream 0 / `unreachable_mask` RENAME `blocked_out`），严格按架构 §6 定稿 DDL；文件体内零 BEGIN/COMMIT（T-10 M9 红线）；迁移幂等 + 001/002 老库升级路径单测；virtual_members.position 语义注释升级「同类内次序」（ADR-0013）
② Store 增 `Remote()`（remote_configs CRUD + remote_cache upsert/get/delete_by_repo/到期候选查询）与 `Virtual()`（成员 CRUD + position 序读）子接口；`-race` 单测覆盖 upsert/级联；顺手 P2 缝：`idx_blobs_sha1`（T-73 sha1 秒传查询用）
③ 建表零行为变化：M1/M2 metadata 套件 + pragma 守卫测试零回归

**T-63** [P0] adapter SPI 扩展与三协议挂载基座（MetadataProvider 注册表 + api/npm|pypi 分发缝） `role:dev-go-core` `area:internal/adapter（SPI 注册表）、internal/httpapi（api 段分发缝）、internal/repo/api.go（两段拆分）` `dep:—（M2 基线）`
review：单 reviewer（架构一致性——本票定义后续全部协议票的契约面）。

AC:
① MetadataProvider 注册表（架构 §5.4，oss-structure §6 启示 1/2）：`adapter.MetadataProvider`（layout/包名判定 + metadata/artifact 分流 + 版本比较器接口缝）+ Register/ForProtocol；`repo/api.go` 拆「公开用例面 / adapter SPI 面」两段，SPI 面含 repo class 只读查询（maven 旁车对 remote 仓透传 404 需要）；注册表无消费者时零行为变化（virtual 择优 M4 缝不实现）
② httpapi：`/binflow/api/npm/**`、`/binflow/api/pypi/**` 前缀分发给已注册 protocol，未注册维持 404 E-01（E-26 断言**不提前反转**）；收编 §5.1 勘误遗留——adapters map 删除 class 键写入（T-33/T-48 遗留，PRD §6.4 第 2 项）；repo key 保留字与既有分发语义不变
③ table-driven（真栈 harness）：分发缝矩阵（注册前 404/注册后路由/scoped `%2f` 与 `%2F` 等价——T-14 EscapedPath 链复用）+ M1/M2 路由测试零回归

**T-64** [P0] repo.Service：三型仓库模型与配置校验（FR-15）+ PutLandedBlob 用例 `role:dev-go-core` `area:internal/repo、internal/adapter/docker（finalize 切换小改，串行）` `dep:T-62,T-63`
review：单 reviewer（PutLandedBlob 事务边界为关注点，来源架构 §11.13「M3 前」承诺）。

AC:
① FR-15-AC1~AC4/AC7：supportedPackageTypes 放开三型 × {generic,docker,maven,npm,pypi}（remote+docker / virtual+docker → 400「not supported in M3」）；remote 字段校验与默认值（url 必填 http/https、retrieval 7200/missed 1800/socketTimeout 15s/assumedOffline 300s/hardFail false、allowPrivateUpstream 仅 admin 可设并记审计）；virtual 校验（repositories 非空/成员存在/禁嵌套 virtual/priorityResolution 成员标记/defaultDeploymentRepo 必须为成员中 local 仓，别名 `defaultDeploymentRepoRef`/`deploymentRepository` 并收，指向非 local → 400）；GET 回显 password 掩码（NFR-S14）；`?type=remote|virtual`、`?packageType=` 过滤（M04）；M01~M05 curl 等价自测
② FR-15-AC6：删 remote 仓 `?deleteContent=true` 级联清 nodes/remote_cache 后再 GET 404（repo 不存在分支）；virtual 成员变更即时生效（逐请求现算，无解析缓存）；E-07/C26 既有断言按 §5.6 反转表同步更新
③ PutLandedBlob：从已提交 BlobRef 建 blobs 台账行 + node（PutFromBlob 变体），docker finalize 切换并删除 adapter O(size) 回读（性能债收口）；table-driven 类型矩阵全分支 + fake 驱动 + docker 域回归

**T-65** [P0] internal/remote：SSRF 防护链 + stdlib 出站 HTTP client（NFR-S13 安全核心） `role:dev-go-core` `area:internal/remote（ssrf-guard、client 子域）` `dep:T-59(规格,done),T-62`
review：**双 reviewer（安全 + 架构）——硬性要求，SSRF 面安全关键**。

AC:
① NFR-S13 七点校验链：scheme ∈ {http,https}（建仓已校验、请求时断言）；host 解析**全部 IP** 过清单（回环 127/8 与 ::1/RFC1918/链路本地含 169.254.169.254 与 fe80::/10/ULA fc00::/7/组播/0.0.0.0/保留，allowPrivateUpstream 豁免）；**DNS rebinding 防护**——net.Dialer Control 回调对实际连接 IP 二次校验、校验后拨号用已校验 IP 不重新走系统解析；重定向手动跟随**每跳重过全链**上限 5 跳；缓冲型响应 64MB 上限超限 502；连接/读超时按仓 socketTimeoutSecs（默认 15s）；拒绝一律 WARN 结构化日志（repo key/被拒目标/原因类别）零堆栈（M42 取证面）
② stdlib-only transport（ADR-0012 决策 5）：net/http 自写装饰（幂等 GET 指数退避 2 次/超时/流式计数）；**go.mod 零新增依赖**（ADR-0005 白名单不破）；Basic 凭据头构造（解密归 T-66，本票只透传）
③ table-driven 恶意上游矩阵（httptest：127.0.0.1/10.x/192.168.x/169.254.169.254（直连+302 两式）//::1/0.0.0.0/5 跳上限/64MB 截断/豁免仓放行）；gosec 零告警（T-26 收窄规则内）

### remote 域（波 3，与协议并行）

**T-66** [P0] internal/remote：pull-through fetcher + 凭据加密 + remote 分流接线（FR-20） `role:dev-go-core` `area:internal/remote（fetcher/cache-state/credentials）、internal/repo（Get/Delete 分流）` `dep:T-59(规格,done),T-63,T-64,T-65`
review：**双 reviewer（正确性 + 架构）——缓存状态机是 M3 最大正确性面**。
【Q1 按 ADR-0012 定案执行：AES-256-GCM、`enc:v1:` 前缀、env `BINFLOW_REMOTE_CREDENTIALS_KEY`、有行无钥启动 fail-fast、存量明文一次性加密；PRD §7 明文暂行待回写（R1）】

AC:
① FR-20 六步请求流（RE-04，v1.1 定案口径）：checksum 后缀（.sha1/.md5/.sha256 等）不回源 404 `Checksums are not downloadable.`；负缓存（missedRetrievalCachePeriodSecs 1800）期内同路径 404 零上游流量；缓存命中（retrievalCachePeriodSecs 7200，content/metadata 两类 TTL 按 MetadataProvider 分流）直发 M1 头集 + `X-BinFlow-Cache: HIT|MISS|REVALIDATED|STALE`；过期/缺失回源走 T-65 client——上游 200 流式 BeginSession→Commit 落盘（node 落 remote 仓命名空间，落盘与响应逐位一致，上游 X-Checksum-* 读为 original 只登记不拒，四值策略 M4）；上游 404 写负缓存 + 有过期副本仍回发（expired-but-serving）；上游 5xx/超时 → assumed-offline 静默 300s（期内零上游流量）+ 有缓存（含过期）服务缓存附 `X-Binflow-Upstream-Error` 头、无缓存 404（hardFail:true → 502）；上游 401/403 视为 unfound 404（message 附上游状态摘要）；PUT/POST → 405+`Allow: GET`；DELETE 仅删本地缓存（再 GET 回源）；**FetchResult 显式区分「有副本（含过期）/真 miss」**（T-71 stale 语义消费，R10）；凭据 AES-GCM 全链（建仓加密入栈/fetch 解密/日志回显零明文，NFR-S14）
② repo.Service 分流（repo.type=remote → remote.Fetch，架构 §5.4 接线）：generic remote 闭环 curl 自测（M41/M43/M44/M48 + mock 上游上游计数负断言）；日志增补 upstream_host/cache_result/upstream_status/upstream_duration_ms（§6.3）
③ table-driven 上游矩阵（kill 上游/慢上游/重定向/凭据对错/并发 miss——singleflight P1 可简化但等待者不得 5xx）；RE-11 `/api/v1/remote/stats`（P2 可延后，hit/miss/negative 计数 + 字节数接口位留下）；1GB 流式 RSS < 256MB（NFR-P14）；冷启动不回退自测

### 三协议（波 3~4，Maven 两票串行、npm/PyPI 并行）

**T-67** [P0] Maven adapter：layout 解析 + deploy/resolve + checksum 三态（FR-16，metadata 外全部） `role:dev-registry-adapter` `area:internal/adapter/maven（layout/传输子域）` `dep:T-59(规格,done),T-63,T-64`
review：**双 reviewer（正确性 + 架构）——判定顺序与覆盖检查豁免是正确性红线**。

AC:
① ME-01/02/03/09：maven-2-default 六字段严格解析（判定顺序 = 先剥 checksum 后缀与 metadata 前缀（含 plugin 群 `metadata-maven-metadata.xml` 变体）再匹配模板；unique snapshot 文件名正则 `{module}-{baseRev}-{yyyyMMdd.HHmmss}-{N}[-classifier].{ext}`、目录仍 `{baseRev}-SNAPSHOT`；目录层数 ≥3、文件名以 `<artifactId>-<version>` 开头）；不合规路径 400【C2 半定案：模型定案、码值暂行，R3】；PUT 校验链（jar/pom/旁车 .sha1/.md5/.sha256：X-Checksum 头链 + 旁车按 `checksumPolicyType` 三态——client-checksums 默认严格 409（message 含 received/actual）/ server-generated 静默接受以实测落盘 / 未配置等价 client-checksums；均未提供服务端计算接受）；GET/HEAD 继承 M1 头集（`X-Checksum-*`/`ETag=<sha1>`/Range 206/416/条件 304，M21）；旁车 GET 返回服务端实测裸 hex（无换行）；`snapshotVersionBehavior`：deployer（BinFlow 默认）与 non-unique 落盘语义 + 已 unique（timestamped）名不改写（unique 服务端改写 P2 不做）；handleReleases/handleSnapshots=false 的 PUT → **409**（M1 勘误定案，M19）；maven-metadata.xml 与旁车**永不触发覆盖检查**（复用 repo.Service 覆盖链 + 豁免标记）；DELETE → 204（metadata 联动归 T-68）；`/.index/**` 404（ME-10）；remote 仓旁车请求透传引擎 404
② MetadataProvider（maven）注册：GAV 解析 + artifact/metadata 分流 + Maven 版本比较器（T-66/T-68/T-72 消费）
③ 真实客户端：mvn 3.9.x M11/M12/M13 全链自测（altDeploymentRepository deploy → 落盘 sha256 对账 → 全新 repo resolve）+ M16c/M17/M18/M19/M20/M21 curl 等价；table-driven layout 正反例 ≥20 例（classifier/timestamped/plugin metadata/穿越变体 NFR-S18）

**T-68** [P0] Maven maven-metadata.xml 计算器（FR-17） `role:dev-registry-adapter` `area:internal/adapter/maven（metadata 子域，与 T-67 串行）` `dep:T-67`
review：**双 reviewer（正确性——触发时机与并发合并是 M3 正确性重面）**。

AC:
① ME-04/05/06/07（v1.1 C1 全量定案）：触发时机四类（上传 unique snapshot 文件 / non-unique pom → 父 version 目录**同步**计算（阻塞 deploy 响应）；其余 artifact 异步；上传 pom 加算祖父版本组目录（非递归）异步；delete → 两侧受影响目录树异步联动）；版本组目录（`{orgPath}/{module}/maven-metadata.xml`：versions = 子版本目录中含 `*.pom` 的目录集（同版本多行取 created 最新去重）、Maven 版本序排序、latest = 排序最后（SNAPSHOT 也算）、release = 最后非 SNAPSHOT、lastUpdated = 计算时刻 `yyyyMMddHHmmss` UTC）；无 pom 子目录 → 删 metadata 及伴随 checksum（**RTFACT-6242 例外**：现存 snapshot 型 metadata 而路径非 snapshot 目录不删）；SNAPSHOT 版本目录（groupId/artifactId 取自同目录 pom 解析、buildNumber/timestamp 取最新 unique snapshot pom（non-unique 或无 unique 文件时固定 1 无 timestamp）、snapshotVersions 每 (extension×classifier) 取最新一条默认开启、`<updated>` = 时间戳去点号）；客户端 PUT maven-metadata.xml → 201 接受（走通用上传链）并触发受影响目录重算——权威内容以服务端存储事实计算（等效合并，并发不丢版本）
② checksum 一致性：metadata 内容每次变化 .md5/.sha1 旁车与响应头同步重算（M14 断言）；FR-17-AC1~AC6 全过自测（M15 新版本合并 latest 单调/M16 两次 unique deploy buildNumber==2/M16b `-U` 强刷解析到 buildNumber 2/M15 附带 DELETE 后 versions 移除/两进程交错 deploy 1.3.0+1.4.0 无 5xx/旁车 == 对 GET XML 现算）
③ table-driven 计算器输入矩阵（release/snapshot/unique/non-unique/classifier 混合目录 + `-race` 并发交错）+ mvn `dependency:get -U` 真实客户端（M16b）

**T-69** [P0] npm adapter：publish/packument/tarball/dist-tags/unpublish/login（FR-18） `role:dev-registry-adapter` `area:internal/adapter/npm` `dep:T-59(规格,done),T-63,T-64`

AC:
① NE-01~NE-06：publish PUT **十步校验链按序**（body JSON 解析失败 400 / `_attachments` 非空 versions 空 400 / 无写权限 403 `Cannot deploy to '<tarballPath>'` / **同版本重复 403** `Cannot modify pre-existing version '<v>', aborting upload for: '<name>'`（v1.1 勘误 409→403）/ name·version 非法（semver + leading zeros）400 / `_attachments` 空含 deprecated → 201 `{"ok":"updated package"}` / 空且非 deprecate 400 / integrity `sha512-` 不一致 400（BinFlow 从严决策，R8）/ `dist.shasum` 不一致 400 / 成功 201 `{"success":true}`）；scoped `@scope%2Fname`（`%2f`/`%2F` 等价，C8 定案）；tarball 落 `<name>/-/<name>-<version>.tgz`（scoped `@<scope>/<name>/-/@<scope>/<name>-<version>.tgz`）；packument GET（name/dist-tags/versions/dist{tarball,shasum,integrity}/_rev/_id/time；dist.tarball 一律重写指回 BinFlow；`ETag`/`X-Checksum-Sha1` = 包文档 JSON sha1 + If-None-Match 304；SLIM `Accept: application/vnd.npm.install-v1+json` 协商 P1；GET 面不返回 `_attachments`）；tarball GET 流式 + M1 头集；dist-tags 两形态（npm ≥8 `/-/package/<name>/dist-tags` 族 + 旧式 `PUT .../<package>/<tag>`：PUT → 201 `{"ok":"created new tag"}`、DELETE → 200 空、缺失 404 `npm package not found with name:<n>, and tag:<t>`）；unpublish（`PUT .../-rev/<rev>` 恒 200 `{"ok":"updated package"}` 假成功占位 + DELETE 单版本/整包 200、versions 条目移除、dist-tags 引用联动清理）；login `PUT /-/user/org.couchdb.user:<name>` → 201 token（复用 TokenRegistry，/v2/token 双入口先例）+ `GET /-/whoami`；包索引以 metadata 表属性为中心**不落 `.npm` 隐藏目录**（规格 §4.5 建议）；`/-/ping` 200 `{}`（P2）与 search/security 404 E-01（M58）；域根 `GET /api/npm/<repo>/` 200 空（P2）
② 存储面：packument 为 node `<pkg>/packument.json`（架构 §5.4.2），publish 合并新版本/unpublish 删版本/dist-tag 原子更新；MetadataProvider（npm）注册（packument/metadata 分流）
③ 真实客户端：npm 10.x M22/M22b/M22c/M23/M24/M25/M26/M27/M28 全过自测（M26 按 403 勘误口径、M27 含 `-rev` 占位显式断言、`npm cache clean --force` 后重装、匿名双模式）；table-driven 十步链全失败分支 + 包名字符集 `[A-Za-z0-9@/._-]` 有限集/穿越变体（NFR-S18）

**T-70** [P0] PyPI adapter：simple index + upload + 下载（FR-19） `role:dev-registry-adapter` `area:internal/adapter/pypi` `dep:T-59(规格,done),T-63,T-64`

AC:
① PE-01~PE-03（v1.1 定案细节全量）：`GET .../simple/<normalized-name>/` HTML（PEP 503 归一化 lower + `[-_.]+`→`-` 三态同页、文档头 `<meta name="api-version" value="2" />`（PEP 629）、每文件 `<a href="#sha256=">` 条目按文件名排序 + HTML 转义、**仅 sha256** 无 md5 fragment、不输出 Artifactory 私有 `rel="internal|external"`）；无尾斜杠 `GET .../simple/<name>` → 302 补尾斜杠（Location）；ETag 稳定内容哈希（不透明串）+ If-None-Match → 304；未知名 404；`simple/` 根索引（P2）；POST upload（`:action` 严格 `file_upload` 否则 400 `unknown action '<action>'`；`md5_digest` 可缺失（twine ≥6.2）服务端自算、提供且不一致走 client-checksums 链 409；缺 name/version/content 400；同 filename 重复 400【暂行：该分支规格未明示】；响应统一 200；存储路径 `<name>/<version>/<filename>` 用元数据原始名不归一化）；下载 href `/binflow/api/pypi/<repo>/packages/<name>/<version>/<filename>` 复用 M1 下载链（checksum 头/Range/304）+ 内容路径 `/binflow/<repo>/...` 同 node 第二入口；PEP 691 JSON simple（P2）；`/pypi/<name>/json` 与 `/api/pypi-ui/**` 404（M58）；MetadataProvider（pypi）注册（simple 页 = metadata 分流）
② 真实客户端：pip 24.x + twine M30~M35/M35b 全过自测（twine ≥6.2 无 md5_digest 形态、wheel+sdist 同版本并存、依赖链 demo-lib 自动解析、pip download hash 对账）；table-driven 归一化矩阵（Demo_Pkg/demo_pkg/demo-pkg）+ `:action` 变体 + multipart 边界 + 穿越变体
③ 匿名边界（M35 等价）：匿名 GET simple/文件 200、POST 401（NFR-S17）

### virtual（波 4~6）

**T-71** [P0] virtual：两桶解析 + 写路由（FR-21 P0/P1，C3/C5 定案） `role:dev-go-core` `area:internal/repo（virtual 解析子域）` `dep:T-59(规格,done),T-66`
【暂行口径 Q2：virtual 默认不可写——未配 `defaultDeploymentRepo` 的 PUT/POST/DELETE → 405 + `Allow: GET` + 定案文案 `No local repository was configured as local deployment repository for the (<key>) virtual repository.`；仅显式配置后路由写入（P1 面）。ADR-0013「M3 只读」按其 PM 联动条款由 PRD FR-21 扩展，解析顺序面不变（R2）】

AC:
① RE-07/08：两桶序解析（priorityResolution 优先桶（桶内声明序）→ 其余成员（桶内声明序）；Artifactory 四桶在无 `<key>-cache` 仓投影下的等价简化，C3 定案）；下载类解析首命中即停 + `X-BinFlow-Resolved-From: <memberKey>` 头（ADR-0013）；local 成员查本地 node、remote 成员走 FR-20 代理链——**stale 命中（含过期副本）即作为该成员结果返回、仅真 404（负缓存/无副本/assumed-offline 无缓存）才继续桶序下一成员**（消费 T-66 FetchResult 状态，R10）；探索性 miss 不落盘（ADR-0013，防成员扫描污染缓存）；全 miss 404 E-01；PUT/DELETE 未配写路由 → 405（M52）；配 `defaultDeploymentRepo` 后写操作路由目标 local 成员执行（权限/覆盖检查/checksum 链按目标仓语义，virtual GET 立即可见，M53）；virtual DELETE 不透传（BinFlow 有意不兼容，405，删缓存请对 remote 成员操作）
② 成员变更（增删/改序）即时生效（逐请求现算、无解析缓存）；List 聚合 = 成员前缀并集（P2 聚合浏览 M55b 可延后）
③ table-driven：local/remote 交叉命中矩阵 + 桶序/声明序/priorityResolution 变体（M51 及 AC9 变体——声明序在后成员标优先 → 得该成员内容）+ stale/miss 语义 + 写路由 node 实落 local 断言（`/binflow/<local>/<GAV>` 亦 200）；M50/M52/M53 curl+mvn 自测

**T-72** [P1] virtual metadata 聚合（FR-21-AC5/AC6，三协议 P1） `role:dev-registry-adapter` `area:internal/adapter/maven、internal/adapter/npm、internal/adapter/pypi（聚合面，与协议票串行派发）` `dep:T-68,T-69,T-70,T-71`

AC:
① Maven：maven-metadata.xml GET 拦截按桶序逐成员取同名 metadata **内存合并**（versions 去重按 Maven 版本序重排/latest/release 重算/snapshot 取 buildNumber 更大者/并 `snapshotVersions` 规避 MNG-5180/优先成员已产出即短路非优先成员 foundByPriority）；结果**不缓存每次现算**；任一成员被 block 整体透传 block（M55）
② npm：首成员基底 + 后续版本 `putIfAbsent`（先到先得不覆盖）、`dist-tags`/`time` 并集、通用字段取先到者、latest 重算——**每次现算**（Artifactory virtual cache 仓 TTL 600s 语义不实现，规格 §4.3 建议等价简化）；PyPI：simple 索引逐成员收集条目合并、格式不一致（请求 JSON 成员只出 HTML）整体回退 HTML、任一成员失败不阻塞其它（remote 成员「包不存在」走独立负缓存复用 FR-20 参数）（M54/M55 等价断言）
③ table-driven 合并矩阵（同包名分散多成员/优先桶短路/单成员故障）+ mvn/npm/pip 经 virtual 的 M54/M55 真客户端自测

### 遗留与收尾（波 5~9）

**T-73** [P2] sha1-only checksum deploy 秒传（M1 遗留收编 §6.4-1） `role:dev-go-core` `area:internal/repo、internal/adapter/maven（checksum-deploy 面）` `dep:T-64,T-67`

AC:
① `X-Checksum-Deploy: true` + 仅 `X-Checksum-Sha1`（无 Sha256）：按 sha1 命中 blobs 台账（`idx_blobs_sha1`，T-62 留缝）→ PutFromBlob 零传输 201（sha1/md5 从台账行取，复用 T-13 契约）；sha1/sha256 均缺维持 400；sha1 未命中维持 404（M1 C15 语义不变）
② 定例 AC：maven 仓对已存在 sha1 的 jar 以 sha1-only 秒传到新 GAV → 201（PRD §6.4）；generic 仓 M1 C15 零回归
③ table-driven 三态（仅 sha256/仅 sha1/双缺/未命中）+ `-race` 并发同 sha1

**T-74** [P0] QA：M3 三协议功能矩阵（PRD §8 剧本 2~5/8） `role:qa-engineer` `area:验收（Maven/npm/PyPI 功能全链路）` `dep:T-68,T-69,T-70,T-71`（T-72 聚合项 M55/M55b 待其 done 后在 T-75 补验）

AC:
① 仓库模型 M01~M05（E-07 反转、docker 组合边界、url scheme 400）+ Maven M10~M21 全序（snapshot `-U` 与 metadata 合并重点、M19 按 **409** 勘误口径、M20 layout 400）+ npm M22 系~M28（M26 按 **403** 勘误口径、M27 含 `-rev` 占位显式断言、M28 匿名双模式）+ PyPI M30~M35b（M31 v1.1 四细节：api-version 头/无尾斜杠 302/ETag-304/`:action` 400；M33 重复 400）；逐条附原始输出
② 边界 M57/M58（探针与不做端点）+ NFR-S16 错误体分层抽查（三协议 E-01）+ NFR-S18 路径穿越变体（`../`、`%2e%2e`、编码斜杠、`@scope%2F..`）+ NFR-S17 匿名写 401；跨协议去重（M12 附带 stats 不增）；任何 5xx/非预期码打回附票号；报告 `reports/agents/T-74-qa.md`
③ mock 上游手法沿 M2 先例；实例环境与其他 QA 票串行防污染

**T-75** [P0] QA：remote/virtual 行为 + SSRF 安全矩阵（PRD §8 剧本 6~7/9） `role:qa-engineer` `area:验收（remote/virtual/安全）` `dep:T-74,T-72`

AC:
① remote M41~M48 全序（上游计数负断言贯穿；M42 SSRF 全变体 127.0.0.1/10.x/192.168.x/169.254.169.254（直连+302 两式）//::1/0.0.0.0/file://·gopher://（建仓 400）+ WARN 日志取证；M44 五段——① 已缓存 200+`X-Binflow-Upstream-Error` ② 未缓存默认 404（message 含 offline 状态）③ hardFail 仓 502 ④ 静默期 300s 零上游流量 ⑤ health 全程 200；M45 maven 代理含 checksum 后缀 404 `Checksums are not downloadable.` 定案断言；M46/M47 公网不可用走 mock 上游并注明）；virtual M50~M55b（M50 混合解析、M51 序、M51 变体 priorityResolution 两桶序、M52/M53 写面、M54、**T-72 聚合项 M55/M55b 补验**）
② NFR-S13~S15 全项 + NFR-S14（GET repo config 不回显明文 + 日志 grep 无 Authorization/明文凭据）+ FR-20-AC10 上游凭据对错（错 → 404 附摘要）+ NFR-P14 1GB 流式 RSS < 256MB
③ 日志取证与 mock 上游脚本存档进报告；任何 5xx 打回附票号

**T-76** [P0] QA：真实客户端矩阵 + 回归基线 + 性能（FR-22） `role:qa-engineer` `area:验收（真实客户端/回归/性能）` `dep:T-75`

AC:
① §5.3 分级矩阵 P0 全过留档：mvn 3.9.x 五链（local deploy → 全新 repo resolve → snapshot `-U` → remote 代理 → virtual 混合解析）、npm 10.x（publish plain+scoped/install 缓存清空/dist-tag add·rm/unpublish/remote 代理/virtual）、pip 24.x + twine（upload wheel+sdist/simple/install 含依赖链/download+hash 校验/remote/virtual）、curl 各域抽查；**M50/M54 收口断言**（local 包与上游包经同一个 virtual 仓分别被 mvn/npm/pip 解析成功 = 代理收口门槛）；Gradle 8.x P2 观察（成败记录不阻塞）
② 回归基线（FR-22-AC5/§5.6 反转表口径）：M1 C 序列 P0 + M2 D 序列 P0 复跑全绿（E-07/E-26 断言反转后）；generic 与 docker 域零回归（P0 硬门槛）；跨协议去重三域变体
③ 性能 M60a 冷启动 < 2s（含三协议路由 + remote/virtual 引擎注册）/M60b 50 并发 pip install（缓存命中）全 0 零 5xx/M60c 代理开销 < 100ms 记录；M3 QA 总报告 + DoD §9 第 1/2 条结论

**T-77** [P1] M3 用户文档（DoD 第 4 条） `role:tech-writer` `area:docs/user` `dep:T-75`（骨架可自波 5 起草，终验在 T-76 后）

AC:
① Maven/npm/PyPI 接入指南三篇（settings.xml/`.npmrc`+`_auth`/pip.conf+twine 配置即拷即用；scoped 包/snapshot/checksum 策略/匿名与凭据）+ remote/virtual 管理指南（建仓字段表 v1.1 定案默认值 7200/1800/15s/300s/false、缓存/负缓存/assumed-offline 语义、DELETE 缓存 = 强刷手法、写路由**【暂行】**口径标注、**SSRF 放行操作指引**（allowPrivateUpstream 审计与风险边界）、出站 443/80 网络要求（ADR-0012 §9）、`BINFLOW_REMOTE_CREDENTIALS_KEY` 部署说明与无钥 fail-fast 行为）
② 有意不兼容清单（docker remote M3 不做、npm search/audit、PyPI JSON API/yank/658、Maven 索引、virtual DELETE 不透传等 §2.2 全表）+ 常见报错码对照（409 snapshot policy / 403 重复 publish / 405 写拒绝 / 404 `Checksums are not downloadable.` 等 v1.1 定案码）
③ 全部命令在 T-76 验收产物上抽样复跑（退出码 0）；文档中文、命令与标识符英文；frontmatter 用 Docusaurus 兼容子集（ADR-0011）

---

## 二、分批派发表（宽度 ≤4，批内 area 不重叠）

| 批次 | 票 | 宽度 | 说明 |
|---|---|---|---|
| 1 | T-62 / T-63 | 2 | 基座双起点：003 迁移（metadata 包）与 SPI/挂载缝（adapter+httpapi+repo/api.go 仅拆段注释不改逻辑）互不交集 |
| 2 | T-64 / T-65 | 2 | repo 模型（dep 62/63）与 SSRF/client（dep 62）并行；internal/repo vs internal/remote 不重叠 |
| 3 | T-66 / T-67 / T-69 / T-70 | **4** | 最大波：remote fetcher（含 repo Get 分流）+ 三协议 adapter 并行——maven/npm/pypi 互不相干；前提 = T-63 已定 SPI 契约、adapter 只经 Service/SPI 编程不摸 repo 内部（R6，派单指令必注） |
| 4 | T-68 / T-71 | 2 | maven metadata（串行 after T-67）与 virtual 解析（dep T-66）；adapter/maven vs internal/repo 不重叠 |
| 5 | T-73 / T-77(骨架) | 2 | sha1-only P2（repo+maven checksum-deploy 面，此时无并行占用）+ 文档起草（docs/user） |
| 6 | T-72 | 1 | virtual 聚合串行触三 adapter 包（依赖 68/69/70/71 全 done；与 T-73 同放会撞 adapter/maven，故错开） |
| 7 | T-74 | 1 | QA 三协议矩阵（功能面冻结后） |
| 8 | T-75 | 1 | QA remote/virtual+安全（QA 串行共用实例，M1/M2 先例；含 T-72 聚合项补验） |
| 9 | T-76 / T-77(终验) | 2 | 客户端矩阵+回归+性能收口；文档终验并行 |

review 强度汇总：**双 reviewer = T-65（安全+架构，硬性要求）、T-66（正确性+架构）、T-67/T-68（正确性+架构）**；单 reviewer = T-62、T-63、T-64、T-69、T-70、T-71、T-72、T-73、T-77；QA 票（T-74/T-75/T-76）按 M1/M2 惯例无 code review，以报告 + conductor 复现核验。

关键路径：**T-62 → T-64 → T-66 → T-71 → T-72 → T-74 → T-75 → T-76**（8 票 9 批）。次长链 T-63 → T-64 于批 2 汇入主链；T-67 → T-68 → T-72 为 Maven 侧支链（与主链在 T-72 汇合）。QA 三段串行（共用实例防数据污染）是路径尾部固定成本。

---

## 三、风险清单（R1~R10，供 conductor 转交）

- **R1（PRD 回写，最急——赶在 T-66 派发前）**：Q1 凭据存储。PRD §7 暂行「明文 SQLite / `BINFLOW_SECRET_KEY` M4 候选」与 ADR-0012 定案「AES-256-GCM / `BINFLOW_REMOTE_CREDENTIALS_KEY` / 003 迁移加密 / 有行无钥 fail-fast」直接冲突（任务硬性要求「凭据加密」与 ADR 同向）。T-62/T-66 已按 ADR 写死；PM 需回写 Q1 定案并统一 env 键名，否则照 PRD 原文执行会漏加密与 fail-fast（M2 R1 同款先例：票按 ADR，PRD +0.1 回写）。
- **R2（architect 措辞回写）**：ADR-0013 写「M3 virtual 只读」，其 PM 联动条款已被 PRD FR-21-AC4（defaultDeploymentRepo 写路由 P1）触发。T-71 按双面实现（未配 405 P0 + 配置路由 P1），建议 architect 在 ADR-0013 补一句联动记录，防 review 争议。
- **R3（C2 半定案）**：Maven layout 拒绝码 400 维持暂行（模型定案、码值未见规格明确值）。T-67 已按 400 写死；PM 后续勘误若改码，改动面为单行常量 + QA 断言（M20）。
- **R4（Q5/Q6 残余定案口径）**：per-path `?refresh` 不做（URL 语义纯净，强刷走 DELETE 缓存 RE-06）、HEAD+Last-Modified 回源协商列 P1（P0 允许到期直接 GET 重取，M41 上游计数断言不受影响）、`snapshotVersionBehavior=unique` 服务端改写 P2（buildNumber 连续性依赖 metadata 计算器，实现后对拍收口）——均已按 PRD 定案落票，无悬置项。
- **R5（E-26 断言时序）**：`/binflow/api/npm|pypi/**` 行为反转只能在协议票落地后生效；T-63 分发缝在协议未注册时维持 404 E-01。若既有 routes 测试硬编码 npm/pypi 404，T-69/T-70 须同步更新并注明来源（M2 R10 同款，保 CI 批 1~3 不红）。
- **R6（批 3 并行边界）**：T-66（internal/repo Get/Delete 分流）与三协议 adapter 票并行的前提是 adapter 只经 repo.Service/SPI 接口编程——派单指令需重申「不得摸 repo 内部」（T-63 的两段拆分即此防线）；若 T-66 需改 Service 接口签名，批 3 作废重排（T-63 先定契约已缓解）。
- **R7（环境）**：M45/M46/M47 真实上游（Maven Central/npmjs/pypi.org）依赖公网，离线走 mock 上游摆 junit/lodash/six 布局并注明（PRD 已留缝）；mvn 3.9/npm 10/pip 24/twine 版本矩阵需 QA 环境预装（§5.3 版本锚定）；不可用时降级路径沿 M2 R11 先例。
- **R8（暂行/从严决策项）**：npm integrity 不一致 400（BinFlow 有意从严，偏离 Artifactory 默认仅记属性）、PyPI 重复 filename 400（规格未明示该分支）、Maven layout 400（R3）——已按 PRD 定案/暂行写死，QA 按 PRD 口径断言；用户或 PM 推翻时均为单点改动。
- **R9（工程必需但 PRD 未显式列，来源补登）**：T-63 的 class 键清理与 MetadataProvider 注册表（架构 §5.1 勘误/§5.4）、T-64 的 PutLandedBlob（§11.13「M3 前」承诺）、T-66 的 remote 日志字段与 RE-11 stats 接口位（PRD §6.3/RE-11）——均有架构/PRD 条文依据，无擅自加戏。devops-engineer 无票：M3 无部署面增量（ADR-0012 §9 出网要求归 T-77 文档；compose/Helm 不内置 egress 代理），CI/Makefile 现状自动覆盖新包。
- **R10（virtual stale 语义实现缝）**：C3 定案「stale 命中即返回、仅真 miss 才继续下一成员」要求 remote 解析结果显式区分「有副本（含过期）/真 miss」——T-66 FetchResult 契约必须暴露该状态（已写入 AC①），T-71 消费；实现遗漏将致 M51 变体/FR-21-AC7 失败，已列为 T-66/T-71 review 关注点。

---

## 输出契约

- 状态: done
- 产出: /Users/lzw/dev-center/reports/agents/T-61.md（本文件，票单唯一全文）；BOARD.md 录入由主会话执行
- 统计: 16 张票（P0 x13 / P1 x2 / P2 x1），首波可并行 2 张（T-62/T-63），最大波宽 4（批 3：T-66/T-67/T-69/T-70）
- 依赖链: 关键路径 T-62→T-64→T-66→T-71→T-72→T-74→T-75→T-76；次长链 T-63→T-64 汇入主链，T-67→T-68→T-72 为 Maven 支链
- 风险/待澄清: R1~R10（见「三」）；R1 最急（Q1 凭据 PRD/ADR 冲突，须赶在 T-66 派发前由 PM 回写）；R2 需 architect 补 ADR-0013 联动记录；R5/R6 为批 1/3 派单指令必注项；Q2 暂行口径已写入 T-71 并标注
