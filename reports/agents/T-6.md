# T-6 工作日志 — M1 工程 ticket 拆解

- role: tech-lead
- 日期: 2026-08-17
- 输入: docs/prd/milestone-1.md（PRD v1.1）、docs/design/architecture.md（12 节）、DECISIONS.md（ADR-0000~0009）、docs/reverse/ 四份规格（rest-api / storage-layout / config-formats / repo-semantics）、BOARD.md（只读）
- 产出: M1 全部工程票 14 张（T-7~T-20），本文为唯一落盘；BOARD.md 录入由主会话执行
- 结论: 状态 done；14 张（P0 x12 / P1 x0 / P2 x1 + 可选并行 P2 x1）；最大并行波 3 张（T-8/T-9/T-10）；关键路径 T-7→T-9→T-12→T-13→T-14→T-15→T-16→T-18→T-19

---

## 一、票据列表（T-7 ~ T-20，共 14 张）

### 脚手架 / 工程化（最前）

**T-7** [P0] 工程脚手架：module + 目录骨架 + Makefile + lint/test + CI `role:devops-engineer` `area:仓库根（go.mod、Makefile、.golangci.yml、.github/workflows/、.gitignore、cmd/binflow-server 骨架、internal/*/doc.go 占位）` `dep:（无，全局前置）`

AC:
① FR-1-AC1/AC2/AC3/AC5：module 路径 `github.com/lzwzzy/binflow`（Q7）；`make build|test|lint` 全绿、`gofmt -l .` 为空、`go mod tidy` 无 diff；`bin/binflow-server --help` 退出码 0 且含 usage，`serve -c <yaml>` 参数占位（装配实现在 T-16）
② FR-1-AC4/AC6/AC7：CI workflow 触发 lint+test+build 三步（首跑绿由主会话确认）；构建打印二进制大小、>40MB 仅 warning；`CGO_ENABLED=0 go build ./...` 通过（Q5 零 CGO 基线）；CI 与 README 注明 GOPROXY 镜像回退（ADR-0005 受限网络）
③ `internal/` 按 architecture.md §2 建包骨架（仅 doc.go 职责说明，接口契约归各模块票的 api.go）；`make dev`/`make clean`；`data/` 入 .gitignore

### 存储与元数据基座（第 2 波，3 张并行）

**T-8** [P0] config 包：YAML + env 覆盖与 fail-fast 校验 `role:dev-go-core` `area:internal/config` `dep:T-7`

AC:
① `Load(path)` 实现架构 §8 全量字段 + env 覆盖（`BINFLOW_` 前缀、`__` 表层级）；匿名读开关**双键名等价支持**：PRD 形态 `security.anonymous_access`（env `BINFLOW_SECURITY_ANONYMOUS_ACCESS`，C27 原文）与架构 §8 形态 `auth.anonymous_read`，两键同给且不一致时启动报错（键名冲突已上报，见风险 R3）
② 校验规则 fail-fast：port 可解析、data_dir 可创建可写、driver 枚举、未知顶层键报错；`BINFLOW_ADMIN_PASSWORD` 只走 env 不入 YAML（秘密不入配置，ADR-0009）；默认值：listen `:8080`、anonymous 默认 true、driver sqlite、session_ttl/gc_grace 24h
③ table-driven 单测覆盖每条校验规则与 env 覆盖矩阵；Postgres 仅做枚举与 dsn 透传（启动报错行为归 T-16，FR-3-AC10）

**T-9** [P0] storage 引擎：上传会话 / checksum 寻址 / 原子落盘 / GC `role:dev-go-storage` `area:internal/storage` `dep:T-7`

review 提示：主会话须派**两名 code-reviewer（正确性 + 架构一致性）**；边界红线：storage 不得 import metadata（架构 §2）。

AC:
① 按架构 §3.1/§4 实现 `Engine/Session`（BeginSession / Append 边写边算 sha256+sha1+md5 / Commit(expect) / Abort）与磁盘布局 `blobs/<xx>/<sha256>`、`sessions/<uuid>/{data,state.json}`；落盘顺序 write→fsync(data)→rename→fsync(dir)（ADR-0006 不可换序）；Commit 命中已存在 blob → 弃会话数据返回既有引用 + per-checksum singleflight；sentinel `ErrBlobNotFound/ErrSessionNotFound/ErrChecksumMismatch`（wrap 后 errors.Is 可判）
② 启动清扫过期 session（ttl 24h）+ `GC(referenced,grace,apply)` mark-sweep **默认 dry-run**；table-driven 单测（`-race`）：去重（同内容两次 Commit 物理一份）、期望摘要不匹配拒绝、Abort 零残留、并发同 blob 收敛、kill -9 模拟崩溃窗口（仅 session 残渣，无可见半写 blob）
③ 流式约束：1GB 内容经 Engine 落盘进程 RSS 增量 < 256MB（包级采样断言，对应 FR-2-AC6/NFR-P3）；Commit 后 Stat 的三摘要与 sha256sum/sha1sum/md5sum 独立计算一致；`Open` 返回 io.ReadSeekCloser（为 T-20 Range 预留）

**T-10** [P0] metadata：SQLite Store + 迁移器 + 001_init DDL `role:dev-go-core` `area:internal/metadata` `dep:T-7`

review 提示：主会话须派**两名 code-reviewer（正确性 + 架构一致性）**；迁移 SQL 受架构 reviewer 把关（ADR-0007）。

AC:
① 架构 §3.2/§6：Store + 六子接口 + embedded 迁移器（`migrations/sqlite/001_init.sql` 事务应用 + schema_migrations 记账；`migrations/postgres/` 占位说明）；`modernc.org/sqlite` + PRAGMA（WAL / foreign_keys=ON / busy_timeout=5000）+ MaxOpenConns=NumCPU；零 CGO
② 001_init.sql 含 §6 全部 9 表与索引 + admin 种子（`BINFLOW_ADMIN_PASSWORD` 优先、缺省 `password` 且仅 admin 不存在时生效，argon2id PHC 串，NFR-S1）；**permissions 表按 PRD E-24 扩展**：命名 permission target（name 唯一）+ include/exclude patterns + principals(users)×(read/write/delete)，与架构 §6 扁平行的差异在迁移文件注释记录（缺口已上报，见风险 R4）
③ table-driven 单测（临时目录建库）：Open 幂等迁移、Node upsert by (repo_key,path)、ListByPrefix、FilterUnreferenced 反连接（分页流式）、TokenStore 仅存 sha256 摘要、删 repo 级联 nodes；时间戳一律 RFC3339 UTC 文本（两方言共同子集）

### 服务层与适配（第 3~5 波）

**T-11** [P0] auth 与 audit：认证 / Token / 路径 ACL / 审计骨架 `role:dev-go-core` `area:internal/auth、internal/audit` `dep:T-8,T-10`

AC:
① 架构 §3.4/§3.5：Authenticator（Basic 口令或 Token 充当口令、`X-JFrog-Art-Api`、匿名 nil,nil）、argon2id（t=1,m=64MB,p=4）、TokenRegistry（Issue 32B 高熵随机 / Verify / Revoke，DB 只存 sha256(plaintext)，明文仅签发返回一次，NFR-S2）、audit.Logger 尽力而为 Append
② Authorizer.Can：admin 全通过；命名 permission target 命中（repo + include/exclude `**`/`*` 两级通配 + principals）授 r/w/d，exclude 优先、无行=拒绝；匿名仅 `anonymous_read==true` 时内容 GET/HEAD 放行，写与管理面一律拒（ADR-0009）；改密（旧口令校验 + 哈希更新）
③ table-driven 单测：权限矩阵（admin/授权/未授权/匿名 × r/w/d × 匿名开关）、token 签发-验证-吊销-过期、改密后旧口令失效；包 doc 注明脱敏红线（NFR-S3 端到端验证归 T-18）

**T-12** [P0] repo.Service：local 用例编排与仓库 CRUD `role:dev-go-core` `area:internal/repo` `dep:T-9,T-10,T-11`

review 提示：Put 事务边界为正确性关键（blob Commit 成功后才写 node+blob link 单事务，禁止反向），建议加派正确性 reviewer。

AC:
① 架构 §3.3 Service（Get/Put/Delete/List + 仓库 CRUD）：repo key `[a-z][a-z0-9-]{1,62}`（PRD FR-3-AC4，与架构 DDL 注释 {1,31} 冲突取 PRD，见风险 R5）+ 保留字 `api`/`v2` 拒绝（ADR-0008）+ rclass 仅 local（remote/virtual 返回「M3 支持」400 语义）+ packageType M1 仅 generic
② 吸收 T-3 规格结论：同路径**同 checksum 幂等重传免覆盖权限检查**；不同 checksum 视为覆盖、需对旧 node 有 delete 权限否则 403（message 含 needs DELETE permission）（repo-semantics §3 高置信度）；覆盖保留 created/createdBy、更新 modified 系字段；Delete 只删 node 引用（目录递归删 + prune 空父目录）、不存在返回 ErrNodeNotFound（幂等 404）；目录 node（尾斜杠）创建与 children 聚合
③ 删仓：非空不带参数 → 错误且 message 含 `deleteContent` 提示；`?deleteContent=true` 删全部 nodes（FR-3-AC5）；table-driven 单测以 fake storage/metadata 驱动全分支（含元数据事务失败回滚不留 node）

**T-13** [P0] adapter SPI 与 Generic 适配器（内容路径四动词）`role:dev-registry-adapter` `area:internal/adapter、internal/adapter/generic` `dep:T-3,T-11,T-12`

AC:
① 架构 §5.1 SPI（Register 重名 panic / All / Layout：首段=repoKey 余为 relPath；dot-segment 归一化并拒绝逃逸 repo 根，`..`/`%2e%2e` 编码变体 → 400（FR-4-AC10，NFR-S4）；双斜杠/空段/超 512 字符 → 400（FR-4-AC11））；generic Handler：PUT → **201** + `Location` + `X-Checksum-Sha256` 头 + FileInfo JSON（uri/downloadUri/repo/path/created/createdBy/**size 为字符串**/mimeType/checksums/originalChecksums，rest-api §1.2 高置信度）；尾斜杠 PUT 建目录 → 201（§5.5 校准项 4 定案）；GET/HEAD → X-Checksum-Sha256/Sha1/Md5 + `ETag: <sha1>` + Content-Type 未知默认 octet-stream（FR-4-AC1/2/3/13）；DELETE → **204 无 body、重复删 404 幂等**（§5.5 校准项 1 定案，FR-4-AC8）；404 为 JSON 错误体（FR-4-AC12）
② 校验头语义（§5.5 校准项 2/3 定案）：`X-Checksum-Sha256/Sha1/Md5` 逐算法独立校验，**不一致 → 409**（repo-semantics §5 高置信度 client-checksums 策略，message 含 received/actual；PRD v1.1 的 400 已上报回写，见风险 R1）；`X-Checksum-Deploy: true` 缺专用头 → 400，格式非法或 blob 未命中 → **404**，命中 → 201 零传输（FR-4-AC6/AC7）；`X-Explode-Archive` → 400（E-25）；路径 `:properties`/`:statistics` 后缀 → 409（rest-api §1.2 高置信度）
③ 真实客户端集成测试：httptest Server + `curl`（os/exec）跑 C07/C08/C09/C13/C14(断言 409)/C15a/C15b/C16/C18/C23 等价断言 + 慢上传中断（--limit-rate + timeout KILL）后 404（FR-2-AC3 包级复现）；规格低置信度项（`?atomic=true` 中间态、checksumDeployed 对外可见性）不作 AC，按 BinFlow 自有语义处理并在包 doc 记录差异

### HTTP 层与装配（第 6~8 波）

**T-14** [P0] httpapi 核心：Server / middleware 链 / 错误信封 / 路由分发 `role:dev-go-core` `area:internal/httpapi（核心）、internal/console（占位）` `dep:T-8,T-11,T-13`

AC:
① 架构 §7：stdlib ServeMux 路由表——`/healthz` `/readyz`（无前缀）；`/binflow` 前缀剥离后按保留段（`api`；`v2` [M2] 预留 404）与 repo key 分发（查 repositories → 按 package_type 挂 adapter）；`/binflow/api/system/ping`（200 纯文本 OK，免认证）、`/api/system/version`（如实版本 + `product:"BinFlow"`，Q4）、`/binflow/api/v1/health`（status ok + storage 可写 + metadata 可用，E-22/FR-6-AC2）、`/binflow/api/v1/storage/stats`（blob 数/逻辑/物理字节，E-23）；console 占位 JSON
② middleware 链顺序固定（§7.2）：requestID→accessLog→recover→CORS→authenticator→authorizer→handler；每请求一行结构化日志（method/path/status/duration_ms/remote_addr/user 匿名记 anonymous/bytes_in/out，**绝不记录认证头**，NFR-S3）；统一错误信封 `{"errors":[{"status":..,"message":..}]}` 适用于全部端点含内容路径（rest-api §0 高置信度定案，**取代架构 §7.3 占位单对象形**，见风险 R6）；E-26 全矩阵 404：一切非 /binflow 根路径（`/artifactory/**` message 提示 /binflow、`/v2/**`、`/api/**`）与 /binflow/api/ 未实现端点（npm/pypi/search/…）
③ 认证分层：内容 GET/HEAD 匿名放行、写与 `/binflow/api/**` 恒需认证（401 + `WWW-Authenticate: Basic realm=...`；已认证无权 → 403；匿名被拒按 401 挑战，rest-api §1.4）；SIGTERM → Shutdown(30s) → 关 metadata → 退出码 0（§7.4/FR-6-AC5）；E-26 与 middleware 链 table-driven + httptest 覆盖

**T-15** [P0] Artifactory 兼容 REST：repositories / storage / security + /api/v1 permissions `role:dev-registry-adapter` `area:internal/httpapi（兼容端点 handlers，与 T-14 串行）` `dep:T-3,T-14`

AC:
① 仓库 CRUD（E-04~E-08）：`GET /binflow/api/repositories`（type 升序 + key 升序、`Cache-Control: no-store`、字段 key/description/type 小写/packageType/url）；`PUT /{key}` 新建 → **200**（rest-api §2 高置信度校准：200 纯文本 `Successfully created repository '<key>'`；PRD v1.1 的 201 已上报回写，见风险 R2）、已存在 → 200 更新；key 非法 → 400 E-01（FR-3-AC4）；remote/virtual → 400（E-07/C26）；DELETE 空仓 2xx、非空需 `?deleteContent=true`（FR-3-AC5）
② `/api/storage`（E-09/E-10）：文件 FileInfo / 目录 FolderInfo（children[{uri:"/<name>",folder}] 按名排序）；字段全集按 rest-api §3 高置信度（§5.5 校准项 6 定案）：uri/downloadUri/repo/path(/ 前导)/created(ISO8601 带毫秒与时区)/createdBy/lastModified/modifiedBy/**lastUpdated/size(字符串)/originalChecksums**/mimeType/checksums{sha1,md5,sha256}；`?list&deep&depth`（P2，FR-3-AC8）：files[].uri 相对路径，**匿名 → 403、根目录 → 400 `Cannot list files of root.`、文件目标 → 400**；`?properties` 等未实现 → 404 E-01
③ security + permissions：`PUT /api/security/password`（C20）、`POST /api/security/token` → 200 `{access_token, token_id, expires_in}`（**校准来源 auth-model.md 缺位，按 PRD 暂定字段以 BinFlow 自有语义实现**，见风险 R7）、`POST /api/security/token/revoke`（token_id 或 token）、`POST/GET /api/security/users`（响应永不含口令字段，FR-5-AC11）、`/binflow/api/v1/permissions` CRUD（E-24，FR-5-AC8/AC10）；C03/C05/C06/C10/C16/C20/C21/C22 序列以 httptest + curl 断言

**T-16** [P0] cmd 装配与生命周期：serve / gc / admin 引导 / 启动检查 `role:dev-go-core` `area:cmd/binflow-server` `dep:T-14,T-15`

AC:
① 构造注入装配链（无全局单例）：config → metadata.Open(自动迁移) → storage.Engine → auth/audit → repo.Service → httpapi.Run；subcommand `serve`（默认，`-c binflow.yaml`、数据目录默认 `./data`、`BINFLOW_HOME` 覆盖，FR-6-AC5）与 `gc [--apply]`（默认 dry-run 打印清单，ADR-0006）
② 启动行为：检测到 admin 缺省口令 `password` 打 WARN（ADR-0009）；`metadata.driver=postgres` → 启动报错退出非 0 + 日志「Postgres 支持未启用」（FR-3-AC10）；启动清扫过期 session；SIGINT/SIGTERM 优雅停机退出码 0；空库冷启动到 ping OK < 2s（NFR-P1，附轮询计时输出）
③ `scripts/smoke.sh` 裸二进制一键冒烟（C01/C03/C07/C08 最小链路，供 T-17/T-18 复用）；参数行为单测

### 部署与文档、QA（收尾）

**T-17** [P0] 开发环境：Dockerfile / compose + README 快速开始 `role:devops-engineer` `area:deploy/dev（Dockerfile、compose）、README.md` `dep:T-16`

AC:
① FR-6-AC1/AC2/AC3：`docker compose up -d --build` 30s 内 ping 返回 OK；`/binflow/api/v1/health` 200 status:ok；volume 持久化经 restart 与 down+up（不 `-v`）两轮上传物仍在（C29）；镜像 `CGO_ENABLED=0` 构建、data_dir=/var/lib/binflow、EXPOSE 8080
② FR-6-AC4 + NFR-S6/S8：README「快速开始」五步命令（构建→启动→建仓→上传→下载→校验，URL 全 `/binflow`）逐行可复制；明示匿名读默认开与关闭方法（`security.anonymous_access` / `BINFLOW_SECURITY_ANONYMOUS_ACCESS`）、缺省 admin 口令仅限评估、`BINFLOW_ADMIN_PASSWORD` 用法、「M1 明文 HTTP 仅限内网/本机」
③ compose 含健康检查（/readyz）与 `stop_grace_period` ≥ 30s（§9）；全新 clone + 空 Docker 环境逐行复跑 README 全部退出码 0（附输出证据）

**T-18** [P0] QA：M1 功能矩阵验收（PRD §7 场景 1/3/4/6/7）`role:qa-engineer` `area:验收（curl 功能全链路）` `dep:T-16,T-17`

AC:
① 按剧本执行并附原始输出：工程基线（make 三件套 + 零 CGO）、仓库生命周期（C03→C04→C05→C06→C19，**C03 期望已校准为 200**）、roundtrip（C07→C08→C09→C10→C13→C15→C18，**C14 期望已校准为 409**）、边界拒绝（C24→C25→C26 + FR-4-AC10/AC11 路径穿越与非法路径变体）、C28
② 认证与 ACL 双模式（C02→C20→C21→C22→C23 匿名读默认开 → C27 关闭匿名读含 read 权限差异化）；专项复核校准项落地：DELETE 204 + 重复 404、checksum-deploy 未命中 404、**同 checksum 幂等重传免覆盖权限**（admin 预置 ci-out/x.bin 后仅 write 权限用户重传同内容 2xx、传不同内容 403）、mkdir 尾斜杠 201、item info 字段全集与 size 字符串形态（jq 断言用 `tonumber`）、`?list` 匿名 403
③ NFR-S1/S2/S3 抽查：SQLite 文件 grep 无明文口令/Token、服务日志 grep 无 Authorization/凭据；任何 5xx 或非预期码打回对应票（报告含票号定位）

**T-19** [P0] QA：存储完整性 / 性能 / 持久化 + README 复跑（PRD §7 场景 2/5/8/9/10）`role:qa-engineer` `area:验收（存储/性能/持久化）` `dep:T-18`

AC:
① 存储完整性：C12 去重前后 blob 计数不变（FR-2-AC1）、慢上传中断后 404 且计数不变（FR-2-AC3）、kill -9 重启后历史上传 200/中断路径 404/health 200（FR-2-AC4）、覆盖写幂等（FR-2-AC5）、1GB 流式 RSS 增量 < 256MB（FR-2-AC6/NFR-P3）
② 性能与持久化：冷启动 < 2s 双路径计时（NFR-P1）、100 并发 GET 10MB 全 0 退出码且 0 条 5xx 日志（NFR-P2）、C29 compose restart/down+up 两轮、`gc` dry-run 与 `--apply`（grace 过期后物理回收）、空载内存 60s 采样记录（NFR-P4，记录不设门）
③ 全新环境 README 快速开始复跑（FR-6-AC4）；产出 M1 QA 总报告（reports/agents/T-19-qa.md），给出 DoD §8 第 1/2 条结论（P0/P1 AC 通过清单 + P2 处置建议）

**T-20** [P2] Range 与条件请求（FR-4-AC14，Q6 定案允许延后） `role:dev-registry-adapter` `area:internal/adapter/generic` `dep:T-13`

AC:
① 单区间 Range → 206 + Content-Range 字节正确；非法区间 → 416 + `Content-Range: bytes */<total>`（rest-api §1.4 高置信度）；多区间与 If-Range 不做 → 200 全量（不得 5xx）
② If-None-Match（容忍 `W/` 与引号）/ If-Modified-Since → 304 无 body（ETag=sha1 已由 T-13 提供）
③ curl `-r`/`-z` 真实客户端断言 + table-driven 单测；**M2 Docker blob 拉取开工前必须 done**，延期须 BOARD 记录

---

## 二、分批派发建议（任何一批宽度 ≤ 4，批内 area 无重叠）

| 批次 | 票 | 宽度 | 说明 |
|---|---|---|---|
| 1 | T-7 | 1 | 全局前置，module/目录不定无法开工（首波） |
| 2 | T-8 / T-9 / T-10 | 3 | config、storage、metadata 三包互不 import，最大并行点 |
| 3 | T-11 | 1 | auth 依赖 config+metadata（Principal/TokenStore） |
| 4 | T-12 | 1 | repo.Service 依赖三基座 + auth 类型 |
| 5 | T-13 | 1 | adapter 依赖 repo.Service；spec 票 T-3 已 done |
| 6 | T-14 + T-20 | 2 | httpapi 核心与 Range 票（仅依赖 T-13）并行填宽 |
| 7 | T-15 | 1 | 与 T-14 同包（internal/httpapi），必须串行 |
| 8 | T-16 | 1 | cmd 装配需全部 handler 就绪 |
| 9 | T-17 | 1 | compose/README 需可运行二进制 |
| 10 | T-18 → T-19 | 1 | QA 两票串行（共用实例环境，避免互相污染数据） |

review 安排：T-9、T-10 双 code-reviewer（正确性 + 架构一致性）；T-12 建议加派正确性 reviewer；其余单 reviewer。主会话在 qa 通过后统一提交。

关键路径：T-7 → T-9 → T-12 → T-13 → T-14 → T-15 → T-16 → T-18 → T-19（存储正确性与 httpapi 兼容层为最长链）。

---

## 三、依赖风险与 PRD/架构矛盾点（编号重排为 R1~R9，供 conductor 转交）

### 需 PM 回写 PRD v1.2 的两处（票内 AC 已按校准值写死，不留悬置）

- **R1** X-Checksum 不一致状态码：PRD FR-4-AC5/C14/E-11 写 400；repo-semantics §5 高置信度为 **409**（client-checksums 默认策略，message 含 received/actual）。T-13/T-18 已按 409，C14 期望值需回写。
- **R2** PUT 建仓状态码：PRD FR-3-AC1/C03 写 201（新建）；rest-api §2 高置信度为 **200 纯文本**。T-15/T-18 已按 200，C03 期望值需回写。

### 需 architect 回写 architecture.md 的四处（票内已按 PRD/规格实现，不阻塞）

- **R3** 匿名读键名冲突：PRD `security.anonymous_access`（env `BINFLOW_SECURITY_ANONYMOUS_ACCESS`）vs 架构 §8 `auth.anonymous_read`（env `BINFLOW_AUTH__ANONYMOUS_READ`）。T-8 双键等价 + 冲突报错兜底；建议以 PRD 用户可见名为准统一。
- **R4** permissions 数据模型：PRD E-24 命名 permission target（name/repos[]/patterns/principals）vs 架构 §6 扁平行（无 name、单 path_prefix）。T-10 已按 PRD 扩展 DDL。
- **R5** repo key 长度：PRD `{1,62}` vs 架构 DDL 注释 `{1,31}`，取 PRD。
- **R6** 错误信封：架构 §7.3 占位 `{"error":{...}}` 被 rest-api §0 高置信度推翻，统一 `{"errors":[...]}`（含内容路径），T-14 已定。

### 规格缺位

- **R7** PRD §5.5 校准项 5 指定来源 `docs/reverse/auth-model.md` **不存在**（T-3 只交付四份）。token 响应字段按 PRD 暂定值以 BinFlow 自有语义实现；建议 conductor 安排 reverse-engineer 补 auth-model 规格或 PM 修订校准来源。

### 低置信度项处置（均不作为 AC）

- **R8** rest-api 低 3 项（atomic 删除中间态、GC 端点流式格式、checksumDeployed 对外可见性）与 storage-layout 低 3 项（node_path 根特例、_pre 清理触发点、GC cron 语义）——BinFlow 布局本就与 Artifactory 不同（sha256 寻址 vs sha1、sessions/ vs _pre），六项对 M1 实现均无约束力，照常「按 BinFlow 自有语义实现并记录差异」，无需动态验证票。

### 流程性提示

- **R9a** 本批 14 张票已含 PRD 未显式列出的工程必需项（优雅停机、错误信封统一、日志脱敏、GC CLI、启动清扫），来源分别在架构 §7.4/§7.3/§6.3 与 ADR-0006，无擅自加戏。
- **R9b** R1/R2 的 C14/C03 期望值变更会导致 T-18 直接执行 PRD 原文时误判失败——PM 回写（建议随 v1.2）应赶在 T-18 派发前完成；若来不及，主会话派发 T-18 时需在指令中注明两处校准值。

---

## 输出契约

- 状态: done
- 产出: /Users/lzw/dev-center/reports/agents/T-6.md（本文件）；BOARD.md 录入由主会话执行
- 统计: 14 张票（P0 x12 / P1 x0 / P2 x1[T-20]），最大并行波 3 张（T-8/T-9/T-10），首波 1 张（T-7）
- 依赖链: 见「二」末行关键路径
- 风险/待澄清: R1~R9（见「三」）
