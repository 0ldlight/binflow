# T-455 — 权限编辑两步弹窗 + 矩阵五列（消费 T-444 新 wire）

状态: review（四门绿 + a11y sweep 见 §5 证据，等 conductor 复核收编）
role: dev-frontend ｜ area: web/src/pages/security（权限域）+ web/e2e（security/m8/m9/m16 spec）+ 锚册三文件的 T-455 批增量
日期: 2026-09-04 ｜ 分支: develop（工作树直改，零 git 操作、BOARD 零写）

## 0. area 映射核对

票面 area = 权限编辑器页面组 + e2e 权限域。实际落点全在 web/src/pages/security/
+ web/e2e/{security,m8,m9,m16} + 三份登记文件（console-ux.md §0/§10.5、parity 册
B-1.6/B-2.16、anchor-audit.mjs STOP 表——T-439/T-453 同款先例，FE 票回写锚册）。
服务端 diff=0（纯 FE 票）。artifacts/NodeDetail 的 ?permissions 字母视图（T-447 域）
零触碰。

## 1. 活体取证（7.161.20 :8082，admin 只读，零写入——弹窗只开不存）

证据目录 **reports/agents/t455-probe/**（截图 + url 边车 + probe-evidence.txt 逐字
DOM 摘录；探针脚本 /tmp/t455-af-probe*.mjs 会话级，不入库）：

- **E1 权限新建/编辑 = 三步向导路由页**（/permissions/create、/permissions/edit/
  readers）：面板 data-cy = panel-Resources / panel-Users / panel-Groups，aria
  「Step 2 of 3: Users」；页脚 Cancel｜Create（编辑态 Save）。
- **E2 Users/Groups 矩阵列**（Repositories 资源型）：**Read / Annotate /
  Deploy/Cache / Delete/Overwrite / Manage 五列**（Builds 资源型 = Read/Annotate/
  Deploy/Delete/Manage；Release Bundles = Read/Annotate/Create/Promote/Distribute/
  Delete/Manage——BinFlow 无此两域，登记差距不伪造）。
- **E3 Add Repositories = 两步弹窗**：双步头「1 Select Repositories」「2 Set
  Patterns (Optional)」**常驻可点**，footer Cancel/OK，双列穿梭（Available ↔
  Selected，Moving item(s): N）。
- **E4 联动形态不可观测**：矩阵勾选门在「资源型选定 + 主体行添加」之后，本实例
  0 仓库（"0 Available Repositories"，Any Local/Any Remote 预置桶亦不列出）——
  write↔annotate 勾选联动**登记未验证，不伪造**。裁定独立位列零联动（§3④）。
- 播障记：7.161 onboarding 浮层反复拦截 Playwright 点击（Vue 重渲染），取证走
  DOM 摘除 + JS dispatch；不影响结论。

## 2. 做了什么

### A. 消费 T-444 wire（预登记红点 + 兼容窗收口）

1. **四处 GET-echo 断言翻新**（T-444 §4.2 预登记，全部由红转绿）：
   - web/e2e/security.spec.ts:175 `['read','write']` → `['deploy-cache','read']`（排序形）
   - web/e2e/m8/permissions.spec.ts:170 `['read','write']` → `['read','deploy-cache']`（回显序）
   - web/e2e/m8/permissions.spec.ts:437 同上
   - web/e2e/m9/permissions-mholder.spec.ts:200 `['manage','write']` → `['deploy-cache','manage']`
2. **词域双层收口**（api.ts 两函数单点）：
   - `normalizePermActions`（wire→UI：'deploy-cache'/'write'→write，闭集外丢弃）——
     编辑器水合 + grantsOfUser/grantsOfGroup 汇总两处消费，T-444 §4.2 登记的
     「展示面漂移」（widgets includes('write') 漏 deploy-cache）就此消解；
   - `wireActions`（UI→wire：write→正名单 deploy-cache，按 read/deploy-cache/
     annotate/delete/manage 序输出）——保存路径序列化，GET→PUT→GET 往返稳定
     （票面「保存=回显数组原样回发」的兼容窗升级为正名单形回发——PUT 的 write
     别名仅收不发，两形等效零语义差）。
   - `PermAction` 扩 'annotate'（UI 五词域）；GET/PUT 体的 principals 诚实类型化为
     `WireAction[]`（'deploy-cache' 不在 UI 词域——类型层不再说谎）。

### B. 矩阵五列 + 两步弹窗（7.161 对齐）

3. **矩阵五列**：编辑器用户/组矩阵 + 用户/组页只读汇总矩阵
   （PermSummaryTable）列序 = read / annotate / write / delete / manage（对位
   E2 活体列序）；列头 title 注 7.161 标签与语义边界（write=Deploy/Cache、
   annotate=Annotate、delete=Delete/Overwrite、manage 不变）。
4. **勾选联动裁定 = 独立位列零联动**：活体不可观测（E4）+ T-444 拆分语义
   write 不携带 annotate（零提权）——UI 自动联动会在部署授权上**越权附带属性写**，
   独立位列是唯一正确形态。表底 admin-note 明示「write 与 annotate 互不隐含」。
5. **两步资源对话框可点步头**：perm-res-step-1/2 双步头常驻可点直跳（对位 E3
   活体；aria-current="step" 标当前步、当前步高亮加粗），既有 perm-res-next /
   上一步 / perm-res-ok / perm-res-cancel / perm-res-step 链零回退（m8 键盘腿
   依赖）。MUI Button 承载（mui-native 纪律），布局类 .perm-res-steps 自持
   security.css。
6. **diff 确认/dirty 语义零变化**：targetdiff ACTION_ORDER 扩 annotate 位；
   水合归一后 baseline 与表单同词域，未改动保存钮禁用（兼容窗零漂移，e2e 断言）。

## 3. 变更清单（逐文件）

**生产代码（5 文件）**

| 文件 | 一句话 |
|---|---|
| web/src/pages/security/api.ts | PermAction 扩 annotate + PERM_ACTIONS 五列序 + WireAction 型 + normalize/wireActions 双点收口 + grantsOf* 归一 + 注释口径 |
| web/src/pages/security/PermissionEditorPage.tsx | 水合归一/保存序列化 + 矩阵五列表头（title 语义）+ colSpan 6 + 两步对话框可点步头（perm-res-step-{1,2}）+ 注释口径 |
| web/src/pages/security/widgets.tsx | PermSummaryTable 五列 + 列头 title（annotate/write/delete 7.161 标签）|
| web/src/pages/security/targetdiff.ts | ACTION_ORDER +annotate 位（排序全序稳定即可）|
| web/src/pages/security/security.css | .perm-res-steps 步头排布局类 |

**e2e（5 文件：1 新 + 4 翻新）**

| 文件 | 一句话 |
|---|---|
| web/e2e/m16/t455-perm-five-columns.spec.ts（新）| 四腿：五列+annotate 往返+payload 正名单形（网络层）/ 兼容窗水合（legacy write 勾选+零漂移禁用+补勾/撤销 annotate 双向）/ 步头双跳+Next 链 / readonly annotate 只读 |
| web/e2e/security.spec.ts | GET-echo 断言翻新（:175）|
| web/e2e/m8/permissions.spec.ts | GET-echo 断言翻新 ×2（:170/:437）+ 注释口径 |
| web/e2e/m9/permissions-mholder.spec.ts | GET-echo 断言翻新（:200）|

**登记文件（3 文件，先例 = T-439/T-453 的 FE 票回写锚册）**

| 文件 | 一句话 |
|---|---|
| docs/design/console-ux.md | §0 版本史 v1.41 行 + §10.5 T-455 批块（2 名新锚 + 族扩成员留痕 + 联动裁定）|
| docs/design/console-artifactory-parity.md | B-1.6 翻正·已落（五列+联动裁定）；B-2.16 翻正·已落主体+预置桶候裁（三步向导差异留痕）|
| web/scripts/anchor-audit.mjs | STOP +v1.41 批（deploy-cache / t455-perm-five-columns / t455-probe / permissions-mholder——行文假阳性，v1.39 users-groups 同款）|

**证据（reports/agents/t455-probe/）**：5 截图 + url 边车 + probe-evidence.txt。

## 4. 锚册增量（v1.41）

- 新锚 2 名：`perm-res-step-1` / `perm-res-step-2`（两步对话框可点步头，
  aria-current；A1/A2 过账——ledger PASS）。
- 族扩成员（零新族名）：`perm-matrix-cell-<kind>-<principal>-<action>` 扩
  annotate 成员；`perm-matrix{,-groups}` / `{user,group}-perm-matrix` 表锚零改名。
- 既有锚零退役、零改名；perm-res-next/step/ok/cancel 与 write 列 testid 全冻结。

## 5. 自测（实际执行——四门证据）

环境：nvm node v24.20.0；测试实例 = make console 同形 embed（npm run build →
internal/console/dist）+ `CGO_ENABLED=0 go build` 的 /tmp/t455-bin/binflow-server，
BASE=127.0.0.1:18097（18095 见 §7 端口事件后迁移）。

1. **tsc**：`npx tsc --noEmit` → 零输出（EXIT=0）。
2. **lint**：`npx eslint <本票全部触碰文件>` → 0 error；2 warning 均存量
   （PermissionEditorPage:490 水合 effect 与 UserDetailPage:94——`git show HEAD:`
   同文件基线对账同形，零新增）。仓级 `npm run lint` 的 33 warning 为他页存量债
   （react-hooks/set-state-in-effect 族），非本票区域。
3. **锚册**：`node scripts/anchor-audit.mjs --ledger` → **PASS**（A1/A2 双零）。
4. **build + 体积**（vite build ×2，HEAD 基线经 git worktree）：
   - PermissionEditorPage chunk：26.98 kB → **28.13 kB**（gzip 8.99 → 9.50，+0.51）
   - index 主 chunk：425.60 kB 持平（gzip 137.29→137.30）
   - security 域共享 CSS（api-*.css）微增（步头排布局类）；SPA 磁盘总量
     +4 KB（du 对账）。
5. **e2e（净窗：`ps aux | grep -c '[g]o test'` = 0）**：
   - 新面 `t455-perm-five-columns`：**4 passed**（首轮 1 腿因 spec 断言次序笔误
     修正后全绿——点击前断言 aria-current 的次序错误，非产品缺陷）。
   - 回归 `m8/permissions + security + rbac`：**14 passed**（24.2s）。
   - 回归 `--project=m9 m9/permissions-mholder`：**4 passed**（19.8s）。
   - 回归 `m8/users-groups`（汇总矩阵五列消费面）：**4 passed**（13.8s）。
   - a11y 双主题 `m8/a11y-sweep`：首轮 2 失败均为环境事件（§7：server 被 shell
     收割后端口被并行票占用 → 401/CONN_REFUSED，**非 axe 违例**）；换 18097
     重跑 → **3 passed (4.1m)，exit 0**（56 面 × 双主题 serious/critical = 0）。
   - **终局统一复跑（18097）**：`t455 + m8/permissions + security + rbac`
     **18 passed**（33.6s）+ `--project=m9 mholder` **4 passed**（20.0s）。
6. **服务端 diff=0**：纯 FE 票（grep 证：internal/ 零触碰）。

## 6. 契约漂移与遗留

1. **Any Local / Any Remote 预置桶（B-2.16 部分翻正）**：parity 册预登记「选仓
   双列 + Any Local/Any Remote 预置」，但 BinFlow wire 契约 repos[] 必须逐个指名
   现存仓库（服务端 400 unknown repository）——预置桶需 BE 先承接通配桶语义；
   且本参照实例 0 仓，预置桶呈现形态亦不可观测。**候裁**：BE 扩列承接 或 与
   Any Distribution 同口径 §9A-S8 缺位登记。FE 未建（不伪造注定 400 的入口）。
2. **7.161 编辑形态 = 三步向导路由页（Resources/Users/Groups）**：BinFlow 编辑器
   本体 stay-out（§9A-S1/E4——整页 + 测试器 + diff 确认是增强面），未向向导分步
   改造；两步弹窗指 Add Repositories 子对话框（已对齐可点步头）。差异已留痕
   parity 册 B-2.16。
3. **Builds / Release Bundles 资源型矩阵**（活体 E2）：BinFlow permission target
   只有仓库域——Build-info/Release Bundle 归 §9A-S8 域缺位，不建。
4. **write↔annotate 联动形态活体未验证**（E4）——已按独立位列实现并留痕裁定
   依据；若后续有仓实例可复测，证据位在 t455-probe/。
5. M17：write 别名移除评估（T-444 §6 原登记）——FE 侧保存路径已不再发别名形，
   别名消费面收敛为 API 直连用户。

## 7. 共享机器事件留痕（conductor 知悉）

- **18095 端口撞车**：本票测试实例（17:36 起，18095）在 17:48:57 被工具 shell
  进程组清理优雅停机；我的 setsid 重启在 macOS 不存在而失败；18:00:40 并行票
  **T-458 的实例（/tmp/t458-data）接管了 18095**，导致 a11y sweep 首轮 401
  invalid credentials（凭据不同实例）。处置：本票迁 18097（配置与 server 由
  harness 后台任务托管），未触碰 T-458 实例。**建议**：并行票的临时端口约定
  区间或登记（本轮 t449=18091 / t451=1809? / t443 / t458 各自为政）。
- 工作树期间 conductor 并行收编了 T-444/T-447（HEAD d9cb2bb → 931216a），
  与本票文件零交集；reports/agents/T-456.md 为他票产物未触碰。
