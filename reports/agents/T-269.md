# T-269 工作日志 — git 历史瘦身 dry-run + 执行手册

- 状态：done（dry-run 与手册交付；**未执行任何重写/force-push，远端零触碰**）
- 交付物：`deploy/git-history-slim.md`（执行手册）
- 试验场：`/tmp/t269/{lab-a,lab-b,lab-c,fresh}`（`--no-hardlinks` 克隆，与主仓对象完全隔离）

## 1. 现状探测（全部只读命令，截至 main@c89e07d）

- 仓库：832 commit（main 单线），8 个 tag（m1–m8-done），远端 origin（GitHub）+ vm（172.16.58.129，main 落后且为本地 main 祖先）。本地 main 与 origin/main 在探测起点一致（df519b9f），探测期间主会话正常前进至 c89e07d。
- 体积：`.git` 70 MB（69.46 MiB / 7408 个**松散**对象、零 pack）；repack 后可达历史 19.88 MiB；tracked checkout 16.3 MB / 1710 文件；工作树磁盘 1.03 GB（docs-site 构建产物为主，非历史问题）。
- 本地另有 216 个不可达对象（5.04 MiB，最大单对象 6.4 MB，reflog 残留），re-clone 即清。
- 关联 worktree：`/private/tmp/bf-triage`（detached），共享主仓对象库——已写入手册前置项。

### 巨 blob 清单（全历史仅 3 个 >1MiB）

| blob | 原始 | 打包 | 路径 | 引入 | 消除 | 含它的 tag |
|---|---|---|---|---|---|---|
| `1b610a24` | 70.18 MiB / 73,587,119 B | 0.54 MiB | BOARD.md | `5c34194` M7 closure 08-23 | `047e789` 同日修复 | 仅 m8-done |
| `2117712f` | 52.75 MiB / 55,314,725 B | 0.46 MiB | BOARD.md | `9a44168` M8 closure 08-24（m8-done 指向它） | `663721f` M9 planning 修复 | 仅 m8-done |
| `84991477` | 21.16 MiB / 22,190,720 B | 11.24 MiB | binflow-server 二进制 | `679dcc9` 08-21 | `bb04e12`（次一 commit，删+gitignore） | m5/m6/m7/m8-done |

**票面口径修正**：所谓"52+55+70 三个 BOARD blob"实为 2 个 BOARD blob（52.75 MiB=55.3 MB、70.18 MiB=73.6 MB，MiB/MB 换算造成"52/55"两种读数）+ 1 个误提交的 `binflow-server` 二进制（21.16 MiB）。且 **m7-done 不含 M7 巨 blob**——损坏发生在 tag 之后；两个 BOARD 巨 blob 仅存在于 m8-done 历史。

## 2. dry-run（git-filter-repo 2.47.0，brew 安装）

`--analyze`（只读）+ 三个 lab 全量重写实测：

| 变体 | size-pack | commit 数 | 备注 |
|---|---|---|---|
| 瘦身前（repack 基线） | 19.88 MiB | 832 | — |
| A `--strip-blobs-bigger-than 10M`（默认 prune-empty=auto） | 7.64 MiB | **830** | M7/M8 closure commit 被剪除；m8-done 落到无关 commit `f7c85ed`（a11y fix） |
| B `--strip-blobs-with-ids` + `--prune-empty never`（**推荐**） | 7.70 MiB | **832** | closure commit 保留（重写形态）；m8-done 正确 peel 到重写后的 `da55ce3` |
| （同 B 但 ids 换阈值无差异） | 7.71 MiB | 832 | 两选项当日等价 |

- 注：2.47.0 无 `--strip-blobs-biggest`（票面提到的选项不存在，实测报错后改用上述两选项）。
- 剥离形态：`binflow-server` 路径从受影响历史树**整体移除**（非空文件占位）；当前 HEAD tree sha `a11ed58` 逐字节不变，HEAD 的 BOARD.md（156,331 B，sha `371ba08f`）不变。
- fresh clone 模拟：`/tmp/t269/fresh` = 8.0 MB（原 70 MB 松散 / ~26 MB 打包）。
- 残留最大 blob 0.67 MiB（docs-site/package-lock.json）。

### tag 映射样本（方案 B 实测；正式执行以当次 ref-map 为准）

m1–m4-done **sha 不变**；m5 `cffde22→54f4ba8`、m6 `7419919→5067a60`、m7 `10372a8→33cc553`、m8 `1bfbddf→6e36b0f`。commit 前 531 个不变（早于 08-21 `679dcc9`），其后 301 个重写（样本 `679dcc9→50b57ea`、`c89e07d→d49ae71`）。映射产出法：filter-repo 自动写 `.git/filter-repo/{ref-map,commit-map}`。

## 3. 预期收益

| 指标 | 前 | 后 | 变化 |
|---|---|---|---|
| fresh clone `.git` | 70 MB（松散）/ 26 MB（打包） | 8.0 MB | −89%（du口径）/ −62%（pack 口径） |
| GitHub >50MB 告警对象 | 3 | 0 | 全消 |
| 历史原始字节 | +144 MB 巨 blob | 0 | cat-file/checkout 峰值消除 |
| 内容损失 | — | 无（工作树、832 commit message、看板现状全保留） | — |

## 4. 边界与安全声明

- 主仓零改动（终检：7408 松散对象、main@c89e07d、8 tag 原样、工作树未触碰）；`BOARD.md` 未改；无任何 git 写操作（连 gc 都没跑）；未向任何远端发起任何请求。
- 全部 filter-repo 实操发生在 `/tmp/t269` 的 `--no-hardlinks` 克隆中。
- 手册中 force-push 序列均标注【用户授权点】，未获授权不得执行。

## 5. 遗留

- 执行窗口的排期与全员公告由 conductor 决定（手册第 3 节 checklist）。
- GitHub 侧分支保护/tag 保护/Releases 绑定情况需在执行前人工核对（本票零远端访问，无法代查）。
- docs-site 构建产物入 tracked（`docs-site/build/`、`.docusaurus/`，git status 常年脏）是另一个历史体积隐患（当前 ~0.5 MiB 级），建议另开票处理 `.gitignore` 与已跟踪构建产物的移除。
