# T-L021-3 — maven 三小面对齐（client-blind 收尾）

Ticket:       T-L021-3 maven 三小面对齐票（LOOP 021 / 轨道 L021-3 / P1）——受理码 202 丢弃式 + XML 渲染 A 形 + 文件 404 措辞
Role:         dev-registry-adapter（领域实例：dev-package-maven）
Area:         internal/adapter/maven
Input:        裁定素材 reports/compatibility/L020-auth401-maven-faces.md（V6/V7/V8 + f1/f2a/f2b/f3a-c 逐字 wire）；l0203-wire/（A/B 双端 .hdr+.body）；级别分列证据 reports/compatibility/L013-maven-evidence.md §0-C1（组目录=逐字回放）+ known-divergence.yaml#maven/manual-put-metadata-visibility note（snapshot 目录=202 丢弃）；docs/reverse/maven-npm-pypi.md §1.4（计算器触发分类学——客户端 PUT 触发不在参照触发表内，与 202 丢弃一致）；模块级 A 形 wire l0142-wire/a/maven/{a1,e2}、l0134-wire/a/maven/{a2,a3,c2c}；票面（无裁定则按参照对齐实现）
Changes:      ①受理码（面一/V6）：put.go handlePut 在 sidecar 分派后加 snapshot 版本目录 maven-metadata.xml 的 202+0B 丢弃受理（acceptDiscardedMetadata——drain body、无 CT/无 Location/无信封、不落盘不触发重算）；门=KindMetadata+标准文件名+末段 -SNAPSHOT+calc 已接+row 为 local（routed virtual 取 member 行）——remote/未路由 virtual 落回存储链保留 405；组目录（version-group）PUT **保持** 201+落盘+同步重算（L013 C1 级别分列——A 组目录=逐字回放，R-21 仍待裁，不越票面扩张）；配套 api.go 加 WithAuthorizer(repo.Authorizer) 可选 ACL seam（npm WithAuth 惯例）——丢弃路径不过服务闸门，401/403 渲染逐字节复刻 writeServiceError 的 ErrUnauthorized/ErrForbidden 臂（防受理变授权旁路）；calc.go afterMetadataDeploy 保留但 snapshot 分支早退（该面已不可达，防御性级别卫）。②XML 渲染（面二/V7/R-20）：calc.go metadataXML 加 ModelVersion attr+Version 移 Versioning 之后（尾部）；versioningXML 字段序改 Latest,Release,Versions,LastUpdated,Snapshot,SnapshotVersions——两文档族字段互斥，单结构体按族坍缩出各自 A 形序（模块：latest,release,versions,lastUpdated；snapshot：lastUpdated,snapshot,snapshotVersions）；recalcModule 组文档加 Version=latest 尾（l0134 a3 wire：snapshot latest 原样）；renderMetadata 空 ModelVersion 默认 "1.1.0"；virtual_metadata.go 合并组文档 Version=重算 latest。③404 措辞（面三/V8）：handler.go notFoundMessage → `File not found.; Path: '<repo>:<path>'`（A 形逐字，含 `.;` 双标点 quirk；一处函数收三臂+sidecar/virtual 404 同源面）
Files:        修改：internal/adapter/maven/{api.go,calc.go,handler.go,put.go,virtual_metadata.go,maven_test.go,calc_test.go}；新增：internal/adapter/maven/metadata_faces_test.go（三面 replay 测试）；删除：无。（deploy/compose/.env.uat 版签更新为 uat-l0213-b906e7cd——UAT 实例配置，gitignored 本地文件）
Tests:        metadata_faces_test.go：TestMetadataPutAcceptanceDiscard（202+0B+无 CT/Location、存储节点仍服务端形、回读 byte-identical 基线（f2a==f2b）、组目录对照臂=201 存储链、remote 405/未路由 virtual C5 405/匿名 401+challenge/无写授权 403 信封逐字节）；TestMetadataRenderAForm（snapshot+模块两文档 A 形元素序逐行断言，时间戳字段容忍值）；TestFileNotFoundWordingAForm（三臂 ghost jar/dir/meta 措辞+信封逐字节）。calc_test/maven_test 既有面更新：version-dir PUT→202、组目录 PUT 保持 201；TestCalcMetadataPutAuthoritative 版本级臂改基线 byte-identical 断言；modelVersion="1.1.0" 子串误命中修正（TestCalcDeleteCascade 改元素形断言）。scratch 验证（已删）：固定计算器时钟 + l0203 fixture 事实 → 渲染 589B 与 A-f2a wire **逐字节相同**
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter（空）&& golangci-lint run（0 issues）；go test ./internal/adapter/... -count=1（全绿）；go test ./internal/adapter/maven/ -count=1 -race -run "TestCalc|TestMetadata|TestFileNotFound"（ok 76s）；UAT 重建 docker compose -p binflow-uat --env-file deploy/compose/.env.uat -f deploy/compose/docker-compose.yml up -d --build（binflow:uat-l0213-b906e7cd-alpine）；双端重放 bash tools/difftest/l0203/maven-faces.sh；mvn 腿：mvn -s settings.xml -DaltDeploymentRepository=binflow-uat::default::http://127.0.0.1:8083/binflow/l0213-mvn-local deploy（×2）+ dependency:get（fresh m2）
Outputs:      双端重放（A=Artifactory 7.161.20 :8082，B=uat-l0213-b906e7cd :8083）：f1 双端 **202+0B 无 CT**（B 原 201+840B 信封）；f2a/f2b 双端 589B A 形（modelVersion 属性+version 尾+lastUpdated 前置），f2b diff 仅时间戳字段（双端各自钟；同秒时逐字节相同——早轮复跑实证 `f2b: byte-identical` 同 sha256）；f3a-c 三臂 message 逐字同 `File not found.; Path: '<repo>:<path>'`；residue A=0 B=0。mvn 3.9.16 真腿：deploy#1 exit 0、deploy#2 exit 0 且 buildNumber 1→2 服务端算术（snapshot 时间戳 20260914.042713，时间戳改写不受丢弃影响）、resolve（fresh m2）exit 0 落 `faces-app-1.0-20260914.042713-2.jar`。scratch 589B：`byte-identical to A-f2a: 589 bytes`
Compatibility: 契约对照：无 maven 契约条目变更（docs/compatibility/contracts/ 无 maven.yaml 条目涉三面）；台账翻绿建议（交 compatibility-engineer/conductor）：①`maven/metadata-xml-shape-three-deviations`（R-20）→ **resolved**（三形态全补齐+双端 wire 逐字节/结构同构+mvn 真腿三绿）；②`maven/manual-put-metadata-visibility`（R-21）→ **范围收窄**：snapshot 目录受理码半面已对齐（A=B 202 丢弃），剩组目录半面（A 逐字回放 vs B 201+同步重算）仍 UNKNOWN 待 R-21 裁——R-21 呈批口径应只留组目录分列；③L020 差异清单 V6/V7/V8 三行 → fixed（B 侧本轮消灭）；L019 Risks③④⑤ → 关闭。残留差分面（归一域非本票）：404/202 CT charset（`application/json` vs `;charset=ISO-8859-1`）+ 错误信封缩进（` : ` vs `: `）= L018 §6-R2/R7 归一提案域，f1 202 的 Content-Length:0 vs chunked 同域。可差分 surface：PUT `/<repo>/<group path>/…/<v>-SNAPSHOT/maven-metadata.xml`→202+0B+无 CT；GET snapshot 版本 metadata→589B A 形（modelVersion/version 尾/lastUpdated 前置）；GET 模块 metadata→A 形（latest,release,versions,lastUpdated+version 尾=latest）；GET ghost file/dir/metadata→404 `File not found.; Path: '<repo>:<path>'`；组目录 metadata PUT→201+ItemCreated（不变，R-21 域）
Security:     路径穿越无新面（Parse 严格 maven 布局+共享 content-path 规则前置）；丢弃路径授权：匿名 401+WWW-Authenticate 复刻、无写授权 403 逐字节复刻存储链（WithAuthorizer seam，测试逐字节钉）——**注意：seam 需 cmd/binflow-server/main.go 一行接线**（cmd/ 越界未接，见 Risks）；remote 代理无涉及；SSRF 无涉及
Performance:  丢弃受理反而减负（无 blob commit/无账本行/无重算，仅 drain）；渲染改动零成本（字段序）；无大制品流式面变化
Risks:        ①WithAuthorizer 接线缺口：main.go:410 未加 `.WithAuthorizer(authz)`（cmd/ 禁区）——prod 丢匿名闸（httpapi 已拦 PUT 凭据，实际不可达）但缺无写授权 403 角（A=403、当前 prod=202）——建议 conductor 补一行接线票或 architect 并入（npm WithAuth 同款惯例）；②组目录 PUT 行为本轮未对齐（票面仅 snapshot 面；R-21 待裁维持）；③f2b 双端时间戳天然异秒（各自钟），差分需时间戳归一（既有 normalize 域）；④UAT 复验途中 Docker Desktop 第 8 次全灭（buildkit EOF+全容器重启+artifactory JVM 僵死 10min+——`docker restart artifactory` 5min 复苏）；mvn 腿覆盖 3.9.16 单版本（与 L019 同版本口径）
Blockers:     无（面一授权角已 in-area 缓解+越界接线列为 Risks①建议票）
Next:         ①R-20 翻绿+R-21 范围收窄落账（compatibility-engineer）；②main.go WithAuthorizer 一行接线票（conductor 派 dev-go-core/cmd 域）；③组目录逐字回放 vs 同步重算 R-21 裁定后跟进票；④金样候选：A-f2a/f2b 589B（已在 l0203-wire）+ 本轮 B 侧同构体；⑤R-21 差分复跑时以 tools/difftest/l0203/maven-faces.sh 为入口（已可复跑）
