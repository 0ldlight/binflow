# T-276 — README 双语更新到 M9 完结态

- 角色: tech-writer ｜ 票据: T-276（用户指令「记得更新 README 文档」派生专项） ｜ 日期: 2026-08-25
- 输入: `README.md`（341 行）/ `README.zh-CN.md`（326 行）改前态；素材 = BOARD.md M7/M8/M9 完结节 + `docs/prd/milestone-9.md` + `docs/reverse/artifactory-full-feature-matrix.md` §0 总览 + `docs/user/admin/rbac-roles.md` / `api-reference.md`（E4~E6）/ `docs/user/integrations/npm.md` 既有验证注记。
- area: 仓库根两份 README。未动 BOARD.md、未 git 提交、未动 docs/user/、未动 Quick start / Security / Configuration / Operations / Development 中仍正确的既有内容（只做事实性小修）。

## 一、改动清单（两份同步、各自行文风格）

### 1. 头部自述（M6 段 → M6+M7~M9 段）
- 五包型句保留不动。原「M6 (current milestone) adds…」段改为：M6 一句带过（企业层）+ M7~M9 精华段（RBAC 三值角色 + `manage` 下放、docker 跨重启续传、Artifactory 对齐控制台 + 24 任务路径对照、服务端收口清单、双架构镜像）。

### 2. 链接表
- Milestones 行：「M1 kernel → M6 enterprise, M1–M5 done」→「M1 kernel → M9 hardening, all done」。
- PRD 行：milestone-6 → milestone-9（users/groups 端点 / usage 批量 / permissions 过滤 / GC 竞态）。
- 新增一行：Artifactory 全量功能对照矩阵（213 条目，M10+ 路线图骨干）。
- 其余行逐条核对链接有效（含新增的 rbac-roles / token-step-up / artifactory-path-map / npm 集成指南）。

### 3. 「Beyond curl」节
- 标题改为「M6–M9 能力与延伸阅读」；M6 原七条保留原样。
- 追加三个子板块：
  - **M7**：三值角色（`adminRole` 示例：建 readonly_admin 用户 201 + 回显）／`manage` 仓库级管理员（carol + app-local + t-app 三命令链）／docker 跨重启续传（kill -9 / SIGTERM / compose restart 三径对称）／step-up 铸 Token（默认 off）。
  - **M8**：控制台对齐 Artifactory（双模式壳 / 制品树深链 / Set Me Up 与 Deploy 对话框 / 键盘可达 / 自有皮肤零复制）+ artifactory-path-map.md（24 任务）。
  - **M9**：用户与组生命周期（DELETE 护栏示例 + includeUsers）／一次请求代替 N 次（usage 批量 `| jq length` + `filter=manage` 三命令）／并发安全 GC／npm CI 发布仅需 write（deprecate 仍需 write+delete）／双架构镜像一句。

### 4. 状态脚注
- 「Pre-GA under active development (M6). M1–M5 done（m1-done…m5-done）」→「All nine milestones done and tagged（m1-done…m9-done）」+ 下一程指针指向主矩阵（M9 时点 213 条：已有 20 / 部分 50 / 缺失 133 / 设计上不做 10）。BOARD.md / reports/ 指针保留。

### 5. 细节修正（过时事实）
- Quick start 数据目录：`sqlite + blobs/ + sessions/` → `sqlite + blobs/ + uploads/`，并注明上传会话本身是 sqlite 行（T-209 后期变更；`internal/storage/engine.go` 常量 blobsDirName/uploadsDirName/dataFileName 对证）。
- Configuration yaml 注释同口径修正（blobs + uploads + sqlite）。
- Operations：GC 段追加一句 M9 并发安全事实（`--apply` 删除前即时复核引用，`graceHours=0` 亦安全）。
- Development：internal 包清单补漏 `docs` 包（实际 15 包，原文漏列）。
- docs-site 链接核对：两份 README 无 docs-site 直链，`make docs` 描述仍准确（docs-site/ 存在、产物嵌入 /binflow/docs/），无需改动。

## 二、验证（全部实际执行）

**自起 scratch 实例端到端复放**（bin/binflow-server 今日构建，:18098/:18099 两个临时实例，跑毕即删）：

- M7 块：建 auditor（readonly_admin）→ 201；回显 `{"name":"auditor","adminRole":"readonly_admin","enabled":true}`；建 carol → 201；建 app-local → 200；建 t-app（carol 持 manage）→ 201。
- M9 块：DELETE auditor → 200（且其先铸的 Bearer Token 此后 401——前一轮验证）；DELETE admin → 400 文本体 `Cannot delete the built-in admin user.`；自删（第二 admin 删自己）→ 400；usage 批量 `| jq length` → **2**（generic-local + app-local，与注释一致）；carol `?filter=manage` → `["t-app"]`；carol 无 filter → 403。
- 组域：`groups/app-admins?includeUsers=true` → `userNames:["carol"]`；users 列表回显含 `enabled`/`adminRole`/`groups` 键族。

**18080 用户实例只读走查**（零写入）：已具备全部 M9 端点（usage 批量 / filter=manage / users 加宽回显 / auth methods 形态不变）——实例为 M9 构建确认。

**链接校验**：两份 README 全部相对链接逐一存在性检查通过（仅页内锚点 #configuration/#配置 为脚本误报，标题实际存在）。

**多架构事实**：`deploy/release/build-release.sh` 头注对证（预编译双 arch 二进制注入 + docker manifest create/annotate/push，CI 无 qemu 依赖）。

## 三、未做 / 边界

- ROADMAP.md 头部仍写「当前里程碑：M9（PRD v1.0 草案，待 conductor 审）」——ROADMAP 唯一写入者是 product-manager，本票不动（已在本日志登记，建议 PM 随 M10 规划刷新头部行；M9 条目本身已是完结态无需改）。
- README 未提及 git 历史瘦身（FR-83 内部仓库 chore，非用户可见能力；克隆体积等数字留给 BOARD/迭代报告）。
- 主矩阵覆盖率百分比（32.9%/40.5% 等多口径）未引入 README——只给原始计数，避免多口径歧义。

## 四、遗留「待确认」项

- 无。本次写入的每条行为句均有代码、QA 报告（T-221/T-222/T-246/T-253 等）或本日实测支撑。
