# T-351 工作日志 — M12 中期回归（L01~L35 首跑 + M11 承证影响核定 + DoD 中期核 + QA UI 自动化首跑）

- 角色：qa-engineer ｜ 状态：**done——中期总裁定 FAIL（裁决①真客户端面两缺陷 + FR-107 装配 panic + 三处未落地缺口；curl/引擎级面大面积绿，修复面已缩到点上）**
- 被测：HEAD `732ff7c`（M12 23/25 在档终态；T-347/T-352+353 已合入）
- 工具链：go 1.26 / dotnet 8.0.424（容器）+ **nuget.exe latest（mono 容器）**+ docker 29 dind（insecure-registry）+ helm 4.x + maven 3.9.9（archive 新装）+ Temurin/brew openjdk 21 + npm 10 + pip（twine wire）+ conan 2.31.2（venv 新装）+ jfrog-cli 2.122.0 + MinIO 容器 + mock NuGet 上游（自定义 @id 三面）
- 纪律遵守：共享工作树**零 git 写操作**（归属审计用只读 log/diff）；零产品代码改动（唯一新增=本票 UI spec `web/e2e/m12/t351-qa-midterm.spec.ts`）；license 用 `./binflow-license-private.pem` 真签名（kid=bf-lic-2026 首发钥，pro.lic 30 天）
- 事件登记：21:50 左右 18380/18381 双实例被外部击落（疑似并行票过宽 pkill——T-327 误杀同款）；数据目录在盘、重启零损，受影响面（$filter 对照、nuget.exe 重试）复跑全绿

## 0. 实例编排（scratch 双形态 + 专用腿）

| 实例 | 端口 | 形态 | 用途 |
|---|---|---|---|
| community 主 | 18380 | 无 license | 门控负臂/硬删回归/P0 矩阵 |
| pro 主 | 18381 | 真签名 pro.lic | NuGet/操作族/Trash/cargo/conan 全量 |
| CI 复刻 | 8080 | 裸起+三 seed（m8/m9/m10） | 全量 playwright（CI 同姿势 workers=2） |
| s3 链 | 18391/18392 | MinIO 底座（18392 装 pro） | MPU/token 窄域/[s3] 链 roundtrip |
| dual-write 腿 | 18390 | [filestore,s3]+dual-write | **boot panic（D-3）** |
| mock 上游 | 18999 | 自定义 @id 三面 NuGet 源 | L06~L09 动态解析/代理/合并/降级 |

## 1. L 矩阵（首跑，35 项）

| # | 项 | 判定 | 关键证据 |
|---|---|---|---|
| L01 | nuget.md 走查 | ✅ | 258 行；as-built+增量双段；v2 十八端点/OData 参数面/FeedUtils 阶梯逐条出处+置信度 |
| L02 | v2 sweep | ⚠️ | 17/18 端点绿：service doc/$metadata/Search±count/FindPackagesById±count/Packages()±单条±/Id/GetUpdates±count（同版 404 空臂+旧基 200 臂）/$batch **202+multipart 回三段+DataServiceVersion 2.0+Unsupported batch entry 逐字**/Download sha256 对账/裸 .nupkg；M10「v2 其余 404」反转=全路由可达。**红：$filter bare 形态 400（D-1）** |
| L03 | publish 重复臂 | ✅⚠ | 异字节+无 d 权限 → **409 `Package already exist: <deployPath>` 逐字**；d 权限覆盖 → 201+sha256 新值核对；同字节 → 201 幂等（规格未载豁免，D-10 上 BOARD） |
| L04 | 真客户端+门控 | ⚠️ | **dotnet 8 绿**（add package+restore 全链）；**nuget.exe 在场：list 400（D-1）/install XML 非法（D-2）**；门控三缝绿：community 建仓 400（license 文案）/pro 200/卸载后 v2 读 200+push 403+trash 403+ops 403→重装 201 |
| L05 | v3 回归 | ✅ | index.json 资源族/flatcontainer trio/registration/push 链 + dotnet restore 零回归 |
| L06 | 动态解析 | ✅ | mock 上游 @id 全自定义（`weird/flatcontainer-custom`/`odd/registrations-xyz`/`fancy/search-custom`）：versions/nupkg 字节/registration 三面全经解析路径取数+**URL 回写本源**；前缀常量无痕迹 |
| L07 | remote 代理 | ✅ | 上游发布 2.0.0（不预下载）→ BinFlow 立即可搜（latest=2.0.0 versions=[1.0.0,2.0.0]）；坏 URL 的 miss-cache（1800s）行为正确 |
| L08 | virtual 合并 | ✅ | 虚仓 search 两域并见（mock.pkg + t351.sample）；M10「virtual=local+已落地缓存」反转实证 |
| L09 | 降级 | ✅ | 停上游：search 200 空/缓存 nupkg 200/虚仓退化本地事实——零 5xx；重启后 fresh 仓立即回代理（3.0.0 可搜） |
| L10 | repo-operations.md 走查 | ✅ | 221 行；copy/move/archive!/目录 zip/explode wire+出处逐条；§1.6 flat 裁定与 Q4 终裁（pro 门）在案 |
| L11 | copy/move | ✅ | 树级 200+messages[]；源/目标 sha256 对账；属性随行（team/build）；copy 源留/move 源失；**2000 节点并发上传零错+copy 2.5s+抽样 5/5 sha 一致**（万节点全量归 T-356）；community 403+`X-Binflow-License-Required: repo-operations` |
| L12 | dryRun/flat | ✅⚠ | dry=1 报告形+**零副作用实证**（dry→404/真 copy→200 对照）；`/api/flat/*` 404=规格裁定（PRD「flat 扁平化」措辞待 PM 回写，非缺陷）；failFast 参数接受 |
| L13 | 索引联动 | ❌ | deb .deb copy → 目标仓 Packages **三连 404**（源仓对照 200）——适配器 reindex 导出缝缺，T-339 §6 自登「接线票未开」**孤儿缺口（D-4）** |
| L14 | 归档族 | ✅⚠ | archive!/：成员/嵌套/`.sha256` 裸 hex/缺员 404/非 GET 405 全绿；explode：201+`X-Binflow-Exploded-Files: 3`+原件不落库+.rar 400（M10 断言反转✓）；目录 zip：默认关 403「Download Folder functionality is disabled.」逐字——**开启旋钮未落（D-7），AC5 开启臂不可验**；community 全族 403+header |
| L15 | 删除入站 | ✅ | DELETE 204→原路径 404→can 可见→**五元组打标**（time/deletedBy/originalRepository/originalRepositoryType/originalPath）；community=硬删（can 404） |
| L16 | 恢复 | ✅❌ | generic：restore 200+sha256 一致+trash.* 剥净；**npm 协议仓：tgz 恢复 200 但 packument 不重算（versions 空-dist-tags 空）→真 npm install notarget（D-4 同根）** |
| L17 | 保留期/治理 | ✅⚠ | empty 200 摘要+非 admin 403「administrator privileges required」+匿名 401+community 403+`X-Binflow-License-Required: trashcan`；**GC 免疫实证**（apply 后条目幸存）；clean 200 `{removed:1}`；保留期 cron 单测绿（13 天不清/15 天清/锁门 skip）——**配置旋钮未落（硬编码 14d，D-8）** |
| L18 | fail-open 停机窗 | ❌ | **dual-write 实例 boot panic（D-3）**：`binflow_replay_*_total` 五 gauge 带 _total 后缀违反 internal/metrics T-197 不变量（M6 起）→装配 panic，确定性×2；引擎级真 MinIO 窗口单测 **PASS**（窗内 PUT 入队 depth=4→排空 drained=5 reconciled 零缺）——服务级验证被阻断 |
| L19 | 持久化/回归 | ❌ | 同 D-3（实例不可起）；[filestore]/[s3] 链 roundtrip 绿（18392 制品+MPU 全落 MinIO） |
| L20 | 资源门 | ✅ | `make footprint` **EXIT 0**（138.7MB→10MB 级，红→绿）；check-size 94.13MB 维持；**冷启动三连 0.06/0.06/0.22s（P95=0.22s<2s）**；懒加载触及路径全绿（全套 UI/API 测试） |
| L21 | HelmOCI 全链 | ➖ | **未落地**：internal/adapter 无 helmoci 实现、零票号提交——T-342 未派（D-6） |
| L22 | HelmOCI 边界 | ➖ | 同上 |
| L23 | conan | ✅❌ | D-F **绿**：`_/_` 坐标混批 [live,stale] v1 packages/delete → **200 空 body+live 树删**（未知 ref 404/非法 pid 400 维持）；conan 2.31.2 真客户端 login/create/upload/list 绿；1.x 回归=承证（T-340 真 1.66 e2e）+curl 级；**forceConanAuthentication 未实现（字段静默丢弃/匿名 ping 200）——T-340 AC2 未承载（D-5）** |
| L24 | cargo | ❌ | 死上游 search → **404+errors 信封，规格要 409**（R-3/R-4 未实现，D-5）；`.cargo/` 写 405/删 204（缓存逐出）收敛面在场 |
| L25 | MUI 四闸门 | ✅ | 锚册 ledger PASS（broken=0）；assert-tokens OK（css 11+tsx 43 零硬编码）；**全量 playwright 210 passed/0 failed/21 skipped（7.7m，CI 姿态 workers=2，m8+m9+m10 三项目）**；gzip 累计 **+20.01%**（341,023B vs T-299 基线 284,185B）≤25% |
| L26 | 收敛 | ✅ | FE 四波+T-352/353 零 internal/ 触碰（归属审计）；旧组件栈残留=0（ledger+T-344E 清扫） |
| L27 | 回写批 | ✅ | architecture §15.4/§23 M12 段在档（操作域/MPU as-built/remote canonical 终态）；cargo.md §8 as-built；conan.md D-F 幂等行（2026-08-29 回写）；D-G/D-H grep 零残留；`make docs` SUCCESS（4.20MB） |
| L28 | socketTimeoutMillis | ✅ | PUT 新拼写 → 回显 `socketTimeoutMillis:2500`；PUT 旧拼写 `socketTimeoutMs` → 接受+回显新拼写 3500（别名链）——canonical 翻转不破 M11 面 |
| L29 | byHash/表单 | ✅ | `byHash:"bogus"` → **400 枚举**（`must be one of ALL, SHA256, NONE` 逐字）；表单腿=repo-policy-keys.spec 7 测试在全量内绿 |
| L30 | token 窄域 | ✅ | MPU 全链：create(token)→urlPart→3 part 200（S3 5MB 分片下限实测）→complete 202→FINISHED/100→checksum-deploy token→**原路径 201/他路径 403 逐字**（`scoped to the checksum deploy of one upload landing…`）→12MB 字节 sha256 对账；jfrog-cli 真机=结构性不可用（jf 硬编码 /artifactory 前缀，E-26 有意不模拟——登记非回归） |
| L31 | auth/拒启 | ✅⚠ | audit 词表 **54 词**（web 镜像计数+cleanup.run/trash.*/artifact.copy 在册；Go 注册表含 StorageReplayWindow/Drain）；userDnPattern 字段接受+回显（行为腿承证 T-346）；env 拒启**点名缺键**（bucket/access_key_id/secret_access_key）——「先于 binstore 报错」次序臂不符（binstore 解析错先出，D-9 低危） |
| L32 | CI runner | ✅ | `.github/workflows/ci.yml` e2e 专用 job 在盘（console+docs 嵌入+真二进制+三 seed+三项目 workers=2）；本机同姿势 210/0——负载 flake 家族零复发 |
| L33 | 回归+归属 | ✅ | 代码提交 22 条 100% 有票号/BOARD 登记；REST 新面四族（copy/move、archive/download、trash 三动词、explode）全在 api-reference M12 速览；M11 承证面：视觉四波→锚册零变化+闸门复验绿；三处断言反转（v2 404→全集/virtual→代理/X-Explode→接受）+footprint 红→绿全部实测核实 |
| L34 | NFR/负向 | ✅ | **100 并发 NuGet GET 1.5s 全 200 零 5xx**；bz2 404（LC-45 维持）/gz 200；条件腿留痕：symbol server Q2 未触发、conan 活体互证 Q5 不可得、chartsBaseUrl 因 T-342 未落未验 |
| L35 | 文档 | ✅ | api-reference M12 增补速览全部命令本轮逐条实跑（行为/状态码/门控头全对得上）；T-347A 五类增量在档 |

**小计：✅24 / ⚠️7 / ❌5 / ➖2（T-342 未落）。**

## 2. M11 承证面影响核定（m11-done..HEAD）

- **视觉四波（T-344A~E）→ M11 L12~L14（MUI 批一/批二锚册面）**：console-ux 锚册**零改动**（ledger broken=0）；四闸门复验全绿（全量 210/0+tokens+gzip）；SPA 累计 +20.01%（批内 +16.47% + T-352/353 +3.5%）仍在 ≤25% 帽内。**结论：纯视觉迁移，无行为面变化。**
- **canonical 翻转（T-346）→ M11 L02/L25 面（socketTimeout* 回显族）**：新拼写 canonical 回显+旧拼写输入别名——T-327 期实测的 PUT/GET 往返语义保持，仅回显拼写翻转（PRD 豁免票归属，PRD §5.4 已回写）。
- **承证 P0 抽样**：docker（dind push/pull 同 digest 链）/maven（deploy+resolve+exec）/npm（publish+install）/pip（twine+pip install）/helm（repo add/search/pull sha 同）+ generic curl roundtrip 双形态全绿；M11 删除族（GC/grace/quota/cleanup）经 L17 GC 免疫与 cleanup.run 词表回归零异常。

## 3. DoD 中期核（PRD §9 八条）

1. P0 AC：FR-104 ✅/FR-105 copy-move ✅/FR-108 ✅/**FR-103 ⚠️**（curl 面 17/18+重复臂绿；真 nuget.exe 面两缺陷）——**未全绿**
2. 量化门槛：v2 覆盖（curl✓/client✗）/search 时效✓/动态解析✓/操作族⚠（索引联动缺）/回收站 ✓（协议索引例外）/fail-open ✗/资源门✓/HelmOCI ✗未落/包型收尾半（D-F✓ force✗ cargo✗）/MUI✓/回写✓/遗留小票大部✓
3. 回归硬门槛：M1~M11 P0 双形态抽样全绿；契约归属 100%；三处断言反转+footprint 红→绿实测核实 ✅
4. 前置产物：nuget.md/repo-operations.md/ADR-0040 齐；Q3（trashcan=pro，17 槽实测）/Q4（repo-operations=pro，三缝实测）终裁归位 ✅
5. tech-writer 五类：T-347A 交付，命令实测可复跑（本轮逐条验证）✅
6. NFR：P53✓/P54⚠（2000 节点抽样，万节点归终验）/P55✓/P56✓/P57✗
7. Non-goals 对账：收口时建段（进行中）
8. m12-done：未到（本票即收官链第一环拦截点）

## 4. QA UI 自动化首跑（本票新能力兑现）

- **新增 spec ×1（3 用例）**：`web/e2e/m12/t351-qa-midterm.spec.ts` —— 真栈解锁态 trash 浏览→下钻→五元组详情→恢复 roundtrip（REST 回读权威断言）/审计 picker 54 词+M12 新词可见/community 锁定态零浏览面。**license tier 运行时自门控**（同一 spec 双形态各跑各腿）；PRD 约定的 `web/e2e/m12/` 目录首用。
- 调试路径：error-context DOM 快照定位全路径 testid 形态/原生 select option 定位/瞬态 toast 改权威断言——7 轮迭代收敛；tsc+eslint 零告警。
- 双形态结果：pro **2 passed**（restore roundtrip+picker）+community **1 passed**（locked）——T-352 全 mock 面（真栈 community 不可达）补上的真栈腿。

## 5. 真实客户端矩阵

| 客户端 | community | pro |
|---|---|---|
| curl generic | ✅（L12~L17 全臂） | ✅（全 M12 面） |
| docker 29（dind） | — | ✅ login/push/pull 同层 |
| maven 3.9.9+jdk21 | ✅ deploy→resolve+exec `hello from binflow t351 maven` | —（同链 M11 期已证） |
| npm | — | ✅ publish+install（含 trash 往返一臂 notarget=D-4 证据） |
| pip/twine | ✅ upload+`--index-url` install+import | — |
| dotnet 8 | — | ✅ add package+restore（v3） |
| nuget.exe（mono） | — | ❌ list 400（D-1）/install XML（D-2） |
| conan 2.31.2 | — | ✅ login/create/upload/list+v1 delete wire |
| helm | — | ✅ 上传/repo/search/pull sha 同 |
| apt（deb 容器） | — | ✅ update+install+二进制跑通 |
| jfrog-cli | — | 结构性不可用（E-26 /artifactory 前缀）——非回归 |

## 6. 缺陷登记（本轮新登记）

| # | 级 | 摘要 | 证据/修复建议 |
|---|---|---|---|
| D-1 | **P0** | NuGet v2 `$filter` bare 形态（`IsLatestVersion` 无 `eq true`）→ 400「unsupported $filter」——**nuget.exe list 全破**；nuget.md §4-89 自身用 bare 措辞、实现 validV2Filter 只认 eq 形态 | `Search()?%24filter=IsLatestVersion&semVerLevel=2.0.0`→400；eq true→200。修：validV2Filter 接受 bare flag（视为 eq true） |
| D-2 | **P0** | v2 standalone entry（`Packages(Id=,Version=)`）XML **缺 xmlns:d/m 声明**——nuget.exe install 全破（'m' is an undeclared prefix, line 10）；feed 文档有声明、entry 文档没有 | curl 原文可复现。修：entry 根补两声明 |
| D-3 | **P0**（FR-107 P1 特性整体不可用） | dual-write 实例**装配 panic**：`binflow_replay_{drained,failed_retry,failed_permanent,source_gone,read_fallback}_total` 五 gauge 带 `_total` 后缀，违反 internal/metrics T-197/D5 不变量（M6 起，「_total 保留给 counter」） | 双次确定性复现；[s3] 单链对照正常；引擎级 live MinIO 单测 PASS（排空对账零缺）。修：去后缀（cleanup 族先例）或改 Counter。附：指标名与 PRD §6.4 拼写（binflow_storage_replay_*）不一致 |
| D-4 | **P1** | **协议仓索引联动未接线（孤儿缺口）**：copy/move/restore 后 deb Packages/npm packument/conan 链不重算——T-339 §6 与 T-345 §5.5 均自登「建议小票」但**无票承载**；PRD FR-105-AC3/FR-106-AC2 协议臂未兑 | deb 目标仓 Packages 三连 404；npm restore 后真客户端 notarget。建议开票：适配器导出 reindex 缝+SystemPrincipal 运行（T-345 拟法） |
| D-5 | **P1** | T-340 AC2/AC3 未承载：forceConanAuthentication 与 cargo R-3/R-4（409）全仓零实现，票已关但迭代报告称「all four ruled items landed」 | 字段 PUT 静默丢/匿名 ping 200；cargo 死上游 404。建议补票或记账勘误 |
| D-6 | **P1** | T-342（HelmOCI）未派未落——BOARD 计 23/25 与 L21/L22 无实现矛盾；FR-109 整面缺 | internal/adapter 无 helmoci、零票号提交。需 conductor 排票或滚 M13 裁定留痕（DoD-1/2 的 HelmOCI 行将红） |
| D-7 | P2 | folderDownload 开启旋钮未落——FR-105-AC5 开启臂不可验（api-reference 自注「配置旋钮未落」） | 默认关 403 逐字 ✓ |
| D-8 | P2 | trashcan enabled/retention_days 配置键未落（装配硬编码 14d；T-345 §5 登记） | 保留期 cron 单测绿（13/15 天） |
| D-9 | P2 | env 拒启「先于 binstore 报错」次序臂不符：binstore 解析错先出 | 拒启本体绿（点名缺键） |
| D-10 | P2 | v2 同字节重传幂等 201 vs nuget.md §5.1 无该豁免——规格-实现分歧 | 上 BOARD 裁定（操作上良性） |
| D-11 | P2 | copy dry-run 消息路径多拼一段（`to …/app/app`） | 文案级 |
| 观察 | — | jf CLI 硬编码 /artifactory 前缀（E-26 有意不模拟）→ jfrog-cli 真机腿结构性不可用 | 非 M12 回归；BOARD 留痕 |

## 7. 总裁定

# **FAIL（中期拦截成立）**

- **必须修复（P0，裁决主线面）**：D-1/D-2（裁决① NuGet v2 真客户端面——两处都是小修：一个 filter 形态、一对 xmlns 声明）、D-3（裁决③ fail-open——五处指标名改名即可；引擎本体已被 live MinIO 测试证明绿）。
- **必须记账/开票（P1）**：D-4（索引联动孤儿票）、D-5（T-340 尾巴）、D-6（T-342 下落）。
- 大面是绿的：NuGet v2 curl 面 17/18+代理/合并/降级全链、操作族 copy/move/dryRun/归档族、Trash 全语义、资源门红→绿、MUI 四闸门+全量 210/0、归属 100%、M11 承证零损。
- 修复面已缩到点上；建议 D-1/D-2/D-3 一张修复票（dev-go-core+dev-go-storage 各半）+D-4/D-5 各一小票，随后终验 T-356 全量复验。

## 8. 清理

- 实例 18380/18381/8080/18391/18392/18390 全停清目录；t351-dind/t351-minio 容器 rm；mock 上游停。
- 测试 license/密钥文档仅在 /tmp/t351/lic（pem 未离仓位）；conan/maven/JDK/dotnet 客户端栈在 /tmp/t351/tools，会话后清。
- 共享工作树唯一新增：`web/e2e/m12/t351-qa-midterm.spec.ts`（+本报告）；零 git 操作。
