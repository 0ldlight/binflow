# T-97 工作日志 — groups 域与权限继承（FR-27，SE-01~08）

- ticket: T-97 [P0] groups 域+权限继承（M4 批 4，**双 reviewer 票：安全 + 架构**）
- 状态: done（编码+自测完成；正确性+安全 review REQUEST_CHANGES 的 2 blocking 已修复复验，待复核）
- 日期: 2026-08-20（初版）/ 2026-08-21（review 修复段）
- area: `internal/auth(groups)` + `internal/metadata(groups)` + `internal/httpapi(groups)`
- 输入: reports/agents/T-88.md T-97 节 AC、docs/prd/milestone-4.md v1.1（FR-27、SE-01~08、W17~W21/W40、§5.5 K1/K3 暂行）、docs/design/architecture.md §6（004 groups/user_groups DDL）/§7.1（groups 路由行）、docs/reverse/auth-model.md（§1.2/1.3/1.4 用户 CRUD、§4 权限概览）、docs/reverse/rest-api.md §3（`?permissions`）、internal/audit/api.go（T-93 词表核实：group.*/permission.* 已有，**无 member 动作**）

## 做了什么

### 1) internal/auth — Principal.Groups 填充 + 并集判定 + 有效权限视图

- `Principal` 增 `Groups []string`（api.go，注释锚 SE-07/NFR-S25）。
- 新文件 `groups.go`：
  - `groupSource`（消费侧接口，`GroupsOfUser(ctx, user) ([]string, error)`）+ `WithGroups` 注入（nil = 惰性，旧测试零影响）；`NewFromStore` 无条件接线（004 表恒在）。
  - `fillGroups`：**认证失败面 fail-closed 于组侧**——membership 查询失败时 principal 带空组继续（直接授权不受牵连、组授权绝不因此放开），Error 级日志。理由：把富集读失败升级为整凭据拒绝会让一次 join 故障炸掉与组无关的全部账号，爆炸半径选错方向。
  - `rowCoversPrincipal`：user 行（大小写不敏感，M1 语义不变）∪ group 行（`slices.Contains(p.Groups, ...)`）。
  - `ItemPrincipals(ctx, repo, path)`：SE-08 视图计算，与 Can **共用** `targetCovers`（从 Can 抽出的 repo∈repos + include 命中（空=全含）+ exclude 优先谓词）——视图与判定不可能分叉；畸形 target 跳过+日志（Can 同姿势）。
- `Authenticate` 拆臂分发 + 出口统一 fill（**逐请求现读 user_groups = 即时生效，无缓存**，NFR-S25）；`Service.Verify`（token 直连缝，docker token 交换用）同样 fill。
- `Can` 重构为 rowCoversPrincipal + targetCovers，行为等价（既有 authorizer_test 全绿）。
- deps.go：`groupStoreAdapter`（`[]*Group`→names）。

### 2) internal/metadata — 两个窄缝

- `PermissionStore.GroupReferences(ctx, group) ([]string, error)`：SE-04 删除保护的查询（JOIN permission_principals，`principal_type='group'` 且名相等，按 target 名排序；user 同名行不算）。
- `UserStore.UpdateProfile(ctx, user, email string, isAdmin bool)`：PUT replace / POST partial 的 email+admin 单语句更新（口令哈希走既有 UpdatePassword 专用缝）。

### 3) internal/httpapi — groups 端点族 + users 扩展 + permissions.groups + ?permissions

- 新文件 `groups.go`（SE-01~04，错误体=用户管理纯文本层；admin 门在路由层，401/403 走面级 envelope——与 users/token 族一致）：
  - `GET /api/security/groups` → `[{name,uri,description}]`；单查同形 + 404 `Group not found`。
  - `PUT /{name}` body `{name,description}`：**创建 201 无 body / 已存在 200 更新**（K1 写死值；注意与 users 的「两态皆 201」不同——PRD SE-02 原文如此）；body name≠路径 → **400**（AC 口径；users 是 409，差异为 AC 明文）。
  - `POST /{name}` → 200 部分更新（仅 description），404 不存在。
  - `DELETE /{name}` → 200 纯文本 `The group: '<name>' has been removed successfully.`；**被 target 引用 → 409 且 message 列 target 名**（K3 从严）；成员联动解除靠 FK 级联（W20 末步 `GET users/jane` groups==[] 实测）。
  - 组名校验：非空 / ≤64 / `^[a-z][a-z0-9._-]*$` / 保留字 `anonymous`、`_system_` → 400（文案 K1 暂行，集中在 `validateGroupName`）。
  - 成员维护缝：`validateGroupNames`（预检名单定 `Unable to find group by name '<g>'.` 精确文案——auth-model §1.3-⑧ 高置信度）+ `setUserGroups`（SetUserGroups + `group.member` 审计）。
- security.go users 扩展（SE-05/06）：
  - **PUT /users/{name} 语义翻转（R5 同批更新）**：M1 的 409 "The user already exists" 退役 → create-or-replace，**两态皆 201 无 body**（auth-model §1.3-⑪ 高置信度原文「已存在 → 更新…返回 201 无 body」；W19b 的 `PUT jane groups:[] → 201` 依赖此语义）。replace 覆盖 email/password/admin/groups，enabled 不在 wire 体内、保持原值；集合路由 `POST /api/security/users` 维持 create-only 409。
  - 新路由 `POST /api/security/users/{name}` = 部分更新（指针字段区分缺省/显式空——`groups:[]` 清成员 vs 缺 groups 不动）；404 未知用户；409 name mismatch；provided-but-blank email/password 400（同校验链）。
  - `GET /users/{name}`：email 恒渲染（去 omitempty）+ groups 实数据回显；永无口令字段；列表简形态 `{name,uri,realm}` 不变（W40 双端点分工）。
- permissions.go（W19 前置 + NFR-S25 授权审计）：
  - `principals.groups` 接受（未知组 400 同族文案）与回显（按 principal_type 分流两栏，空渲染 `{}`）。
  - 补 `permission.create/update/delete` 审计事件（词表既有常量；NFR-S25「授权变更留审计」，POST 按 target 新旧分 create/update，DELETE 落 delete）。
- storage.go + router.go（SE-08）：
  - `GET /api/storage/{repo}/{path}?permissions`：`{uri, principals:{users:{r/w/d:[名]},groups:{...}}}`（**r/w/d 位 → 主体名集合**——docs/reverse/rest-api.md §3 括注原文「key 为 r/w/d/a 权限位，value 为主体名集合」，高置信度，按规格字面实现；见遗留③）。门序：未知仓 404 envelope → 非 local **400**（spec 同款；先判型后取 node，remote 查询**不触发上游拉取**）→ 内容面读门（reposvc Get/List，匿名策略与 403/404 文案与 item GET 完全同源；根目录走 serveRootFolder 同款 List 门）。计算走 `auth.Service.ItemPrincipals`（server.go 按 `permissionViewer` 断言发现，503 兜底假栈）。
  - E-26 拒绝列表移除 `permissions`（compat_test 同批注明翻转来源）。
- audit/api.go：定义 `ActionGroupMember = "group.member"`（T-93 词表核实后无 member 动作，按派单指示本票定义并注明；入 Actions() 供查询面/选择器）。

### 4) 并行共存

- T-94 在我工作期间落了 `Deps.GC`/`v1/system/gc`/`system_gc.go`（磁盘共现，router.go 不同 case 分支，零冲突）；T-98/T-111 的 web/、adapter/docker 改动未触碰。

## 改动文件

新增：`internal/auth/groups.go`、`internal/httpapi/groups.go`、`internal/auth/groups_test.go`、`internal/metadata/t97_group_refs_test.go`、`internal/httpapi/t97_groups_test.go`
修改：`internal/auth/{api,authenticator,authorizer,token,deps}.go`、`internal/metadata/{api,substores_auth}.go`、`internal/audit/api.go`、`internal/httpapi/{security,permissions,storage,router,server}.go`
测试同批更新（翻转注明来源）：`internal/httpapi/{compat_test,routes_compat_test,security_test}.go`、`internal/auth/authorizer_test.go`（M1「groups ignored」注释与断言更新为「group 行仅经 Principal.Groups 生效」）

## 自测（实际命令与关键输出）

- `go build ./...` ✓；`go vet ./internal/...` ✓；`gofmt -l .`（除 web/）零输出；`golangci-lint run`（全仓）**0 issues**。
- `go test ./internal/auth/...` → ok 57.7s（含新 TestGroupsFilledAtAuthentication / TestCanGroupUnion / TestGroupMembershipImmediateEffect / TestGroupsFillFailsClosed / TestItemPrincipals）。
- `go test ./internal/metadata/...` → ok 17.2s（新 TestGroupReferences / TestUpdateProfile）。
- `go test ./internal/httpapi/...` → ok 76.2s（新 TestGroupsCRUD / TestGroupMembershipViaUsers / TestGroupAuthorizationInheritance / TestGroupDeleteProtection / TestStoragePermissionsView / TestGroupHasNoAdminBit）。
- `go test -race -run 'TestGroup|TestStoragePermissionsView|TestUsersRoutes|TestItemPrincipals|TestCanGroupUnion|TestAuthenticate$|TestCompatPlaneE26|TestE26FullMatrix' ./internal/auth/... ./internal/httpapi/... ./internal/metadata/...` → 全 ok（6.7s/9.7s/5.5s）。
- `go test ./internal/{audit,repo,config,console,storage}/... ./cmd/...` → 全 ok；`go test ./internal/adapter/...` → 全 ok（首轮全量并行时 `TestIdleSessionEviction`（adapter/docker，存储会话清扫，与本票无接触面）一次 flake——同测试单独复跑通过、adapter 全量复跑全绿，与日志中并发 "database is locked" 的既有 busy-contention 特征一致）。
- 真机 curl（`make build` → 19.40MB 二进制，127.0.0.1:18097 独立 data dir）W17~W21/W40 全序列：
  - W17：PUT devs 201 / 列表含 devs / POST partial 200 / GET description=updated / PUT existing 200 / anonymous、_system_、Devs、1devs 全 400。
  - W18/W40：PUT jane（email+groups）201 → `{"groups":["devs"],"email":"jane@example.com","admin":false}`；`groups:["nope"]` → 400 `Unable to find group by name 'nope'.`。
  - W19：devs-rw（groups{devs:[read,write]}，devs/**）→ jane PUT 201 / DELETE 403 / other/** 403。
  - W19b：PUT jane groups:[] 201 → 同 PUT devs/w2.bin **403（下一请求即生效）**。
  - W19c：user 直接 read + 组 write → GET 200 / PUT 201；cleaners 组 delete → DELETE 204。
  - W20：被引用 DELETE → 409 `Cannot delete group 'devs': it is referenced by permission target(s): devs-rw. …`；删 target 后 DELETE 200 `The group: 'devs' has been removed successfully.`；jane groups → `[]`。
  - W21：`?permissions` → `{"users":{"r":["jane"]},"groups":{"r":["devs"],"w":["devs"]}}`；未知 item/未知 repo 404；virtual 仓 400。
  - AC9：bob 入 `admins` 组，认证 200 但管理 API 403（组无 admin 位）。
  - 门矩阵：匿名 401 / 非管理 403；session cookie 臂在 groups 面 200（三臂等价）；cookie + 跨站 Origin 的 permission 写 → 403（CSRF 面）。
  - 审计：group.create×3 / group.update×2 / group.delete×1 / group.member×5 / permission.create×3 / permission.delete×1（`/api/v1/audit?action=` 可查）。

## 决策登记（K1/K3 暂行实现口径 + 本票裁决）

1. **groups PUT 已存在 → 200（非 201）**、body name 不符 → **400（非 users 的 409）**、DELETE 文案 `The group: '<name>' has been removed successfully.`、409 文案与 `Group not found`、组名校验文案——全部 K1 暂行，集中在 groups.go 单文件，逆向扩编回写后为窄改。
2. **users PUT 已存在 → 201（create-or-replace）**：auth-model §1.3-⑪ 高置信度 + W19b 依赖，M1 的 409 为简化实现，本票按 R5 流程翻转并同批更新 security_test（注释注明来源）；集合 POST 保持 409。
3. **membership fill 失败 → 组侧 fail-closed**（直接授权存活）：见上文理由，reviewer 请重点看一眼该取舍（auth/groups.go fillGroups）。
4. `group.member` 动作词本票定义（T-93 词表无）；`permission.*` 事件顺手补齐（NFR-S25 授权变更留审计，常量已备）。

## 遗留 / 待上游确认

1. **`?permissions` 映射方向**：按 docs/reverse/rest-api.md §3 括注「key 为 r/w/d/a 权限位，value 为主体名集合」实现（位 → 名单）。若逆向扩编或真机对照 Artifactory 发现实为「主体名 → 位列表」，翻转点在 `bitsToNames`（storage.go）单函数 + 测试断言。请安全 reviewer 裁定。
2. `?permissions` 对匿名读开启实例的匿名调用：按 spec 字面（读门过即出视图）实现；若需收紧为认证专属（视图含主体名单，属安全配置数据），改 router.go 该分支 `routeAuth{required: true}` 一行——未擅自收紧，待 review 意见。
3. W21 仅断言「users 含 jane / groups 含 devs」，对映射方向不敏感；T-103 QA 跑 W 序列时以本票实现为准。
4. permission target CRUD 在 M1 无 DELETE 用户/组的联动清理（组删除有 409 保护，故不存在悬挂）；user 删除端点 M4 未做（不在 AC，FR-28 UI 的「删」需后续票补 `DELETE /api/security/users/{name}` + 审计）。
5. `group.member` 事件在「请求显式携带 groups 字段」时记录（含值未变的重放）；如需「仅变更时记录」需前置读一次成员集，未做（噪声 vs 额外查询的取舍，登记待 review）。

---

## 修复段（2026-08-21，正确性+安全 review REQUEST_CHANGES 的 2 blocking）

review 全文：reports/agents/T-97-review-correctness.md（核心权限链全过：并集/fail-closed/三臂等价/CRUD 校验/201 翻转白名单/审计；fillGroups 取舍明确认可）。两条 blocking 均是对本日志遗留①②的裁定，reviewer 已做 reverse-src 取证。

### B1（遗留①裁定：翻转）— `?permissions` 映射方向

- 裁定：参考实现（RestAddonImpl.java:2240-2286）构造 **key=主体名、value=权限字母集合**，空集合跳过，官方公开 REST 文档同形。rest-api.md §3 括注为误读（conductor 另立勘误票，不占本 area）。
- 修复（internal/httpapi/storage.go）：`bitsToNames` 删除，新 `principalLetters`——主体名 → r/w/d 字母序列（固定 r,w,d 顺序）；**零权限主体不出现**（空集合跳过）；`permissionsView` 文档注释改为新形态并注明 review B1 依据。
- 测试：t97_groups_test.go W21 断言更新为 `users:{"jane":["r"]}`、`groups:{"devs":["r","w"]}`、两栏各恰一个主体。
- 真机复验：`curl .../devs/w.bin?permissions` → `{"uri":"...","principals":{"users":{"jane":["r"]},"groups":{"devs":["r","w"]}}}`。

### B2（遗留②裁定：收紧）— `?permissions` 匿名/非管理可达

- 裁定：真机第一道门是 canManage（管理面）；匿名读实例上空门 routeAuth{} 使任意访客可枚举全部用户名/组名及 r/w/d 分布，洞穿登录面的存在性不泄露。BinFlow 无 manage 位，admin 为最近映射。
- 修复（internal/httpapi/router.go 该分支）：`routeAuth{}` → `routeAuth{required: true, admin: true}`（一行）；handler 与路由注释补 B2 理由（视图=安全配置数据，管理面语义）。
- 测试：t97_groups_test.go 原「item 读门」腿改写为门矩阵——**匿名 401 / 非管理（jane）403 / admin 200** 三腿断言。
- 真机复验：anonymous 401 / jane 403 / admin 200（新实例 /tmp/t97-qa2，anonymous_access=true）。

### 修复段自测

- `go build ./...` ✓；`go vet ./internal/httpapi/...` ✓；`gofmt -l internal/httpapi/` 零输出；`golangci-lint run ./internal/httpapi/...` **0 issues**。
- `go test -count=1 ./internal/httpapi/ ./internal/auth/` → **两包全 ok**（48.4s / 7.5s）。
- 真机 W21 复验（make build 19.40MB，fresh data dir）：新形态断言 + 匿名 401 / 非管理 403 / admin 200 全过（输出见上）。

### non-blocking 6 条（维持登记，本票不做）

user_groups 前缀索引热路径 / 组删除 TOCTOU NOT EXISTS 守卫 / 审计 detail / PUT 事务性 / 非 local 404-vs-400 /（第六条见 review 全文）——留台账，由 conductor 录 BOARD P2 区。
