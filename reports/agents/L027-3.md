# L027-3 工作日志 — dev-registry-adapter（领域实例：dev-package-deb）

```
Ticket:       L027-3（LOOP 027：deb 包型条件键实现票——deb rclass 默认键族补齐至 A 62 键 + optionalIndexCompressionFormats 包型条件键落键表 + debian.md §40 勘误）
Role:         dev-registry-adapter（领域实例：dev-package-deb）
Area:         internal/adapter/deb + internal/httpapi/repo_config_render.go（六面键表渲染器本体——票面所称「internal/repo 配置渲染」实为此文件，L026-3 的 appendUnmodeledBlobKeys 同址；未触碰 httpapi release 面）+ docs/reverse/debian.md
Input:        reports/agents/L026-5.md（候选补键清单：v1×1 + v2×3 + 值级×1；optionalIndexCompressionFormats 定谳 默认 []）；
              reports/compatibility/l026r-wire/a-l026r-optdeb-v1-deb.json（62 键）/a-l026r-optdeb-v2-deb.json（21 键）/a-l026r-optgen-*（61/18 键）；
              docs/reverse/debian.md // 2.1（L40 勘误候选）+ §11 S4 行；internal/httpapi/repo_config_render.go（L025-6 键表 + L026-3 blob 回显）；
              internal/adapter/deb/reindex.go（RepoConfig 注释默认 ["bz2"] 旧口径）；A 活体 = http://172.16.58.130:8082（pro 7.161.15，admin，只读 curl + 本票造数 l027x-* 用后删净）
Changes:      ① A 活体逐键对拍（本轮新证，L026-5 wire 复验）：建 l027x-deb(debian)/l027x-gen(generic) 对拍五面——v1 deb=62/gen=61（deb-only=optionalIndexCompressionFormats=[]，空 JSON 数组非 null；
              值级差 enableDebianSupport deb=true）；v2 deb=21/gen=18、configurations deb=21/gen=18（deb-only=ddebSupported=false+debianTrivialLayout=false+optionalIndexCompressionFormats=[]，
              两面键集全同仅方言键 rclass/type 异）；设值臂 POST ["bz2"]+enableDebianSupport:false → 回显 ['bz2'] 键数不变、enableDebianSupport 仍 true（A 忽略显式置 false——写面行为，登记不实现）。
              ② 键表落座：repo_config_render.go 新增 debV1LocalSeats（optionalIndexCompressionFormats 默认 []any{}）与 debV2LocalSeats（ddebSupported/debianTrivialLayout/optionalIndexCompression
              Formats），configFaceSeats 加 packageType 参（debian×local 才附加；remote/virtual debian 未实测不猜、维持共享面）；renderConfigSeats 默认臂改走 seatDefault（debian local 行
              enableDebianSupport 缺省 true，blob 实存值仍优先）；四处调用点（repo_config_read.go ×3 + repositories.go handleRepoGet）同步传 row.PackageType；blob 回显注释「待 A 探定谳」段改已定谳落座。
              ③ deb 引擎注释勘误：reindex.go 头注与 RepoConfig.OptionalIndexCompressionFormats/normalized() 注释默认值改口径 []（L026-5 定谳），引擎行为零改（本就无默认渲染、bz2 无 writer 的登记分歧不变）；
              补「A 引擎对默认配置仓是否发 Packages.bz2 未实测」注记。④ debian.md // 2.1 勘误落账（行为规格句式）：L34 图示注 + L40 条目重写（Runtime>反编译 定谳、读面默认 []、设值可存可回显、
              引擎发文件臂未定待 pro 档差分腿）+ §11 S4 行改「读面 高 / 引擎臂 未定」。
Files:        修改：internal/httpapi/repo_config_render.go（+deb 条件座两表 +seatDefault +configFaceSeats 三参 +头注/回显注释更新）、internal/httpapi/repo_config_read.go（3 调用点）、
              internal/httpapi/repositories.go（1 调用点）、internal/adapter/deb/reindex.go（3 处注释勘误，无行为改）、docs/reverse/debian.md（// 2.1 图示注+L40 勘误+§11 S4）。
              新增：internal/adapter/deb/config_face_keys_test.go（TestDebConfigFaceConditionalKeys + TestDebConfigFaceSetValuesWin）。删除：无。
Tests:        internal/adapter/deb/config_face_keys_test.go（realstack 体例，行直播 md.Repos().Create 绕过写面门，读面全走真实 httpapi 路由）：
              1) TestDebConfigFaceConditionalKeys——四面 table（v1 62/61、v2 21/18、configurations 21/18、batch=v1 投影 62/61）：键数断言 + deb−generic 键差精确集 + 方言键；
              条件键形态子测：oICF=空数组（非 null）、enableDebianSupport deb=true/gen=false、v1 共享 ddeb 对=false、v2 无 enableDebianSupport、generic 面无 oICF。
              2) TestDebConfigFaceSetValuesWin——设值臂：oICF=[bz2,xz] 字面回显 v1 键数仍 62；v2=22（21 座 + enableDebianSupport 走 L026-3 blob 回显——v1 族专座在窄面为未建模键，注释钉明）；
              blob 实存值全量胜出（enableDebianSupport=false / ddeb=true / trivial=true）。
              既有面回归：httpapi TestRepoConfigKeyFacesAdmin/UnmodeledKeyEcho（generic 61/18/46/12 零动）全绿——deb 条件座只在 debian 行附加，共享面不染。
Commands:     · go build ./...（exit 0）
              · go vet ./internal/adapter/deb/ ./internal/httpapi/ ./internal/repo/（无输出）
              · gofmt -l internal/adapter internal/httpapi internal/repo（空）
              · golangci-lint run internal/adapter/deb/... internal/httpapi/... internal/repo/... → 0 issues.
              · go test ./internal/adapter/deb/ ./internal/repo/ -count=1 → ok deb  ok repo
              · go test ./internal/httpapi/ -count=1 → ok（渲染器所在包全量）
              · A 对拍（只读 curl + 造数删净）：PUT l027x-deb/l027x-gen → GET /api/repositories/{k}、/api/v2/repositories/{k}、
                /api/repositories/configurations?packageType={deb,generic} → POST 设值 → GET 复读 → DELETE ×2 → 列表 l027* 残留=[]
Outputs:      A 活体（2026-09-17，pro 7.161.15 :8082）：v1 deb keys=62 / v1 gen keys=61；v2 deb keys=21 / v2 gen keys=18；conf deb=21 / conf gen=18；
              v1 deb-only=['optionalIndexCompressionFormats']，v2/conf deb-only=['ddebSupported','debianTrivialLayout','optionalIndexCompressionFormats']；
              v1 值级差 enableDebianSupport True/False；设值臂 v1 keys=62 oICF=['bz2'] eDS=True、v2 keys=21 oICF=['bz2']（A 忽略显式 false）。
              本地测试：TestDebConfigFaceConditionalKeys 四面全过（62/61、21/18、21/18、62/61）+ 形态子测过；TestDebConfigFaceSetValuesWin 过（v1 62 / v2 22=21+回显 1）。
Compatibility: 契约对照：docs/compatibility/contracts/d02-config.yaml#deb-package-conditional-keys（L026-5 落，SPECIFIED）本票实现其 local 读面臂——默认面与设值回显两臂与 wire 逐键一致；
              已知分歧 d02/deb-default-key-family 的 B 臂由「代码读数推定」变「本地 realstack 实测」（pro 档差分腿仍待 licensed 实例，dev community 不可达维持）。
              与票面一处出入（证据与预期不符，带现场上报不静默迁就）：票面任务 3 称「仅 v1 local 面——v2/consumer 不渲染」，但 L026-5 wire 与本轮 A 复测一致证明 v2 单仓面与
              configurations 面 deb 行均渲染条件键（21 键，含 oICF=[]）；已按 A wire 实现（62/21/21 三面），未按票面短语砍 v2/configurations 臂。若 conductor 另有 pro 档复测依据，翻案只需
              删 debV2LocalSeats 附加分支。
              可差分 surface（供 differential-qa-engineer / 金样采集）：GET /api/repositories/{deb} 期望 62 键（oICF=[]、eDS=true）；GET /api/v2/repositories/{deb} 期望 21 键；
              GET /api/repositories/configurations deb 行期望 21 键（rclass 方言）；POST 设 oICF=["bz2"] 后三面键数不变、值回显；generic/maven/npm/pypi/docker 各面维持 61/18 等共享口径零动。
              漂移登记（新，转 compatibility-engineer）：A 对 deb 仓 POST enableDebianSupport:false 更新被忽略（复读仍 true）——写面行为，BinFlow 读面维持 blob-wins（存 false 回 false）；
              A 引擎对默认配置 deb 仓是否仍发 Packages.bz2 未实测（debian.md 勘误已标「未定」臂）。
Security:     无新攻击面：配置读面纯渲染；未动认证/权限投影（非 admin 四键投影不经 configFaceSeats）；A 侧造数仅本票自有 repo key（l027x-*）用后删净、列表残留校验=[]；未触碰 A 其他仓/权限/用户。
Performance:  无（每请求多至 3 个 seat 的 map 写入；共享 seat 切片仍只读复用，withEnvironments 既有拷贝语义未变）。
Risks:        ① remote/virtual debian 行的条件键未实测（A 上未造 deb remote/virtual 对拍），维持共享面——若 A 侧此类行也渲染条件键，属后续补拍；② A 忽略 enableDebianSupport 显式 false 的写面行为未实现
              （BinFlow 存什么读什么），已登记候裁；③ ddebSupported/debianTrivialLayout 在 v1 面本就是共享座（false），deb 行为键语义（trivial 布局引擎）另属 deb 布局实现面，本票只锁读面键形；
              ④ 客户端版本维度：本票为 REST 配置面，无包管理器客户端版本差异臂；A 版本 7.161.15 单点。
Blockers:     无（A 可达、造数通道正常；dev 实例 community 门按票面绕行——本地 realstack 验证）。
Next:         ① 转 compatibility-engineer：d02-config/deb-package-conditional-keys 契约 SPECIFIED→IMPLEMENTED 状态推进 + 上述 enableDebianSupport 写面忽略漂移登记（known-divergence 候条目）；
              ② pro 档差分腿（licensed 实例）跑上表可差分 surface；③ A 引擎 Packages.bz2 发文件臂探针（造数 deb 仓 + debPUT + 列 dists/ 树）随差分腿；④ debian.md 勘误的同串联动三处
              （inv-3-protocols.md L73 / artifactory-full-feature-matrix.md L72 / protocols/registry.yaml L289）归 reverse-engineer/compatibility 域，本票未碰。
```
