# T-397 [P1] 文档 A：接入与运维增量（M14 B6）

- 角色：tech-writer；area：docs/user/ + docs-site（侧栏核查）+ README 双语。dep：T-392（docker remote 首航）、T-394（npm login + helm keep）✓。
- 纪律：零 git 操作（conductor 收口）；不碰 web/（T-386 并行）、internal/、cmd/。产品代码 diff = 0。
- 状态：done（AC1/AC2/AC3 全过；全部客户端命令本机复跑取证，见 §2）。

## 1. 产出清单（9 文件）

| 文件 | 增量 |
|---|---|
| `docs/user/admin/remote-virtual.md` | **AC1 主体**：banner M14 增补；`packageType` 合法值段更新（remote+docker 开闸、virtual+docker 维持 400）；**新增「docker remote 仓（M14，FR-129）」专节**（建仓命令、url 形态表、MISS/HIT/STALE/降级/认证舞步/边界、SSRF 注记、dind 调试注记〔`--feature containerd-snapshotter=false` + macOS PMTU/MSS 1300 环境症〕）；上游兼容性速查加 docker 行；**L195 过期行修正**（原「docker remote 建仓 400」作废留痕 + virtual 拒绝新行）；常见报错码表加 virtual docker 400 行（live 逐字文案）；缓存管理节加 docker manifest digest 寻址强刷注记 |
| `docs/user/docker-registry.md` | banner M14 增补 + 新增「remote 仓：pull-through 代理上游（M14）」接入节（建仓/pull/观测三步，自指上游实测形态示例、Docker Hub URL 形态标注待公网验证）；常见报错表 +3 行（dind snapshotter 卡住 / macOS PMTU 黑洞 / remote 404 带 ssrf-guard） |
| `docs/user/integrations/npm.md` | **零编辑**——T-394 已回写（banner + login 段重写）；本票职责为**命令实测复跑**（§2 腿 N，全绿确认现状文档如实） |
| `docs/user/install/helm.md` | banner M14 增补（keep 注解实测口径）；**卸载节重写**：`helm.sh/resource-policy: keep` 恒注入（用户注解撞键 keep 单键胜出）、卸载有意幸存 + 重装复用、**手动清卷二路**（按名 `<release>-binflow`——NOTES.txt 同款；按标签 instance=<release>）、existingClaim 边界 + 「没有开关可关」如实注记 |
| `docs/user/api-reference.md` | banner M1~M14（docker remote 开闸 + npm login 端点族）；**NE: npm 域 +5 行**（couch login PUT〔豁免/幂等再铸/类型钉死〕、`-rev` 重试拼写、`-/v1/login` 401 回落、whoami、ping）；仓库管理域表后 M14 形态注记（remote docker 200 community / virtual docker 400 逐字文案） |
| `docs/user/faq.md` | banner M14；**新增「M14 增补两问」**：① docker remote 缓存命中观测（docker 客户端不显头 → curl `/v2` 路径看 `X-Binflow-Cache` 三态；逐路径 MISS→HIT 口径；上游计数冻结 delta 0；STALE 形态）② helm uninstall 留卷（keep 姿态 + 手动清卷路径） |
| `docs/user/guides/oidc-config.md` | **armed 起法一行修订**（T-382 契约漂移）：启动命令补 `BINFLOW_REMOTE_CREDENTIALS_KEY` + 注记（缺钥 fail-fast 文案逐字、补钥后 `"oidc":true` 实证） |
| `docs/user/README.md` | 导航三行增量（docker remote M14 / npm login M14 / helm keep） |
| `README.md` + `README.zh-CN.md` | **AC3 双语收口**：陈旧括号修正（「cargo remote/virtual pending」→ cargo 已于 M11 交付，现行唯一缺口 = docker virtual）；新增 M14 进行中一段（docker remote / npm login / PVC keep / **品牌 logo + 包型图标一行**）；里程碑行 M13 done → M14 in progress；+M14 PRD 链接行（双语镜像） |

docs-site/sidebars.js 零改动（无新页面，全部增量落既有挂载页——核查见 §3）。

## 2. 实测取证（全部本机复跑，2026-08-31；二进制 bin/binflow-server 当日 22:15 构建——启动行即 T-394 新措辞自证新鲜）

环境：A=上游 :18501（中途改绑 0.0.0.0）、B=下游 :18502（带 `BINFLOW_REMOTE_CREDENTIALS_KEY`，**零 license**）、C=npm :18503、armed :18508 + mock IdP :18599；dind 29.7.2（`--insecure-registry`×2 + `--feature containerd-snapshotter=false` + OUTPUT/INPUT 双向 TCPMSS 1300）；npm 10.9.8 / node v22.23.2 / helm v3 / expect。

### D 腿：docker remote（对应 AC1/AC2，镜像 alpine:3.20）

| # | 命令/操作 | 结果 |
|---|---|---|
| D1 | B（`licensed:false, tier:"community"`）PUT `rclass=remote packageType=docker` | **200**；同实例 `rclass=virtual packageType=docker` → **400** 逐字 `repository type not supported: virtual docker repositories are not supported (docker serves local and remote; PRD Q4 keeps virtual docker unserved)` |
| D2 | dind push `…:18501/up-local/t397img:1` | digest `sha256:6c2a9711b0a9…`（与 T-392 同镜像同值） |
| D3 | 首拉经 B `docker-remote` | RepoDigest **全等**；上游 access 计数 +1（manifest；层在 dind 本地已有——客户端不重复取，印证「逐路径」口径） |
| D4 | rmi+prune 后二拉 | RepoDigest 同值；上游计数 **delta 0 冻结** |
| D5 | curl `/v2/.../manifests/1`（by-tag）/ by-digest / blobs（config 612B + layer 3630321B） | **MISS → HIT** 逐路径全实证（layer `Content-Length: 3630321` 与 A 侧一致） |
| D6 | TTL 5s 仓 + 杀 A + 过期回取 | **200 + `X-Binflow-Cache: STALE` + `X-Binflow-Upstream-Error: upstream unreachable` + `Docker-Content-Digest` 不变**；未缓存 tag → **404 `MANIFEST_UNKNOWN`**（message 附上游 dial 摘要，零 5xx）；dind docker pull 已缓存 tag 照常成功（`Status: Image is up to date`） |
| D7 | SSRF 腿：url 用 `host.docker.internal` | 404 message 含 `ssrf-guard: … lookup host.docker.internal: no such host`（服务端视角无此名——文档注记依据）；改 host LAN IP + `allowPrivateUpstream:true` 后通 |
| D8 | **Docker Hub 直连探测**：建仓 200（url=`https://registry-1.docker.io/v2`），但 host 与 server 双侧 `registry-1.docker.io` 均不可达（解析到污染 IPv6 超时） | **环境限制非产品面**——文档如实标「公网 registry 直连待公网环境验证；Bearer 舞步同构」（Bearer 舞步本体以 T-392 mock 上游实测为据） |
| D9 | tag 路径 / 裸 hex 路径 DELETE（强刷探测） | 双拼法 404、缓存不动——manifest 按 digest 寻址，写入缓存管理节注记 |

### N 腿：npm legacy login（AC1，验证 T-394 回写可复跑）

| # | 操作 | 结果 |
|---|---|---|
| N1 | expect 伪 TTY 裸 `npm login --auth-type=legacy` | `Logged in on http://127.0.0.1:18503/binflow/api/npm/npm-local/.`；`.npmrc` 改写 `//127.0.0.1:18503/binflow/api/npm/npm-local/:_authToken=15ef75bc…`（带尾斜杠限定形态） |
| N2 | token 全链 | `npm whoami`→admin；`npm publish`→`+ t397-probe@1.0.0`；全新目录 `npm install` + `require` → `t397` |
| N3 | curl 直连 login ×2 | **201 幂等再铸**双次（token 各异，`{"ok":true,"id":"org.couchdb.user:admin"}`）；错口令 **401 + `WWW-Authenticate: Basic realm="BinFlow Realm"`** |
| N4 | api-reference 新行对齐 | `-ping` 匿名 `200 {}`；`-/whoami` 匿名 401 / token `{"username":"admin"}`；`POST -/v1/login` **401**；couch 族打 npm-mount 下的 generic 仓 **401**（类型钉死不外溢） |

### H 腿：helm keep（AC2）与 A 腿：armed OIDC（AC2）

| # | 操作 | 结果 |
|---|---|---|
| H1 | `helm lint charts/binflow` | 1 chart(s) linted, 0 failed |
| H2 | `helm template --show-only templates/pvc.yaml` 三态 | 默认含 `helm.sh/resource-policy: keep`；用户注解撞键 `remove` 时 **keep 单键胜出**（无重复键）；`existingClaim` 时模板零产出 |
| H3 | `helm install --dry-run` NOTES 渲染 | keep 段 + 手动清卷行 `kubectl delete pvc --namespace default bf-binflow`（PVC 名 = `<release>-binflow`，instance 标签 = release 名——文档按名/按标签双路依据） |
| A1 | armed.yaml 起（无钥） | fail-fast 逐字 `…seeding section oidc: field "client_secret": … no master key: BINFLOW_REMOTE_CREDENTIALS_KEY is not set (required to store secrets)` |
| A2 | 补钥 + mock IdP 起 | readyz 200；`GET /api/v1/auth/methods` → `{"password":true,"oidc":true,"ldap":false}`（oidc-config.md 自检行原文对齐） |

### 构建与侧栏（AC2/AC3）

- `make docs`：**SUCCESS**（docs site size 4.53 MB）；第一轮撞 MDX 编译错（表格 cell 内 code span 藏 `|` 把 span 劈开、`<镜像名（` 被 JSX 化）——修正后两轮全绿，**零断链**（Docusaurus onBrokenMarkdownLinks 无输出；新锚点 `docker-remote-仓m14fr-129` / `remote-仓pull-through-代理上游m14` / `卸载` / faq M14 段在 build 与内嵌 `internal/docs/dist` 均落位，跨页 href 经 percent-encoding 与 id 逐对核对）。侧栏核查：全部增量落既有挂载页，无新文件 → sidebars.js 零改动即挂载。

## 3. AC 判定

| AC | 判定 | 依据 |
|---|---|---|
| AC1 docker remote 接入指南（三态齐装叙事 + 缓存命中/降级语义）+ npm login 注记衔接；命令实测可复跑 | **达成** | remote-virtual.md 专节 + docker-registry.md 接入节 + README/docs 导航（三态叙事 = local+remote 齐装、virtual 拒绝如实）；npm.md 现状（T-394 回写）经 N1~N4 全链复跑确认；D1~D9 实测 |
| AC2 helm keep 说明 + api-reference 增量（npm login 端点族）+ FAQ 增补 + make docs SUCCESS 零断链 + 侧栏挂页 + armed 一行修订 | **达成** | helm.md 卸载节（H1~H3）；api-reference NE+5 行与 M14 形态注记（N4 对齐）；faq M14 两问；make docs SUCCESS 零断链；sidebars 核查零改动；oidc-config.md 一行修订（A1/A2） |
| AC3 README 双语收口（M14 进行中态 + 品牌面一行）+ 全部命令实测 | **达成** | README/README.zh-CN 对称四改（陈旧括号修正/M14 段〔含品牌 logo + 图标一行〕/里程碑行/PRD 链接行）；§2 全部命令本机复跑 |

## 4. 遗留 / 待确认

1. **Docker Hub 等公网 registry 直连未实测**（本机网络对 `registry-1.docker.io` 解析污染双侧超时——环境限制）；文档已标注「待公网环境验证，Bearer 舞步同构」。建议随 T-399 UAT 或任一可出网环境补一轮。
2. docker virtual（聚合）仍拒（PRD Q4）——是否开矩阵归 conductor（T-392 遗留④原文传递）。
3. docs/user/guides/migrate-artifactory.md L17「不支持的包类型（nuget 等）」措辞陈旧（nuget M10/M12 已交付）——超出本票面（迁移指南 nuget 语义另核），登记后续文档票顺笔。
4. 集群内 `helm uninstall` 幸存 UAT 与 Chart 版本 bump 归 T-399（票面既定）；本文 keep 注解面以 template/dry-run 三态实证。
5. 强刷探测发现 docker remote manifest 的按路径 DELETE 双拼法不命中（digest 寻址）——已在文档如实注记；是否提供 by-digest 强刷入口属产品决策（候选池）。

## 5. 环境清理

A/B/C/armed/mock-IdP 五进程 kill（18501/18502/18503/18508/18599 端口复核全 DOWN）；dind 容器 rm；`/tmp/t397/`（实例数据 + 日志 + expect 脚本）保留供复验，可整删。并行票的 3 个 binflow-server 进程未触碰。
