# T-429 — M15 release 烟测 + UAT 随里程碑 PR（release-engineer）

- 票：T-429 [P1] release 烟测 + UAT 随里程碑 PR（M15-SPLIT §1.2；area: deploy/ + charts/ + CD 链；dep: 全部实现票 + T-426）
- 状态：**done**（七腿烟测全绿 + Chart **1.4.0→1.5.0** bump 定案并 kind 真装贯通 + F1 门 120MB 再基线**首验 PASS** + 资源门三连 GREEN + UAT AFTER 清单备妥——候 conductor 在 T-430 PASS 后建 develop→main 里程碑 PR）
- 日期：2026-09-02
- 红线遵守：**零 git 写操作**（scratch 取自 `git archive HEAD`=**9bc44b9**；working tree 只改本票 area 7 文件）；未推送任何镜像/Chart/二进制（`REGISTRY= PUSH=0` 本地止步）；UAT 仅只读取证 + 出网探测（零写、零服务变更）；对外发布红线维持。
- 被测树：**HEAD 9bc44b9**（M15 全量：T-407~T-428 + T-431 落齐、T-421 收波后的 sprint 1239 提交）。

---

## 1. Chart 版本收口：**1.4.0 → 1.5.0**（bump 定案）

- **判据核对**：M15 含 behavior 变化三处——virtual 聚合开禁（T-412/416，FR-21-AC8 兑现）、docker×virtual 建仓矩阵开禁（T-431/Q6）、AQL 全新端点面（T-415/417）——按派单口径 **minor bump 1.5.0**。
- **新增渲染键**：`config.replication.blockPush` / `blockPull`（T-422 / FR-138.3 全局复制急刹双键——M15 **唯一**新增全局配置键族；AQL K63 门 = 内部常量零配置键，Q2 裁定）。三处落地：
  - `values.yaml`：双键 + SEED-ONLY 语义注释（仅首启播种 `replication_globals` 行，此后该行为权威、改 chart 值不再生效；运行时翻转走 REST `POST /api/v1/system/replications/block|unblock` + 控制台开关——K31 双源姿态，注释写自 api.go 原文语义）；
  - `templates/configmap.yaml`：`replication.block_push` / `block_pull` 显式渲染（T-183 教训——键沉默缺席即沉默缺省）；
  - `values.schema.json`：双键 boolean + 描述（**类型执法实证**：`--set config.replication.blockPush=x` → `got string, want boolean` 拒绝）。
- **changes 条目**：按「每版重写、新置顶、旧版条目留后」惯例（65ba574→e8aec33→282bb48 先例链）——M15 两条置顶（急刹双键 + 「M15 应用面零新配置键」升级说明），1.4.0 PVC keep 条目及其后 M13 族条目维持原位。
- **验证**：`helm lint` = 1 chart(s) linted, 0 failed；四态渲染——默认 `block_push: false/block_pull: false`、`--set =true` 双 true、schema 类型拒绝、`helm package` → **binflow-1.5.0.tgz**。
- **kind 真装贯通（新键三段链）**：`helm install --set config.replication.blockPush=true` → ① ConfigMap 实渲染 `block_push: true`；② pod boot log `"replication: global block state seeded from binflow.yaml and now REST-managed","block_push":true`；③ REST `GET /api/v1/system/replications` = `{"blockPushReplications":true,...}` → `unblock` 200 → GET 复位 false。**chart 键 → yaml → seed → REST 态全链实证**。
- **PVC keep 复验（1.5.0）**：uninstall 后 PVC 幸存（同 uid `67225235` 同 vol `pvc-67225235` + `keep` 注解）→ 手动清卷 → ns 删除。M14 T-394 语义在 1.5.0 维持。

## 2. 部署矩阵烟测——七腿全绿

构建形态：`git archive 9bc44b9` → /tmp/bf-t429 → `make console && make docs && make build` 全绿（vite 7.3.6 = T-432 升级形态；SPA gz 预算内；docs 4.78MB；裸 bf 15.28MB / bf-migrate 8.96MB / **binflow-server 40.59MB——首破 40MB 原始线，见 §5-F1**）。

| 腿 | 结果 | 关键实测 |
|---|---|---|
| **R 六平台 goreleaser 复刻** | ✅ | /tmp/t429-mkdist.sh 按 `.goreleaser.yaml` 逐字复刻（`-s -w -X main.version/-X main.revision=9bc44b9`、`-trimpath`、CGO=0、目录包裹+README、zip/tar.gz、sha256 checksums）；`make release-verify RELVER=v1.0.0` = **6/6 archives + checksums，shasum -c 全 OK**；**F1 门（120MB 再基线）首验：聚合 104.52MB PASS**（M14 103.36 → **+1.16MB/+1.1%**——AQL 引擎 + 复制包 B + docs/SPA 增量，距门 15.5MB 余量，非红旗） |
| **I 镜像链**（build-release.sh） | ✅ | `REGISTRY= PUSH=0`：Dockerfile.assets 原生资产级 + 双变体×双 arch 零-RUN 注入 → 四镜像 config arch 字段四验全对；amd64 smoke readyz OK + `version v1.0.0 revision 9bc44b9`（M15 位注入实证）；arm64 qemu 实跑同报（宿主有 qemu）。合并名 tag 以 amd64 档重打（PUSH=0 无 manifest 组装——离线包/compose 消费口径） |
| **L1 compose**（round 1/2/3） | ✅ | prebuilt `binflow:v1.0.0-alpine` up → healthy → healthz/readyz OK + version 9bc44b9；favicon-996d22f6 200 immutable + 与源资产 cmp 逐字节一致；roundtrip 建 200→PUT 201→**cmp 字节一致**→删 200；**M15 面五探针**（§3） |
| **L3 k8s 清单**（kind bf-ga，distroless） | ✅ | kind load（sha 81631d=M15 位）→ `kubectl kustomize` 127 行 → sed 密码 → apply 4 资源 → rollout OK + PVC Bound；port-forward → healthz OK + version 9bc44b9 + favicon 200 **IDENTICAL** + roundtrip 字节一致 + **AQL 200 total:1** + `POST /api/v1/replications/99999/run` = **404 "replication config not found: 99999"**（T-420 路由活体）；`kubectl delete -f` 还原空态 |
| **L-chart**（kind，AC1 核心） | ✅ | 见 §1 三段链 + roundtrip（PUT 201 逐字回读）+ AQL total:1 + PVC keep |
| **L4 systemd**（debian:bookworm-slim） | ✅ | 静态：`bash -n` OK + `--dry-run` 全走通 + unit 语义面 6/6 在册（User=/Group=binflow、Restart=on-failure、ReadWritePaths/ReadOnlyPaths、EnvironmentFile=-）。活体：0600 root 属主 env **服务用户直读被拒**（恰证 manager 注入设计）→ root source env + `setpriv --reuid/--regid/--clear-groups` 降权 boot `v1.0.0 (9bc44b9)`；**`BINFLOW_REPLICATION__BLOCK_PUSH=true` env 种子 → REST `blockPushReplications:true` 闭环**（M15 键 env 面）；`retention_days:21` 回显（M13 env 注入维持）；favicon 引用 + roundtrip IDENTICAL + AQL total:1；容器已 rm |
| **L5 offline** | ✅ | 手工按 README 布局构包（docker save 双变体 + **helm package → binflow-1.5.0.tgz** + k8s/compose 面 + linux amd64/arm64 二进制 + SHA256SUMS〔排除自身〕，18 文件 51M tar）；净目录解包 `shasum -c` **全 OK**；`--compose`（.env 18097/retention 10）→ healthy + version 9bc44b9 + retention 回显 10 + favicon 200 + roundtrip IDENTICAL + **AQL total:1** + **docker×virtual 200**；`--helm --dry-run` → **chart 自动发现 binflow-1.5.0.tgz + 钉捆绑镜像** `--set image.repository=binflow --set image.tag=v1.0.0-distroless`（T-376 C3 修复在 1.5.0 维持）；down -v 清净 |

## 3. M15 功能面部署探针（各腿内嵌）

| M15 面 | 证据 | 腿 |
|---|---|---|
| **AQL**（T-409/411/413/415） | `POST /api/search/aql` `items.find({"repo":"X"}).include("name")` → 200 + 流式 envelope（results 行命中 + range total:1）；`$eq` 形与隐式形双绿；garbage → 400 逐字（`items.find(*)` 裸星 → 400 E-01 语法错——文法诚实拒绝）；裸二进制面最全（envelope 全字段：repo/path/name/type/size/created/created_by…） | compose/bare/k8s/chart/systemd/offline 六面 |
| **老搜索**（T-417） | `GET /api/search/gavc` 200（generic 仓无 maven 坐标 → 空集——语义正确，弱标记如实登记） | compose |
| **virtual 聚合**（T-412/416） | generic virtual（成员 local）`GET /api/storage/<vg>/smoke?list` → **成员行命中**（t429.bin + sha1 + folder:false）——成员存储行作答、零回源 | compose |
| **docker×virtual**（T-431） | 无成员 400 = **成员校验错**（矩阵门已过——M14 为组合矩阵 400）；带成员 200 "Successfully created repository" | compose/k8s/offline 三面 |
| **Replicate Now**（T-420） | 同实例双仓腿：dst 建 200 → src 上传 → `POST /api/v1/replications`（name/source_repo/target_url/target_repo/target_username/target_password——**需实例主密钥** seal）→ id=1 → `POST .../run` **200** + info 锚串 "The replication tasks was successfully scheduled to run" + scheduled:2 → **~2s 后 dst 侧逐字节回读一致** → DELETE 204 | compose |
| **blockPush/blockPull**（T-422） | `POST .../block?push=true` 200 text → `run` **409** + 逐字锚文 "Push replication is blocked, skipping replication (config t429-repl; POST /api/v1/system/replications/unblock to resume)" → `unblock` 200 复位；boot seed 行四处在册（compose/k8s chart/systemd env/bare） | compose + chart + systemd |

## 4. AC2 — 资源门三连 + wire-brand 接线核验

**资源门三连（全 GREEN）**：
1. **footprint ≤100MB**：`make footprint EXPECT=1` → **GREEN**——vmmap-physical-footprint@ready **11.9MB**（idle+15s 12.7MB；ps rss 149MB 为 darwin 共享页口径，非判定面）。
2. **冷启动 <2s**：同探针 **514ms**（复测 1576ms 亦绿）。⚠️ 首测 12259ms RED 系**宿主负载 153~233 撞车假象**（T-421 e2e 大波 + T-430 go race 波并行时段）——错峰复测两连绿，登记 F5 环境注记非产品面。
3. **check-size ≤120MB**：release-verify 内嵌 = **104.52MB**（F1 再基线首验 PASS；M14 基线 103.37 → +1.16MB，见 §2-R）。

**F1 六平台趋势**：94.13（T-326 建门）→ 103.36（M14）→ **104.52（M15）**——M15 净增 +1.16MB 含 docs +0.2MB（T-426/T-428）+ SPA 增量 + AQL/复制 Go 代码；单档最大 18.24MB（≤40MB 硬门维持）。**显著上浮红旗未触发**。

**SPA/favicon/docs go:embed 接线（wire-brand 指纹贯穿）**：`favicon-996d22f6.ico` 指纹在 **compose（alpine）/ k8s（distroless）/ chart（kind）/ systemd（goreleaser 单二进制）/ offline / 裸二进制** 六面全 200 且与 `web/public/brand/favicon.ico` **cmp 逐字节一致**（M14 已验 negative 臂与 manifest 链，本轮面矩阵维持）；docs 面 `/binflow/docs/aql` 200 + 10×`items.find` + 子集边界文案在场（**T-426 腿① AQL 指南已内嵌**——sitemap 定位）。

## 5. 发现登记（报不自裁）

| # | 发现 | 处置态 |
|---|---|---|
| **F1** | **binflow-server 裸二进制 40.59MB 首破 40MB 原始预算线**（M14 39.87 → +0.72MB）：check-size 面 1 为 WARN-only（`make build` 设计上不因尺寸失败），且单档压缩档 18.24MB 距 40MB 硬门余量大、运行时门全绿——但 40MB PRODUCT 线是 T-7 以来首次实破 | **登记 M16 观察项**：建议 conductor 裁——维持 WARN 面（预算线随 GA 前增长自然上移）或再基线（如 44MB）；非 M15 突增（+1.8%），趋势与 F1 聚合门同向 |
| F2 | compose 首腿一次**首 PUT 405**（healthy+2s 发出；后续两轮受控三连发 [healthy+2/8/15s] 全 201、round-2 同形请求 200——不可复现）。疑似满载下首请求路由竞态；注意 405 也是该路径对真不支持方法（POST）的正确应答，无混淆风险面 | QA 观察项（T-430 若有暖机探针可顺带）；非阻塞 |
| F3 | `make offline-bundle` 目标**配方内注释续行断链**（recipe 停在 header 不产包）+ 其 SHA256SUMS 生成 `find . -type f` **自指包含**（verify 必 FAILED）——T-399 即以手工构包绕开（先例在册），本轮同样手工路径；另 `compose/.env.example` **末行无换行**（append 即粘连注释行）+ `install-offline.sh` 健康窗 **30s 硬编码**（满载宿主冷启 60~90s 必超时回滚——`.env` 端口本身已被 T-376 修对，探测口 18097 与发布口一致，纯时间窗问题） | 三处均**预存在**（非 M15 引入）；候小票修（Makefile 属根构建配置，需单独票）；offline 腿证据以手工路径补齐闭环 |
| F4 | `bf` 15.28MB 触 15MB WARNING——m13/m14 同值维持态 | 维持登记 |
| F5 | **Docker Desktop VM「Internal Virtualization error」崩溃**（13:53 起宕——早于本票会话；常规 quit+relaunch 无效）→ **彻底清进程周期**（quit + pkill backend/Docker Desktop + 重开）修复。e2e 大波期间 daemon 曾不可用约 40 分钟 | 环境事件留痕；若复发需宿主重启（用户动作） |
| F6 | AQL `items.find(*)` 裸星形 400（BinFlow 文法要求 find() 收对象）——与 Artifactory 文档 `items.find(*)` 形有差；错误文案逐字友好（E-01）。aql.md/T-409 已按文法定案，非缺陷 | 知悉项：docs/user/aql.md 示例均为对象形，无用户误导面 |

## 6. UAT BEFORE 基线核 + AFTER 清单（候 conductor 建 PR）

**现状核（只读，2026-09-02 10:33Z）——基线已翻转到 M14**：

```
uat.13ca2d6（T-399 BEFORE）→ 现役 **uat.2a11096**（= origin/main HEAD，M14 里程碑 PR #65 已合并部署）
unit binflow-uat active since 2026-09-02 07:46:22 UTC；healthz OK
favicon-996d22f6.ico 维持（品牌面零变化）
M15 端点 BEFORE 形态：POST /api/search/aql 匿名 403；GET /api/search/gavc 200 空集；
  POST /api/v1/replications/99999/run 匿名 401（路由面在 auth 后——带凭据 M15 翻转标记见下）
```

**AFTER 清单（PR 合并触发后逐条可粘贴；$UAT_PW conductor 持有）**：

```bash
SSH="ssh -i ~/.ssh/binflow-uat.pem -o IdentitiesOnly=yes ubuntu@52.79.109.153"

# 1) 换装 + 基线翻转
$SSH 'systemctl is-active binflow-uat; curl -fsS http://127.0.0.1:8080/binflow/api/system/version'
#    期望 uat.<合并sha> ≠ uat.2a11096

# 2) M15 标记 A：AQL 端点（T-415）
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X POST \
      http://127.0.0.1:8080/binflow/api/search/aql -H "Content-Type: text/plain" \
      --data "items.find({\"repo\":{\"$eq\":\"artifactory\"}})"'   # 任意仓键；200 + envelope 即翻转（M14 无路由）
$SSH 'curl -s -u admin:$UAT_PW -X POST http://127.0.0.1:8080/binflow/api/search/aql \
      -H "Content-Type: text/plain" --data "garbage" | head -c 200'
#    期望 400 QueryError envelope（语法/域错文案）

# 3) M15 标记 B：Replicate Now 双仓腿（T-420；同实例 A→B，自建自删纪律）
$SSH 'curl -s -o /dev/null -w "src:%{http_code} " -u admin:$UAT_PW -X PUT http://127.0.0.1:8080/binflow/api/repositories/t429-uat-src -H "Content-Type: application/json" -d "{\"rclass\":\"local\",\"packageType\":\"generic\"}";
      curl -s -o /dev/null -w "dst:%{http_code} " -u admin:$UAT_PW -X PUT http://127.0.0.1:8080/binflow/api/repositories/t429-uat-dst -H "Content-Type: application/json" -d "{\"rclass\":\"local\",\"packageType\":\"generic\"}";
      printf t429-uat-repl-payload > /tmp/t429p;
      curl -s -o /dev/null -w "put:%{http_code}\n" -u admin:$UAT_PW --data-binary @/tmp/t429p http://127.0.0.1:8080/binflow/t429-uat-src/smoke/t429.bin'
$SSH 'curl -s -u admin:$UAT_PW -X POST http://127.0.0.1:8080/binflow/api/v1/replications \
      -H "Content-Type: application/json" -d "{\"name\":\"t429-uat-repl\",\"source_repo\":\"t429-uat-src\",\"target_url\":\"http://127.0.0.1:8080\",\"target_repo\":\"t429-uat-dst\",\"target_username\":\"admin\",\"target_password\":\"$UAT_PW\",\"enabled\":true}"'
#    记 id；⚠️ target_password 需实例主密钥已设（UAT M11 起已配；若 400 提示无密钥则跳过本腿只跑 2)/4)——零风险优先
$SSH 'curl -s -u admin:$UAT_PW -X POST http://127.0.0.1:8080/binflow/api/v1/replications/<id>/run'   # 200 + "successfully scheduled"
$SSH 'sleep 5; curl -fsS -u admin:$UAT_PW http://127.0.0.1:8080/binflow/t429-uat-dst/smoke/t429.bin; echo'   # 回读 payload 逐字
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X DELETE http://127.0.0.1:8080/binflow/api/v1/replications/t429-uat-repl'  # 204；随后删两仓（?deleteContent=true）

# 4) M15 标记 C：封锁 409 锚文（T-422；可选——全局态往返、解锁先行）
$SSH 'curl -s -w " %{http_code}\n" -u admin:$UAT_PW -X POST "http://127.0.0.1:8080/binflow/api/v1/system/replications/block?push=true"'  # 200 text
$SSH 'curl -s -w " %{http_code}\n" -u admin:$UAT_PW -X POST http://127.0.0.1:8080/binflow/api/v1/replications/<id>/run'               # 409 "Push replication is blocked, skipping replication"
$SSH 'curl -s -w " %{http_code}\n" -u admin:$UAT_PW -X POST "http://127.0.0.1:8080/binflow/api/v1/system/replications/unblock?push=true"'  # 200 复位（务必执行）

# 5) M15 标记 D：docker×virtual 建仓开禁（T-431）
$SSH 'curl -s -o /dev/null -w "%{http_code}\n" -u admin:$UAT_PW -X PUT http://127.0.0.1:8080/binflow/api/repositories/t429-uat-dl -H "Content-Type: application/json" -d "{\"rclass\":\"local\",\"packageType\":\"docker\"}";
      curl -s -w " %{http_code}\n" -u admin:$UAT_PW -X PUT http://127.0.0.1:8080/binflow/api/repositories/t429-uat-dv -H "Content-Type: application/json" -d "{\"rclass\":\"virtual\",\"packageType\":\"docker\",\"repositories\":[\"t429-uat-dl\"]}"'  # 200 200；随后删两仓

# 6) 文档/品牌面维持 + CI 键复核
$SSH 'curl -s http://127.0.0.1:8080/binflow/docs/sitemap.xml | grep -c /docs/aql'   # ≥1（T-426 AQL 指南内嵌）
$SSH 'curl -s http://127.0.0.1:8080/binflow/ui/ | grep -oE "favicon-[a-f0-9]{8}\.ico"'  # favicon-996d22f6.ico 维持
git fetch origin && git show origin/main:.circleci/config.yml | grep -c UAT_SSH_KEY_B64  # ≥1（T-399 §4 结论维持）
```

UAT 数据纪律：零写除 §6-3/4/5 自建自删项；不动 `/opt/binflow-uat/binflow.yaml` 与 data/。

## 7. 交付文件（工作树未提交态，均在本票 area）

```
charts/binflow/Chart.yaml            1.4.0→1.5.0 + changes 置顶 M15 双条（急刹键 + 零新配置键说明）
charts/binflow/values.yaml           config.replication.blockPush/blockPull + SEED-ONLY 注释
charts/binflow/templates/configmap.yaml  replication.block_push/block_pull 显式渲染
charts/binflow/values.schema.json    双键 boolean schema + 描述（类型执法）
deploy/README.md                     矩阵第 4 行 M15/1.5.0 增补 + M15 里程碑烟测记录行
deploy/offline/README-offline.md     chart 包名示例 1.4.0→1.5.0
deploy/offline/install-offline.sh    注释示例 1.4.0→1.5.0（行为零变化）
```

（blockPush/blockPull 渲染键为 M15 新配置面收口——T-422 实现票归 dev 侧、部署面渲染归本票，与 M13 T-376 先例同分工。）

## 8. 清理对账

本地：compose×4 栈、offline 栈全 `down -v`（binflow 容器零残留——docker ps 仅 t226〔他票〕/kind 节点/buildx builder）；kind bf-ga 还原空态（系统基线外零资源，t429-chart ns/PVC 全删）；systemd 容器×4 已 rm；裸实例（:18093）已杀；port-forward 全断。留存备复验（/tmp，可整删）：`/tmp/bf-t429`（scratch 树）、`/tmp/t429-*.log|sh`（各腿脚本+日志）、`/tmp/binflow_offline_v1.0.0{,.tar.gz}`、`/tmp/t429-offline-test`。本地镜像 `binflow:v1.0.0-{alpine,distroless}{,-amd64,-arm64}` 已刷新为 M15 位（旧 M14 位被同 tag 覆盖——里程碑递进预期；未推任何 registry）。UAT：零写（§6 现状核全 GET/systemctl/curl 只读）。
