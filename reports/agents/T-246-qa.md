# QA 报告 T-246 — M8 终验：U01~U15 全量 + 零学习成本剧本×8 + NFR/回归硬门槛/DoD 收口

- 日期：2026-08-24 ｜ role：qa-engineer ｜ HEAD `74f4f08`（与派发一致）
- 工作树核查：产品源码（internal/cmd/web/scripts/Makefile）**零脏**；脏文件仅 docs-site 构建产物（任务前已存在）。无需 worktree 兜底。
- 环境：本机新鲜构建（`make console && make build`，Go 输入与 console 嵌入面新鲜度守卫均空）；被测实例 127.0.0.1:8080（scratch 数据目录）；docker 腿专实例（容器形态 18083 + 0.0.0.0:18082，daemon 在 VM 内需 localhost 侧可达）；用户实例 18080 全程零触碰（PID 36174 前后一致）。
- 方法：已有 spec 覆盖面跑全量套件；缺断言面补手工/脚本腿（U04 curl 对照、U09 真实 docker、P37 采样、axe 全页扫、剧本×8）——临时 spec 跑完已删，工作树复原。
- 判定口径：交互断言制（ADR-0029 决策 3），零像素对比。

---

## 一、U01~U15 全量（PRD §5 矩阵逐条）

| U | 面（UI-#） | 证据 | 结果 |
|---|---|---|---|
| U01 | 登录/回跳（UI-01） | `auxiliary.spec.ts`「login: inline error state, docs link, return round-trip」+ `login.spec.ts` OIDC 404/302 双态（T-158 mock 面，LDAP 同表单零分支）——全量套件绿 | ✅ |
| U02 | 双模式壳/深链/负向（UI-02/23/24） | `shell.spec.ts` 5 腿：admin 键盘切模式 + 12 条目逐项可达；readonly_admin 可达 + 无快速写入口；普通用户无切换入口 + `/admin/**` 深链 L2/L3 收敛；Access Tokens 占位归位；**负向：树无 Xray/Builds/Pipelines 节点**（UI-24 反断言） | ✅ |
| U03 | 旧路由 redirect 20 条 | `shell.spec.ts`「legacy routes: all 20 …redirects land on the new route with the page mounted」 | ✅ |
| U04 | 树+详情+三 checksum+下载对照（UI-03/04） | 套件（checksum 逐值剪贴板/拷贝全值断言）+ 本票手工腿：`curl GET /binflow/m8-perf-local/perf/w00/f000.txt` 与种子体 `cmp` **逐字节一致**，sha256 `5e939269…` 与 `/api/storage/...` 回显一致；剧本③ UI 下载文件 hash == 剪贴板 sha256（三方一致） | ✅ |
| U05 | 文件夹 children 表（UI-05） | `tree-list`（名称/类型/大小/修改时间/sha256/操作）+ 当前层过滤 + 加载更多（100/170 实测）+ 空态。**规格分歧登记**：PRD §4.2「列排序」未实现于树 children 表——console-m8 §6.3 线框收窄为「过滤+加载更多」，列头排序（C1）落在管理面表格（repos/users/perms 排序锚 + aria-sort 断言均在套件绿）。按冻结设计规格判过，分歧交 conductor 备案 | ✅（带登记） |
| U06 | Deploy 上传对话框（UI-06） | `setmeup-deploy.spec.ts`：拖拽投放 + 特殊文件名 + 编码回显（%→%25/空格→%20/中文→UTF-8 percent）+ checksum 徽标 + API 对账；剧本④ 1.0s 完整走通 | ✅ |
| U07 | 删除确认（UI-07） | `artifacts-tree.spec.ts`（输错路径 delete 禁用、输对执行 + API 对账）+ `repositories-admin.spec.ts`（typed-key 两段强确认 + deleteContent 对账） | ✅ |
| U08 | 大 repo 性能腿（FR-72 AC4） | 10,291 节点种子实例实测：登录→首屏可交互 p95 **734ms**（8 样本 677–743）≤1500ms；树根展开（50 子目录层）p95 **144ms**（12 样本 55–144）≤200ms；二级链层展开 p95 **75ms**（8 样本 46–75） | ✅ |
| U09 | 建仓→Set-Me-Up→**真实 docker 腿**（UI-08/09/10） | 剧本①分组向导建 docker 仓 1.6s；剧本②Set-Me-Up 铸币拿命令 1.7s（`echo "<TOKEN>" \| docker login http://… -u admin --password-stdin`）；**真客户端**：`docker login` Login Succeeded（口令形态 + Bearer token 形态双过）→ push digest `sha256:137cf75a…` → `docker rmi` 删缓存 → pull 重下 → `/v2/<name>/tags/list` = `["t246"]` 对账一致 | ✅ |
| U10 | 全局搜索/深链（FR-74） | `auxiliary.spec.ts`：搜索键盘链/计数/recentSearches 持久/`?q=` 深链回显/行 Enter 跳树；面包屑逐级回跳（artifacts 树腿） | ✅ |
| U11 | 用户/组（UI-12/13） | `users-groups.spec.ts` 4 腿：三角色 spec + 穿梭框 + 排序 + 角色徽章 + 删除守卫 + readonly 写重放 403；剧本⑥ 设 readonly_admin 1.4s + API `adminRole` 回读 | ✅ |
| U12 | 权限编辑器 wire 对照（UI-14） | `permissions.spec.ts`：两步资源对话框/四动作矩阵/pathmatch 测试器/diff 确认/toast/列表徽章/m-holder 覆盖集（wire 断言按 V07 逐字段在 spec 内钉死）；剧本⑤ 建组授权 1.7s + 列表 API 对账 | ✅ |
| U13 | 治理五页四态（UI-15~20） | `governance-monitoring.spec.ts`：治理 5 页 + 存储概要 + 系统信息、storage 表 API parity、readonly 三禁用 + 重放 403、user L2/L3 收敛；剧本⑦ 审计过滤 1.3s、剧本⑧ GC dry-run 1.4s（dry-run 后 stats 无变化） | ✅ |
| U14 | 主题等价（UI-21） | `theme-smoke.spec.ts`（默认亮色 Q2/data-theme 纯换值/持久化/首访 prefers）+ `keyboard.spec.ts` badge 双主题 axe 腿 + 本票全页扫双主题 38/40 组合净 | ✅ |
| U15 | 零复制扫描 + axe（UI-22 / FR-76 AC2+AC4） | `npm ls --all \| grep -iE 'jfrog\|artifactory'` = **0 匹配**；`git ls-files 'web/public/**/*.{png,jpg,gif,ico}'` = **空**（图标全 SVG 源码）；src 外链 grep 仅表单 placeholder 字符串（无 CDN/字体/脚本加载）。**axe 全页扫（20 页 × 双主题）：2 页各 1 处 serious 违例**（见缺陷 QA-1/QA-2），其余 38 组合净 | ⚠️ 扫描面过 / axe 面 **FAIL（2 缺陷）** |

## 二、零学习成本剧本 ×8（PRD §8-8「Artifactory 用户无文档完成」口径）

执行法：真实 Chromium，全程只走可见壳 affordance（模式切换/带标签导航组/工具栏按钮/对话框），无文档无深链；逐步计时。

| # | 剧本 | 操作路径（可见入口） | 耗时 | 卡点 | 结果 |
|---|---|---|---|---|---|
| ① | 建仓 | 登录 → 管理 → 仓库 → 添加仓库 → docker 网格 → 表单 → toast | **1.59s** | 无 | ✅ |
| ② | Set-Me-Up 拿命令 | 管理 → 仓库 → 行动作 Set Me Up → 铸币 → Configure 面 docker login 命令在手（token 已嵌入） | **1.61s** | 无 | ✅ |
| ③ | 浏览制品到下载 | 制品树 → repo → perf → 过滤 w00 → 进层 → 选 f000.txt → 拷 sha256 → 下载（hash 校验一致） | **1.48s** | 观察甲（过滤残留，见下） | ✅ |
| ④ | 上传 | 树工具栏 Deploy → 选仓 → 拖入文件 → 部署 → checksum 一致徽标 + API 对账 | **1.05s** | 无 | ✅ |
| ⑤ | 给组授权 | 管理 → 权限 → 新建 → 两步资源对话框（默认 include \*\*）→ 加组 → 勾 r+w → diff 确认 → toast + API 对账 | **1.68s** | 无 | ✅ |
| ⑥ | 设 readonly_admin | 管理 → 用户 → 行进编辑器 → 角色下拉 → 保存 → API `adminRole` 回读 | **1.42s** | 无 | ✅ |
| ⑦ | 查审计 | 管理 → 治理 → 审计日志 → actor 过滤 → 100 行 | **1.30s** | 无 | ✅ |
| ⑧ | 跑 GC dry-run | 管理 → 维护（GC） → Dry-run → 报告呈现（stats 前后无变化） | **1.56s** | 无 | ✅ |

**8/8 PASS，零卡壳**。两条观察（非缺陷，登记 UX 候选）：
- **观察甲**：树「过滤当前层」输入词跨层残留——下钻子层后过滤词仍生效致「已显示 0」，需手动清空（我方剧本清空后 1 步恢复；Artifactory 心智里过滤框通常随层复位）。
- **观察乙**：children 表首屏渲染 100/170，w00 类靠后条目需「加载更多」或过滤——过滤框即可达，符合大目录策略，仅记录。

## 三、NFR（P36/P37/P38 + S45~47）

| NFR | 门 | 实测 | 结果 |
|---|---|---|---|
| P36 体积/首屏 | gzip ≤350KB；二进制 <40MB；登录→首屏可交互 ≤1.5s | gzip **192,565B（188.1KB，54% 预算）**；二进制 **26.1MB**；首屏可交互 p95 **734ms** | ✅ |
| P37 交互延迟 | 树层展开 ≤200ms P95（10k 节点） | 根层 p95 **144ms** / 二级链层 p95 **75ms**（10,291 节点实例） | ✅ |
| P38 零服务端开销 | 服务端 P95 偏差 <10%（M7 基线 6.8ms 匿名）；管理面首屏请求 ≤ 现役+2 | 匿名 SPA GET 空载 p50 0.51ms / **p95 0.606ms** / p99 0.67ms（n=300，远优于基线，无回退；并跑期 24ms 为 CPU 争用噪声）；审计页首屏 4 请求。**扇出面两处登记**（QA-4）：repos 列表「已用」列 per-repo `GET /v1/storage/usage/{key}`（N 仓=N 请求，本实例 ~170；T-99 列表端点无 usage 的契约缺口的权宜，页面注释已声明）；users 页 per-user 详情 N+1（T-237 契约缺口③，无汇总端点） | ✅（时延）/ ⚠️（扇出两条登记） |
| P38/76-AC4 axe | 全页 serious=0 | 20 页 × 双主题：**38/40 净；2 页各 1 serious**（QA-1/QA-2） | ❌（2 缺陷） |
| S45 前端注入面 | 特殊字符路径不逃逸 | U20 矩阵三姿势全绿（下）；React 转义 + 部署对话框编码回显断言 + 树按原名呈现（服务端原值不二次编解码） | ✅ |
| S46 资源自包含 | 无外链 CDN/字体/脚本；CSP 基线维持 | src 扫描唯一命中 = remote 仓 URL 表单 placeholder（非加载资源）；SPA 只引用 `/binflow/assets/*`（immutable 缓存头在）；CSP 头 m7-done 与 HEAD **同为无**（基线维持） | ✅ |
| S47 权限呈现不扩大 | 深链仍 403 收敛 | shell/governance/users/perms 各 spec 的 plain-user 深链 L2/L3 腿 + 写重放服务端 403 腿全绿 | ✅ |

## 四、回归硬门槛

| 门槛 | 证据 | 结果 |
|---|---|---|
| 全量 e2e `--workers=1` | **152 tests → 148 passed / 0 failed / 4 skipped（6.6m，exit 0）**。首轮 2 failed 为**测试环境变量缺口非产品缺陷**：`t146 G19b-1` 默认 `ADMIN_PW=docsqa-test-pw-146`（M4 期 QA 脚本约定）、`t104 P20` 需 `BASE` env（否则 about:blank origin → Invalid URL）；带 `BASE=http://127.0.0.1:8080 ADMIN_PW=password` 复跑全绿（两腿单独复跑亦绿）。**环境注记**：全量跑需导出 `BASE`+`ADMIN_PW`（chore 候选，见 QA-5） | ✅ |
| M1~M7 契约终审 | `git diff m7-done..HEAD -- internal/ cmd/` = **31 文件 +1385/−218**，逐文件归属：`684e71c`(T-231)×6 + `b3b3c06`(T-233)×20 + `5e3f3a4`(T-249)×5；`internal/client/client.go` 由 T-231+T-233 双触（均豁免）。**三张裁定豁免票之外零改动** | ✅ |
| 锚对账器终态 | `anchor-audit.mjs`：src **423** 家族（493 落点）/ 册 483 / spec 引用 372；**unregistered=0、broken=0**（dead 112 家族为 §10.6 登记态；retired 均有显式条目）——与 T-244 报告 423/0/0 一致 | ✅ |
| 续传双臂 | `m7-resume-probe.sh --stop kill` **GREEN**（Range 0-524287 续块、PUT 201、blob 逐位一致）+ `--stop sigterm` **GREEN** | ✅ |
| RBAC 矩阵 | `m7-rbac-matrix.sh --expect`：R01~R12/W01~W07 全列符合 PRD 目标表，**0 deviations**；零副作用守卫 OK（m7bob/仓配置/组/create-arm 仓四点不变） | ✅ |
| Go 全量 race | `make test`（TEST_TIMEOUT=20m）**exit 0、全包 ok、零 FAIL**（migrate 11.3s / remote 172s / replication 106s / repo 218s / storage 185s…） | ✅ |
| lint/fmt | `make lint` **0 issues**；`gofmt -l cmd internal` 空 | ✅ |

## 五、FR-77 债券六项复核（DoD-7）

| AC | 复核 | 结果 |
|---|---|---|
| AC1 T-231 矩阵 5×2 | **curl 臂 5/5**（`a%25b`/`a%23b`/`a%3Fb`/`a%20b`/UTF-8 上传 201 + 下载 200 + `cmp` 逐字节一致 + sha256 与 storage API 一致）；**bf CLI 臂 5/5**（原始文件名直传 rc=0，打印 URI 可复制可回放——两枚打印 URI 下载 cmp 一致）；UI 臂 = 套件部署对话框特殊文件名腿。**勘误登记**：PRD §5.4 U20 骨架命令对 `%`/空格腿照抄会在 curl 侧自伤（字面 `%b` 非法转义→400、空格 curl 拒发 000）——正确姿势为预编码 URL（服务器行为正确） | ✅ |
| AC2 --skip-users | Go 测试族在（`migrate_test.go:1320+`：403→告警续迁/500 fail-closed/无旗标零回归），全量 race 绿；T-233 已收敛 | ✅ |
| AC3 CI -timeout 20m | Makefile `TEST_TIMEOUT ?= 20m` + ci.yml 显式传参；本地 `make test` 全绿实证；CI 面证据 = T-248 Jenkins 三级（nightly 全仓 race 15.5m@2C 绿） | ✅ |
| AC4 V28 附录 | `docs/user/admin/real-env-appendix.md` §V28 实录在位（7.84.10+PG、执行票 T-228、差异清单 R-1/R-2/R-7），与 faq.md / migrate-artifactory.md 交叉链接 + `reports/agents/T-228-qa.md` 归档互链 | ✅ |
| AC5 dialer 样板 | 命名形样板 grep = **0**；未具名形全仓 2 处 = 两构造本体（合规口径） | ✅ |
| AC6 UI 打磨 4 条 | 随域票收口（Q6 终裁并入域票 AC；T-236~T-244 各票日志），全量套件绿承载 | ✅ |

## 六、DoD 八条判定（PRD §9）

| # | DoD | 判定 | 证据 |
|---|---|---|---|
| 1 | §4 全部 P0 AC 全绿；P1 全绿 | **P0 ✅ / P1 一处 ❌** | P0（FR-71/72/73）：本报告 §一 U01~U09/U11/U12 全绿 + 各域 spec；P1：FR-74/75/77 绿（FR-75 键盘 6 腿 + 批量腿在套件），**FR-76 AC4 axe 全页 2 serious（QA-1/QA-2）→ 该 AC 未达** |
| 2 | §8 剧本全绿（含 8 零学习成本）；§5.3 分级矩阵 P0/P1 全过 | **剧本 ✅ / 矩阵一处 ❌** | §二 8/8 零卡壳（最慢 1.68s）；§5.3 各面：IA/交互 Playwright ✅、视觉行为规格核对 ✅（零复制扫描三件套 + 组件清单由各域票 review 承载）、服务端零改动 ✅、W 锚 ✅、债务包 ✅、真实 docker 腿 ✅；**axe 面同 DoD-1 缺口** |
| 3 | 回归硬门槛（M1~M7 P0 复跑 / W 锚 / git diff 审计） | ✅ | §四全绿：e2e 148/0、31 文件全溯源三豁免票、锚 0/0、续传双臂、RBAC 0 偏差、Go race 全绿 |
| 4 | 前置产物（ui-console 冻结 / ADR Accepted / console-ux v2.0） | ✅（带版本号登记） | `docs/reverse/console-ui.md` 冻结；ADR-0029 **Accepted**（2026-08-23，含 Q1~Q6 终裁）；console-ux 实际 **v1.7**（v1.4 M8 路由重排 + v1.7 锚册回写收 T-243 D-1~D-4）——PRD 字面「v2.0」未发生版本号跳变，内容以增补版式承载，记**版本口径分歧**交 conductor 备案 |
| 5 | tech-writer 改版 + 对照表 | ✅ | T-245（`a88b728`）：`docs/user/console.md` 新 IA 重写 + `artifactory-path-map.md`（24 任务两列）+ 七页路径修订 + 三向交叉链接 |
| 6 | NFR-P36 归档；lint/test/CI 绿；lint 0 / gofmt 空 | ✅（附注） | gzip 188.1KB 归档（本报告 §三）；`make test` exit 0 / `make lint` 0 issues / gofmt 空；CI = Jenkins 三级绿（T-248 `8eb207f`）；GitHub Actions 链接本机 gh CLI 不可用，以 T-248 报告 + workflow 文件（`make test TEST_TIMEOUT=20m`）承载 |
| 7 | FR-77 六项全收口 | ✅ | §五逐项复核全绿（含 5×2 矩阵三姿势复验 + 样板 grep=0 + V28 附录在位） |
| 8 | 主会话 git tag `m8-done` | **就绪待执行** | 证据总账即本报告；tag 打与推送归 conductor（推送须用户授权） |

## 七、缺陷清单

| # | 级 | 摘要 | 复现/证据 | 建议 |
|---|---|---|---|---|
| QA-1 | P2 | 仓库详情页 `<pre>` 滚动区无键盘访问（axe `scrollable-region-focusable` serious，双主题复现） | `/binflow/ui/admin/repositories/m8-perf-local`（内容溢出时）；target=`pre`（repo 详情自有 pre；对照：SetMeUp CmdBlock 的 pre 已带 tabindex=0） | 该 pre 补 `tabindex=0`（或滚动容器统一走共享件） |
| QA-2 | P2 | GC 页 field-hint 内链接仅靠颜色区分（axe `link-in-text-block` serious，双主题复现） | `/binflow/ui/admin/governance/gc`；target=`.field-hint > a`（「审计日志」链接） | hint 内链接加下划线/等价区分（CSS 一行） |
| QA-3 | P3 | 树「过滤当前层」输入词跨层残留（下钻后 0 显示需手动清空） | 剧本③观察甲 | 下钻时过滤词复位或清空提示（UX 候选） |
| QA-4 | P3 | 管理面扇出两处：repos 列表「已用」列 per-repo usage（N 仓=N 请求，实测 ~170）；users 页 per-user 详情 N+1 | `[t246-perf] fanout` 采集；均为已登记契约缺口的权宜（T-99 / T-237 缺口③） | M9 端点候选：列表带 usage / 用户列表带组汇总（或批量 usage） |
| QA-5 | P3 | e2e 全量跑需 `BASE`+`ADMIN_PW` 环境变量（t104 P20 / t146 G19b-1 缺省不自洽：P20 about:blank origin 抛 Invalid URL；G19b-1 默认口令是 M4 期 QA 脚本口令） | 本报告 §四首尾两轮对照 | chore：两 spec 缺省对齐 harness 约定（BASE 默认 127.0.0.1:8080 / ADMIN_PW 默认 password），或 README 注明必导出变量 |

无 P0/P1 缺陷。QA-1/QA-2 为 DoD-1/DoD-2 的唯一堵点（各一行修复量级，可 fix-forward 后补跑 axe 面）。

## 八、M9 候选债全量汇总（收官材料）

实现票登记（源：reports/agents/T-23x~T-24x）：
1. 旧路由 redirect 移除（Q3：删 8 条路由 + shell 映射腿）〔T-235〕
2. 共享层小票余项：T-237/T-241 页面组自持对比度类回退语义 badge；repos-deploy 入口 readonly 预收敛统一〔T-244〕（T-250 主体六债已由 T-244 收口）
3. `internal/adapter/npm/packument.go` 第三份逐段转义同构体收敛至 `client.EscapePathSegments`〔T-233〕
4. remote singleflight 负载敏感观察〔T-233〕
5. npm deprecate 严格侧产品裁量（write-only 可否涵盖 deprecated 字段增删）〔T-249〕
6. `replication/push_npm.go` 非 admin principal 覆写臂自查〔T-249〕
7. T-247 文档措辞更新（npm CI principal 需 write 即可）〔T-249→tech-writer〕
8. m-holder 覆盖集过滤列表端点（CapSecurityRead）或 L2 定案回写 §7.10〔T-241〕
9. 用户域契约缺口四条：GET users enabled 回显 / DELETE users/{name} / 组成员汇总端点 / 组 adminPrivileges〔T-237〕
10. repos 列表/详情「制品/缓存」「更新时间」列（等 E-04 回显体扩字段）〔T-240〕
11. assert-tokens 扩展扫描 pages/**/**.css（编译期断言覆盖页面级 css）〔T-239〕
12. 顶栏搜索框升真输入框（现按钮 + /search autoFocus）〔T-239〕
13. 搜索漏斗三过滤/类型下拉/类型化结果列（R2 契约后回填）〔T-239〕
14. OIDC 腿 step-up（`step_up_grant` 回跳续铸，等 SSO 消费面）〔T-242〕
15. Tokens 页铸币路径联动（R6 落地票）〔T-242〕
16. 树全量 spacer 窗口化虚拟滚动（万级单层再升级；现 10k 达标）〔T-236〕
17. 锚册家族口径（423）与旧落点口径（293/242）统一；§10.6 守卫规矩入票 AC 模板；死锚 112 家族两大版本退役流程〔T-244/console-ux §10.6〕
18. gc `graceHours=0` 并行竞态根治（现 --workers=1 兜底）〔T-232〕
19. 存储 migration pass-gate 501 行（throwaway 配置无 dual-write，架构事实登记）〔RBAC 矩阵口径〕
20. 用户实例 18080 为 M7 期构建（指纹核验）——M8 收官后提议刷新〔T-245 发现〕
21. 本票新增：QA-1/QA-2 axe 两处（一行级）；QA-3 过滤复位；QA-4 扇出两处端点候选；QA-5 e2e 环境变量对齐 chore；PRD U20 骨架命令勘误（% 与空格腿需预编码姿势）；console-ux「v2.0」版本口径分歧备案；UI-05 列排序规格分歧备案（console-m8 §6.3 收窄）。

## 九、结论

**PASS（带 2 处 P2 a11y 缺陷收尾建议）**——M8 六段终验：U01~U15 除 axe 面外全绿；零学习成本剧本 8/8 零卡壳；NFR 三条 + 安全三条全达（P38 扇出两条登记）；回归硬门槛全绿（e2e 148/0、契约 31 文件零违约、锚 423/0/0、续传双臂、RBAC 0 偏差、race 全绿）；DoD 八条中 1/2 受 QA-1/QA-2 堵（一行级修复），4 有版本口径备案，其余全绿；m8-done tag 证据就绪。建议：fix-forward QA-1/QA-2 后补跑 axe 面即满 DoD；或 conductor 裁定两处 a11y 记 M9 微票后打 tag。

— 清单：被测实例（8080/18081/18082 + docker 容器 18083）已停，scratch 数据目录与临时 spec 已删，docker 凭据已 logout、本地 tag 已清；用户实例 18080 全程未触碰（PID 36174 前后一致）。
