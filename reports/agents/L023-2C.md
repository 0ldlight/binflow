# L023-2C — 批删 + retention 对位（§11.10 票 C，build 域实现票收官）+ ADR-0045 Errata 二③随票收口

Ticket:       L023-2C（LOOP 023 批次 1 票 C：批删两出口 + retention 两遍删序/豁免/时机）P0；**追加两条**（conductor 派单后补）：①ADR-0045 Errata 二③ 翻正落地（promote build 侧门 r→w）+ 清 ④㋔ 陈注；②Errata 开放项① 的 commit 锚回填登记。
Role:         dev-go-core（area = internal/build + internal/httpapi）
Area:         与 L023-2A/2B 同域（internal/build）
Input:        docs/reverse/build-info.md §1（retention 行）/§2.5（retention 形态）/§11.1（E6/E7/E9/E10）/§11.6（retention 深规格：count 门/两遍删序/promoted+pinned 豁免/async 缺省同步/发布尾部触发）/§11.7（批删集合语义：body 6 字段/特殊字符号/E6 文案族/404 两分支）；L023-1 活体探针（count=0 400 逐字/count=1 删 2 留 3+promoted 幸存/E7 即删性）；conductor 追加指令（ADR-0045 Errata 二③）
Changes:      按序：
              1) **批删 POST /api/build/delete**（§11.7/E10）：路由 + handler——body 六字段 {buildRepo, project, buildName, buildNumbers[], deleteArtifacts, deleteAll}；与 DELETE 路径面**同一 DeleteRuns command 全同语义**（E6 双段文案/404 两分支/同号至多删一个/deleteAll 无尾点）；数组形态承接 CSV 装不下的特殊字符号；blank buildName → 400 逐字 `Please state the name of the build to be removed`（此 400 仅 body 面可达——路径面语法上非空，票 B 登记项落位）；非 deleteAll 无号 → 400 逐字 `Please provide at least one build number to delete`；?project= 族拒收姿态与全族一致。
              2) **retention 时机与门**（§11.6-1/2 + E7/E9）：缺省 **async=false 同步**（原 true 作废——E9 源码口径）；成功响应 **204**（原 200 作废）；body 缺失或显式 count≤0 → 400 **text/plain** 逐字 `Max count retention needs to be a positive number`（服务侧 WireError 导出 + handler text 渲染）；count 缺省（body 内无该键）= -1 不启用 count 维度（*int 三态建模）；执行出错 → 400 text/plain 逐字 `Errors have occurred while maintaining build retention. Please review the logs for further information.`
              3) **两遍删序 + 豁免**（§11.6-4/5）：PrepareRetention 重写为两遍——第一遍日期（started 日期严格早于 minimumBuildDate 日期者删，日期粒度取 UTC 帧〔canonical 存储〕）；第二遍 count（**幸存者**非豁免集按 started 倒序保最新 N、其余删——日期遍受害者不占 count 算术，修正原单遍混算）；豁免两源共用：buildNumbersNotToBeDiscarded + **promoted 豁免**（promotion 历史非空〔status 非空白〕的 run 永不被 retention 删且不占 count 名额——新增，活体钉死）；保留数可因此少于 count。
              4) **发布尾部触发**（§11.6-8）：Info 增 buildRetention 块（*RetentionRequest），PUT 上传携带时在发布尾部（audit/webhook 之后）同步执行本窗口——best-effort（发布已成立，窗口故障仅 slog warn，不以失败 PUT 覆盖已落数据）。
              5) **jf 真腿驱动的 wire 容错**：jf rt build-discard 发送**字符串形态** `"count":"1"`（参照 Jackson 会强转）——RetentionRequest 增自定义 UnmarshalJSON（数字/数字串双收，非数字串 400）。
              6) **追加①：promote build 侧门 r→w**（ADR-0045 Errata 二③/§11.5-1）：CanRead 改 allow(w)，403 文案随之 access/Read → **upload/Upload**（§7 upload 句）；门梯 doc 注释同步改写并清除仍引已作废 ④㋔ upfront 门的陈注（票 B 已改为逐项 annotate warning）。
              7) **追加②：Errata 开放项① commit 锚**——本票代码未 commit（纪律）；conductor 封票提交后的 commit 号即 DECISIONS.md 开放项①的回填锚，本报告留位，我不改 DECISIONS.md。
Files:        新增：internal/httpapi/build_batch_delete_retention_semantics_test.go（五用例：批删 body 孪生+特殊字符号/count 门逐字 text/plain/两遍删序+promoted 豁免/发布尾部触发/async 臂）。
              修改：internal/build/promote.go（RetentionRequest *int+UnmarshalJSON+两遍删序+豁免+Execute 故障文案载体；promote 门 r→w）；internal/build/upload.go（buildRetention 块+runRetentionTail+WireError 导出注释）；internal/httpapi/build.go（retention handler 重写：同步缺省/204/count 门 text 渲染/执行故障逐字；handleBuildBatchDelete 新面）；internal/httpapi/router.go（build/delete POST 路由）。
              测试对位：internal/build/promote_test.go（retCount 助手+*int 字面量；retention 204 期望；wenda 权限面加 build 侧 w——追加①的联动）；internal/build/emit_test.go（retCount）；internal/httpapi/build_promote_test.go（sync 204+async 显式臂）；internal/httpapi/build_webhook_test.go（retention 204+臂序重排——promote 臂移到窗口之后，§11.6-5 豁免使被 promote 的 run 不再被窗口删）。
              删除：无。
Tests:        新增五用例：批删孪生（E6 双段整串逐字含 `1.0.0-rc+1` CSV-敌对号/404 两分支/blank-name 400 逐字〔body 面独有臂〕/无号 400/deleteAll 清场 404）；count 门（count=0 与 body 缺失同句逐字 text/plain；count 缺省 floor-only 合法 204）；两遍删序+豁免（五 run 夹具：floor 外最新者不占 count 槽〔单遍混算会误删的分歧用例〕+promoted 豁免幸存）；发布尾部触发（PUT 带 buildRetention 后号单立即可见旧 run 消失）；async=true 分离臂（204 即答+轮询收敛）。存量 retention/webhook 族全部对位后绿。
Commands:     go build ./... && go vet ./...；gofmt -l（空）；make lint（0 issues）；go test ./internal/build/ ./internal/metadata/ -count=1；go test ./internal/httpapi/ -count=1（全量 234.2s，最终代码）；go test -race ./internal/build/ -count=1（98.2s）；go test -race ./internal/httpapi/ -count=1 -run 'TestBatchDelete|TestRetention|TestBuildRESTRetention|TestBuildWebhook'（42.3s）；metadata 本票零生产改动（票 B 的 -race 188.1s 仍有效）；冒烟：隔离实例 127.0.0.1:18097（独立 data-dir，零 docker 零 172.16.58.130，用后清除）curl 六臂 + jf CLI 2.122.0 build-discard 真腿。
Outputs:      四门全绿。冒烟（§11.6/§11.7 逐断言）：C1 count=0 → `Max count retention needs to be a positive number` **400 text/plain** 逐字；C3 count=1 同步（无 async 参数）→ 204，幸存 `['/3','/2']`——promoted run 2 豁免幸存、最新 3 占 count 槽、最旧 1 删除（豁免臂钉死）；C4 批删 body 形态 `{"buildName":"probe-c","buildNumbers":["1.0.0-rc+1","99"]}` → 200 E6 双段逐字（特殊字符号经数组落删）；C5 发布尾部触发：PUT 带 `"buildRetention":{"count":1}` → 204 后号单立即 `['/5','/2']`（新 run 占槽、promoted 幸存、中间 run 已被尾部窗口删除——E7 即删性）；C6 deleteAll 无尾点文案 + 清场后 `404 No builds were found`。**jf 真腿**：`jf rt build-discard jf-ret --max-builds=1`（2.122.0）→ CLI `Builds discarded.`、3 run → 幸存 `['/3']`——**真客户端暴露的 `"count":"1"` 字符串形态**已由容错 UnmarshalJSON 承接（首轮严格 *int 400 的现场留痕于本票过程，修复后复验通过）。
Compatibility: 契约对照：E6 双段文案（路径面+body 面同 command 同文案——E10「与 DELETE 族共用同一删除 command」钉死）；E7 即删性（同步缺省+发布尾部触发双臂落位）；E9 async 缺省同步（JAX-RS 原语口径，对外可观察差异面仅时序——功能面零分歧）；count 正数门 400 text/plain 逐字；promoted+pinned 豁免与不占名额；两遍删序独立算术。**登记**：① 派单提及的 buildDays/buildsPerDay **不在本域规格**（§2.5 body 四字段高置信双源）——按「UNKNOWN 不猜」未实现，建议转 reverse-engineer 补规格票（官方新版 API 或有该字段族）；② minimumBuildDate 日期粒度的时区帧（「本地日期」）按 canonical UTC 帧实现（BinFlow 存储帧），±1 日边界与源码本地帧可能有差——低风险登记；③ 执行故障 400 文案取 §11.6-2 error 臂逐字（warning 同构句未落——无对应分支面）；④ retention 404 文案未逐字（规格未载，writeBuildError 兜底 `Build-Info not found`）；⑤ **追加①已落地**：promote build 侧门 = w（§11.5-1/Errata 二③），403 = §7 upload 句——ADR-0045 决议点 5 的 r 臂正式翻正，实现与规格一致；⑥ **追加②**：DECISIONS.md 开放项① 的 commit 锚 = 本票收口提交号（conductor 封票时回填，我未触碰 DECISIONS.md）。
Security:     批删 body 面与路径面共用 d 门（无 oracle 姿势不变）；count 门先于存在查找；发布尾部窗口 best-effort 不放大权限面（PrepareRetention 内仍有 d 门+404 梯）；错误文案内插（name/numbers）经 text/plain+nosniff；jf 口令仅 shell 传递；smoke 实例与临时文件用后清除。
Performance:  无新测量面。promoted 豁免按 run 一次 ListPromotions 索引查询（O(runs)，CI 元数据量级；promotions 表有 (build 坐标, promoted_at) 索引）；两遍删序单次 ListBuildNumbers 内存遍历；发布尾部触发仅当文档携带 buildRetention 块时执行（零块零开销）。其余无影响。
Risks:        ① 派单/规格错配项（buildDays/buildsPerDay）需 conductor 裁决是否开规格补票；② 日期粒度时区帧（登记②）契约化前建议边界探针；③ jf 字符串 count 容错是参照 Jackson 行为的合理承接（规格未明载 wire 类型宽容——契约化时以本实现为准登记 golden）；④ promote 门 r→w 后，只读观察者（原可 status-only promote 的 r 持有者）被拒——语义与规格/Errata 一致，但消费方行为面变化（若有 r-only CI 流水线做 status-only promote 需加 w 授权）。
Blockers:     无。
Next:         ① conductor：封票 commit 后回填 DECISIONS.md 开放项①锚 + 建议翻 matrix D07 全 11 行状态（A/B/C 三票后 build 域 REST 面全量对位规格）；② compatibility-engineer：D07 契约族 golden 候选=A/B/C 三票冒烟输出合集（204+checksum 头/E5 重复条目/E12 双臂/E6 三文案/count 门 text/403 三句+upload 句）；③ reverse-engineer：buildDays/buildsPerDay 字段族规格补票（若官方新版 API 确载）；④ postgres 025 补齐小票（票 B 登记）；⑤ buildDays 之外留待验证面（§9 #9 patternArtifacts、#5 diff 响应形态）维持面外登记。
