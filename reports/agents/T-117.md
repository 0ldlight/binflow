# T-117 [P1] PRD v1.2→v1.3 勘误（T-103 QA E1~E5 + T-100 review ④ AC 口径回写）

- role: product-manager
- 日期: 2026-08-21
- 产出: docs/prd/milestone-4.md **v1.2 → v1.3**（§0 修订记录 + 正文 14 处落点）
- 输入: reports/agents/T-103-qa.md §4（E1~E5）、reports/agents/T-100-review.md non-blocking ④、docs/prd/milestone-4.md v1.2、BOARD.md（只读）

## 1. 勘误前后对照（六项）

### E1 — W01 挂载点断言健壮化（§5.4）

| | 内容 |
|---|---|
| 前 | `curl -s $UI/ | grep -c 'id="root"'   # 1（SPA 挂载点）` |
| 后 | `curl -s $UI/ | grep -q 'id="root"' && echo MOUNT_OK   # 挂载点存在（真构建 shell 含 2 处 id="root"，计数断言不作口径——E1/v1.3）` |
| 依据 | T-103 实测：真 SPA 构建产物 shell 含 2 处 `id="root"`（占位 shell 为 1）——计数断言在真构建上必 FAIL，断言意图是挂载点存在性 |

### E2 — 未认证（匿名关）search 姿态钉死：**403**（FR-26 + SR-01/02 + W16 剧本）

| | 内容 |
|---|---|
| 前 | PRD 未钉（T-92 review NB4 挂「PRD 半句」；QA 观察 O-3：search 403 vs 同域 audit/repositories/内容路径 401 姿态不一致） |
| 后 | FR-26 新增行为规格 bullet：未认证 → **403（E-01 信封，无挑战头）**；已认证零 read 授权 → **200 空结果集（`results:[]`）**；SR-01/02 矩阵行同步；§5.4 W16 块补一行可执行断言 |
| 裁决 | **QA「统一 401+挑战」建议不采纳**。核实现状（三层证据）：① 实现——`internal/httpapi/router.go` search 两路由门开放（`routeAuth{}`，镜像 /api/storage 读姿态）+ `internal/repo/search.go searchGate` 匿名关拒绝映射 403，`t92_search_test.go` 已钉死「anonymous callers meet the closed-instance 403」；② 规格——`docs/reverse/rest-api.md` §4 明文「未认证 → 403」，**高置信度**（`ChecksumSearchResource` + 官方文档 searchChecksum 同口径）；§3 `?list`「匿名 → 403」同族先例。③ 语义分层——401+挑战保留给管理面（`/api/v1/**`、`/api/repositories`、`/api/security/**`）与内容面下载（rest-api §1.4「403 且匿名 → 401 挑战」升级规则的面）；search 与 `?list` 是规格明文的 403 族，改 401 反而偏离 Artifactory。**现状即正确，零改码**，QA 观察 O-3 的「不一致」实为「规格对齐结果」。已认证零授权 → 200 空集为 `filterVisible` 逐 node ACL 过滤的自然结果（T-92 NB4 半句收口） |

### E3 — FR-31 配额拒绝语义定案：**按每写原子**（5 处落点）

| | 内容 |
|---|---|
| 前 | 行为规格「**原子拒绝**（无部分写入：路径 404、stats 不变、docker 上传会话可弃）」；AC3「docker push 超限 → 客户端报错且 manifest/blob 零残留」；W26c 注释「+ 零残留」；GE-05「原子拒绝」；§1.2 门槛「无部分写入」 |
| 后 | 统一为「**按每写原子拒绝**」：被拒的那一次写本身原子（被拒路径 404 / stats 不变 / docker 上传会话可弃 / **manifest 不落** / 被拒 blob 零残留）；多部件客户端会话中**先成功且配额内的写按每写语义保留**——docker 多层 push 先落层（含 cross-repo mount 的 config 层）与 maven 先落 pom/maven-metadata 即此形态（T-103 **D-1/D-2，P2 接受**：无 manifest/jar 引用、内容 API 面不可达，仅 usage 计量可见；「无 manifest 引用的 docker blob node 纳入 GC 候选」列 **M5+ 评估项**）。AC3 改为「被拒写原子：manifest 不落、被拒 blob 零残留、catalog/tags 不可见」；W26c 注释拆四行写明断言口径；npm/pypi 维持「零残留」（其发布单写原子成立） |
| 依据 | T-103 D-1/D-2：docker/maven 配额拒绝后「先落者残留」是协议会话结构（多层/多件）的固有形态，BinFlow 配额执行点在每写（T-95），非会话级预留；QA 建议方案 A（接受并注记）|

### E4 — cookie `Path=/binflow` 作用域排除 /v2 注记（FR-23 + CE-03）

| | 内容 |
|---|---|
| 前 | FR-23 只说「session cookie 与 Basic/Token 是等价认证凭据（内容路径与管理面同用）」，未提 /v2 |
| 后 | FR-23 补**作用域注记**：cookie `Path=/binflow` **结构性排除根级 `/v2`**（ADR-0010 例外路由）——docker 域（`/v2/token`、manifest/blob）不消费 session cookie，docker 客户端登录走 `/v2/token` 换 Bearer；控制台 docker 视图取数同须 Bearer（ux R10 等价性示例改道 npm packument / pypi simple）；机制等价 QA 已实证（强制携带 Cookie 访问 `/v2/tags/list` → 200，session 主体在 /v2 面生效）。CE-03 矩阵行补半句指引 |
| 依据 | T-103 E4 + 附带矩阵（cookie 对 generic/npm packument/pypi simple/管理面全 200，/v2 面 401 属 Path 作用域，强制携带则 200）；T-100 已按 /v2/token 取数（BOARD 在案） |

### E5 — W31「为空」放行锁文件残留（FR-32 + W31 + AC4）

| | 内容 |
|---|---|
| 前 | W31 注释「import 退出码非 0 且 /tmp/fresh2 为空」；FR-32 规格/AC4「目标目录清回空（无半恢复）」未提锁文件 |
| 后 | W31 改「无数据残留（0 字节 `.maintenance.lock` 锁文件残留属 fail-fast 设计内——import 整程持锁**先于**空目录检查，非恢复态）」；FR-32 规格与 AC4 注明「无半恢复 = **数据零残留**；锁文件残留属锁协议常态非恢复态」 |
| 依据 | T-103 O-5：三种失败路径目标目录均残留 0 字节锁文件、数据零残留；QA 建议方案 A（措辞放宽）。实现侧不追改（锁先于空检查是 T-96 B1 修复的既定次序，清锁反而引入新窗口） |

### AC①（T-100 review ④）— 树表 createdBy 列改「详情面板呈现」口径（FR-25 + AC1）

| | 内容 |
|---|---|
| 前 | FR-25「目录/文件图标区分、size/lastModified 列」未钉列集边界；AC1「（size/时间列非空）」——T-88 票面 AC① 按字面可读出「树表 createdBy 列」 |
| 后 | FR-25 钉死：**树表列集不含 `createdBy`/`modifiedBy`**（children 列表与 `?list` 契约均无主体字段，契约上不可得）——主体元数据归**详情面板**呈现（item info 单查 E-09 含 `createdBy`）；AC1 补「树表无 createdBy 列——主体字段归详情面板呈现」；两处均标注「T-100 ④ / v1.3，T-104 不得按树表列断言 createdBy」 |
| 依据 | T-100 review non-blocking ④（FE 已转详情面板呈现，防 T-104 按票面 AC 字面断言树表列误判） |

## 2. 落点清单（14 处）

| 位置 | 勘误 |
|---|---|
| 文档头「状态」 | v1.3 摘要（六项一览） |
| §0 修订记录 | v1.3 行（全量变更说明） |
| §1.2 量化门槛·配额行 | E3 |
| FR-23 登录会话 bullet | E4（作用域注记） |
| FR-25 树浏览 bullet + AC1 | AC① |
| FR-26 行为规格（新增 bullet） | E2 |
| FR-31 执行点 bullet + AC3 | E3 |
| FR-32 import bullet + AC4 | E5 |
| §5.2 CE-03 / SR-01 / SR-02 / GE-05 | E4 / E2 / E2 / E3 |
| §5.4 W01 / W16 块 / W26c 注释 / W31 注释 | E1 / E2 / E3 / E5 |

## 3. grep 自检（命令与输出）

```bash
# ① 旧表述零活体（四组旧串）
$ rg -n "grep -c 'id=\"root\"'|/tmp/fresh2 为空|manifest/blob 零残留|原子拒绝（无部分写入|零残留；npm" docs/prd/milestone-4.md
20:grep -c 'id="root"'
20:/tmp/fresh2 为空
# 仅第 20 行（v1.3 修订记录「引旧文以勘误」的历史引用，T-112 先例口径）；
# 「manifest/blob 零残留」「原子拒绝（无部分写入」全文 0 命中

# ② 「零残留」/「为空」残留语义复核
$ rg -n -o ".{18}零残留.{0,14}|.{10}为空.{0,10}" docs/prd/milestone-4.md
# 全部命中为：E3 新口径（被拒 blob 零残留 / npm/pypi 零残留 / 数据零残留）、
# FR-32 import 前置条件「目标 data dir 必须为空」（有意保留，与 W31 断言不同契约）、
# 头部/修订记录行的 quote-then-revoke

# ③ 新口径落点
$ rg -c "v1\.3" docs/prd/milestone-4.md      # 16 处（状态头/修订记录/正文各标记）
$ rg -n "createdBy" docs/prd/milestone-4.md  # 2 处（FR-25 规格 + AC1）
$ rg -n "未认证·匿名关" docs/prd/milestone-4.md  # W16 新断言行在案
```

## 4. 结论与移交

- 六项勘误全部回写，v1.2 → v1.3；**E2 为裁决项：钉 403、驳回 QA 的 401 建议**（三层证据见 §1，现状即规格、零改码）；其余五项为措辞/注记收口，均不产生后端改动票。
- **T-104 影响**：W01 断言改 `grep -q`；W16 补未认证 403 断言；树表断言不得含 createdBy 列（详情面板口径）。
- **T-105 无影响**（E1~E5 不触性能面）。
- **移交 conductor 两条**（非本票 area，不动他人文件）：
  1. `docs/design/console-ux.md` §9 R10 行（754 行）仍写「packument GET、tags/list GET 均可带 session 凭据」——与 E4 定案相悖（tags/list 在根级 /v2，cookie 结构性不可达），建议 ux-designer 随下版勘误（例已应改道 npm packument / pypi simple）；
  2. E3 附带的「无 manifest 引用 docker blob node 纳入 GC 候选」已列 **M5+ 评估项**（D-1/D-2 的收紧路径），如需立票归 conductor 排期。
