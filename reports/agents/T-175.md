# T-175 QA 报告 — 复制多协议 + Prometheus 指标 + bf CLI + bf-migrate 集成验收

- 票据：T-175 [P1] 集成 QA（复制+Prometheus+CLI+bf-migrate）
- Agent：qa-engineer；日期：2026-08-22
- 被测物：当前工作树源码（HEAD 00f73e7 + 在途改动，含 T-180/189/191/185/186/190 合入）自建二进制
- 结论：**FAIL**（29 项 H 序列：17 PASS / 4 PARTIAL / 7 FAIL / 1 阻塞腿；6 项 P1 缺陷，集中在复制协议面与迁移制品面）

## 0. 环境与方法

- 双实例：宿主 `binflow-server`（127.0.0.1:18101 = 源 A，:18102 = 目标 B，:18103 = 指标认证腿 C，:18105 = 迁移空目标 D），各自独立 data_dir；`BINFLOW_REMOTE_CREDENTIALS_KEY`（base64×32B）用于复制凭据封存。
- docker 腿：本机 daemon 在 VM 内（127.0.0.1 不可直达），按 T-45 先例起容器化实例（发布端口 18111/18112，当前源码构建镜像 `binflow:t175qa`），127.0.0.0/8 在 insecure registries 内。
- 迁移源：**mock Artifactory**（python3，:18104，形状按 docs/reverse/rest-api.md §2 + auth-model.md §1/§3.3；备 120 个制品文件供拉取）。Q9 真实 Artifactory 实例为条件腿，未跑（待用户环境定案）。
- 指标：promtool 经 `docker run --entrypoint promtool prom/prometheus`；另起真 Prometheus server（:18113）抓取验证。
- 控制台：真 Chromium（web/node_modules Playwright）登录后访问复制页。
- 自动化全量：`go test -count=1 ./...` → **23 包 ok、0 FAIL、exit=0**（race 覆盖归各 dev 票与 T-172 基线）。
- 观察 O1（版本注入）：`make build` 产出 dev 版本；`dist/` 内 goreleaser 产物为 8-21 旧物（无 M6 路由，容器首测 404 实锤）。本票 CLI/迁移用 `go build -ldflags "-X main.version=v1.0.0-snapshot.00f73e7 ..."` 手工注入验收。建议 release 面刷新 dist 或 QA 派单注明。
- 清理：本票所起进程/容器/网络/镜像/docker login 凭据全部清除；`/tmp/t173` 下 binflow-server 属 T-172/T-173，未动。

## 1. 复制域（H39~H51）：7 PASS / 2 PARTIAL / 3 FAIL（另 1 条 mvn 真客户端腿阻塞）

口径：Q6/Q7 按 T-162 暂行（PRD v1.1 §7）——replica 只读由「未配写路由的 virtual 门面 + 可写 backing local」实现；冲突=checksum 不一致 → 任务终态 failed、不动目标。REST 面按 T-180 落地四端点 + status 聚合。

前置：A 建 `libs-release`(local/generic)；B 建 `replica-local`(local/generic) + `replica`(virtual, members=[replica-local])；A `POST /binflow/api/v1/replications`（to-b，带 target_password，201，密码不回显）。

| # | 验收标准 | 结果 | 证据（命令+输出摘要） |
|---|---|---|---|
| H39 | push 单向复制 roundtrip | ✅ | A `PUT /binflow/libs-release/org/app/1.0/app-1.0.bin` → 201；2s 后 B `GET /binflow/replica/org/app/1.0/app-1.0.bin` → 200；sha256 `8cc27c…ea5e` 两侧一致 |
| H40 | replica 只读（PUT/DELETE 405） | ✅ | PUT → 405、DELETE → 405（"No local repository was configured as local deployment repository…"，M3 virtual 既定文案；Q6 暂行门面） |
| H41 | 源重启后 pending 重新消费（60s 窗口） | ✅ | B 停机下上传 art-3 → kill -9 A（DB 留 `in_progress,attempts=2`）→ 先起 B 再起 A → 2s 内 B GET 200、sha `da7435…d871` 一致（首轮 drain 恢复） |
| H42 | 目标宕机：上传不受影响 + 恢复后兜底补齐 | ⚠️ 部分 | ①目标停机时 A PUT → 201（0.154s，非阻塞）✅ ②事件最终 failed 且 last_error 带连接拒绝原文 ✅ ③短时宕机（~8s）恢复 → 重试第 5 次成功、sha 一致 ✅ ④**长时间宕机（>31s 烧满 6 次）→ 终态 failed(attempts=6)，B 恢复 + 75s（>1 个 sweep 周期）后 cron 不复活，B 永久 404 ❌ → 缺陷 D1** |
| H43 | 同 sha256 二次上传只传一次 blob | ✅ | 重传后 A 两条任务均 success(attempts=1)；B 访问日志：第二次仅 `HEAD … 200`、**零 PUT**（第一次 HEAD 404→PUT 201 bytes_in=4096） |
| H44 | 事件可按 status 查询 | ⚠️ 部分（暂行口径过） | 字面 `GET /api/v1/replication/events?status=pending` → **404（端点未实现）**；暂行面 `GET /api/v1/replication/status` events[] 逐条带 status（实测捕到 `in_progress,attempts=2`；pending 亦可见）。与 T-180 契约一致，差异已录 D9 |
| H45 | status 聚合端点 | ✅ | 200，targets[]（pending/in_progress/succeeded/failed/skipped/last_success_at）+ events[] newest-first；凭据零泄漏 |
| H46 | 控制台「复制」面板 | ✅ | 真 Chromium 登录后访问 `/binflow/ui/governance/replication`：`repl-page`/`repl-targets`/`repl-events` 全在；目标表 4 行（含状态/上次成功）、事件表 11 行；截图 work/h46-replication-page.png |
| H47 | 冲突不覆盖目标 | ✅（Q7 暂行） | B 侧预置不同 sha（`295e91…d5ff`）→ A 推同路径 → 任务终态 failed，last_error 明示 "conflict: target holds sha256 295e…；source …；target left untouched (Q7 interim)"；B 内容逐字未动。无 `conflict` 状态词（009 闭集），已录 D9 |
| H48 | docker push → 目标 docker pull | ❌ | 容器实例 A2 push `127.0.0.1:18111/docker-release/t175/hello:1` 成功（digest sha256:137cf75…）；3 条 blob 任务入队但全部终态 failed：目标 docker 面对 generic PUT 回 `404 UNSUPPORTED "unknown /v2 route"`；B2 `docker pull` → "not found"。**缺陷 D2**（协议化推送未实现，T-162 遗留⑥预估成真） |
| H49 | maven deploy → 目标 resolve | ✅（curl 等价腿）/ ⛔ mvn 腿 | **mvn 未安装**（真客户端腿 BLOCKED，缺 mvn 3.9.x）。curl 等价：A PUT jar+pom（maven 布局）→ 3 条任务全 success attempts=1（jar/pom/自动生成 maven-metadata.xml）；B GET jar/pom 200，sha `857e4f…683`/`cf8f45…9f7` 逐字一致 |
| H50 | npm publish → 目标 npm install | ❌ | npm 10.x publish `@t175/test-pkg@1.0.1` 到 A 成功；tarball 任务对目标 `PUT → 405 "tarballs are read-only"`、packument 任务 `PUT → 400 "invalid dist-tag body…"`（双终态 failed）；B `npm install --registry=…/npm-replica/` → E404。**缺陷 D3** |
| H51 | pip upload → 目标 pip install | ❌ | twine 等价（curl multipart `:action=file_upload`+name/version/content）上传 wheel 到 A → 200、simple 索引在；**零复制任务入队**（PutLandedBlob 路径未挂钩）；B simple 404；pip 26 `install --index-url=…B…` → "No matching distribution found"。**缺陷 D4** |

补充观察 O4：单 worker 队列背压实测——3 条 docker 失败任务把队列顶住约 90s（31s/任务），与 T-162 设计注记一致；H42④ 的恢复延迟与此同源。

## 2. Prometheus 指标域（H52~H55）：2 PASS / 2 PARTIAL

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| H52 | /metrics 200 + 四类名 + TYPE/HELP | ✅ | 匿名 200，`Content-Type: text/plain; version=0.0.4; charset=utf-8`；`binflow_http_requests_total`(20 series)/`binflow_storage_blobs_total`/`binflow_auth_logins_total`(local=2 实测登录计数) 全在；7 个 family 各带 HELP/TYPE |
| H53 | promtool 通过 + 基数 <100 | ⚠️ 部分 | 真 Prometheus（prom/prometheus:latest，3.x）抓取 `up=1`、25 series 入库 ✅；30 条不同内容路径 + 杂项 API 打流后 path 标签去重 **16 个** « 100 ✅（归一化模板生效）；`promtool check metrics` **可解析但 exit=3**：`binflow_storage_blobs_total` / `binflow_storage_blob_bytes_total` 为 gauge 却带 `_total` 后缀（命名规约 lint；名字承自 PRD FR-61 本身）→ **D5（PRD/ADR 命名校准）** |
| H54 | require_auth 两态 | ✅ | 实例 C（YAML true）：匿名 401 + `WWW-Authenticate: Basic realm="BinFlow Realm"`；admin Basic 200；错口令 401；`BINFLOW_METRICS__REQUIRE_AUTH=false` env 覆盖 → 匿名 200 |
| H55 | 复制指标在场（按实际指标名校验，差异如实记录） | ⚠️ 部分 | `binflow_replication_tasks{status=pending\|in_progress\|success\|failed\|skipped}` 在场且与 sqlite 事实核对一致（failed=4/success=7，逐值相等）；**差异**：PRD 名 `binflow_replication_events_total{target,status}` 与 `…_latency_seconds` 未实现（T-163 遗留①，无 target 维度、无延迟直方图）；「replication.enabled=false → 不暴露」腿不可测——cmd 无条件接线复制（无 enabled 配置门），零配置时 family 仍以预播 0 值在场 → D5/D9 |

## 3. bf CLI 域（H56~H61）：6/6 PASS

隔离：`BF_CONFIG` 指向不存在路径；凭据走 env。二进制为当前源码 + ldflags 注入（见 O1）。

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| H56 | --version 非 dev + --help 四子命令 | ✅ | `bf v1.0.0-snapshot.00f73e7 (00f73e7)`；help 列 repo create/artifact upload/user create/token create |
| H57 | repo create → 服务端 200 | ✅ | `Repository 'test-repo' created.` exit 0；`GET /binflow/api/repositories/test-repo` → 200（T-189 修复后真服务端链通） |
| H58 | artifact upload → sha256+URI 一致 | ✅ | 输出 `sha256: b0966f…57f0` + uri；服务端 GET 200，本地/服务端 sha 逐字一致 |
| H59 | user create → 可登录 | ✅ | `User 'ci-user' created.`；`POST /api/v1/session`（ci-user:pw123）→ 200；非 admin 打 admin 面 → 403（AC 口径）；错口令 → 401 |
| H60 | token create → 可用 | ✅ | 首行即 token 值（+token_id/scope/expires_in 元数据行）；`Authorization: Bearer` → 200；AC 字面 `curl -su admin:$TOKEN` Basic 形亦 200 |
| H61 | --profile staging | ✅ | 双 profile 配置（default→A/staging→B）；`bf --profile staging repo create staging-repo` → B 200 / A 404（寻址正确）；未知 profile → exit 1 列可用名 |

## 4. bf-migrate 域（H62~H67）：2 PASS / 4 FAIL（对 PRD 字面）

方法：mock Artifactory（repos=libs-generic[LOCAL/generic]+docker-prod、users=alice/bob+anonymous+saml 域 svc-cicd、tokens=2 条元数据；另备 120 个制品文件）；目标为真 BinFlow 实例（B=非空 / D=空）。

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| H62 | 非空目标 → 拒绝（exit≠0） | ❌ | 对**非空** B（约 10 个既有仓库）执行 migrate：**exit=0**，照常写入 2 仓 + 2 用户，输出零拒绝语义（grep empty/refus=0）。空实例守卫未实现 → **D6** |
| H63 | generic 仓 100+ 制品迁移 → sha256 一致 | ❌ | 全量迁移后 D 的 `libs-generic` 仓 200 但 **children=[]、制品 0/120**；mock 访问日志证明工具**从未请求**任何文件清单/下载端点（三阶段仅 repos→users→tokens，制品面未实现）→ **D7（票面核心价值缺失）** |
| H64 | migration_report.json 产出 | ❌（字面） | 无 `migration_report.json`；等价物=stdout 摘要 + `.bf-migrate-progress.json`（version/source/target/逐项 done-skipped，0600）。制品成功率统计仅存于 stdout 文本 → **D8** |
| H65 | 迁移用户可登录 | ✅ | `--passwords-out`（0600）生成 24 位口令；alice 用生成口令 `POST /api/v1/session` → 200；错口令 401。注：无「首次登录强制改密」机制，按 PRD 备选臂（admin 持口令可手工重置）成立 |
| H66 | 迁移 Token 可用 | ❌（字面）/设计性偏离 | tokens: found=2 migrated=0 skipped=2 + 明示政策注记 "token values cannot be exported from Artifactory…"。Artifactory 硬约束（值仅创建时返回一次），**不可实现**，需 PRD 改口径 → D9 |
| H67 | --dry-run 零迁移 + 统计一致 | ✅ | dry-run：repos found=2 migrated=2（计数口径）、users found=4（2 迁 2 跳：anonymous/saml 域，理由明示）、tokens found=2 skipped=2，与 mock 源逐值一致；目标 D 仓库列表 `[]`；progress 0 记录；exit 0 |

附带观察：converter 对 docker/maven/npm/pypi 包类型仓是「配置壳 1:1 迁移」（PRD 写 M6 只做 generic）——与 H63 制品缺失叠加后语义易误导读（空 docker 仓出现在目标），随 D7 一并定口径。

## 5. 缺陷清单（建议开票）

| 编号 | 级别 | 域 | 摘要 | 复现（期望 vs 实际） |
|---|---|---|---|---|
| D1 | P1 | replication | 目标长时间宕机（>31s）烧满 6 次退避后任务终态 failed，目标恢复后 cron sweep **不复活**（drain 候选只收 attempts<6），制品永久漏复制 | 停 B → A 上传 → 等 40s（failed/attempts=6）→ 起 B → 等 75s → B GET 404。期望（PRD FR-58/FR-57-AC4）：cron 扫 error 事件重新入队补齐 |
| D2 | P1 | replication×docker | docker push 复制链不通：blob 任务以 generic PUT 打目标 docker 面 → 404 UNSUPPORTED，docker pull 目标 not found | docker push A → 看 A status（3 任务终态 failed，last_error=UNSUPPORTED）+ docker pull B。期望 H48 tag+sha 一致可达 |
| D3 | P1 | replication×npm | npm 复制链不通：目标 npm 面拒收复制传输（tarball PUT 405 "tarballs are read-only"；packument PUT 400 invalid dist-tag） | npm publish A → 任务双 failed；npm install B → E404。期望 H50 install 成功 |
| D4 | P1 | replication×pypi | pypi 上传走 PutLandedBlob 未挂复制钩子，零任务、零复制 | twine 等价上传 A → status 无该 repo 任务；pip install B 失败。期望 H51 install 成功 |
| D5 | P2 | metrics | 命名规约：`binflow_storage_blobs_total`/`…_blob_bytes_total` 为 gauge 带 `_total` 后缀，promtool check exit=3；PRD H55 名（events_total/latency_seconds）未实现（实际 `binflow_replication_tasks{status}`，无 target 维度/延迟） | 见 H53/H55 证据。需 PRD/ADR-0022 命名终裁（改名或接受 lint 豁免） |
| D6 | P1 | bf-migrate | 无「目标空实例」守卫：对非空目标 exit=0 照常写入 | 见 H62。期望 exit≠0 + 错误提示（PRD FR-63-AC1） |
| D7 | P1 | bf-migrate | **制品迁移整体未实现**（0/120；无文件清单/下载/上传阶段） | 见 H63。期望 100+ 制品 sha256 逐条一致（票面 AC ④核心） |
| D8 | P2 | bf-migrate | 无 migration_report.json 产物（仅 stdout 摘要 + progress 文件） | 见 H64 |
| D9 | P2（PRD 校准包） | 跨域 | PRD 字面与实现的既定差异需修订：H44 事件端点形状（实为 status 聚合）、H47 冲突词表（failed 而非 conflict）、H55 指标名、H66 token 不可迁（设计性）、H62 语义；部分已在 PRD v1.1 §7，余量建议 PM 一并收口 | 见各 H 行 |

观察项（不开票）：O1 make build 无版本注入/dist 陈旧；O2 Docker Desktop VM 需容器化实例做 docker 协议腿（文档可收录）；O3 复制引擎无条件接线、无 enabled 配置门（H55 缺席腿不可测）；O4 单 worker 队列背压 ~31s/失败任务；O5 converter 迁 docker 等类型仓壳与 PRD「M6 只做 generic」措辞差。

## 6. 结论

- 复制域：**FAIL**（H48/H50/H51 三协议断链 + H42 长宕机兜底缺口；generic 主链 H39~H41/43/45~H47 全绿，引擎内核质量好）
- Prometheus 域：**PASS（有校准项）**（H53 lint、H55 命名差异需 PM/ADR 终裁，功能面全通）
- bf CLI 域：**PASS 6/6**（真服务端全链，T-189/T-191 修复验证有效）
- bf-migrate 域：**FAIL**（H62 守卫缺失、H63 制品面缺失为核心；H65/H67 过）
- 总判：**FAIL**——D1~D4、D6、D7 六项 P1 需修复或由 PM 裁定降级/改期后再收口。
