# T-390 工作日志 — FR-127 包型图标 30 枚四消费点接线（M14 B5 FE）

- 角色：dev-frontend ｜ 状态：**done**（四闸门绿 + t390 spec 5/5 + m14 16/16 + m10 5/5 + 邻接面 39 绿 + m8 全量 69/0 + a11y-sweep 56 面双主题 serious=0 + SPA +5,725B ≤10KB；服务端 diff=0）
- area：`web/` 独占（无并行 FE 票）。落点：`web/src/assets/pkg-icons/`（30 枚搬运）+ `web/src/components/PkgIcon.tsx|pkg-icon.css`（单点组件）+ 四消费点页面 + `web/e2e/m14/t390-pkg-icons.spec.ts`（新）+ `web/src/styles/tokens.css`（暗色档 token，纯新增）+ `docs/design/brand/package-icons/README.md`（K61 登记，票面明示的回写位）。
- 参数源：K56 生产件（package-icons v1.2：mono/brand × 15 = 13 包型 + trashcan/webhook，npm/go 已 path 化零 `<text>`，conan brand 已 #669ACC）——**逐字节搬运，零改动**。

## 0. 交付形态（单点引用）

- **搬运**：`cp docs/design/brand/package-icons/{mono,brand}/*.svg → web/src/assets/pkg-icons/`（15+15）。全量复核：`grep -l "<text"` 30 枚零命中；mono 15 枚全 currentColor。
- **组件** `PkgIcon.tsx`：`import.meta.glob('…/*.svg', { query: '?raw', import: 'default', eager: true })` × 双目录——资产按名内联成字符串（**换文件/补文件零返工**，且零独立请求，gzip 计入 SPA 预算）。模块加载期剥 XML 注释与 `<title>`（注释是死重；title 会抢可及名，图标全部与可见文字同现 = 装饰位 aria-hidden，`label` prop 留语义位逃生口——本票四消费点均有邻接文字，逃生口零启用）。
- **API**：`<PkgIcon id={PackageType|'trashcan'|'webhook'} variant="brand"|"mono" size={px} className? label?/>`；返回恒为元素（MUI Chip 的 `icon` 槽可直接消费）。命名例外（README §1）：wire `debian` → 资产 `deb.svg`（`ASSET_NAME` 映射）；`go` 同名零映射（注记性例外）。未知运行时 wire 值回退 generic 形（树/列表既有口径）。
- **联合对齐**（AC1）：`lib/repos.ts` PackageType 补 `'helmoci'`（第 13 型，slots.go 早已装配、联合此前缺席——图标键集 Record 完备性所需）。`PACKAGE_TYPES` 常量不动（五核心静态地板语义，SetMeUpDialog 等既有消费方零变化）。

## 1. 四消费点（AC2 逐点）

| # | 消费点 | 形态 | 断言（t390 spec） |
|---|---|---|---|
| ① | pkg-grid（RepositoryFormPage） | 可选磁贴 = **brand 22px**；门控/组合禁用 = **mono + 磁贴 opacity 0.4 + pkg-tier-\* 徽章**（`.pkg-grid-item:disabled` 0.45→0.4）；PKG_ITEMS/GATED_PKG_ICON 的字符族（◈◆◾◇▫/✦）退役 | test 1：五核心 brand + aria-hidden + npm 红方 fill=#CB3837；八门控 mono+0.4+pro 徽章+disabled；mono currentColor 逐值比对；deb↔debian 映射；`svg text`/`svg title` 双零；**T-383 440px 档复证不破** |
| ② | smu-grid（SetMeUpDialog） | 药丸 **brand 18px**；`CLIENT_PKG_META.icon` 字段删除（接口+五条目） | test 2：generic/npm 药丸各恰 1 枚 brand svg、`.pkg-name` innerText 纯 label；**字符图标 grep=0**（`▫⬢⌬⬒⬓` + `◈◆◾◇` + `✦` 对 web/src 全量 grep 退出码 1——注释里的退役注记也改写为文字描述，保证 grep 干净） |
| ③ | 类型列 | 仓库列表 Chip（MUI `icon` 槽，mono 13px）+ 制品树 `.ico`（mono 14px，rclass 字符角标保留——30 枚集不含 rclass 形）；**搜索页豁免**：SearchPage 无类型列（固定类型「制品」单一值不渲染列，页面头注在案——不造假消费点） | test 3：Chip/树两处 `data-variant="mono"` + computed stroke/fill 与宿主 color 逐值相等（currentColor 接线证明） |
| ④ | License 矩阵（LicenseAddonsPage） | 13 包型槽 + trashcan/webhook 两 feature 槽 = **brand 18px**（名称格）；properties/ha/repo-operations/xray-integration 四槽**注记豁免**（不在 30 枚集，不造假图形）；mono trashcan/webhook **注记豁免**（§1 的 mono 定位是列表/表单/树——四消费点中 addon 槽只走 brand 矩阵位） | test 4：13+2 行 brand 在场、deb 映射复证、四豁免行 `.pkg-svg` 计 0 |

30 枚消费对账：13 brand 包型（①）+ 13 mono 包型（①门控）+ brand trashcan/webhook（④）= 28 消费；mono trashcan/webhook 2 枚豁免（注记如上）。

## 2. 暗底抽查与提亮档（AC3 / K61）

- **勘误**：README §6.4 的暗底抽查口径（对背景 `#12161d` 抽 docker/npm/pypi）不成立于**真实宿主面**——brand 图标实际落在 surface-2（磁贴/药丸 `#202834`）、surface-1（矩阵表 `#1a202a`）、surface-3（悬停 `#262f3d`），比背景亮、对比度更低。全 15 枚实算（WCAG 相对亮度，`/tmp` 一次性脚本）：**11 个官方深色 <3:1**。
- **处置**：`tokens.css` 暗色块新增 `--bf-pkgicon-*` 10 枚（webhook 与 generic 同源色共用 token）——保色相抬 HSL 明度，最差宿主 surface-3 实测 ≥3.4（s1/s2 更高）；`pkg-icon.css` 在 `[data-theme='dark']` 下按官方色字面值**定位形件**（属性值子串选择器）替换 stroke/fill，**资产文件零改动**、亮色主题零覆盖（原色在亮底全部 ≥3:1）、mono 不在提亮面（currentColor 自随）。stroke/fill 规则分离（合并声明会把描边件误填色——svg 根 `fill="none"` 是继承值）。
- **量级**：九枚在「+10% 亮度微调」量级内（HSL L +0.06~0.13）；**helm（#0F1689，起点 1.04）/nuget（#004880，1.44）两枚藏青超量级**——纯蓝通道相对亮度上限 0.0722，暗面 3:1 物理不可达，抬幅 L +0.38/+0.21 是下限所需。已按 K61 回 README §4 登记（表 + 处置形态 + 量级注记 + ux 复核出口：若 K 系出资产级暗色版，token 组整体退役）。
- **可及性**：全部装饰位 aria-hidden（图标恒与文字同现）；无独立语义位 → aria-label 逃生口零启用（API 在位）。
- **SPA 预算**：361,147 B gz（T-389 基线 355,422 → **+5,725 B**，30 枚 ?raw 内联 + 组件/CSS；≤10KB 门内，5MB 硬顶维持）。

## 3. 票内发现：pkg-grid 磁贴类名自 T-240 起未落 DOM（dead CSS）

门控三件套的 opacity 0.4 断言首跑失败 → 追因：磁贴 button **从未带过** `className="pkg-grid-item"`（`git log -S` 全历史零命中，T-240 原始提交即无）——`repositories.css` 的卡面族（边框/surface 底/hover/禁用置灰/名·述排版）**dead 至今**，磁贴实为原生 button（T-383 实测的「190×44 磁贴」量的是原生按钮形态）。处置：**复线类名（一属性）**，卡面 + 禁用置灰随之类名复活；T-383 的 440px Dialog 宽度档断言复证不破（宽度由 paper sx 钉死，磁贴形态变化不外溢）——本节即「spec 若破需联动更新」的票内留痕（断言未破，几何档复证绿）。**这是本票唯一的视觉外溢面**（磁贴从原生按钮回到 CSS 册定义的卡面），源码注记 + 本节双留痕，供 conductor 知会 ux 复核。

## 4. 自测证据（实际执行；实例 /tmp/bf-t390 :8147，BINFLOW_ADMIN_PASSWORD=password）

```sh
cd web
npm run typecheck   # 通过（零输出）
npm run lint        # 通过（零输出）
npm run build       # assert-tokens: OK — css 12 + tsx 48；✓ built in 3.09s
node scripts/anchor-audit.mjs --ledger   # PASS（unregistered/broken 双零；本票零新锚——图标为装饰件，
                                        #  断言走既有锚 + .pkg-svg 结构选择器，锚册零改动）
# SPA 预算（AC3）：dist/assets js+css gzip 合计 361,147 B（T-389 基线 355,422 → +5,725 B ≤10KB）
make console 手作等价步（rm assets + cp dist → internal/console/dist）+ go build → :8147 净实例

BASE=http://127.0.0.1:8147 npx playwright test e2e/m14/ --project=chromium          # 16 passed（21.5s）
   └ 内含 t390-pkg-icons.spec.ts 5/5（pkg-grid 三件套/药丸/类型列/矩阵+暗档/axe 双主题）
BASE=… npx playwright test e2e/m10/console-license-addons.spec.ts --project=m10     # 5 passed（7.9s）
BASE=… npx playwright test e2e/m8/repositories-admin.spec.ts e2e/m8/setmeup-deploy.spec.ts \
         e2e/m8/artifacts-tree.spec.ts e2e/repositories.spec.ts e2e/artifacts.spec.ts \
         e2e/repo-policy-keys.spec.ts --project=chromium                            # 39 passed + 1 skipped（1.8m）
BASE=… npx playwright test e2e/m8/ --project=chromium                               # 69 passed + 1 skipped（4.5m，零红）
BASE=… npx playwright test e2e/m8/a11y-sweep.spec.ts --project=chromium            # 3 passed（含 56 路由面 × 双主题）
BASE=… npx playwright test e2e/m9/ --project=m9                                     # 20 passed + 1 skipped
BASE=… npx playwright test e2e/m12/ e2e/m13/ --project=chromium                    # 26 passed + 2 skipped

# M8 建仓/SetMe Up spec 联动结论：零改动通过（旧断言从不锚字符图标——锚的是
# 结构/门控/徽章/锚点族）；图标层断言由 t390 spec 增量承载。
# 服务端 diff=0：git diff -- '*.go' go.mod go.sum 为空（internal/console/dist gitignored）。
```

e2e 环境注记：首跑曾对 :8144 全红——该端口被 16:44 起的**旧控制台实例**占用（stale console，无 .pkg-svg），非产品缺陷；换净实例 :8147 后全绿。:8147 实例留供复验（后台 serve，日志见后台任务输出），可整删 /tmp/bf-t390。

## 5. AC 判定

| AC | 判定 | 证据 |
|---|---|---|
| AC1 搬运 30 + `<text>`=0 + 联合对齐（helmoci 小改 + deb/go 例外注记） | **PASS** | §0（grep 零命中；helmoci 入联合、PACKAGE_TYPES 地板不动；deb↔debian 映射在 ASSET_NAME + spec test 1/4 双复证，go 同名注记） |
| AC2 四消费点逐点（30 枚全消费或注记豁免） | **PASS** | §1 表：①brand+三件套（mono+0.4+徽章）②brand+字符退役 grep=0 ③mono currentColor 两处+搜索页豁免注记 ④brand 矩阵+四 feature 槽/mono addon 两枚豁免注记；对账 28 消费 + 2 豁免 |
| AC3 暗底 ≥3:1（K61 登记）+ aria + SPA ≤10KB + 四闸门 + axe 双主题 + M8 spec 联动全绿 | **PASS** | §2（11 枚提亮档最差面 ≥3.4；helm/nuget 超量级留 ux 复核出口；README §4 登记 + §6.4 勘误）；aria-hidden 全覆盖；+5,725B；四闸门 + axe（t390 双主题 2 面 ×2 主题 + a11y-sweep 56 面 + theme-smoke 双腿）+ m8 建仓/SetMe Up 零改动绿（§4） |

## 6. 契约漂移

- 服务端 API 契约：**无**（零 API 消费变化；helmoci 是 FE 类型联合补齐，wire 注册表既有值）。
- 规格面（非漂移，两笔回写供 conductor 分发）：① package-icons README §6.4 的暗底抽查口径错误（背景 vs 宿主面）——已按 K61 在 §4 登记（ux-designer 的 README，票面明示回写位）；② pkg-grid 磁贴类名 T-240 起缺席（dead CSS）——本票复线，磁贴形态回到 CSS 册定义卡面，console-ux.md 无字符图标/锚位描述需改（ledger PASS）。

## 7. 遗留

1. **assert-tokens 的属性选择器盲区**：pkg-icon.css 用无井号子串形（`[stroke*='d70a53' i]`）承载官方色匹配键——语义上是「定位键」非「色值声明」（落色恒为 var），但闸门当前按行扫 `#hex` 字面量。建议后续给 assert-tokens 增「属性选择器匹配键豁免」显式规则（比子串形直白）；本票未动闸门脚本（治理工具的放宽应单独走票）。
2. **helm/nuget 暗档抬幅超「+10% 微调」量级**（§2 物理下限论证）——留 ux-designer 拍板：接受 token 档现状，或 K 系出资产级 brand-dark/ 目录（届时 `--bf-pkgicon-*` 整组退役）。
3. **磁贴卡面复活**（§3）是本票唯一视觉外溢：磁贴从原生按钮回到卡面（~192px 两列卡、图标+名+述+徽章）。T-383 报告里「紧凑卡磁贴 190×44」实为原生按钮形态的测量——parity 册 M1 行如需按新形态刷新描述归 ux/conductor。
4. mono trashcan/webhook 两枚未消费（§1 ④ 注记豁免）；未来 addon 出现在列表/树场景时按 §1 纪律走 mono。
5. /tmp/bf-t390 实例（:8147）留供复验；/tmp/t390-contrast.mjs、/tmp/t390-tints.mjs 一次性分析脚本未入库（对比度表已固化进 README §4 与本日志）。

## 8. 变更清单

```
web/src/assets/pkg-icons/{mono,brand}/*.svg     30 枚搬运（K56 逐字节，零改动）
web/src/components/PkgIcon.tsx                  新增：单点组件（glob ?raw + 卫生化 + 双版纪律 + deb 映射）
web/src/components/pkg-icon.css                 新增：尺寸槽 + 暗底提亮 11 色（token 落色）
web/src/styles/tokens.css                       暗色块 +10 枚 --bf-pkgicon-*（纯新增）
web/src/lib/repos.ts                            PackageType +'helmoci'（第 13 型，联合补齐）
web/src/pages/repositories/RepositoryFormPage.tsx  ①brand/门控 mono 三件套 + 磁贴类名复线 + 字符图标退役
web/src/pages/repositories/repositories.css     禁用磁贴 0.45→0.4（三件套口径）+ pkg-icon 槽注记
web/src/pages/repositories/commands.ts          CLIENT_PKG_META icon 字段退役（②）
web/src/components/SetMeUpDialog.tsx            药丸 brand 18px（②）
web/src/components/dialogs.css                  smu pkg-icon 槽注记（字号档退役）
web/src/pages/repositories/RepositoriesPage.tsx  包类型 Chip 挂 mono 13px（③）
web/src/pages/artifacts/ArtifactsBrowser.tsx    树 .ico 包型角标 mono 14px（③）
web/src/pages/artifacts/browser.css             .tree-pkg 基线对齐
web/src/pages/admin/LicenseAddonsPage.tsx       矩阵行 brand 18px + 豁免集（④）
web/e2e/m14/t390-pkg-icons.spec.ts              新增（5 tests）
docs/design/brand/package-icons/README.md       §4 K61 暗底提亮登记 + §6.4 勘误注记
```
