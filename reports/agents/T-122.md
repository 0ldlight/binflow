# T-122 — architecture §7.1 两行回写（H-1 ?permissions 行 + H-2 changePassword 别名行）

- ticket: T-122 [P1]（T-107 移交 H-1/H-2，T-115 遗留 1/2——M4 DoD 前文档一致性收口）
- 日期: 2026-08-21
- 角色: architect；area: `docs/design/architecture.md` §7.1（路由表内两行新增）
- 状态: done（待主会话审核）

## 一、事实源核实（改动前，全部 grep/read 实测）

| 事实 | 源 | 结论 |
|---|---|---|
| ?permissions 路由与门 | `internal/httpapi/router.go:382-395` | `GET storage/...?permissions` → `routeAuth{required: true, admin: true}`——**admin 门**（注释明示 T-97 review B2 定案：管理面数据，枚举主体名与 r/w/d 分布；BinFlow 无 manage 动作、admin 为最近映射；空门将使匿名读实例枚举 users/groups） |
| 视图形状（定案方向） | `internal/httpapi/storage.go:128-147`（permissionsView godoc + 例）与 `:215-237`（principalLetters） | `{"uri","principals":{"users":{"jane":["r"]},"groups":{...}}}`——key=主体名、value=权限字母集合（r/w/d 顺序）；无任何权限的主体不出现；无 target 覆盖=空对象 |
| 规格勘误后形态 | `docs/reverse/rest-api.md:118` + `:124`（T-113 勘误注记） | 同上；字母全集 r/w/n/d/m、BinFlow 用 r/w/d 子集；非 local/cached → 400、local+item 不存在 → 404 |
| 视图/判定同源 | `internal/auth/authorizer.go:63,77-83` + storage.go:149-153 godoc | 共用 `targetCovers` 谓词，视图不可能与 `Authorizer.Can` 分叉 |
| changePassword 别名 | `internal/httpapi/router.go:420-421` + `:444-449` | `POST security/users/authorization/changePassword` → `routeAuth{required: true}`（**认证即门、非 admin**）；exact-match case 先于 `security/users/` 前缀 POST（不落 /{name} 部分更新臂） |
| 别名语义 | `internal/httpapi/security.go:62-88` | userName 缺省=调用者本人；admin 可指名他人、非 admin 指名 → 403；newPassword1≠2 → 400；旧口令错误 → 400 非 401；200 纯文本 |
| 家族定义 | `docs/prd/milestone-1.md:280`（E-16） | E-16 = 改密双路由：自有 `PUT /api/security/password` + 真实路径别名（body userName/oldPassword/newPassword1/newPassword2） |

**关键发现（影响回写方式）**：T-107 H-1 表述「§7.1 行仍为旧 r/w/d 位方向」经核实为**该行从未存在**——T-97 review 草案②（§7.1 增补建议）迟未落地（T-115 遗留 1 亦确认「草案②未动」），architecture.md 全文此前无任何 `?permissions` 行（grep "permissions" 仅命中 §6 元数据 DDL 两处）。故本票采取**直改（新增行）+ 行内勘误注记**，非 quote-then-revoke（无旧文可引）——与 T-115 先例（users 行替换、勘误句内联）同形的透明度。另草案②自身有一处过时：其「读门与 item GET 同源（匿名策略一致）」写于 B2 裁定**之前**，与现行实现（admin 门）相反——新行按 B2/实现书写，并在行尾注明草案②门位已被取代，防止后人依旧 review 草案复刻错误门位。

## 二、改动对照（`docs/design/architecture.md` §7.1 路由表，+22 行 0 删）

### H-2（先落，位置：groups 行之后、storage/usage 行之前——security 族连续）

新增行（820-827）：

```
  POST   /binflow/api/security/users/authorization/changePassword
                                                 改密（E-16 双路由的真实路径别名，与自有 PUT /api/security/
                                                 password 并存）；认证即门、非 admin——自助：userName 缺省=
                                                 调用者本人，admin 可指名他人、非 admin 指名 → 403；
                                                 newPassword1≠newPassword2 → 400、旧口令错误 → 400 非 401；
                                                 200 纯文本（用户管理纯文本层，auth-model §2.1/§2.4）；exact-
                                                 match case 先于 security/users/ 前缀 POST、不落 /{name}
                                                 部分更新臂——T-122 补行（实现早于 T-115 即在 router，表漏列）
```

- 门按实：`required: true` 无 `admin`（与 users/groups 行的 admin 门形成对照，故「认证即门、非 admin」显式写出）。
- 「与自有 PUT /api/security/password 并存」一句使 E-16 双路由语义完整（自有路径行未列，见下「移交」）。

### H-1（后落，位置：storage/usage 行之后、generic `/binflow/api/...` 兼容行之前——storage 族相邻、具体行先于泛族行）

新增行（830-843）：

```
  GET    /binflow/api/storage/{repo}/{path}?permissions
                                                 有效权限视图（SE-08/FR-27，M4 T-97；path 空=仓根）；
                                                 **admin 门**——T-97 review B2 定案：管理面数据（枚举主体名
                                                 与 r/w/d 分布），BinFlow 无 manage 动作、admin 为最近映射，
                                                 空门会使匿名读实例枚举 users/groups、击穿登录面存在性隐藏；
                                                 非 local 仓 → 400（先判型，remote 不触发上游拉取）、local+
                                                 item 不存在 → 404；形状 {"uri","principals":{"users":{"<主体
                                                 名>":["r","w","d"]},"groups":{...}}}——key=主体名、value=
                                                 权限字母集合（字母全集 r/w/n/d/m、BinFlow 用 r/w/d 子集；
                                                 无任何权限的主体不出现、无 target 覆盖=空对象）——T-113
                                                 勘误定案（逆向规格 §3 原记键值方向相反）；视图与判定共用
                                                 targetCovers 谓词、与 Authorizer.Can 不可能分叉——T-122
                                                 补行：T-97 草案②迟未落地，径按定案形态书写（草案②「匿名
                                                 同门」门位亦被 B2 admin 门取代）
```

- 定案形态三要素全落：key=主体名 / value=权限字母集合 / 无权限主体不出现（T-113）。
- 门位、400/404 分流、targetCovers 同源三条架构载荷均保留（分别为 B2 裁决、T-113 核实结论、T-97 设计不变量）。

## 三、grep 验证（实际执行）

```
$ grep -n "permissions\b\|changePassword" docs/design/architecture.md
624:-- permissions 行（username×repo_key×path_prefix 单前缀）被 PRD 推翻，差异见下注释）。        # §6 DDL 注释，非路由行，不受影响
644:-- ① target 有 name（可被 /binflow/api/v1/permissions CRUD 引用与整体删除，删除即授权失效）； # 同上
820:  POST   /binflow/api/security/users/authorization/changePassword                              # H-2 落地
830:  GET    /binflow/api/storage/{repo}/{path}?permissions                                        # H-1 落地

$ grep -n "r/w/d 位\|位映射" docs/design/architecture.md
（零命中——architecture.md 无旧方向活体残留；旧表述仅存于 rest-api.md/PRD 的 T-113 quote-then-revoke 引文内，本票不重复引入）

$ grep -cn "key=主体名" docs/design/architecture.md
1        # 唯一命中 = 新 ?permissions 行（定案方向唯一事实源行）

$ awk 'NR>=818 && NR<=843 {i=0; while (substr($0,i+1,1)==" ") i++; printf "%d indent=%d\n", NR, i}'
819 indent=49 / 821-827 indent=49 / 829 indent=49 / 831-843 indent=49
（续行 49 列对齐表内 groups/usage 行先例；路径行 2 空格起——与 T-115 补行同 posture）

$ git diff --stat docs/design/architecture.md
docs/design/architecture.md | 22 ++++++++++++++++++++++
（纯新增、零删改；两行均落在 §7.1 fence 内、generic 兼容行之前）

$ grep -rn "changePassword" internal/httpapi/router.go
420: case rest == "security/users/authorization/changePassword" && r.Method == http.MethodPost:
（行内路径/动词与代码逐字一致）
```

## 四、边界与移交

- BOARD.md 只读；仅动 `docs/design/architecture.md` §7.1 两行；未动 DECISIONS.md（无新决策——两行均为既有定案的文档落地）。
- **相邻缺口登记（不动，交 conductor 裁量）**：§7.1 亦未列 ① `PUT /binflow/api/security/password`（E-16 自有路径，router.go:418，认证门）与 ② `GET/POST /binflow/api/v1/permissions[...]`（permission target CRUD，router.go:324-326，§6:644 已有文字引用该端点）。本票 H-2 行内以「与自有 PUT /api/security/password 并存」一句使其语义可查；是否补独立行建议并入 M4 DoD 文档收口批或另开微票。
