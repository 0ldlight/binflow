# T-245 工作日志 — M8 用户文档改版：控制台指南（新 IA）+ Artifactory→BinFlow 操作路径对照表

| 项 | 值 |
|---|---|
| 票据 | T-245 [P1]（BOARD B5 尾票之二，与 T-244 并行；area = `docs/user/ + docs-site`） |
| 角色 | tech-writer |
| 日期 | 2026-08-24 |
| 状态 | done（文档四件套全交付；docs-site 零告警同步；全部 UI 断言在 HEAD 构建实测栈走查通过） |
| 击落-恢复 | 03:37 被 API 配额上限击落；06:02 后恢复续作。击落点 = MDX 裸尖括号排查中段。恢复后核实已完成面（console.md 重写、path-map 新页、npm 语义、路径修订、首次 `make docs` 均已落盘），从 MDX 收尾续起：全量隐患扫描归零 → 文案真实性终审（修正 4 处实现不符表述）→ `make docs` 重同步零告警 → 三向交叉链接补全 → 清理脚手架。无返工，无丢稿。 |

## 1. 交付物

### 1.1 控制台指南改版（`docs/user/console.md`，全量重写）

M7 版（六组页面 + `/repositories` 族路径）→ M8 版，按 console-m8 v1.0 新 IA 组织：

- **双模式导航**：应用/管理两模式表、模式切换（管理/返回应用）、面包屑、版本行、`⌘K`/`/` 全局搜索、空实例引导卡（localStorage）；管理五分组 12 条目全图（源码核对 AppShell.tsx）。
- **制品树浏览器**：工具栏四件（Set Me Up / 部署 Deploy / 管理仓库→ / 过滤仓库）、懒加载一层、URL 即状态 + 深链自动展开、详情右联、右键三形态（文件/文件夹/仓库）、BIG_DIR=2000 提示、详情面板（常规/有效权限 Tab、checksum「上传时提供：一致 ✓」徽标、docker tag 徽标与标签/摘要列）。
- **Set Me Up 对话框**：三入口、包类型网格（已有仓包类型并集）、配置/部署双 Tab、生成令牌三分支（admin 免二次口令 / 非 admin step-up 内联重验〔401 双码形态〕→ 与 token-step-up.md 交叉链接、一次性明文面板 + 24h/token_id）。
- **Deploy 对话框**：local Generic/Maven 候选集、字段序、显式「部署」步、percent-encode 双回显（T-231）、409/403/413 原样呈现、maven GAV 表单。
- **管理域五分组**：仓库（三 Tab/向导/详情四 Tab/编辑/两段强确认）、用户与权限、治理（审计/GC/配额/复制/备份）、监控与常规（存储新页/系统信息/profile 拆分）、搜索与仪表盘。
- **旧路径 → 新路径**：10 条映射表 + **M9 移除计划注记**（console-m8 §1.4 / Q3 终裁）。
- 角色可见性表更新（readonly_admin 管理模式读面 / user 角色树区无权限卡但深链按 ACL）；M8 边界表更新（不建清单：Packages/Xray/Providers/Layouts/Move·Copy/Trash Can…）。
- 登录/会话/CSRF 段保留（服务端语义 M8 零改动），改密入口改为 `/profile`。

### 1.2 新页：Artifactory → BinFlow 操作路径对照表（`docs/user/artifactory-path-map.md`）

- 导航结构对照（双模式/落点/版本行/搜索）+ **常见任务逐条两列表**（24 行：建仓/找仓/编辑/删仓/建用户/建组/配权限/找制品/树操作/Set Me Up/UI 上传/下载/删制品/token 签发与吊销/GC/配额/审计/备份/恢复/存储/系统信息/改密/复制/用户菜单——Artifactory 路径取自 clean-room 行为规格 `docs/reverse/console-ui.md`，BinFlow 路径全部实测）+ 备注（语义差异如实：GC 无 cron、备份任务式、权限四列非五列、删仓输入 key 强确认）。
- **无对应面如实登记**：Xray/Pipelines/Distribution/Builds/Projects/Packages/认证 Provider UI/Layouts/Mail·Webhooks·Proxies/System Logs/Trash Can·Favorites·Move·Copy/AQL 搜索族。
- 反向表：BinFlow 有而 Artifactory OSS 无的六项（审计页/per-repo 配额/全局复制/readonly_admin/step-up 内联/测试器+diff）。

### 1.3 T-249 语义更新（`docs/user/integrations/npm.md`）

- 新节「**发布权限语义（M8 起）**」四臂表：连发新版本与 dist-tag 移动 = **仅 write**；deprecate/改既有版本/缩减 versions = 需 delete；同版本重发 = 无条件 403；给 CI 配权最短答案 = read+write。
- 常见报错表新增「连发第二版本 E403（仅旧实例）」行（升 M8 或过渡期补 delete）。
- FAQ 三处同步（403 覆盖行加 npm 例外、速查表两行、版本头 M8）。

### 1.4 既有文档路径/事实修订

| 文件 | 修订 |
|---|---|
| `admin/groups-permissions.md` | §控制台对应操作：`/security/*` → `/admin/security/*`（M8 + 旧路径重定向说明）；补两步资源对话框形态；下一步加 path-map 链接 |
| `admin/governance.md` | §控制台对应页面：五治理页新路径全列 + M9 重定向注记；**token 审计过时表述修正**（词表补 token 行；「不落审计」M4 缺口段与 Token 审计现状段改写为已修复——代码 `security.go` 双 handler 落 `audit.Record` + scratch 实测） |
| `admin/token-step-up.md` | §SSO 用户 CLI 路径 2：控制台铸币位已落地（Set Me Up 内联 step-up，本地/LDAP 腿）；OIDC grant 自动续铸仍待接；下一步加 console 链接 |
| `admin/remote-virtual.md` | 不兼容表「每协议控制台视图」行改写（docker tag 徽标已交付 / npm·pypi·maven metadata 后续项——如实分档） |
| `admin/real-env-appendix.md` | 追加「迁移相关文档」四链（faq/console/path-map/migrate）——三向交叉链接的 appendix 侧 |
| `guides/migrate-artifactory.md` | ①**过时缺口修正**：virtual 成员/patterns 丢失已修（internal/client wire 拼写已按 `repositories`/`includesPattern` 对齐，代码核对）；②**S-1（V28 R-7/R-2，BOARD 记档归本票域）落笔**：merge 重迁轮 maven-metadata 服务端重算注记 + OSS 源 docker/npm config-only 能力边界；③V28 已执行状态与 path-map 链接 |
| `README.md` | 导航：控制台条目 M4 → M8（新 IA 描述）+ path-map 新条目 |
| `docs-site/sidebars.js` | 管理指南类目加 `artifactory-path-map`（console 之后） |

### 1.5 docs-site 同步

- `make docs` ×3（内容迭代后各一次）：构建 **零告警、零断链锚**；`internal/docs/dist` 同步（raw 2.99 MB，15 MB 预算内）；`artifactory-path-map` 入 build/sitemap/嵌入 dist（UTF-8 内容核验在位）。

## 2. 验证证据（全部实跑）

### 2.1 实例选择：18080 不可用于 M8 验证 → 自起 scratch 栈

18080 用户实例经资产指纹核验为 **M7 期构建**（SPA chunk 含 `SettingsPage`/`TreePage` 旧页面名，无 `ArtifactsBrowser`/`SetMeUpDialog`/`SystemInfoPage`）——M8 路径无法在其上验证。改自起 scratch：`bin/binflow-server`（8/24 03:10 构建，含 T-242 对话框族）@127.0.0.1:18091，REST 播种七仓（generic/docker/maven/npm/pypi local + maven remote + maven virtual，覆盖全部五种包类型与三种 rclass）。

### 2.2 UI 路径走查（Playwright，临时 spec，跑后已删）

```
BASE=http://127.0.0.1:18091 npx playwright test e2e/t245-docwalk.spec.ts
→ 11 passed (8.1s)
```

11 腿：①壳（应用侧栏 2 条目 + 「管理」切换钮 + nav-version 版本行）②树（工具栏四钮 + tree-repo-* 顶层仓节点 + 深链 `/artifacts/generic-local` 落位）③管理模式（五分组 12 条目 + 「返回应用」+ 面包屑 topbar-breadcrumb）④仓库三 Tab + repos-create + 行级 Set Me Up ⑤仓库详情/编辑双路由 ⑥**10 条旧路径重定向**（`/settings`→`/admin/general/settings`、`/security/*`、`/audit`、`/governance/*`、`/repositories*` 族——全部落新路由）⑦Set Me Up 全链（五类型网格→主对话框→repo 下拉→双 Tab→admin 无口令框直生成→令牌面板明文）⑧Deploy 对话框（repo/drop/submit）⑨用户详情 + 权限新建路由 ⑩/profile ⑪storage 页。
首跑 7/11，4 失败全为本方 spec 选择器猜测错（`.nav-version` 应为 testid、面包屑锚、perms-create 锚、详情面板锚），修正后全绿——**无一是产品缺陷**。

### 2.3 T-249 语义活体复验（scratch 栈，npm 10.9.8 / node 22）

write-only（read+write，无 delete）CI 号 `t245-ci` + `_authToken` 形态 .npmrc：

| 腿 | 结果 |
|---|---|
| publish 1.0.0 | `+ t245-doc-pkg@1.0.0` 成功 |
| **publish 1.0.1（T-247 场景）** | 成功（write 臂）；`npm view versions` = `['1.0.0','1.0.1']` |
| 改内容重发 1.0.0 | E403（tarball 层无条件臂） |
| `npm deprecate` | E403（字段覆写保持 DELETE 臂） |
| `npm dist-tag add … beta` | 成功（write 臂） |

四臂与文档表格逐行一致。

### 2.4 token 审计活体复验

`token.issue`（detail: fingerprint/subject/ttl_seconds——含走查腿铸的 86400 token）与 `token.revoke`（token_id=5）均落审计表——governance.md 过时段修正的实证。

### 2.5 文案真实性终审（修正的 4 处实现不符）

初稿按 console-m8 规格书写，逐条对源码核验后修正：①搜索三过滤（仓库/包类型/仓型）**不建**（仅仓库收窄——SearchPage.tsx 注记）；②系统信息页 Server Name/匿名读/数据目录/日志级别**无端点不展示**；③备份页是 **CLI 引导卡**非任务进度卡（R5 未落地）；④仪表盘 remote 上游健康**不渲染**（ux R9 兜底，显示 `—`）；另：树详情无 maven GAV 片段/virtual 成员来源列（未实现，改记后续项）、仓库表仅 key/包类型两列可排序、用户菜单 `quick-set-me-up` 在 HEAD 为占位链接（T-244 在途收口——文档如实注记）。**T-244 的未提交工作树改动一律未计入文档**（以 HEAD 为准）。

### 2.6 MDX 安全扫描

击落前后两轮全量扫描（剥离 fenced/inline code 后匹配 `<Word>` 与 `{expr}`）：修复一处 `{Generic, Maven}` 被 MDX 当 JS 表达式（改写为中文顿号表述）后 **0 隐患**；修复一处断链锚（自引用 `#常规系统信息与编辑档案` → 实存节锚）。

## 3. 清单与边界

- 改动文件：`docs/user/` 11 修改 + 1 新增；`docs-site/sidebars.js`；`internal/docs/dist`（make docs 产物，git 忽略面为主）。
- 未动：`web/`（T-244 在途文件原样）、`docs/design/`（console-m8 只读依据；console-ux.md 的工作树改动非本票）、BOARD、git 提交。
- 脚手架清理：临时 spec / test-results / scratch 实例（18091 已停）/ tmp 文件全部移除；**18080 实例零触碰**（核验仅 GET 资产指纹与 version）。

## 4. 遗留 / 待确认

1. **T-244 合入后回写两处**：用户菜单 `quick-set-me-up` 占位链接注记（console 指南与 path-map 各一句）——接线落地后删除注记即可；键盘可达增强面（FR-75）文档未展开，待 T-244 定稿后视增量补一小节。
2. **OIDC 腿 step-up 的控制台形态**：Set Me Up 内联重验目前为本地/LDAP 腿（`step_up_password`）；OIDC mint grant 自动续铸待 SSO 消费面（token-step-up.md 已如实注记过渡形态）。
3. **quick-set-me-up 之外的 T-242 遗留**（Access Tokens 页联动 R6、树页双 Deploy 入口收敛）在边界表/正文按现状表述，收敛后无迫切回写项。
4. **deprecate 严格侧**：T-249 遗留的产品裁量（npmjs 仅需 publish）若放开，npm.md 语义表第二行需同步——已交由票据链跟踪，文档面零动作可等。
5. `bin/` 下 03:10 构建含 T-242 但不含 T-244 在途改动——若 T-244 改变交互面（如全局对话框位），console.md「三入口」表述或需加「用户菜单」为第四入口。
