# T-L009-2 — list 补参大票 P0+P1（LOOP 009 轨道 L009-2，dev-go-storage）

Ticket:       L009-2 list 参数族补参 P0+P1（P0 递归语义+形态+行为差；P1 listFolders/includeRootPath）
Role:         dev-go-storage
Area:         internal/httpapi（?list handler 面——票面唯一实例特批；internal/storage 读面未改动）
Input:        reports/compatibility/L008-list-params-diff.md §1（as-built 语义 14 条）§2（32 臂矩阵）§3（缺参清单 a/b/c）§4（形态差 5 维）§5（P0-P3 分组）；行为逐臂照证据，无猜测补齐
Changes:      ① 递归三连修（storage.go handleStorageList 重写）：deep=1 为唯一递归触发（==1 布尔化）；depth 仅作修饰——deep=1&depth=N 钳 N 层、depth≤0 不限、无 deep 时任何 depth 不递归；无 listFolders 时任何深度零文件夹行（原死守卫根因=尾斜杠计入层数，改为去尾斜杠后计段数）；② 形态五项：条目 uri 前导斜杠（/f1.txt）、文件夹行 /d2 形（无尾斜杠、size -1、无 sha）、顶层 uri 去尾斜杠（根=裸仓 key）、created=请求时刻墙钟（毫秒，+74ms 漂移证据照抄，弃文件夹创建时刻）、Content-Type=application/vnd.org.jfrog.artifactory.storage.FileList+json（writeJSONBodyCT 新助手，writeJSONBody 委托）；③ 行为差三项：仓库根可列（200 直接子文件，根拒绝仅属无仓段请求——路由层已挡）、七参（deep/depth/listFolders/mdTimestamps/statsTimestamps/includeRootPath/includePropertiesMd5）任一存在值非整数→400 envelope `For input string: "<v>"`（Java parseInt 逐字文案）、文件目标 400 文案 `Expected a folder but found a file, at: <repo>:<path>`（冒号拼写）；④ P1 两参：listFolders=1 文件夹行入列（字母序混排）/includeRootPath=1 被查文件夹自身为首个条目 uri:/ size:-1 folder:true；⑤ 全列字母序 sort（证据 §1.14：/d2/d3/d4/f5.txt 先于 /d2/d3/f4.txt）
Files:        修改 internal/httpapi/storage.go（list 段重写+net/url import）、internal/httpapi/repositories.go（writeJSONBodyCT 助手）、internal/httpapi/compat_test.go（TestStorageList 三处旧分歧断言翻新：根 400→200 可列、uri 相对形→前导斜杠形、文件目标文案）；新增 internal/httpapi/storage_list_params_test.go（行为命名）；删除无
Tests:        storage_list_params_test.go：TestStorageListIntegerValidation（七参非数值 400 逐字文案+数值全过）、TestStorageListRecursionSemantics（9 表臂：deep=1 零泄漏/deep≠1 平/depth 单独不递归/depth=99/deep=1&depth=1|2|4 钳层）、TestStorageListFolderRows（/d2 形 size-1 无 sha/字母序混排/listFolders=0 排除/文件行 sha 保留）、TestStorageListRootPathEntry（/ 首条+A19/A20 组合）、TestStorageListRepoRootArms（A26 根可列/A27 根组合/根 uri 裸仓 key）、TestStorageListWireForm（vendor CT/顶层无尾斜杠/created 请求时刻——种子后 sleep+FolderInfo 对照证非文件夹时刻）；compat_test.go TestStorageList 翻新（403 保留/根 200/文件 400 新文案/deep=1 无泄漏/depth 单独平）；故障路径覆盖：非数值拒收（corruption 类输入校验）、参数解析先于目标解析（顺序契约）
Commands:     go build ./... && go vet ./...（空）；gofmt -l internal/storage internal/remote internal/httpapi（空）；~/go/bin/golangci-lint run ./internal/httpapi/... ./internal/storage/...（0 issues）；go test ./internal/storage/... ./internal/httpapi/ -count=1；go test -race ./internal/httpapi/ -run 'TestStorageList|TestStorageItem|TestRemoteBrowse' -count=1（solo 串行）；差分：make release VER=uat-l0092-03396aa1 → 离线镜像组装（DaoCloud EOF，l0072 脚本复放）→ 独立容器 :8084（不碰共享 binflow-ga）→ 32 臂矩阵双发两轮 → 清理（双端 DELETE 仓、容器+自有匿名卷删除、/tmp 证据件删除）；Review B 返工后：go build ./... && go vet ./internal/httpapi/ && gofmt -l（空）&& golangci-lint run ./internal/httpapi/...（0 issues）+ go test ./internal/httpapi/ -run 'TestStorageList' -count=1（ok 4.589s）+ race solo 同子集（ok 21.378s）；差分单臂复验按 Review B 裁定延至 LOOP 010 合并复验；Review A 返工后（int32 界）：四门同命令重跑（build/vet/gofmt 空、lint 0 issues）+ 定向 TestStorageList -count=1（ok 4.686s）+ race solo 同子集（ok 21.718s）
Outputs:      go test：ok internal/storage 76.801s / ok internal/httpapi 195.799s；race 子集：ok 22.013s；矩阵两轮同分布：状态列 32/32 对齐（A09/A24/A25/A29/A32=400×2，A26/A27 bf 200 对齐）；归一判定（时间戳值+uri 主机前缀归一）：SAME 25 / DIFF(P2-keys) 7（A15/A16/A17 mdTimestamps.properties、A21/A22 propertiesMd5、A23 两键、A30 mdTimestamps.artifactory.stats——条目集/uri 形/size/folder/sha 全同，仅 P2 元数据键缺席）；逐字证据：bf A09 `For input string: "abc"`、A24 `Expected a folder but found a file, at: l0092-loc:d1/f1.txt`、A29 `For input string: "true"`、A02 created `...T06:50:32.523Z`（请求毫秒）顶层 uri `.../api/storage/l0092-loc/d1`（无尾斜杠）；Review B 返工：空值/空白臂（deep、deep=、depth=、listFolders=、includeRootPath=%20、depth=%20%20）全 200 平列表 + 「坏参×文件目标」优先级臂 parse 400 先胜——7 测试族全 PASS；Review A 返工：depth=2147483648 / -2147483649 → 400 `For input string: "<v>"` 同文案，边界值 2147483647 / -2147483648 仍 200
Compatibility: L008-3 §2 原 26 DIVERGENT 臂收敛 25（A02/A05/A06/A07/A09/A10/A11/A12/A14/A18/A19/A20/A24/A25/A26/A27/A29/A32 全收敛 + 原「同集形态差」臂升为归一 SAME）；余 7 臂分歧全部且仅落在 P2 三参键（mdTimestamps/includePropertiesMd5/statsTimestamps）——与【不做】范围逐臂吻合，零范围外分歧；lastModified 秒级 .000Z 维持（M16 已定谳 audit 派生族，不翻案）
Security:     凭据未落盘（BinFlow 口令运行时 source .env.uat 进环境变量，参照口令运行时 env 传入，均已随进程销毁）；错误文案不含凭据；路径校验走既有 storageNode/服务层路径守卫未动
Performance:  列表仍单次 List 调用+内存投影；每文件行 1 次 blob ledger 读（既有行为）；排序 O(n log n) 内存操作；无新增 IO 路径
Risks:        ① includeRootPath 根条目（/）在仓库根上无 node 行，lastModified 渲染 1970 零时刻——证据未记录该字段形态（A27 ref 侧字段级未取证），UAT 复验 A27 归一 SAME（时间戳值归一掩盖）——若 LOOP 010 差分逐字节对拍发现该字段体裁差需再修；② 【Review B 返工已修】空值/空白参数首版误判为 400——只读了 parseInt 文案、漏读 getQueryParameterAsInt 的 containsKey&&isNotBlank 短路（ArtifactResource.java:376-382，空值回 0 不进 parseInt），且旧代码 dv!="" 恰与参照一致构成回归；已改为 TrimSpace 空值视缺席并补 6 空白臂+优先级臂测试；②' 【Review A 返工已修】int32 界窄窗：Atoi 64 位吞下 (2^31, 2^63) 值而参照 parseInt 溢出 400 同文案——已加 MaxInt32/MinInt32 显式钳并补 4 边界测试臂（两溢出 400 / 两边界 200）；③ P2 三参已做整数校验但无列表效果（A15-A23/A30 保持分歧，按票面预期）；④ 参照侧 GET 已删仓返回 400（Artifactory 自有体裁，残留复核以 DELETE 200 为准）
Blockers:     无
Next:         ① LOOP 010 P2：mdTimestamps（需属性修改时刻跟踪+属性写面——BinFlow 无 PATCH /api/metadata 面，跨面票）/includePropertiesMd5（需 compatibility-engineer 先定属性规范序列化 md5 规格——跨系统等值性）/statsTimestamps（?stats 数据已在，只差入列）；② matrix 翻绿建议（归 compatibility-engineer 裁定）：rest/storage-list-recursion-semantics（BUG，b-1/b-2/b-3）→ resolved，evidence=本轮 32 臂复跑；rest/storage-list-params-family（UNSUPPORTED_FEATURE 五参未读）→ 收窄为三 P2 参未实现；D01-R05 建议从 partial 收窄（25/32 臂归一 SAME）；③ UAT :8083 共享容器未动——conductor 决定是否以 binflow:uat-l0092-03396aa1-alpine（已留本地）重建 binflow-ga 后跑全量协议矩阵
