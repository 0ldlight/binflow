Ticket:       L003-2 remote 面头集+分页票族（契约残余 DIVERGENT 清偿）P1
Role:         dev-registry-adapter（领域实例：dev-package-docker）
Area:         internal/adapter/docker（+测试；helmoci 经 docker 平面继承，零改动）
Input:        reports/compatibility/L002-contract-replay.md 逐案（#1/#2/#6/#7/#11/分页取证段）+ /tmp/l0022 原始捕获（a_ping*/a_pingbad*/a_mf*/a_mfh*/a_blob*/a_blobr*/a_tagsn*/a_tagsl*/a_tagsinv*）+ docs/compatibility/contracts/docker-remote.yaml（21 条目）+ docs/compatibility/known-divergence.yaml（tags-catalog-pagination/C06 族）+ ADR-0047/0048（chain gate/negative-cache 交界）
Changes:      ① remote 面 manifest GET/HEAD 补全 Artifactory 实测头集（Etag=sha1 无引号/Last-Modified=落缓存时刻/Accept-Ranges/Content-Disposition+X-Artifactory-Filename（manifest.json|list.manifest.json）/X-Artifactory-Origin-Remote-Path=上游根+请求引用逐字；HEAD 另加 X-Artifactory-Docker-Registry=<repoKey>）——remoteFace{registry,origin} 贯穿 direct/virtual 两平面 12 个 serve 臂；blob GET/206 补 Last-Modified+Origin-Remote-Path+sha256__<hex> 文件名对（在 serveRemoteBlobCopy 前置，206 同乘）。② errors.go CT 参数化（writeSpecErrorCT/writeStatusFormErrorCT），仅 ping 两 401 臂传 application/json;charset=ISO-8859-1（全捕获面证明其余面裸 application/json）。③ RenderAuthFailure ping 路由分支：Basic realm="Artifactory Realm" + pretty "Bad Credentials"（charset 同臂）；未触碰 internal/httpapi（路由经 v2AuthFailure seam 委托进 adapter）。④ tags/list 与 repo 域 _catalog 尊重 n/last（复用 catalog.go 的 slicePage/nextPageLink）：无效 n → 404 pretty "Not Found" 先于上游接触；截断时 Link rel="next"，catalog Link 照抄参照 quirk 指向 /v2/_catalog；裸 GET 维持全量。⑤ Review A 三件：remoteManifestRefs 返错+landFetchedManifest 落 cache_result=refs-unparsed WARN（chain-unrecorded 对偶行）；TestChainGateColdDigestPosture 加重复链外腿（同形 404+往返冻结+负缓存行不落）；T-L002-1.md 两处 17 包→16 包+文末勘误注。
Files:        新增 internal/adapter/docker/remote_face.go（face/hints/文件名/Last-Modified/origin/remotePageSize）、remote_face_test.go、ping_face_test.go；修改 internal/adapter/docker/{errors,handler,remote,virtual}.go、remote_chain_gate_test.go、token_test.go（ping 臂新裁决）；reports/agents/T-L002-1.md（勘误）。删除：无。收编轮（conductor 指令、test-only、httpapi 域例外授权）：internal/httpapi/{docker_v2_test.go,docker_token_test.go} 四处期望改锚 ping 新形态。
Tests:        收编轮: TestV2RejectedCredentialRendersSpecBody（ping 臂→assertPingRefusedFace 新helper、资源臂维持 Bearer+spec）、TestV2ExpiredBearerRendersSpecBody / TestV2RevokedBearerRendersSpecBody / TestV2TokenRevocation（/v2/ 臂→ping 新形态）——期望全部锚 BinFlow 实测响应，文件头注释挂 L003-2+证据指针。单测新增 TestRemoteFaceManifestHeaderSet（MISS/HIT/HEAD 三臂全头集+值语义）、TestRemoteFaceBlobHeaderSet（200+206）、TestRemoteFaceIndexFilename（list.manifest.json）、TestRemoteListPagination（invalid-n 零上游/Link 逐字节/last 游标/裸全量）、TestRemoteCatalogPagination、TestPingFaceAnonymousCharset（charset+compact+Bearer；认证 200 裸 CT）；改 TestRenderAuthFailureTokenPathIsArtifactoryForm（ping 第三臂）、TestChainGateColdDigestPosture（重复腿）。集成=dind 真客户端+双系统对拍（下）。
Commands:     go build ./... ; go vet ./internal/adapter/... ; gofmt -l internal/adapter（空）; golangci-lint run ./internal/adapter/...（0 issues）; go test ./internal/adapter/docker/ ./internal/adapter/helmoci/ ./internal/repo/ -count=1; go test 同两包 -race -count=1 -run 'TestRemoteFace|TestRemoteListPagination|TestRemoteCatalogPagination|TestPingFace|TestChainGate|TestT392|TestRenderAuthFailure|TestVirtual'。收编轮: go test ./internal/httpapi/ -count=1 -run 'TestV2'（ok 13.0s）; go test ./internal/httpapi/ -count=1（ok 143.3s，含 L003-1 storage admin 新测试无交互）; gofmt -l internal/httpapi 空 + go vet 该包净。客户端: docker build uat-l0032-952e556d-alpine → 独立验证实例 :8084（不碰共享 binflow-ga）→ registry:3 上游 :5588 种 busybox:t1/t2/t3+hello-world+l0032-probe → dind docker pull :8084/…/busybox:t3 ×2 + hello-world + curl 全腿双端（/tmp/l0032/{legs,alegs,final}.sh 原文）。
Outputs:      双端活体逐头一致——mf GET: Etag 7b175af6…/Last-Modified=拉取秒/Origin=…:5588/v2/busybox/manifests/t1/校验和三值同（shasum 独立核算）；HEAD 带 X-Artifactory-Docker-Registry: audit-l0032-docker-remote；blob(206 同): sha256__c6348fa8… 文件名对+e6329bed/105e5808 校验和双端同值；ping 两臂 charset+Basic realm 逐字；分页: ?n=2→[t1,t2]+Link </v2/audit-l0032-docker-remote/busybox/tags/list?last=t2&n=2>; rel="next"（与 Artifactory 逐字节同）、?n=2&last=t1→[t2,t3] 无 Link、?n=abc→404 "Not Found"、catalog n=2 Link </v2/_catalog?last=hello-world&n=2>（参照 quirk 同形）；pull exit 0 digest 逐字（1cfa4e2b…8525f8==上游回执）二拉上游计数冻结（access+otel 双行归因后 1 次往返）；INM 200 全量 610B（永无 304 维持）。三包 ok（34.9s/27.3s/120.7s）+race ok。收编轮两级: TestV2 族 ok（13.0s）/ httpapi 全包 ok（143.3s）。收编轮补测差分新证: Artifactory /v2/+未知 Bearer = Basic realm + charset + pretty "Props Authentication Token not found"（与 bad Basic 的 "Bad Credentials" 同形不同消息）——BinFlow 暂同形同消息，消息粒度 delta 登记残留（报告残留节已更新）。
Compatibility: 出证 reports/compatibility/L003-remote-face-diff.md。翻绿建议：#1/#2/#6/#7/#11 → VERIFIED（#2 需 expect.pattern 改锚 Basic realm；#11 body note「上游 mtime」按活体改「落缓存时刻」；#7 可先按 L002-2 escalation#3 复核再翻，两口径均过）；分页=契约补条目面（known-divergence docker/remote-tags-catalog-pagination UNKNOWN→可实现，四臂双端值已可执行化）。**上报勘误 ×2**: L002-2 报告分页表「Link 头无」行与自身捕获 a_tagsn.h 矛盾（Artifactory 截断时发 Link，本轮活体复证）——L003-2 按 ground truth 实现；catalog Link 指 /v2/_catalog（registry 级路径）为参照 quirk 已照抄。可差分 surface（供 differential-qa）：GET/HEAD /v2/<repo>/<img>/manifests/<tag|digest>（头集×MISS/HIT）、GET/Range blobs、GET/HEAD /v2/（两臂）、tags/list 与 _catalog 的 {bare,n,last,n+last,invalid-n}×Link 有无。
Security:     路径参数校验沿既有（digest/tag 形状前置拒绝）；Origin-Remote-Path 由配置上游 URL+wire path 拼接，无用户输入注入面；remote 代理仍走 internal/remote 守护 dials（allowPrivateUpstream 显式开启方连私网上游，本票未改）；ping 坏凭据臂不回显凭据材料。
Performance:  manifest 面新增 ledgerRow(ctx,dgst) 单行查（本地 SQLite）+node 时间戳格式化，无正文重算；blob 面零新增 IO（头全来自既有 node/ledger 行）；分页在聚合后内存切片（上限=上游全量，既有聚合语义不变）；face 的 RemoteUpstream 一次 repo 行读（miss 臂与 remoteSession 重叠读一次，可接受）。大制品流式路径（blob 流式落缓存）未触碰。
Risks:        ping 面被拒 Bearer 类消息粒度未分（BinFlow 统一 "Bad Credentials" vs 参照 "Props Authentication Token not found"——挑战头/信封已齐，后续票裁）；HEAD 头集对参照活体为超集（契约无 absent 断言，两口径过，条目 expect 复核在 compatibility-engineer）；blob Last-Modified 语义与契约 E2-5 note 冲突（两轮 registry:3 活体均=落缓存时刻，待裁）；n=0/负数与 catalog last/invalid-n 的 Artifactory 臂未单拍（对称实现）；虚拟面 Origin-Remote-Path 仅 fetch 臂有值（HIT 臂缺省，虚拟面无证据面）；客户端版本差: dind docker 29.7.2（票面方法学 27.5.1 同形——两版对 ping/token/头集均无版本敏感面）。
Blockers:     无。（过程披露: 共享 Artifactory 参照实例窗口内多次自重启致清理迟滞一轮，恢复后已删净；共享宿主整模块 make lint 红全在并行轨道 L003-3 的 httpapi WIP 文件，本票包面 0 告警。）
Next:         ① compatibility-engineer 裁 #2 改锚/#7 复核/#11 note 勘误 + 分页契约补条目（含 Link 形态锚定）；② L002-2 重放报告「分页取证段」Link 行勘误入台账；③ 差分票可按上 surface 清单跑 L5；④ 金样候选: manifest 头集活体（值可变头以 derived 规则锚）；⑤ 虚拟面 remote 服出头集待证据面出现时对齐。
