# L026-1 — D08 release-bundle 域规格核对（批次 4 首票）

Ticket:       L026-1 规格核对票：D08 release-bundle 域（matrix D08-R01~R06；R07 Distribution 服务侧 ⛔ 面外）
Role:         reverse-engineer（release-bundle 域实例）
Area:         docs/reverse/release-bundle.md（§10 新增 + §1 表五处勘误）+ docs/reverse/rest-api.md（§7 新增）+ docs/reverse/README.md（两行索引）
Input:        派发领域 D08 六行；缺口来源 = matrix D08-R01~R06 全 absent 行（source_row §9 行 1-6）；参照公开规范 = JFrog Distribution/Release Bundle 官方页（T-488 已锚，本票沿用）；参照实例 A = 172.16.58.130:8082 pro 7.161.15 全 addons（admin/JFrog@2026）；反编译 7.161.24
Changes:      ① 活体探面 64 发（p01–p63）：v1 查询族空态 → ② v2 面发现与定案（`/api/v2/release_bundle` 下划线，路由注册改写自 lifecycle 命名空间）→ ③ R01 装配校验链 + 命中形态（建临时仓 l026q-rbsrc + 单制品 + 属性差分）→ ④ R02 事务三段错误臂 → ⑤ R03 store 四臂（含 release-bundles 系统仓自动创建副作用实证 p48/p48b）→ ⑥ R06 权限矩阵（非 admin 用户 l026r-d08-user + 匿名 + ANY DISTRIBUTION 桶 + distribution rclass 不可建）→ ⑦ 规格回写（release-bundle.md §10 七小节 + 表内勘误五处；rest-api.md §7）→ ⑧ 资产自清复核
Files:        新建 reports/compatibility/l026a-wire/{p01..p63}.txt + p48b JSON（64 文件，已扫凭据零命中——唯一 "Basic" 命中为 Www-Authenticate 质询头非泄漏）；修改 docs/reverse/{release-bundle.md, rest-api.md, README.md}
Tests:        活体全录：v1 空态 p01–p12/p59、R01 装配 p27–p37、R02 事务 p38–p43、R03 store p44–p49、R06 权限 p50–p58/p61–p63、v2 面 p13–p26/p60。资产清理实测：l026q-rbsrc 删 200 + 复查 400；l026r-d08-user 删 200；探针副作用 release-bundles 系统仓删 200（deletedArtifactsCount=0）；实例仓列表恢复至探针前（余 l026r-* 三仓属 L026-5 并行票，未触碰）
Commands:     反编译定位：`find reverse-src/artifactory/backend -name "ReleaseBundleResource*"`（命中 artifactory-rest/org/artifactory/rest/resource/release/ReleaseBundleResource.java 18 端点逐方法）；`grep -rln "v2/release_bundle" backend --include=*.java`（3 命中：BundleHandler 客户端 URL 构造 / ArtifactoryRouterRegistrationData 路由改写 / Retention 服务）；MetaDataFilter.java 全读（过滤按仓类型+元数据路径非属性）；RepoTypeHelper.java 全读（REST 仓建接受集五类）；ReleaseBundleAddonImpl storeBundle/getResolvedStoringRepo/createReleaseBundlesRepoIfNotExist 逐段读。活体：cap 脚本 64 发 curl（-D/-o 分离捕获，状态+CT+Allow+校验和头+body 入档，Authorization 不录）
Outputs:      规格覆盖：端点 18（v1）+ 9（v2 读/错误面）+ 1（audit）= 28 端点行；语义流程 7 节（v1/v2 并存定案 / R01 校验链 / R02 错误臂 / R03 四臂顺序 / R04 404 三消息族 / R05+v2 分页信封 / R06 权限矩阵）；布局规则 3（`<storingRepo>/<name>/<version>` bundle_path、v2 `<repo>/<name>/<version>/release-bundle.json.evd`、release-bundles 系统仓 packageType=releasebundles 且列表不可见）
Compatibility: 置信度分布 高 22 / 中 4 / 低 1（§10 内逐条标）。与官方规范差异/补充：v2 面形态（下划线路径、jfds 仓名、.evd 布局、audit 七必填）官方 Distribution 页未载——「此条补充官方规范」；矩阵勘误建议：D08-R01~R05 描述维持 v1 不翻案（活体证明 v1 全存活、v2 并存增量）；D08-R04/R05 行内可补「DELETE 不存在 404 三消息族」；R06 行建议补「ANY DISTRIBUTION 非 REST permission target 实体（名单仅 Any Remote/Anything）」
Security:     R06 权限面全录：非 admin 对 admin 门端点（store/config/async-status/fat_manifest）403 errors 信封 `Forbidden`；user 角色对装配与查询放行；匿名全域 401 `Authentication is required`（实例关匿名；403 裸分支反编译单源中标中）；distribution rclass 不可经 REST 创建（400 逐字）；凭据零入档（扫描复核）
Performance:  无时序/缓存阈值类条目；唯一量级锚：v2 records 分页缺省 limit=1000/offset=0（p18）；HEAVY_RELEASE_BUNDLE_SIGNATURE_LENGTH=100000 仅日志开关阈值（反编译，不入行为面）
Risks:        ① store 202/200/409 三态与事务 happy-path 未实弹（无 Distribution 签名链）——§3.1 维持反编译置信中；② received/{name}/{version} GET 恒 400 名字校验疑路由缺失（pending #6）；③ jfds 仓名语义单源低（pending #7）；④ entitlement 400 与匿名 403 两分支本实例不可达（pending #8，反编译单源）；⑤ v1 与 v2 并存下的回显混合形态（有真实 v2 bundle 时 /release/bundles 是否混出）未证（pending #4 残留）
Blockers:     无（会话重启一次，transcript 续接，进度未丢；探针前缀换代 l026q→l026r-d08 已按复活令执行并双代清理）
Next:         契约化票（compatibility-engineer：D08-R01~R05 错误臂/空态可直接固化 contract，锚 l026a-wire）；G/D 差分票在 BinFlow 实现 RB 最小面后跑；Distribution 联调环境到位后补 store 三态 + v2 写面 + 回显混合形态三组活体；matrix 行描述勘误归 conductor
