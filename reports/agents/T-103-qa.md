# QA 报告 T-103 — M4 后端面全量验收矩阵（curl/CLI）

- role: qa-engineer
- 日期: 2026-08-21
- 票据: T-103 [P0] QA：M4 后端面全量验收矩阵（AC 全文见 reports/agents/T-88.md T-103 节；派单口径含 T-116/T-113 定案注入）
- 验收口径: docs/prd/milestone-4.md **v1.2**（W01~W40 后端面 + NFR-S19~S26 抽查；R1 裁决面按 PRD——`/binflow/ui/**`、`binflow_session`、`/api/v1/session` 三动词、Origin CSRF）
- 被测对象: commit **3c3cd36**（后端冻结基线：T-89~T-97、T-111、T-113~T-116 全部 done）。独立 git worktree `/tmp/t103/wt` 检出；`make console`（npm ci + vite build，SPA gzip **105,840 B**，与 T-99 记录一致）→ `make build` 真二进制 **19.45MB**（含 embed 控制台）。3c3cd36..HEAD(39a06c7) 期间零 backend 提交（web/ only），基线仍有效
- 环境: darwin/arm64；实例 A=`127.0.0.1:8310`（匿名关 + `BINFLOW_ADMIN_PASSWORD`，备份阶段叠加 `BINFLOW_REMOTE_CREDENTIALS_KEY`）/ TTL 实例（同库 `console.session_ttl_seconds=5`）/ 恢复实例 B=`127.0.0.1:8311`；mock 上游 `127.0.0.1:9199`（计数日志，T-76 同款）；全部 fresh data dir
- 客户端: curl（主驱动）/ **docker CLI 27.5.1（dind 27.5.1 容器内，`--insecure-registry host.docker.internal:8310`，宿主 Docker Desktop 零改动）** / npm 10.9.8（node 22.23.2，HOME 隔离）/ mvn 3.9.9 + Temurin JDK 21.0.12（独立 settings）/ twine 7.0.0（隔离 venv）
- 口径备注: `/api/repositories` 与 `/api/v1/health` 断言按 **T-116 定案 admin-only**；`?permissions` 断言按 **T-113 定案 key=主体名、value=r/w/d 字母集合**

## 总结论: **PASS**

- 后端面 W 序列断言 **217 项全过 + 附带项 8 项**（5xx 零容忍达成：三实例日志合并扫描 **5xx=0、ERROR=0**；唯一 panic 为本 QA 错配 `BINFLOW_REMOTE_CREDENTIALS_KEY` 非 base64 触发的 fail-fast——设计内行为，见 §W30b）
- 真实客户端矩阵: docker push/pull/mount（dind）、npm publish、mvn deploy、twine upload、curl 全部按 PRD §5.3 P0 口径执行；W26c 五协议 413 负断言全过（含 T-111 mount-413 verbatim 真客户端实证）
- 备份恢复 W28~W32 真机往返全过: 四协议 sha256 逐位一致、jane/devs/权限矩阵/token/quota 仓/审计历史全保真、mtime 全等、remote 凭据密文随行且无钥 fail-fast 链成立
- **无 P0/P1 缺陷**；2 条 P2 观察（docker/maven 配额拒绝后的「先落者残留」，见缺陷建议 D-1/D-2）+ 3 条轻量观察；**PRD 勘误建议 E1~E5**

---

## 1. AC 逐条结论（T-88 T-103 节）

| # | AC | 结果 | 证据摘要 |
|---|---|---|---|
| ① | 基座全周期 W01→W01b→W02→W03~W08→W37a→W38 + 搜索 W14/W15/W16 + W36 边界 | ✅ | §2.1/§2.2：301+Location、SPA shell/深链/immutable 资产、保留字五拒（ui/docs/console/api/v2）、session 全周期（401/200+三属性 cookie/同文案 401/204 吊销重放 401/TTL 5s 过期/重启不掉线）、CSRF 六变体；搜索 200/400/ACL 零泄漏；W36 十端点 404 |
| ② | 权限与治理 W17→W18/W40→W19/b/c→W20→W21 + 组边界；W22/b→W23/b→W39→W24/W25/b→W12a→W26/b/c→W27；零 5xx | ✅ | §2.3~§2.6：groups CRUD 全矩阵、组继承/即时生效/并集/409 删除保护、?permissions 新形态、审计过滤/分页/词表/脱敏/append-only、GC dry-run/apply/幸存/三方互斥锁、pattern 双值码、配额 413 原子性 + 五协议 + B1 幂等重传面；5xx=0 |
| ③ | 备份 W28/W28b→W30/W30b→W31→W32 真机往返；报告落盘 | ✅ | §2.7：在线 export 退出码 0/manifest 齐全/0700、引用完备 + 窗口语义、full-verify import、四协议 + 账号体系 + token + quota + 审计全保真、无钥 fail-fast/有钥代理链通、损坏矩阵 fail-fast 无半恢复、mtime 35/35 全等；本文件即报告 |

## 2. W 序列明细（命令 + 关键输出）

### 2.1 控制台基座（FR-23，W01~W08/W37a/W38 + NFR-S19）— 32/32

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W01 | `GET /binflow/` → 301 | `301 http://127.0.0.1:8310/binflow/ui/` | ✅ |
| W01 | SPA shell 挂载点 | `id="root"` 命中（grep -c = 2，见 E1） | ✅ |
| W01 | 深链 `/binflow/ui/repositories` | 200（history fallback） | ✅ |
| W01b | 保留字建仓全拒 | `ui`/`docs`/`console`/`api`/`v2` → 全 400 E-01（`repository key is reserved ... ADR-0008`）；合法 key `legit-local` → 200 | ✅ |
| W02 | 指纹资产缓存头 | `/binflow/assets/index-BFtxO2ui.js` → `Cache-Control: public, max-age=31536000, immutable`；shell → `no-cache` | ✅ |
| W03 | 未认证 whoami | 401 | ✅ |
| W04/W04b | 登录 200 + cookie 属性 | `{"username":"admin","admin":true}`；`Set-Cookie: binflow_session=<64hex>; Path=/binflow; Max-Age=86400; HttpOnly; SameSite=Lax`（256bit 高熵；明文 HTTP 不加 Secure——R1 附注口径） | ✅ |
| W05 | 错误凭据 401 同文案 | 错口令与不存在用户均 `{"errors":[{"status":401,"message":"invalid credentials"}]}`；`login.failed` ×2 落审计 | ✅ |
| W06 | whoami | `admin`/200 | ✅ |
| W07 | 登出吊销 | DELETE → 204；同 cookie 重放 → 401 | ✅ |
| W37a | 重启不掉线 | 登录 → kill/重启 → whoami 200 | ✅ |
| W08 | TTL 覆盖键 | `console.session_ttl_seconds=5` 重启 → 登录 `Max-Age=5` → sleep 6 → 401 | ✅ |
| W38 | CSRF 六变体 | cookie+`Origin: http://evil.example` PUT → **403**（E-01 信封）；无 Origin → 201；同源 Origin → 201；**Basic+evil Origin → 201（CI 零感知）**；cookie GET+evil Origin → 200（读豁免）；cookie 写落盘内容 201 | ✅ |
| NFR-S19 | session 值不落日志 | 两实例 cookie 值 grep serve 日志 = 0 命中；值长度 64 hex | ✅ |

附：session cookie 等价性矩阵（T-91 AC②/ux R10）——cookie 对 generic 内容 GET 200、npm packument 200、pypi simple 200、audit/usage 管理面 200、内容写 201 全部成立；**/v2 面 401 属 cookie `Path=/binflow` 作用域（curl 不携带）**，强制携带 Cookie 头访问 /v2/tags/list → 200（user=admin），机制本身等价（见 E4）。

### 2.2 治理字段与配额——字段面（W12a + GE-05 字段）— 8/8

| 断言 | 实测 | 结果 |
|---|---|---|
| pattern-local 建仓（includes `**/*.jar`/excludes `secret/**`） | 200；GET 回显 `{"excludesPattern":"secret/**","includesPattern":"**/*.jar"}` | ✅ |
| PUT `.txt`（不含于 includes） | **409**，message 含双 pattern：`rejected deployment of 'a/t.txt': does not match includesPattern '**/*.jar' (excludesPattern 'secret/**')` | ✅ |
| PUT `secret/t.jar`（excludes 优先） | **409**：`matches excludesPattern 'secret/**'` | ✅ |
| PUT `a/ok.jar` | 201 | ✅ |
| GET excludes 命中路径 | **404** | ✅ |
| 默认仓（未配 pattern）`.txt` PUT | 201（M1~M3 行为不变） | ✅ |
| quotaBytes 回显 | `configuration.quotaBytes: 4096`（M3 configJSON 形态）；usage 端点 `{repo,usedBytes,quotaBytes}` | ✅ |
| quotaBytes 负值 | 400 `quotaBytes must not be negative (got -1; 0 means unlimited)` | ✅ |

### 2.3 搜索（W14/W15/W16/W36 + NFR-S24）— 20/20

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W14 | `?name=artifact` | results 含 `generic-local /acme/artifact.bin` 与 `other-local /mirror/artifact.bin`；缺 name → 400 | ✅ |
| W15 | `?sha256=` 同 blob 双路径 | 恰 2 node（跨仓去重可见）；无 checksum 参数 → 400（`requires at least one of sha256, sha1 or md5`）；`sha256=xyz` 非法 → 400 | ✅ |
| W16 | `repos=` 过滤 | `repos=other-local` → 仅 other-local | ✅ |
| W16/NFR-S24 | ci-bot ACL 零泄漏 | ci-bot（仅 other-local read）name/checksum 搜索结果均 `["other-local"]`；内容面 generic-local 403 / other-local 200 同源；admin 全见 | ✅ |
| W16/T-116 | admin-only 面 | ci-bot `GET /api/repositories` → 403、`GET /api/v1/health` → 403；admin → 200 | ✅ |
| W36 | 未实现族 404 | `/api/search/{props,users,artifactory,pattern,badge}` 全 404；`/api/system/storage/prune/{start,POST}`、`/api/system/storage/prune/status` 404；`/api/v2/security/permissions/x` 404；`/api/export/system`、`POST /api/import/system` 404（E-01 族） | ✅ |

观察 O-3：匿名关实例上未认证 search → **403**（无挑战头），同域 `/api/v1/audit`、`/api/repositories`、内容路径均 401——姿态不一致，无 AC 钉死（T-92 NB4 已挂「PRD 半句」），转 E2。

### 2.4 groups 与权限继承（W17~W21/W40 + FR-27-AC9）— 37/37

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W17 | PUT devs | **201 无 body**（Content-Type 无、body 空）；GET 列表 `["devs"]`；POST 改 description → 200；GET 单组 `{name,uri,description}` | ✅ |
| W17 | 边界 | body name≠路径 → 400；已存在再 PUT → 200 更新；`anonymous`/`_system_`/`BadName` → 400（纯文本层：reserved name / `[a-z][a-z0-9._-]*` 规则文案）；POST 不存在组 → 404；非 admin GET → 403 | ✅ |
| W18 | jane + email + groups | PUT 201；GET `{name,email:"jane@example.com",admin:false,groups:["devs"],realm:"internal"}` 无口令字段；`groups:["nope"]` → 400 逐字 `Unable to find group by name 'nope'.` | ✅ |
| W40 | 列表简形态不变 | `[{"name":"jane","uri":...,"realm":"internal"}]`（email/groups 仅单查端点） | ✅ |
| W19 | 组授权继承 | target devs-rw（groups devs r+w，`devs/**`）→ jane PUT `devs/w.bin` **201**；DELETE → **403**；`other/o.bin` PUT → **403**；GET → 200 | ✅ |
| W19b/NFR-S25 | 即时生效 | `groups:[]` 更新 201 后同刻 PUT `devs/w2.bin` → 403、GET 原 allowed 路径 → 403（无重启无窗口） | ✅ |
| W19c | 并集语义 | user 直接 read+delete + 组 write（union/**）：PUT 201 / GET 200 / DELETE 204 | ✅ |
| W20 | 删除保护 | 引用中 DELETE → **409** `referenced by permission target(s): devs-rw, devs-union-w`；删 target 后 DELETE → 200 纯文本；jane groups 联动清空 `[]` | ✅ |
| W21 | 有效权限视图 | `?permissions` → `{"uri":...,"principals":{"users":{"jane":["r"]},"groups":{"devs":["r","w"]}}}`——**key=主体名、value=字母集合（T-113 定案形态）**；virtual 仓 → 400（`only supported on local repositories`）；不存在路径 → 404 | ✅ |
| AC9 | 组无 admin 位 | jane（devs 成员）GET groups / GET audit / PUT group → 全 403 | ✅ |

### 2.5 审计（W22/W22b/W23/W23b/W39 + NFR-S21）— 32/32

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W22 | 过滤矩阵 | `actor=jane` 全 jane；`action=login.failed` ≥1（实 2）；since=now 只含新事件、until=昨 → 0（闭开区间）；limit=1001 → 400、1000 → 200、0 → 400；ci-bot → 403；未认证 → 401 | ✅ |
| W22b | 分页 | total 46 条；limit=2 + nextCursor 追随两次 → 三页 6 行 id 全异、倒序（46..41）；cursor 不透明 | ✅ |
| W23 | 词表 | deploy×45 / delete×6 / repo.create×16 / group.create×2 / group.update×2 / permission.create×5 / gc.run×10 / quota.exceeded×8 / export.run×1 / login.success×5 / login.failed×2 全部可查 | ✅ |
| W23b | 脱敏 | 全量导出 grep：admin/jane/ci-bot 口令字面量 0、token 明文 0（新签 token 对照）、Authorization 0；detail 为对象（quota.exceeded detail `{actor,path,quota,repo,used}`） | ✅ |
| W39 | append-only | cookie（先过 CSRF）与 Basic 双形态：DELETE/PUT `/api/v1/audit`、`/api/v1/audit/1`、POST → 全 **404**（路由层无写面） | ✅ |

### 2.6 GC（W24/W25/W25b + FR-30-AC5）— 16/16

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W24 | 孤儿 dry-run | 唯一内容上传→DELETE → `{"candidateCount":1,"candidateBytes":64,"deletedCount":0}`；stats blobs 不变（6→6）；非 admin 403 / 未认证 401 | ✅ |
| W24 | grace 缺省 | 无 graceHours 字段（config 缺省 24h）→ 刚删孤儿 **0 候选**（mtime 基准生效） | ✅ |
| W24 | 非法体 | `graceHours:-1` → 400（0..876000 边界文案） | ✅ |
| W25 | apply | `{"candidateCount":2,"deletedCount":2}`；stats 8→6（降量==deletedCount）；被引用 keep.jar GET **200 幸存**；再 dry-run 0 候选 | ✅ |
| W25b | **三方互斥** | ①真 export 进程持锁（400MB 数据 + SIGSTOP 冻结）→ REST gc **409** message 含 `export in progress` + holder `pid/op`；并发第二 export CLI exit 1；释放后 export exit 0。②`op=gc` 持锁 → export CLI exit 1、import CLI exit 1、REST gc 409（`another gc run is in progress`）。③`op=import` 持锁（目标目录）→ gc CLI exit 1、export CLI exit 1。错误统一含 `data directory is locked` + last holder 记录 | ✅ |

手法注明: ①为真实进程（flock 持有期间 SIGSTOP 冻结，锁仍持有）；②③ CLI gc/import 在本机数据量下快于轮询窗口，按 T-96 先例以 python flock 按 `.maintenance.lock` 文件协议写入 `pid=<n> op=<op>` 首行仿真持有者（锁原语为跨进程 flock，被测的正是该协议）。`gc.run` 审计 REST 路径 actor=admin ×N（CLI 路径归 T-114 已验）。

### 2.7 配额 enforcement（W26/W26b/W26c/W27 + FR-31-AC5 + T-95 B1 + NFR-S23）— 30/30

| 断言 | 实测 | 结果 |
|---|---|---|
| W26 | 超限 413 双值 | tiny(quota 1024) PUT 800B → 201；再 PUT 800B → **413** `quota exceeded: used 800 of 1024 bytes; the write to 'b.bin' needs 800 more bytes` | ✅ |
| W26b | 原子性 | 被拒路径 GET 404；usage 精确 `usedBytes:800`；stats 不变 | ✅ |
| T-95 B1 | 顶点幂等重传 | 同路径同内容 re-PUT（used==quota 临界）→ **201 非 413** | ✅ |
| AC5 | 秒传超限 | `X-Checksum-Deploy` 引用已有 blob 到新路径 → 413 | ✅ |
| AC5/T-111 | docker mount 413 | mount `sha256:25f1d6b…`（3.6MB）入 tiny-docker → **413 verbatim**（docker spec 信封 `{"errors":[{"code":"DENIED","message":"…quota exceeded: used 117 of 1024…needs 3630321 more…"}]}` + WARN `cross-repo mount refused`）；对照非配额 docker-local2 → 201 且 blob 可读。真 docker CLI push 亦触发同臂（见 W26c） | ✅ |
| W26c-docker | 真客户端 | dind `docker push tiny-docker/t103img:v1` → **exit 1**，`denied: Repository 'tiny-docker' quota exceeded: used 117 of 1024 bytes…`；服务端 413 + WARN×2；`_catalog` 无 tiny-docker、tags 404、manifest 404（大 blob/manifest 零残留）；控制组 push/pull docker-local 正常 | ✅（残留观察 D-1） |
| W26c-npm | 真客户端 | `npm publish` 2.0.0（8.4KB）→ **exit 1 `E413`**；服务端 WARN + 413；packument 404、usage 0（零残留）；控制组 1.0.0 publish exit 0 | ✅ |
| W26c-mvn | 真客户端 | `mvn deploy` 2.0.0 → **exit 1**（`status code: 413`）；jar 路径 404（残留观察 D-2：pom+metadata 先落，usage 639）；控制组 1.0.0 exit 0 | ✅ |
| W26c-pypi | 真客户端 | `twine upload` 2.0.0 → **exit 1 `HTTPError: 413`**；simple index 404、usage 0（零残留）；控制组 1.0.0 exit 0 + index 200 | ✅ |
| W27 | 读删不受限+回落 | 超限后 GET a.bin 200、DELETE 204、usage 800→0、再传 800B → 201、usage 回 800 | ✅ |
| GE-06 | usage ACL | 有 read 用户 200 / 无 read 403 / 不存在仓 404 | ✅ |
| 词表 | quota.exceeded | detail `{actor,repo,path,used,quota}` 落审计（b.bin/c.bin 各 1 + 协议面 8 条） | ✅ |

### 2.8 备份恢复（W28/W28b/W30/W30b/W31/W32 + NFR-S22）— 35/35

导出前构造：四协议制品（generic/docker/maven/npm/pypi）+ jane∈devs + devs-rw target + 有效 token T1 + quota 仓 tiny（used 800）+ 带凭据 remote 仓（mock 上游已缓存一文件）。

| W | 断言 | 实测 | 结果 |
|---|---|---|---|
| W28 | 在线 export | serve 运行中 exit 0；`manifest.json`（formatVersion/createdAt/binflowVersion/blobCount=30/totalBytes=423091501/metadata{file,sha256}/graceNote）；`shasum -a 256 metadata.db` == manifest.metadata.sha256（aba9b4a0…）；layout `blobs/ metadata.db manifest.json`；`export.run` 落源实例审计 | ✅ |
| NFR-S22 | 产物权限 | 目录 `drwx------`(0700)、metadata.db/manifest.json `-rw-------`(0600)；manifest grep 口令字面量 0 | ✅ |
| W28b | 引用完备+窗口 | manifest 引用 30 blob 全存在（missing=0）；export 后新上传 sha `21eb3332…` ∉ manifest | ✅ |
| W30 | 全量保真 | `import --verify full` exit 0（30/30 rehash）；恢复实例：四协议 sha256 **pre/post 全 MATCH**（generic f18a386f…/npm 9ee13e07…/maven 44dcbf31…/pypi 41c07a7f…/docker digest sha256:20eaf17b…）；jane session 登录 200 + 组授权矩阵复验（GET 200/PUT 201/DELETE 403/越权 403）+ email/groups 回显；token T1 Bearer → 200；quota 仓 usage 800/1024 + 配置回显；审计历史全量可查 + `import.run` ×1 | ✅ |
| W30b | 凭据恢复链 | 无 `BINFLOW_REMOTE_CREDENTIALS_KEY` 启动 → **fail-fast exit 2**，panic 文案点名 env（`stores encrypted credentials but no master key is configured: set BINFLOW_REMOTE_CREDENTIALS_KEY (base64 of exactly 32 bytes)`）；设 key 后 serve 正常、remote 代理链 GET 200 字节一致且**上游计数冻结**（缓存随备份恢复） | ✅ |
| W31 | 损坏矩阵 | 非空目标（marker）→ exit 1 + 明示；删被引用 blob → exit 1 + 点名 sha + 目标无数据残留；`blobCount+1` 篡改 → exit 1（31≠30 entries） | ✅（锁文件残留观察 O-5） |
| W32 | mtime 保留 | 源 dataA vs 恢复 fresh-data：共同 35 blob **mtime 0 偏差**（export→restore 亦 0；源第 36 个为 export 后新上传，W28b 已证不入备份）；`--verify full` 30/30 rehash | ✅ |

### 2.9 附带记录项（非本票门槛）— 8 项

| 项 | 值 |
|---|---|
| NFR-P16 冷启动（T-105 正式） | fresh 空库 3 次：**0.177 / 0.145 / 0.149 s**（<2s） |
| NFR-P19 export 吞吐 | 400MB 在线：**828.1 MiB/s**（7 blob）；全量 423MB：**376.0 MiB/s**（30 blob）——记录不设门 |
| 二进制/SPA | 19.45MB / gzip 105,840 B（=T-99 记录，预算 5MB 的 2.0%） |
| import 耗时 | full rehash 30 blob 1.10s |

## 3. 缺陷与观察（转 conductor）

无 P0/P1。建议缺陷登记：

| # | 级 | 项 | 事实 | 建议 |
|---|---|---|---|---|
| D-1 | P2 | docker 配额 push 的「先落层残留」 | tiny-docker(1024) push：117B config 经 cross-repo mount 先成功落 node（usage=117），随后 3.6MB 层 413；push 整体失败后该 node 无 manifest 引用、catalog 不可见、API DELETE 不可达（blob node 路径 404），仅能整仓删除回收 | 按每写原子性接受并 PRD 注记（E3），或立小票：无 manifest 引用的 docker blob node 纳入 GC 候选（需 docker_refs 感知的 mark 补集） |
| D-2 | P2 | maven deploy 的 pom/metadata 先落 | tiny-maven deploy 413 后：jar 404（拒绝原子）但 pom+校验和+maven-metadata.xml 先落（usage 639、metadata 200）；PRD 对 maven 仅要求 curl 抽验 413，已满足 | 同 E3 一并注记；如需收紧需 deploy 会话级预留（M5+ 评估） |
| O-3 | 观察 | 未认证 search 403 姿态 | 匿名关实例：search 无凭据 → 403（无挑战），audit/repositories/内容路径 → 401 | PRD 定姿态（E2），建议统一 401+挑战 |
| O-4 | 观察 | `?permissions` uri 形态 | `http://host/<repo>/api/storage/<path>` vs item info uri `http://host/<repo>/<path>`（M1 形态）拼接不一致，纯观感、无 AC | 酌情小票统一（P2） |
| O-5 | 观察 | import fail-fast 留 `.maintenance.lock` | 三种失败路径目标目录均残留 0 字节锁文件（数据零残留） | PRD 措辞放宽（E5）或失败路径清理锁文件 |

## 4. PRD 勘误建议（E 编号，供 conductor 转 product-manager）

| # | 位置 | 现文 | 建议 |
|---|---|---|---|
| E1 | §5.4 W01 `grep -c 'id="root"' # 1` | 真 SPA 构建产物 shell 含 2 处 `id="root"`（占位 shell 为 1） | 改 `grep -q 'id="root"'`（意图为挂载点存在） |
| E2 | FR-26/§5.2 SR-01 | 未钉未认证（匿名关）搜索的姿态 | 定案：建议 401+WWW-Authenticate 与 /api 家族一致（现实现 403，T-92 NB4 同源） |
| E3 | FR-31-AC3「docker push 超限 → manifest/blob 零残留」 | 多层 push 中先成功的小层（含 mount config）会保留 | 注记「零残留」语义 = 被拒写本身原子 + manifest 不落；先落且配额内的层属每写语义保留（D-1/D-2） |
| E4 | FR-23 cookie `Path=/binflow` × ux R10「docker tags」例 | cookie 作用域结构性排除根级 `/v2`（curl 实证：强制携带则 200，机制等价） | 注记控制台 docker 视图需经 `/v2/token` 换取或消费既有 Bearer；ux R10 例改 npm packument/pypi simple |
| E5 | §5.4 W31 注释「/tmp/fresh2 为空」 | fail-fast 后残留 0 字节 `.maintenance.lock` | 改「无数据残留（锁文件除外）」或实现侧清理 |

## 5. 服务端卫生（5xx 零容忍）

- 三实例（A/TTL/B）+ no-key fail-fast 日志合并：**`"status":5xx` 0 条、`"level":"ERROR"` 0 条**
- 唯一 panic：本 QA 以非 base64 值设 `BINFLOW_REMOTE_CREDENTIALS_KEY` 的 fail-fast（exit 2，文案指明修复动作）——ADR-0012 设计内
- WARN 全部设计内：quota 拒绝 ×8、mount refused ×2、凭据拒绝 ×6（故意错口令探测）、SSRF loopback 拒绝 ×1（allowPrivateUpstream 配置前）、remote fetch ×1

## 6. 环境与清理

- 实例 A/TTL/B、mock 上游 9199 已停，端口 8310~8313/9199 零监听；dind 容器 t103-dind 已删、为本票拉取的 `docker:27-dind` 镜像已 rmi（宿主 Docker Desktop/daemon.json 零改动）
- worktree `/tmp/t103/wt` 已移除；`/tmp/t103` 全目录（含 admin 口令、remote key、token、cookie jar）已删除，无凭据写入任何提交文件；主仓工作树的 web/ 改动全部来自并行 FE 票（T-101/T-102），本票零代码改动
- npm 全程 HOME=/tmp/t103 + 独立 userconfig；pip/twine 隔离 venv；mvn 独立 settings.xml（deploy 阶段插件解析可能读过用户 ~/.m2 缓存——只读缓存无项目文件触碰，如实注明）；用户 ~/.npm、~/.gradle、~/.cache/pip 零触碰

## 7. 遗留给 T-104/T-105 的输入

- W09~W14b/W33~W35 浏览器面归 T-104（本票 curl 面已全部就绪：testid 无关的后端断言均可复用）；W37 性能正式口径与 NFR-P17/P18 造数归 T-105（本票冷启动/吞吐已留记录值）
- D-1/D-2 残留语义若 conductor 裁定收紧，将影响 FR-31-AC3 断言写法（先裁 E3 再派）
- E4 若采纳，console-ux 的 docker 仓视图取数路径需在 T-100 前知会（FE docker 两级 catalog→tags 若直连 /v2 将吃不到 session cookie）
