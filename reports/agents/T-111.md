# T-111 工作日志 — docker adapter StatusError-verbatim 渲染臂

- ticket: T-111 [P1] docker adapter StatusError-verbatim 渲染臂（M4 批 4；T-95 配额票遗留①）
- role: dev-registry-adapter；area: internal/adapter/docker（+其测试）
- 日期: 2026-08-20
- 状态: done（自测全绿；真机 curl 复现 413/409；两项跨包失败为并行票进行中编辑所致，见「环境备注」）
- 输入: reports/agents/T-95.md 遗留①、四 adapter verbatim 先例（generic T-66 / maven·npm·pypi T-82）、docs/prd/milestone-4.md W26c、docs/reverse/docker-registry.md §1（错误信封）

## 做了什么

补齐 docker /v2 面对 `*repo.StatusError` 的 verbatim 渲染臂——T-95 遗留①定位的两处：

1. **`writeManifestPutError`**（manifest.go）：manifest PUT 的两步落仓（step-1 `svc.Put`、step-2 `svc.PutManifest`）返回 StatusError 时按**原状态码 + 原文案**渲染（此前落 default 臂 500 UNKNOWN）。臂放在 switch 最前，与四 adapter「verbatim first」先例一致；鉴权类拒绝从不以 StatusError 形态到达此面（plain sentinel，走既有 challenge 臂），前置安全。
2. **blob 注册失败支路**（uploads.go `writeBlobCreated`）：Commit 成功后 `PutLandedBlob` 注册被治理拒绝（quota 413 / pattern 409）时 verbatim 渲染（此前 500 UNKNOWN）。blob 物理已落盘、node 未写——verbatim 的 413/409 即「push 未落成」的诚实信号（W26c 契约）；解除拒绝后重推安全（Commit 去重 + 幂等重传豁免补齐行）。日志 WARN（policy 拒绝非故障，T-41 卫生），原 busy→denied→500 顺序保持，StatusError 臂插在 busy 之后（busy 不可能是 StatusError）。

### 信封 code 决策（注明裁决）

`writeVerbatimStatusError`（errors.go 新增）+ `specCodeOfVerbatim` 状态→code 映射：

- **413（quota）/409（pattern）→ `DENIED`**——官方码表中语义最贴（"the access controller denied access for the operation on a resource"＝策略拒绝）；adapter 内一致性：DENIED 本就是本 adapter 全部拒绝类的信封码。
- **TOOMANYREQUESTS 弃用**：规范把它绑 429 限流，配 413 是 status/code 错配。
- **不造自定义码**：无客户端消费 QUOTA_EXCEEDED 之类的自造码；逆向规格 §1 行 3 已示注册实现自由发散（Artifactory 的 SIZE_INVALID/NO_TAGS_FOUND），有官方码可用时不扩面。
- 其余状态沿用既有信封码：405→UNSUPPORTED（并携带 StatusError 自带 Allow 头）、401→UNAUTHORIZED、403→DENIED、未映射→UNKNOWN。
- verbatim 契约本身（status+message）不受映射影响：413 按原状态码出、message 逐字。

### 顺带（area 内，注释级）

- `tryMount` 的 PutFromBlob 失败注释补明：治理拒绝（413/409）**保持降级 202**（mount 是优化，spec 降级即正确路径），回退上传自身在 finalize 处出诚实 verbatim 拒绝——行为零改动，只把决策写明。
- **未动读面**（`writeBlobReadError`/`writeManifestReadError`）：quota 只在写面；pattern 的下载臂在 repo 层返回与 miss 逐字相同的 ErrNodeNotFound（service.go:263），docker 面既有 404 BLOB_UNKNOWN/MANIFEST_UNKNOWN 臂天然正确（新增测试 TestGovernanceRefusedReadsServe404 钉死）。remote docker 仓 FetchError 型 StatusError 的读面渲染是另一类缺口，登记遗留。

## 改了哪些文件

- internal/adapter/docker/`errors.go`：+`writeVerbatimStatusError`、+`specCodeOfVerbatim`（含决策注释）、+repo import
- internal/adapter/docker/`manifest.go`：`writeManifestPutError` verbatim 臂 + WARN 日志
- internal/adapter/docker/`uploads.go`：`writeBlobCreated` 注册失败 verbatim 臂 + WARN 日志、`tryMount` 注释、+repo import
- internal/adapter/docker/`governance_render_test.go`（新）：注入层（信封形状/code 映射/Allow 携带）+ 真栈层（真 repo.Service + 治理配置）
- internal/adapter/docker/`token_test.go`：`fakeUsers.UpdateProfile` 桩——**被迫的 test-only 编译修复**：并行票正给 metadata.UserStore 加 UpdateProfile，波及本包 fake，其扫改尚未到 docker 包（T-95 同款机械波及，请 reviewer 知悉；若并行票随后补桩会冲突则以其为准）

## 实际运行过的命令与输出摘要

- `go build ./internal/adapter/docker/` / `go vet ./internal/adapter/docker/` / `gofmt -l internal/adapter/docker/` → 通过/干净/空
- `$(go env GOPATH)/bin/golangci-lint run ./internal/adapter/docker/...` → `0 issues.`
- `go test -count=1 ./internal/adapter/docker/` → **ok** 24.3s（全量）
- `go test -race -count=1 -run 'TestVerbatim|TestGovernance' ./internal/adapter/docker/` → ok 4.6s
- 新用例（-v 摘录）：TestVerbatimRenderBlobRegistration（quota 413/pattern 409 两子例）、TestVerbatimRenderManifestPut（step-1 413 / step-2 409 / 405+Allow 三子例）、TestSpecCodeOfVerbatim、TestGovernanceQuotaDockerPush413、TestGovernancePatternDockerPush409（blob 409/manifest 409/未配置回归三子例）、TestGovernanceRefusedReadsServe404 → 全 PASS
- `go test -count=1 -run 'TestV2|TestDocker|TestManifest|TestBlob' ./internal/httpapi/` → ok 26.3s（跨包 docker 面回归）
- `go build ./...`、`go vet ./...` → 通过（此刻工作树状态）

### 真机 curl（scratch：binflow-server serve，127.0.0.1:18091，docker 仓 tiny quotaBytes=1024）

```
[1] config blob 117B POST ?digest=          -> 201
[2] manifest v1 PUT（放得下）                -> 201
[3] layer 300B POST ?digest=                -> 413
    {"errors":[{"code":"DENIED","message":"Repository 'tiny' quota exceeded: used 783 of 1024
     bytes; the write to 'app/blobs/10bf7a…' needs 300 more bytes.","detail":null}]}
    （响应头：Content-Type: application/json + Docker-Distribution-Api-Version: registry/2.0）
[4] manifest v2 PUT 超限                     -> 413（…used 783 of 1024 bytes; … needs 335 more bytes…）
[5] /v2/tiny/app/tags/list                  -> 200 {"name":"tiny/app","tags":["v1"]}   ← W26c 零残留
[6] 拒绝 layer GET                          -> 404
[7] /api/v1/storage/usage/tiny              -> {"usedBytes":783,"quotaBytes":1024}（117+2×333 双 node 计数）
[8] picky 仓（excludesPattern **/blobs/**）blob POST -> 409
    {"errors":[{"code":"DENIED","message":"Repository 'picky' rejected deployment of
     'app/blobs/8445…': the path matches excludesPattern '**/blobs/**' (includesPattern '**/*')."}]}
    拒绝 blob GET -> 404
[9] free 仓（未配置治理）blob POST -> 201 / manifest PUT -> 201 / tags ["v1"]  ← 回归
```

修复前同序列（T-95 真机记录）：[3]/[4] 出 500 UNKNOWN、message 携带 quota 文案——本票后状态码与文案均 verbatim，W26c「服务端 413」断言可过。Docker Desktop 直达宿主 127.0.0.1 受限（改 daemon 配置超出边界），curl 驱动 /v2 面为 T-96/T-103 认可口径。

## 遗留 / 需上游确认

1. **remote docker 仓读面的 FetchError 型 StatusError**（`writeBlobReadError`/`writeManifestReadError` 的 default 500）：unound 族已因 cause 包裹 ErrNodeNotFound 走 404 正确臂；上游 401/5xx 等非 miss 态仍 500。非本票范围（治理只写面），建议随 remote-docker 代理票收口。
2. **token_test.go 的 UpdateProfile 桩**与并行 profile 票的机械扫改可能撞车（先到先得，后到者以重复定义编译错显式暴露）。
3. 跨包 sweep 此刻的红：`internal/auth [build failed]` 与 `httpapi TestCompatPlaneE26/POST_/binflow/api/security/users/someone`（body 非 errors[] 信封）——两者均在并行票修改中的文件（internal/auth/*、internal/httpapi/security.go），与 docker /v2 面无涉；本票包内与 httpapi docker 面全绿。
4. W26c 的「docker 客户端报错」半句仍待真 docker CLI（T-103 口径下宿主受限）：curl 已证服务端 413 + 信封形状，docker CLI 侧对 413 DENIED 的显示不再有服务端阻碍。
