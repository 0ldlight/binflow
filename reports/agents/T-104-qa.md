# QA 报告 T-104 — Playwright 浏览器矩阵（FR-33-AC1/AC4，剧本 3）

- role: qa-engineer
- 日期: 2026-08-21
- 票据: T-104 [P0] QA：Playwright 浏览器矩阵（AC 全文见 reports/agents/T-88.md T-104 节）
- 被测对象: commit **77718cc**（FE 冻结基线：T-98~T-102 全 done + T-100 B1 残余臂修复）。独立 git worktree `/tmp/t104/wt` 检出；`make console`（SPA gzip **151,255 B**，与 T-100 修复段二记录一致）→ `make build` 真二进制 **19.57 MB**
- 环境: darwin/arm64（Apple silicon）、node 22.23.2、Playwright **1.62.1**（chromium-1234 / WebKit 内置 / Firefox 153.0，后两者本机新装 `npx playwright install webkit firefox`）；scratch 实例 `127.0.0.1:18120`（`BINFLOW_SERVER__LISTEN` + 独立 data dir + `BINFLOW_ADMIN_PASSWORD` + 匿名关）；docker 播种腿 = 宿主 Docker Desktop 之上 `docker:dind` 容器（`--insecure-registry=host.docker.internal:18120`，宿主 daemon 零改动，T-103 同款手法）
- 断言锚口径: console-ux **v1.1 §10.2/§10.3** + 以 `web/src` 实落锚为准（§10.3 之外实落锚差异清单见勘误 E3，供 T-118 v1.2 回写；本 QA 断言全绿 = 与代码无冲突）

## 总结论: **PASS**（附 1 P1 + 1 P2 缺陷登记，4 条勘误建议）

- **Chromium 主矩阵 47/47 全绿 exit 0**（全量 spec：既有 7 文件 32 例 + 本票新增 4 文件 15 例；`--workers=1`，T-100 全量并行超时教训）——W09→W10/W10b/W11→W12/W12b/W12c/W12d/W13→W14b→W33/W33b/W33c→W34→W35 全链走通
- **零 5xx**：全程 3,360 请求，唯一 1 条 5xx 为 `client_disconnect:true`（`context_canceled`——artifacts.spec B1 例 route 放行 abort 请求的既有形态，T-100 已定性）；ERROR 级日志 0、panic 0
- **WebKit/Firefox 抽查（P2 观察）**：登录链双引擎 ✅；树/上传链 ❌——根因为 **D-104-1（P1）懒加载 chunk 资源 URL 段错位**，Chromium 同根因表现为懒页自有 CSS 静默丢失（不阻塞功能）；按 AC③ 记录不设门槛
- 性能记录：1GB UI 上传服务进程 RSS 增量 **0~28 KB**（预算 <256MB，FR-25-AC6 大幅达标）；SPA 冷加载（fresh context）**~389 ms** 可见 / DCL 32~33 ms；50 并发登录零串号（50/50 身份回读正确 + session 值全互异）

---

## 1. AC 逐条结论（T-88 T-104 节）

| # | AC | 结果 | 证据摘要 |
|---|---|---|---|
| ① | Chromium 全链 exit 0 且零 5xx（W09→W10/W10b/W11→W12/W12b/W12c/W12d/W13→W14b→W33/W33b/W33c→W34→W35）；testid 按 console-ux 终稿；实例独立防污染 | ✅ | §2 W 明细：47/47 全绿（2.6m）；3,360 请求非断连 5xx=0；独立 scratch 实例（18120 段、fresh data dir、与 T-103 串行）；断言锚全部以 v1.1 §10 + 代码实落为准 |
| ② | FR-25-AC6：UI 上传 1GB 服务进程 RSS 增量 <256MB；50 并发 Playwright 登录无 session 串号抽查（NFR-P20 P1） | ✅ | §4：clean 轮 baseline=peak=222,580KB（**delta 0KB**）、warm 轮 delta 28KB；1GB sha256 三方对账一致（本地 shasum == 浏览器流式 == 服务端 item info）；50 独立 cookie jar 并发登录 50/50 whoami 身份正确 + `binflow_session` 值 50 个全互异 |
| ③ | WebKit/Firefox 三链（登录/树/上传）P2 观察（成败记录不阻塞）；报告归档 | ✅（观察含 ❌） | §3：登录链 webkit/firefox 双过；树/上传链双失败——根因 D-104-1（严格 MIME 下 vitePreload 拒绝 → 懒加载路由整页空白），录缺陷不改结论（AC 原文「成败记录不阻塞」）；本文件即归档 |

## 2. W 序列浏览器面明细（Chromium 主矩阵）

| W | 断言 | 结果 | 证据（用例 → 关键输出） |
|---|---|---|---|
| W01/W02 | SPA shell/深链/挂载点 | ✅ | console-smoke ×2：`#root` attached、深链 fallback 200 |
| W03/W04/W05（浏览器腿） | 未登录守卫 `?return=`；错凭据行内 401 同文案不泄露；登录落壳 | ✅ | auth-shell ×5：重定向 `/login?return=%2F`、`login-error`「用户名或密码错误」、`session-user`=admin、主题切换、404 留壳、登出吊销后守卫再拦 |
| W09 | 登录全周期（含设置页改密行内错误） | ✅ | auth-shell ×5 + crossbrowser chain1（正负双腿） |
| W10 | UI 表单建 `docker-ui-local`（local+docker）→ 列表行 + REST `packageType=="docker"` | ✅ | t104-supplements #1（既有 repositories.spec 覆 generic/maven，本票补 docker 原文仓型）；docker 仓树页 `tree-commands` 在、`tree-upload` 无（P6 命令块替代） |
| W10b | UI 编辑 remote url → REST 回显新值；password 永不回显 | ✅ | t104-supplements #4：url A→B 往返回显、创建/编辑前后响应文本零命中 secret（补既有 spec 只预填不保存的缺口） |
| W11 | UI 删非空仓：不勾 deleteContent → 400 原因呈现；勾选 → 成功 + 内容面 404 | ✅ | t104-supplements #3（docker-ui-local 非空两段流：`holds N node(s)` 行内 → 勾选成功 → storage 404 + `/v2/.../tags/list` 404）；repositories.spec #1 同构（generic） |
| W12/W12b | 树浏览/面包屑/size 列/建目录 | ✅ | artifacts #1（+左树同步、目录递归删除） |
| W12c（P1） | docker 仓树可见 manifest node；maven 仓 GAV 目录 | ✅ | docker：t104-supplements #3——真 docker 客户端（dind push，回执 `digest: sha256:f7df…`）→ 树 `t104img → manifests → <64hex>` 行命中（特化两级视图为 T-100 遗留 1 的 P1 登记项，统一路径树形态判过，见 E1）；maven：artifacts #4（GAV 四层钻取 + item info 200） |
| W12d | ro 用户 UI 删除 → 403 原因行内 + 指引；node 仍在（curl 200） | ✅ | artifacts #2（delete-403 腿）+ **本票补 upload-403 腿**（t104-review-leftovers #1，T-100 review NB③）：行内 `HTTP 403` + `permission denied` + 「当前会话对该路径没有所需权限（写入需 write…）」、非 admin 无权限页链接、被拒路径 404 / keep.bin 200 |
| W13 | UI 上传 → 树新行；sha256 双源对账 | ✅ | artifacts #1（✓checksum 一致徽标 + node:crypto == 服务端） |
| W12a（UI 面） | 409 pattern / 413 quota 原样呈现 | ✅ | artifacts #3 |
| W14b | 搜索框 → 结果表 → 行点击跳树定位 | ✅ | artifacts #5（防抖/过滤收窄/`?focus=` 定位） |
| W33 | UI 三步流（组→用户入组→target 双栏矩阵 + 模式测试器 + diff 确认）+ API 三实体对账 | ✅ | security #1（含 exclude 优先判定、`授予组 … read/write` diff 行） |
| W33b | UI 移出组 → 第二上下文同会话 PUT 即时 403 | ✅ | security #2（无重登；自有 read 存活 GET 200） |
| W33c | 删被引用组 → 409 + target 名呈现（含点名解析）；解除后可删 | ✅ | security #3/#4 |
| W34 | 审计页过滤 → 行集与 REST 同结果集 | ✅ | governance #1（repo/action/时间窗 + keyset 续页 + B1 晚到页丢弃）+ **本票补 actor 腿**（t104-supplements #5）：专属 actor 3 次 UI 登录 → UI 行数 == REST `?actor=` events 数且首行命中 |
| W35 | GC 页 dry-run → 二次确认 apply → deleted；stats/审计对账 | ✅ | governance #2（grace 变更即过期 apply 关回、`gc.run` detail.deletedCount 对账、再 dry-run 0 候选空态） |
| （注入）review B1 主路径 | uploading 相关闭泵：PUT 计数恒 1、排队 404、usage ≤8B | ✅ | artifacts #6（既有例复跑全绿） |
| （注入）review B1 残余臂 | **hashing 相关闭零新 PUT**（修复 77718cc 首证） | ✅ | t104-review-leftovers #2：128MB 文件、行 0「哈希中」时点关闭 → 9s 观察窗 `puts===0`、内容面 404、`usedBytes===0` |
| （注入）sameSnapshot | 双 pattern 逆序加回集合等价（T-101 复核遗留） | ✅ | t104-review-leftovers #3：`[a,b]→[b,a]` === true、三元素轮换 true、真变更 false（判别力对照） |
| （注入）W23b UI 腿 | 审计页 DOM 零秘密 | ✅ | t104-supplements #6：login.failed 行渲染下错误口令字面量 0 命中；未过滤首屏 token 明文 0 命中（token 签发本身不落审计——D-104-2） |

## 3. 浏览器矩阵（FR-33-AC3/AC4）

| 引擎 | 范围 | 结果 |
|---|---|---|
| Chromium（Chrome for Testing 151.0.7922.34，playwright 1.62.1） | **全量**（W 序列 + 注入断言 + 性能，47 例） | **47/47 ✅**（2.6m，workers=1） |
| WebKit（playwright webkit-2336，内置 Safari 内核） | 三链（登录/树/上传） | 登录 ✅；树 ✘、上传 ✘——懒加载 CSS 严格 MIME 拒绝 → `Unable to preload CSS` pageerror → 路由空白（D-104-1） |
| Firefox 153.0（playwright firefox v1538） | 三链 | 登录 ✅；树 ✘、上传 ✘——同根因（strict MIME + link onerror → vitePreload 拒绝动态 import） |

**D-104-1 复现（任一引擎）**：`make console && make build` 起实例后

```
curl -s -o /dev/null -w '%{content_type}\n' http://127.0.0.1:8080/binflow/ui/assets/<tree-*.css>   # text/html（SPA fallback 应答 shell，应为 text/css）
curl -s -o /dev/null -w '%{content_type}\n' http://127.0.0.1:8080/binflow/assets/<tree-*.css>      # text/css（正确段）
```

DOM 证据（Chromium，树页）：`document.styleSheets` = index.css **218 规则生效** + `tree-CoCedvEL.css` **0 规则**（MIME 阻断）；serve 日志全程 `/binflow/ui/assets/*.css` 命中 40 次（200 但 bytes_out=497 = shell HTML）、另有 ~20 个懒 JS 的 `/binflow/ui/assets/*.js` 死预取（AppShell/TreePage/useAsync 等，各 497B shell）。

## 4. 性能记录（正式口径归 T-105）

| 项 | 实测 | 门/口径 |
|---|---|---|
| 1GB UI 上传服务进程 RSS | clean 轮 baseline 222,580KB = peak 222,580KB（**delta 0KB**）；warm 轮 delta 28KB | <256MB（FR-25-AC6）✅；浏览器侧 4MB 分片哈希 + 服务端流式落盘双确认（1GB 全程 ~26s 含浏览器哈希） |
| SPA 冷加载（fresh context ×3） | tVisible 388/387/389 ms；DCL 33/33/32 ms；load 33/33/32 ms | <3s（FR-33-AC3，记录）✅ |
| 50 并发登录 | 50/50 login 200 + whoami 200；身份回读全对；session 值 50 全互异 | NFR-P20 P1 无串号 ✅ |
| 体积 | SPA gzip 151,255 B；二进制 19.57 MB | ≤5MB / 40MB 预算（记录） |

## 5. 缺陷登记（conductor 派修；本票零代码改动）

| # | 级 | 摘要 | 复现/证据 | 期望 vs 实际 | 修复方向（dev 定） |
|---|---|---|---|---|---|
| D-104-1 | **P1** | 懒加载 chunk 的运行时资源 URL 指向 `/binflow/ui/assets/**`（SPA fallback 以 `text/html` 应答）——Chromium 下懒页自有 CSS **0 规则生效**（tree.css、api.css——树/治理/搜索族页面缺局部样式）+ 每次导航 ~20 个 497B 死 shell 预取；**WebKit/Firefox 整路由空白**（FR-33-AC4 树/上传链失败根因） | §3 复现块 + serve 日志（`/binflow/ui/assets/tree-CoCedvEL.css` 200×497B=text/html）；Chromium styleSheets 实测 0 规则；机制自 T-89 relink-assets（只重写 index.html）即存在，CSS 承载懒 chunk（tree/api）自 T-100/T-102 起进构建故受影响面随之扩大——e2e 只断言 DOM 不断言样式，故至今未暴露 | 懒 chunk CSS 以 `text/css` 生效、跨引擎可达 | ① console handler 在 `/binflow/ui/assets/**` 段同样服务指纹资产（服务端窄改，零构建变更，最小爆炸半径）；② Makefile relink-assets 扩到 JS chunk 内资源映射；③ vite `base`/`renderBuiltUrl` 构建期定案。修复后 WebKit/Firefox 三链应整体转绿 |
| D-104-2 | P2 | token 签发/吊销**不落审计**：PRD FR-29 词表把 `token.issue`/`token.revoke` 列为「M1 既有」，实现无 append 路径（internal/httpapi/security.go 签发/吊销 handler 无审计调用） | `curl -su admin:pw -X POST $BASE/binflow/api/security/token -d 'grant_type=client_credentials'` → 200 + access_token；随后 `GET /api/v1/audit?action=token.issue` → **0 events**（两次实测） | 词表全集可查且留痕 vs 安全凭据签发无审计痕迹 | PM 勘误 E2（改词表口径）**或**补小票给 issue/revoke 各加一行 Append（detail: actor/token_id/expiry，token 明文禁入——W23b 口径） |

观察（不立项）：O-104-1 非 admin 打开 docker/npm/pypi 仓见 generic 上传入口（T-100 遗留 6 已登记，本票 upload-403 腿在 generic 仓验证同一 403 管线成立）；O-104-2 W12c 协议特化两级视图为 T-100 遗留 1 P1 登记项，本票按统一路径树断言 manifest node 判过（E1）。

## 6. 勘误建议（E 编号，转 PM/ux/conductor）

- **E1**：PRD FR-33-AC1 链含 W12c——「两级 catalog→tags 特化视图」已按 T-100 遗留 1 降级 P1；建议 §5.4 注记「W12c 浏览器断言 = 统一路径树可见 manifest/tag node（特化视图 P1 债务）」，防后续 QA 按字面复测再判。
- **E2**：PRD FR-29 动作词表「M1 既有 …`token.issue`/`token.revoke`…」与实现不符（D-104-2）——勘误词表来源或转补票，二选一。
- **E3**：console-ux §10.3 预定锚 vs 实落锚差异（T-118 v1.2 回写素材；本 QA 全部以代码实落为准断言，无冲突）：树页补 `tree-upload/tree-commands/tree-mkdir/tree-mkdir-input/tree-breadcrumb/tree-empty-dir/tree-filter/tree-refresh/node-download/node-download-verify/node-perms/delete-error`；上传补 `upload-file-input/upload-verify-<i>/upload-gav-<field>/upload-maven-preview/upload-maven-input`；搜索实落仅 `search-filter-repo`（预定 `search-filter-{repo|package|type}` 三件未全落）+ `search-count`；权限编辑器补 `perm-form-name/perm-repo-add/perm-repos/perm-pattern-{input,add,remove}-*` 族、`perm-pattern-verdict`（预定仅 `perm-pattern-result`，实落 verdict+result 双锚）、`perm-matrix-cell-{user|group}-<name>-<action>` 细化、`perm-matrix-remove-group-<g>/perm-add-user/perm-add-group/perm-save/perm-diff`；安全组补 `groups-create/group-form-*/group-edit-<n>/group-delete-<n>/group-delete-reason`、`users-create/user-form-*/user-row-<n>/user-detail-page`；治理补 `audit-filter-{actor|since|until}/audit-table/audit-row-<i>/audit-count/audit-empty-filtered/audit-more`、`gc-danger-zone/gc-grace-hours/gc-confirm-text/gc-empty-ok`、`quota-bar-<key>/quota-input-<key>/quota-save-<key>`；`settings-health` 已按 §3.6.3 落地（§10.3 预告兑现）。
- **E4**：playwright.config.ts 仅 chromium 工程（T-89 口径「console 支持矩阵」）——本票新增 `web/playwright.matrix.config.ts` 为 AC4 观察面专用 overlay（scoped 到 crossbrowser spec，防全量套件静默扩引擎）；D-104-1 修复后建议该 overlay 转常驻抽查。

## 7. 产出与复现

新增（本票交付，零产品代码改动；待 conductor 随票提交）：

- `web/e2e/t104-supplements.spec.ts`（W10 docker 链/W12c+W11 docker/W10b/W34 actor/W23b UI 腿）
- `web/e2e/t104-review-leftovers.spec.ts`（upload-403 腿/B1 hashing 相零 PUT/sameSnapshot 纯 Node）
- `web/e2e/t104-perf.spec.ts`（1GB RSS/50 并发登录/SPA 冷加载）
- `web/e2e/t104-crossbrowser.spec.ts` + `web/playwright.matrix.config.ts`（三引擎三链）

复现（全量矩阵）：

```
git worktree add /tmp/t104/wt 77718cc && cd /tmp/t104/wt && make console && make build
BINFLOW_SERVER__LISTEN=127.0.0.1:18120 BINFLOW_DATA_DIR=/tmp/t104/data \
  BINFLOW_ADMIN_PASSWORD=password BINFLOW_SECURITY_ANONYMOUS_ACCESS=false \
  ./bin/binflow-server serve &            # PID 记为 $SPID
docker run -d --name t104-dind --privileged -e DOCKER_TLS_CERTDIR= \
  docker:dind --insecure-registry=host.docker.internal:18120
docker exec t104-dind sh -c 'mkdir -p /tmp/ctx && echo x > /tmp/ctx/hello.txt && \
  printf "FROM scratch\nCOPY hello.txt /hello.txt\n" > /tmp/ctx/Dockerfile && docker build -t t104img:v1 /tmp/ctx && \
  docker login -u admin -p password host.docker.internal:18120'
cd web && npx playwright install webkit firefox
BASE=http://127.0.0.1:18120 ADMIN_PW=password QA_DIND=t104-dind QA_PORT=18120 \
  QA_SERVER_PID=$SPID QA_TMPDIR=/tmp/t104 \
  npx playwright test --project=chromium --workers=1          # 47 passed
BASE=http://127.0.0.1:18120 npx playwright test e2e/t104-crossbrowser.spec.ts \
  --config playwright.matrix.config.ts                        # chromium 3/3；webkit 1/3；firefox 1/3（D-104-1）
```

清理记录：serve 进程已停（端口 18120 零监听）；dind 容器与为本票拉取的 `docker:dind` 镜像已删（宿主 Docker Desktop/daemon.json 零改动，`docker:cli` 等既有镜像未动）；`/tmp/t104` 大文件（1GB/128MB 播种、截图）与 scratch data dir 已删（serve.log 与两份矩阵 log 保留至 T-105 复核）；git worktree 已移除；主仓工作树仅余上述 5 个新增文件（untracked）。
