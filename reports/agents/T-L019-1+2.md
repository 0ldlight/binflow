Ticket:       L019-1 conan 三 DIVERGENT 对齐（D2/D3/D4 落地 + D1 注记）+ L019-2 maven pom-prerequisite（LOOP 019 合并票，P0/P1 级——conductor 派发裁定随票）
Role:         dev-registry-adapter（领域实例：dev-package-conan + dev-package-maven，两域串行）
Area:         internal/adapter/{conan,maven}（+ 契约 docs/compatibility/contracts/conan.yaml；UAT 版签 .env.uat gitignored 本地文件）
Input:        reports/compatibility/L018-conan-evidence.md（§1 探针矩阵/§3 D1-D11/§6 归一提案）+ contracts/conan.yaml（16 条目底本）+ l018-wire/{a,b} 逐字节错误体（v2-05d/v2-10c/v2-11c/v1-22b/v2-09a/fix-pkgmeta-q*——本轮逐文件复核）+ docs/reverse/maven-metadata-pom-prerequisite.md（§1.1/§1.2/§2 触发矩阵/附录 A/B）+ known-divergence.yaml#maven/version-metadata-pom-prerequisite（BUG、review_gate=LOOP 019 实现票）+ npm errors.go（D4 信封收敛点先例）
Changes:      conan：①errors.go 新增 writeEnvelope（npm errors.go 同构 errors[] 信封，adapter 不 import httpapi 先例）+ msgNotFound；②v2.go 本地 ghost GET 族（latest/revisions/pkg latest/pkg revisions/files 清单空）改 envelope "Not Found"，ghost DELETE 族（revision/recipe/pkgrev）改 envelope Couldn't find path + 尾斜杠剥离（msgPathFor），packages 全删 ghost 同包络；③v1.go digest/download_urls/snapshot/remove_files/files GET ghost 族 envelope，S8 checksum-deploy miss 同收口，writeError ErrNodeNotFound/ErrIsFolder 臂 envelope；④v1.go serveV1RecipeDelete ghost 臂 404→200 幂等（D3；v2 面保持 404=参照两面分叉同型）；⑤search.go rejectBadQuery 挂 v2 两 ref-search 路由（空 q/`*`/wire-ref 三态 400 `Unexpected query syntax: <echo>`，CT=application/json 参照 quirk；非三态拼写维持 as-built 不猜语法）；remote/virtual 面（msgNoRevisions 8 站点）不在裁定内不动。maven：calc.go recalcVersionDir snapshot 分支加 hasPom 硬前置——无 pom（jar-only/空目录）removeMetadata（生成门+触发删除一臂双语义），recalcVersionDir 文档注释补 §1.1 引用；组目录统计（recalcModule pom-bearing 子目录）走读核对已对齐无需改；§1.3 buildNumber 种子、§1.4 202 丢弃不在票面（遗留清单）。契约 conan.yaml：D2/D3/D4 三条目重写为对齐形态翻 VERIFIED（evidence 挂 L018 原始 wire+本轮复验），D2 子族②401 措辞分立新条目 conan/auth401-wording（DIVERGENT），D1 v2ping-posture 注记 L019 裁不实现=INTENTIONAL 候裁，v2-delete/v2-revision-chain/v1-snapshot-search/handshake-auth 交叉注记同步，头注反向门+收尾统计更新（16 条目=VERIFIED 14/DIVERGENT 2，yaml.safe_load 机读门过）
Files:        新增 internal/adapter/conan/errors.go、internal/adapter/conan/error_envelope_test.go（D2/D3/D4 行为三测）、internal/adapter/maven/pom_prerequisite_test.go（wire 流三臂+预置文档臂）；修改 internal/adapter/conan/handler.go、v1.go、v2.go、search.go、handler_test.go、v1_test.go、search_test.go，internal/adapter/maven/calc.go、calc_test.go，docs/compatibility/contracts/conan.yaml，deploy/compose/.env.uat（版签 uat-l0191-47f37739，gitignored）；删除无。scratch（不 commit）：/tmp/l019-arms.sh、/tmp/l019-client.sh、/tmp/l019-mvn.sh、/tmp/l019-wire/{a,b}
Tests:        conan error_envelope_test.go：信封族表测（ghost GET×6+ghost DELETE×2：文案/CT/无尾斜杠断言）+TestV1GhostDeleteIdempotent（v1 ×2=200 空/v2=404 envelope 两面分型）+TestRefSearchQuerySyntax（三态 400 体+CT 逐字节、非三态+v1 面 as-built 200）；存量改动 handler_test（空链/删族信封化+尾斜杠负断言）、v1_test（D3 second delete=200、snapshot/channel/checksum-miss ghost 信封化、D8 双面分型 wantAgain）、search_test（v2 腿 q 落点改写）；maven pom_prerequisite_test.go：TestPomPrerequisiteFlow（jar-only 404→pom 到位 200 buildNumber=1+组目录异步→删 pom 404+sidecar 删+jar 存活）+TestPomPrerequisiteSeededDocument（预置文档 jar-only 重算删除）；calc_test 快照矩阵补 jar-only 丢文档臂+classifier 臂补 pom
Commands:     go build ./... && go vet ./... && gofmt -l internal/adapter（空）&& ~/go/bin/golangci-lint run（0 issues，全仓+adapter 子树双跑）；go test ./internal/adapter/conan/... ./internal/adapter/maven/... -count=1 -race（ok 70.1s/57.7s）；go test ./internal/... -count=1（全 ok）；docker compose -p binflow-uat --env-file .env.uat -f deploy/compose/docker-compose.yml up -d --build（binflow:uat-l0191-47f37739-alpine）；差分复验 /tmp/l019-arms.sh {a,b}（18 请求臂+setup/teardown，串行 settle 2/10s）；真实客户端 /tmp/l019-client.sh + /tmp/l019-mvn.sh（conan 2.31.2/conan 1.66.0/mvn 3.9.16）；python yaml.safe_load 契约机读门（16 entries）
Outputs:      差分复验（A=:8082 7.161.20，B=uat-l0191-47f37739）：D2 五臂——d2-latest/revs/v1snap A/B 均 404 envelope `{"errors":[{...,"message":"Not Found"}]}`（74B/84B 仅缩进形态差=R2 域），d2-delrev 双端 `Couldn't find path 'l019/hello19/1.0/stable/deadbeef…'` **无尾斜杠逐字同**，d2-delrecipe `Couldn't find path '_/nope/1.0/_'` 同；D3——d3-del{1,2,3} 三连删 A/B 全 200 空（B 旧形态 ghost=404 已消）；D4——d4-q{empty,star,ref} A/B 同码 400 同体 25B/26B/72B（`Unexpected query syntax: `/`… *`/`… hello19, version=1.0, user=l019, channel=stable`）+CT application/json 双端同；maven 三臂——M1 jar PUT 201→meta GET 双端 404，M2 pom PUT→双端 200（buildNumber=1、jar+pom snapshotVersions 同值），M3 pom DELETE 204→settle 10s→双端 404+jar 200（9B 同字节）。真实客户端：conan 1.66 `remove -f` ×2——服务端日志 `DELETE /v1/conans/hello19/1.0/l019/stable → 200`（树在，179ms）+`→ 200`（GHOST，0ms，客户端干净退出=D3 实客户端面）；conan 2.31.2 upload 舞步完整（revisions 404 探测→check_credentials→PUT 探测 404→实体 201×2→search 200→revisions 200）+list 命中；mvn 3.9.16——M1 deploy-file generatePom=false 后 GET metadata 404（mvn 部署前探测 metadata 404→buildNumber 从 1 起算=§1.3 种子行为活体）、M2 pom 部署后 200 服务端计算形、M3 dependency:get RESOLVE-EXIT=0 经生成 metadata 解析落 ~/.m2
Compatibility: 契约 conan.yaml 翻绿三条目（error-envelope-family[D2①③]/v1-ghost-delete[D3]/v2-pkgmeta-search[D4]→VERIFIED，evidence=L018 wire 原始取证+本轮 uat-l0191 双端复验）；新分立 conan/auth401-wording（D2②，DIVERGENT 待裁——B 渲染点在 auth 中间层 adapter 外）；D1 v2ping-posture 注记 INTENTIONAL 候裁（实现面无动作）；known-divergence 登记建议：#conan/{error-envelope-family,v1-ghost-delete-idempotency,pkgmeta-q-semantics} 改记 resolved-by-alignment、#maven/version-metadata-pom-prerequisite（BUG→resolved，本轮三臂+mvn 实腿即 review_gate 所指实现票验收）归 compatibility-engineer 复核落账。可差分 surface（后段批跑输入）：①pkgmeta 合法 q 语法探明臂（complex_search 属性式）；②X-Checksum-Deploy 命中臂；③conan 1 revisions-on 面；④`_` 占位 ref；⑤remote/virtual 面（msgNoRevisions 裸文本现状=后段票裁定面）；⑥maven 手 PUT snapshot 目录 metadata 受理面（§1.4：B 201+同步重算 vs A 202 丢弃——终态同为服务端形，受理码差待裁）
Security:     路径穿越无新面（rejectBadQuery 只读 query 参数；ghost 路径消息为服务端拼装坐标，尾斜杠剥离不涉解析）；无 remote 代理改动；凭据全程 env/临时 settings.xml（盘面零落盘）；/tmp wire 捕获 token 三形态扫描（JWT eyJ/AKCp/cmVt）+authorization 头扫描均空（且 /tmp scratch 不入 git）；UAT 重建沿 L018 先例（工作树构建，.env.uat 版签本地 gitignored）；**环境事故申报：Docker Desktop 第 6 次全灭（构建中双端口 000/CLI 挂起——L015-018 同款），按 runbook 处置（杀僵尸 build+kill -9 backend+Docker Desktop 重启+手工 start postgres artifactory），层缓存续建成功**
Performance:  conan 变更全为错误渲染路径（零热路径影响）；maven pom 前置=recalcVersionDir 既有 List 上加布尔判定（零额外 IO）；差分 18 臂双端各 ~30s 零超时；race 套件（70s/58s）无告警
Risks:        ①D4 三态外 q 拼写维持 as-built 200 全 map（A 合法语法未探明——clean-room 不猜，语法臂后段定案）；②conan 2.31.2 `remove`（无 -f 形态）在本机 Python 3.14 环境**对 A/B 双端同样自旋**（99% CPU 纯客户端计算、零服务端流量——sample 栈为 PyEval 纯 Python 帧；L018 亦未跑过 conan 2 CLI remove 腿，conan 1.66 remove 需 -f 否则 EOF 提示重试死循环）——环境怪癖非回归，已双端取证在案；③maven M2 中 mvn 自 PUT maven-metadata.xml（586B）B 受理 201+同步重算 vs A 202 丢弃（§1.4 受理码差，终态同=服务端形）；④maven metadata XML 渲染形态差（A modelVersion 属性+version 元素后置 vs B 无属性前置——语义同，M2 对照可见，候选后续渲染对齐票）；⑤M1/M3 404 体措辞差（A `File not found.; Path:` vs B `Failed to find the requested resource`——maven 文件 GET 404 措辞面，票外遗留）
Blockers:     无（Docker Desktop 全灭已按 runbook 处置；建议 conductor 知会 devops 立案——频次 6 次/5 天）
Next:         compatibility-engineer：①conan.yaml 三条目翻绿复核入册+auth401-wording 登记；②known-divergence 四条 resolved-by-alignment 改记+#maven/version-metadata-pom-prerequisite BUG→resolved；③normalize#conan 七规则登记（L018 提案在案）。conductor：④D1 INTENTIONAL 终裁；⑤UAT 常驻 pro 档裁定（L018 在案）。后段票候选：⑥pkgmeta q 语法探明+差分批跑（§收尾七项 surface）；⑦maven §1.4 受理码+XML 渲染形态+404 措辞三面小票；⑧devops：Docker Desktop 全灭立案
