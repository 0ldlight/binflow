# T-428 [P2] FR-140.3 文面回写簇——工作日志

- 角色：tech-writer（②③两处 ux 口径**代笔**，留痕均标「ux 会签位」——按 ux 册既有定案落笔，未自创设计值）｜area：四处目标文件 + 本日志。
- 纪律遵守：只写四处目标文件 + 本日志；BOARD 零写、零 git、未起栈（`make docs` 编译一次）；机器审计 workflow 在跑期间未动任何运行面。
- 状态：**done**（AC1 四处落笔 + 留痕齐 + `make docs` SUCCESS 零断链）。
- 日期：2026-09-02。

## 1. 四处回写（AC1 逐项）

### ① docs/reverse/README.md——补 npm.md 行（T-393 遗留 #2）

- 「预期规格清单」表在 `goproxy.md`（M10）与 `aql.md`（M15）之间插入 **`npm.md` 行（M14）**：K60 六条定案 / login wire 语义 / `.npmrc` 键匹配坑 / 「客户端源码即规范」+ live 抓包对拍；范围声明（协议主面仍在 `maven-npm-pypi.md` §0/§2）随行带出——与 `docs/reverse/npm.md` 自身头部的范围声明同口径。
- 留痕：行内注「**索引行 T-428 补齐——T-393 遗留 #2 登记**」。

### ② docs/design/console-artifactory-parity.md——M1 行 440px 定案升级 + M3 行 MUI Paper 代差（T-398 §4-2 登记；ux 会签位）

- **M1 行**三格升级：
  - MUI 映射：原「宽 880px ≈ `maxWidth="lg"`」→ 记 Artifactory 实测 880px + **BinFlow 定案 440px 紧凑档不追平**（paper sx `min(440px, calc(100vw - 48px))` 钉死）；
  - BinFlow 载体：补 **T-390 卡面复活后形态**（`.pkg-grid-item` 类名复线，卡面/禁用置灰生效；可选磁贴 brand 22px、门控 mono + opacity 0.4 + 徽章）；
  - 差距：原尾注「网格 modal 宽度参考 880px（BinFlow 现档位即可）」升级为**定案口径**（880px 系 33 包型 90×90 大磁贴 vs BinFlow 5 核心 + 8 门控 190×44 高密度卡——追平即大面积留白；T-383 票内定案 + e2e 断言 `toBeCloseTo(min(440, vw-48))`；T-390 复证不破）。
- **M3 行** MUI 映射格补 **MUI Paper 代差注记**：分节容器 Paper 化按域分期——建仓/编辑六节 `<Paper component="section">`（缺省 elevation 1；T-344 批 D 换装、T-383 加 `form-section-*` 锚）vs 用户/组表单 CSS `.form-section`（mui-native-visual security.css 行标「留」）；代差是 MUI 换装既定分期，非 parity 缺口（Artifactory 自绘 jf 面板无 elevation 概念）。
- 版本留痕：头部状态行 v1.3 → **v1.4**；§0 修订表增 v1.4 行（两处均标「ux 会签位」+ 证据清单）。

### ③ docs/design/brand/package-icons/README.md——helm·nuget 暗底提亮超 +10% 量级拍板（ux 会签位）

- §4 末块 K61「量级注记」：原「留 ux 档复核」收口为**量级拍板**——按 K56 资产现值（`brand/helm.svg` `#0F1689`、`brand/nuget.svg` `#004880`，K56 只动 npm/go/conan，资产零改动口径维持）定案：超量级抬幅（暗色档 token `#6b73ef`/`#0083e9`）**定案认可、不回折 +10% 微调线**（藏青起点 1.04/1.44 上 3:1 是物理下限非风格选择）；退役条件维持（K 系出 brand-dark/ 资产级暗色版则本组 token 整体退役）。
- §6.4 条款尾补拍板指针；头部状态行 v1.2 → **v1.3**（2026-09-02，T-428，标 ux 会签位）。

### ④ docs/user/guides/migrate-artifactory.md——措辞陈旧清账（T-397 遗留 #3 登记）

触发点为 L17「不支持的包类型（nuget 等）」，清账按 as-built 全文核对（`cmd/bf-migrate/main.go` + `internal/migrate/` 逐文件）：

| # | 陈旧措辞 | as-built 修订 |
|---|---|---|
| 1 | 「不支持的包类型（nuget 等）」（读作 BinFlow 不支持 nuget） | 改为**映射集口径**：bf-migrate 转换器映射集 = generic/docker/maven/npm/pypi（gradle→maven 别名）；新增「**工具映射面 ≠ 产品能力面**」段——nuget/go/cargo/conan/helm/helmoci/rpm/deb M10~M14 已交付，目标侧手建同 key 仓 + 协议客户端重灌（代码依据：`converter.go` `binflowPackageTypes` 五型 vs `internal/repo` 13 型） |
| 2 | 「三阶段」「制品本体不在三阶段内……后续票据范围」 | **四阶段**（repos → users → tokens → **artifacts**，T-196 落地）：迁移表 + artifacts 行（generic/maven 布局、双哈希校验、`--concurrency` 并行；docker/npm/pypi 制品留源侧、remote 缓存不迁、virtual 无本体）；章节改题「四阶段语义」 |
| 3 | 旗标表缺 4 旗标 | 补 `--allow-non-empty` / `--concurrency`（缺省 4）/ `--report-file`（`migration_report.json`）/ `--skip-users` |
| 4 | 无空目标守卫说明 | 前置条件 + 四阶段语义 + 常见报错三处落**空目标守卫**（拒绝文案逐字、dry-run 只观测、resume 不拦、32 条批检查点） |
| 5 | dry-run 示例输出为杜撰格式（「phase repos: found 6, migratable 5」） | 改为真实 summary 渲染（`repos: found=… migrated=…`，dry-run 的 migrated = 将写入计数——`migrate.go:86` 注释为据）；全量 summary 的 artifacts 行/逐仓明细行补充说明 |
| 6 | 常见报错表缺两类 | + 空目标拒绝行、映射集外跳过行 |

- 留痕：banner 一行（M6 建面 T-167 + M15 时点措辞清账 T-428——T-397 遗留 #3 登记，制品阶段与守卫随 T-196）。

## 2. 验证

| # | 项 | 结果 |
|---|---|---|
| V1 | `make docs` | **SUCCESS**（docs site size 4.78 MB；输出零 `broken`/`error` 行——Docusaurus `onBrokenLinks: throw` 下构建通过即零断链；docsDir/blogDir 两行 Warn 为既有配置噪音非本票引入） |
| V2 | 产物抽查 | `docs-site/build/guides/migrate-artifactory/index.html` 含「四阶段语义」「工具映射面」「target instance is not empty」「bf-migrate summary (dry-run)」新文面 |
| V3 | 代码事实核对（写前） | `RepositoryFormPage.tsx:373` `min(440px, calc(100vw - 48px))` + `:383` paper slotProps；`:620` 起六节 `<Paper component="section">`（无 elevation prop = 缺省 1）；`UsersPage.tsx:157/225/240/263` `.form-section` CSS 分区；`tokens.css:138/142` `--bf-pkgicon-helm: #6b73ef` / `--bf-pkgicon-nuget: #0083e9`；`brand/helm.svg` `#0F1689`、`brand/nuget.svg` `#004880`（K56 后现值）；`converter.go` 五型集合、`artifacts.go` artifactUnsupported 三型、`migrate.go` 守卫/summary 格式串逐字 |
| V4 | 票据登记源核对 | T-393 遗留 #2（README 漏行）、T-397 遗留 #3（L17 措辞）、T-398 §4-2（parity M1/M3 待刷新）、T-383/T-390/T-400（440px 定案与卡面复活证据链） |

## 3. 留痕与变更清单

```
docs/reverse/README.md                                        ① npm.md 行（行内 T-428 留痕）
docs/design/console-artifactory-parity.md                     ② 状态行 v1.4 + §0 v1.4 行 + M1 三格 + M3 Paper 代差（ux 会签位）
docs/design/brand/package-icons/README.md                     ③ 状态行 v1.3 + K61 量级拍板 + §6.4 指针（ux 会签位）
docs/user/guides/migrate-artifactory.md                       ④ banner 清账行 + 四阶段/包型口径/守卫/旗标/示例/报错表
reports/agents/T-428.md                                       本日志
```

## 4. 遗留 / 待确认

1. **ux 会签**：②③两处「ux 会签位」为代笔——ux-designer 复核签字后即收口；若对 440px 定案表述 / Paper 代差口径 / helm·nuget 拍板措辞有异见，改动均为行级。
2. migrate dry-run 示例中 artifacts 行未入示例块（dry-run 下制品计数的呈现未逐字核实）——正文以全量 summary 行形描述，格式串以 `migrate.go:732` 为据；如需示例块内展示，跑一次 dry-run 补数值即可。
3. bf-migrate 转换器映射集（五型）与 `internal/repo` 支持集（13 型）的差是**工具面**现状——是否扩 converter 集合属产品决策，本票只如实分面表述，不预示扩展。
4. `docs/reverse/README.md` 清单表无版本行机制，①的留痕用行内注——若 reverse 域今后引入版本行，可迁移。
