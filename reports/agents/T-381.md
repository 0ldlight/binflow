# T-381 工作日志 — FR-123 活体核验 V1~V8 + parity 置信度回写 + 差距矩阵复核基线

- role: qa-engineer；日期：2026-08-31（BOARD 只读）
- 票据：T-381 [P0] FR-123 活体核验 V1~V8 + parity 置信度回写（改契约留痕）+ §7 矩阵复核基线 + 三出口 + 低置信三枚图标顺带腿
- 回写物：`docs/design/console-artifactory-parity.md` v1.0→**v1.1**（§0 留痕）；`docs/design/brand/package-icons/README.md` v1.0→**v1.1**（§4 三枚修正 + 六枚旁证）
- 证据：`reports/agents/t381-evidence/`（27 截图 + `t381-measurements.json` 全量结构化测量）

## 总结论：**PASS（带 1 起事故已完整恢复 + 2 项降级子项）**

V1~V8 全部完成活体核验并回写置信度；零静默升格（§8 逐行对应）。**三个改契约级发现**：决策项 A（M1 单 modal 化）与决策项 B（M3 创建 modal 化）的 parity 前提被实测证伪、撤销；L2 的 ⋮ 菜单在 7.84 无对位形态、原改造项撤销。执行期间发生一起误删事故（example-repo-local，详见 §3——**已按字节集无损标准完整恢复**，快照与取证材料保留在 VM）。

## 0. 核验源与方法

| 项 | 值 |
|---|---|
| 实例 | `t226-artifactory`（用户 VM 172.16.58.129，T-228 保留栈）——`docker ps -a` 本机无此容器（在 VM 上），`docker start t226-pg t226-artifactory` 恢复，API 8181 / UI 8182 |
| 版本 | **Artifactory OSS 7.84.10 rev 78410900**（`api/system/version` license=Artifactory OSS；与规格核验期望版本一致） |
| 凭据 | admin 口令取自 VM `~/t228-admin-pw`（0600，T-228 留存）——仅会话内使用，**零仓库/报告落盘** |
| 驱动 | Playwright chromium headless（复用 `web/node_modules`，脚本全在 `/tmp/t381/`，**未改 web/ 任何文件**——T-391 并行区不碰） |
| 方法 | DOM 结构化测量为主（rect/class/文本/按钮序，1600×900 视口 + 1280 宽度探针）——本 agent 环境截图无法回显自阅，故结论全部以 DOM 实测数据为准，截图仅作归档人证 |
| 语料基线 | 7 仓 / 158 制品（example-repo-local 120 + t228-generic 35 + t228-maven 3）/ 4 用户 / 5 token 元数据（T-228 §8 快照） |

## 1. V1~V8 逐项核验

### V1（D1 Set Me Up）— ✅ 核验完成，三处修正

- **方法**：顶栏快捷下拉 → Set Me Up；进入后逐层 dump（抽屉 rect/Tab/凭据区/底栏）；1280 宽度探针。
- **结论**：右侧抽屉（`el-drawer rtl` + 轻遮罩），**宽 50vw**（1600 视口实测 **800px**，1280 视口 640px）全高；标题 "Set Up a client" → 选型后 "Set Up A Generic Client"；**步 0 = 包型药丸**（与 BinFlow smu-grid 步 0 同构）；**Tab 三枚 Configure / Deploy / Resolve**；仓选择下拉；凭据区 "Generate Token & Create Instructions" + Dismiss；**片段每 Tab 平铺、无手风琴**（token 生成前隐藏，DOM 占位 `curl -uadmin:<PASSWORD> -T …` / `-L -O …`）；底栏「Select a different package type（左）+ Done（右 primary）」。
- **回写**：D1 置信度 中高→**高**；宽度 480px→**50vw（480~800）**；Tab 补 **Resolve**；折叠形态修正为平铺。T-382 的主形态断言（右抽屉）成立且参数化。
- 证据：`20/21/22-setmeup-*.png` + measurements.json `V1_setmeup`。

### V2（M1 建仓向导）— ✅ 核验完成，**决策项 A 撤销（改契约）**

- **方法**：仓库列表 → Add Repositories 入口 + 直达 `/ui/admin/repositories/local/new`；选型前后两轮 overlay/表单 dump；实测建仓全程。
- **结论**：rclass 由列表页「Add Repositories」**下拉**预选定（不在向导内）；「Select Package Type」modal **880px** 居中（90×90 磁贴）；**选型后 modal 关闭、落整页路由表单**（"New Local Repository"）——**7.84 即「网格 modal + 整页表单」两段式**，v1.0「全程单 modal」记忆证伪；字段/页脚（Cancel 左 transparent + Create Local Repository 右 primary）全实测。
- **回写**：M1 置信度 中/低→**高**；差距 △→**✅（决策项 A 撤销——BinFlow 现形态已对齐）**；§7 矩阵行改判；§10 批 1 ② 划线撤销。
- 证据：`10/11/12*.png` + `V2_newrepo`。

### V3（F1 toast）— ✅ 核验完成，E7 转「再议」

- **方法**：UI 实建临时仓触发 success toast，250~300ms 轮询 el-message 生命周期（位置/入场/离场/存活）。
- **结论**：**顶部居中单条**（1600 视口 x=588 w=425 → 正中；settle y≈20；顶部滑入/向上淡出）；**自动消失 ~2-3s**；**无堆叠**。文案例 `Repository 't381-ui-probe' was created successfully`。
- **回写**：F1 置信度 低→**高**；E7 转「再议」（BinFlow 右下堆叠为有意设计，默认不改，是否对齐交 PM/用户）。
- 证据：`13-create-toast-tail.png` + `V3_toast`（完整时间序列）。

### V4（L2 行内菜单）— ✅ 核验完成，**原改造项撤销（改契约）**

- **方法**：仓库列表/用户列表行结构 dump（ag-grid 行内全部按钮与图标类名）+ hover/click 差集探测。
- **结论**：**7.84 仓库行与用户行均无 ⋮ 菜单**——行尾是**单个 `icon-trash` 直删图标按钮**；无编辑/复制类行内动作收纳。v1.0「行尾 ⋮ 收纳行操作」前提不成立。
- **回写**：L2 置信度 中→**高**；差距 △→**✅（豁）**；原「加 ⋮」改造撤 parity 旗（复制 key/SetMeUp 行内快捷可留作可选自有增强票）；§10 批 1 ④ 划线撤销。
- 证据：`14*.png`、`33-user-row-menu.png` + `V4_rowmenu`。

### V5（N2 侧栏图标）— ✅ 核验完成，维持改造（不降级）

- **方法**：应用域/管理域双侧栏 DOM dump（条目 × i/svg/img 计数）+ 侧栏截图。
- **结论**：**条目带图标**——两域侧栏（230px）一级条目均带 icon-font 图标（管理域每项 2 枚 `i`；应用域每叶 1 枚；Environments 为 svg），**子项无图标**；父级分组可折叠。
- **回写**：N2 置信度 中→**高**；维持 △（批 2 ⑧ 照排，档位=一级条目）。
- 证据：`05-*.png` + `V5_sidebar`。

### V6（M3 实体表单）— ✅ 形态核验完成，**决策项 B 撤销（改契约）**；token 生成表单子项降级

- **方法**：users/groups 页 + `/new` 表单 dump（只开表单不提交）；profile 页 token 面观察。
- **结论**：**用户/组创建 = 整页路由表单**（用户：User Name/Email/Password/Retype + 5 复选项；组：Group Name/Description/External ID + 复选项 + 成员选择列表；页脚 Cancel 最左/Reset/Save 右）——**不是 modal**，v1.0 记忆证伪。Token 面 = profile 页「Authentication Settings → Generate an Identity Token」+ **Identity Tokens 表**（4 列 + 列选器）。
- **降级子项**：profile 编辑有密码 Unlock 门 → **Generate Token 表单字段集与一次性明文结果面板未核验**（避免制造凭据变更）；admin 集中 token 管理页在 OSS 导航不可达。维持「以核验为准」，接线票以 BinFlow `smu-token-panel` 既有形态覆盖。
- **回写**：M3 置信度 中→**高**（形态面）；差距 △→**✅（决策项 B 撤销）**；Tokens 页 ✗（BinFlow 缺口）维持并补参照形态。
- 证据：`30/31/32/34-*.png` + `V6_forms`。

### V7（D3 依赖树）— ✅ 存在面核验完成，**本项关闭**

- **方法**：Builds 页直达 + 制品树页签 dump。
- **结论**：**Builds 面存在**（OSS 应用域，4 列 + 空态——实例无 build 数据，build 详情内依赖列表形态不可观测）；**制品详情页签 = General/Effective Permissions/Properties/Followers，无依赖视图**（v1.0「制品页签级依赖视图」证伪）。
- **回写**：D3 置信度 低→中（面级）；**关闭**——依赖数据挂 Builds 面（§9 永不建），BinFlow 维持不建。
- 证据：`40/41/42-*.png` + `V7_dependency`。

### V8（M2 删仓确认）— ✅ 核验完成，文案实证

- **方法**：自有探针仓行 trash 点击 → message-box 全量 dump（文本/按钮样式/input 可见性）→ ESC 关闭（不确认）。
- **结论**：**520px 居中 message-box**；标题 "Delete Repository"；正文 **"Are you sure you want to delete the {key} repository? All artifacts will be permanently deleted."**；按钮 Cancel（左）/ **Delete（右，绿色主按钮 rgb(64,190,70)）**；**无 type-the-key**（input 隐藏 w=0）；ESC 安全关闭。
- **回写**：M2 维持高置信 + 文案实证；BinFlow 的输入 key 确认 = 更严有意偏离，E1 豁免强化。
- 证据：`16/16b-*.png` + `V8_deletebox`。

## 2. 低置信三枚图标活体对照（AC2 顺带腿）

- **方法**：建仓向导磁贴内联 SVG（JFrog 渲染版官方标，33 包型）——fill 色/path 数/几何包围盒结构化比对（**clean-room：只析不拷，官方矢量零入库**；原始 SVG 仅存 /tmp 分析用，未进仓库）。
- **结论**（已回写 package-icons README §4）：
  - **docker**：单色 **`#0091E2`**（10 path，鲸+集装箱堆）；BinFlow 双蓝可保留或收敛单色；布局与官方 Moby 标一致（集装箱背左上）。置信度 中→**中高**。
  - **conan**：**灰蓝系 `#669ACC`/`#7CA8D4`/`#316699`** + 渐变（5 path）——BinFlow `#0095D5` 过亮，**README 主色已改 `#669ACC`**（brand/conan.svg 本体换色留 ux-designer 执行）。置信度 低→**中高**。
  - **nuget**：官方已用**现代曲线双形**（8 path，`#016FD2` + `#5FFFE6`/`#B3DADD`）非经典斜方+球；BinFlow 经典形识别度仍高、保留注记。置信度 低→**中**。
  - 旁证色 6 枚：maven `#BE202E`/npm `#FF553E`/pypi `#4B7196`+`#F4BF52`/go `#00ACD7`/debian `#A80030`/rpm `#D72123`。
- 证据：measurements.json `icon_leg` + `11-newrepo-step1-grid.png`（磁贴网格全景）。

## 3. 事故记录与恢复（透明度优先，评审必读）

**INC-1（P0 过程事故，已恢复）**：stage 3e 行菜单探测中，行定位选择器误匹配 AG Grid 容器级元素 → 点开的「行尾按钮」与后续 /^delete$/i 点击落在**首行 example-repo-local** 的删除确认框上 → **误删了 T-226 语料仓（120 制品）**（2026-08-31 03:50:06Z，服务端日志 `MetadataEventServiceImpl:265` 在案）。当次脚本误把该确认框当成「无确认直删」——stage 4d 差集法复测还原了真实时间线（trash 点击即弹 V8 确认框，我的 Delete 点击满足了它）。

**恢复过程（全部只读取证 + 纯增量写回）**：
1. 立即停止一切 fixture 变更；确认 filestore 159 blob 全在（删仓只清 DB 元数据，二进制未动，无 .trash、未触发 GC）。
2. **只读快照**：`docker cp` filestore + `artifactory-request.log` → VM `~/t381-incident-recovery/`（15MB，含 120 条 PUT 路径+尺寸记录；T-226 语料生成时的逐文件 PUT 全在日志里）。
3. **恢复**：UI 重建 example-repo-local（generic local）→ 按日志原路径逐文件回 PUT（尺寸配对分配 blob；同尺寸组内为字节集保持、path↔内容映射可能与原排列不同——无逐文件校验和可考，此为恢复上限）。
4. **验证**：120/120 PUT 201；**filestore 文件数恢复前后均 159**（checksum 存储复用既有 blob → 恢复字节集与原语料**可证等价**）；随机抽样 8 路径 200 + sha1 与快照 blob 逐一匹配；`/api/repositories` 8 仓齐全；UI "Happily serving 294 artifacts"。
5. **残留**：`t381-ui-probe`（本轮 UI 建仓/toast 探针空仓）——REST 删除被 OSS license 门挡（400），**不做 UI 删除重演**，留作文档化 QA 残留（先例：t228-probe/t228-probe2）；恢复材料留 VM `~/t381-incident-recovery/` 供取证/再核（conductor可决定清理时机）。

**教训已吸收进后续步骤**：之后所有探测改「精确行级选择器 + 差集法 + 永不点确认」；破坏性流程全部走只读观察。

## 4. AC 判定

| # | 验收标准 | 结果 | 证据 |
|---|---|---|---|
| AC1 | V1~V8 逐项核验结论 + 证据归档；置信度列回写（§0 留痕）；零静默升格 | ✅ | §8 表逐行结论+置信度变化；§0 v1.1 行；27 截图 + measurements.json；grep 对照：§8 全部 8 项均落结论，规格内 V1~V8 引用点（M1/D3/F1 行内标注、E7、§10 待 V5/V3/V2V6）全部同步更新，无「以核验为准」悬空项（唯余 V6c token 表单子项明示降级，非静默） |
| AC2 | §7 重印 + 三出口 + 图标三枚活体对照回 README §4 | ✅ | §7 v1.1 重印（M1/M3/L2 行改判 + 汇总重排）；三出口 = V5 不降级/V7 关闭/E7 再议（§8 表下注）；README §4 v1.1（docker 中高/conan 中高+主色改/nuget 中 + 六枚旁证） |
| AC3 | 降级路径内置 | ✅（未触发主降级） | 核验源一次恢复成功，主降级路径未启用；子项级降级 2 处如实标注：① V6c token 生成表单/结果面板（profile 密码门）② V7 build 详情依赖列表形态（无 build 数据不可观测）——均维持「以核验为准」，不阻塞批 1 |

## 5. 差距矩阵复核基线（§7 三出口执行摘要）

1. **V5 不降级**：侧栏一级条目带图标实测成立 → N2 维持 △（批 2 ⑧ 照排，档位修正为一级条目）。
2. **V7 关闭**：依赖视图挂 Builds（永不建）+ 制品页签无对位 → D3 关闭。
3. **E7 再议**：toast 锚位差异实证（顶部居中 vs 右下）→ 默认不改，交 PM/用户；改动成本一行级。
4. 连带改判：**决策项 A/B 撤销、L2 撤 parity 旗**——批 1 只剩 **D1 抽屉化**（+Resolve Tab、50vw 宽）；批 2 ⑤ Tokens 页获参照形态。

## 6. 遗留与建议（回 conductor）

| # | 项 | 建议 |
|---|---|---|
| L01 | T-382 依赖输入已就绪：D1 主形态断言 = 右抽屉 50vw(480~800) + Configure/Deploy/**Resolve** 三 Tab + 底栏返回/Done | D1 票参数按 v1.1 规格执行；Deploy 对话框维持居中（决策项 C 建议） |
| L02 | conan brand 主色 README 已改 `#669ACC`，**`brand/conan.svg` 本体未动** | ux-designer 一并换色（mono 版不受影响） |
| L03 | 事故残留：VM `~/t381-incident-recovery/`（15MB 取证快照）与 `t381-ui-probe` 空仓 | conductor 决定清理时机；如需彻底清 probe 仓，建议人工 UI 操作或 Pro license（REST 被 OSS 门挡） |
| L04 | 顺带观察（走 PM，未动规格置信度）：Customize Columns 列选器旁证 L1；顶栏快捷下拉；侧栏父级可折叠与 N2 描述出入 | PM 拆票时参考 §8 v1.1 顺带观察清单 |
| L05 | ux-designer 共笔确认（票面要求「ux-designer 共笔回写」） | 本轮回写由 qa-engineer 执行并全量留痕，ux-designer 复核签认待 conductor 路由 |
| L06 | incident 流程建议 | UI 自动化探测默认禁用破坏性动作点击；对共享 fixture 的写操作（建/删）前先做快照——本次教训成文，供后续 QA 票引用 |

## 7. 环境收尾

- t226 栈（t226-artifactory + t226-pg）**保持 Up**（保留栈常态，与 T-228 收尾口径一致）；语料恢复后 8 仓 / 制品数健康（"Happily serving 294 artifacts"）。
- 本地零残留：脚本/临时 SVG 分析均在 /tmp/t381/（不入仓库）；admin 口令零落盘（会话内 + VM 0600 原文件）。
- 未做任何 git 操作；web/ 零触碰（T-391 并行区）。
