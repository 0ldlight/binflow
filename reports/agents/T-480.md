# T-480 — README 精简 + 去迭代化（tech-writer）

日期：2026-09-06 · 分支 develop（工作树直改，未 commit）· 写入面：README.md、README.zh-CN.md（+本日志）

## 1. 前后行数对比

| 文件 | 前 | 后 | 精简率 |
|---|---|---|---|
| README.md | 649 | **200** | -69.2% |
| README.zh-CN.md | 649 | **194** | -70.1% |

双语同构：标题层级逐节对应（`#` ×1 → `##` ×9 → `###` ×2，顺序一致）；
代码块数量与内容逐字对应（bash ×6、yaml ×1）。

## 2. 删除类目

| 类目 | 处置 |
|---|---|
| 里程碑叙事段落（M6/M7–M9/M10–M14 六大段 + "Beyond curl: what M6–M9 bring"、"M7/M8/M9/M10-M11/M12" 五个子章） | 全删 |
| PRD/里程碑链接表（milestone-12/13/14.md、ROADMAP、BOARD、reports/ 行） | 全删（对齐「README 不出现迭代信息」指令㉑；ROADMAP/BOARD 属内部迭代资产，不该出现在门厅） |
| 命令演示长廊（RBAC carol 全链、trash restore、copy 403 门控、M9 user 删除、storage/usage 等 ~200 行 curl 演示） | 全删——细节归文档站与 docs/user/ 各篇，README 只留五步快速开始 |
| Artifactory 全量功能对照矩阵链接（docs/reverse/213 条） | 删（内部对齐跟踪资产，同属迭代追踪面） |
| `make` 目标全表（11 行表） | 压成一句话（`make dev` 门禁 + `make help`） |
| 保留 | 定位一句话、13 包型矩阵（三档如实 + 全部指南深链）、五步快速开始（真实可跑）、能力清单 10 行（每行链完整指南）、部署形态表 8 行、安全须知、配置、运维、开发、文档链接、状态 |

## 3. grep 自证（零迭代信息）

```
$ grep -nE 'M[0-9]|T-[0-9]|sprint|milestone|里程碑|迭代|iteration' README.md README.zh-CN.md
（无输出，exit 1 — 零命中）
```

附带核查：全文无 m1-done 类 tag、无 ADR 号叙事（ADR-0005/0009 引用随段落删除）、无 FR-/Q-/K 编号。

## 4. 事实核对注记（不照抄旧文，逐项对源）

| 事实 | 旧 README | 新文 | 证据 |
|---|---|---|---|
| 包型数量 | "twelve" | **thirteen（十三）** | `internal/addons/slots.go` 13 个 PackageType 槽（generic/docker/maven/npm/pypi/go/nuget/cargo/conan/helm/helmoci/rpm/debian）；Fern 站 index 也为 13 |
| docker virtual 聚合 | "remains unserved"（M14 口径） | **三仓型齐备**（定位句不再特判 docker） | `internal/repo/validate.go` L38-42 矩阵无拒绝格；`internal/repo/docker_test.go` L295+ 与 `internal/httpapi/t80_repo_model_test.go` L306 实测建仓成功（本轮 `go test ./internal/repo/ -run Docker` 与 httpapi t80 均过，exit 0）；deploy/README M15 烟测 "docker×virtual 200" |
| quickstart 命令链 | — | **真机实跑** | 见 §5 |
| compose 缺 ADMIN_PASSWORD 拒启 | 有此说 | 保留 | `deploy/dev/docker-compose.yml` L40 `:?` 强制守卫 |
| `/binflow/api/v1/addons` | — | 保留 | 实跑 200，首槽 `{id:generic, minTier:community, enabled:true}` |
| 匿名读默认开 | 有此说 | 保留 | 实跑：无凭据内容 GET 200；`/binflow/api/v1/health` 无凭据 401、带凭据 200 |
| docs 站链接 | 无 | 新增站点根链 | curl -L 307→200（binflow.docs.buildwithfern.com）；深链仅中文 slug 路径真实可达（/getting-started/quickstart 等 ASCII 路径 404），故只链根 |
| 许可/徽章 | "License / status" 段含 M1~M14 tag 叙事；无 badges | 无徽章可清理；状态段改为如实两句：pre-GA + 仓库无 LICENSE 文件（未声明开源许可）+ 自带三档体系 | `ls LICENSE*` 不存在；go.mod/package.json 无 license 字段 |
| 版本标注 | 无 | 头部 "applies to v1.0.0"（与 Fern 站口径一致） | fern/pages/index.mdx「本文档适用于 BinFlow v1.0.0」 |

### 发现的文档面不一致（他域，仅登记不修）

- `fern/pages/integrations/docker.mdx` L99「docker virtual 建仓 400（不支持聚合）」与 `docs/user/docker-registry.md` L127「virtual 聚合暂不做」均为旧口径——代码（validate.go/docker_test/t80 + M15 烟测）已开禁 docker×virtual。建议 fern/docs 维护方跟进。

## 5. 验证记录（实际执行）

- `make build` → bin/binflow-server、bf、bf-migrate 三产物 + 六平台发布档。
- 沙箱实例（/tmp，`BINFLOW_ADMIN_PASSWORD` 生效）：
  - `curl /binflow/api/system/ping` → `OK`；`/readyz` → 200；`/binflow/ui/` → 200
  - 建仓 → `Successfully created repository 'generic-local'`
  - 上传 `hello.txt` → 回显 sha256 与本地 `shasum -a 256` 逐位一致
  - 匿名下载 → `diff` identical、sha256 复验一致
  - `GET /api/v1/auth/methods` → `{"password":true,"oidc":false,"ldap":false}`
  - 口令轮换 PUT → `Password has been successfully changed`，新口令 /health 200
  - `bf repo create demo` / `bf artifact upload`（bin/bf）全链成功
  - `GET /metrics` 匿名 200（binflow_addon_gate_requests_total 等指标族）
- compose 路径：命令与 `deploy/dev/`（.env.example、docker-compose.yml 守卫、BIND_PORT/127.0.0.1 绑定）逐项核对 + release-engineer M15 烟测证据（deploy/README.md），未重复起 docker 构建。
- 链接有效性：两文 51 个相对链接目标全部 `OK`（脚本逐一 `[ -e ]` 核验）；站内锚点 #configuration / #配置 与标题匹配。
- 沙箱已停（pkill 后 ping 000）、/tmp/t480-qs 已删；`git status` 确认本轮写入仅 README.md、README.zh-CN.md、本日志。

## 6. 遗留 / 待确认

1. **Fern 与 docs/user 的 docker virtual 旧口径**（见 §4 登记项）——需 fern/ 与 docs/user/ 维护方翻新，非本票写入面。
2. 版本号 "v1.0.0"：dev 构建自报 `dev`，发布链版本形如 `v1.0.0-ci.<sha>`；README 采用与 Fern 站一致的 v1.0.0 口径，若后续定版号请两处同改。
3. 开源许可声明缺席为现状如实记录；若决策补 LICENSE，状态段一句话随之更新。
