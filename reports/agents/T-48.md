# T-48 工作日志 — §5.1 依赖方向勘误 + N2 债务登记 + T-33 RepoTypes 勘误落地

- 角色: architect
- 日期: 2026-08-18
- 输入: reports/agents/T-38-review-arch.md（第二节 N1 措辞 / N2 措辞）、reports/agents/T-33-review-arch.md（第三节 RepoTypes 四点勘误——核对确认**从未正式落入** architecture.md，§5.1 注释仍是 T-30 前旧草图，本票一并落）
- 状态: done

## 落点清单（architecture.md 五处编辑）

### 1. §5.1 依赖方向勘误（T-38 review N1 措辞）

原文「禁止直接 import storage/metadata（唯一例外：经 repo.Service 扩方法）」与 §5.3 映射表（POST→BeginSession 等）互相矛盾——新读者按 §5.1 会误判 T-38 实现违线。改为**两条明确例外 + 其余维持**：

- **例外一**（§5.3 裁定第 1 条，docker blob upload）：上传端点族直持 storage.Engine 驱动 Session 生命周期，协议态（received、UUID 配对）在 adapter、storage 不感知协议头；仅限上传会话对接，blob 读路径仍经 repo.Service.Get。附带补 metadata 路由数据只读查询的同构先例（T-14 终判匿名读前置缝，docker repolookup 同构）。
- **例外二**（T-13 review B1 终判先例）：digest 台账 READ-only 查询经 consumer-side BlobLedger 接口注入。
- 「需要流式细节时经 repo.Service 扩方法，不得绕过」维持；§5.3 blob upload 对接段加反向交叉引用。

### 2. §11 技术债第 13 行（T-38 review N2）

`PutLandedBlob 用例缺口`：docker finalize 后 store.Open 回读喂 svc.Put 绕过「扩方法」条款 + 每次 finalize 多一轮 O(size) 回读与三摘要重算；根因是 repo.Service 缺「从已提交 BlobRef 建台账行 + node」用例。处置写明：M3 前增 `PutLandedBlob(ctx, p, repoKey, path, ref, mime)`（PutFromBlob 建台账变体），docker finalize 与未来 maven/npm chunked 共用，届时删 adapter 回读；独立 dev-go-core 小票，不塞 T-39。

### 3. T-33 RepoTypes 四点勘误正式落地（核对结论：T-26 时未落，本票落）

- Handler 注释：RepoTypes 收窄为**声明性元数据**（供校验/文档/未来 class 能力），不得作为分发 map 键；docker M2 声明 `{"local"}`，原草图「{local,remote,virtual}」随 remote/virtual docker 顺延 M3 勘误。
- Register 注释：byType 键仅 package type（≡ Protocol()）；「重复 class panic」条款删除（generic 与 docker 同声明 class=local 合法）；空 RepoTypes panic 保留。
- 新增「分发 map 键约束」段：httpapi adapters map 键仅 package type，class 键为零读取死键且唯一碰撞源，New() 不写、不得 reintroduce；dispatchContent 语义不变。
- **Layout 错误契约两态化**：语法错（dot-segment/坏编码/超长）wrap ErrBadRequestPath→400；不可寻址形状（单段 name/无路由尾）→404 形。Layout 注释同步。

## 边界自律

- 仅改 docs/design/architecture.md（§5.1 三段 + §11 一行，共五处编辑）；未改 DECISIONS.md（无新决策，均为已定裁定的文档同步）；未改代码；未动 BOARD.md。
- 措辞以 reviewer 给定文本为准，architect 仅做编排与交叉引用补全。

## 遗留问题

1. §11.13 的 PutLandedBlob 小票需要 conductor 派发（dev-go-core area），M3 前完成；T-39 派单时建议附本日志链接防实现者按旧 §5.1 误判。
2. T-33 勘误第 2 点的代码侧（httpapi New() 删 class 键写入）属实现票范围——本文档已定语义，代码是否已按此执行归 conductor 核对（T-38 取证显示 adapters map class 键仍存在写入路径，与勘误目标态不一致，需实现票跟进）。
