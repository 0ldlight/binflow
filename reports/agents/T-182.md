# T-182: [P2] console-ux 复制页回写

- **Role**: architect
- **Area**: `docs/design/console-ux.md`（唯一改动文件）
- **Status**: done（自测全绿，见下）
- **Dep**: T-159（复制页实现，done）；款式基准 T-176（迁移面板回写，working tree 未提交状态——先读其 §3.2/§4.11/§10.3 写法后落笔）

## 做了什么（AC 对应）

1. **AC① §3.1/§3.2 路由表补录**：
   - §3.1 导航树治理组在「存储 & GC」与「备份 / 恢复」之间补「复制」条目
     （`/governance/replication`，与 AppShell.tsx 实际顺序一致：审计日志 → 存储
     & GC → 复制 → 备份 / 恢复 → 配额），注记「v1.3 补列，T-182 回写：单向 push
     复制目标与事件状态页，实现先于契约——见 §4.11」。
   - §3.2 路由表新增 `/governance/replication` 行：数据源 `GET
     /api/v1/replication/status`、10s 轮询、四态收敛；数据源单元格标注**前端
     假设契约**（形状按 internal/replication/model.go 推定；假设清单见
     reports/agents/T-159.md 与组件头注释，T-180 桥接票对齐基准）。
   - 连带一致性修正（§3.1 补列的直接后果，共 3 处）：导航条目总量 12 → 13
     （§3.1 注记）；admin 可见入口 12 → 13（§3.3 表）；治理组页面枚举
     「审计/GC/备份/配额」→「审计/GC/复制/备份/配额」（§3.6.4 矩阵）。
2. **AC② §4.11 复制页线框**：先读 `web/src/pages/governance/ReplicationPage.tsx`
   （338 行全量）再写，全部要素取自实现，无凭空设计：
   - 页头「复制 · 单向 push：源仓库 → 目标实例（ADR-0021）」（组件 308-312 行）
     + 两张只读卡（复制目标 / 最近事件）；每 10s 轮询（REPLICATION_POLL_MS =
     10_000，挂载即取；轮询刷新不清空已到达数据——loading 骨架仅首帧）。
   - 目标表 9 列如实：状态（运行态标签 + 状态点，优先级 已停用 > 异常（N 失败）
     > 复制中 > 排队（N）> 正常，targetState() 135-142 行）/ 目标名 / 目标 URL
     （mono+拷贝）/ 仓库（源 → 目标）/ pending / 进行中 / 失败（>0 标红）/
     累计成功 / 上次成功（'—' = 从未成功）；`targets: []` → 「未配置复制目标」
     空态（配置经 REST /api/v1/replications 或实例配置——M6 桥接后可用）。
   - 事件表 6 列如实：时间 / 状态 badge（值原样英文不翻译，沿 §4.10 口径；009
     闭集 pending|in_progress|success|failed|skipped）/ 制品路径（replication_id
     映射源仓前缀补全）/ sha256（截断展示、拷贝完整值，§7.3）/ 尝试 / 最近错误；
     跨配置合并、时间倒序（最近 N 条，无分页——量级远低于 §6 虚拟化阈值，
     T-159 遗留 3）。
   - 四态收敛（§5.1/§3.6.3）：403 → L2 单张无权限卡（复用缺省 empty-state 锚）；
     404（端点未桥接）/ 501（实例未启用复制）→ 降级一句提示；其它错误无数据 →
     错误卡 + 重试、有旧数据 → 保留表格 + 行内「上次刷新失败」（瞬断不清屏，
     同迁移面板语义）；控制台只读——CRUD/trigger 归 REST 面与 CLI。
3. **AC③ §10.3 治理组补 `repl-*` 锚**：逐一 grep 自组件（`data-testid` 9 处 +
   EmptyState `testid` prop 2 处），**11 锚**如实登记：repl-page /
   repl-targets / repl-targets-table / repl-target-\<i\> / repl-empty-targets /
   repl-events / repl-events-table / repl-event-\<i\> / repl-empty-events /
   repl-unavailable（404/501 降级）/ repl-stale（轮询瞬断）；并注明 403 分支
   复用缺省 empty-state 锚（§10.1 四态基元）、两空态锚为 testid prop 形态
   （沿 §10 导言「含 testid 以 prop 形态传入 EmptyState」惯例）。
4. **AC④ 回写标注**：三处补录点均带「T-159 回写：实现先于契约，T-159 核验
   发现」+「端点为前端假设契约待 T-180 桥接对齐」注记；§4.11 框尾专设「回写
   记录」段（假设契约推定来源 internal/replication/model.go、序列化沿
   MigrationStatusView 惯例、假设清单指针 reports/agents/T-159.md 与组件头注释）。
5. **AC⑤ T-176 同款式**：框体采用同款「──── T-159 回写：实现形态
   （ReplicationPage）────」题头 + 80~82 显示列宽（迁移框实测 80~83，同容差）；
   §10.3 采用同款「（T-159 回写，随 T-182 补录——…；e2e/replication.spec.ts
   断言）：」块头；与 T-176 一致**不升版本、不动 §0 修订表**（补录非改版，
   溯源靠内联标注；文首版本仍为 v1.3）。

## 刻意不动（避免范围蔓延）

- §5.3 四态矩阵不新增「复制」行：T-176 对迁移面板同样未加行，四态已内联在
  §4.11 框内（AC⑤ 同款式）。
- §10 文末「锚总量（v1.2 核对基准）242/27」不动：该数字是 v1.2 全量 grep 快照，
  T-176 增补 14 锚时也未回算；当前工作树含他人 in-flight 前端改动，现算会把
  他人增量混入，全量复核归下一版 testid 盘点票。
- §10.4 / §9 R 清单 / 文首票据行与状态行：均无复制页相关条目需动。

## 自测（实际运行）

```
# 1) 补录锚逐一存在于组件（11/11 OK，数量与 ground truth 一致）
$ for a in repl-page repl-targets repl-targets-table 'repl-target-${i}' \
    repl-empty-targets repl-events repl-events-table 'repl-event-${i}' \
    repl-empty-events repl-unavailable repl-stale; do grep -q ...ReplicationPage.tsx; done
  → 11 anchors OK（repl-targets/repl-events 各 2 hits：卡锚为表锚前缀）
$ grep -o "repl-[a-z-]*" web/src/pages/governance/ReplicationPage.tsx | sort -u
  → 11 个去重族，与 §10.3 登记一一对应（数量如实）

# 2) git diff 仅 console-ux.md（本会话前后 diff --stat 快照对比）
$ diff /tmp/t182-diff-before.txt /tmp/t182-diff-after.txt
  → 仅 console-ux.md 16 → 56 行变化（T-176 基线 +36 行 = 本票增量）；
    milestone-6.md 的 29 行变化系并行票 T-181（product-manager，area
    docs/prd）同工作树所致，非本票触碰（本票全部 Edit 仅 console-ux.md）
$ git diff docs/design/console-ux.md | grep '^@@'
  → 7 hunks：§3.1(+2)/§3.1 注记/§3.2(+1)/§3.3/§3.6.4/§4.11(+33)/§10.3(+12)

# 3) 陈旧计数清零
$ grep -n "12 个" docs/design/console-ux.md → none
```

## 偏差与说明

- 无票面偏差。AC④ 的假设契约注记在 §3.2/§4.11/§10.3 三处均落位（AC 只要求
  「标注」，多点冗余是为任一处单独被读时假设不被丢失——T-180 对齐时的防漂移）。
- 版本策略沿 T-176：不升 v1.4、不加 §0 修订行。若 conductor 认为补录也应记
  版本，一行改文首状态行 + 一行 §0 即可，不阻塞本票。

## 遗留

（无新增）既有遗留维持：T-159 遗留 1（T-180 桥接对齐，本票三处标注即其 UI 侧
锚点）、遗留 3（事件分页待真实端点）、遗留 4（桥接前深链显示降级提示为预期）。
