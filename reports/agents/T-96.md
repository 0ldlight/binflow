# T-96 工作日志 — 备份/恢复 CLI：export / import（FR-32，GE-07/08/09）

- ticket: T-96 [P0] M4 批 3 `role:dev-go-core`（双 reviewer 票：正确性 + 架构）
- 状态: done（自测全绿，待 review）
- 日期: 2026-08-20
- 输入: reports/agents/T-88.md T-96 节 AC 全文；ADR-0015（决策 3/4/5 + T-108 勘误③）、ADR-0006 勘误②（mtime 硬约束）、ADR-0012（enc:v1 恢复链）、architecture §4.1/§4.4/§7.6/§11.19；PRD milestone-4 §FR-32（W28/W28b/W30/W30b/W31/W32）与 §8 剧本；BOARD.md（只读）
- Q4 写死口径执行: 在线 export（锁互斥 + 快照先行 + manifest 边界，窗口内多余 blob 允许）；import 一律停机 + 空 data dir；`--tar` P2（flag 已定义、显式报「not implemented in M4」）

## 做了什么

### 1. 互斥锁原语（storage.AcquireDataLock——T-94 未落地，本票先行实现并申报）

**申报（T-94 派单时避免重复）**：`internal/storage/datalock.go` + `datalock_unix.go` + `datalock_windows.go` 已实现 data 目录级跨进程互斥锁。T-94 只需在 REST gc 面（`internal/httpapi/system/gc.go`）消费 `storage.AcquireDataLock(dataDir, "gc")` 并在 409 body 里带上 `ErrDataLockHeld`/锁文件内容即可，**不要重写原语**。

- 形态（R9，供架构 review 把关）：`<data_dir>/.maintenance.lock`，0600，flock（unix）/ LockFileEx（windows，kernel32 直调——不把 x/sys 从 indirect 提为 direct，ADR-0005 依赖门不动）；`LOCK_EX|LOCK_NB`，争用返回 wrap `ErrDataLockHeld`；锁文件首行写持有者诊断记录 `pid=<n> op=<gc|export|...>`（进程死掉会留 stale 记录，错误文案明确说 "last holder"）。
- 同进程两个 fd 也互斥（flock 语义按 open file description）——serve 进程内 REST gc 与 CLI export 进程在同一 data 目录上真实争用。
- **gc CLI（cmd/binflow-server/main.go runGC）已接线持锁**（acquire → 整个 mark/sweep/ledger 清理 → release）：这是我的 area（cmd），使 W25b 双向互斥今天即可真机验证；REST 面归 T-94。
- import 不取锁：目标是空目录 + 停机，无争用对象（日志注明）。

### 2. export 子命令（GE-07，W28/W28b）

`binflow-server export -c <cfg> --output <dir> [--tar]`，流程（顺序硬规则，ADR-0015 决策 3）：

1. 取 data 目录锁（与 GC 双向互斥）；
2. **先 DB 快照**：`VACUUM INTO`（在线 backup API 面，serve 持续可写，WAL 兼容；实测产物为 plain rollback-journal 单文件、无 -wal/-shm 残留，已拒绝对已存在目标重写）；快照落 `<out>/metadata.db` 后 chmod 0600；
3. **purge 瞬态**：`DELETE FROM web_sessions`（架构 §11.19：web_sessions 不入备份；remote_cache 保留——其行描述随 blobs/ 走的缓存）；
4. **拷贝 blobs**：整树 `<data>/blobs/<2hex>/<sha256>` → `<out>/blobs/`，逐文件 `Chtimes` 保 mtime + 保权限位（ADR-0006 勘误②）；只拷合法形态（2-hex 分片 + 64-hex 且前缀匹配），外来文件不洗白（与 GC 扫描同过滤）；
5. **manifest.json**：`{formatVersion:1, createdAt, binflowVersion, blobCount, totalBytes, metadata:{file:"metadata.db", sha256}, graceNote, blobs:[{sha256,size,mtime}]}`（blobs 清单 = ADR-0015「sha256 清单」，按 sha 排序——import「前 100」抽验因此确定）；**引用集 = 快照自身的 nodes ∪ docker_refs**（不是磁盘现状、不是 blobs ledger——窗口内新增 blob 允许多拷但不被引用，import 后成 GC 候选）；导出时逐一 stat 输出文件，**引用缺失 = fail-fast**（源实例悬空引用，W28b「全存在」由导出侧自证）；
6. 产物目录 chmod 0700（NFR-S22，umask 无关）；metadata.db/manifest.json 0600；
7. `export.run` 审计写**源实例**（快照已封存，事件属源实例历史——恢复侧不含它，属正确语义）；actor=admin（对齐 T-94 对 gc.run 的 CLI 口径）；action 常量消费 T-93 已落的 `audit.ActionExportRun`（期间 T-93 落地，我删了本地重复常量改消费其面）；结构化 INFO 日志含吞吐（NFR-P19 记录不设门）。

防护：--output 非空拒绝（防混批）、output 与 data dir 互相嵌套拒绝（防自读自写）、失败清理半成品（blobs/metadata.db/manifest.json）。

### 3. import 子命令（GE-08，W30/W30b/W31/W32）

`binflow-server import -c <cfg> --input <dir> [--verify spot|full]`：

- **前置**：目标 data dir 必须空（唯一豁免 `.maintenance.lock`——锁残留不是状态；binflow.db/blobs/sessions 任一存在即拒，退出码非 0）；此检查先于读备份——目标永不被污染；
- **校验阶段（只读、先验证后写盘）**：manifest 结构校验（formatVersion ≤ 1、blobCount == len(blobs)、totalBytes == Σsize、metadata.file 必须裸文件名防穿越、sha256 形态、去重）；metadata.db 实测 sha256 == manifest 值；**schema 天花板**：快照 schema_migrations > 本 build 最新版 → fail-fast（迁移链只向前，ADR-0015 决策 4；旧快照在恢复后首开时自动升）；blob 完整性 = **size 全验 + sha256 抽验前 100**（sha 排序后的前 100，`--verify full` 全量重哈希）；
- **写阶段（db 先、blobs 后，ADR-0015 决策 4）**：copy metadata.db → sqlitePath(cfg)（默认 data_dir/binflow.db，显式 dsn 尊重之）+ CopyBlobsTree 保 mtime → data dir chmod 0700 → `metadata.Open` 打开恢复库（旧 schema 就地升迁）→ `import.run` 落审计（actor=admin，action 消费 T-93 的 `audit.ActionImportRun`）；**任何失败 clearDirContents 清回空（无半恢复）**，stderr 明示；
- `--verify` 非法值（非 spot/full）400 风格 CLI 报错。

### 4. GE-09 / 边界

`/api/export/**`、`/api/import/**` 路由本就无此面——真机验证 404 + E-01 信封（见下）；无代码（PRD「不做」项，Q4：import 写面高危不走 REST）。`--tar` flag 定义但显式 fail-fast「not implemented in M4 (P2 backlog)」。

### 5. metadata 快照面（internal/metadata/snapshot.go，新文件、与并行票零文件交集）

- `(*sqliteStore).VacuumInto(ctx, dst)`——cmd 侧消费接口 `metadataSnapshotter` 定义在消费侧（cmd），类型断言满足；
- `SnapshotChecksums(ctx, path)`——只读（`?mode=ro`，文件级只读 + 文件 mtime/size 不变的断言测试）打开快照，`SELECT DISTINCT sha256 FROM nodes UNION SELECT DISTINCT blob_digest FROM docker_refs`（GC mark 同形；不经 metadata.Open——快照是制品不是活库，Open 会跑迁移+种子）；
- `SnapshotSchemaVersion` / `LatestSchemaVersion`（后者不硬编码 4/5——005 已随 T-95 并行落地，动态取嵌入集顶；测试用关系断言）；
- `PurgeTransientFromSnapshot`——rw 打开但**不带 WAL pragma**（不改制品 journal mode、不留伴文件）。

### 6. storage 备份助手（internal/storage/backup.go，新文件）

`Manifest`/`ManifestMetadata`/`ManifestBlob` 类型 + `Validate`（ErrManifestInvalid 哨兵）/`WriteManifest`（0600）/`LoadManifest`（容忍未知字段前向兼容）；`CopyBlobsTree`（保 mtime/mode、合法形态过滤、缺源=空库）；`HashFile`；`BlobPath`（导出的 engine.blobPath 改为委托它，消重复）；`SortManifestBlobs`。

## 改动文件

| 文件 | 变更 |
|---|---|
| `internal/storage/datalock.go` 新 | data 目录跨进程互斥锁（类型、哨兵、持有者记录） |
| `internal/storage/datalock_unix.go` 新 | flock（//go:build unix） |
| `internal/storage/datalock_windows.go` 新 | LockFileEx/UnlockFileEx（kernel32 直调，SyscallN；GOOS=windows 编译验证过，运行时行为无 windows 真机——见遗留） |
| `internal/storage/backup.go` 新 | manifest 类型/校验/读写、blobs 拷贝保 mtime、HashFile、BlobPath |
| `internal/storage/engine.go` 改 | blobPath 委托导出的 BlobPath（4 行去重，零行为变化） |
| `internal/metadata/snapshot.go` 新 | VacuumInto + 快照只读检查（checksums/schema 版本）+ 瞬态 purge |
| `cmd/binflow-server/backup.go` 新 | export/import 子命令（约 500 行） |
| `cmd/binflow-server/main.go` 改 | 子命令分派 + usage 文本 + gc CLI 持锁（+注释） |
| `cmd/binflow-server/backup_test.go` 新 | 命令级测试（含损坏矩阵） |
| `internal/storage/backup_test.go` 新 | manifest 矩阵/拷贝 mtime/锁互斥 |
| `internal/metadata/snapshot_test.go` 新 | 快照一致性/checksums/schema/purge |

并行协同：T-93 期间落地了 `audit.ActionExportRun/ImportRun` 与 metadata 测试基建，本票改消费其常量；T-95 期间落地 005 迁移（LatestSchemaVersion 动态取值所以无感）；两者均无文件冲突。

## 自测（实际运行过的命令与输出摘要）

构建/静态：
- `go build ./...` ✅；`go vet ./cmd/... ./internal/storage/... ./internal/metadata/...` ✅
- `golangci-lint run ./cmd/binflow-server/... ./internal/storage/... ./internal/metadata/...` → **0 issues**（整库 `make lint` 当前在 internal/adapter/docker 测试文件上报 typecheck——T-95 给 repo.Service 加 Usage 后 docker 侧 fake 未跟上，非本票文件）
- `gofmt -l internal cmd` → 空
- 交叉编译：`GOOS=windows go build ./internal/storage/`、`GOOS=linux go build ./internal/storage/`、`GOOS=linux GOARCH=arm64 go build ./...` ✅

测试（race）：
- `go test -race -count=1 ./cmd/binflow-server/ ./internal/storage/ ./internal/metadata/` → **全 ok**（cmd 32s / storage 60s / metadata 44s）
- 整库回归（非 race）：16 包 **全 ok**（含 adapter 五协议、httpapi、repo）
- 关键用例：损坏矩阵 table-driven 8 行（删引用 blob/篡改 blobCount/篡改 totalBytes/翻转 metadata.db 字节/截断 blob/未来 schema 版本/未来 formatVersion/非空目标）；spot-vs-full（101 blob：前 100 内损坏默认即拒、第 101 个损坏 spot 放行 full 拒——P0/P1 分界固化）；悬空引用导出拒绝 + 半成品清理；锁双向（gc 持锁 export 拒 / export 持锁 gc 拒）；W32 mtime 相等；import.run 审计 + 两次导入指纹一致（幂等）；VACUUM INTO 并发写下快照一致。

真机自测（W28~W32 全序列，产物在 /tmp/t96/e2e/selftest4.log）：
- W28：serve 运行中 export 退出码 0；manifest 字段齐全；metadata.sha256 与 `shasum -a 256 bk/metadata.db` 一致（124c9962… 两处相同）；产物 `drwx------`（0700）、metadata.db/manifest.json `-rw-------`；
- W28b：export 后新上传 sha ∉ manifest（OK）；manifest 引用 6 blob 全存在（missing=0）；ledger-only 孤儿 blob 随拷但不被引用（单测断言）；
- W25b 双向：python flock 持锁 → export 退出码 1；export 大拷贝运行中 → gc 退出码 1（错误均含 `data directory is locked`）；
- W30：停 serve → `import --verify full` 退出码 0 → 恢复实例 GET：generic a.bin 200（sha 2b0ecdd6… 与导出前一致）、big.bin 100MiB 200（7d0f10bc… 一致）、maven jar 200（同 sha）、npm tarball 200（内容 diff 一致，npm 10.9 真客户端 publish）、pypi 200（sha 一致）、docker manifest+layer 200（/v2 面 curl 驱动——本机 Docker Desktop daemon 只能经 host.docker.internal 且未标 insecure，CLI push 需改 daemon 配置故不做，归 T-103）；jane Basic 200 / 授权仓 200 / 非授权仓 403；jane token Bearer 200；审计历史（download/deploy/repo.create）全在 + import.run 落库；remote 凭据密文 `enc:v1:…` len=67 原样随行、API 回显 password 字段缺席；
- W30b：恢复库无 `BINFLOW_REMOTE_CREDENTIALS_KEY` 启动 → **fail-fast**（serve 退出，log 名出 env 变量）；设 key 后 ping 200；
- W31：删引用 blob → import 退出码 1 且 `/tmp/fresh2` 为空；`jq '.blobCount += 1'` 篡改 → 退出码 1（blobCount 7 ≠ 6 entries）；
- W32：6 blob 的源/恢复 mtime_ns+size 对账 **0 mismatch**（grace 时钟不重置）；
- GE-09：`GET /api/export/system`、`POST /api/import/system` → 404 + E-01 信封；
- NFR-P19 吞吐记录：100MiB+ 在线 export `throughput=400.8 MiB/s`（249ms，含快照+拷贝；APFS 本机，记录不设门）。

## 规格依据

- ADR-0015 决策 3/4/5 + 勘误③（在线 export 顺序硬规则、import 仅空实例与迁移链可达、import 不走 REST、GC↔export 互斥）；ADR-0006 勘误②（mtime 必保）；架构 §7.6/§11.19（顺序/WAL 面/web_sessions 不入备份、remote_cache 随 db）；PRD FR-32-AC1~AC7 与 §8 W28~W32 命令行。产物目录形态取 PRD 面（`<out>/blobs/` 目录 + `--tar` P2），架构 §7.6 写「blobs.tar」为 ADR 草案形态——PRD 后出且为 W 序列验收锚（T-88 R1 同款裁决先例），此处按 PRD。
- manifest 的 `blobs` 顶层清单字段为 ADR-0015「manifest（版本、时间、blob 数、sha256 清单）」中「sha256 清单」的落形，PRD 字段集的超集（PRD 只列举必答字段）；graceNote 常量含「产物按密文保管」警示（NFR-S22 文档配套）。
- 置信度处理：全部高置信度规格直接实现；「GC 运行中 export CLI 退出码非 0」的 409 文案方向（REST 面「export in progress」）归 T-94，本票只供 `ErrDataLockHeld` + 持有者记录。

## 遗留 / 需上游确认

1. **T-94 消费锁原语**（申报）：`storage.AcquireDataLock(dataDir, op)` + `ErrDataLockHeld` + 锁文件首行 `pid=<n> op=<op>`（409 body 素材）；REST gc 必须与 CLI gc/CLI export 同一原语，勿重写。
2. **windows 锁运行时未验**：datalock_windows.go 通过 GOOS=windows 编译/vet，但无 windows 真机（M4 部署矩阵 QA 未覆盖 windows 运行时）；实现为 LockFileEx 标准语义，遗留 M5。
3. **docker CLI push 真机**：本机 Docker Desktop daemon 无法以 127.0.0.1 直达宿主且未配 insecure-registry（改 daemon 配置超出安全边界），docker 协议保真以 /v2 面 curl 驱动验证（blob 上传 + manifest PUT/GET + sha 对账）；真实 docker/mvn/twine 客户端全链归 T-103（其 W30 断言含四协议真客户端）。
4. **groups 全保真（W30 的 jane/devs 组矩阵）**：groups REST 是 T-97（批 4），本票以 DB 整库快照语义覆盖（groups 表随 db 走，与 users/permissions 同列）；T-97 落地后 T-103 复验 W19/W40 链。
5. **产物 db 文件名 `metadata.db`**（PRD W28）与恢复落位 `binflow.db`（config 默认）不同名是有意的：备份内名固定（manifest 引用），落位尊重目标机 config（显式 dsn 亦然）；文档（T-107）需写明。
6. 架构 §7.6 的「blobs.tar / admin REST 异步 export」与 PRD 目录形态/P2 债务的出入，建议 architect 勘误回写时以 PRD 为准（本票实现即此形态）。

---

## 修复段 — correctness review REQUEST_CHANGES 回合（2026-08-20）

评审输入：reports/agents/T-96-review-correctness.md（B1 锁生命周期 + B2 driver/dsn 越界守卫；其余六区一次过）。与 T-94 在途改动同文件共存：datalock.go 的 DataLockOpGC/DataLockOpExport、LockHolder/HolderOp 与 main.go 的 GC 接线**原样保留**，本回合只在其上叠加。

### B1 — import 全程持数据锁 + 清理不 unlink 活锁

- `runImport` 在 loadServeConfig 与空目录检查之间取 `storage.AcquireDataLock(cfg.Storage.DataDir, storage.DataLockOpImport)`，整程持有（defer Release 注册在失败清理 defer 之前 → 清理在持锁状态下执行）。空目录检查与写阶段由此成为不可分区间：并发 `gc --apply` 在同一新目录上建 binflow.db 的竞态关闭；`requireEmptyDataDir`/清理对锁文件的既有豁免使整程持锁天然兼容。
- `clearDirContents` 跳过锁文件（判 `dataLockResidue = storage.MaintenanceLockName`，storage 侧为此导出该常量——单一拼写来源）；unlink-while-held 正是 datalock.go 注释点名的经典 flock 反模式，原清理路径在持锁时执行（defer 序），修复后活锁文件不再被自己的清理删掉。
- `storage.DataLockOpImport = "import"` 进 op 词表常量（词表注释同步扩展）。

### B2 — 非 sqlite driver / 越界 dsn 双守卫

- driver：import 在读备份之前拒 `metadata.driver != sqlite`（文案指明「恢复的是 SQLite 快照文件」）。postgres + URL dsn 不再被当路径喂给 MkdirAll/copyFile（评审实证的 `./postgres:/user@host:5431/binflow` 垃圾路径树消失，exit 非 0 且零残留）。
- 越界 dsn：写阶段前解析 `dbDst` 与 data 目录的相对位置（`pathOutside`），在 data 目录之外时要求目标**不存在**（Stat 命中即拒——O_TRUNC 静默覆盖既有库的洞关闭，ADR-0015 决策 4「仅空实例」补全到显式 dsn 落点）；非 ErrNotExist 的 Stat 错误同样拒绝。
- 失败清理 `cleanupFailedImport(dataDir, externalDB, hadExternal)`：data 目录清回空（锁文件豁免）+ **越界 dbDst 一并移除**——写阶段开始前已验证其不存在，此刻在那里的只可能是本次的半恢复产物，绝非操作者文件；data 目录外不再有残留逃逸。

### 补测（backup_test.go +4）

- `TestImportHoldsDataLockAgainstGC`（B1）：gc 持锁 → import 拒（ErrDataLockHeld）；释放后同命令成功（守卫是锁不是坏命令）。
- `TestClearDirContentsPreservesLockFile`（B1 单元）：db/blobs/sessions/杂文件/嵌套目录全清，活锁文件独存。
- `TestImportRefusesNonSQLiteDriver`（B2a）：postgres 拒（文案含 metadata.driver 与 sqlite）；fresh 目标零残留；CWD 无 `postgres:` 垃圾树。
- `TestImportGuardsOutOfDataDirDSN`（B2b）：既有外部 db → 拒（"already exists"）且**内容逐字未动**；子测「rollback removes out-of-bounds residue and keeps the live lock」单元级钉 cleanupFailedImport（越界产物移除 + data 目录清空 + 活锁存活——端到端中途写失败无法无竞态强制，测试注释如实声明，与评审建议的取证形态一致）；收尾外部 dsn happy path（db 落配置点、blobs 落 data 目录）。

### 本回合自测

- `go build ./...` ✓；`go vet ./cmd/... ./internal/storage/...` ✓；`golangci-lint run ./cmd/binflow-server/... ./internal/storage/...` → `0 issues.`（首轮报过一次 unused 系编辑中途态缓存，复跑干净）；`gofmt -l` 空
- `go test -count=1 ./cmd/binflow-server/ ./internal/storage/` → ok（26.4s / 37.5s）
- `go test -race -count=1 -run "TestDataLock|TestImport|TestExport|TestGC"` 两包 ok
- 四个新测 -v 复跑：4 PASS（含子测）

### 本回合变更文件

- cmd/binflow-server/backup.go（锁整程持有 + driver/dsn 守卫 + cleanupFailedImport/pathOutside + dataLockResidue 别名）
- cmd/binflow-server/backup_test.go（+4 测）
- internal/storage/datalock.go（DataLockOpImport 词表 + MaintenanceLockName 导出；T-94 的 GC/Export 常量与 LockHolder 族保留未动）
- cmd/binflow-server/main.go（仅 T-94 的 GC 接线与 op 常量加固，本回合零改动、随构建一并验证）

### 评审 non-blocking 项处置

NB1（checkDirDisjoint 时序）/NB2（mtime 双值析取）/NB3（LoadManifest 后防御性 Sort）/NB4（--verify full 的 mtime 对账）/NB5（windows LockFileEx 诊断降级）/NB6（export 对不存在源的防呆）/NB7（copyFilePreserving syncDir）——均未在本回合处理，维持登记待 conductor 归置（NB4/NB7 建议随 M4 收尾小票）。

---

## 修复段（2026-08-20，正确性 review REQUEST_CHANGES → 修复完成）

review：reports/agents/T-96-review-correctness.md（2 blocking / 7 non-blocking；架构 review 已 APPROVE）。B1/B2 已修，non-blocking 7 条未动（见下）。

### B1 — import 不参与数据锁 + 失败清理会 unlink 活锁

- `runImport` 全程持锁：`storage.AcquireDataLock(cfg.Storage.DataDir, storage.DataLockOpImport)`（新增常量，追加进 T-94 的 op 词表，只增不删），取锁在 requireEmptyDataDir **之前**——空目录检查与写阶段对 gc 成为不可分割区间（gc 持同锁同目录，双向互斥闭合）；`requireEmptyDataDir`/清理路径豁免锁文件，天然兼容。
- `clearDirContents` 跳过锁文件（新增 `storage.MaintenanceLockName` 导出常量 = 私有 `maintenanceLockName` 的别名，cmd 侧 T-94 的 `dataLockResidue` 本地常量改指向它——单一事实源，双拼写不漂移）；注释写明：清理发生在持锁期间，unlink 活锁 = datalock 类型注释点名的经典竞态。
- 测试：`TestImportHoldsDataLockAgainstGC`（gc 持锁 → import 拒 ErrDataLockHeld，释放后同命令成功——证明守卫就是锁而非命令损坏）；`TestClearDirContentsPreservesLockFile`（含 binflow.db/blobs/sessions/杂物的目录清空后仅剩锁文件，且幸存锁仍被持有——第二获取者仍拒、Release 正常）。

### B2 — 非 sqlite driver / 越界 dsn 无守卫（越界写、覆盖既有库、残留逃逸）

- **driver 守卫**：runImport 在任何路径运算之前拒非 sqlite（`cfg.Metadata.Driver != config.DriverSQLite` → fail-fast，文案点名「import restores a SQLite snapshot file」）；postgres URL 不再有机会被当文件路径在 CWD 下拉出垃圾目录树（reviewer 探针实证的形态）。
- **越界 dsn 守卫**：`dbDst` 解析为绝对路径并判定是否在 data 目录外（新增 `pathOutside` 助手）；在外时要求**不存在**才继续（ADR-0015 决策 4「仅空实例」，杜绝 O_TRUNC 静默覆盖既有库），并纳入失败清理：rollback 提取为 `cleanupFailedImport(dataDir, externalDB, hadExternal)`——写前已验不存在，故清理时在场的外部 db 必为本轮产物，可安全移除（data 目录外零残留）。
- 测试：`TestImportRefusesNonSQLiteDriver`（拒 + fresh 空 + CWD 无 `postgres:` 垃圾树）；`TestImportGuardsOutOfDataDirDSN`（既有外部 db → 拒且原文件逐字节未动；rollback 单元级——data 目录含活锁+残渣清空、外部 partial db 移除；happy path 外部 dsn 成功落位 + blobs 进 data 目录）。写阶段中途失败的端到端不可确定性构造（无竞态手法），以 rollback 契约单元测试覆盖 B2 关切面，已在测试注释注明取舍。

### 与并行票 T-94 的共存

- T-94 在制改动已保留：`storage.DataLockOpGC/DataLockOpExport`、`LockHolder/HolderOp`、export/gc 调用点的常量化、cmd 的 `dataLockResidue` 本地常量（我把它从字面量改指向 `storage.MaintenanceLockName`，消除其 unused 告警与双拼写漂移——只改值不改名，T-94 引用处零感知）。
- datalock.go 仅追加（DataLockOpImport + MaintenanceLockName 导出），未动 T-94 任何内容。

### 修复后自测

- `go build ./...` / `go vet ./cmd/binflow-server/ ./internal/storage/` ✅
- `golangci-lint run ./cmd/binflow-server/... ./internal/storage/...` → **0 issues**
- `gofmt -l` 空
- `go test -count=1 ./cmd/binflow-server/ ./internal/storage/` → ok（22.5s / 56.0s）
- `go test -race -count=1 ./cmd/binflow-server/ ./internal/storage/` → ok（43.7s / 242.3s）
- 新增 4 测试全绿：TestImportHoldsDataLockAgainstGC / TestClearDirContentsPreservesLockFile / TestImportRefusesNonSQLiteDriver / TestImportGuardsOutOfDataDirDSN

### non-blocking（未处理，供 conductor/review 轮定夺）

N1 checkDirDisjoint 应挪到 prepareBackupDir 之前；N2 storage mtime 断言应 per-blob 定值（cmd 级对账已正确）；N3 LoadManifest 后防御性 SortManifestBlobs；N4 --verify full 顺带 mtime stat 对账；N5 windows lockHolder 读降级（随 windows 运行时遗留）；N6 export 对不存在源 db 的防呆；N7 copyFilePreserving 补 syncDir。均为小改，未纳入本轮（blocker-only 纪律），可随下一轮或 T-103 前的小票收口。
