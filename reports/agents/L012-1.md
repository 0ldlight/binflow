Ticket:       LOOP 012 / L012-1 — npm dist-tags 扩章首票前段（真实 wire 取证）
Role:         differential-qa-engineer
Area:         tools/difftest/l0121/ + reports/compatibility/
Input:        契约域=npm dist-tags 族（尚无契约文件——本票即其证据前段）；模式=dual；A=:8082 Artifactory 7.161.20 / B=:8083 BinFlow 重建至 d301cf31（原 uat-l0111-293a754c-wip2）
Changes:      新增 wiretap.py（一次性线程化记录式反向代理，Authorization 落盘前 redacted，真实凭据仅存进程 env）、disttag-family.sh（npm 10.9.8 全族批跑入口）、curl-matrix.sh（20 格端点族矩阵）；UAT 重建链=git worktree d301cf31 → docker build Dockerfile.alpine → compose 滚动（数据卷保留）
Files:        reports/compatibility/L012-dist-tags-evidence.md（主交付）；reports/compatibility/l0121-wire{,-pass1}/{a,b}/*.log + curl-matrix-{a,b}.txt（原始 wire）；tools/difftest/l0121/{wiretap.py,disttag-family.sh,curl-matrix.sh}；deploy/compose/.env.uat（VER 行滚动，后被并行轨 L012-3 改写为 a81cd053-wip）
Tests:        CLI 腿 15 case × 双端 × 2 pass（exit 向量两遍零差）；curl 矩阵 20 格 × 双端 × 2 pass（byte-identical，md5 对拍贴于报告 §6）；负例（rm 幽灵 tag/幽灵包 ls/add 不存在版本）双端同为 4xx 族
Commands:     disttag-family.sh a|b（经 wiretap 19082/19083）；curl-matrix.sh a|b（--noproxy '*'）；判别探针（set latest→DELETE→GET）A/B 各一轮，B 在纯 d301cf31 一次性容器复判；npm 版本=10.9.8（评估票钉）
Outputs:      报告见上；提金候选=npm CLI happy 序列（ls/add/rm+scoped）、m12 再删 404（双端逐字一致）、%2f/%2F 等价双绿、ping {}；状态机反馈=V-7 闭合（npm 10.9.8 100% 显式端点族，零 packument 旁路/零 etag rev-dance）→ D12-R05 契约化对象即端点族本体
Compatibility: 差异分类建议：BUG 0 / UNSUPPORTED 0 / UNKNOWN 6（D2 DELETE-latest 读时语义=A 重算 vs B 真删【最重】；D3 PUT 404 版本/标签位文案错位；D4 幽灵 404 措辞；D5 坏 body A500/B400+Go 内幕泄漏；D6 bulk PUT/POST 面 A405/B201；D7 Cache-Control A max-age=60/B 无）；另有实例配置差 1（匿名读）与归一化面 3；回归基线=首份 npm 域差分（无上轮）
Security:     auth 面顺带对照：whoami/ping 双端与 K60 一致（401+Basic realm 各自产品名）；PUT 匿名双端 401；凭据零落盘（wiretap env + 日志 redacted 复核）；假 _authToken 仅过客户端 ENEEDAUTH 预检
Performance:  家族单 case 亚秒级；无超时（--fetch-timeout 30s 全程未触发）；publish ~0.3s/发
Risks:        ①UAT 容器收尾时点被并行轨 L012-3 换至 a81cd053-wip——已用 d301cf31 一次性容器复判关键探针且 git diff 证 npm/httpapi 零漂移，证据基线纯但时间窗交错在案；②wiretap 为取证工具非合规代理（hop-by-hop 处理简化）；③npm 缓存干扰已用 --prefer-online 顶掉，但该 CLI 行为（fetchTags kebab-case prefer-online 死代码）本身是客户端缺陷观察，复现者须知（E0-2）
Blockers:     无（Artifactory 一次中途重启 503，自愈后完成；第五波限额击落于落报告前，恢复后核实资产零丢失）
Next:         待 compatibility-engineer/conductor 裁定 UNKNOWN×6（D2/D6 为契约票前置必裁项）→ 后段拆票建议见证据报告 §7（契约票→实现票→差分翻绿票三段）
