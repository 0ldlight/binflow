Ticket:       T-L003-1 LOOP 003 轨道 L003-1 — prune/gc 管理面实现（主体）+ remote-cache 引擎 §8 B/C（串行）；P0/P1
Role:         dev-go-storage
Area:         internal/storage + internal/remote（主体）；最小侵入 httpapi（路由 10 臂 + Deps.BackupRunner seam + t215 计数表——ticket 明示允许）
Input:        docs/reverse/storage/prune-gc-admin.md（E1-E10/§2.1-2.2 schema/§3 语义/§6 差异清单/§7 V 挂账）；docs/reverse/storage/binary-provider-chain.md §4.3 G 组；docs/design/remote-cache-v2.md §5.1/§5.3/§8 + ADR-0047/0048（DECISIONS.md）；ADR-0012（TTL+条件再验证框架）；活体参照 :8082（admin basic，2026-09-10/11 两轮只读取证：prune/status 全量 26 字段、size、info、info 树 JSON 形态）
Changes:      ① internal/storage/prune.go：Pruner 能力面（可选接口，MultipartUploads 发现模式）——00..ff 全枚举逐目录走查，复用 GCSweep 同门（mark 快照/hold 门/grace 窗/删除前 hold→Live 双门，ADR-0031）；Stop 标记目录边界消费；StartFrom 跳过但计数（分母恒 256）；估算模式（dryRun）只计 processed 恒不删；per-dir 计数+Deleted 清单（blobs 台账拆除用）
              ② internal/httpapi/system_storage_admin.go：E1-E10 十端点——prune/start+stop+status（202 异步 job 化/pruneManager 单飞 412/停止标记/26 字段 schema 渲染/跨重启持久化 <data>/prune_report.json 原子写；装载时 running→stopped 恢复改写）、gc（200 同步 + text/plain;charset=utf-8 点流，码钉 200，错误流内行；DataLockOpGC 409 并发拒）、optimize（202 空体无 CT）、compress（复用 RunMetadataCompress 载体，流式）、backup（缺 key 500 errors 信封逐字对齐/disabled 500/成功臂 BackupRunner seam）、size（blobs/ du 裸数字）、info（statfs 投影，参照实例 usageSpace=分区占用口径对齐）、exportds（恒 500）
              ③ router.go +10 路由臂（写 CapSystemWrite/读 CapSystemRead，T-214① 姿态）+ t215 门计数 79→89 带台账注记；server.go +Deps.BackupRunner seam + s.prune manager 自装配
              ④ internal/remote/ttldefaults.go：包型默认单点解析（docker/helmoci 21600s，余 7200s；显式>0 优先/存量不回改/解析单点化）+ fetcher loadRepo 接线（fresh cfg 每读归一，land 与 304 时钟滑动双消费同值）
              ⑤ internal/remote/fetcher.go：条件再验证臂（§5.3 引擎腿）——过期 metadata 类副本带存档 ETag/Last-Modified 发 If-None-Match/If-Modified-Since；上游 304 → 既有 304 臂（时钟滑动 + CacheRevalidated 服务）；content 类恒无条件（digest 不可变，ADR-0012 决策 1）
Files:        新增：internal/storage/prune.go、internal/storage/prune_walk_test.go、internal/httpapi/system_storage_admin.go、internal/httpapi/system_storage_admin_test.go、internal/httpapi/storage_info_unix.go、internal/httpapi/storage_info_windows.go、internal/remote/ttldefaults.go、internal/remote/ttl_revalidate_test.go、reports/compatibility/L003-storage-admin-diff.md、本日志
              修改：internal/httpapi/router.go（+10 臂）、internal/httpapi/server.go（+2 seam 字段）、internal/httpapi/t215_guard_test.go（计数 79→89+台账）、internal/remote/fetcher.go（loadRepo 归一 + contactUpstream 条件臂）
              删除：无
Tests:        新增 storage/prune_walk_test.go（11 用例）：估算模式不删/apply 删孤儿留引用/grace 窗保护/hold 门保护在途上传/Live 不确定保守跳过并报错/停止标记一目录内落地/256 全枚举含缺失分片/startFrom 跳过但计数/非法 startFrom 拒绝/并发 prune 与发布者竞态（-race 验证）/ctx 取消即停
              新增 httpapi/system_storage_admin_test.go（12 用例）：PUD 生命周期（412 两臂/202 文案/并发 412/运行中 dryRun 回显/26 字段逐键）/stopped 终态/resume 文案与分母/D6 极性默认真删（实测断言）/报告跨重启保持/running 残留恢复为 stopped/gc 点流形态+409 并发/optimize 空 202/compress 流/backup 三臂/size+info 形态/exportds 恒 500/匿名 401+非 admin 403/坏 body 400——异步臂用 gatingPruner（注入等待的可控 job 框架）
              新增 remote/ttl_revalidate_test.go（5 用例）：解析器表驱动/包型默认端到端（docker 0 行→21600、显式 7200 不回改）/条件 GET 304→REVALIDATED+HIT 零上游/上游移动→200 全量替换/content 类无条件再取
Commands:     go build ./...；go vet ./internal/storage/... ./internal/remote/... ./internal/httpapi/；gofmt -l internal/storage internal/remote internal/httpapi；golangci-lint run internal/storage/... internal/remote/... internal/httpapi/...（本票文件 0 告警）；go test ./internal/storage/... ./internal/remote/... -count=1；go test ./internal/httpapi/ -count=1；solo race：go test -race ./internal/storage/ -count=1；go test -race ./internal/httpapi/ -run 'TestStorage' -count=1；go test -race ./internal/remote/ -run 'TestResolveContentTTL|TestFetchContentTTL|TestFetchMetadataRevalidation|TestFetchContentRefetch|TestFetchUnsolicited304|TestFetchDualTTL' -count=1；真实 curl 对拍（工作树实例 :8084 全端点 + 参照 :8082 只读腿，实录见 L003-storage-admin-diff.md 附录）
Outputs:      build/vet/gofmt/lint 全绿；storage 全套 ok；remote 全套 ok（98s）；httpapi 全套 FAIL 仅剩 4 个 TestV2*（docker token 面——stash 探针证明与本票无关：我的 httpapi 文件全部暂存后该失败仍在，系并行轨道 L003-2 的 adapter/docker 在途改动所致）；race solo：storage ok、httpapi TestStorage ok（曾抓到 snapshot() 返回活指针的真数据竞争——已改深拷贝复验绿）、remote 子集 ok；curl 实测关键行：`412 {"info":"No Prune task found"}`、`202 {"info":"Pruning Unreferenced Data task has been submitted"}`、运行中 `"progress":"233 of 256"`、resume 文案、gc `[HTTP 200 CT=text/plain;charset=utf-8]` 256 dots/3 newlines、optimize `[HTTP 202 CT=[] bytes=0]`、真删 `finished | dryRun False | processed 2 | cleaned 1 | bytes 24`、重启保持 `finished | 256 of 256 | dryRun True`
Compatibility: 端点族/响应形态/状态码/schema 逐条对照 prune-gc-admin.md（对拍矩阵见 L003-storage-admin-diff.md）：E2/E3/E4/E6/E8/E9 SAME；E1/E5/E10 形态 SAME（点数粒度/后端/信封细节登记）；E7 错误臂 SAME、成功臂 PARTIAL（cmd 接线 3 行未落，本票禁改 cmd）。差异清单 D1-D10 清偿计数：6 全清（D1/D2/D3/D4/D5/D6）、D10 端点全落（backup 成功臂除外）、D9 部分、D7 不动（形态映射）、D8 维持 known-divergence（既定裁定）。§8 B：解析器+引擎消费落地，repo 侧两处消费（config.go 建仓默认 + dockerremote.go remoteContentTTL 的 7200 兜底）未接线（internal/repo 禁域）——handoff 补丁：docker/helmoci 建仓默认改调 remote.DefaultContentTTLSecondsFor、remoteContentTTL 兜底改调 remote.ResolveContentTTLSeconds（需 row.PackageType 入参）；§8 C：引擎腿（metadata 类条件 GET+304→REVALIDATED）落地，docker 面客户端 304 透传臂归 L003-2（adapter/docker 禁碰；FetchResult.CacheState=REVALIDATED 即其映射信号）；§8 D：BLOCKED 跨域（见 Blockers）
Security:     十端点全部路由门禁（写 CapSystemWrite/读 CapSystemRead）+ handler 重复 admin 校验；匿名 401/非 admin 403 实测；backup 响应用 store 拼写非 query 回显（G705 taint 修复）；错误信息不含凭证（gc 409 信封走既有 gcLockRefusedMessage 脱敏链）；prune 报告文件 0600
Performance:  prune 走查为流式目录枚举（内存与制品数无关）；报告持久化分级——进度更新 rename-only（无 fsync）、终态 fsync（实测教训：逐目录 fsync ~170ms/dir×256≈40s+，分级后全枚举 ~180ms）；gc 点流每目录一点实时 flush；size 为 O(blob) du 单次走查
Risks:        ① E7 成功臂在装配实例 503（cmd 接线待落）② 并行轨道 L003-2 的 adapter/docker 在途改动致全树 4 个 TestV2* 失败（已证明非本票；其间全树 build 一度断，恢复后复核绿）③ prune 与 REST gc 并发时双清扫器竞逐未引用 blob（一门得手另一门得 NotFound 记 firstErr——保守方向，未锁互斥：Artifactory 语义未定，不擅加）④ V-1/V-2/V-4/V-5/V-7 维持规格挂账 ⑤ running→stopped 装载改写为 BinFlow 侧恢复纪律（源行为未覆盖）⑥ S3 引擎无 Prune 面——S3 装配实例该族 503（诚实降级模式，同 Uploads seam 先例）
Blockers:     §8-D（Invalidate 连带删 refs）：跨域 BLOCKED——所需面全部在禁域：metadata 缺 DeleteRefsByImage/前缀面（现仅有整仓 DeleteRepoRefs，且 DeleteRepo teardown 已接线 service.go:2809）；repo 缺全仓 Invalidate 载体面（现 Invalidate 仅逐路径，经 deleteRemoteCache；docker 缓存内容不走 node 行故无从连带）；「unused-cleanup 不删 refs」语义注释属 repo.CleanupEngine。L003-2 已落 ADR-0047 的 BlobInImageChain oracle，D 的链面消费点即其旁——建议 D 整票转 conductor 归 internal/metadata+internal/repo。另：E7 cmd 接线（dev-go-core，3 行：构造 backupRunner 提前到 Deps 装配前 + 传 Deps.BackupRunner）
Next:         ① conductor：E7 cmd 接线转派 + §8-D 整票转派 ② §8-B repo 侧双消费接线（补丁已在 Compatibility 给出）③ 契约票：E1 gc 409 信封/E5 compress 后端差异/E9 info 映射值（tempDir/fileStoreDir）归 contracts/ 冻结 ④ 差分复验：prune 并发 412 竞速（python 双线程，规格 §8 命令骨架）⑤ GC 压测（256 目录×大 blob 的 prune/gc 长跑 + 断电恢复）⑥ V-4 error 态故障注入取证
