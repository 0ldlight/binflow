# T-432 段一 — web 工具链升程（vite 6→7 + plugin-react 4→5 + eslint-plugin-react-hooks 5→7）

状态：done（四门 + build/relink/wire-brand + A/B 甄别 + m9 全绿；全量 e2e 见 §4 环境受限注记）
role: dev-frontend ｜ area: web/package.json + web/eslint.config.js（+ 证据用只读 git diff）
日期：2026-09-01/02 ｜ 分支：develop ｜ 零 git 写操作、BOARD 零写
集成注记：conductor 已于 09-02 00:35 将本段并入 commit `1a6338d`（feat(T-420,T-432-1)）——本日志 §1 的 diff 即该 commit 的工具链面。

## 1. 变更面（3 文件 + lock）

| 文件 | 前 | 后 |
|---|---|---|
| web/package.json `vite` | `^6.3.0`（lock 实装 6.4.3） | `^7.3.6`（实装 7.3.6） |
| web/package.json `@vitejs/plugin-react` | `^4.5.0`（实装 4.7.0） | `^5.2.0`（实装 5.2.0；peer `vite ^4–^8`——段二免再升，lock diff 实证：`"vite": "^4.2.0 || ^5 || ^6 || ^7"` → `…|| ^8.0.0"`） |
| web/package.json `eslint-plugin-react-hooks` | `^5.2.0`（实装 5.2.0） | `^7.1.1`（实装 7.1.1） |
| web/package.json `engines.node` | `>=20` | `^20.19.0 \|\| >=22.12.0`（vite 7 底座线；本机 v22.23.2 / CI node "20"→20.19.x 均满足） |
| web/eslint.config.js | 13–19 行手工块（`plugins:{'react-hooks':…}` + `rules:{...reactHooks.configs.recommended.rules}`） | `reactHooks.configs.flat['recommended-latest']` + ratchet override 块（见 §2） |
| web/package-lock.json | — | 193 insertions / 146 deletions（三 dep 直改 + 传递依赖） |

## 2. 票面条款④执行实录——BOARD 降级语义在 v7 失效与替代方案

`npm install` 后 `npx eslint .`：**37 errors**（set-state-in-effect 25 / refs 8 / immutability 2 / purity 1 / preserve-manual-memoization 1，横跨 21 文件）> 30 预算 → 按票面降级。**但实测 v7.1.1 只有两个 flat tier 且都全量启用 compiler 派生规则**：

```
configs.flat 键 = ['recommended-latest', 'recommended']
recommended(16 rules) 与 recommended-latest(17) 的差集 = 仅 react-hooks/void-use-memo
set-state-in-effect 在 recommended 里同为 "error"
```

即「降级 `configs.flat.recommended`」在 v6 语义下是回到经典两规则，在 v7 是**同一套 compiler 规则原样**（BOARD 该条款写于 v6 语义，v7 已失效）。逐条审视 12 处非 set-state-in-effect 违规：全部是**有意为之的 escape-hatch 模式**——AuthContext 的 render 期 latest-value ref 镜像（行内注释记载它修的 401 并发重复 toast 竞态，review B1）、MUI `anchorEl={ref.current}`、非受控 `holder` 逃逸、回调内 `window.innerWidth` 读取。在「行为零变化预期」的纯工具链票里重写这些 = 正是 >30 条款要防的 churn。

**替代执行**（保持票面意图：lint 绿 + compiler 规则上线 + 欠账可见）：
- 主 tier 用 `recommended-latest`（票面首选目标）；
- 命中存量代码的 5 条降 `warn`（CI 的 `npm run lint` = 裸 `eslint .`，warning 不败门）——每条 warning 都是可见的清理欠账（ratchet）；
- **10 条零违规的 compiler 规则**（set-state-in-render / use-memo / void-use-memo / static-components / error-boundaries / incompatible-library / globals / unsupported-syntax / config / gating）+ 经典两规则（rules-of-hooks / exhaustive-deps）全部 `error` 当日生效——较 v5 实为净增强。

结果：`npx eslint .` → **0 errors / 37 warnings / exit 0**。
（ratchet 欠账的清偿应另立行为敏感清理票：镜像/锚点模式的时序语义改动不属于工具链票。）

## 3. 自测证据

### 四门（web/）

```sh
npx tsc --noEmit                                   # 通过（零输出，exit 0）
npx eslint .                                       # exit 0：0 errors, 37 warnings（§2 ratchet）
npm run build                                      # 见下——vite 7 产物 + relink/wire-brand 兼容
node scripts/anchor-audit.mjs                      # broken=0（unregistered=0；dead 4 家族全既有）
node scripts/anchor-audit.mjs --ledger             # PASS（A1~A4 全绿）
```

**build（重点验证 relink-assets / wire-brand-assets 对 vite 7 产物形态）**：

```
✓ built in 36.35s
relink-assets: 2 full URL(s) and 1 preload join(s) -> /binflow/assets/ (2/77 candidate file(s) rewritten)
relink-assets: verified — no ui-segment asset reference survives under dist/
wire-brand-assets: 7 brand files fingerprinted onto /binflow/assets/ (apple-touch-icon …, manifest-ddcb36c7.json)
```

- 三面改写在 vite 7 下全部在场：index.html 2 个完整 URL（entry js + css）+ JS chunk 内 1 个 preload join（`"/binflow/"+n`，实测形态不变）；自检零残留。
- dist 内仅存的 2 处 `binflow/ui` 字面量均合法：`basename:"/binflow/ui"`（router，票面明确不触）+ AuthConfigPage 的 placeholder 文案 `https://registry.example.com/binflow/ui/login`；`binflow/ui/assets` 全 dist 零命中。
- chunk 图与 vite 6 完全同构（77 个 js/css/html 产物文件数不变）。

### 官方门 + 嵌入

```sh
make console    # cd web && npm ci（lock 重装成功）+ npm run build → 同 hash 产物
                # console SPA payload (gzip, js+css): 384,394 bytes（≤5MB W37）
make build      # 二进制全绿（check-size 94.13MB / 120MB 预算）
```

### SPA 产物尺寸对比（js+css+html，gzip）

| | 文件数 | raw | gzip |
|---|---|---|---|
| vite 6.4.3（升前 dist 实测） | 77 | 1,128,736 B | 386,054 B |
| vite 7.3.6（升后 dist 实测） | 77 | 1,126,503 B | 385,346 B |
| Δ | 0 | **−2,233 B（−0.20%）** | **−708 B（−0.18%）** |

主 chunk index：426,111 → 424,069 B raw；136,982 → 136,464 B gzip。无预算回归。

### e2e（净实例 :8080，BINFLOW_DATA_DIR=/tmp/bf-t432，BINFLOW_ADMIN_PASSWORD=password）

净实例启动实测：`/binflow/ui/` 200；共享挂载 `/binflow/assets/index-B-ygsu7s.js` 200（424,069 B——relink 后 URL 真解引用）。

## 4. e2e 结果（本机长夜饱和负载下的拼合证据 + A/B 甄别）

**环境前提（诚实注明）**：本机（用户实机）整夜处于重载——uptime 负载全程 63→481（16 核；两个常驻 VM + 远程桌面串流 + 多 agent 残留实例）。此负载级下「全量套件一轮全绿」在本机不可达（T-414 在册先例：同机负载超时 33 红 + 15 did-not-run，与 diff 零耦合）。证据为拼合式：全量两轮 + 定向甄别 + A/B 对照。

| 轮次 | 姿态 | 结果 | 判读 |
|---|---|---|---|
| 全量 chromium（270 测试清单） | 默认 4 workers，净实例 | **224 passed / 0 failed**（summary 另见 ~23 红 + 7 skip + 16 did-not-run——tail 截断导致首读误判 0 failed，trace zip 数 22 与 test-results 目录名对账后修正） | 红 = axe 扫描/CRUD/导航的负载超时家族；16 did-not-run = 串行组连带 |
| 红件串行甄别（17 spec 文件，100 测试） | `--workers=1` 净实例 | **37 passed / 55 failed**（其中 ~53 个 = **0ms seed 崩溃**：helpers.spec 的「REST 面播种 ≥10,000 节点」在负载下超时 → artifacts-tree/keyboard/repo-policy/theme-smoke 等整文件连带；真测试体红仅 2：a11y-sweep 全路由 11.3m〔CPU 密封〕+ t404:337） | 串行也无法过种子门 → 协议甄别力失效，改用 A/B |
| **A/B 对照：t404:337（双轮红、唯一存疑真测试体）** | 净实例 ×2，同窗同负载（load ~400） | **vite 6.4.3 基线二进制：1 passed（29.9s）**；**vite 7.3.6 二进制：1 passed（1.3m）** | **双绿 = 工具链无关**，先前双红为负载 flake。注：该腿的现行断言是 T-420 翻转版，票内未真栈实跑过（T-420.md 遗留 #1 明示） |
| **m9 usage-fanout** | `--project=m9 --workers=1` 净实例 | **4 passed（3.1m）** | 净实例口径全绿 |

A/B 方法注记：vite-6 基线二进制在 /tmp 沙箱副本树内构建（`npm install --no-save vite@6.4.3 @vitejs/plugin-react@4.7.0` + build + go build），主树零触碰；基线 build 同样过了 relink 三面自检（vite 6 产物形态对照）。首轮 A/B 误踩 **端口碰撞**（:8099 被他票残留实例 `bf-t300-headconsole-server`〔IPv4 127.0.0.1〕先占，seed Basic 认证打错服务 401）——已换 8321/8323/8325 已验空端口重跑。**本机残留实例占端口是跨票 hazard，后续净实例票建议先 lsof 验端口。**

**结论**：无 vite 7 行为回归证据；全部红可归因饱和负载（或其连带）。未能在本机取得「一轮全量全绿」——如需该形态的最终证据，建议在机器安静窗由 conductor 复跑一次 `npx playwright test --project=chromium`（预期基线形态 = T-419 的 259/1/10，唯一常红为在册假阳性家族）。

## 5. 遗留 / 注记

- **BOARD 条款④降级语义漂移**：`configs.flat.recommended` 在 hooks v7 非「温和 tier」（差集仅 void-use-memo），本票按 §2 替代执行；conductor 可据此回写票面/另立 ratchet 清理票（37 warnings，其中 25 处 set-state-in-effect 是行为敏感的 effect 生命周期重写）。
- **lint ratchet 起点清单**（清理票底稿，`npx eslint .` 现报 37 warnings）：set-state-in-effect 25（横跨 21 文件）/ refs 8（AuthContext·AppShell·DeployDialog·AuditPage 的 latest-value 镜像与 anchorEl——**有行内注释记载的竞态修复，动前先读注释**）/ immutability 2 / purity 1 / preserve-manual-memoization 1。
- **段二未动**（vite 8 Rolldown + plugin-react 6）——票面另票；本票已留对照证据：vite 6 与 vite 7 的 relink 三面形态一致（基线沙箱 build 亦过自检），Rolldown 的产物形态差异仍需段二票面前置验证。
- **工作树非本票变更**：`web/src/lib/replications.ts` 有 +27 行未提交（T-420 的 runReplicationNow API 腿，commit 1a6338d 未含——conductor 集成面，本票未触碰）。
- **过程异常记录**：任务中段收到过一次「package.json/eslint.config.js 被外部改回旧版」的文件状态通知，与磁盘实态不符（实测两文件均保持本票版本，md5 f4b793…/a1d135…）——判定为 conductor 集成流程（后证实为 1a6338d 合入）的瞬时状态抖动，未采取任何回写动作。
- 临时物已清：/tmp/bf-ab*（A/B 沙箱与数据）、/tmp/binflow-vite6、/tmp/bf-t432*；实例全部停止。证据日志保留 /tmp：t432-serial-rerun.log、t432-ab6.log、t432-ab7.log、t432-m9.log。
