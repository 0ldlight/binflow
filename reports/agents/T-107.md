# T-107 工作日志 — M4 用户文档

- ticket: T-107 [P1] M4 用户文档四篇（FR-33-AC6，DoD 第 4 条）`role:tech-writer`
- 日期: 2026-08-21
- area: docs/user
- 输入: T-88 T-107 节 AC、docs/user 既有篇目（M1~M3 风格先例）、console-ux v1.2、ADR-0014/0015（勘误后）、T-91/T-94/T-95/T-96/T-97/T-110/T-113/T-115 工作日志、T-103-qa/T-104-qa/T-105-qa 验收报告
- 状态: done（待主会话审核）

## 一、篇目清单（4 新增 + 1 增补）

| 文件 | 性质 | sidebar_position | 覆盖（对 AC ①②） |
|---|---|---|---|
| `docs/user/console.md` | 新增 | 30 | 控制台使用指南：登录/会话（**TTL 塌缩语义——会话必死于 created_at+TTL 与活跃度无关**）、CSRF Origin 防线（CI 零感知）、六组页面走查（仓库治理字段 local-only、树统一路径树+上传 sha256 对账、搜索 ACL 过滤、安全组三步流/模式测试器、治理审计/GC 二次确认/配额水位）、角色可见性（非 admin 制品面经树/搜索可达）、浏览器三引擎兼容（Chromium 主矩阵 47/47；WebKit/Firefox 三链经 T-120 修复后可用）、四态语义（401/403 分流、危险区确认模式） |
| `docs/user/admin/groups-permissions.md` | 新增 | 41 | 权限与组管理：三步流（组→用户入组→target）、并集、**即时生效**（groups:[] 即刻 403）、组 CRUD 表（PUT 201 无 body/已存在 200）、409 引用保护（列 target 名）、组名规则、`?permissions` 视图（key=主体名 value=r/w/d 字母集，T-113 形态）、**组无 admin 位**（有意不兼容） |
| `docs/user/admin/governance.md` | 新增 | 42 | 治理：审计查询（参数表/闭开区间/keyset 游标/append-only）+ 词表（以 internal/audit/api.go 常量为准：deploy/download/delete、repo.*、group.*+group.member、permission.*、password.change、gc.run、quota.exceeded、export.run、import.run、login.*）+ token.issue/revoke 不落审计已知缺口；搜索（name 子串 + checksum 精确 + ACL 零泄漏 + 未实现族 404）；GC 双步（dry-run→apply、graceHours 缺省 vs 显式 0、REST 与 CLI 的 0 值语义差异、互斥 409 含义、无状态端点走审计）；配额与 pattern（409 pattern 双态/404 下载、413 used/quota 双值、原子拒绝、**幂等重传豁免**、读删不受限、usage 端点、五协议客户端表现、D-1/D-2 先落残留边界如实注记） |
| `docs/user/admin/backup-restore.md` | 新增 | 43 | 备份恢复手册：export 在线（锁→快照先行→blobs mtime 保真的顺序硬规则与窗口语义）、产物形态与 **0700 保管告警（NFR-S22：含口令哈希与 enc:v1: 密文）**、import 停机+空目录（--verify spot=抽验前 100 / full=全量重哈希、无半恢复）、**BINFLOW_REMOTE_CREDENTIALS_KEY 无钥 fail-fast 恢复链**、web_sessions 不入备份（恢复后重登）、停机强一致可选操作、运维排程建议、互斥语义 |
| `docs/user/faq.md` | 新增（README 既定篇目落地） | 90 | FAQ：401/403/404/409/413 信封解读总表（409 pattern/413 quota/409 组引用/401 session/403 CSRF 全覆盖）+ **高 QPS 用 Access Token（O-MEM1：argon2id 每请求 64MB 瞬态内存）** + **docker login 走 /v2/token（E4：cookie Path=/binflow 不达根级 /v2）** + M4 有意不兼容清单（组无 admin 位/SE-09 v2 permissions/SR-04 搜索族/GE-04 prune/GE-09 REST export/Q6 同步 GC）+ Artifactory 迁移对照表（README 承诺的悬空链接收口）+ 排障信息收集 |
| `docs/user/README.md` | 增补 | — | 导航补「Web 控制台」分组与三篇 admin 新篇目、FAQ 行更新 |

## 二、验证（AC ③）

全部文档命令在本机 scratch 实例（commit `7593d8e` 工作树、`make` 产物二进制 19.45MB 含控制台 embed，端口 8907/8908）上抽样复跑，退出码 0（错误码腿为文档明示的预期行为）。T-103（commit 3c3cd36）/T-105（62d92f1）验收环境已按其报告清理章节销毁，本文以同源序列在本机复现为等效验证。实测覆盖：

- **会话全周期**：/binflow/ 301→/binflow/ui/、登录 200+Set-Cookie（HttpOnly/Path=/binflow/SameSite=Lax/Max-Age=86400）、whoami、登出 204、重放 401、cookie+跨站 Origin PUT 403
- **保留字六拒**：ui/docs/console/api/v2/assets 建仓全 400
- **pattern 409 双态**（含实测完整文案 `the path does not match includesPattern '**/*.jar' (excludesPattern 'secret/**').` / `the path matches excludesPattern ...`）+ 下载 404
- **配额链**：413 双值文案逐字、被拒路径 404、usage `{repo,usedBytes,quotaBytes}`、临界幂等重传 201、删后回落再传 201、quotaBytes:-1 400
- **groups 链**：建组 201、jane 入组、target（principals.groups 动作数组形态——首试按对象形态 400 后以 internal/httpapi/permissions.go 契约修正）、继承（put 201/get 200/del 403/越界 403）、并集（user r+d ∪ group w 三动作全通）、`groups:[]` 即时 403、?permissions 字母集合形态、删被引用组 409 列 target 名、组名/保留字 400
- **审计/搜索**：action 过滤、quota.exceeded detail 五元组、name/checksum 搜索（同 blob 双路径 results=2）、缺参 400、未实现族四探针 404
- **GC**：唯一内容孤儿 dry-run `{"candidateCount":1,"candidateBytes":24,"deletedCount":0}` → apply deleted=1 → 复扫 0 → 被引用同 blob 幸存；缺省 grace 遮蔽新鲜孤儿；graceHours:-1 400；CLI gc dry-run；export 持锁期 REST gc **409（含 holder pid/op 文案）**
- **备份往返**：在线 export exit 0（layout blobs/+metadata.db+manifest.json；目录 0700、文件 0600；manifest formatVersion/binflowVersion/blobCount/totalBytes/metadata.sha256）、import --verify full exit 0（rehashed 2/2）、恢复实例制品/quota 配置与用量/审计全保真、mtime 逐 blob 全等、非空目标 exit 1、--tar 显式报未实现
- **token**：admin 签发（可为他人指名）、非 admin 自签 403（D3 定案——FAQ 示例据此修正）、Bearer 可用

**如实性修正两处**（写作中发现与 QA 报告引用不一致，以实测为准）：① 409 pattern 文案实际含 `the path ...` 前缀（T-103 引用为节选）；② token 签发是 admin-only（非 admin 403），admin 可 `username=` 指名替目标用户签发。

## 三、遗留移交清单（conductor 转 owner，本票未动 docs/design）

| # | 项 | 归属 | 内容 |
|---|---|---|---|
| H-1 | architecture.md §7.1 `?permissions` 行未更新 | Architect | T-115 遗留 1：草案②未动，依赖 B1 方向翻转落定。T-113 已定案正确形态（key=主体名、value=r/w/d 字母集合；非 local → 400 `This method can only be invoked on local/cached repositories.`），§7.1 行仍为旧「r/w/d 位」方向，待回写（本文档已按 T-113 形态书写） |
| H-2 | architecture.md §7.1 缺 changePassword 别名路由行 | Architect | T-115 遗留 2：`POST /api/security/users/authorization/changePassword`（router.go 已实现）路由表未列，先于 T-115 的缺口 |
| H-3 | console-ux §9 R10 行与 E4 相悖 | UX-designer | R10 以「docker tags」为 session 等价性例——cookie `Path=/binflow` 结构性不含根级 `/v2`（T-103 E4：强制携带 Cookie 则 200，机制等价但默认不可达）；建议例改 npm packument/pypi simple（T-103 §7 已建议，控制台 docker 取数路径需知会） |

## 四、边界与自律

- BOARD.md 只读；docs/design 零改动（H-1~H-3 仅列清单）；scratch 实例与 /tmp/t107 已清理（端口零监听、无凭据入提交文件）。
- frontmatter 仅 Docusaurus 兼容子集（title/sidebar_position，ADR-0011）；相对链接全部指向实存文件；术语沿用 Artifactory 命名（repo key / deployment / permission target / rclass）。
- 待确认项（写作中如实标注，未编造）：token 签发不落审计（D-104-2，文中标「已知缺口」）；docker/maven 配额先落残留（D-1/D-2，文中标 P2 观察边界）；未认证 search 403 vs /api 家族 401 姿态不统一（T-103 O-3/E2，文中一句带过标「已登记勘误」）；T-106 compose 烟测报告撰写时未归档（console.md 的 compose 链验证依据为 T-104/T-105 本机二进制 + QA 序列，未引用不存在的报告）。
