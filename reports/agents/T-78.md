# T-78 — M3 PRD v1.2：R1 凭据回写（ADR-0012 → Q1 关闭） `role:product-manager`

- 日期：2026-08-19
- 输入：ADR-0012 决策 4（凭据 = B：AES-256-GCM + env 主键）；tech-lead T-61.md R1 转交（最急，赶 T-66 派发）；R3 的 C2 半定案顺手项
- 产出：`docs/prd/milestone-3.md` v1.1 → **v1.2**（只改该文件，边界遵守）
- 状态：done（待 conductor 核验）

## 落点清单（7 处）

1. **状态头** → v1.2（摘要 + v1.1 历史保留）。
2. **§0 修订记录** 追加 v1.2 行（全量对照）。
3. **§2.2 Non-goals**「remote 高级认证」行的「凭据存储形态见开放问题 Q1」→「凭据静态加密已定案（ADR-0012 决策 4），原 Q1 已关闭」。
4. **FR-15 remote 字段** `username/password`：存储形态从「见 Q1」改为定案全文（AES-256-GCM、随机 12B nonce、`enc:v1:<base64(nonce+ciphertext)>`、env `BINFLOW_REMOTE_CREDENTIALS_KEY`（base64 32B）不入 YAML/不落盘、有行无钥 fail-fast、轮换不做）；**新增 FR-15-AC9（P0）**四断言：DB grep 密文前缀/无明文、缺 env 重启 fail-fast（退出码非 0 + 日志指明）、重设 env 后 Basic 链仍通、存量明文 003 迁移后明文消失——均可被 QA 直接执行。
5. **FR-20-AC10** 措辞统一（注明凭据走 ADR-0012 加密链、env 键在场）。
6. **NFR-S14** 重写为六点（静态加密形态/密钥注入与永不回显/fail-fast/password 掩码/日志脱敏/审计/不轮换），验收列指向 FR-15-AC9。
7. **§7** Q1 行改「已定案（v1.2，ADR-0012 决策 4）」（v1.0 暂行明文方案标注作废）；§7 头部状态注与 **§9 DoD-3** 同步（待拍板仅余 Q2）。

顺手项（R3）：**§5.5 校准表下新增 C2 补注一行**——「码值 400 维持暂行（规格只见模型未见码值），单点改动面（一处常量 + FR-16-AC8 + M20 期望值），后续定案单独回写」；C2 定案本体未动。

## 自检证据

- `rg 'BINFLOW_SECRET_KEY|见开放问题 Q1|存储形态见开放问题'` → 0 命中（v1.0 暂行方案的 env 键名与「待 Q1」指引零残留）。
- `BINFLOW_REMOTE_CREDENTIALS_KEY` / `enc:v1:` 落点核对：状态头、§0 v1.2 行、FR-15 字段、FR-15-AC9、FR-20-AC10、NFR-S14、§7 Q1 已决行——七处口径一致（base64 32B / 随机 12B nonce / fail-fast / 003 迁移四要素齐全）。
- 与 ADR-0012 决策 4 逐词对照：AES-256-GCM ✓、`enc:v1:` 前缀 ✓、env 键名 ✓、有行无钥 fail-fast ✓、存量明文 003 一次性加密（需密钥在场）✓、轮换不做 ✓。

## 遗留 / 转交（范围外冲突，需 conductor 裁决派票，本票未动）

对读 ADR-0012/0013 全文时发现与 PRD v1.1 尚存三处口径张力（T-78 范围仅凭据面，未擅自改动）：

1. **上游故障未缓存路径的默认码**：ADR-0012 决策 2 = 无副本 → **502**（stale-while-error + Warning 头）；PRD v1.1 = 默认 **404**（assumed-offline，repo-semantics §7.6 高置信度）+ `hardFail:true` → 502。两者是「Artifactory 对齐（PRD）」vs「网关语义（ADR）」的产品取向冲突——需 PM/architect 联合定案后统一（另：ADR 的 `Warning: 111` 头 vs PRD 的 `X-Binflow-Upstream-Error` 头、ADR 的 `X-BinFlow-Cache` 头 PRD 未收录）。
2. **virtual 解析顺序**：ADR-0013 = **local-first 固定 + 逐成员 priority 留缝不实现**；PRD v1.1 = priorityResolution 两桶（依 T-59 规格 repo-semantics §8.1 四桶定案）。ADR-0013 自带「规格产出后校准，冲突时新 ADR」触发条款——T-59 规格已落地，按条款应触发 architect 新 ADR 或修订，PRD 侧待其收敛后再对齐（FR-21/RE-02/RE-07/FR-15/FR-21-AC9 受牵连）。同理 ADR-0013 决策「M3 virtual 只读（选项 A）」与 PRD FR-21-AC4 写路由 P1 的关系——ADR 已预留「PM 联动：PRD 若立 FR 再议」，尚未议。
3. **次要参数差异**（可并入上两条一并收口）：SSRF 重定向跳数（PRD 5 跳 / ADR 3 跳）、响应体上限（PRD 缓冲型 64MB / ADR 流式 8GB——语义可共存但需写清分工）、超时默认（PRD socketTimeoutSecs 15s / ADR 连接 10s+响应头 30s）、缓存再验证（PRD P0 到期重取+P1 协商 / ADR 决策 1 直接定 TTL+条件再验证，且 ADR 有 metadata/artifact 分流与 003 新表 remote_cache——PRD 未收录）。

建议：凭据面（本票）先行满足 T-66 派发；上述三项由 conductor 汇给 architect + PM 开一张联合对齐票（ADR 增补或 PRD v1.3），赶在 remote/virtual 实现票（T-66 及以后）派发前收口。
