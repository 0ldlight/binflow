Ticket:       LOOP 022 轨道 L022-3（tech-writer）——文档后续两件：whats-new 破坏性公告 + openapi 生成器条目
Role:          tech-writer
Area:          docs/user/（whats-new / api-reference / README）+ fern/pages/whats-new.mdx + tools/openapi-spec/（票面显式授权）+ fern/openapi/binflow.json（生成产物）
Input:         派发令（两任务串行）；口径源=docs/compatibility/matrix.yaml（D01-R05 list 七参族 compatible、D02-R03/R04 PUT create-only 与 POST merge compatible）+
              internal/httpapi/repositories.go（handleRepoPut/handleRepoPost）与 internal/httpapi/storage.go（parseListOptions/listQueryParams）；
              协议批次素材=README 同步提交 d1a15995（fern 七页）+ fern/pages/integrations/{maven,pypi,npm,conan}.mdx 既有内容
Changes:       ① whats-new 两树新增两节：「破坏性变更：仓库配置动词语义收紧」（PUT create-only 400 逐字文案 + POST 合并三列 + repeat-PUT 影响 +
              PUT→POST 迁移 curl 对照 + upsert 两步法）与「协议语义对齐批次：客户端可见面」（六域 npm/PyPI/Conan/Docker/Go/Helm + Maven +
              存储管理面七条）；intro/兼容性影响/下一步联动改写（「API 面纯增量」收窄为控制台批次口径，新批唯一破坏臂点名）。
              ② docs/user/api-reference.md 三行陈旧语义翻正（PUT 行「替换既有仓」→ create-only 400；POST 行→合并三列；?list 行→七参族）。
              ③ docs/user/README.md 导航行补公告新主题。
              ④ 生成器：d_content.py storageList 补七参（deep/depth/listFolders/includeRootPath/mdTimestamps/statsTimestamps/includePropertiesMd5，
              各自 integer schema + default 0 + 语义描述）+ 200 响应 FileList vendor CT 与 files[] 条目 schema + 400 `For input string` 示例；
              d_search.py repoPut 翻为 create-only（400 already-exists 逐字示例；200 纯文本创建文案——实测为 200 非 201，原 spec 的 201 臂删除）、
              repoPost 翻为 merge 合并三列（description 层保留/覆写语义 + 404 + 400 body 非法）。
Files:         docs/user/whats-new.md、docs/user/api-reference.md、docs/user/README.md、fern/pages/whats-new.mdx、
              tools/openapi-spec/d_content.py、tools/openapi-spec/d_search.py、fern/openapi/binflow.json（生成）、reports/agents/T-L022-3.md
Tests:         实跑（UAT 实例 127.0.0.1:8083，容器 binflow-ga，凭据经 .env.uat 注入未入任何文档）：
              PUT 建仓 200 `Successfully created repository 'docs-verify-tmp'`；重复 PUT 400 逐字
              `error when validating repository name: docs-verify-tmp : Repository key already exists`；无 rclass PUT 400（packageType 空槽拒绝，
              先于 key-exists——handler 注释所引「Missing repository type」措辞未在现码出现，按实测文案描述不引字面）；
              POST 省略 description→保留 'first'；显式值→'changed'；null→''；未知 key→404；POST 成功文案 `Repository docs-verify-tmp update successfully.`；
              ?list 平铺/deep=1/deep=2（仍平铺）/deep=1&depth=1（限深）/listFolders=1/includeRootPath=1（`/` 根行）/mdTimestamps=1
              （properties 键）/statsTimestamps=1（下载后 artifactory.stats 键）/includePropertiesMd5=1（propertiesMd5）/deep=abc→400
              `For input string: "abc"`；CT=application/vnd.org.jfrog.artifactory.storage.FileList+json。验证后 DELETE ?deleteContent=true 清理（200，复查 404）。
              生成器：python3 tools/openapi-spec/build_spec.py → 112 path items / 158 operations / 20 tags / 54 schemas / 313213 bytes，self-check clean；
              python3 -m json.tool 通过；redocly lint（npx @redocly/cli）「valid」，警告 71→70（storageList 补 400 消一告警）。
Commands:      curl -u admin:$PW -X PUT/POST/DELETE "$BASE/binflow/api/repositories/docs-verify-tmp" …（动词全矩阵）；
              curl -u admin:$PW "$BASE/api/storage/docs-verify-tmp?list[&deep=1&depth=1&listFolders=1&includeRootPath=1&mdTimestamps=1&statsTimestamps=1&includePropertiesMd5=1|&deep=abc]"；
              python3 tools/openapi-spec/build_spec.py；python3 -m json.tool fern/openapi/binflow.json；
              npx -y @redocly/cli@latest lint fern/openapi/binflow.json --config tools/openapi-spec/redocly.yaml（含 HEAD 基线对照）
Outputs:       docs/user/whats-new.md（公告主文）· fern/pages/whats-new.mdx（精简镜像）· fern/openapi/binflow.json（再生成）
Compatibility: 与 matrix.yaml 逐行核对一致：D01-R05（?list 七参族 compatible——参数名/布尔臂==1/depth 修饰符/int32 界/For input string 文案全对齐）、
              D02-R03（PUT-on-existing=400 create-only compatible——逐字文案+零副作用）、D02-R04（POST 更新 compatible——三列矩阵含 0-as-absent 双面收口、
              数组空值保留/对象 {} 复位、description 键在场判定）。已知差异面零新增；公告如实标注 conan 域为双端对照验证（本地 license 不能跑）
Security:      文档示例凭据一律 admin:*** 占位；UAT 真实口令（.env.uat）未进任何文档/日志正文（本日志仅记「经 .env.uat 注入」）；
              实跑产物仓库 docs-verify-tmp 已删净（复查 404）；sensitive 扫描（口令字面 grep docs/user fern tools）零命中
Performance:   不适用
Risks:         ① handler 注释引「Missing repository type」措辞与现行为（packageType 空槽 400）不符——spec 按实测行为描述，未引该字面（注释陈旧属工程面，
              已在此留痕）；② whats-new 协议批次为摘要面，逐字段细则仍以各接入指南为准（链接已给）；③ redocly 70 条警告为存量风格项
              （operation-4xx-response 等），非本票引入
Blockers:      无
Next:          api-reference.md 的 ?list 与 PUT/POST 行已随本票翻正；如后续为 list 七参族补逐参 curl 示例章节，建议落 docs/user/api-reference.md
              或 generic 接入指南（fern 侧 generic.mdx 已有七参段——docs/user/integrations/generic.md 未同步，候下票镜像）
