# T-L013-4+5 — differential-qa：maven 元数据计算取证 + R-15 预授权探针 + packument 差分腿

Ticket:        L013-4+5 合并票（differential-qa-engineer；REST 探针轻负载与爬取并行；串行命令）
Role:          differential-qa-engineer
Area:          tools/difftest/l0134/ + reports/compatibility/（wire: l0134-wire/{a,b}/{r15,maven,npm}）
Input:         派单（D12-R08 ◐ 锚 + maven-npm-pypi.md §1 规格中厚；pending-rulings v2.1 §2 R-15 四臂 + §4-4 预授权式；known-divergence `npm/packument-latest-recompute`）；自读 rest-api.md §1.1/§1.5/§2 行 85、L003-3.md §2 重建指南、l0072 离线建像配方、l0121 npm 探针先例
Changes:       ① UAT 重建至 bebd92b1 树（offline 配方，见 Tests）② 四个可复跑脚本：r15-probe / maven-evidence（403 退避+2s 间隔）/ mvn-deploy-leg / packument-probe ③ 两份证据/结论件
Files:         tools/difftest/l0134/{r15-probe.sh, maven-evidence.sh, mvn-deploy-leg.sh, packument-probe.sh, build-image-offline.sh}；reports/compatibility/{L013-maven-evidence.md, L013-r15-packument-probes.md, l0134-wire/**}；deploy/compose/.env.uat（版本键→uat-l0134-bebd92b1，gitignored）
Tests:         maven 矩阵 18 臂：一致 11 / 差异 7（BUG 候选 2 根因、UNKNOWN 3）；R-15c 五臂双端全落；R-15a 五臂双端全落；packument 七步双端全落；mvn CLI 双端 exit=0。UAT 重建：version=uat-l0134-bebd92b1、healthz 200；`git diff bebd92b1..cc55428d -- cmd/ internal/` = 空（revision 标签 cc55428d 为 goreleaser 戳，Go 树等价）
Commands:      见上述脚本（入口即复跑面）；UAT 重建 = make console/docs/release → build-release.sh（死于 DaoCloud EOF@phase3）→ build-image-offline.sh（本地 alpine:3.24 digest 钉基）→ compose up -d --force-recreate
Outputs:       reports/compatibility/L013-maven-evidence.md（矩阵 18 臂+根因聚类 5 条）；L013-r15-packument-probes.md（四象限判定+归属面定案+packument BUG 素材）；提金候选：A1-A5/C2/D1-D4/B3/E1（等价面，maven 契约断言素材）
Compatibility: 差异分类建议：BUG 候选 2（unique snapshot 服务端改写缺失〔A6/B1/B2 同根〕；X-Checksum 头物化旁车存储项〔E2〕）+ packument latest 读时重算缺失（BUG，两读面矛盾）+ `?project=` 忽略（R-15c 定案=案乙小实现）；UNKNOWN 3（metadata XML 形态三偏差、手 PUT metadata 即时重算、并行竞态参照自丢 metadata）；无上轮差分基线（本域首轮 wire 取证）→ 回归对照=新增 7+3
Security:      凭据全程 env（ARTI_AUTH/BF_AUTH/L0134_MVN_PASS=${env} 插值；npmrc 用后即删，grep 复核 /tmp 零残留）；R-15c 顺带证实参照 admin 锁可被并行轨道误伤（非本票面）
Performance:   全矩阵串行单连接；参照侧 403 退避最长 9s×3；无超时 case；UAT 重建 console+docs+goreleaser ~18min（离线组装 ~1min）
Risks:         ① 参照 :8082 窗口内被外部 docker stop ×2（22:18:47Z/22:32:03Z，exit 0）——并行轨道生命周期操作，本票两次重启各耗 ~2min；复跑者需预期共享实例竞争 ② 参照 recurrent-failure 拦截器 + admin 登录锁在并行 audit 轨道（audit-probe-user 404 流）叠加下误伤，脚本已带退避但重跑可能再遇 ③ R-15a known-project 臂不可测（参照无项目域）④ C3 参照侧反常（竞态丢 metadata）登 UNKNOWN，勿当对齐目标
Blockers:      无（两项断供均已自愈：DaoCloud 镜像 EOF→离线配方；参照停机→重启）
Next:          ① conductor/PM：R-15c/d 按案乙落格开小实现票；R-15a 呈裁（file 面静默吞参使现案 B 不成立，需改双面登记或实现）② compatibility-engineer：翻 npm/packument-latest-recompute 分类 UNKNOWN→BUG（TestDistTagRecomputeIsReadTimeOnly 声明与 wire 不符，疑似账实不符/回归——§12 上报生效）；复核 matrix D01-R03 行注记 501→实际 404 漂移 ③ maven 元数据计算票实现段按 L013-maven-evidence.md §1 两 BUG 根因排期，§2 等价面直接入契约断言
